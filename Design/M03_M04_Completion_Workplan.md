# M3 / M4 completion workplan

Starting point: `4522a97c8`, 10 September 2026. This continues the delivered missions and the readiness audit; it does not replace the original production plans. Editor QA only. Review captures and recordings stay outside Design and Git.

## Acceptance and execution order

1. **Movement and animation.** Reproduce moving infantry/specialist animation at tactical and close camera distances. Verify actual movement, selected GPU clip, visible renderer clip and changing rendered pose. Fix stationary fallback while moving and any actor/LOD propagation issue found. Exercise move, chase, fire, stop, boarding, disembarkation and death.
2. **Camera and action readability.** Check both opening tours against the actual subjects, their RTS return, active warning jumps, user pan/follow behavior, and endings. Frame live actors/action instead of fixed empty-ground coordinates. Preserve player control outside intentional camera sequences and verify skip/reduced-motion/teardown.
3. **Mission completeness.** Repair Campaign objectives/star labels, ensure guidance and all class entries remain bilingual, verify reward/result/next-mission flow, and tune the M4 pursuit/rescue timing and M3 idle pressure against repeatable play strategies. Complete supported voice/audio scope without silently removing a requirement.
4. **Architecture and regression failures.** Work through every failure in the nine-fixture, 137-test architecture audit and the broader mission/legacy regression runners. Fix source ownership, dependencies, lifecycle and allocation problems. Correct a stale test only when its replacement asserts the intended contract and document why; do not suppress failures, relax budgets, or add blanket exceptions.
5. **Integrated Editor QA.** Run M3 and M4 success, failure, retry/replay, guidance and camera journeys against the final generated assets, both languages and aspect ratios. Include real movement/rendered animation, actual screen visibility, saved rewards, clean actor teardown, and timing/allocation measurements. Record exact run markers and source hashes. Human enjoyment is not a unit-test result; document concrete play observations and remaining limitations honestly.
6. **Delivery.** Reconcile acceptance checklists, commit and push the verified code/assets and text reports. Keep evidence images and recordings local.

## Progress

- [x] Movement/rendered animation acceptance
- [x] Camera subject/action acceptance
- [ ] Mission UI, guidance, pacing and audio scope
- [x] Architecture checks: 139/139 passed
- [ ] All required regression gates green (M4 voices remain missing)
- [ ] Final integrated Editor QA and allocation checks
- [ ] Documentation reconciled, committed and pushed

The prior readiness report remains historical evidence until superseded by recorded passing runs. No completion box is checked merely because implementation exists.

## Current verification notes

- Movement clip resolution and propagation: ten focused checks passed in `/private/tmp/warline-completion-animation-01.log` (wrapper exit 0). Moving combat falls back to locomotion when blended clips are absent; matching visual roots no longer skip stale child clip indices. Rendered motion still requires live acceptance.
- M4 observation without the old camera-repositioning vehicle diagnostic passed the real rescue in `/private/tmp/warline-completion-m04-motion-01.log` (wrapper exit 0). It exposed a close starting-area view while active rescuers leave the frame. GPU animation owners advanced their run playback; the next observation includes the shader pixel properties on visible mesh entities.
- The new mission overview framing and extraction focus checks passed in `/private/tmp/warline-completion-camera-01.log` (wrapper exit 0): both missions at 16:9/20:9; moving specialists, boarded specialists, current APC/helicopter location, and explicit departure destination. M4's Team action previously pointed at the original pickup anchor even after the team moved; the new implementation resolves current actors/transport positions.

- Live M4 motion audit 02 passed the real rescue, but its camera remained incorrect: a pending generic match-intro settle overwrote the authored pose. All 1,532 sampled visible-mesh records for moving unboarded characters used Run, matched the animator's frame data, and included 378 distinct shader frame values. This confirms GPU playback, not artistic pose quality. Captures/data: `/private/tmp/warline-completion-motion/20260910-073618`.
- Camera tests now exercise `RtsCameraRequestSystem`, pending intro/smooth state, and map boundaries. Run 02 correctly failed for the occluded landing zone. Run 03 passed after explicit poses cancel pending generic intro state and camera-only map padding accommodates HUD occlusion. Playable bounds, minimap extent and physical map remain unchanged.
- `/private/tmp/warline-completion-focused-01.log` (wrapper exit 0) applied and validated both camera-bound assets. Focused tests: 26 passed, 1 failed (result prefab localization keys and U+066C font coverage). Architecture: 113 passed, 25 failed across nine fixtures (138 tests after adding parser regression coverage). The source-size guard failures are fixed without increasing ceilings; the aggregate is still FAILED.
- Source guard corrections retain positive checks: distinguish an actual concrete view type from an interface-backed property name; distinguish a static collection field from a method parameter; inspect unmanaged mission constants across the actual owner's partial files. Locale lookups now publish immutable snapshots rather than clearing shared dictionaries in place.

- Live M4 motion run 03 is a failed launch: map content-pack hashes were not refreshed with the camera-bound metadata changes. No gameplay sample was produced. The repair now refreshes and validates linked catalog records, and probes preflight the catalog and treat bootstrap errors as failures. Pending source fixes are being made during this already failed run; it is not used as gameplay evidence.
- Remaining architecture work includes four intentionally managed UI bridges (localized radio/campaign strings and Unity pause state), now explicitly classified. Three pure warning/Ping/interaction systems are being made Burst-compatible; the camera-pose capture uses unmanaged quaternion direction math. New declarations will be included in the generated inventory.
- The result builder was resetting all existing stable keys to empty strings. It now preserves them, with a targeted saved-prefab repair using existing catalog translations. Persian font repair appends missing glyphs and preserves existing atlas glyphs/UVs. These changes still await validation.

- 2026-09-10: Focused camera/animation/localization run 02: **27 passed**, architecture **122 passed / 17 failed**. Burst query construction subsequently corrected; the run is not an all-green architecture result. Log: `/private/tmp/warline-completion-focused-02.log`.
- M4 motion run 04: wrapper exit 0 and complete mission pass, no C# or Burst errors. Real APC movement, boarding/unloading, helicopter boarding, 20-second clearance, departure, bilingual debrief/results, three saved unit unlocks and Campaign return. Log: `/private/tmp/warline-completion-m04-motion-04.log`; normal-camera samples: `/private/tmp/warline-completion-motion/20260910-082947`. Visual review found the overview must also reserve the left selection panel; tightening that viewport contract before final QA.
- Shared Persian font coverage and all prefab localization bindings pass after a fresh Editor launch; the repair now avoids rewriting complete atlases and initializes FontEngine before adding missing glyphs.

## Shell source identity review — 2026-09-10

The frozen `UIShellContentView` fingerprint is refreshed for the already implemented M3/M4 guide, threat, result, and route integration and the explicit Build Drawer underlay binding. Its existing 951-line, 40,958-byte, 50-state-slot, and 14-domain-occurrence limits remain unchanged. Resource Exchange lifecycle still belongs only to `ResourceExchangeShellBinding`; the source retains every required boundary call and none of its forbidden direct Resource Exchange ownership. The historical binding source has been restored exactly; the footer Cancel forwards to its already owned Close button. Resource Exchange refresh now follows an explicit MainMenuPlayUI instance reference, and no global view stack remains. The deterministic visual preview is Editor-only.

The D-113 selection-panel growth exception is retired from the active manifest: extracting its transport presentation into the same component owner reduced the source below both historical ceilings. Its original decision remains in the audit history; no size ceiling is raised.

## 2026-09-10 completion pass — verified checkpoints

- `/private/tmp/warline-completion-architecture-05.log`: wrapper exit 0; camera/animation/localization **28 passed**; architecture **136 passed, 3 failed, 139 total**. The remaining failures were diagnostic-file size, a new presentation assembly's root namespace, and names on the rendering boundary. Those corrections are implemented, pending the complete Editor test run. No guard ceiling was increased.
- ECS system helper queries now iterate chunk views instead of copying entity/component arrays. Map vehicle ownership defers structural additions until iteration completes. Production scheduling still gathers ready producers before spawning. Rendering bounds are provided by a stateless rendering-owned implementation through a contract; follow-camera bounds and helicopter landing-clearance behavior are retained.
- Runtime UI diagnostics now use a composition-bound sink gated to Editor/development builds. Missing-reference errors remain observable. Scenario Lab UI resides in its own UI assembly. Reviewed instantiation classifications cover only owned profile copies and collider-disabled presentation objects.
- M3 `/private/tmp/warline-completion-m03-motion-02.log`: complete victory/debrief/bilingual result/reward/Campaign-return pass. In the corresponding motion recording, moving on-screen characters have **804 detailed/mid animated samples**, with one transient far-tier sample during refresh; 103 other far-tier movement samples are outside the camera. There are **1,106 distinct visible shader-frame values**. Captures remain under `/private/tmp/warline-completion-motion/20260910-091320`.
- M4 `/private/tmp/warline-completion-m04-motion-07.log`: wrapper exit 0 and full rescue pass at 74.144 simulated seconds, including real APC boarding/driving/unloading, helicopter boarding, 20-second clearance, departure, debrief, bilingual results, saved unlocks, and Campaign return. The current M4 ARIA text is visibly rendered in Farsi. Blank intermediate captures came from the inherited two-second instruction delay, not missing fonts/text. Twelve-step mission guidance now changes immediately and cannot be suppressed by legacy M1/M2 completion acknowledgements; a regression traverses all 12 steps and verifies cinematic hide/resume.
- The complete Unity EditMode suite is being run via the GUI-licensing wrapper with NUnit XML at `/private/tmp/warline-completion-editmode-all-01.xml`. No final all-tests-pass claim is made before that run and corrective reruns complete.


## Full-suite checkpoint and approved audio, 10 September

- The complete Editor run produced **4,358 tests: 4,162 passed, 194 failed, 2 skipped** (`/private/tmp/warline-completion-editmode-all-01.xml`). This is a real failing checkpoint, not a completed gate. The failure inventory is retained outside the repository at `/private/tmp/warline-editmode-failures-01.json`.
- The user approved the prepared 23 M3 bilingual script pairs. All **46 local clips** were generated, their hashes and caption text recorded in the voice manifest, then imported through the required wrapper. `/private/tmp/warline-completion-m03-voice-import-01.log` exited 0 with both `[M03VoiceImports] result=Passed` and `[M03RadarWarningNarrativeBuilder] result=Passed`. Narrative deadlines now include the longer of the two voice clips.
- Corrected explicit World diagnostics in AI fixtures, initialized missing native job containers, restored NUnit fixture setup for the settlement tests, and replaced EditMode `SendMessage` lifecycle broadcasts with invocation on the intended component. Prepared Persian glyphs survive legacy first-launch rebuilds.
- The current targeted regression run is `/private/tmp/warline-completion-regressions-02.xml`; its outcome remains pending. Final playthroughs, full-suite rerun, and delivery are still required.
- M4's separate 19-pair / 38-clip payload is prepared at `Design/AgentReports/M04Airlift/voice_payload_review.json`. Its external generation approval is pending; existing M3 approval is not treated as permission for additional scripts. The proposed Laila performance uses the paid account's licensed Sarah preset, distinct from Dalia, Samira and ARIA.

## Regression and live-combat repair checkpoint, 10 September

- Focused Editor runs: `regressions-05` **302/316 passed**; `regressions-06` **254/262 passed**. The latter includes passing synthetic static-map fixtures, current EntityScene ownership, resource-header icons, vehicle adornments, transport drawer, and threat localization. These do not replace the final complete suite.
- Restored seven systems whose cached ECB singleton queries omitted `EntityQueryOptions.IncludeSystems`. The installed Entities package stores these singleton components on system entities. Health bars, destroyed-unit visuals, attack-state initialization and target synchronization must actually update; multiple previously failing behavior checks now pass.
- Corrected `UiLocaleCodes.Persian` to the catalog's canonical `fa-IR`; first-launch language selection and narrative settings now retain Farsi.
- M4's real player CAMERA-button journey successfully frames the moving APC and airborne helicopter in the usable HUD viewport. Captures remain in `/private/tmp/warline-m04-readiness`. The same run failed its stricter idle-combat acceptance: pursuers reached the landing zone with unchanged health and no engagement.
- A dedicated idle diagnostic traced `CanAttack=1`, `AutoEngage=0` across all combatants. M4's required ending cinematic was incorrectly activating M1's tutorial combat hold. The hold is now gated by the explicit M1 tutorial-finale policy; a regression covers M3/M4 exclusion. Pursuit route orders also preserve autonomous target acquisition instead of issuing manual movement that suppresses combat. Both changes await the corrected live replay.
- Restored AI breach planning for unit targets inside walled bases. The optimization that considered only building targets bypassed the existing gate/approach path. The bounded snapshot now remains available whenever combat buildings exist in the owning World.
- Historical static-presentation tests now use isolated, structurally complete scene/manifest fixtures. Current production ownership tests assert the accepted EntityScene cutover. Retired phase-zero probes are required to reject missing or changed historical inputs without publishing success; their archived reports are not represented as current acceptance.
- Localization source tests now inspect all partial files belonging to the original consumers, retaining 91 resolver calls and 86 literal key/fallback pairs. Literal hashing is sorted by owning consumer, so extracting a partial file does not falsely alter the contract. The new defense-warning cache is explicitly included in the zero-allocation/rebind matrix.

## Combat, resources and portable evidence — 10 September

- `regressions-07`: **278/282 passed**. `regressions-08`: **66/69 passed**, including all four new AI reserve/refund/insufficient-resource cases. The remaining failures were two source-size guards and an uninitialized test World for the footer Build interaction; the corrections are being validated without raising a ceiling.
- M4 idle contact now produces a real combat defeat at **156.682 simulated seconds**, with one civilian loss and no extraction timeout (`/private/tmp/warline-completion-m04-idle-combat-05.log`). The same Editor invocation initially encountered a test compile error, which was fixed before Play Mode; a clean final rescue/recovery run is still required.
- AI unit production was reading a Materials amount through the historical `Price` read-model field and deducting it as Credits. The projection now carries separate Credits, and the AI uses the shared dual-resource transaction to reserve both amounts, settle once, and restore both on rejected production. Pending rollback receipts remain outstanding until restoration succeeds. Production-request handling is an owned partial of the existing AI system.
- Python tooling run 08: **435 tests, 433 passed, one failed, one error**. The failure was an outdated expected package-count error string; the error is an unavailable historical validation log. The package graph legitimately contains the already committed Unity Pipeline and RTLTMPro packages. No package was removed to satisfy an obsolete count.
- Archived evidence retains its original hashes. Hash resolution now accepts the exact LF and CRLF checkout forms of reachable immutable Git blobs, fixing Windows-capture comparisons on macOS. Owner-map assertions verify their recorded baseline; current ownership remains governed by the separately executed C# architecture guards. The Python generic lifecycle check now counts the same anchor symbols as its canonical C# counterpart.
- Final screenshots and recordings are directed to `/private/tmp` by default. No evidence images are included under Design.

## Committed implementation and architecture checkpoint

- Implementation checkpoint: `eba47c95f5a9309b2f9124b0594452fd2914fd1e`. Focused Editor run 09 passed **41/41**, including every source-growth test, AI production/rollback and the real footer Build interaction.
- Correctly terminated checkpoint run 11 (`/private/tmp/warline-completion-architecture-checkpoint-11.log`) exited **0**, with **17/17** source-growth checks, **1/1** exact authorization check, no C# compiler errors, and no missing prefab bindings (`updated=0`). APH-700 now records 23 assemblies and 132 first-party dependency edges.
- The current architecture log is compressed under `Design/AgentReports/ArchitectureMaturity/Logs/m03_m04_completion_source_growth_20260910.log.gz`. Its manifest pins the implementation commit, tree, validator and unchanged contract. The prior AMFR-017 metadata remains historical, with an explicit note that its ignored local logs were unavailable.
- Checkpoint invocation 10 omitted `-quit` and timed out despite its test markers; it is rejected as acceptance evidence. Full-suite invocation 02 returned without an XML while that Editor remained open and is also rejected. Invocation 03 is the actual complete-suite rerun.
- Complete Python run 11 passed **435/435** (`/private/tmp/warline-completion-python-tests-11.log`, 246.785 seconds). This includes the fresh compressed-log binding and all historical evidence, package, audio-import, source-ownership and wrapper-contract checks.

## Full suite and final gameplay rerun — 10 September

- Full Editor run 03 completed with **4,362 tests: 4,352 passed, 8 failed, 2 existing opt-in balance probes skipped** (`/private/tmp/warline-completion-editmode-all-03.xml`, wrapper exit 2). All nine core architecture fixtures passed. This remains a failing checkpoint.
- Targeted regression run 04 passed **63/63**, wrapper exit 0. Regenerated the 26 missing M3 ARIA event constants/hashes; the importer contract now recognizes the existing `Narrative/<mission>/Voice` directory as Voice. The six-speaker catalog validates Laila separately; the squad tray validates the actual gradient hit target and decorative frame behavior.
- M1/M2 scenario assets retain Unity's canonical serialized defaults and whitespace, restoring byte-stable regeneration without weakening the tests. Historical lifecycle ownership evidence remains unchanged; the two relocated Scenario Lab owners resolve by their preserved MonoScript GUID and current UI assembly.
- The only known outstanding full-suite failure is M4's required local narrative voices, awaiting the separate prepared-payload approval. M3's approved 46 clips are installed. Final M3/M4 Play Mode runs and performance observations are underway.

## Live M3 guidance checkpoint

- `warline-completion-m03-final-guidance-03.log` exited 0. Real Full Guidance reached Victory at **212.236 simulated seconds**, retained the full optional budget and both Ping charges, then completed the finale, three debrief panels, bilingual results, guide return, durable first-clear rewards, M4 unlock, and actual Campaign return with the old actors removed. All six briefing/debrief panels passed both locales and both aspects (24 large-caption captures).
- Motion observation (`/private/tmp/warline-completion-motion/20260910-123659`) recorded 1,090 samples and 499 visible moving-infantry observations: 486 Run at detail/mid tier, 11 RunAim, and two transient far-tier Run observations. All 1,988 visible mesh samples matched their resolved animation target; 497 distinct shader-frame values demonstrate advancing playback. No moving infantry sample used idle.
- Visual review still found an optional-lesson row extending into ARIA and a truncated main-convoy contact estimate. Authoring now moves the row into the clear center column and allows the warning text to fit within its fixed banner. These improvements require visual retest.
- Full Guidance skipped Move because the authored starting positions already met the spatial threshold. The mission still won, but this omitted teaching regrouping. Completion now also requires an accepted Move receipt after the current command baseline. A real ECS regression covers old/rejected receipts, accepted-but-travelling units, and completed arrival; the next live run must visit Move.

- Guidance rerun 04 won combat at **195.634 simulated seconds** and rendered all 24 comic variants, but correctly failed acceptance because ARIA remained on Move. The selection UI drains its transient Move result synchronously. The correction now observes retained `UnitMoveOrderResultElement` receipts, including their request kind, and accepts only issued grouped-manual orders for the current mission's friendly actors. Existing Stop receipt handling is retained. Run 04 visually confirms the warning's full contact estimate and the centered control row; it is not a guidance acceptance pass.

- Guidance/layout regression run 02: **29/30 passed**. The new bilingual 16:9/20:9 warning/ARIA non-overlap check passed, as did retained move-result completion. The sole failure was `UnitMoveOrderRequestSystem.cs` reaching 501 lines. Its pure receipt conversion now resides in `UnitMoveOrderRequestSystem.Results.cs`, keeping the same system owner and unchanged review threshold. The next full guidance replay is run 05.

- Final guidance run 05 exited **0**: real regrouping Move -> Hold -> Stop -> restored Hold, Victory at **199.692 simulated seconds**, all 24 comic variants, finale, debrief, four localized/aspect results, guide return, Campaign return, old actor disposal, durable rewards and M4 availability passed. The inspected Farsi 20:9 result shows three stars, seven defeated enemies, one lost rifle, zero civilian losses and all four reward lines with an unobstructed Continue button.

- M4 player-camera run 03 failed early because a fresh M3 guide rebuild cleared the shared prefab's M4 catalog. Canonical serialized-binding authoring now restores the extraction guide on every shared guide build. `warline-completion-guide-regeneration-01.xml` passed **50/50**, including the actual rebuild-order regression, move-order tests, both-language/aspect layout assertions and all 17 source-growth checks.
- Visual review also found legacy squad shortcuts labeled APC / Tank / Helicopter while their callbacks select combat vehicles / helicopters / jets. Runtime and prefab fallbacks now match those groups, with explicit central English/Farsi bindings for Vehicles / Helicopter / Jet.

- M4 player-camera run 04 exited **0** with fresh captures: both HUD languages/aspects, 12 topics and 57 classes per language, rescue/debrief/rewards/Campaign, actual Replay carrier-loss defeat, clean Retry, and normal-combat idle Defeat at **158.482 seconds**. Inspected APC road movement and helicopter departure show intact parts and subjects within the usable viewport.
- M3 defeat run 06 exited **0**: normal rifle retreat caused core-breach Defeat at **117.979 seconds**, localized failure and guide return passed, and actual Retry restored all 20 members, resources and both Ping charges.
- Final M3/M4 motion samples show Run/RunAim with advancing GPU frames. Every playback-index mismatch was a nonzero crossfade to a different target frame (112 M3 and 130 M4 mesh samples), matching the existing half-second transition implementation.

- Sensor-loss run 06 exited **0**, winning at **185.384 seconds** through normal rifle commands after the deliberate sensor-only fault. Scout warning remained visible, Ping unavailable and both charges retained.
- Visual inspection rejected defeat run 06 as complete presentation acceptance despite its interaction pass: the 20:9 result composition extended above/below the screen. `MainMenuV3SectionLayoutView` now detects root-canvas logical-size changes before rendering; fixed-size children may not receive a dimensions callback after CanvasScaler changes. Added a live composition-bounds assertion and a persistent-popup resize regression.
- Allocation attribution now traces the actual `MissionDefenseHudView.Refresh` marker and requires every owner to have samples; missing markers cannot silently appear as zero allocations.

- Result resize and source-growth regression run 01 passed **22/22** (wrapper exit 0). The live defeat replay now checks the entire result composition against actual screen bounds in both languages and both aspects; run 07 is underway.

- Defeat resize run 07 was rejected because its new Editor-update assertion used Scene-view `Screen` dimensions, not the rendering canvas viewport. The assertion now uses `rootCanvas.pixelRect`. Run 08 was interrupted by the observed UnityEditor.Connect token-exchange exception before gameplay acceptance. Both remain failed runs. A narrow exact-message/Editor-stack classifier preserves that exception in the log separately and still fails game exceptions/assertions; a regression covers those boundaries. Run 09 is the next clean replay.

- Defeat resize run 09 exited **0**. Both locales/aspects passed the rendering-canvas bounds checks and real Retry reset. The fresh Farsi 20:9 capture was inspected: header, failure explanation and Retry all fit. The observed Editor cloud-token exception was retained separately in this run; no game exception was suppressed.

- Save recovery run 06 correctly failed the newly strengthened presentation check: English `PROGRESS NOT SAVED` was ellipsized in the result title. The real failed atomic write left profile progress unchanged. Result title auto-sizing now permits the full headline within its authored panel; four English/Farsi/aspect cases cover the delivered prefab. The write/retry journey still requires rerun after this fix.

- Save headline / result resize / Editor-cloud classification / source-growth run 02 passed **27/27**, wrapper exit 0. Save recovery is rerunning as run 07 with all text and canvas bounds checked.

- Save recovery run 07 exited **0**. Victory at **192.821 seconds**, real atomic-write failure, unchanged saved profile, four bilingual/aspect error screens with complete headings, actual Retry Save, duplicate rejection, exactly one durable reward grant, debrief, localized victory and guide return all passed. The Farsi 20:9 error screen was visually inspected.
- M4 player-camera run 05 uses a fresh output folder and additionally checks/captures both victory and carrier-loss defeat at 20:9 in English and Farsi. The previous accepted run-04 captures are retained under `/private/tmp/warline-m04-readiness-final04`.

- Final M4 player-camera run 05 exited **0**: complete bilingual/aspect HUD and guide inventory, actual rescue, debrief, exactly-once rewards, Campaign return, Replay carrier-loss defeat, clean Retry and normal-combat idle Defeat at **164.011 seconds**. Victory and defeat now pass canvas bounds at both aspects in both languages. Both wide victory images and the wide Farsi defeat image were visually reviewed; all headers, objectives, rewards/reasons and Continue/Retry controls fit.

- Implementation and functional QA checkpoint **720b161af6fbcecebdc7eaa5de8353c81efc2a90** is pushed. Allocation run 06 exited **0** with valid positive controls and samples for all eleven owners: mission systems 48 B total (one Mono.JIT sample), HUD 25,930 B across 31 changed-text calls; unchanged HUD windows 0 B. This does not pass an inclusive Editor zero-allocation target.

- Normal M3 performance run 06 failed the unchanged 20 ms P95 budget (43.9–51.5 ms across phases). Attempt 07 improved to 16.8–29.1 ms P95, but reported `gameViewMaximized=false`; it does not prove focused-view performance. Run 08 applies maximization after the mission has entered Play Mode. Both completed combat. The .NET current-thread allocation counter failed its 8192-byte positive control; the older large-match benchmark now also rejects this unavailable counter instead of accepting a false zero. Four regression cases preserve the allocation and frame thresholds.

- Final source/authorization checkpoint 12 exited **0** at `ae88ed74af37f57197f0a1719f95be5e0be26bd6`: all **18** checks passed; regenerated inventory covers 23 first-party assemblies and 132 dependency edges. The previous compressed source-growth evidence remains immutable history. Final Python run 12 passed **435/435**, exit 0 (319.615 seconds).
- The verified maximized M3 run 08 completed combat but still failed frame P95 (23.9/36.5/50.5/46.2 ms by phase). Large-match run 12 exited **1**, recording 23.092 ms P95 and explicitly unavailable allocation data after the positive control failed. Fresh results are retained separately; historical accepted baseline files were restored byte-for-byte. These gates remain unaccepted.

- Full Editor run 04 exited **2**: **4,375 total, 4,370 passed, three failed, two existing opt-in probes skipped**. All **139** core architecture tests passed. Besides the known M4 voice requirement, it found an unlocalized runtime subtitle placeholder and a cache-dependent Edit Mode voice test.
- The subtitle now starts empty until the selected-unit presentation supplies its live value. The voice test pumps the actual Addressables request using the existing Edit Mode helper, retaining the real Persian clip/path assertion and the original timeout. Targeted run 05 exited **2**, **46/47 passed**: both new failures are repaired; the sole failure is still M4 `m04-brief-01` missing its required clip. Localization coverage passed for 38 prefabs / 1,199 bindings / 1,885 keys per language. Four allocation evidence checks passed. Shipping correction commit: `6d5e916be8a09316274a002c017072f1dfeac537`. Full and supplemental XML, repository results and exact log/artifact hashes are archived in `Design/AgentReports/M03M04Completion`.

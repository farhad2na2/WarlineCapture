# Operation 1 interface redesign

The SCN-11 Operations dashboard was never removed. `OperationsMissionScreenView.Install` disabled every child of the authored dashboard and displayed a plain mission briefing in its place. The mission HUD also placed its action panel across the center of the battlefield.

The revised menu keeps the district map, readiness rail, warnings, theater header, and command bar visible. Street Signals now appears as a compact card inside the existing theater briefing area. In the match, only small Mission Guide and ARIA Play/Stop buttons persist in the upper-right ARIA area. Mission objectives, status, remaining time, Save & Exit, and mission actions open as a right-edge popup. It starts closed, leaving the selected-unit area at upper left and the central battlefield clear. The separate ARIA tutorial portrait is hidden during Operations so that it does not cover the battlefield or duplicate ARIA controls.

The image-generated concepts used for layout direction are [Operations dashboard](dashboard-mission-mockup.png) and [match HUD with guide](hud-guide-popup-mockup.png). They are visual references; the shipped UI is native Unity UI and retains the actual O001 actions and localization.

The restored dashboard now binds credits, command, day, action points, district-wide security/trust/threat/heat/supply averages, and actual incidents to the saved Operations run. A fresh profile shows a new-city state instead of the prefab's fixed Day 12, 24,750 credits, 8,430 command, readiness percentages, and three warnings. The map and secondary command destinations remain the existing SCN-11 presentation; this work does not certify the wider Operations campaign.

Editor evidence on Unity 6000.5.2f1 through `Tools/CI/invoke_unity_macos.sh`:

- `/private/tmp/o001-dashboard-localization.log`: `[ConfiguredUiTables] result=Passed configEntries=405` for English/Farsi UI strings.
- `/private/tmp/o001-dashboard-live-smoke-3.log`: `[OperationsReconLaunchSmokeValidation] result=Passed` for dashboard hierarchy and fresh values, compact closed HUD, and Mission Guide open/close. Captures: `Build/EditorEvidence/O001OperationsDashboard.png`, `O001SharedWorldLaunch.png`, `O001MissionGuidePopup.png`.
- `/private/tmp/o001-dashboard-return-smoke-2.log`: `[OperationsReconLaunchSmokeValidation] result=Passed journey=deploy-withdraw-save-return-redeploy`, including saved Day 1/AP 2 reflected on the returned dashboard.
- `/private/tmp/o001-aria-evidence-wait.log`: `[OperationsReconLaunchSmokeValidation] result=Passed journey=aria-victory-return input=visible-touch seed=1102 ap=2`. ARIA completed three scans, waited for the full evidence recovery channel, extracted, saved Victory, and returned to Operations. The earlier failed ARIA runs identified and corrected a recovery retry loop: pressing Recover again reset its 15-second channel.
- `/private/tmp/o001-aria-integrated-foreground.log`: the same full ARIA Victory/return passed again on pushed commit `95d3c06b2`, after integration with the parallel Skirmish changes to shared Watch code. Two intervening reruns stopped with `stopReason=2` when the macOS Editor lost focus; they are not gameplay passes.
- `/private/tmp/o001-aria-seed2102-rerun.log` and `/private/tmp/o001-aria-seed3102.log`: full visible-input ARIA Regular Victory/return passed on both additional seeds, with three scans, evidence recovery, extraction, saved result and dashboard AP 2. The first seed-2102 attempt (`/private/tmp/o001-aria-seed2102.log`) failed before Play Mode because the test fixture used an invalid Operations command ID; the corrected fixture generated the passing rerun.
- `/private/tmp/o001-aria-seed4102-foreground.log`: another full visible-input ARIA Regular Victory/return passed with dashboard AP 2. Its first run (`/private/tmp/o001-aria-seed4102.log`) stopped at 145 mission seconds because the macOS Editor lost focus (`stopReason=2`); that run is not counted as gameplay evidence. Four Regular seeds (1102, 2102, 3102, 4102) have passing journeys; seed 5102 and Farsi seed 9102 remain untested.

The seed-1102 Editor journey is playable by manual English/Farsi input (recorded in the integration evidence) and by ARIA visible input. Wider seed/difficulty coverage, unfamiliar-player review, and the rest of the Operations campaign remain outside this evidence.

## 2026-09-24 continuation

The Farsi dashboard capture showed English district marker labels inside an otherwise localized screen. The O001 source table and shipping catalog now include five Persian district marker labels, and `OperationsDashboardScreenView` reads them at presentation time. The checked catalog import passed (`/private/tmp/o001-localization-import-20260924.log`, `configEntries=410`). The [verified Farsi dashboard](dashboard-farsi-verified-20260924.png) shows all five labels translated. The Farsi journey harness was also corrected: first-launch language had overridden its requested locale while its old pass marker still said `fa`. It now sets and checks the actual runtime locale at deployment and dashboard return. The initial seed-9102 log is diagnostic only; `/private/tmp/o001-aria-seed9102-fa-verified-20260924.log` passed with `language=fa-IR`.

The current working-tree Editor candidate, based on `f07b3bcf5baa32c2568df483a150a60cc856c67b` with uncommitted Operations and concurrent Skirmish changes, has a visible-touch Regular matrix of **4/5 Victory/return**: seeds 1102 (`/private/tmp/o001-aria-seed1102-current-20260924.log`), 2102 (`/private/tmp/o001-aria-seed2102-background-validation-20260924.log`), 4102 (`/private/tmp/o001-aria-seed4102-current-20260924.log`), and 5102 (`/private/tmp/o001-aria-seed5102-20260924.log`) passed with saved dashboard AP 2. Seed 3102 (`/private/tmp/o001-aria-seed3102-current-20260924.log`) completed three scans and evidence recovery but ended in Defeat during extraction; it remains the matrix loss. The first and third seed-2102 attempts stopped on macOS focus loss (`stopReason=2`) and its second attempt stopped in ARIA's startup Blocked state. The final seed-2102 pass used the existing Editor-only `WARLINE_ARIA_BACKGROUND_VALIDATION=1` switch to prevent focus changes from stopping synthetic touch input. All attempts are retained; the interrupted and blocked attempts are not wins. This is a working-tree validation record, not a pinned release build.

Focused Editor regressions passed **69/69** (`/private/tmp/o001-focused-regressions-20260924.log`, XML alongside it). The broader selected suite passed 112/115 and failed three tests (`/private/tmp/o001-editor-regressions-20260924.log`): the global V3 prefab localization audit reports 226 issues in unchanged UI prefabs, and two Skirmish guide localization cases lack `ui.skirmish.guide.1.title` in the concurrent Skirmish work. The O001 localization import, Farsi visual check, and focused Operations/Campaign/Skirmish session checks passed; this is not a clean broad regression result.

Host checks P0–P4, ARIA evidence/capture wiring, and mobile-ready content guards passed on September 24. P0 initially failed to parse the catalog's UTF-8 BOM; `check_p0.py` now opens it with `utf-8-sig` and its 13 checks pass. These host checks do not establish live play or device readiness. The second reviewed tactical approach, deeper interruption states, broader UI regression failures, and unfamiliar-player review remain open for the Editor readiness claim. Android/device release remains outside the current scope.

Active-scan checkpoint coverage was added to the checked Editor harness. The same-process Save & Exit → Resume passed with 2.000 seconds of site-1 scan progress preserved (`/private/tmp/o001-active-scan-resume-20260924.log`). A first fresh-process save attempt timed out in the ARIA observation stage and is retained as a failed validation attempt. Its traced retry saved at 2.008 seconds (`/private/tmp/o001-active-scan-process-save-retry-20260924.log`); a separate Editor process restored the session, clock, AP and scan progress (`/private/tmp/o001-active-scan-process-restore-verified-20260924.log`). The first restore pass only checked session/clock/AP, so it was rerun with an explicit scan assertion. A live carrier checkpoint also passed: after three scans, evidence pickup was saved at 423 seconds with roster member 15 carrying it (`/private/tmp/o001-carrier-process-save-20260924.log`); a fresh Editor process restored recovered evidence on that same stable roster member, along with the session/clock/AP (`/private/tmp/o001-carrier-process-restore-20260924.log`). Carrier-loss recovery and terminal-boundary process recovery still need live checks.

Terminal-boundary process validation was attempted but did not reach settlement. Four launches entered ARIA's `Blocked` state at 14 seconds while the macOS Editor was unfocused; a direct title-bar click advanced one run to 105 seconds, but a second focus click counted as player interruption (`stopReason=7`). These are failed/interrupted validation runs, not terminal evidence. Their full logs are retained; the unverified terminal-only harness branch was removed.

After removing that branch, an attempted carrier restore found the disposable `/private/tmp/o001-process-checkpoint.json` marker missing because the terminal setup had cleared it. The wrapper timed out after the setup exception; this run is invalid as a carrier check. The marker was reconstructed from the recorded carrier-save fixture, and the final cleaned-code restore passed with `carrierIndex=15` (`/private/tmp/o001-carrier-process-restore-final-valid-20260924.log`). The restore entry point now logs a failed setup marker and exits if its fixture is missing.

Full September 24 Unity logs and NUnit XML are retained in [`_Evidence/20260924/`](_Evidence/20260924/), including failed and interrupted runs.

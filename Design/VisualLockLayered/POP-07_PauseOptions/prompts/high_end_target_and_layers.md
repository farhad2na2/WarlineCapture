# POP-07_PauseOptions High-End Target And Layers Prompt

Regenerate `Pause Options Popup` as a landscape mobile UI target and export every reusable visual element as a separate transparent PNG layer. The target must match the original `Design/VisualLock/POP-07_PauseOptions/POP-07_PauseOptions_Landscape_Target.png` composition, not redesign it.

Required output: target PNG, all frame/button/card backgrounds as 9-slice-ready transparent-corner PNGs, all icons as separate transparent PNGs, no baked text inside reusable backgrounds, and a manifest mapping every layer to a Unity destination.

# Dense City Virtualized Render Proxy Android Performance Implementation Tracker

Date: 2026-07-28
Status: Candidate implementation in progress through accepted render-only and stateful House/Shop virtualization, deterministic camera/projection capacity with 20% headroom, and paired-Android destruction-transition validation; broader coverage, amended 54-FPS-class acceptance, and production cutover remain open
Parent tracker: `dense_city_editor_bake_hybrid_runtime_implementation_tracker.md`
Related contracts: `performance_regression_contract.md`, `gameplay_solid_ecs_contract.md`, `operation_map_runtime_ownership_chain.md`, `operation_map_scene_split_rollback_recipe.md`

## 1. Objective

Raise the dense-city candidate on Xiaomi `24090RA29G` from the measured `15.7-16.3 FPS` to a sustained 54-FPS-class Android result without reducing gameplay ownership, deleting accepted city placements, loading authoring GameObjects, or partitioning simulation state by camera.

The current candidate contains `82,797` baked render rows. Of those, `68,722` rows come from repeated generated presentation entries using only `377` repeated presentation signatures and `57` repeated prefab sources. Shared meshes and materials already prevent geometry/material duplication, but every placement still exists as a runtime render entity. The corrected Android run is CPU-main-bound at approximately `56.7-59.8 ms`, while GPU time is approximately `15.6-16.4 ms`, CPU render-thread time is approximately `1.8-2.3 ms`, and instrumented gameplay update time is approximately `0.4 ms`.

This tracker changes the representation of repeated permanent presentation:

```text
Editor authoring and generation
  -> preserve every accepted/generated stable placement and gameplay owner
  -> extract deterministic shared render prototypes and renderer-part recipes
  -> bake immutable placement, transform, color, bounds, state, and spatial-cell data
  -> bake a fixed-capacity set of generic ECS render-proxy slots
  -> remove the virtualized source render rows from player EntityScene output

Runtime
  -> load the existing thin binding scene and one map EntityScene once
  -> keep all simulation/gameplay entities and the immutable placement database resident
  -> keep one preallocated render-proxy slot set resident
  -> use the existing ECS camera snapshot to select nearby spatial cells
  -> use Burst jobs to retain/release/rebind proxy slots
  -> enable only the required render slots before Entities Graphics consumes them
  -> perform no GameObject instantiation, no scene streaming, and no steady-state structural changes
```

The target is not runtime procedural city generation. The complete city remains deterministic editor-baked data. Runtime only virtualizes which baked visual records are materialized as render entities.

## 2. Why This Amendment Is Authorized

The parent tracker originally selected whole-map render-entity residency and explicitly deferred render-only partitioning until Android evidence proved that representation unacceptable. That evidence now exists:

- exact APK: `555,817,853` bytes, SHA-256 `dec15d022c10eda2f3e3e4cb397f37871fac69656af5d7cdccd77741b32ae079`;
- exact device: Xiaomi `24090RA29G`, Android 16/API 36, ARM64;
- dense candidate EntityScene: `c00140f2e94a04c3084c8dcb0c18cbd0`;
- stable FPS: `15.7-16.3`;
- stable frame time: `61.5-63.7 ms`;
- typical CPU main: `56.7-59.8 ms`;
- GPU: `15.6-16.4 ms`;
- CPU render thread: `1.8-2.3 ms`;
- gameplay instrumentation: approximately `0.4 ms`;
- render rows in the packed candidate: `82,797`;
- repeated generated render rows: `68,722`;
- shared render-mesh arrays: `1`;
- repeated presentation signatures: `377`;
- repeated prefab sources: `57`.

The Android result proves that sharing mesh/material assets and relying only on Entities Graphics frustum culling is insufficient for this map. This amendment supersedes the parent's no-pooling/whole-render-residency clauses only for a candidate whose definition explicitly selects virtualized render residency and passes every gate in this tracker.

It does not supersede these parent decisions:

- all gameplay/simulation entities remain resident for the loaded map lifetime;
- no existing/generated authoring GameObject hierarchy ships or loads at runtime;
- no runtime city generation;
- no second map loader, static map-visual streamer, manager, service, controller, or mutable static registry;
- no production cutover before candidate Android acceptance;
- no mutation or deletion of the frozen static rollback package;
- no weakening of collider, ownership, deterministic-output, matrix/bounds parity, readiness, or teardown gates.

## 3. Success Definition

**User-authorized acceptance amendment (2026-08-07):** To shorten final closure while retaining a meaningful high-end Android performance gate, the accepted target is now 54-FPS-class rather than 60-FPS-class, and the final thermal route is two minutes rather than ten minutes. The tracker filename remains stable for references and history. This amendment supersedes earlier forward-looking 58/55 FPS, 17.2 ms average-frame, 60-FPS-class, and ten-minute-thermal clauses; historical evidence rows remain unchanged.

The feature is complete only when the exact candidate revision passes all of the following on the target Android device:

- average FPS `>= 54` after warmup over each representative 120-second route;
- 10th-percentile FPS `>= 50`;
- average frame time `<= 18.6 ms`;
- p95 frame time `<= 20 ms`;
- p99 frame time `< 25 ms`;
- CPU-main average `<= 12 ms` and p95 `<= 16 ms`;
- GPU average `<= 16 ms` and p95 `<= 18 ms`;
- steady-state operation-map managed allocation `0 B/frame`;
- render-virtualization overflow count exactly `0`;
- render-virtualization rebuild count changes only when the camera envelope or visual state requires it;
- no synchronous job completion attributed to the virtualization systems during steady camera traversal;
- no visible holes, stale proxy state, duplicate visuals, camera-edge popping, destroyed/intact leakage, or material/color leakage;
- all simulation entities remain resident and gameplay-equivalent;
- no map-visual scene load/unload occurs during camera travel;
- offline load, two-cycle load/unload, failure/reset/retry, and package ownership remain accepted;
- a final 2-minute thermal route remains within the tracked Android product budget from `performance_regression_contract.md`.

The amended 54-FPS goal is stronger than the existing product p95 budget. A result that passes the older `<25 ms` high-end p95 gate but fails the targets above does not complete this tracker.

Virtualization primarily addresses CPU-main work. The current GPU time is already close to the amended 54-FPS boundary, so final acceptance may also require the existing parent-tracker LOD, shadow, transparency, and small-detail optimizations. Those changes require separate evidence and may not be hidden inside a proxy-pool commit.

## 4. Non-Negotiable Architecture Decisions

### 4.1 Simulation And Presentation Ownership

- `OperationMapBuildingComponent`, health, faction, grid occupancy, navigation blockers, targeting identity, selection identity, production state, and destroyed state remain on stable gameplay entities.
- Render-proxy slots never become gameplay targets or authoritative owners.
- Minimap, navigation, AI, placement, targeting, runway, helipad, and scenario-anchor logic must read canonical ECS/map data, never proxy presence.
- A proxy binding is disposable presentation state. Losing a binding must not lose gameplay state.
- The complete immutable render-placement database remains resident until map teardown.
- The fixed proxy-slot entities load once with the map EntityScene and unload once with it.

### 4.2 No Runtime Instantiation Loop

- Do not instantiate/destroy an entity when the camera moves.
- Do not add/remove `RenderMeshArray`, `RenderFilterSettings`, `MaterialMeshInfo`, `LocalToWorld`, `RenderBounds`, or color components during steady-state traversal.
- Use pre-baked slot entities and enable/disable rendering through the enableable `MaterialMeshInfo` component.
- Rebind slots by writing existing unmanaged components.
- Do not use `Disabled` on gameplay owners.
- Do not add/remove `DisableRendering` every frame.
- Do not create a GameObject pool.

### 4.3 No New Streaming Owner

- Keep `OperationMapPresentationKind.EntityScene`.
- Add a render-residency mode beneath EntityScene presentation; do not add another presentation kind or map loader.
- Camera travel changes proxy assignments only. It cannot issue `SceneSystem.LoadSceneAsync`, Addressables operations, or static-presentation requests.
- The source scene, map EntityScene, metadata, and thin binding keep the existing load/readiness/failure/teardown owner.

### 4.4 Candidate-Only Until Accepted

- `OperationMapRenderResidencyMode.ResidentEntities` is the default.
- `OperationMapRenderResidencyMode.VirtualizedProxyPool` is fail-closed and valid only with a matching database schema, pool layout, candidate definition, and packed EntityScene contract.
- Production remains `StaticSceneChunks` until the parent tracker authorizes the separate production cutover.
- The accepted dense candidate and frozen rollback assets must be checkpointed by the existing transactional owner before any regeneration.

## 5. Eligibility Policy

Virtualize in this order:

1. repeated generated `Vegetation` and `Prop` presentation;
2. repeated generated noninteractive building attachments;
3. repeated generated `Infrastructure` that has no gameplay, animation, light, particle, or unique material-property owner;
4. repeated intact/destroyed building visual hierarchies after state synchronization passes;
5. repeated accepted-map render-only presentation after dense generated content is accepted;
6. named unique content only when a separate report proves benefit and parity.

Keep resident initially:

- vehicles and animated unit presentation;
- particles, trails, line renderers, skinned meshes, and GPU-animation owners;
- runtime lights, reflection probes, decals, audio, Animator, scripts, or mutable material-property owners;
- unique terrain, water, horizon, canal, mountain, runway, and bridge content until separately classified;
- selection/placement/order markers;
- any unresolved mixed gameplay/presentation owner;
- any renderer whose material/submesh/filter policy cannot be represented by the closed prototype schema.

Every exclusion requires a stable reason code in the virtualization report. Names, hierarchy guesses, renderer shape, or “looks static” are not valid classification.

## 6. Runtime Data Contract

### 6.1 Explicit Configuration

Add:

```text
Assets/Game/Scripts/Configs/OperationMapRenderResidencyMode.cs
```

with the closed values:

```csharp
public enum OperationMapRenderResidencyMode : byte
{
    ResidentEntities = 0,
    VirtualizedProxyPool = 1
}
```

Extend `OperationMapDefinition` with a serialized render-residency mode. Validation rules:

- `StaticSceneChunks` requires `ResidentEntities`;
- `EntityScene + ResidentEntities` retains the current behavior;
- `EntityScene + VirtualizedProxyPool` requires the virtualization database authoring reference in the SubScene, exact matching operation-map id/schema/content hash, at least one pool bucket, zero capacity deficit, and zero virtualized source render rows in packed output;
- unknown enum values fail;
- production cutover and candidate package builders must report the mode explicitly.

Do not use scripting defines, environment variables, platform checks, or map-id special cases as the ownership switch.

### 6.2 Blob Schema

Add unmanaged blob/component definitions in:

```text
Assets/Game/Scripts/Components/OperationMapRenderVirtualizationComponents.cs
```

The first accepted schema must contain:

```csharp
public struct OperationMapRenderDatabaseBlob
{
    public FixedString64Bytes OperationMapId;
    public FixedString128Bytes ContentHash;
    public int SchemaVersion;
    public float CellSize;
    public float3 GridOrigin;
    public int2 GridDimensions;
    public BlobArray<OperationMapRenderPrototypeBlob> Prototypes;
    public BlobArray<OperationMapRenderPrototypePartBlob> Parts;
    public BlobArray<OperationMapRenderPlacementBlob> Placements;
    public BlobArray<OperationMapRenderCellBlob> Cells;
    public BlobArray<int> CellPlacementIndices;
    public BlobArray<OperationMapRenderPoolBucketBlob> PoolBuckets;
}
```

Required logical fields:

| Record | Required fields |
|---|---|
| Prototype | collision-checked `OperationMapRenderIdentity128` content identity (`ulong Low`, `ulong High`); first part; part count; combined local bounds; semantic category; eligibility flags |
| Prototype part | renderer-path hash; mesh-array index; material-array index; submesh; local-to-placement matrix; local bounds; linear base color; fixed render-policy bucket and exact pool-bucket index; LOD/shadow flags |
| Placement | collision-checked `OperationMapRenderIdentity128` stable identity hash; prototype index; exact world matrix; cell index; state-owner index or `-1`; required visual state; priority; semantic category |
| Cell | deterministic integer coordinate; world AABB; first placement-index entry; placement-index count |
| Pool bucket | closed render-filter policy including coarse bucket, layer, rendering-layer mask, motion-vector mode, and shadow flags; first slot; capacity; computed peak required count; headroom; report identity |

Rules:

- Keep exact `float4x4` placement and part matrices in schema 1. Quantization requires a later matrix/bounds parity report and schema bump.
- Compute proxy world matrices as `placementWorld * partLocalToPlacement`.
- Store local renderer bounds; do not rediscover bounds from runtime mesh objects.
- Store linear base color explicitly. Do not instantiate a material to carry per-instance color.
- `MaterialMeshInfo` selects a mesh/material/submesh from the one shared sorted `RenderMeshArray`.
- A prototype fingerprint includes renderer hierarchy path, mesh GUID/local id, material GUID/local id, submesh, local matrix, local bounds, base color, the complete render-filter policy (coarse bucket, layer, rendering-layer mask, motion-vector mode, and shadow flags), and LOD policy.
- Schema 1 prototype fingerprints serialize those fields into an explicit little-endian binary payload with length-prefixed UTF-8 strings and raw finite IEEE-754 values before SHA-256 projection. Renderer paths must be normalized relative hierarchy paths; asset GUIDs must be 32 lowercase hexadecimal characters; local ids must be nonzero; indices/extents and colors must be valid; and unknown policy/shadow/LOD values fail closed.
- A placement identity hash derives from the existing stable identity using a documented SHA-256-to-`OperationMapRenderIdentity128` projection. The builder must retain the full stable id in the editor report and reject any 128-bit collision.
- Schema 1 projects the exact UTF-8 bytes (no BOM, no normalization) through SHA-256, stores digest bytes `0..7` as little-endian `Low` and bytes `8..15` as little-endian `High`, and sorts by unsigned `(Low, High)`. Empty sources fail closed; repeated identical sources are idempotent; any different full source registered to the same 128-bit value is a fatal collision.
- A renderer logical-row identity derives from placement identity plus prototype renderer-path identity. The editor report retains the full source identity/path for parity.
- Arrays are sorted deterministically; timestamps, absolute paths, instance ids, entity indices, and editor-session data are forbidden hash inputs.

### 6.3 Compound Buildings, Roofs, Walls, And Interiors

A shared building prototype is a part recipe, not a prefab instantiated at runtime.

```text
Building placement
  -> intact prototype
       -> wall-shell part
       -> roof part
       -> window/sign part
       -> interior/tent-content parts
  -> destroyed prototype
       -> rubble-shell part
       -> destroyed attachment parts
```

Related requirements:

- intact and destroyed visual records use one deterministic state-owner index belonging to the gameplay building;
- roof equipment, interiors, goods, signs, awnings, lamps, furniture, and tent contents inherit exactly one intact/destroyed state requirement;
- no attached part becomes an independent always-visible placement;
- independently damageable/selectable parts require their own canonical gameplay owner and state index;
- state changes update the canonical state array even while the building is off-camera;
- a newly assigned proxy must read current canonical state before becoming render-enabled;
- recycling must reset transform, mesh/material selection, color, bounds, binding identity, LOD state, and enable state.

### 6.4 Runtime Components

Add these unmanaged components/buffers with exact single ownership:

| Type | Owner | Purpose |
|---|---|---|
| `OperationMapRenderDatabaseComponent` | one map EntityScene entity | Blob reference, schema, map generation, content hash |
| `OperationMapRenderProxySlotComponent` | every fixed slot entity | Stable slot index, fixed pool-bucket index, current placement/part binding, assignment generation |
| `OperationMapRenderVirtualizationStateComponent` | one runtime state entity | Initialized flag, initial-view-applied flag, active envelope, camera signature, active/dirty/overflow counters |
| `OperationMapVirtualizedBuildingPresentationComponent` | canonical gameplay building | Dense state-owner index; replaces render-entity root ownership in virtualized mode |
| `OperationMapRenderStateChangeComponent` | one map-owned bounded buffer | Rare authoritative intact/destroyed state transitions |
| `OperationMapRenderVirtualizationMetricsComponent` | one map-owned diagnostics entity | Capacity, enabled slots, retained/released/rebound counts, overflow, rebuild reason |

Do not add managed collections or UnityEngine object references to these components.

### 6.5 Fixed Render-Policy Buckets

`RenderFilterSettings` is shared data and must not be changed when a slot is rebound. The bake therefore creates a fixed slot range for each accepted policy bucket. The initial closed buckets are:

- opaque, shadows on;
- opaque, shadows off;
- alpha-clipped, shadows on;
- alpha-clipped, shadows off;
- transparent, shadows off;
- always-resident exception.

Layer, rendering-layer mask, motion-vector mode, shadow casting, receive-shadows behavior, and static-shadow policy are part of the bucket identity. Unknown combinations fail candidate bake or remain resident with a named exclusion.

Transparent and alpha-clipped content do not silently fall into the opaque bucket.

The schema-1 pure classifier returns a complete immutable policy key containing the coarse bucket plus layer, rendering-layer mask, motion-vector mode, and shadow flags. A changed fixed-filter field therefore cannot alias an existing pool policy. Static-shadow casting requires ordinary shadow casting, transparent shadow casters are rejected, zero rendering-layer masks and layers outside `[0,31]` are rejected, and unknown material/motion/shadow values fail closed. The always-resident bucket is selected only by an explicit exception input; it is not a fallback for an unsupported combination.

## 7. Spatial Index And Capacity Contract

### 7.1 Cells

- Start with deterministic `32 m` X/Z cells, matching the prior static-presentation diagnostic scale.
- Derive coordinates from the accepted operation-map origin using floor division.
- Assign a placement to every cell intersected by its combined transformed bounds, not only its pivot cell.
- Schema 1 treats nonzero AABBs as half-open on their maximum X/Z edge, while a zero-extent point belongs to the cell containing that point. This prevents an AABB ending exactly on a boundary from leaking into the next cell.
- Deduplicate placement indices inside each cell.
- Sort cells by `(z, x)` row-major index and placement indices by stable-identity rank. Multi-cell gathers deduplicate before returning globally sorted placement indices, so selected-cell traversal order cannot change output.
- Keep large multi-cell placements resident initially unless measured evidence approves virtualized multi-cell ownership.

### 7.2 Camera Envelope

Reuse `RuntimeCameraSnapshotComponent`. Do not read `Camera.main`, call scene searches, or access a `Camera` from a Burst system.

The active envelope is:

- the camera frustum projected against the operation-map presentation height range;
- expanded by one full visible safety cell;
- prefetched by one additional guard cell;
- clamped to operation-map camera bounds.

Rebuild when:

- the required frustum footprint is no longer contained by the current guard envelope;
- rotation/projection/orthographic mode changes enough to expose a new cell;
- camera teleport/tactical-follow change invalidates the envelope;
- the map generation changes;
- a currently bound building state changes;
- a test explicitly forces a rebuild.

Do not rebuild merely because `Time.frameCount` advanced.

### 7.3 Capacity

The editor capacity validator must sweep:

- every canonical operation-map camera pose;
- normal, build, fullscreen-map, maximum-zoom, and tactical-follow projection limits;
- every spatial cell as an envelope center;
- all accepted yaw/pitch/projection extrema;
- safety and guard margins.

For each render-policy bucket:

```text
capacity = ceil(maximumSimultaneouslyRequiredPartRows * 1.20)
```

Capacity must be deterministic and checked into the report. A manual capacity constant without the sweep report is forbidden.

Schema-1 sweep inputs use a nonempty ordinal sample identity, a complete validated render-policy key, and a nonnegative required-part-row count. Every reported policy must cover the identical canonical sample-identity set; missing policy/sample pairs and duplicate pairs fail closed. Input order cannot affect results: policies are sorted by bucket, layer, rendering-layer mask, motion-vector mode, then shadow flags. Peak aggregation uses the maximum per policy, and capacity uses overflow-checked integer ceiling `(peak * 120 + 99) / 100`, avoiding floating-point or culture differences.

Provisional design limits:

- packed runtime entities carrying enabled/disabled map `MaterialMeshInfo`: `<= 24,000`;
- simultaneously enabled map proxy rows in representative gameplay views: `<= 8,000`;
- headroom in every bucket: `>= 20%`;
- runtime overflow: exactly `0`.

Changing a limit requires a report with before/after Android CPU, GPU, memory, visual, and overflow evidence.

### 7.4 Deterministic Overflow Safety

Candidate validation must make overflow unreachable for accepted routes. Runtime still needs a bounded safety policy:

1. retain gameplay-building presentation;
2. retain selected/targeted or damage-transition presentation;
3. retain infrastructure required for spatial readability;
4. retain nearest opaque presentation;
5. retain vegetation/props last.

Within a priority, sort by distance then stable identity. Increment an overflow counter and emit one gated development diagnostic. Do not allocate, throw every frame, or spam logs. Any overflow rejects Android acceptance and production cutover.

## 8. Bake And Package Pipeline

### 8.1 Authoring Asset

Add:

```text
Assets/Game/Scripts/Configs/OperationMapRenderDatabaseBakeConfig.cs
Assets/Game/Scripts/Authorings/OperationMapVirtualizedPresentationAuthoring.cs
```

`OperationMapRenderDatabaseBakeConfig` is a generated ScriptableObject and deterministic bake input containing sorted Unity asset references plus serialized logical records. It is not queried at player runtime.

Candidate output path:

```text
Assets/Game/GeneratedOperationMapEntityPresentationCandidate/VirtualizedPresentation/
```

The accepted future config path is `Assets/Game/GeneratedOperationMapEntityPresentationCandidate/VirtualizedPresentation/OperationMapRenderDatabaseBakeConfig.asset`; the accepted future report path is `Design/AgentReports/2026-07-28_dense_city_render_virtualization_database.json`. Their producer, transaction, rollback, rejection, and non-source rules are recorded in `dense_city_generated_output_ownership.md`. `VRP-020` defines only the generated `OperationMapRenderDatabaseBakeConfig` schema and creates neither output.

### 8.2 Editor Builder

Add:

```text
Assets/Game/Scripts/Editor/OperationMapRenderDatabaseBuilder.cs
Assets/Game/Scripts/Editor/OperationMapRenderDatabaseValidator.cs
Assets/Game/Scripts/Editor/OperationMapRenderVirtualizationReportBuilder.cs
```

The builder must:

1. consume accepted migration/generation stable records;
2. resolve renderer ownership without names or geometry guesses;
3. classify eligible/excluded renderer rows;
4. group eligible rows into deterministic prototypes;
5. preserve renderer-local part matrices and local bounds;
6. produce logical placements and state-owner relationships;
7. create the deterministic cell index;
8. compute policy-bucket capacity;
9. write the bake config transactionally;
10. emit a machine-readable report before candidate replacement;
11. run twice and require identical logical hash, counts, ordering, and serialized bytes.

### 8.3 Baker And Pool Slots

Add:

```text
Assets/Game/Scripts/Authorings/OperationMapVirtualizedPresentationAuthoring.cs
Assets/Game/Scripts/Rendering/Baking/OperationMapRenderVirtualizationBakingSystem.cs
```

The authoring baker must:

- register the blob using the supported baking blob-asset ownership API;
- build the sorted shared mesh/material arrays;
- create the database component entity;
- create exactly the reported number of slot entities per bucket;
- apply fixed `RenderFilterSettings` and the shared `RenderMeshArray`;
- add `MaterialMeshInfo`, `LocalToWorld`, `RenderBounds`, `URPMaterialPropertyBaseColor`, and `OperationMapRenderProxySlotComponent`;
- initialize `MaterialMeshInfo` disabled;
- avoid `Parent`/`Child`/`LocalTransform` on generic leaf slots;
- avoid one proxy root hierarchy per logical placement.

The post-baking system must:

- match every eligible converted source render row to exactly one logical database row;
- mark eligible source-only render entities `BakingOnlyEntity` or strip their rendering ownership without deleting canonical gameplay entities;
- replace virtualized building render-root references with `OperationMapVirtualizedBuildingPresentationComponent`;
- retain excluded render rows unchanged;
- reject any eligible packed source render row that survives;
- reject a source row removed without a matching logical record;
- reject a proxy slot missing required render components or using an unreported policy bucket.

### 8.4 Package Ownership

The built package must contain:

- the existing thin runtime binding;
- the map EntityScene with simulation entities, immutable render database, fixed proxy slots, and named resident exceptions;
- the accepted map surface/minimap/metadata;
- transitive mesh/material assets required by the shared `RenderMeshArray`;
- zero source/candidate authoring hierarchy;
- zero explicit runtime dependency on `OperationMapRenderDatabaseBakeConfig` when the baked blob already owns the data;
- zero legacy static-presentation ownership in the candidate.

The runtime-content report must add resident-render rows, virtualized logical rows, prototypes, parts, cells, policy buckets, total slots, per-bucket capacity, packed database bytes, source rows removed, excluded rows by reason, and source-hierarchy counts.

The schema-1 render-virtualization report uses schema identity `warline.operation-map.render-virtualization`, exact schema version `1`, a trimmed operation-map id, a 64-character lowercase content hash, explicit `VirtualizedProxyPool` mode, a closed metrics object, and a strictly sorted `capacityByPolicy` array. Structural database/slot metrics and per-policy sweep/peak metrics must be positive; zero-valid resident/exclusion/removal/hierarchy counts must still be present and nonnegative. Unknown, missing, duplicate, default, negative, inconsistent-headroom, duplicate-policy, unsorted-policy, unequal-sweep-count, and total-slot reconciliation failures reject serialization or parsing.

## 9. Runtime Scheduling Contract

### 9.1 Systems And Groups

Add real ECS systems in `Game.Rendering`:

```text
Assets/Game/Scripts/Rendering/Systems/OperationMapRenderVirtualizationInitializationSystem.cs
Assets/Game/Scripts/Rendering/Systems/OperationMapRenderCellSelectionSystem.cs
Assets/Game/Scripts/Rendering/Systems/OperationMapRenderProxyAssignmentSystem.cs
Assets/Game/Scripts/Rendering/Systems/OperationMapRenderProxyApplySystem.cs
Assets/Game/Scripts/Rendering/Systems/OperationMapRenderStateSyncSystem.cs
Assets/Game/Scripts/Rendering/Systems/OperationMapRenderVirtualizationMetricsSystem.cs
```

Ordering:

```text
RuntimeCameraReferenceSystem snapshot publication
  -> OperationMapRenderStateSyncSystem
  -> OperationMapRenderCellSelectionSystem
  -> OperationMapRenderProxyAssignmentSystem
  -> OperationMapRenderProxyApplySystem
  -> EntitiesGraphicsSystem
```

Use explicit update-group/order attributes. Do not rely on file order.

### 9.2 Initialization

Initialization runs once per map generation and:

- validates one database and one state owner;
- validates operation-map id, schema, content hash, residency mode, slot counts, and slot indices;
- allocates system-owned persistent native state with exact bounded capacities;
- builds slot-to-binding and logical-row-to-slot maps;
- initializes canonical building state once through a Burst job;
- performs the first envelope selection and assignment;
- sets `OperationMapRenderVirtualizationStateComponent.InitialViewApplied` only after required first-view slots are applied;
- leaves `OperationMapReadinessFlags`, acceptance/failure codes, and final readiness publication exclusively owned by the existing `Game.Composition` readiness path, which validates the mode-specific virtualization state;
- records integration timing separately from steady-state frame timing.

Dispose persistent native containers when map generation changes, unload begins, or the system is destroyed. Complete dependencies only at these lifecycle boundaries.

### 9.3 Cell Selection Jobs

Use Burst `IJob`/`IJobFor` work over the precomputed cell index:

- compute required cells from the immutable camera snapshot and active guard envelope;
- gather placement indices from only required cells;
- deduplicate multi-cell placements with a bounded native bitset/hash set;
- filter by current building visual state;
- expand selected placements to logical renderer-part keys;
- sort deterministically only when assignment order requires it.

Do not scan all `82,797` logical rows per frame.

### 9.4 Assignment Job

Maintain:

- active logical-row key to slot index;
- slot index to logical-row key;
- per-bucket deterministic free-slot stacks;
- assignment generation;
- dirty slot bitset;
- active placement/cell bitsets.

On rebuild:

1. retain existing assignments still required;
2. release assignments no longer required;
3. assign newly required rows from the matching bucket;
4. generate one fixed-size slot command per changed slot;
5. leave unchanged slots untouched;
6. publish bounded counters and overflow.

Run assignment in a Burst job. It may be single-threaded inside the job when deterministic hash/free-list mutation requires it; the point is to remove that work from the main thread. Do not run the algorithm as a managed loop.

### 9.5 Parallel Apply Job

Apply commands with `IJobChunk` or an equivalent parallel job where every job iteration owns one unique slot:

- write `LocalToWorld`;
- write `RenderBounds`;
- write `MaterialMeshInfo`;
- write `URPMaterialPropertyBaseColor`;
- write proxy binding/generation;
- set the enableable `MaterialMeshInfo` state;
- fully reset released slots.

The slot entity must not carry `Parent`, `Child`, or `LocalTransform`. Flattened leaf `LocalToWorld` avoids transform-hierarchy work for proxy presentation.

Set `state.Dependency` and let Entities Graphics consume the dependency. A steady-state path may not call `Complete()`, `CompleteDependency()`, `EntityManager.CompleteAllTrackedJobs()`, or synchronously read job output.

### 9.6 Stable Camera Fast Path

When the current required envelope is contained by the guard envelope and there are no visual-state changes:

- do not schedule selection/assignment/apply jobs;
- do not allocate temporary native containers;
- do not query all placement or render entities;
- do not write slot components;
- only update bounded diagnostics at its throttled cadence.

This no-op path is a required performance test.

## 10. Building State And Gameplay Integration

### 10.1 Canonical State

`OperationMapBuildingDestroyedComponent` remains authoritative. Add a deterministic dense state-owner index to each virtualized gameplay building.

Adapt `OperationMapBuildingDestructionSystem`:

- resident mode keeps the existing render-root scale behavior;
- virtualized mode changes canonical destroyed state and appends one `OperationMapRenderStateChangeComponent` buffer record;
- it does not search proxy bindings or write render transforms;
- it does not instantiate a destroyed visual;
- it remains Burst compiled.

### 10.2 State Synchronization

`OperationMapRenderStateSyncSystem`:

- initializes the state array once from all virtualized buildings;
- consumes the bounded state-change buffer;
- marks only affected logical placement rows/cells dirty;
- releases intact assignments and assigns destroyed assignments when currently visible;
- retains off-camera state without a render slot;
- clears consumed events through the accepted ECS dependency/ECB path.

### 10.3 Gameplay Independence Tests

Required cases:

- destroy a visible generated house and verify complete intact-to-destroyed transition;
- destroy an off-camera house, travel to it, and verify only destroyed parts appear;
- destroy a building, recycle all its former slots through unrelated buildings, return, and verify no stale intact/color/material state;
- select/target an off-camera building through canonical gameplay data without a proxy dependency;
- minimap markers remain present when their world visuals have no assigned proxy;
- navigation/blocking does not change as proxy assignments change;
- camera traversal changes zero gameplay entity counts and zero gameplay component values.

## 11. Diagnostics And Performance Instrumentation

Add profiler markers for:

- `OperationMapRenderVirtualization.Initialize`;
- `OperationMapRenderVirtualization.SelectCells`;
- `OperationMapRenderVirtualization.AssignSlots`;
- `OperationMapRenderVirtualization.ApplySlots`;
- `OperationMapRenderVirtualization.SyncState`.

Structured metrics:

- logical placements/parts and resident exceptions;
- total slot capacity and per-bucket capacity;
- enabled/disabled slots;
- retained/released/new assignments;
- active cells and placements;
- rebuild reason;
- overflow count and highest deficit;
- initialization/integration duration;
- average/p95/p99/max CPU time for each named system/job boundary;
- managed allocation after readiness;
- Entities Graphics batches and render entity counts;
- CPU main/render thread and GPU frame distributions.

Diagnostics must be gated before string construction and sampled no more often than the existing performance-diagnostics cadence. No direct per-frame `Debug.Log*`.

## 12. Validation Contract

### 12.1 Pure And EditMode Validation

Required new tests:

```text
Assets/Tests/Editor/OperationMapRenderDatabaseBuilderTests.cs
Assets/Tests/Editor/OperationMapRenderDatabaseValidatorTests.cs
Assets/Tests/Editor/OperationMapRenderVirtualizationBakingTests.cs
Assets/Tests/Editor/OperationMapRenderVirtualizationCapacityTests.cs
Assets/Tests/Editor/OperationMapRenderVirtualizationDeterminismTests.cs
Assets/Tests/Editor/OperationMapRenderResidencyModeTests.cs
```

Cover:

- stable sorting and hashing;
- collision rejection;
- prototype deduplication;
- compound part matrices;
- nonuniform/negative scale parity;
- material/submesh/color preservation;
- bounds and multi-cell membership;
- policy-bucket classification;
- capacity sweep and headroom;
- source-row-to-logical-row bijection;
- slot composition and disabled initial state;
- unknown schema/mode/bucket rejection;
- two-run byte equality;
- accepted/frozen asset isolation.

### 12.2 Runtime And PlayMode Validation

Required new tests:

```text
Assets/Tests/PlayMode/OperationMapRenderVirtualizationPlayModeTests.cs
Assets/Tests/PlayMode/OperationMapRenderVirtualizationPackedPlayModeTests.cs
```

Cover:

- first-view readiness;
- stable-camera no-op;
- slow pan, fast pan, zoom, rotation, and teleport;
- cell-edge guard band;
- retention before reassignment;
- deterministic slot reuse;
- zero overflow;
- visible logical-row matrix/bounds/color parity;
- state transitions and off-camera destruction;
- no map-visual scene requests during travel;
- no runtime GameObject map visuals;
- failure/reset/retry;
- two load/unload cycles;
- all persistent native containers disposed;
- zero virtualized source render rows after packed load;
- unchanged simulation entity counts during camera travel.

### 12.3 Parity Amendment

The existing parity contract currently expects every permanent render row to exist as a runtime entity before culling. For `VirtualizedProxyPool`, replace that boundary with two exact gates:

1. logical parity: every accepted source renderer maps to exactly one resident exception or one immutable database logical row with matching world matrix, local bounds, mesh/material/submesh, color, state owner, and semantic identity;
2. materialized parity: for every canonical camera, every required logical row maps to exactly one enabled proxy whose matrix/bounds/material/color match the logical row, and no nonrequired row is enabled outside the guard policy.

Off-camera absence of a proxy is valid only after logical parity passes. Missing logical data is never culling.

### 12.4 Android Routes

Capture the same exact revision and package across:

- fixed initial gameplay pose;
- dense downtown slow pan;
- maximum-speed pan across district boundaries;
- zoom minimum to maximum and back;
- camera rotation/tactical-follow where supported;
- selected building plus destruction;
- minimap/fullscreen-map transition;
- Menu -> Match -> Menu -> Match -> Menu;
- 120-second steady route for iteration;
- 2-minute thermal route for final acceptance.

Record raw profiler data before and after. Acceptance metrics must come from diagnostics-disabled release-equivalent runs; profiler-enabled captures are attribution evidence only.

## 13. File And Type Ownership Plan

No new assembly definition is planned.

| Assembly | Files/types |
|---|---|
| `Game.Components` | blob schema, proxy slot/state/metrics, building presentation state index, state-change buffer |
| `Game.Configs` | `OperationMapRenderResidencyMode`; `OperationMapRenderDatabaseBakeConfig`; `OperationMapDefinition` field/validation |
| `Game.Authoring` | virtualized-presentation root authoring/baker; add only the required `Unity.Entities.Graphics` reference if compilation proves it necessary |
| `Game.Rendering` | baking cleanup system; initialization, cell selection, assignment, apply, state sync, metrics systems |
| `Game.Editor` | deterministic database builder/validator, capacity sweep, report, Bake All integration |
| `Game.Tests.Editor` | schema, builder, capacity, determinism, bake/package tests |
| `Game.Tests.PlayMode` | live and packed lifecycle/parity/performance behavior |

Naming rules:

- only real ECS implementations use the `System` suffix;
- editor analyzers use `Builder`, `Validator`, or `ReportBuilder`;
- no `Manager`, `Service`, `Controller`, `Facade`, `Provider`, or mutable static runtime registry;
- helper extraction is allowed only when it owns one cohesive algorithm and does not hide lifecycle state.

## 14. Implementation Phases And Checklist

Difficulty labels:

- `[TERRA]`: bounded additive schema, pure algorithm, report, test, or documentation work with no runtime ownership switch;
- `[SOL]`: bake ownership mutation, hot-path scheduling, gameplay presentation migration, packed lifecycle, acceptance policy, or production cutover.

Terra must stop and request Sol when the next dependency-ready item is marked `[SOL]`.

### Phase 0: Evidence And Amendment

- [x] `VRP-000 [TERRA]` Record the exact corrected-APK Android FPS/CPU/GPU/memory evidence. Depends on: none.
- [x] `VRP-001 [SOL]` Approve candidate-only render virtualization under the parent's measured-failure escape clause. Depends on: `VRP-000`.
- [x] `VRP-002 [SOL]` Recover one raw Android Unity profile and name the dominant PlayerLoop child owners before representation mutation. Depends on: `VRP-000`.
- [x] `VRP-003 [TERRA]` Emit exact eligible/excluded counts by semantic category, prototype signature, renderer type, policy bucket, and gameplay ownership. Depends on: `VRP-000`.
- [x] `VRP-004 [TERRA]` Add a fixed-camera/route baseline report that reconciles current `82,797` render rows and Android measurements at one exact revision. Depends on: `VRP-002`, `VRP-003`.

**Exit:** The before state and dominant CPU owner are reproducible; eligibility is not guessed.

### Phase 1: Additive Schema And Pure Contracts

- [x] `VRP-010 [TERRA]` Add `OperationMapRenderResidencyMode` with default resident behavior and closed validation tests. Depends on: `VRP-001`.
- [x] `VRP-011 [TERRA]` Add unmanaged blob/component schema with field-level validation and no runtime use. Depends on: `VRP-010`.
- [x] `VRP-012 [TERRA]` Add stable 128-bit identity projection, collision detection, and deterministic sorting tests. Depends on: `VRP-011`.
- [x] `VRP-013 [TERRA]` Add pure prototype fingerprinting for mesh/material/submesh/path/matrix/bounds/color/filter/LOD inputs. Depends on: `VRP-011`.
- [x] `VRP-014 [TERRA]` Add pure cell assignment and multi-cell deduplication algorithms with boundary tests. Depends on: `VRP-011`.
- [x] `VRP-015 [TERRA]` Add pure policy-bucket classification with fail-closed unknown combinations. Depends on: `VRP-011`.
- [x] `VRP-016 [TERRA]` Add deterministic camera-capacity sweep inputs/results and 20% headroom calculation tests. Depends on: `VRP-014`, `VRP-015`.
- [x] `VRP-017 [TERRA]` Add schema/report serialization and reject missing/default/negative metrics. Depends on: `VRP-012` through `VRP-016`.

**Exit:** All new data algorithms compile and pass without changing candidate or production bake output.

### Phase 2: Deterministic Database Builder

- [x] `VRP-020 [TERRA]` Add bake-config schema and generated-output ownership documentation; create no asset yet. Depends on: `VRP-017`.
- [x] `VRP-021 [SOL]` Join accepted/generated stable owners to exact renderer rows and emit a source-row inventory. Depends on: `VRP-003`, `VRP-020`.
- [x] `VRP-022 [SOL]` Build shared prototype/part recipes with exact compound child transforms and bounds. Depends on: `VRP-021`.
- [x] `VRP-023 [SOL]` Build logical placements and intact/destroyed state-owner relationships. Depends on: `VRP-022`.
- [x] `VRP-024 [TERRA]` Build the deterministic cell index from logical placement bounds. Depends on: `VRP-023`.
- [x] `VRP-025 [TERRA]` Compute per-policy capacity and provisional entity/active-slot budgets. Depends on: `VRP-024`.
- [x] `VRP-026 [SOL]` Write the candidate bake config transactionally and prove accepted/frozen/production isolation. Depends on: `VRP-025`.
- [x] `VRP-027 [TERRA]` Run unchanged input twice and require identical record hash, ordering, counts, and serialized bytes. Depends on: `VRP-026`.
- [x] `VRP-028 [SOL]` Expand the logical database and prove source matrix/bounds/mesh/material/submesh/color/state parity before any source render row is removed. Depends on: `VRP-027`.

**Exit:** A complete deterministic logical rendering database exists beside unchanged current render entities.

### Phase 3: Candidate Pool Baking

- [x] `VRP-030 [TERRA]` Add root authoring/baker scaffolding that bakes only the database component; source rendering stays unchanged. Depends on: `VRP-028`.
- [x] `VRP-031 [SOL]` Build one sorted shared `RenderMeshArray` and validate all logical mesh/material/submesh indices. Depends on: `VRP-030`.
- [x] `VRP-032 [SOL]` Bake fixed leaf proxy slots per policy bucket with disabled `MaterialMeshInfo`. Depends on: `VRP-031`.
- [x] `VRP-033 [TERRA]` Validate slot indices, bucket ranges, required components, absent hierarchy components, and disabled initial state. Depends on: `VRP-032`.
- [x] `VRP-034 [SOL]` Mark only eligible source render rows baking-only/stripped while preserving canonical gameplay entities and named resident exceptions. Depends on: `VRP-033`.
- [x] `VRP-035 [SOL]` Replace virtualized building render-root ownership with the state-index component only after exact source-to-logical parity. Depends on: `VRP-034`.
- [x] `VRP-036 [SOL]` Extend readiness/package gates for residency mode, database, slots, zero eligible packed source rows, and exact resident exceptions. Depends on: `VRP-035`.
- [x] `VRP-037 [TERRA]` Extend the packed-content report with database/slot/source-removal metrics and fail-closed defaults. Depends on: `VRP-036`.
- [x] `VRP-038 [SOL]` Run direct bake and packed candidate validation twice with production cutover disabled. Depends on: `VRP-037`.

**Exit:** The candidate package contains compact logical data and fixed proxy slots instead of eligible permanent render rows, with no runtime assignment yet.

### Phase 4: Jobified Runtime Assignment

- [x] `VRP-040 [SOL]` Make camera snapshot publication a single ordered frame boundary with a version/signature usable by Burst systems. Depends on: `VRP-038`.
- [x] `VRP-041 [SOL]` Implement map-generation initialization and bounded persistent native state. Depends on: `VRP-040`.
- [x] `VRP-042 [TERRA]` Implement/test the pure guard-envelope rebuild decision. Depends on: `VRP-040`.
- [x] `VRP-043 [SOL]` Implement Burst cell selection/gather/dedup/state-filter jobs. Depends on: `VRP-041`, `VRP-042`.
- [x] `VRP-044 [SOL]` Implement deterministic retain/release/assign logic inside a Burst job. Depends on: `VRP-043`.
- [x] `VRP-045 [SOL]` Implement parallel slot apply with unique writes and enableable `MaterialMeshInfo`. Depends on: `VRP-044`.
- [x] `VRP-046 [SOL]` Order apply before Entities Graphics without a hot-path synchronous completion. Depends on: `VRP-045`.
- [x] `VRP-047 [TERRA]` Add stable-camera no-op, zero-allocation, no-component-write, and no-rebuild tests. Depends on: `VRP-046`.
- [x] `VRP-048 [TERRA]` Add bounded metrics/profiler markers with gated formatting. Depends on: `VRP-046`.
- [x] `VRP-049 [SOL]` Prove load/unload disposes all persistent native ownership and leaves no job using unloaded blob/entity data. Depends on: `VRP-047`, `VRP-048`.

**Exit:** Repeated render-only proxy presentation follows camera movement through jobs with zero steady-state structural changes.

### Phase 5: Render-Only Pilot

- [x] `VRP-050 [TERRA]` Select the largest safe repeated `Vegetation`/`Prop` pilot from the inventory and freeze its exact stable identities/count. Depends on: `VRP-003`, `VRP-049`.
- [x] `VRP-051 [SOL]` Enable virtualization only for that pilot in the candidate definition/output. Depends on: `VRP-050`.
- [x] `VRP-052 [SOL]` Validate slow/fast pan, zoom, rotation, teleport, guard bands, deterministic reuse, and zero overflow in packed PlayMode. Depends on: `VRP-051`.
- [x] `VRP-053 [SOL]` Capture Android before/after CPU main, render thread, GPU, FPS, memory, active slots, and visible parity on the same route. Depends on: `VRP-052`.
- [x] `VRP-054 [SOL]` Accept expansion only if CPU-main p95 improves materially, visual/state gates pass, and no new dominant main-thread owner offsets the gain. Depends on: `VRP-053`.

**Exit:** Android evidence proves the representation change helps before broader ownership migration.

### Phase 6: Building And Attachment State

- [x] `VRP-060 [SOL]` Add deterministic state-owner indices to virtualized gameplay buildings. Depends on: `VRP-054`.
- [x] `VRP-061 [SOL]` Adapt `OperationMapBuildingDestructionSystem` for resident versus virtualized presentation without changing canonical destruction semantics. Depends on: `VRP-060`.
- [x] `VRP-062 [SOL]` Add bounded state-change events and jobified state synchronization. Depends on: `VRP-061`.
- [x] `VRP-063 [SOL]` Virtualize one complete building family including walls, roof, interiors, intact attachments, and destroyed recipe. Depends on: `VRP-062`.
- [x] `VRP-064 [SOL]` Pass visible destruction, off-camera destruction, recycle/return, and state-leak tests. Depends on: `VRP-063`.
- [x] `VRP-065 [SOL]` Prove selection, targeting, minimap, navigation, and blockers remain independent of proxy assignment. Depends on: `VRP-064`.
  - Corrected 2026-08-02 after Android user review exposed that the original functional gate covered movable units but the click lookup and focus lifecycle still excluded canonical `OperationMapBuildingComponent` entities. Live, non-destroyed canonical buildings are now included in the grid/screen lookup and may receive focus/selection without `UnitMove` or movable-unit cache ownership. Focused lookup and lifecycle gates plus the aggregate ECS selection suite pass on Windows/Editor. Android confirmation remains pending, and ECS-backed building production is a separately reopened parent-contract item.
- [x] `VRP-066 [SOL]` Expand to all eligible repeated generated building families with exact exclusion report. Depends on: `VRP-065`.
- [x] `VRP-067 [SOL]` Capture Android destruction-transition frame cost and visual matrix for house/shop/tent/military variants. Depends on: `VRP-066`.

**Exit:** Complete building compound presentation recycles safely while gameplay state stays resident and authoritative.

**Sequencing amendment (VRP-067 acceptance):** VRP-067 owns only the dependency-safe capacity remediation required to replay its measured House/Shop Android envelopes with zero overflow and at least 20% per-bucket headroom. The accepted schema-2 sweep covers the measured inclusive `12 x 9` envelope and its `9 x 12` quarter-turn at every grid coordinate. `VRP-074` retains the broader representative-route camera/projection/zoom/tactical-follow capacity tuning and may change capacities only from its own before/after evidence; this amendment neither checks nor bypasses VRP-074.

**VRP-074 bounded transition amendment (user-authorized 2026-08-08):** The exact Samsung representative route proved that steady max zoom and isolated tactical follow fit the accepted `7,784` slots with zero overflow, but chaining a far view into tactical follow produced a transient `4,853`-row deficit because camera position eased toward the target while the effective look target crossed the whole map. Raising fixed capacity for that invalid transitional frustum would exceed VRP-075's `8,000` representative-proxy limit. VRP-074 is therefore authorized to repair only this tactical-follow interpolation so position and look target move coherently and preserve a bounded downward footprint. The repair may not raise capacity, weaken overflow/headroom gates, change gameplay selection/follow ownership, or bypass fresh checked Editor and matched Android before/after validation.

**VRP-075 exact packed-count amendment (2026-08-08):** The packed limit is measured from entities carrying `MaterialMeshInfo` after excluding Unity `BakingOnlyEntity` source-conversion entities. Logical resident rows are not assumed to be one-to-one with render entities because multiple logical/submesh rows may share a converted entity. The checked two-pass bake therefore records the exact shipped `MaterialMeshInfo` entity count and separately retains the exact fixed proxy-slot count. Since the complete fixed pool is below `8,000`, every runtime enabled subset is bounded below the representative-proxy limit; matched Samsung command evidence remains required to prove normal routes do not overflow or deficit. Wrapper-launch detachment, restricted-host WMI/pipe denial, or licensing IPC failure is a recoverable execution condition under `AGENTS.md`, not permission to pause, bypass the checked wrapper, or weaken a pass marker.

**VRP-081 canonical materialization amendment (2026-08-08):** The canonical input boundary for materialized parity is the accepted schema-2 capacity contract: both measured inclusive `12 x 9` and `9 x 12` materialized-envelope orientations at every origin-aligned grid coordinate (`14,592` envelopes), evaluated for every canonical visual-state variant. Historical five-camera screenshot fixtures and the earlier sparse-pilot PlayMode route are not valid capacity inputs for the expanded current database; they remain rejected diagnostics. VRP-086 separately owns five fixed-camera screenshots and camera-edge visual evidence. VRP-081 must run the real production selection, gather, deterministic assignment, and slot-apply jobs, prove every required row has one reciprocal enabled binding with exact matrix/bounds/material/mesh/submesh/color data, prove every released slot is reset/disabled, and prove zero overflow or extra active slots across all canonical envelope/state samples.

### Phase 7: Coverage, LOD, And GPU Headroom

- [x] `VRP-070 [SOL]` Expand to eligible generated infrastructure and noninteractive attachments. Depends on: `VRP-067`.
- [x] `VRP-071 [SOL]` Classify alpha-clipped/transparent buckets and keep unsupported content resident by stable reason. Depends on: `VRP-070`.
- [x] `VRP-072 [SOL]` Add/fix LOD, shadows, and small-detail policy only from Android GPU/visual evidence. Depends on: `VRP-071`.
- [x] `VRP-073 [TERRA]` Regenerate exact resident/logical/prototype/slot/bucket/cell/package metrics. Depends on: `VRP-072`.
- [x] `VRP-074 [SOL]` Tune cell/envelope/capacity only through before/after route evidence; retain 20% headroom and zero overflow. Depends on: `VRP-073`.
- [x] `VRP-075 [SOL]` Require packed runtime map `MaterialMeshInfo` entities `<= 24,000` and representative enabled proxies `<= 8,000`, or amend with measured evidence. Depends on: `VRP-074`.

**VRP-072 execution checklist required to advance from `56 / 80` to `57 / 80`:**

- [x] Freeze the exact before revision, APK/device route, Android captures, harsh-black-shadow evidence, and the observed low-FPS evidence without conflating the blue Autobahn/canal or missing-bridge defects.
- [x] Inventory every eligible logical/prototype part by stable source identity, semantic category, LOD flags, shadow flags, bounds, and fixed render-policy bucket.
- [x] Attribute the visual defect to the smallest supported row set; retain LOD0 because the candidate contains no alternate baked LOD recipes and an invented LOD chain would be unevidenced.
- [x] Implement one deterministic candidate-only policy: Vegetation/Prop and renderer rows at or below the established `1 m` world-size small-detail threshold stop casting/static shadows while preserving receive-shadows; large structures and resident exceptions remain unchanged and unknown input fails closed.
- [x] Prove focused classification, exact source identity, LOD0 retention, fixed-policy regeneration, protected/production isolation, and no manual VRP-074 envelope/capacity tuning.
- [x] Regenerate the exact inventory and database transactionally, prove unchanged-input byte determinism, and pass two identical real candidate bakes.
- [x] Pass the checked `48/48` render-virtualization suite, `17/17` Android visual-quality suite, and `93/93` Editor-first functional aggregate.
- [x] Build exactly one fresh checked profiler APK from the pushed implementation revision and record its revision, byte size, and SHA-256.
- [x] Install that APK once only to the authorized Android device and capture a matched route proving the harsh black patches improve without holes, popping, shadow leakage, overflow/deficit, gameplay regression, or a GPU/frame regression.
- [x] Update both trackers, check `VRP-072`, and advance to `57 / 80` only after the Android visual/GPU acceptance is complete; do not start VRP-073 early.

**Independent correctness queue (does not advance the 80-item VRP checklist):** the blue-Autobahn defect has a bounded Editor correction: the candidate blue override now requires both the canonical canal-water material and canonical canal-surface prefab, while the protected Autobahn scene validator requires every replacement renderer material slot to match its approved original opaque road prefab. Focused material isolation and the full `1,108`-tile / `5,540`-material-slot scene audit pass; final acceptance remains pending visual confirmation on the fresh production Android package. The intended canal-water presentation remains blue. The building-road clearance correction is accepted on the rebuilt Android candidate after the reconciled generator, inventory, database, capacity, and direct-bake gates: reviewed base and urban device views show generated buildings beside, rather than intersecting, rendered roads. All six mandatory Editor-first functional rows are now visibly accepted: correct selection-panel images, footprint-scaled building markers, vehicle world/roster selection, helicopter/rope/drop unit production, spawned-unit visibility/interaction, and canonical building placement/spawn. Their Android final functional confirmation remains pending. Road-over-water-without-bridge remains a separate open defect and is not evidence for the Autobahn material correction. The urban Android view also exposes harsh black shadow patches; that evidence belongs to VRP-072's shadow/small-detail policy gate and is not conflated with these correctness defects.

The latest Xiaomi Tent rerun remains a historical Android rejection: one Recruit consumed `25` materials and raised canonical units/source keys from `5005` to `5006` plus rendered models from `22` to `23`, but the then-current package bypassed helicopter/rope/drop presentation and reproduced the wrong Contractor Tent portrait and oversized cyan marker. Subsequent bounded Editor work visibly accepted those functional behaviors through the real Deploy and in-map interaction routes, but a package from that accepted state has not yet received device confirmation. The same historical focused base view fell as low as the on-screen `19 FPS`; that remains VRP-072 Android evidence and does not advance this checklist.

**Editor-first functional acceptance queue (all rows passed; Android final confirmation is dependency-ready):**

- [x] Correct selection-panel image mapping for the selected owner, including Contractor Tent.
- [x] Selection marker matches the selected owner's rendered presentation bounds, while direct-click ownership remains independent from broad gameplay footprints.
- [x] Vehicle world and roster selection resolve the intended canonical vehicle.
- [x] Unit production visibly runs the helicopter arrival, rope/drop, and completion sequence.
- [x] The spawned unit visibly exists after delivery and is interactable through the normal selection path.
- [x] Building placement/spawn visibly completes with canonical ownership, footprint, interaction, and presentation intact.

Each row is a separate bounded correctness item with its own checked-wrapper and visible Editor evidence. Passing one row cannot close another. Android is reserved for final functional confirmation and performance evidence after all six Editor rows pass.

**Exit:** The complete candidate rendering representation is bounded for CPU and has enough GPU headroom for the amended 54-FPS target.

### Phase 8: Lifecycle, Determinism, And Parity

- [x] `VRP-080 [SOL]` Pass direct logical parity for every source renderer and resident exception. Depends on: `VRP-075`.
- [x] `VRP-081 [SOL]` Pass materialized parity for every canonical camera and state variant. Depends on: `VRP-080`.
- [x] `VRP-082 [SOL]` Pass first readiness, failure cleanup, reset/retry, and two packed load/unload cycles. Depends on: `VRP-081`.
- [x] `VRP-083 [SOL]` Prove camera travel issues zero map scene/Addressables/static-streamer operations. Depends on: `VRP-082`.
- [x] `VRP-084 [SOL]` Prove camera travel changes zero simulation entity counts and gameplay state. Depends on: `VRP-082`.
- [x] `VRP-085 [TERRA]` Prove two full Bake All runs produce identical virtualization logical hashes/counts/ordering/bytes. Depends on: `VRP-082`.
- [x] `VRP-086 [SOL]` Pass five fixed-camera screenshot sets plus camera-edge traversal video/image evidence with no holes or stale state. Depends on: `VRP-083` through `VRP-085`.

**Exit:** The candidate is deterministic, visually equivalent when materialized, and lifecycle-safe.

### Phase 9: Android 54 FPS Acceptance

- [x] `VRP-090 [SOL]` Build the exact clean candidate APK through the documented Unity wrapper and record revision/SHA-256/package ownership. Depends on: `VRP-086`.
- [x] `VRP-091 [SOL]` Install and capture all representative 120-second routes with diagnostics disabled. Depends on: `VRP-090`.
- [x] `VRP-092 [SOL]` Pass FPS/frame/CPU/GPU targets from Section 3 on every representative route. Depends on: `VRP-091`.
- [x] `VRP-093 [SOL]` Pass zero allocation, zero overflow, bounded rebuild, and no synchronous-completion gates. Depends on: `VRP-091`.
- [x] `VRP-094 [SOL]` Capture peak/retained/graphics memory and compare exact Phase 0/current-candidate baselines. Depends on: `VRP-091`.
- [x] `VRP-095 [SOL]` Pass Android visible/off-camera destruction and proxy-recycling state tests. Depends on: `VRP-091`.
- [x] `VRP-096 [SOL]` Pass offline Menu -> Match -> Menu -> Match -> Menu lifecycle with full unload. Depends on: `VRP-091`.
- [x] `VRP-097 [SOL]` Pass the final 2-minute thermal route without dropping below accepted budgets. Depends on: `VRP-092` through `VRP-096`.

**Exit:** 54-FPS-class dense-city presentation is accepted on the real target Android device.

### Phase 10: Cutover And Closeout

- [x] `VRP-100 [TERRA]` Update architecture/workflow/output-ownership documentation with exact accepted types, hashes, counts, and rollback steps. Depends on: `VRP-097`.
- [x] `VRP-101 [SOL]` Update parent parity/residency clauses to accepted virtualized semantics and close superseded candidate-only assumptions. Depends on: `VRP-100`.
- [x] `VRP-102 [SOL]` Authorize production `EntityScene + VirtualizedProxyPool` only after every parent production prerequisite also passes. Depends on: `VRP-101` and parent gates.
- [ ] `VRP-103 [SOL]` Validate production package, Android artifact, and rollback restore from the exact cutover revision. Depends on: `VRP-102`.
- [ ] `VRP-104 [SOL]` Retire frozen static production ownership only when the parent tracker separately authorizes deletion; otherwise retain it untouched. Depends on: `VRP-103`.

**Exit:** Production uses the accepted representation with a tested rollback path and complete evidence.

## 15. Required Validation Commands

Follow repository `AGENTS.md`.

Windows:

```powershell
$unityExe = & Tools/CI/ResolveUnityEditor.ps1

powershell.exe -NoProfile -ExecutionPolicy Bypass `
  -File Tools/CI/InvokeUnityExecuteMethodValidation.ps1 `
  -UnityExe $unityExe `
  -ProjectPath (Get-Location).Path `
  -ExecuteMethod OperationMapRenderVirtualizationValidation.RunFocusedValidation `
  -LogFile "$env:TEMP\warline-render-virtualization-focused.log" `
  -RequiredPassMarker "[OperationMapRenderVirtualizationValidation] result=Passed" `
  -TimeoutSeconds 900
```

Use `Tools/CI/InvokeUnity.ps1` for Unity Test Framework/PlayMode runs. Every run needs an explicit log, timeout, exact filter/marker, and nonzero/missing-marker failure handling. Confirm no active Unity process owns this project first. Do not terminate an existing Editor.

macOS:

```bash
Tools/CI/invoke_unity_macos.sh --timeout 900 --log /private/tmp/warline-render-virtualization-focused.log -- \
  -quit -executeMethod OperationMapRenderVirtualizationValidation.RunFocusedValidation
```

Never add `-batchmode` on macOS and never bypass the wrapper.

For every stable step also run:

```text
git diff --check
git status --short
```

Do not claim a Unity, Android, profiler, package, or device validation that did not run to completion.

## 16. Agent Execution Protocol

On every continuation:

1. Re-read `AGENTS.md`, this tracker, and the parent tracker clauses touched by the selected item.
2. Inspect branch/worktree and preserve unrelated changes.
3. Select exactly one dependency-ready unchecked item.
4. Respect its `[TERRA]`/`[SOL]` label.
5. Do not combine schema, ownership mutation, runtime scheduling, Android tuning, and production cutover in one commit.
6. Add or update focused validation with the implementation.
7. Run only the validations actually needed for that bounded step.
8. Update the checkbox, progress summary, and validation log honestly.
9. Commit only the stable step and push it.
10. Stop when the next item is `[SOL]` and the active model is Terra.

Forbidden shortcuts:

- reducing city density before representation optimization is measured;
- hiding source render rows without a complete logical parity row;
- using `Disabled` on simulation entities;
- invoking `Schedule(...).Complete()` in a steady-state virtualization path;
- using per-frame LINQ, managed lists/dictionaries, string formatting, scene searches, reflection, runtime asset loading, or mutable static state;
- creating/destroying proxy entities during camera travel;
- changing shared render components during camera travel;
- enabling production mode from a scripting define or map-id special case;
- weakening overflow, matrix/bounds, ownership, readiness, package, or lifecycle gates;
- mutating accepted/frozen production assets without the existing transactional authorization.

## 17. Progress Summary

| Phase | Status |
|---|---|
| Phase 0: Evidence and amendment | Complete; Android failure, design authorization, exact eligibility inventory, raw PlayerLoop-owner profile, and the fixed-camera/route reconciliation are recorded at exact revision `f78a338692f6913d8034a14c0313c0b198bf7d8b` |
| Phase 1: Additive schema and pure contracts | Complete; all default-safe schema, identity/fingerprint, spatial-cell, fixed-policy, capacity-sweep, and canonical-report contracts accepted |
| Phase 2: Deterministic database builder | Complete through the VRP-070 generated-infrastructure expansion; generated schema/ownership, exact normalized source inventory, shared compound recipes, logical placements, spatial index, exact 12x9/9x12 all-grid capacity sweep, transactional config/report, unchanged-input determinism, and persisted source-to-logical field parity are accepted for render-only Vegetation/Prop, complete House and Shop families, and component-safe generated Infrastructure. CityHall remains atomically resident under an exact unsupported-policy exclusion. Unsupported render policies and non-generated/resident exceptions remain explicit. |
| Phase 3: Candidate pool baking | Complete for the authorized direct-bake gate; the historical VRP-038 unsaved gate produced identical fingerprint `4d859a9a...2d17d`, one schema-1 database (`22` prototypes, `26` parts, `9,721` placements, `1,635` cells, two buckets), `704` fixed disabled leaf slots, `11,299` exact eligible render-entity rows marked baking-only, and `70,710` exact resident render-entity exceptions from `82,009` independently counted stable-owner renderer rows. VRP-051 later persisted the same authoring/output contract and enabled only the candidate definition. The historical `82,797` packed inventory remains the renderer/material-row count; its `788` additional rows are resident multi-material expansion and are not source-ownership buffer rows. Production remains `StaticSceneChunks + ResidentEntities`, production cutover is `0`, and protected accepted/frozen/production/Addressables ownership remains unchanged. No packed EntityScene/package was produced by this step. |
| Phase 4: Jobified runtime assignment | Checklist complete; the ordered camera boundary, exact map-generation native initialization, pure guard-envelope decision, bounded selection, deterministic assignment, parallel apply, apply-to-Entities-Graphics dependency boundary, stable apply no-op gate, bounded instrumentation contract, and explicit persistent-owner/database-blob lifetime proof are accepted. The persistent coordinator now schedules selection, assignment, command publication, apply, and initial-view completion for the candidate camera envelope; a camera update wholly inside the materialized guard publishes no jobs, command version, or rebuild. Initialization and dynamic job results publish bounded placement/logical-part/resident/capacity and slot/activity/assignment/overflow/deficit/version/rebuild metrics. Selection now reads the exact map-owned canonical-state buffer; the current render-only pilot owns its valid zero-length form. |
| Phase 5: Render-only pilot | Complete and Android expansion accepted for the exact frozen Vegetation/Prop pilot. The candidate remains `EntityScene + VirtualizedProxyPool` with 9,721 placement identities, 22 repeated prototypes/26 parts, 1,635 cells, 704 fixed slots, 11,299 stripped eligible rows, and 70,710 resident authoring rows; the three excluded Prop rows remain resident. Packed PlayMode retains its accepted slow/fast pan, zoom, rotation, teleport, guard-band no-op, deterministic return, 319-slot return, and zero-overflow proof. After the first Android sample exposed zero active proxy state and 69.040 ms/frame of unchanged presentation scans, VRP-054 added projected-frustum initial residency, terminal map ancestry, and exact packed resident-row classification in bounded 12,000-row passes. The corrected APH-803 stationary Android capture reports 122/704 active slots, 25 active cells, 114 active placements, and zero overflow/deficit. Across frames 300..799, average frame time improves from 93.94 ms to 21.62 ms and CPU-main p95 from 110.66 ms to 35.88 ms; faction-tint backfill falls from 51.722 to 2.239 ms/frame and mass render settings from 17.318 to 2.171 ms/frame. No replacement dominant owner appears, and the retained post-capture view shows intact dense-city presentation/HUD/minimap without a gross hole; exact historical camera-pose equivalence remains qualified because the retained before artifact did not serialize its pose. Phase 6 is dependency-ready, but Phase 9's amended 18.6 ms/54 FPS acceptance remains open. Production remains `StaticSceneChunks + ResidentEntities` and `productionCutover=0`. |
| Phase 6: Building and attachment state | Complete through accepted VRP-067 Android House/Shop destruction-transition evidence. Deterministic contiguous state-owner indices remain owned by 4,933 canonical gameplay buildings: 3,638 complete House owners and 1,295 complete Shop owners. Candidate-only intact-visual normalization removes the embedded `Destroyed` sibling from realized House/Shop intact visuals, eliminating exactly 4,396 duplicate renderer/material rows without mutating source, accepted, frozen, or production prefabs. The normalized database contains 78,401 packed rows, 24,904 eligible logical rows, 24,717 selected physical renderers, 52,928 resident physical renderers, 19,614 placements, 9,879 prototypes, 13,595 parts, 1,660 cells, and 3,858 fixed slots. Its exact 12x9/9x12 all-grid camera/projection sweep retains at least 20% per-bucket headroom. On the paired Xiaomi, House used 1,856 slots/106 cells/1,260 placements and Shop used 1,765 slots/108 cells/1,225 placements; both advanced canonical sequence `1`, reported zero overflow/deficit, and visibly replaced the logged intact target with its destroyed presentation. House post-transition p95 was 24.951 ms (about 40 FPS) and Shop 33.284 ms (about 30 FPS), so this accepts state/visual/capacity correctness only; Phase 9's amended 18.6 ms/54 FPS gate remains open. A later Android user review exposed that canonical map buildings were excluded from click focus despite their resident gameplay state; the Windows/Editor correction now selects live canonical buildings without granting movement authority, while device confirmation and ECS-backed building production remain open. Production remains `StaticSceneChunks + ResidentEntities` with `productionCutover=0`. |
| Phase 7: Coverage, LOD, and GPU headroom | Complete through accepted VRP-075. After the independently tracked corrected building-road layout was transactionally regenerated, the candidate virtualizes 38,428 component-safe, supported-policy generated Infrastructure rows under 21,621 owners with eligible render rows. Exact material-surface classification records 72,264 opaque rows (`58,296` eligible), 3,769 alpha-clipped rows (`3,629` eligible), and 484 transparent rows (`0` eligible). All transparent rows remain resident under the closed unsupported-render-policy reason. VRP-072 retained LOD0 because no alternate baked LOD recipes exist and moved exactly 12,185 eligible Vegetation/Prop/sub-meter detail rows into shadow-off buckets while preserving receive-shadows and all large/resident exceptions. VRP-073 regenerated and reconciled `76,517` authored source rows, `61,925` eligible logical rows, `61,783` selected packed physical renderers, `14,017` packed resident logical rows, `40,460` placements, `9,107` prototypes, `12,293` parts, `1,934` occupied cells, four fixed buckets, and `7,784` fixed slots; the packed candidate contains zero eligible source rows. On exact Samsung `SM-S918B` / `R5CTC1J02VB`, matched fresh-package evidence reduced pitch-black urban pixels from 10.527% to 4.323-4.932%, retained intact geometry without visible holes, popping, or leakage, and reported zero overflow/deficit through command versions 1-4; the metrics traversal held 54-55 FPS after rebuilds. VRP-074 retained the existing capacity and repaired only the proven far-view-to-tactical-follow frustum transition; the matched chained route passed command versions 1-5 with zero overflow/deficit and at most `1,193/7,784` enabled slots. VRP-075's exact two-pass packed query records `14,592/24,000` shipped `MaterialMeshInfo` entities and the fixed pool remains `7,784/8,000`, so both provisional design limits pass without amendment. Amended Phase-9 54-FPS acceptance remains open. Production remains unchanged with `productionCutover=0`. |
| Phase 8: Lifecycle, determinism, and parity | Complete through accepted VRP-086. Direct logical/resident parity remains accepted from VRP-080 and exhaustive materialization from VRP-081. The current packed candidate passes two complete readiness/load/unload cycles plus failure cleanup/reset/retry with zero stale ownership. Full production Menu -> Match camera routes prove normal in-map camera travel starts zero map-scene/Addressables/static-streamer operations and changes zero authoritative gameplay state in a controlled quiescent interval. Two complete current virtualization Bake All passes reproduce every persisted logical byte, database content/order hash, count, capacity, and direct packed fingerprint. The live production-camera evidence set passes five fixed views before/after a seven-frame cell-edge traversal with zero holes, stale logical bindings, overflow, or deficit. |
| Phase 9: Android 54 FPS acceptance | Complete through accepted VRP-097. Exact Samsung `SM-S918B` / `R5CTC1J02VB` passed the complete diagnostics-disabled representative set and every accepted route reports `0 B/frame` managed allocation with zero overflow/deficit/failure matches. Current focused validation proves initial/map-generation/camera/visual-state rebuild ownership, stable-envelope no-op behavior, asynchronous apply dependency ordering, and completion only at initialization replacement, owner removal, or teardown. The corrected 120-second current-package memory route retains `2,140,028 KiB` total PSS, `2,232,260 KiB` total RSS, and `901,816 KiB` graphics: respectively `14.132%`, `15.025%`, and `17.757%` below the exact frozen Phase 0 steady/match artifacts, while native heap is `3.347%` higher. Fresh exact-device state evidence passes visible intact-to-destroyed replacement, stable compatible intact reuse, off-camera canonical destruction with zero materialization/no disturbance, destroyed-only arrival and return, and terminal zero overflow/deficit. The current accepted package also completes two Menu -> Match -> Menu cycles with fresh candidate/support EntityScene streams on both entries and identical clean post-unload menu frames. The cooled final thermal route sustains `59.980` average FPS / `59.997` p10 FPS, `16.672/16.670/16.740 ms` average/p95/p99 frame time, `11.568/13.218 ms` CPU average/p95, `13.849/14.001 ms` GPU average/p95, and `0 B/frame` allocation for `120.007 s`; device thermal status remains `0`, battery drain is `0%`, and start/end frames retain complete live Match presentation. |
| Phase 10: Cutover and closeout | In progress through accepted VRP-102. The protected production definition and thin runtime binding now use `EntityScene + VirtualizedProxyPool`, production Addressables own the single accepted dense EntityScene and zero current-map static manifest/chunk entries, and the rollback checkpoint binds the pre-cutover revision plus definition/runtime/Addressables/static-package hashes. VRP-103 next requires a fresh clean production package, exact Samsung evidence, and rollback-restore proof before any static output is deleted. |

Checklist progress: `78 / 80` complete.

## 18. Validation Log

| Date | Step | Validation | Result | Notes |
|---|---|---|---|---|
| 2026-08-08 | `VRP-092` GC-fallback fixed-route exact-device attempt | User explicitly removed the unplugged-device precondition while leaving every performance/thermal/correctness gate unchanged; official ADB 37 verified exact Samsung `SM-S918B` / `R5CTC1J02VB`; exact `543,548,337`-byte APK SHA-256 `1bb34e92cf01d01ac734b6cb7b721e0b170924132b315a9265e7940b1a6d0f73`; one `install --no-streaming -r` returned `Success`; rejected evidence `Build/AndroidDenseCandidate/Evidence/2026-08-08-vrp092-gc-fallback-f0d70aff2` | Rejected; checklist remains `69 / 80`; this APK will not be reinstalled or reused for another route | The cold fixed route visibly reached Match and completed `6,627` samples / `120.013 s` after `15.001 s` warmup with zero ordinary `FrameRateDiag`/slow-frame/freeze-log or fatal matches. The bounded fallback names `main-thread-allocated-bytes` and records zero allocated bytes, while battery stayed at `97%` and device battery temperature moved from `32.2 C` before launch to `34.1 C` after capture. Average `18.110 ms` / `55.219 FPS` and GPU `12.552/12.943 ms` pass, but p10 FPS `40.021`, frame p95/p99 `25.008/41.677 ms`, and CPU `18.112/32.813 ms` fail unchanged gates. There are `674` frames at or above `20 ms`; the dominant at-or-above-`25 ms` spike gap is exactly `12` frames (`327` occurrences), proving a periodic CPU owner remains. The app was stopped for cooldown. No other route is attempted with this rejected package, no gate is weakened, and the two independent map defects remain separate. |
| 2026-08-08 | `VRP-092` GC-fallback replacement release package | Pushed implementation head `f0d70aff2`; checked GUI-licensing clean build `Build/Logs/vrp092-f0d70aff2-gc-fallback-release-apk.log`; package marker passed with zero missing-script matches; exact APK `543,548,337` bytes / SHA-256 `1bb34e92cf01d01ac734b6cb7b721e0b170924132b315a9265e7940b1a6d0f73`; `git diff --check` | Fresh replacement package accepted for one future install; checklist remains `69 / 80`; it has not been installed | The package carries the release allocation fallback and records exact pushed source `f0d70aff2`; its dirty flag is still explained only by the preserved generated report/recovery scenes. The verified Samsung has cooled to thermal status `0` / skin `34.7 C`, but remains AC-powered. Installation and the fixed-route retry remain deferred until power is physically disconnected, matching the prior rejection's cooled/unplugged precondition. No rejected APK is reinstalled and no metric is claimed. |
| 2026-08-08 | `VRP-092` release GC-allocation fallback after exact-device counter rejection | Checked GUI-licensing focused `Build/Logs/vrp092-gc-fallback-focused.log` `[AndroidPerformanceRecorderValidation] result=Passed tests=26`; aggregate `Build/Logs/vrp092-gc-fallback-aggregate.log` passed `93/93`; APH-808 `Build/Logs/vrp092-gc-fallback-aph808.log` passed; `git diff --check` | Editor-accepted bounded measurement correction; checklist remains `69 / 80`; no replacement package is claimed | APH-804 keeps its profiler-counter-only fail-closed contract. Only VRP-092 falls back, when `GC Allocated In Frame` is unavailable in a clean release player, to allocation-free deltas from `GC.GetAllocatedBytesForCurrentThread`; the JSON names the exact source and still requires at least one sample. The already rejected package and its failing timing evidence remain rejected. A replacement package is deferred until the exact Samsung is unplugged/cooled because the prior AC-powered route ended at thermal status `1`; no performance, allocation, thermal, or correctness gate is weakened. |
| 2026-08-08 | `VRP-092` first diagnostics-disabled fixed-route package attempt | Official ADB 37 verified exact Samsung `SM-S918B` / `R5CTC1J02VB`; exact `543,548,429`-byte APK SHA-256 `5175c2f3aa5a80e76c4c896aa42b117fb6d6cde4ea6b89da2aa45bce6a2a251e`; one `install --no-streaming -r` returned `Success`; rejected evidence `Build/AndroidDenseCandidate/Evidence/2026-08-08-vrp092-diagnostics-disabled-5355f5fb4` | Rejected; checklist remains `69 / 80`; this APK will not be reinstalled or reused for another route | The correctly shell-quoted cold fixed route reached visible Match and recorded exactly `6,506` frames / `120.013 s` after `15.010 s` warmup with zero ordinary `FrameRateDiag`/slow-frame/freeze-log matches. It rejected fail-closed because the release player exposed neither the profiler GC-allocation counter nor the already-known batch counter. Independent raw timing also fails acceptance: average `18.447 ms` / `54.211 FPS` passes only the amended average boundary, while 10th-percentile FPS is `40.007`, p95 is `33.335 ms`, p99 is `49.995 ms`, CPU average is `18.447 ms`, and CPU p95 is `34.480 ms`; GPU average `13.955 ms` and p95 `14.633 ms` pass. The post-route device was AC-powered at thermal status `1` with `41.1 C` skin, so a cooled/unplugged replacement-package run is required; no thermal exemption or performance gate is weakened. |
| 2026-08-08 | `VRP-092` diagnostics-disabled 120-second release-recorder contract and fresh checked package | Final checked GUI-licensing focused `Build/Logs/vrp092-diagnostics-disabled-recorder-focused-final.log` `[AndroidPerformanceRecorderValidation] result=Passed tests=25`; final aggregate `Build/Logs/vrp092-diagnostics-disabled-recorder-aggregate-final.log` `[EditorFirstProductionFunctionalBatchValidation] result=Passed suites=8 tests=93`; final APH-808 `Build/Logs/vrp092-diagnostics-disabled-recorder-aph808-final.log` passed; pushed implementation head `5355f5fb4`; checked clean release build `Build/Logs/vrp092-5355f5fb4-release-candidate-apk.log` passed with zero missing-script matches; exact APK `543,548,429` bytes / SHA-256 `5175c2f3aa5a80e76c4c896aa42b117fb6d6cde4ea6b89da2aa45bce6a2a251e`; rejected earlier focused/aggregate and licensing-failed APH-808 attempts remain non-acceptance evidence; `git diff --check` | Editor/package-accepted measurement checkpoint; checklist remains `69 / 80` and VRP-092 remains open pending one exact-device install and matched route results | `VRP-092` now has an exact release-only route contract for `fixed`, `slow-pan`, `fast-pan`, `zoom`, `tactical-follow`, `destruction`, `fullscreen-map`, `steady`, and `thermal`. Each run enforces Mobile/60, a 15-second post-ready warmup, exactly 120 captured seconds, route-specific output, clean release provenance, CPU/GPU timings, GC allocation, memory, and battery evidence. Only VRP-092 suppresses standard frame-rate/freeze/slow-frame logs while it owns measurement, so their periodic entity queries, string construction, and log writes cannot contaminate CPU acceptance. APH-804 retains its existing 60+600-second behavior and full render-counter requirement; only VRP-092 tolerates the device's unavailable batch counter. The package records exact pushed source `5355f5fb4`; its dirty flag is explained solely by the explicitly preserved generated report and recovery scenes, which remain unstaged and were not packaged as implementation. The authorized recovery ladder precisely removed one orphaned failed wrapper tree, restarted only the verified stale licensing client after confirming no Editor, and then passed the same checked wrappers without bypassing their markers. No performance result, checkbox, protected/production asset, render policy, capacity, or `productionCutover=0` value changed. The two independent map defects remain separate. |
| 2026-08-08 | `VRP-091` complete release-equivalent representative Android route set | Pushed source head `f79fa966259ae21b915940ec7a443834884b07ac`; checked clean build `Build/Logs/vrp091-f79fa9662-release-candidate-apk.log`; exact APK `Build/AndroidDenseCandidate/WarlineCapture-DenseCityCandidate.apk`, `543,539,773` bytes / SHA-256 `63a51ed4dbd55e7a14b1f007b176bd434d0ec5570a17fc22b70f9dcde3f88986`; official ADB 37 verified `SM-S918B` / `R5CTC1J02VB`; one `install --no-streaming -r` returned `Success`; same-package evidence `Build/AndroidDenseCandidate/Evidence/2026-08-08-vrp091-release-routes-f79fa9662` | VRP-091 accepted; checklist advances to `69 / 80`; VRP-092 remains rejected/open | All required 120-second routes were captured on the same release-equivalent package with opt-in profiler markers and top-system diagnostics disabled: fixed/steady/thermal, slow pan, fast pan, zoom min/max/back, tactical follow, full-screen-map transitions, and selected-building destruction, plus a visible two-cycle Menu -> Match -> Menu -> Match -> Menu lifecycle. The ordinary release low-FPS log remained active, so its `54.756-54.934 FPS` fixed/pan/zoom/tactical observations and the failing full-screen-map (`51.656 FPS` / `19.390 ms`), destruction (`52.520 FPS` / `19.040 ms`), and intermittent CPU samples are diagnostic only and do not close VRP-092. Route scans report zero overflow/deficit/exception failure matches. The white-screen correction remains accepted, production/protected assets and `productionCutover=0` remain unchanged, preserved worktree files remain unstaged, and the two independent map defects remain separate. |
| 2026-08-08 | `VRP-091` fresh diagnostics-suppression package and two-cycle Android acceptance | Pushed implementation head `24afba97b5cc1fb10c78725ca474f95a2097c854`; checked GUI-licensing clean build `Build/Logs/vrp091-24afba97b-profiler-apk.log`; package marker passed; zero missing-script matches; exact APK `573,342,961` bytes / SHA-256 `60a3659ae433defc76787421ce38a47a73d9e956d93207292717d42f04920d0b`; official ADB 37 verified `SM-S918B` / `R5CTC1J02VB`; one `install --no-streaming -r` returned `Success`; device evidence `Build/AndroidDenseCandidateProfiler/Evidence/2026-08-08-vrp091-match-diagnostics-24afba97b` | White-screen defect and two-cycle lifecycle accepted on the fresh package; checklist remains `68 / 80` pending the complete diagnostics-disabled representative route set | Cold main-page Deploy visibly reached Match without the full-screen diagnostics panel. Cycle 1 and cycle 2 each used the normal visible Pause and Exit controls, emitted one `[OperationMapSourceScene] stage=SceneUnloadRequested`, and returned to the real main page; accepted Match frames show 56 and 54 FPS, and the combined log has zero `MenuDiagnostics`, `NullReferenceException`, `MissingReferenceException`, or `result=Failed` matches. The first cycle's center Exit probe landed on the development FPS overlay; a normal tap on the same visible Exit bar away from that overlay passed and was repeated in cycle 2. No rejected APK was reinstalled. The diagnostics-disabled 120-second routes remain mandatory and are not claimed by this lifecycle proof. No protected/production asset, capacity, render policy, `productionCutover=0`, preserved worktree file, or independent map defect changed. |
| 2026-08-08 | `VRP-091` Match-route legacy diagnostics suppression | Rejected installed/package head `73a9eb5e6afcaae664c25623ec4062e7bec94ac0`; live device evidence `Build/AndroidDenseCandidateProfiler/Evidence/2026-08-08-vrp091-immediate-loading-73a9eb5e6/05-live-white-diagnostic.png` and `06-after-diagnostics-close.png`; focused checked GUI-licensing `Build/Logs/vrp091-match-diagnostics-suppression-focused-rerun.log` `9/9`; aggregate `Build/Logs/vrp091-match-diagnostics-suppression-aggregate-rerun.log` `93/93`; APH-808 `Build/Logs/vrp091-match-diagnostics-suppression-aph808.log` passed | Editor-accepted bounded correction; checklist remains `68 / 80`; installed `73a9eb5e6` package is rejected and will not be reinstalled | The persistent white surface is not loading or a stopped simulation: the live screenshot contains the legacy full-screen runtime log panel while diagnostics report about 54 FPS, `playRequested=1`, `simulationActive=1`, and `5,005` units. `MenuDiagnosticsUiSystemHelper` now closes and refuses to reopen only that full-screen panel while the authoritative shell route is Match; the small FPS display remains available, and explicit full logs remain available on menu routes. The first focused attempt rejected immediately on a missing namespace import before its wrapper timeout, and the first aggregate invocation rejected on an incorrect execute-method class; neither is counted. No protected/production asset, capacity, render policy, `productionCutover=0`, preserved worktree file, or independent map defect changed. |
| 2026-08-08 | `VRP-091` immediate opaque-loading completion correction | Pushed predecessor/package head `2d6437c686ff42bb1b29028b61c1441ad230362b`; checked clean package `Build/Logs/vrp091-2d6437c68-clean-profiler-apk.log`; exact APK `569,812,926` bytes / SHA-256 `96985c07073582f4ff6045f9d7560a763f45bcf5541e3186eb793c034606e2e2`; one official-ADB-37 `--no-streaming -r` install only to verified `SM-S918B` / `R5CTC1J02VB`; rejected device evidence `Build/AndroidDenseCandidateProfiler/Evidence/2026-08-08-vrp091-loading-only-2d6437c68`; isolated checked GUI-licensing `Build/Logs/vrp091-immediate-loading-completion-narrow.log` `1/1`; aggregate `Build/Logs/vrp091-immediate-loading-completion-aggregate.log` `93/93`; APH-808 `Build/Logs/vrp091-immediate-loading-completion-aph808.log` passed; `git diff --check` | Editor-accepted third correction; checklist remains `68 / 80`; the installed `2d6437c68` package is lifecycle-rejected and will not be reinstalled | The loading-only package still showed the opaque curtain over live Match simulation and emitted no source-scene unload request, ruling out both Pause ordering and `ExitMatchHud` animation as the remaining cause. The final zero-duration `ShowLoading` command was visually applied, but its completion still depended on coroutine scheduling before ECS could clear `IsTransitionRunning`; packaged Android never delivered that acknowledgment. A single-command opaque loading sequence now cancels prior motion, installs and resets the loading layer, and invokes its sequence completion synchronously only after opacity is established. Development diagnostics record command start, completion, and ECS completion flush. The broader shell runner twice rejected at its pre-existing test-7 default-world fixture before reaching the new regression; those failures are not counted, while the isolated exact test, authoritative 93-test aggregate, and APH-808 all pass through checked wrappers. GUI-licensing IPC recovery used the documented same-integrity checked wrapper path after the stale client and version-matched routes were rejected; wrappers and pass markers remained fail-closed. No protected/production asset, capacity, render policy, `productionCutover=0`, preserved worktree file, or independent map defect changed. |
| 2026-08-08 | `VRP-091` loading-only return-to-menu sequencing correction | Pushed replacement head `b48bda68d528d9eb91a9879e50eca48a0133ca47`; checked clean package `Build/Logs/vrp091-b48bda68d-clean-profiler-apk-version-matched.log`; exact APK `569,812,925` bytes / SHA-256 `b6fdc7c7bf3bbafec71f39eff32b8f09d751a55fde60ef4f8182b7025f278c23`; one official-ADB-37 `--no-streaming -r` install only to verified `SM-S918B` / `R5CTC1J02VB`; rejected device evidence `Build/AndroidDenseCandidateProfiler/Evidence/2026-08-08-vrp091-pause-exit-b48bda68d`; focused `Build/Logs/vrp091-return-menu-loading-only-focused.log` `8/8`; header routing `Build/Logs/vrp091-return-menu-loading-only-header-routing.log` `14/14`; rejected wrong aggregate entry point `Build/Logs/vrp091-return-menu-loading-only-aggregate.log`; corrected aggregate `Build/Logs/vrp091-return-menu-loading-only-aggregate-rerun.log` `93/93`; APH-808 `Build/Logs/vrp091-return-menu-loading-only-aph808.log` passed; `git diff --check` | Editor-accepted second correction; checklist remains `68 / 80`; the installed `b48bda68d` package is lifecycle-rejected and will not be reinstalled | The replacement package closed the visible Pause modal and installed the opaque loading layer, proving the prior popup ordering repair, but `04-main-menu-cycle1.png` and Unity-only logs show the Match continuing with `playRequested=1`, `simulationActive=1`, `5,005` units, and no `[OperationMapSourceScene] stage=SceneUnloadRequested`. The remaining coupling was the same transition sequence appending animated `ExitMatchHud` after the zero-duration loading command; `MenuBootstrap` intentionally waits for presentation completion before beginning authoritative unload. Return-to-menu now emits only opaque `ShowLoading`; after map/scene teardown completes, the existing `ExitLoading + EnterMenu` sequence replaces all HUD regions while covered. Focused ECS coverage requires exactly that one command after the separately completed Pause hide. No protected/production asset, capacity, render policy, `productionCutover=0`, preserved worktree file, or independent map defect changed. |
| 2026-08-08 | `VRP-091` pause-modal exit sequencing correction | Fresh-package evidence `Build/AndroidDenseCandidateProfiler/Evidence/2026-08-08-vrp091-representative-routes-8c138f362`; fixed, slow/fast pan, zoom, tactical-follow, selected-building destruction, and minimap/full-map captures completed; lifecycle `21-lifecycle-pause-probe.png` proves the visible Pause modal; `22-lifecycle-menu-cycle1.png`, `23-lifecycle-menu-cycle1-ready.png`, and `lifecycle-cycle1-logcat.txt` reject Exit because the opaque loading layer remained while Match simulation continued and no operation-map unload request was emitted; checked focused `Build/Logs/vrp091-match-menu-popup-close-sequencing-focused.log` `8/8`; header routing `Build/Logs/vrp091-match-menu-popup-close-header-routing.log` `14/14`; aggregate `Build/Logs/vrp091-match-menu-popup-close-aggregate.log` `93/93`; APH-808 `Build/Logs/vrp091-match-menu-popup-close-aph808.log` passed; `git diff --check` | Editor-accepted correction; checklist remains `68 / 80`; the installed package is lifecycle-rejected and will not be reinstalled | `MatchMenu` previously queued the main-menu route while the Pause popup was still the active popup owner. The shell could therefore install its opaque loading layer while the popup transition still owned the popup sequence, leaving packaged Android before teardown with `playRequested=1` / `simulationActive=1`. The action now queues a Pause hide before the route; focused ECS sequencing proves HidePopup is transition 21 while the route remains queued, and only its matching completion starts transition 22 with ShowLoading plus ExitMatchHud. All other seven fresh-package routes remain retained evidence, but VRP-091 is not claimed until a replacement package proves the complete two-cycle lifecycle. Production/protected ownership, capacity, render policy, `productionCutover=0`, preserved worktree files, and the two independent map defects remain unchanged. |
| 2026-08-08 | `VRP-091` lifecycle-repair clean profiler package and exact-device install | Pushed head `8c138f3629ca8046ed47c8f1e648d58795d0ef00`; checked clean GUI-licensing build `Build/Logs/vrp091-8c138f362-clean-profiler-apk.log`; package marker passed; zero missing-script matches; exact APK `569,056,801` bytes / SHA-256 `ff5b46aba9623ba6ff57b15e727cabbdc92275971192ef22f6b4943e7e19c328`; official ADB `37.0.1`; exact endpoint `192.168.2.33:43479`; verified `SM-S918B` / `R5CTC1J02VB`; one `install --no-streaming -r` returned `Success` | Fresh package accepted for VRP-091 capture; checklist remains `68 / 80` until the complete same-package route set passes | The build started only after focused settings/pause `8/8`, header routing `14/14`, aggregate `93/93`, and APH-808 passed. It used the checked wrapper with GUI licensing, explicit log/timeout, clean-build mode, and the existing short Gradle home; the APK was installed once only to the verified Samsung. A cold main-page Deploy reached `[LoadingGate] ready ... playRequested=1 simulationActive=1`, and the first fresh-package fixed-pose route has start/end frames; complete route-set evidence and metric acceptance remain open. The earlier VRP-090 install is rejected for lifecycle and is not reused. Production/protected ownership, capacity, render policy, `productionCutover=0`, preserved worktree files, and the two independent map defects remain unchanged. |
| 2026-08-08 | `VRP-091` Match HUD lifecycle-route enabling repair | Rejected exact-package Samsung evidence `Build/AndroidDenseCandidateProfiler/Evidence/2026-08-08-vrp091-representative-routes` after seven 120-second diagnostic routes because Pause/Settings taps passed through; checked prefab repair `Build/Logs/vrp091-match-lifecycle-controls-repair-host.log`; focused settings/pause `Build/Logs/vrp091-match-lifecycle-controls-focused.log` `8/8`; rejected first header-routing run `Build/Logs/vrp091-match-lifecycle-header-routing-focused.log` at a destroyed assistant-popup reference; corrected focused rerun `Build/Logs/vrp091-match-lifecycle-header-routing-focused-rerun.log` `14/14`; aggregate `Build/Logs/vrp091-match-lifecycle-editor-first-functional-batch-rerun.log` `93/93`; APH-808 `Build/Logs/vrp091-match-lifecycle-aph808.log` passed; `git diff --check` | Editor-accepted enabling repair; checklist remains `68 / 80`, and VRP-091 still requires one fresh checked package plus the complete same-package route set | `HeaderContent` intentionally batches through a nested Canvas without a raycaster; unlike the already working ResourceStrip, Pause and Settings had no scoped parent-canvas raycaster, so device taps reached underlying world/roster controls. Pause also lacked shell action ownership, `UiShellContentView` ignored the existing Pause popup request, and the authored pause menu buttons had no actions. The repair gives only those two header buttons scoped unsorted Canvas/raycaster ownership, wires Pause show/hide plus Resume/Settings/Exit actions, and serializes the existing PauseMenu prefab through the shell. The focused route then exposed a real Unity destroyed-object lifetime hole when a large popup replaced the assistant popup; Unity-safe explicit null checks now make that close/dispose path idempotent, and the rerun passes all 14 tests. Restricted-host licensing/BIOS/pipe failures were recovered through the authorized host-access GUI wrapper and version-matched client without bypassing wrappers or markers. The installed VRP-090 APK is rejected for lifecycle and will not be reused; its seven routes are diagnostic only. Production/protected ownership, capacity, render policy, `productionCutover=0`, preserved report/recovery files, and the two independent map defects remain unchanged. |
| 2026-08-08 | `VRP-090` exact clean-cache candidate profiler APK | Rejected long-cache checked wrapper `Build/Logs/vrp090-3ca09f86c-clean-profiler-apk.log`; rejected lost-environment retry `Build/Logs/vrp090-3ca09f86c-clean-profiler-apk-short-gradle.log`; rejected stale-CMake-cache retry `Build/Logs/vrp090-3ca09f86c-clean-profiler-apk-short-gradle-corrected.log`; accepted checked recovery `Build/Logs/vrp090-3ca09f86c-clean-profiler-apk-short-gradle-recovered.log`; `[DenseCityCandidateAndroidPackage] result=Passed`; exact pushed revision `3ca09f86c9a99fb3dee48e4548bf4a5204b2ada1`; clean build-cache mode; APK `567,845,052` bytes / SHA-256 `7e8efc51b32090ae39cd73a537dae71abad18b9350b5a5a49b2daddc21154cbc`; zero missing-script matches; `git diff --check` | Passed; checklist advances to `68 / 80`, and VRP-091 is dependency-ready | The first checked build failed closed after native compilation because Gradle/Ninja resolved a GameActivity header through a path longer than 260 characters. The repository-approved process-scoped `.g` Gradle cache was then inherited correctly, but a generated ignored `.utmp` CMake cache still embedded the old `Library/GradleCache` path. After verifying no Editor and identifying only the two wrapper-created Unity OpenJDK Gradle 9.1 daemons, Gradle's own `--stop` command stopped them and only the workspace-local rebuildable `.utmp` cache was removed. The final identical clean checked build regenerated native metadata from `.g`, exited successfully, and passed exact candidate EntityScene/package ownership with `productionEntitySceneIncluded=0`. Build provenance reports `dirty=True` only because the user-preserved Android runtime-content report and seven `_Recovery` scene/meta pairs were present; local `HEAD` and `origin/main` both equal the recorded revision, and no tracked implementation or protected/production asset changed. The APK has not been installed. Blue Autobahn/canal isolation and road-over-water-without-bridge remain separate. |
| 2026-08-08 | `VRP-086` five-camera and camera-edge visual continuity | Checked recovered full production Menu -> Match PlayMode `Build/Logs/vrp086-production-five-camera-edge-accepted.log`; XML `Build/TestResults/vrp086-production-five-camera-edge-accepted.xml` `1/1`; `[DensePackedCameraVisualEvidence] result=Passed fixedViews=5 fixedCaptures=10 edgeFrames=7 captures=17 maxRepeatedMeanDelta=0.0005080157 maxRepeatedInteriorChangedRatio=0.0001595052 slotReassignments=2612 holes=0 staleBindings=0 overflow=0 deficit=0 productionCutover=0`; generated `Design/AgentReports/2026-08-08_dense_city_vrp086_camera_visual_evidence.json`; visual inspection of all five fixed-A frames plus edge frames 1/4/7; focused `Build/Logs/vrp086-render-virtualization-focused.log` `48/48`; aggregate `Build/Logs/vrp086-editor-first-functional-batch.log` `93/93`; `git diff --check` | Passed; Phase 8 completes, checklist advances to `67 / 80`, and VRP-090 is dependency-ready | The accepted route uses the real production camera after current packed readiness, captures five repeatable gameplay views, traverses seven 100-m cell-edge samples, and repeats all five views. Every capture has nonzero visual variance, no transparent holes, a bounded nonzero active set, unique slot/logical ownership, zero overflow/deficit, and a command version matching the published command state. Return views reproduce the exact sorted placement/part/bucket logical set; `2,612` physical slot reassignments are recorded as legitimate fixed-pool recycling rather than misclassified as stale content. Repeated pixels remain far below the existing `0.0025` mean / `0.01` interior thresholds. Rejected diagnostics remain explicit: the obsolete sparse pilot no longer initializes the expanded database; the first invented offsets included bare terrain; the historical overview sits outside the current active envelope; exact PNG hashes rejected harmless vehicle/pixel motion; exact physical slot IDs rejected legitimate pool reuse. GUI licensing detached one wrapper-owned Editor; after exact ownership verification, only that tree and the verified stale licensing client were restarted under `AGENTS.md`, and the same checked wrapper then reached tests. No production/protected ownership, capacity, render policy, `productionCutover=0`, preserved Android report, or `_Recovery` scene changed; blue Autobahn/canal isolation and road-over-water-without-bridge remain separate. |
| 2026-08-08 | `VRP-085` two-run full virtualization Bake All determinism | Checked GUI-licensing full gate `Build/Logs/vrp085-two-full-bake-all-determinism.log`; `[OperationMapRenderVirtualizationTwoRunBakeAll] result=Passed passes=2 logicalFiles=9 logicalBytes=131560240 logicalHash=efe4a13c...8c21 contentHash=bfb350f0...96e orderingHash=a43040ee...318 packedFingerprint=23bebb45...55a placements=40460 slots=7784 productionCutover=0`; generated `Design/AgentReports/2026-08-08_dense_city_render_virtualization_two_run_bake_all.json`; focused `Build/Logs/vrp085-render-virtualization-focused.log` `48/48`; aggregate `Build/Logs/vrp085-editor-first-functional-batch.log` `93/93`; `git diff --check` | Passed; checklist advances to `66 / 80` and VRP-086 is dependency-ready | Each pass runs the current complete virtualization generation chain: exact candidate inventory/source rows, prototype/part recipes, logical placements, spatial cells, measured-envelope capacity, transactional database asset/report, and a real direct packed bake. The gate hashes the sorted manifest of all nine persisted logical outputs and compares exact bytes, total bytes, content/order hashes, every database count, capacity, all packed ownership counts, and the packed fingerprint; protected production remains byte-identical. The historical pre-expansion Candidate Bake All no-op route is deliberately not substituted because it owns obsolete candidate-count assumptions and an unrelated preserved report. The current scene dependency hash is regenerated consistently after the new validation assembly is imported; both complete passes match. Production/protected ownership and `productionCutover=0` remain unchanged; the unrelated modified Android runtime-content report and all `_Recovery` scenes remain preserved and unstaged; blue Autobahn/canal isolation and road-over-water-without-bridge remain separate. |
| 2026-08-08 | `VRP-084` camera-travel simulation-state independence | Checked full dense production PlayMode route `Build/Logs/vrp084-dense-packed-camera-authoritative-state.log` and XML `Build/Logs/vrp084-dense-packed-camera-authoritative-state.xml` `1/1`; `[PackedCameraSimulationState] result=Passed simulationEntities=4999 buildings=4977 vehicles=22 healthOwners=4999 canonicalStates=4530`; zero-operation marker also passed; focused `Build/Logs/vrp084-render-virtualization-focused.log` `48/48`; aggregate `Build/Logs/vrp084-editor-first-functional-batch.log` `93/93`; `git diff --check` | Passed; checklist advances to `65 / 80` | After real packed readiness and the initial production-camera sample, the route pauses only `SimulationActive`, allows tracked jobs to quiesce, snapshots every live `UnitGrid + Faction + UnitHealth` owner, then moves the real camera and compares exact authoritative state before restoring the original gameplay flag. Exact rows include stable building/vehicle ownership, grid cell, faction, health, destroyed/presentation state, building production cursor, all canonical virtualized-building visual states/versions, and every non-camera runtime gameplay flag. Two rejected diagnostics showed expected continuously normalized vehicle presentation rotations while simulation time elapsed; `LocalTransform` is deliberately excluded from the authoritative gameplay fingerprint rather than masking those presentation updates. Entity counts and all canonical gameplay values are exact, not tolerance-based. Production/protected ownership and `productionCutover=0` remain unchanged; the unrelated modified Android report and all `_Recovery` scenes remain preserved and unstaged; both independent map defects remain separate. |
| 2026-08-08 | `VRP-083` zero camera-travel streaming operations | Transactional current runtime-content refresh `Build/Logs/vrp083-runtime-content-rebuild.log`; checked full dense production PlayMode route `Build/Logs/vrp083-dense-packed-camera-zero-operations-production-camera.log` and XML `Build/Logs/vrp083-dense-packed-camera-zero-operations-production-camera.xml` `1/1`; `[PackedCameraTravelOperations] result=Passed mapSceneOperations=0 addressablesOperations=0 staticStreamerOperations=0`; focused `Build/Logs/vrp083-render-virtualization-focused.log` `48/48`; corrected aggregate `Build/Logs/vrp083-editor-first-functional-batch-corrected.log` `93/93`; `git diff --check` | Passed; checklist advances to `64 / 80` | The runtime now exposes monotonic diagnostic-only operation-start counters at the existing map loader and static presentation streamer boundaries. The focused route enters the actual current packed candidate through Menu -> Match, waits for readiness, snapshots source-scene, presentation-manifest/Addressables, packed EntityScene, unload, and static-streamer counters, then moves the real production camera inside the map while proving every count remains unchanged and section/database/residency ownership remains stable. A rejected synthetic top-down fixture correctly exposed an invalid virtualization envelope; acceptance uses the production perspective projection instead of suppressing that failure. The known Editor-only Unity AI Relay exit diagnostic is excluded only for its two exact `-1073741819` signatures. An earlier aggregate shorthand used the filename rather than its real class and ran zero tests; the corrected checked entry point passed all 93. Production/protected ownership and `productionCutover=0` remain unchanged; the unrelated modified Android runtime-content report and all `_Recovery` scenes remain preserved and unstaged; blue Autobahn/canal isolation and road-over-water-without-bridge remain separate. |
| 2026-08-05 | Independent correctness: completed-bootstrap late-packed vehicle reconciliation | Checked GUI-licensing focused `Build/Logs/map-vehicle-completed-bootstrap-reconcile-editor.log` `[MapVehiclePlacementStartupCompletionValidation] result=Passed tests=7`; rejected aggregate shorthand `Build/Logs/editor-first-functional-batch-completed-bootstrap-reconcile.log` before tests because the class did not exist; checked corrected aggregate `Build/Logs/editor-first-functional-batch-completed-bootstrap-reconcile-rerun.log` `[EditorFirstProductionFunctionalBatchValidation] result=Passed suites=8 tests=93`; `git diff --check` | The fresh Samsung Armor/Transport `NoCandidates` cause is repaired and Editor-accepted; visible helicopter/rope/drop and one replacement checked package remain open; checklist progress stays `56 / 80` | The fresh device evidence proved all 22 authored vehicle presentations were present but remained neutral/source-stale. A bootstrap stream can persist `MapVehiclePlacementProgressState` as queued and hidden before the candidate EntityScene arrives. Once the positive readiness contract became ready, `SyncProgressSnapshot` marked the helper complete and returned before the existing late-packed ownership reconciliation branch. Packed startup now reconciles ownership/source identity once before honoring that completed cursor. The regression creates a completed bootstrap cursor, then a ready late-packed vehicle, and proves the real update path assigns player ownership and canonical source identity; the complete functional batch remains 93/93. No replacement APK or Android acceptance is claimed yet. VRP-072 remains unstarted; blue Autobahn/canal isolation and road-over-water-without-bridge remain separate. Both generated reports and all `_Recovery` scenes remain unstaged; no protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |
| 2026-08-05 | Independent correctness: packed-infantry canonical transport fallback | Checked GUI-licensing focused `Build/Logs/canonical-tent-packed-infantry-fallback-focused.log` `[CanonicalTentTransportPresentationValidation] result=Passed transportVisible=1 ropeVisible=1 dropVisualVisible=1 ownership=canonical latePackedTransport=1 packedInfantryFallback=1`; refreshed visible `Build/EditorEvidence/CanonicalTentTransportDrop.png`; checked aggregate `Build/Logs/editor-first-functional-batch-packed-infantry-fallback.log` `[EditorFirstProductionFunctionalBatchValidation] result=Passed suites=8 tests=93`; `git diff --check` | The Samsung Recruit direct-spawn cause is repaired and Editor-accepted; one fresh checked profiler APK and consolidated Samsung pass remain open; checklist progress stays `56 / 80` | The rejected package proved a packed infantry prefab could remain a valid production target while authoring-only `UnitGridAuthoring` metadata was unavailable. Although the runtime registry contained the explicitly configured canonical helicopter, the metadata guard converted the delivery to `NotRequired` and spawned the Recruit directly. Metadata-less non-vehicle, non-air prefabs now retain that canonical helicopter; likely ground and air vehicles remain fail-closed. Focused coverage reproduces the packed infantry condition and the visible delivery test continues to prove helicopter, rope, drop visual, deferred spawn, and canonical ownership; the complete functional batch remains 93/93. The restricted-host wrapper was rejected before Unity start by duplicate `Path`/`PATH`; the checked host-access route passed. Only the verified wrapper-started headless Unity process that had already logged complete shutdown was terminated after its command host returned early. No replacement APK or Android acceptance is claimed yet. VRP-072 remains unstarted; blue Autobahn/canal isolation and road-over-water-without-bridge remain separate. Both generated reports and all `_Recovery` scenes remain unstaged; no protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |
| 2026-08-05 | Package evidence: packed-infantry fallback fresh profiler APK | Implementation/package head `a01ecbf6f`; checked GUI-licensing short-path build `Build/Logs/editor-batch-a01ecbf6f-profiler-apk-shortpath.log` `[DenseCityCandidateAndroidPackage] result=Passed`; exact `Build/AndroidDenseCandidateProfiler/WarlineCapture-DenseCityCandidate-Profiler.apk`, 696,278,899 bytes, SHA-256 `a91eb453d577024d1c0b4ec26227cf860a4fb0f197817639ae603557e890ea4d` | One fresh checked package is ready for the consolidated Samsung functional pass; checklist progress stays `56 / 80` | Packaging began only after the focused delivery gate and aggregate 93/93 passed. The checked wrapper used the accepted temporary `W:` project mapping to avoid the known Windows Gradle/MAX_PATH failure; the mapping was verified before use and removed immediately after success. No installation or device acceptance is claimed yet. This exact hash is the sole package authorized for Samsung `SM-S918B` / `R5CTC1J02VB`. VRP-072 remains unstarted; blue Autobahn/canal isolation and road-over-water-without-bridge remain separate. Both generated reports and all `_Recovery` scenes remain unstaged; no protected/production asset, Jenkins/Unity path, render policy, or `productionCutover=0` changed. |
| 2026-08-05 | Independent correctness: resolvable-MonoScript fresh package Samsung rejection | Implementation/package/documentation head `96be8078a`; checked GUI-licensing short-path build `Build/Logs/editor-batch-96be8078a-profiler-apk-shortpath.log` `[DenseCityCandidateAndroidPackage] result=Passed`; exact 696,278,899-byte APK SHA-256 `4ed33553c81aa8b816337e6b7bb336e598b7ce17235c3945cac21f5d05826522`; streamed only to Samsung `SM-S918B` / `R5CTC1J02VB` at `192.168.2.33:33675`; main-page Deploy; Android screenshots and selection diagnostics under `Build/AndroidDenseCandidateProfiler/Evidence/2026-08-05-monoscript-consolidated` | Candidate metadata binding and baked-building overlap rejection pass, but Armor/Transport ownership and visible helicopter/rope/drop fail; consolidated Android closure is rejected and checklist progress stays `56 / 80` | The fresh package no longer emits the missing-`CombinedMeshBaker` warning, reaches the operation map through the required main-page Deploy control, and visibly rejects a `13x11` Contractor preview overlapping an existing beige Tent as `BLOCKED BY ROAD OR BLOCKER`. Correctly scaled Armor and Transport roster taps still emit `SquadTraySelectSkipped ... reason=NoCandidates`. A clear-ground Contractor completed, selected with the expected portrait/title and `350/350`, and two Recruit actions each spent `25` materials and increased stable runtime unit/model ownership, but an initial 16-frame capture and a second 45-frame wide-camera capture across the full production window showed no helicopter, rope, or drop presentation. Earlier accepted Contractor and spawned-soldier interaction evidence remains retained; this pass does not claim the missing delivery visuals. Samples remained roughly `39-51 FPS`; this is evidence only and authorizes no VRP-072 work. VRP-072 remains unstarted; blue Autobahn/canal isolation and road-over-water-without-bridge remain separate. Both generated reports and all `_Recovery` scenes remain unstaged; no protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |
| 2026-08-05 | Independent package readiness: post-cold-binding fresh profiler APK | Implementation/package head `aee4403d0`; checked GUI-licensing short-path build `Build/Logs/editor-batch-aee4403d0-profiler-apk-shortpath.log` `[DenseCityCandidateAndroidPackage] result=Passed`; exact final 696,278,899-byte APK SHA-256 `1c6563a495998654eb9f1807c71e34aead1c79ce4e55788af3306dd4f978e395`; verified temporary `W:\` mapping removed; bundled ADB 36.0.0 restart/discovery | One fresh package is accepted and reserved for the exact Samsung consolidated pass; no install/device acceptance yet; checklist progress stays `56 / 80` | The checked build completed from local/origin `aee4403d0` after the cold candidate-binding repair passed its two-stage Editor gate and aggregate 93/93. The exact Samsung `SM-S918B` / `R5CTC1J02VB` advertises `_adb-tls-connect._tcp` as `adb-R5CTC1J02VB-lJmfCF` at `192.168.2.33:33675`, and the endpoint is TCP-reachable, but ADB authentication/connect currently fails. No other device is authorized, no APK was installed, and no Android functional claim is made. The heartbeat automation now retries bounded discovery/connect and will stream-install only this hash when that exact Samsung authorizes. VRP-072 remains unstarted; blue Autobahn/canal isolation and road-over-water-without-bridge remain separate. Both generated reports and all `_Recovery` scenes remain unstaged; no protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |
| 2026-08-05 | Independent package readiness: cold candidate runtime-binding component migration | Accepted checked GUI repair stage `Build/Logs/candidate-runtime-binding-correct-order-repair.log` `[CandidateRuntimeBindingColdRepairValidation] result=Passed scenes=2`; separate fresh-Editor focused `Build/Logs/candidate-runtime-binding-correct-order-cold-focused.log` `[OperationMapEntitySceneCandidateBakeAllValidation] result=Passed tests=24`; checked aggregate `Build/Logs/editor-first-functional-batch-candidate-cold-repair.log` `[EditorFirstProductionFunctionalBatchValidation] result=Passed suites=8 tests=93`; `git diff --check` | The fresh checked APK build's cold candidate binding blocker is repaired and Editor-accepted; one new checked APK and consolidated Samsung confirmation remain; checklist progress stays `56 / 80` | The first post-`b63d0836c` package attempt reached the candidate builder and rejected a cold-unresolved Decorations `CombinedMeshBaker`; licensing was not the cause. Existing working scenes/prefabs proved the serialized source identity is global `CombinedMeshBaker` from `Assembly-CSharp`, while the runtime class incorrectly declared `MovedFrom(... sourceAssembly: "Game.Runtime")`. Migration metadata now matches `Assembly-CSharp`. Candidate regeneration saves a typed production-scene copy without saving production, strips the single stale candidate baker row fail-closed, creates one candidate baker, rewrites its embedded script reference to the checked-in script GUID, and uses edit ordering that cannot remove an earlier YAML block. Both generated candidate runtime bindings now carry the established `Assembly-CSharp::CombinedMeshBaker` identity and pass a separate cold Editor at 24/24; aggregate behavior remains 93/93. No APK is claimed yet. VRP-072 remains unstarted; blue Autobahn/canal isolation and road-over-water-without-bridge remain separate. Both generated reports and all `_Recovery` scenes remain unstaged; no protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |
| 2026-08-05 | Independent correctness: EntityScene vehicle-readiness propagation and licensing recovery | User-authorized Windows licensing recovery ladder recorded in `AGENTS.md`; rejected checked GUI run `Build/Logs/map-vehicle-entityscene-readiness-editor-authorized-recovery.log` exposed compile error CS1503; rejected corrected compile run `Build/Logs/map-vehicle-entityscene-readiness-editor-recovery-rerun.log` used a nonexistent namespaced execute-method class; accepted checked GUI focused `Build/Logs/map-vehicle-entityscene-readiness-editor-recovery-corrected.log` `[MapVehiclePlacementStartupCompletionValidation] result=Passed tests=6`; accepted checked GUI aggregate `Build/Logs/editor-first-functional-batch-entityscene-readiness-recovery.log` `[EditorFirstProductionFunctionalBatchValidation] result=Passed suites=8 tests=93`; `git diff --check` | The remaining packaged Armor startup cause is repaired and Editor-accepted; one fresh checked APK and consolidated Samsung pass remain required; checklist progress stays `56 / 80` | EntityScene canonical-presentation mode now propagates into map-vehicle startup and requires the packed `OperationMapEntityPresentationReadinessContract` even while a compatibility vehicle authoring root remains assigned. The regression proves this exact mixed packed/compatibility configuration waits fail-closed. Licensing did not remain a blocker: Hub had already replaced the stale client, the recovered run reached compilation, and the only source correction was using `default` rather than invalid `null` for the value-type `RuntimeUnitPrefabSystem` fixture argument. The exact global execute-method then passed 6/6 and the aggregate remained green at 93/93. No APK was built or installed yet. VRP-072 remains unstarted; blue Autobahn/canal isolation and road-over-water-without-bridge remain separate. Both generated reports and all `_Recovery` scenes remain unstaged; no protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |
| 2026-07-27 | `VRP-000` corrected APK Android evidence | Exact APK install/cold launch; dense definition/EntityScene resolution; 30-second stable observation; `dumpsys meminfo`; `top -H`; screenshots; opt-in marker rerun; structured JSON | Passed as diagnostic baseline; performance acceptance rejected | `15.7-16.3 FPS`, CPU main `56.7-59.8 ms`, GPU `15.6-16.4 ms`, render thread `1.8-2.3 ms`, gameplay update approximately `0.4 ms`; raw profile not recovered after wireless ADB disconnected |
| 2026-07-28 | `VRP-001` measured design amendment | Parent-contract/evidence audit; packed asset-sharing report; architecture/type/assembly inventory | Design accepted for candidate implementation | Simulation remains resident; only render materialization is virtualized; production and frozen rollback remain unchanged |
| 2026-07-28 | Naming and SOLID/ECS alignment correction | `file_naming_architecture_contract.md`; `gameplay_solid_ecs_contract.md`; proposed type/file/readiness-owner audit; checklist/dependency consistency audit; `git diff --check` | Passed documentation contract alignment; implementation remains open | All proposed ECS components now use `*Component`, the buffer record no longer uses forbidden `*Element`, the generated ScriptableObject uses `*Config`, every bare `*System` remains a real ECS system, and `Game.Composition` remains the exclusive operation-map readiness publisher |
| 2026-07-28 | `VRP-010` default-safe render-residency mode | Windows GUI-licensing wrapper `%TEMP%\warline-render-virtualization-vrp010-host.log`; `[OperationMapRenderVirtualizationValidation] result=Passed tests=6`; wrapper exit code `0`; `git diff --check` | Passed; additive contract accepted | Added the closed `ResidentEntities = 0` / `VirtualizedProxyPool = 1` enum and serialized definition field. Existing definitions remain resident without asset resaves, unknown values fail closed, static chunks reject virtualization, and EntityScene virtualization remains rejected until the database/pool contract exists. The first sandboxed wrapper attempt timed out on denied BIOS/licensing access and its wrapper-owned process tree was cleaned after timeout; the identical host-access wrapper compiled and passed. No candidate, accepted, frozen, production, Addressables, EntityScene, or Android artifact changed. |
| 2026-07-28 | `VRP-011` unmanaged render-virtualization schema | Rejected compile `%TEMP%\warline-render-virtualization-vrp011.log`; corrected Windows GUI-licensing wrapper `%TEMP%\warline-render-virtualization-vrp011-rerun.log`; `[OperationMapRenderVirtualizationValidation] result=Passed tests=11`; wrapper exit code `0`; `git diff --check` | Passed; additive schema accepted | Added the complete schema-1 database/prototype/part/placement/cell/pool records and the six named runtime ECS contracts without wiring them to any baker or system. Field-level construction proves matrices, bounds, colors, policy/state, indices, capacity/headroom, and identities survive blob creation; every type is unmanaged and every runtime contract has the required ECS kind. The first compile rejected the design shorthand `ulong2`, which Unity.Mathematics does not provide; the schema and tracker now use the explicit unmanaged `OperationMapRenderIdentity128` (`ulong Low`, `ulong High`) and the exact rerun passed. No source render row, runtime query, candidate/accepted/frozen/production asset, Addressables content, EntityScene, or Android artifact changed. |
| 2026-07-28 | `VRP-012` deterministic 128-bit identity projection | Rejected shutdown-crash wrappers `%TEMP%\warline-render-virtualization-vrp012.log` and `-rerun.log`; accepted Windows GUI-licensing wrapper `%TEMP%\warline-render-virtualization-vrp012-final.log`; `[OperationMapRenderVirtualizationValidation] result=Passed tests=15`; wrapper exit code `0`; `git diff --check` | Passed; pure identity contract accepted | The editor-only projection hashes exact UTF-8 stable identities with SHA-256, reads digest bytes `0..7`/`8..15` into little-endian `Low`/`High`, rejects empty input, detects a different full source registered to an occupied 128-bit value, permits idempotent repeats, and sorts deterministically by unsigned `(Low, High)`. A fixed known vector guards byte order. The first two runs compiled and passed all 15 assertions but were rejected because Unity emitted `Crash!!!` during shutdown; the identical final run exited through the wrapper with code `0`. No baker/runtime consumer, source row, candidate/accepted/frozen/production asset, package, EntityScene, or Android artifact changed. |
| 2026-07-28 | `VRP-013` canonical prototype fingerprinting | Windows GUI-licensing wrapper `%TEMP%\warline-render-virtualization-vrp013.log`; `[OperationMapRenderVirtualizationValidation] result=Passed tests=18`; wrapper exit code `0`; `git diff --check` | Passed; pure prototype contract accepted | Added an editor-only schema-1 binary fingerprint over normalized renderer hierarchy path, mesh/material GUID and local id, submesh, exact local matrix/bounds, linear base color, fixed policy bucket, shadow flags, and LOD flags. Length-prefixed UTF-8 strings and little-endian primitive writes avoid culture/platform formatting. Tests prove unchanged input is stable, every contract field changes the result, and absolute/session paths, uppercase/malformed GUIDs, non-finite matrices, negative extents, and unknown policy/flag values fail closed. No baker/runtime consumer, render row, asset, package, EntityScene, or Android artifact changed. |
| 2026-07-28 | `VRP-014` deterministic spatial-cell assignment | Windows GUI-licensing wrapper `%TEMP%\warline-render-virtualization-vrp014.log`; `[OperationMapRenderVirtualizationValidation] result=Passed tests=24`; wrapper exit code `0`; `git diff --check` | Passed; pure cell/dedup contract accepted | Added editor-only X/Z assignment from accepted origin/cell size with row-major `(z,x)` output, half-open maximum edges for nonzero bounds, point ownership on exact boundaries, grid clipping, and complete outside rejection. The reference multi-cell gather validates every range/index, deduplicates repeated placement ownership, and globally sorts stable-identity ranks so selected-cell order cannot change results. Boundary, four-cell, partial-overlap, invalid-grid/bounds, repeated-range, and corrupt-index cases pass. No baker/runtime consumer, render row, asset, package, EntityScene, or Android artifact changed. |
| 2026-07-28 | `VRP-015` closed render-policy classification | Windows GUI-licensing wrapper `%TEMP%\warline-render-virtualization-vrp015.log`; `[OperationMapRenderVirtualizationValidation] result=Passed tests=28`; wrapper exit code `0`; `git diff --check` | Passed; pure fixed-policy contract accepted | Added an editor-only classifier for the six closed residency buckets while retaining layer, rendering-layer mask, motion-vector mode, and cast/receive/static-shadow flags in the immutable policy key. Opaque and alpha-clipped content map independently by cast-shadow state; transparent content never falls into opaque and rejects shadow casting; always-resident ownership requires an explicit exception. Unknown material/motion/shadow values, invalid layers, empty rendering masks, and static-without-cast combinations fail closed. Tests also prove any fixed-filter field change produces a different key. No baker/runtime consumer, render row, asset, package, EntityScene, or Android artifact changed. |
| 2026-07-28 | `VRP-016` deterministic camera-capacity sweep | Windows GUI-licensing wrapper `%TEMP%\warline-render-virtualization-vrp016.log`; `[OperationMapRenderVirtualizationValidation] result=Passed tests=32`; wrapper exit code `0`; `git diff --check` | Passed; pure sweep/headroom contract accepted | Added an editor-only capacity sweep over canonical sample identities and complete validated render-policy keys. Every policy must contain the identical sample set, duplicate pairs and negative/default/invalid inputs fail closed, aggregation takes the per-policy maximum, and results sort by every fixed-filter field independent of input order. Overflow-checked integer ceiling implements exact `ceil(peak * 1.20)` and records sample count, peak, capacity, and headroom. Tests cover order reversal, multiple-policy sorting, peak-versus-sum behavior, fractional ceiling, incomplete coverage, duplicates, invalid keys, negative counts, and Int32 overflow. No baker/runtime consumer, manual capacity, render row, asset, package, EntityScene, or Android artifact changed. |
| 2026-07-28 | `VRP-017` canonical virtualization report schema | Rejected compile `%TEMP%\warline-render-virtualization-vrp017.log`; corrected Windows GUI-licensing wrapper `%TEMP%\warline-render-virtualization-vrp017-rerun.log`; `[OperationMapRenderVirtualizationValidation] result=Passed tests=36`; wrapper exit code `0`; `git diff --check` | Passed; Phase 1 pure contracts complete | Added deterministic editor-only JSON serialization and fail-closed parsing for schema identity/version, map/content ownership, residency mode, structural metrics, and complete sorted per-policy capacity results. Exact property sets reject missing/unknown values; the parser rejects duplicates; positive versus zero-valid metrics are explicit; capacity rows revalidate policy, sample count, exact 20% headroom, uniqueness/order, and total-slot reconciliation. Round-trip output is byte-stable. The first run was rejected because the test assembly does not directly reference Newtonsoft; the corrected fixture uses dependency-free string mutations while production editor serialization retains the package already referenced by `Game.Editor`, and all 36 tests pass. No baker/runtime consumer, report asset, render row, package, EntityScene, or Android artifact changed. |
| 2026-07-28 | `VRP-020` generated render-database bake-config schema and ownership | Rejected assertion `%TEMP%\warline-render-virtualization-vrp020.log`; corrected Windows GUI-licensing wrapper `%TEMP%\warline-render-virtualization-vrp020-rerun.log`; `[OperationMapRenderVirtualizationValidation] result=Passed tests=38`; wrapper exit code `0`; exact future-path absence checks; `git diff --check` | Passed; Phase 2 additive schema prerequisite accepted | Added the generated-only `OperationMapRenderDatabaseBakeConfig` and serializable mesh/material/prototype/part/placement/cell/pool records with read-only access and no `CreateAssetMenu`. Validation requires accepted operation-map identity/content hash, finite grid data, nonempty logical arrays, sorted exact asset identities and policy keys, valid cross-record ranges/indices, complete fixed-filter ownership, exact capacity/headroom, and finite matrix/bounds/color data. The blob and fingerprint contracts now retain the exact pool-bucket index plus layer/rendering-mask/motion/shadow fields, preventing coarse-bucket aliasing before builder work. Exact future config/report paths and rollback owners are documented; both outputs remain absent. The first run correctly rejected the synthetic `opmap.test.*` fixture; the corrected fixture and report validator use the real `OperationMapIdentityRules`, and all 38 tests pass. No config/report asset, builder, source render row, candidate/accepted/frozen/production asset, package, EntityScene, or Android artifact changed. |
| 2026-07-28 | `VRP-003` exact render-row eligibility inventory | Rejected compile `%TEMP%\warline-render-virtualization-vrp003.log`; rejected null-prefab probe `%TEMP%\warline-render-virtualization-vrp003-rerun.log`; rejected over-strict unresolved-owner probe `%TEMP%\warline-render-virtualization-vrp003-final.log`; intermediate successful reconciliation `%TEMP%\warline-render-virtualization-vrp003-accepted.log`; final Windows wrapper `%TEMP%\warline-render-virtualization-vrp003-category-scoped.log`; `[OperationMapRenderEligibilityInventoryProbe] result=Passed`; wrapper exit code `0`; `git diff --check` | Passed; exact additive inventory accepted, ownership mutation remains unauthorized | The read-only candidate-scene probe reconciles exactly `82,797` renderer/material rows to packed evidence and emits sorted counts for `1,801` category-scoped prototype signatures, one renderer type, five complete/unsupported policy groups, six gameplay-ownership groups, semantic categories, and stable reason codes. The first safe pilot candidates are exactly `11,299` rows: `5,423` repeated Vegetation and `5,876` repeated Prop; `71,498` rows remain resident, including `14,062` accepted-map rows, `18,375` gameplay-building rows, `39,005` Infrastructure rows, `53` Horizon rows, three unique Prop rows, and all unsupported/unresolved ownership. The report explicitly records `mutationAuthorized=false`: VRP-002 profiling and VRP-021 exact source-row joining remain prerequisites. Unity import auto-normalized ten generated façade-material float strings during probe runs; those unauthorized serialization deltas were reverted and are excluded from the step. No candidate, accepted, frozen, production, Addressables, EntityScene, package, or Android artifact changed. |
| 2026-07-28 | `VRP-021` exact accepted/generated source-row join | Initial source-row Windows wrapper `%TEMP%\warline-render-virtualization-vrp021.log`; rejected compression compile `%TEMP%\warline-render-virtualization-vrp021-gzip.log`; corrected wrapper `%TEMP%\warline-render-virtualization-vrp021-gzip-rerun.log`; unchanged determinism wrapper `%TEMP%\warline-render-virtualization-vrp021-determinism.log`; `[OperationMapRenderEligibilityInventoryProbe] result=Passed`; wrapper exit code `0` twice; gzip decompression/JSON parse; logical/JSON/gzip SHA-256 reconciliation; `git diff --check` | Passed; deterministic source-row inventory accepted, representation mutation remains unauthorized | Schema-2 eligibility evidence now references a compressed schema-1 row document containing exactly `82,797` unique logical rows. Exact stable owners join `68,735` dense-generated plus `13,707` accepted-map rows (`82,442` total); all `11,299` eligible rows are joined. The remaining `355` noneligible unresolved rows retain stable GlobalObjectId diagnostics and stay resident. Every row retains namespaced full owner identity, indexed renderer-relative path, collision-checked 128-bit owner/path/logical projections, mesh/material GUID/local id, submesh, category-scoped prototype signature, renderer type, fixed/unsupported policy, eligibility, and reason code. Two unchanged runs produced identical logical hash `2476874...f69d8`, JSON hash `1426108a...596c8`, and gzip hash `b89d7605...56333`; decompression found `82,797` unique logical identity sources. The report remains `mutationAuthorized=false` because VRP-002 raw Android profiling is open. The first pretty row document was deliberately not retained because its `160,650,770` bytes exceed GitHub's single-file limit; deterministic gzip is `11,017,910` bytes. Unity's ten generated façade-material float-format deltas were reverted and excluded. No config asset, candidate/accepted/frozen/production asset, source render ownership, package, EntityScene, or Android artifact changed. |
| 2026-07-28 | `VRP-022` shared compound prototype/part recipes | Rejected numerical-sharing result `%TEMP%\warline-render-virtualization-vrp022.log`; corrected authored-local-chain wrapper `%TEMP%\warline-render-virtualization-vrp022-local-chain.log`; unchanged determinism wrapper `%TEMP%\warline-render-virtualization-vrp022-determinism.log`; `[OperationMapRenderEligibilityInventoryProbe] result=Passed`; wrapper exit code `0` twice; recipe range/count/bounds/asset/identity validation; logical/JSON SHA-256 reconciliation; `git diff --check` | Passed; exact shared prototype recipes accepted, representation mutation remains unauthorized | The additive builder consumes all `11,299` eligible source rows from `9,721` stable logical owners and emits exactly `22` shared prototypes with `26` parts: 10 single-part Vegetation prototypes covering `5,423` placements and 12 Prop prototypes/16 parts covering `4,298` placements. Every part retains the indexed owner-relative renderer path, persistent mesh/material identity, submesh, exact authored local-chain matrix, local mesh bounds, linear base color, complete fixed policy, and LOD0 flag; combined prototype bounds are computed from transformed part corners. Part fingerprints and compound prototype identities are collision checked; prototype ranges are contiguous; `sum(placementCount * partCount) = 11,299`. The first implementation used `worldToLocal * localToWorld`, whose placement-dependent float residue incorrectly produced `9,721` prototypes, so that evidence was rejected. Exact authored local-chain multiplication restored sharing. Two unchanged corrected runs produced logical hash `ae1095f7...f2969` and JSON hash `1635d92e...a6f9`. Source-row hashes remained byte-identical to VRP-021. The report remains `mutationAuthorized=false`; no placement records, config asset, candidate/accepted/frozen/production asset, source renderer ownership, package, EntityScene, or Android artifact changed. |
| 2026-07-28 | `VRP-023` exact logical placements and state-owner policy | Windows wrappers `%TEMP%\warline-render-virtualization-vrp023-run1.log` and `%TEMP%\warline-render-virtualization-vrp023-run2.log`; `[OperationMapRenderEligibilityInventoryProbe] result=Passed`; wrapper exit code `0` twice; identity/prototype/matrix/state/cell/priority/part-row reconciliation; logical/JSON SHA-256 equality across unchanged runs; `git diff --check` | Passed; exact render-only logical placements accepted, representation mutation remains unauthorized | The additive builder emits exactly `9,721` stable logical placements, sorted by collision-checked owner identity and mapped to the accepted `22` shared prototypes. Every placement retains its exact world matrix, semantic category, and valid prototype index; reconstructing prototype part counts yields all `11,299` eligible source rows. The current pilot is exclusively repeated Vegetation/Prop with `RenderOnly` ownership, so all `9,721` placements correctly use `stateOwnerIndex=-1` and `RequiredVisualState=Any`; the state-owner table is empty and no gameplay relationship is invented. Cell assignment remains the explicit `-1` sentinel for VRP-024, and priority remains `0`. Two unchanged runs produced logical hash `c53d4326...59d9` and JSON hash `b0b531b4...a9484`; source-row and prototype hashes remained byte-identical to VRP-021/022. Building intact/destroyed state linkage remains deferred to Phase 6. The report remains `mutationAuthorized=false`; Unity's ten generated façade-material float-format deltas were reverted and excluded. No config asset, candidate/accepted/frozen/production asset, source renderer ownership, package, EntityScene, or Android artifact changed. |
| 2026-07-28 | `VRP-024` deterministic 32 m spatial-cell index | Rejected presentation-scene view assumption `%TEMP%\warline-render-virtualization-vrp024-run1.log`; rejected gameplay-grid extent assumption `%TEMP%\warline-render-virtualization-vrp024-rerun.log`; corrected Windows wrappers `%TEMP%\warline-render-virtualization-vrp024-envelope.log` and `%TEMP%\warline-render-virtualization-vrp024-determinism.log`; `[OperationMapRenderEligibilityInventoryProbe] result=Passed`; wrapper exit code `0` twice; placement/cell/range/coverage/membership reconciliation; logical/spatial/summary JSON SHA-256 equality across unchanged runs; `git diff --check` | Passed; exact additive spatial index accepted, representation mutation remains unauthorized | The builder reads the same accepted `MatchSubScene_GridAuthoring_Config` origin used by the migration contract, derives absolute cell coordinates from that origin, and creates an aligned `32 m` envelope over all transformed prototype bounds. The accepted origin is `(0,0,0)`; eligible presentation occupies coordinate offset `(13,0)` with aligned origin `(416,0,0)`, dimensions `69 x 48`, and `1,635` occupied cells out of `3,312`. Exactly `9,721` placements produce `11,706` sorted cell memberships; all placements are covered, `1,741` cross a boundary, the maximum membership is four cells, and the maximum occupied-cell load is 43 placements. Placement primary cell references, compact cell ranges, absolute row-major coordinates, exact X/Z cell prisms, per-cell vertical bounds, and stable-identity-ranked membership indices reconcile with zero invalid or duplicate ranges. The initial assumption that the presentation scene retained an `OperationMapSceneView` was rejected because the isolated scene intentionally has none; the next assumption that content was bounded by the `2048 x 1024` gameplay grid was rejected when valid presentation extended beyond it. The corrected envelope retains the accepted origin without clipping those placements. Two unchanged runs produced logical hash `5c8b1dd8...8ace` / JSON `a0688109...4681` and spatial hash `ed5ac397...54b9` / JSON `4f5e523b...a4a3`; source-row and prototype hashes remained byte-identical. The report remains `mutationAuthorized=false`; Unity's ten generated façade-material float-format deltas were reverted and excluded. No config asset, candidate/accepted/frozen/production asset, source renderer ownership, package, EntityScene, or Android artifact changed. |

| 2026-07-28 | `VRP-025` provisional capacity and entity/active-slot budgets | Initial sandboxed wrapper attempts `%TEMP%\warline-render-virtualization-vrp025.log`, `-retry.log`, and `-gui-recovered.log` rejected before project execution; documented host-access Windows GUI wrappers `%TEMP%\warline-render-virtualization-vrp025-host.log` and `%TEMP%\warline-render-virtualization-vrp025-determinism-host.log`; `[OperationMapRenderEligibilityInventoryProbe] result=Passed`; explicit Unity return-code-zero marker twice; capacity-report JSON parse; `git diff --check` | Passed; deterministic provisional budgets accepted, final camera/projection capacity acceptance remains open | The additive builder sends every complete fixed-filter policy through the accepted capacity-sweep contract over all `3,312` accepted-origin-aligned grid cells. Each provisional sample expands a nominal center cell by one safety cell plus one prefetch-guard cell, deduplicates multi-cell placements, and supplies the identical canonical sample set to every policy. Opaque peak `576` yields capacity `692`; alpha-clipped peak `10` yields capacity `12`; total provisional active slots are `704` with exact `ceil(peak * 1.20)` headroom, safely below the `8,000` representative-view ceiling. The `9,721` immutable logical placements are also below the `24,000` map-MMI entity limit. The report explicitly remains provisional because projection, zoom, rotation, and tactical-follow sweeps are pending and no runtime overflow claim is made. Initial sandbox runs hit the documented Windows licensing/BIOS-session denial before compilation; the mandated host-access reruns compiled, executed, and exited cleanly. Unity's ten generated facade-material float-format deltas were reverted and excluded. No config asset, source render ownership, candidate/accepted/frozen/production asset, package, EntityScene, or Android artifact changed. |
| 2026-07-28 | `VRP-026` transactional candidate database config | Initial compile rejection and corrected host-access Windows GUI wrappers `%TEMP%\warline-render-virtualization-vrp026-host.log`; `[OperationMapRenderDatabaseBuilder] result=Passed`; focused contract wrapper `%TEMP%\warline-render-virtualization-vrp026-contracts-host.log`; `[OperationMapRenderVirtualizationValidation] result=Passed tests=38`; wrapper exit code `0`; persisted config schema/content-hash validation; report round-trip validation; protected-production SHA-256 snapshots; `git diff --check` | Passed; candidate config/report transaction and production isolation accepted | Added the deterministic editor builder and generated-only initialization boundary. One file journal owns both generated parent `.meta` files, the config asset and `.meta`, and the database report, deleting partial first creation or restoring prior bytes on failure. The persisted `5,425,858`-byte config contains exactly `26` meshes, `2` materials, `22` prototypes, `26` parts, `9,721` placements, `1,635` occupied cells, `11,706` membership indices, two complete fixed-policy buckets, and `704` total slots under content hash `f0555edc...9ad8a`. The report records serialized config SHA-256 `40e9f994...b8ae`. Pre/post snapshots proved the accepted map/SubScene, production definition/thin binding, frozen rollback root, and `Assets/AddressableAssetsData` unchanged. The first run was rejected on one definite-assignment compile error before the execute method; the corrected builder and focused 38-test contract suite both passed through the documented GUI-license wrapper. VRP-002 still blocks representation ownership mutation, and VRP-027 must separately prove unchanged-input serialized-byte determinism. No source render row, runtime query, accepted/frozen/production asset, package, EntityScene, or Android artifact changed. |
| 2026-07-28 | `VRP-027` unchanged-input database determinism | Host-access Windows GUI wrapper `%TEMP%\warline-render-virtualization-vrp027-host.log`; `[OperationMapRenderDatabaseBuilder] determinism=Passed`; wrapper exit code `0`; exact two-pass config/report byte comparison; record-order/count/content-hash comparison; `git diff --check` | Passed; deterministic candidate database bytes accepted | The focused entry point executes the complete transactional builder twice without changing any input and rejects any difference in content hash, canonical record-order hash, per-record counts, total capacity, serialized config bytes, or report bytes. Both passes retained content hash `f0555edc...9ad8a`, canonical record-order SHA-256 `0eb0019f...a6ef`, config SHA-256 `40e9f994...b8ae`, exactly `5,425,858` config bytes, and exactly `882` report bytes. Both underlying transactions repeated schema/report round-trip validation and accepted/frozen/production isolation. VRP-002 remains open and no representation ownership mutation is authorized. No source render row, runtime query, accepted/frozen/production asset, package, EntityScene, or Android artifact changed. |
| 2026-07-28 | `VRP-028` persisted source-to-logical field parity | Rejected final-count assertion and rejected DTO compile wrappers followed by corrected host-access Windows GUI wrapper `%TEMP%\warline-render-virtualization-vrp028-host.log`; `[OperationMapRenderEligibilityInventoryProbe] result=Passed`; two `[OperationMapRenderDatabaseBuilder] result=Passed`; `[OperationMapRenderDatabaseBuilder] determinism=Passed`; `[OperationMapRenderDatabaseBuilder] sourceParity=Passed`; wrapper exit code `0`; exact per-record bit/identity/index/policy/state comparison; protected-production snapshots; `git diff --check` | Passed; Phase 2 render-only logical database complete, source ownership unchanged | The focused gate first re-read the live candidate EntityScene and reconciled all `82,797` renderer/material rows: exactly `11,299` eligible rows expand from `9,721` placements through `22` prototypes and `26` shared parts, while `71,498` rows remain resident and `sourceRowsRemoved=0`. The builder then required every persisted mesh/material GUID/local-id/reference, prototype identity/range/bounds/category, part renderer-path identity/matrix/bounds/mesh/material/submesh/color/full policy/LOD/shadow field, placement identity/world matrix/cell/state/priority/category, cell/range/membership, and pool-bucket field to match the freshly generated source records; all float matrices, bounds, and colors compare by exact IEEE-754 bits. Two unchanged transactional builds remained byte-identical at `5,425,858` config bytes and `1,092` report bytes under content hash `f0555edc...9ad8a`, ordering hash `0eb0019f...a6ef`, and config hash `40e9f994...b8ae`. The first run reached all field comparisons but the final report check incorrectly compared `11,299` logical rows to `26` shared parts; the next attempt caught the new count field in the wrong DTO at compile time; both were rejected and corrected. Unity's known ten facade-material float-format deltas were reverted and excluded. VRP-002 still blocks representation ownership mutation. No source render row, runtime query, accepted/frozen/production asset, package, EntityScene, or Android artifact changed. |
| 2026-07-28 | `VRP-031` sorted shared render-mesh array | Windows GUI-licensing wrapper `%TEMP%\warline-render-virtualization-vrp031-host.log`; `[OperationMapRenderVirtualizationValidation] result=Passed tests=39`; `Tundra build success`; wrapper exit code `0`; `git diff --check` | Passed; shared asset-array bake contract accepted | The candidate root baker now attaches exactly one `RenderMeshArray` built from the generated database's already strict GUID/local-id order. The focused gate resolves all `26` sorted mesh references and both sorted material references by exact Unity object identity, then validates every one of the `26` logical prototype parts against the resulting mesh/material arrays and its selected mesh's real submesh count. Schema validation now rejects an out-of-range submesh before baking, and the builder repeats complete mesh/material/submesh range checks before constructing the shared component. This step creates no proxy slots, strips no source renderer, changes no runtime ownership, and does not mutate candidate/accepted/frozen/production assets, EntityScene content, package output, or Android artifacts. VRP-002 continues to block later representation-ownership mutation. |
| 2026-07-28 | `VRP-032` fixed disabled proxy-slot bake | Rejected compile `%TEMP%\warline-render-virtualization-vrp032-host.log`; blocked rerun `%TEMP%\warline-render-virtualization-vrp032-host-rerun.log`; rejected AABB-reference compile `%TEMP%\warline-render-virtualization-vrp032-host-final.log`; corrected Windows GUI-licensing wrapper `%TEMP%\warline-render-virtualization-vrp032-host-final2.log`; `[OperationMapRenderVirtualizationValidation] result=Passed tests=40`; `Tundra build success`; explicit success marker and wrapper exit code `0`; `git diff --check` | Passed; fixed leaf slot bake accepted | The candidate root baker now creates exactly the reported `704` additional leaf entities in two contiguous immutable policy ranges: `692` opaque/shadows-off slots followed by `12` alpha-clipped/shadows-off slots. Every slot receives the one shared sorted `RenderMeshArray`, its bucket's exact layer/rendering-mask/motion/shadow `RenderFilterSettings`, `MaterialMeshInfo`, `LocalToWorld`, `RenderBounds`, `URPMaterialPropertyBaseColor`, and stable slot/bucket identity; placement/part bindings start at `-1`, assignment generation starts at zero, and `MaterialMeshInfo` starts disabled. The plan rejects noncontiguous, nonpositive, or over-24,000 capacity before entity creation. The first compile exposed the UnityEngine namespace for motion-vector mode; its rerun remained behind that wrapper-owned failed Editor. After correcting the namespace, both exact wrapper-owned Editors were terminated under the user's standing recovery authorization, without touching Hub or licensing. The next compile exposed the explicit `Unity.Mathematics.Extensions` dependency; that wrapper-owned failed Editor was likewise removed, and the final clean wrapper passed all 40 tests and exited successfully. No source renderer is stripped, no runtime assignment starts, and no candidate/accepted/frozen/production asset, EntityScene output, package, or Android artifact changes. VRP-002 still blocks representation-ownership mutation. |
| 2026-07-28 | `VRP-033` baked proxy-slot contract | Rejected in-memory bake `%TEMP%\warline-render-virtualization-vrp033-host.log`; corrected Windows GUI-licensing wrapper `%TEMP%\warline-render-virtualization-vrp033-host-final.log`; `[OperationMapRenderVirtualizationValidation] result=Passed tests=41`; `Tundra build success`; explicit success marker and wrapper exit code `0`; `git diff --check` | Passed; baked slot-shape contract accepted | The focused suite now creates a temporary virtualized-presentation authoring root, reflects through Unity's baking utility, and inspects the actual baked entities. It proves exactly `704` unique slots, exact contiguous bucket membership (`692` and `12`), required render shared/components, no `Parent`, `Child`, or `LocalTransform`, unbound `-1` placement/part values, zero generation, and disabled initial `MaterialMeshInfo`. The first real bake exposed that `TransformUsageFlags.None` stripped the explicit `LocalToWorld`; the baker now requests `Renderable`, retaining world-space `LocalToWorld` without hierarchy components, and the corrected run passed. No source renderer is stripped, no runtime assignment starts, and no candidate/accepted/frozen/production asset, EntityScene output, package, or Android artifact changes. VRP-002 still blocks representation-ownership mutation. |
| 2026-07-29 | `VRP-002` raw Android PlayerLoop owner profile | Xiaomi `24090RA29G` / Android 16 / API 36 / ARM64; isolated 579,937,484-byte Development profiler APK SHA-256 `5eeb468a...cccfe`; dense package gate; 497,493,532-byte raw profile SHA-256 `1d991606...547ff8`; accepted Windows GUI-licensing analysis wrapper `%TEMP%\warline-vrp002-raw-profile-analysis-accepted.log`; 500-frame hierarchy scan `300..799`; structured evidence and summary reports; `git diff --check` | Passed diagnostic owner-attribution gate; release FPS acceptance and representation mutation remain separate | The raw hierarchy names `UnitFactionTintTargetBackfillSystem` as the dominant PlayerLoop child at `38.946 ms/frame`, followed by `UnitMassRenderSettingsSystem` at `13.068 ms/frame` and the cadenced `ThreatDetectionWarningSystem:ThreatScanJob (Burst)` at `5.211 ms/frame`. Supporting overlap includes `WaitForJobGroupID` at `8.670 ms/frame` and `JobHandle.Complete` at `8.551 ms/frame`; `EntitiesGraphicsSystem` averages `0.580 ms/frame`, the main-thread render loop `2.936 ms/frame`, and the render-thread render loop `3.460 ms/frame`. Source audit ties the top owners to per-frame unmatched render-child/parent-chain classification and backfill, temporary collections, render-setting patching, structural playback, and explicit job completion. The Development capture averaged `71.73 ms` / `13.9 FPS` and is not a release performance sample; the corrected release baseline remains `15.7-16.3 FPS`. The first analysis wrote all 500 frames but was rejected because direct editor exit emitted no wrapper-recognized success sentence; the corrected post-write marker rerun exited `0`. The phone was restored to the exact retained release APK and its device-side raw file was removed only after the verified pull. No runtime owner, render row, accepted/frozen/production asset, Addressables setting, package contract, or production cutover changed. |
| 2026-07-29 | `VRP-004` fixed-camera/route baseline reconciliation | Read-only Git equivalence checks from corrected APK build through `f78a3386` and current evidence revision; release APK/performance JSON; VRP-003 inventory/source-row hashes; VRP-002 raw-profiler evidence; JSON parse; `git diff --check` | Passed diagnostic reconciliation; Android acceptance and representation mutation remain closed | `2026-07-29_dense_city_vrp004_fixed_camera_route_baseline.json` binds the unchanged dense candidate scene, EntityScene GUID, exact `82,797 = 11,299 eligible + 71,498 excluded` rows, corrected release `15.7-16.3 FPS` / `56.7-59.8 ms` CPU-main baseline, and raw owner attribution to exact revision `f78a338692f6913d8034a14c0313c0b198bf7d8b`. The retained 30-second fixed Match-camera observation has resolution/render-scale/screenshot identity but no serialized camera pose; the report records that gap instead of inventing a route transform. Development capture remains owner attribution only, not a release substitute. No runtime ownership, source-row, candidate/accepted/frozen/production asset, package, or Android artifact changed. |
| 2026-07-29 | `VRP-034` fail-closed eligible source-row stripping | Rejected compile/method/bake iterations `%TEMP%\warline-render-virtualization-vrp034.log`, `-rerun.log`, `-final.log`, `-final2.log`, `-actual-method.log`, `-owner-buffer.log`, `-origin-resolution.log`, `-origin-resolution-final.log`, and `-entity-diagnostic.log`; rejected sandboxed licensing runs `%TEMP%\warline-render-virtualization-vrp034-render-mesh-unmanaged.log`, `-recovered.log`, and `-gui-licensing.log`; accepted host-access Windows GUI-licensing wrappers `%TEMP%\warline-render-virtualization-vrp034-gui-licensing-elevated.log` and `-final-accepted.log`; `[OperationMapRenderVirtualizationValidation] result=Passed tests=42` twice; `Tundra build success`; explicit success marker and wrapper exit code `0` twice; `git diff --check` | Passed; opt-in source-row baking-only gate accepted | Stable identity bakers now emit temporary owner/path-to-render-entity rows, while an explicitly configured virtualization source root supplies the exact eligible logical rows from the generated database. The post-baking system requires one-to-one owner/path matches, render-only ownership, the accepted single-submesh pilot, and exact baking-time mesh/material/submesh identity before adding `BakingOnlyEntity`; any missing, duplicate, gameplay-owned, or asset-mismatched eligible row fails closed. The focused in-memory bake proves the exact eligible render row is baking-only while an unmatched named resident exception and canonical gameplay entity remain present. Initial iterations honestly exposed assembly boundaries, definite assignment, execute-method naming, cross-authoring entity ownership, and baking-stage render-asset representation before the accepted temporary-buffer/post-bake design. Sandboxed runs were rejected on the documented Windows machine-identity/licensing denial; only the host-access GUI-licensing runs compiled and passed. The source-root reference remains opt-in and no candidate/accepted/frozen/production asset, EntityScene output, package, or Android artifact changed. |
| 2026-07-29 | `VRP-035` conditional virtualized-building state-index ownership | Host-access Windows GUI-licensing wrapper `%TEMP%\warline-render-virtualization-vrp035.log`; `[OperationMapRenderVirtualizationValidation] result=Passed tests=43`; `Tundra build success`; explicit success marker and wrapper exit code `0`; `git diff --check` | Passed; conditional building render-root replacement gate accepted | Every baked canonical building now carries a temporary collision-checked identity marker. Stateful eligible logical rows must agree on one nonnegative state-owner index, use an explicit intact/destroyed requirement, resolve only to gameplay-owned visual entities separate from the canonical building, and cover every source renderer under that owner identity. Only after complete logical/source/asset parity does post-baking remove `OperationMapBuildingPresentation` and add `OperationMapVirtualizedBuildingPresentationComponent`; the canonical `OperationMapBuildingComponent`, destroyed-state ownership, health, faction, grid, blocker, targeting, and other gameplay data remain resident. The focused real-authoring bake proves the visual row becomes baking-only, the canonical building never does, resident render-root references are removed, state index `7` is retained, and `OperationMapBuildingDestroyedComponent` survives. The current `11,299`-row Vegetation/Prop pilot has no state owners, so this path is dormant and no candidate/accepted/frozen/production asset, EntityScene output, package, or Android artifact changed. |
| 2026-07-29 | `VRP-036` mode-specific database/slot/source packed-readiness gates | Rejected host-access Windows GUI-licensing wrappers `%TEMP%\warline-render-virtualization-vrp036.log`, `-rerun.log`, and `-final.log`; accepted wrappers `%TEMP%\warline-render-virtualization-vrp036-pass.log`, `-readiness.log`, and `-scene-loading.log`; `[OperationMapRenderVirtualizationValidation] result=Passed tests=43`; `[OperationMapEntityPresentationReadinessValidation] result=Passed tests=11`; `[OperationMapSceneLoadingValidation] result=Passed tests=18`; `Tundra build success`; explicit success markers and wrapper exit code `0`; `git diff --check` | Passed; virtualized packed-readiness contract accepted | The candidate baker now emits the complete schema-1 logical database blob and a compact exact resident-source-row buffer. Post-baking publishes mode/count ownership only after exact source parity, while runtime readiness routes the definition residency mode through the existing `Game.Composition` owner. Resident mode rejects any virtualization artifact. Virtualized mode requires one created matching database with nonempty prototypes/parts/placements/cells/buckets, exact contiguous pool ranges and unique slot identities, positive slot capacity, zero packed eligible-source survivors, exact virtualized building ownership, and a unique resident-exception row for every retained source render entity in the resolved sections. Focused tests accept a complete contract and reject a missing database, an eligible survivor, and a duplicate slot. The first two runs were rejected on definite-assignment and updated-test-double compile errors; the third exposed an additive slot-only bake being treated as source stripping and a test helper disposing its blob store before assertions. Both lifecycle boundaries were corrected before the three accepted runs. No candidate/accepted/frozen/production asset, definition mode, EntityScene output, package, or Android artifact changed; `VRP-037` must add persisted report metrics and defaults before direct package validation. |
| 2026-07-29 | `VRP-037` packed-content report extension | Windows GUI-licensing wrapper `%TEMP%\warline-render-virtualization-vrp037.log`; `[DenseCityPresentationBudgetValidation] result=Passed tests=24`; explicit success marker and wrapper exit code `0`; persisted schema-2 report inspection; `git diff --check` | Passed; report/default contract accepted | The packed-asset-sharing report now reads and validates the persisted render-virtualization database before publication. It records the schema-1 content hash plus `22` prototypes, `26` parts, `9,721` placements, `1,635` cells, two policy buckets, `704` slots, `11,299` eligible logical rows, and database `sourceRowsRemoved=0`. It deliberately sets `renderVirtualizationMetricsComplete=0` and every actual packed virtualized/eligible/resident/removal count to `-1`: those facts cannot be inferred from the unmodified resident candidate package. Invalid hash/count/parity/isolation evidence and any nonzero database source-removal count reject publication. No candidate/accepted/frozen/production asset, definition mode, EntityScene output, package, or Android artifact changed; `VRP-038` remains the first direct package-validation step. |
| 2026-07-29 | `VRP-038` two-pass direct virtualized candidate bake | Rejected compile/contract wrappers `%TEMP%\warline-render-virtualization-vrp038.log`, `-rerun.log`, and `-final.log`; accepted Windows GUI-licensing wrapper `%TEMP%\warline-render-virtualization-vrp038-final2.log`; `[OperationMapRenderVirtualizationCandidateValidation] result=Passed passes=2`; `Tundra build success`; exact two-pass fingerprint comparison; protected candidate/production/Addressables snapshots; persisted-mode checks; explicit success marker and wrapper exit code `0`; JSON evidence inspection; `git diff --check` | Passed; Phase 3 unsaved direct-bake gate accepted | A temporary unsaved authoring root enables `VirtualizedProxyPool` only inside each direct bake of the real 243 MB dense candidate scene. Both conversions produced fingerprint `4d859a9a...2d17d`, one matching database under content hash `f0555edc...9ad8a`, `704` unique contiguous disabled slots, `11,299` exact eligible render entities marked with the baking-only exclusion contract, and `70,710` unique resident render-entity exceptions from `82,009` authoring-side stable-owner renderer rows. The validator independently recounts authoring renderers with the same nested-owner boundary and requires exact `eligible + resident = source` reconciliation. The `788` difference from the historical `82,797` renderer/material packed inventory is resident multi-material expansion, not missing ownership. Initial attempts were rejected on definite assignment/blob-ref compile errors, an enableable-component query that hid intentionally disabled slots, and the incorrect assumption that renderer/material rows equal renderer-ownership rows. The accepted gate wrote only its evidence report; the persisted candidate stays `EntityScene + ResidentEntities`, production stays `StaticSceneChunks + ResidentEntities`, `productionCutover=0`, no EntityScene/package is persisted, and known ten Unity facade-material float-format drifts were restored and excluded. |
| 2026-07-29 | `VRP-040` ordered versioned camera snapshot publication | Rejected compile wrapper `%TEMP%\warline-render-virtualization-vrp040.log`; accepted Windows GUI-licensing wrappers `%TEMP%\warline-render-virtualization-vrp040-rerun.log` and `-architecture.log`; `[RuntimeCameraReferenceFocusedValidation] result=Passed tests=8`; `[EcsBurstHotPathArchitectureValidation] result=Passed tests=11`; `Tundra build success`; explicit success markers and wrapper exit code `0`; `git diff --check` | Passed; Phase 4 camera boundary accepted | `RuntimeCameraReferenceSystem` now creates its singleton once, runs order-first in `SimulationSystemGroup`, and publishes one unmanaged snapshot per system update. `SetWorldCamera`/`ClearWorldCamera` change only managed ownership; repeated `TryGetCameraSnapshot` calls are pure reads and cannot produce intra-frame drift. Each publication advances a nonzero `uint` version (including invalid boundaries), while valid snapshots hash exact pose/view/projection bits into the existing unmanaged 128-bit identity shape; unchanged cameras retain the signature across versions and pose changes alter it. Focused tests prove boundary-only publication, read stability, signature behavior, invalidation, and explicit ordering. The first attempt was rejected before execution because the expanded test lacked the component namespace import; only the exact failed wrapper-owned Editor was closed under the user's standing sole-project authorization. No runtime virtualization initializer/selection/assignment system, candidate/accepted/frozen/production asset, package, EntityScene, or Android artifact changed. |
| 2026-07-29 | `VRP-041` map-generation initialization and bounded persistent native state | Rejected compile/test/architecture wrappers `%TEMP%\warline-render-virtualization-vrp041.log`, `-rerun.log`, and `-architecture.log`; accepted Windows GUI-licensing wrappers `%TEMP%\warline-render-virtualization-vrp041-final2.log`, `-contract.log`, and `-architecture-final.log`; `[OperationMapRenderVirtualizationInitializationFocusedValidation] result=Passed tests=6`; `[OperationMapRenderVirtualizationValidation] result=Passed tests=43`; `[EcsBurstHotPathArchitectureValidation] result=Passed tests=11`; `Tundra build success`; explicit success markers and wrapper exit code `0`; `git diff --check` | Passed; Phase 4 initialization boundary accepted | The baked database entity now owns the single state/metrics/change-buffer contract. The new initialization system fails closed on duplicate/missing owners, map/schema/hash/residency mismatch, empty logical arrays, invalid bucket ranges, count drift, duplicate slots, out-of-range slots, and invalid prototype-part expansion. It allocates exact persistent slot/logical-row maps plus placement prefixes, initializes every binding to `-1`, performs no same-generation rebuild, and completes/disposes only at map-generation, database-removal, or system-destroy boundaries. The first compile was rejected by Unity blob-ref safety analysis and only its exact wrapper-owned Editor was closed under the user's standing sole-project authorization. The second run was rejected because `SystemHandle.Update` logs exceptions rather than returning them to `Assert.Throws`; negative tests were moved to the same internal pure gates. The accepted focused test explicitly disposes the native state and proves every container returns to uncreated/zero capacity. Its shutdown still reports the same four persistent allocations already present in the accepted VRP-040 camera-only wrapper, and the independent bake-contract wrapper reports the same count; this known Editor/wrapper baseline is recorded rather than misclaimed as absent. The first architecture run rejected the new lifecycle file as unclassified; it is now concretely classified as a managed bootstrap boundary without raising hot-path debt ceilings, and its accepted run has no leak marker. Camera selection, render assignment/apply, candidate output, accepted/frozen/production assets, package, EntityScene, and Android artifacts remain unchanged. |
| 2026-07-29 | `VRP-042` pure guard-envelope rebuild decision | Rejected sandbox GUI wrapper `%TEMP%\warline-render-virtualization-vrp042.log` before Unity launch because the sandbox inherited duplicate `Path`/`PATH` environment keys; accepted host-access Windows GUI-licensing wrapper `%TEMP%\warline-render-virtualization-vrp042-host.log`; `[OperationMapRenderGuardEnvelopeValidation] result=Passed tests=7`; `Tundra build success`; explicit successful-exit marker and wrapper exit code `0`; `git diff --check` | Passed; Phase 4 guard-decision contract accepted | Added an unmanaged, allocation-free decision boundary over explicit inclusive required/guard cell envelopes. It schedules a rebuild only for initial view, map-generation change, explicit force/camera discontinuity, required-envelope escape, or pending visual-state changes; contained stable camera envelopes return `None`, without any frame-count input. Invalid envelopes return failure with `None` so a future caller must reject the input rather than scheduling an ambiguous rebuild. This step owns no camera projection, runtime state mutation, job scheduling, proxy assignment, candidate/accepted/frozen/production asset, package, EntityScene, or Android artifact. |
| 2026-07-29 | `VRP-043` bounded Burst cell selection/gather/dedup/state filtering | Rejected Windows GUI-licensing wrapper `%TEMP%\warline-render-virtualization-vrp043.log`; accepted wrappers `%TEMP%\warline-render-virtualization-vrp043-rerun.log` and `-architecture.log`; `[OperationMapRenderCellSelectionValidation] result=Passed tests=7`; `[EcsBurstHotPathArchitectureValidation] result=Passed tests=11`; `Tundra build success`; explicit success markers and wrapper exit code `0`; `git diff --check` | Passed; Phase 4 Burst selection jobs accepted | Added two `[BurstCompile]` scheduled jobs over immutable blob data and caller-owned native scratch. Required-cell selection scans the compact cell table only; gathering validates selected ranges, deduplicates placements with a fixed bitset, filters canonical intact/destroyed state, sorts stable placement indices, and expands exact prototype parts into pool-bucket logical-row keys. Every output uses explicit maximum counts plus `AddNoResize`; failures clear outputs and return a closed code. Tests prove inclusive selection, multi-cell deduplication, traversal-order determinism, intact/destroyed filtering, missing-state and corrupt-range rejection, and bounded capacity without growth. The first run compiled but rejected the assumption that a `NativeList` constructor count equals allocator-reserved capacity; explicit policy maxima now enforce the bound independently of allocator rounding. No runtime system wiring, slot assignment/apply, candidate/accepted/frozen/production asset, package, EntityScene, or Android artifact changed. |
| 2026-07-29 | `VRP-044` deterministic Burst retain/release/assign kernel | Rejected compile wrapper `%TEMP%\warline-render-virtualization-vrp044.log`; accepted Windows GUI-licensing wrappers `%TEMP%\warline-render-virtualization-vrp044-rerun.log` and `-architecture.log`; `[OperationMapRenderAssignmentValidation] result=Passed tests=6`; `[EcsBurstHotPathArchitectureValidation] result=Passed tests=11`; `Tundra build success`; explicit success markers and wrapper exit code `0`; `git diff --check` | Passed; Phase 4 deterministic assignment kernel accepted | Added one `[BurstCompile]` single-threaded assignment job over caller-owned fixed native state. Before mutation it validates contiguous bucket ranges, exact placement prefixes/capacities, sorted unique selected cells/logical rows, part-to-bucket identity, and both directions of every existing slot/logical binding. Rebuilds retain required reciprocal bindings, release stale bindings, deterministically reuse the lowest free slot in the matching bucket, advance generations only for changed slots, maintain active cell/placement plus required/dirty-slot bitsets, emit one final fixed-size command per dirty slot, and publish bounded retained/released/assigned/overflow counters. Tests prove deterministic initial assignment, unchanged retention, release, same-pass slot reuse without duplicate dirty commands, explicit overflow, and fail-before-mutation rejection of unsorted input or corrupt reciprocal state. The first compile was rejected because three test assertions used the allocator-requiring `NativeList.ToArray` overload; the corrected run passed. No runtime scheduling/system wiring, entity/component apply, source ownership, candidate/accepted/frozen/production asset, package, EntityScene, or Android artifact changed. |
| 2026-07-29 | `VRP-045` parallel complete-state slot apply kernel | Accepted Windows GUI-licensing wrappers `%TEMP%\warline-render-virtualization-vrp045.log` and `-architecture.log`; `[OperationMapRenderSlotApplyValidation] result=Passed tests=5`; `[EcsBurstHotPathArchitectureValidation] result=Passed tests=11`; `Tundra build success`; explicit success markers and wrapper exit code `0`; no leak/exception marker; `git diff --check` | Passed; Phase 4 parallel apply kernel accepted | Added one `[BurstCompile] IJobChunk` over the fixed proxy-slot archetype with `IgnoreComponentEnabledState` supplied by its future caller. Every iteration owns one immutable slot identity and clean slots receive zero writes. Valid assignments compose `placement.WorldMatrix * part.LocalToPlacement`, write exact local bounds, `MaterialMeshInfo` array indices/submesh, linear base color, binding, and generation, then enable rendering. Releases reset matrix, bounds, mesh/material/submesh, color, binding, and generation before disabling `MaterialMeshInfo`. Command, slot-range, placement/prototype/part/bucket, finite-matrix/bounds/color, and index validation occurs before any slot write; invalid dirty commands leave the complete prior slot state untouched and set one bounded per-slot failure. Tests cover assigned parity, full release reset, clean-slot immutability, invalid-command atomic rejection, and absence of `Parent`, `Child`, and `LocalTransform`. No runtime system/dependency scheduling, synchronous hot-path completion, structural change, source ownership, candidate/accepted/frozen/production asset, package, EntityScene, or Android artifact changed; VRP-046 owns scheduling before Entities Graphics. |
| 2026-07-29 | `VRP-046` asynchronous apply-to-Entities-Graphics ordering | Accepted Windows GUI-licensing wrappers `%TEMP%\warline-render-virtualization-vrp046.log`, `-apply-regression-rerun.log`, `-contract-final.log`, `-architecture-rerun.log`, and `-assignment-regression.log`; rejected regression/setup wrappers `%TEMP%\warline-render-virtualization-vrp046-apply-regression.log` and `-architecture.log`; `[OperationMapRenderProxyApplySystemValidation] result=Passed tests=3`; `[OperationMapRenderSlotApplyValidation] result=Passed tests=5`; `[OperationMapRenderAssignmentValidation] result=Passed tests=6`; `[OperationMapRenderVirtualizationValidation] result=Passed tests=43`; `[EcsBurstHotPathArchitectureValidation] result=Passed tests=11`; `Tundra build success`; explicit success markers and wrapper exit code `0`; no accepted-log leak/exception marker; `git diff --check` | Passed; Phase 4 asynchronous apply scheduling boundary accepted | The existing database owner now bakes exactly one schema-checked `OperationMapRenderSlotCommandComponent` buffer entry per immutable slot, initialized as generation-zero released state. Assignment writes that fixed unmanaged element type; apply detects changed work by command-versus-slot generation, eliminating a cross-system dirty-bit handoff while preserving the assignment job's bounded dirty list. The new `[BurstCompile]` presentation `ISystem` requires one command owner and exact command/slot parity, keeps a fixed persistent per-slot failure array, schedules `OperationMapRenderSlotApplyJob` with `ScheduleParallel(..., state.Dependency)`, and orders before `UpdatePresentationSystemGroup` and `EntitiesGraphicsSystem`. Recurring `OnUpdate` contains no completion or result read; completion exists only when capacity ownership changes or the system is destroyed. Integration tests prove explicit ordering, asynchronous scheduled apply, and equal-generation no-write behavior. The first apply regression was rejected because its former dirty-bit test supplied generation `15` against slot generation `4`, correctly causing a write; equal generation `4` passed. The first architecture run rejected the recurring system as unclassified non-Burst work; marking the real system lifecycle/update Burst compiled passed without adding debt. Upstream selection/assignment systems, stable-camera zero-schedule gating, candidate/accepted/frozen/production assets, package, EntityScene, and Android artifacts remain unchanged. |
| 2026-07-29 | `VRP-047` stable-camera/apply no-op proof | Accepted Windows GUI-licensing wrappers `%TEMP%\warline-render-virtualization-vrp047.log`, `-contract.log`, and `-architecture.log`; `[OperationMapRenderProxyApplySystemValidation] result=Passed tests=6`; `[OperationMapRenderVirtualizationValidation] result=Passed tests=43`; `[EcsBurstHotPathArchitectureValidation] result=Passed tests=11`; `Tundra build success`; explicit success markers and wrapper exit code `0`; no leak/exception marker; `git diff --check` | Passed; stable apply no-op boundary accepted | Added one default-zero unmanaged command-version component to the existing database owner. Version `0` means no command publication; a changed nonzero owner/version schedules exactly once, while an unchanged owner/version returns before command-buffer access, slot counting/querying, type-handle updates, failure-array work, or job scheduling. The warmed repeated stable system call measured exactly `0 B` on the current thread, scheduled zero additional apply jobs, preserved `LocalToWorld`, proxy binding/generation, and `MaterialMeshInfo` enable state bit-for-bit, and left virtualization `RebuildCount=7` unchanged. Separate pure checks prove the schedule decision allocates `0 B` across 10,000 calls and a stable required envelope contained by its guard returns valid `OperationMapRenderRebuildReason.None`. This step tests the accepted guard/apply boundaries; upstream selection/assignment runtime systems remain explicitly open and must publish the version only after fixed commands are ready. No source ownership, candidate/accepted/frozen/production asset, package, EntityScene, or Android artifact changed. |
| 2026-07-29 | `VRP-048` bounded metrics and profiler instrumentation | Accepted Windows GUI-licensing wrappers `%TEMP%\warline-render-virtualization-vrp048.log`, `-initialization.log`, `-architecture.log`, `-contract.log`, and `-selection-regression.log`; `[OperationMapRenderVirtualizationInstrumentationValidation] result=Passed tests=6`; `[OperationMapRenderVirtualizationInitializationFocusedValidation] result=Passed tests=6`; `[OperationMapRenderCellSelectionValidation] result=Passed tests=7`; `[OperationMapRenderVirtualizationValidation] result=Passed tests=43`; `[EcsBurstHotPathArchitectureValidation] result=Passed tests=11`; `Tundra build success`; explicit success markers and wrapper exit code `0`; no leak/exception marker; `git diff --check` | Passed; bounded Phase 4 instrumentation contract accepted | Added the exact five marker names `Initialize`, `SelectCells`, `AssignSlots`, `ApplySlots`, and `SyncState` beneath `OperationMapRenderVirtualization.*`. Initialization and every accepted selection/assignment/apply job now emit their real scope through Burst-compatible static markers; `SyncState` is registered without inventing execution before that system exists. The unmanaged metrics component now carries logical placements/parts, resident exceptions, capacity/enabled/disabled slots, retained/released/rebound assignments, active cells/placements, overflow/highest deficit, command version, and rebuild reason. Initialization publishes immutable counts; a Burst job validates and projects a complete dynamic snapshot fail-closed, including slot parity and overflow/deficit consistency. Formatting is fixed-capacity and explicitly gated before construction; 10,000 disabled formatting and projection calls each measured `0 B`. No direct log or per-frame formatting path was added. Dynamic selection/assignment publication and CPU percentile/graphics/frame recorders remain owned by their future runtime integration and Android evidence steps. No source ownership, candidate/accepted/frozen/production asset, package, EntityScene, or Android artifact changed. |
| 2026-07-30 | `VRP-049` persistent-owner and database-blob lifetime fence | Accepted host-access Windows GUI-licensing wrappers `%TEMP%\warline-vrp049-apply.log` and `%TEMP%\warline-vrp049-init.log`; `[OperationMapRenderProxyApplySystemValidation] result=Passed tests=8`; `[OperationMapRenderVirtualizationInitializationFocusedValidation] result=Passed tests=7`; `Tundra build success`; explicit success markers and wrapper exit code `0`; `git diff --check` | Passed; Phase 4 lifecycle boundary accepted | The parallel apply job now carries a read-only `ComponentLookup<OperationMapRenderDatabaseComponent>` for its exact command/database owner and resolves the blob through that tracked owner inside the job. ECS database-component removal therefore waits for the apply dependency chain before entity/blob ownership can be released. Apply-owner removal completes the in-flight slot write, then disposes and clears the exact persistent failure array; system destruction completes the same chain before disposal. Initialization database removal disposes its exact persistent slot/logical/placement maps and returns observable capacity to zero. Focused tests prove owner-removal and system-destroy completion, final slot generation, and both persistent owners returning to uncreated/zero capacity. Initial sandbox attempts never reached project import because the inherited environment contained duplicate `Path`/`PATH` keys and the sandboxed Editor could not reach the existing licensing IPC client; only exact failed wrapper-owned Unity PIDs were closed under the standing authorization. The identical checked-in wrappers passed with host access. No steady-state completion, structural mutation, source ownership, candidate/accepted/frozen/production asset, package, EntityScene, or Android artifact changed. |
| 2026-07-30 | `VRP-050` exact render-only pilot freeze | Rejected compile wrapper `%TEMP%\warline-vrp050.log`; accepted host-access Windows GUI-licensing wrappers `%TEMP%\warline-vrp050-rerun.log` and `%TEMP%\warline-vrp050-determinism.log`; `[OperationMapRenderVirtualizationPilotFreezeValidation] result=Passed placements=9721 rows=11299`; `Tundra build success`; explicit success markers and wrapper exit code `0` twice; unchanged report SHA-256 `b2523dd8b2f16e73a5cf5e12b25a045d9e88af11072a3a86306ef9d467904e75`; `git diff --check` | Passed; exact additive pilot selection frozen, enablement remains off | The accepted inventory/logical-placement/prototype evidence selects its complete stable-owner-joined repeated render-only Vegetation/Prop set: 5,423 Vegetation placements/rows and 4,298 Prop placements expanding through compound recipes to 5,876 rows, for 9,721 exact placement identities, 22 repeated prototypes, 26 parts, and 11,299 rows. Every identity is unique and strictly ordered by the accepted unsigned projection, every prototype has more than one placement, every placement remains state-owner `-1` with `Any` visual state, and category/prototype/part-row counts reconcile back to the accepted source hashes. The three inventory-excluded Prop rows remain resident. The report freezes all exact full stable owner ids plus projected identities and prototype identities under canonical selection hash `76854a3a72f5553ab6c9108f44567cc1d57e88915867b9adfb1fb5ba0c773bae`; two unchanged accepted runs produced byte-identical report output. The first compile was rejected because Unity's .NET profile lacks the three-argument `File.Move` overload; the report-only replace path now uses the supported API, and only that failed wrapper-owned Editor was closed. `mutationAuthorized=false` and `selectionEnabled=false`; no definition, bake config, scene, source ownership, candidate/accepted/frozen/production asset, package, EntityScene, or Android artifact changed. |
| 2026-07-30 | `VRP-051` persisted candidate-only pilot enablement | Rejected contract compile `%TEMP%\warline-vrp051-contract.log`; accepted contract `%TEMP%\warline-vrp051-contract-rerun.log` 43/43; accepted transaction `%TEMP%\warline-vrp051-enable.log`; unchanged persisted rerun `%TEMP%\warline-vrp051-persisted-rerun.log`; `[OperationMapRenderVirtualizationPilotEnablement] result=Passed placements=9721 rows=11299 slots=704 candidateMode=VirtualizedProxyPool productionCutover=0`; `[OperationMapRenderVirtualizationCandidateValidation] result=Passed passes=2`; identical report SHA-256 `13800b4c38d626daa802e44d38c952f19653ae826d5f6a98dc5917109f6ee8da`; runtime initialization `%TEMP%\warline-vrp051-runtime-rerun.log` 8/8; proxy apply `%TEMP%\warline-vrp051-apply-rerun2.log` 8/8; ECS architecture `%TEMP%\warline-vrp051-architecture-rerun.log` 11/11; explicit success markers and wrapper exit code `0` | Passed; exact pilot persisted and enabled only on candidate | A transactional owner changed only the dense candidate definition to `VirtualizedProxyPool`, persisted exactly one configured virtualization authoring root in its candidate scene, and rolled back both files on any failure. Two unchanged direct-bake passes reproduced fingerprint `4d859a9a...2d17d`, 22 prototypes, 26 parts, 9,721 placements, 1,635 cells, two buckets, 704 fixed slots, 11,299 stripped eligible rows, and 70,710 resident authoring rows; the saved report remained byte-identical on a second invocation. Runtime wiring now chains bounded cell selection, deterministic assignment, command publication, parallel slot apply, and post-apply initial-view publication through dependencies, while a camera still inside the materialized guard performs no rebuild or command publication. The first runtime test rejected an uncreated zero-length canonical state array; the accepted render-only path owns a constructed zero-length persistent array. A later apply run exposed and rejected managed query-array creation under Burst; `EntityQueryBuilder` now constructs both queries without that debt, and the architecture list removed the coordinator's now-stale bootstrap exemption. Production stays `StaticSceneChunks + ResidentEntities`, `productionCutover=0`; accepted/frozen/production/Addressables assets and Android artifacts were unchanged. Packed movement/guard-band/overflow proof remains VRP-052. |
| 2026-07-30 | `VRP-052` packed virtualized camera route | Rejected sandboxed/startup attempts `%TEMP%\warline-vrp052-playmode-d3d11-rerun2.log` through `-rerun8.log`; accepted host-access GUI-licensing wrapper `%TEMP%\warline-vrp052-playmode-host.log`; Test Framework `%TEMP%\warline-vrp052-playmode-host.xml` 1/1 in `13.169 s`; `[DensePackedRenderVirtualizationRoute] result=Passed placements=9721 rows=11299 capacity=704 active=319 rebuilds=4 overflow=0`; initialization EditMode `%TEMP%\warline-vrp052-init-editmode-host.xml` 9/9; ECS architecture `%TEMP%\warline-vrp052-ecs-architecture-host.log` 11/11; explicit Test Framework/architecture success markers and wrapper exit code `0`; `git diff --check` | Passed; packed motion, guard-band, deterministic reuse, and zero-overflow gate accepted | The isolated Windows candidate content binds the current virtualized candidate definition/SubScene fingerprints and loads 9,721 logical placements, 11,299 logical renderer rows, and 704 fixed slots. A packed PlayMode route proves zoom/rotation and an adjacent slow pan remain within the materialized guard without rebuilding, while a separated fast pan, far teleport, and return to origin produce exactly four rebuilds. Returning to the original envelope reproduces the exact sorted active slot bindings; every observed stage reconciles enabled plus disabled slots to 704, reports zero overflow and zero highest deficit, and returns with 319 active slots. The low-level packed fixture now creates its explicit active-map projection after package readiness; runtime initialization treats zero active maps as transient during composition but still fails closed on duplicates or mismatched ownership. Sandboxed retries exposed compile, fixture-ownership, and licensing-channel failures and contribute no pass claim; the repository-documented host-access wrapper workaround produced all accepted results. Unity material float-format drift and transient test scenes were removed. Production, accepted/frozen assets, Addressables settings, Jenkins paths, Android artifacts, and source representation ownership remain unchanged. Android before/after capture is VRP-053. |
| 2026-07-30 | `VRP-053` Android before/after pilot capture | Instrumentation contract `%TEMP%\warline-vrp053-metrics-contract-final.log` 18/18; rejected long-path profiler builds `%TEMP%\warline-vrp053-profiler-metrics-apk.log` and `-short-gradle-rerun.log`; accepted short-path checked wrapper `%TEMP%\warline-vrp053-profiler-metrics-apk-subst.log`; profiler APK 583,131,036 bytes / SHA-256 `4eaf7858...269817b`; paired wireless Xiaomi `24090RA29G`; raw capture 504,864,320 bytes / frames 0..900; checked wrapper exporter `%TEMP%\warline-vrp053-after-profiler-export.log`; `[ProfilerCaptureSummaryExporter] result=Passed frames=500 range=300..799`; JSON parse; `git diff --check` | Capture complete; Android result and pilot expansion rejected | An opt-in profiler-only `-warlineVrp053Metrics` path reports bounded virtualization state at most once per second and remains absent from ordinary players. The same APH-804 stationary-input Match route measured 10.6 FPS average, 93.94 ms average / 110.68 ms p95 frame, 93.93 ms average / 110.66 ms p95 CPU active, and 15.53 ms average / 16.59 ms p95 GPU. The device loaded 9,721 placements, 11,299 parts, 70,710 resident rows, and 704 fixed slots, but enabled zero with zero active cells and placements despite initial rebuild reason 1 / command version 1. Raw markers rank `UnitFactionTintTargetBackfillSystem` at 51.722 ms/frame, `UnitMassRenderSettingsSystem` at 17.318 ms/frame, and Entities Graphics at only 0.683 ms/frame. The release recorder independently rejected at approximately 8.64 FPS and retained 2,168,541 KiB final PSS, but lacked required profiler counters. Exact camera-pose image parity is qualified because the historical before artifact did not serialize its pose; the after view showed no obvious gross hole. Windows path-limit attempts and an unavailable release-counter attempt remain rejected rather than hidden. Production, accepted/frozen assets, source representation ownership, Jenkins/Unity paths, and production cutover remain unchanged. VRP-054 must reject expansion, correct initial-view publication and unchanged whole-world presentation scans, then rerun Android before any broader mutation. |
| 2026-07-31 | `VRP-054` Windows remediation substep | Rejected sandbox licensing attempts; rejected host compile `%TEMP%\warline-vrp054-camera-host.log` (`EA0009`); rejected hand-built camera fixture `%TEMP%\warline-vrp054-camera-host-rerun.log`; rejected nonexistent architecture execute method `%TEMP%\warline-vrp054-ecs-architecture-host.log`; accepted host-access GUI-licensing wrappers `%TEMP%\warline-vrp054-camera-host-final.log` 9/9, `%TEMP%\warline-vrp054-unit-render-host.log` 36/36, `%TEMP%\warline-vrp054-faction-tint-host.log` 4/4, `%TEMP%\warline-vrp054-windows-steady-state.log`, and `%TEMP%\warline-vrp054-ecs-architecture-host-rerun.log` 11/11; `[VRP054WindowsSteadyState] result=Passed renderChildren=70710 samples=300 p95Ms=0.0016 budgetMs=8.3333`; `git diff --check` | Windows remediation passed; Android remeasurement and VRP-054 acceptance remain open | Camera residency now projects the production snapshot frustum against the baked presentation height range before safety/guard expansion. The mass-render parent walk checks operation-map identity/authored-vehicle exclusions before incomplete-unit retry, so map parents carrying `UnitGrid`/`Faction` drain instead of reappearing every frame. That one-shot classification supplies missing unit tint defaults, and the legacy backfill excludes already classified renderers. The exact dense resident child count drains within the bounded pass count; 300 stationary updates of the two former owners fit the interim 120-Hz Editor CPU budget. This proxy is not a whole-frame FPS result and cannot accept Android expansion. `VRP-054` stays unchecked, checklist progress stays `45 / 80`, Phase 6 stays blocked, and production/protected assets/Android artifacts remain unchanged. |
| 2026-07-31 | `VRP-054` exact packed-resident Android acceptance | Rejected deep-parent Android sample retained as diagnostic (`91.79ms` CPU-main p95; old owners still dominant); rejected first focused resident-buffer run on the pre-existing transient GC assertion after the new test passed; accepted rerun `%TEMP%\warline-vrp054-resident-buffer-focused-rerun.log` `[UnitRenderBudgetFocusedValidation] result=Passed tests=38`; accepted exact 70,710-row Windows steady-state `%TEMP%\warline-vrp054-resident-buffer-steady-state.log` `[VRP054WindowsSteadyState] result=Passed renderChildren=70710 samples=300 p95Ms=0.0025 budgetMs=8.3333`; checked profiler APK build `%TEMP%\warline-vrp054-resident-buffer-profiler-apk.log`; paired Xiaomi `24090RA29G`; raw capture SHA-256 `805860c3...d866`; checked export `%TEMP%\warline-vrp054-resident-buffer-profiler-export.log` `[ProfilerCaptureSummaryExporter] result=Passed frames=500 range=300..799`; Android summary and visual review; `git diff --check` | Passed; render-only expansion accepted and Phase 6 dependency-ready | The exact immutable `OperationMapRenderResidentSourceRowComponent` buffer, rather than transform ancestry, now classifies all 70,710 packed resident render entities in bounded 12,000-row passes before legacy unit processing. The corrected APH-803 stationary sample reports 122/704 active slots, 25 active cells, 114 active placements, and zero overflow/deficit. Versus VRP-053, average frame time improves `93.94 -> 21.62ms`, CPU-main p95 `110.66 -> 35.88ms` (`67.6%`), faction-tint backfill `51.722 -> 2.239ms/frame`, and mass render settings `17.318 -> 2.171ms/frame`; no new dominant main-thread owner offsets the gain. The post-capture Android view retains dense-city presentation, HUD, and minimap without an obvious gross hole; exact historical pose parity remains qualified because the before artifact lacks a serialized pose. This accepts only VRP-054 expansion: the final `16.667ms`/60 FPS gate remains open. Checklist progress is `46 / 80`; production, accepted/frozen assets, source ownership, Addressables, Jenkins/Unity paths, and `productionCutover=0` remain unchanged. |
| 2026-07-31 | `VRP-060` deterministic gameplay-building state-owner indices | Rejected first checked Windows GUI-licensing wrapper `%TEMP%\warline-vrp060-state-owner-indices.log` because the focused test assembly omitted the `Game.Rendering` namespace import; exact failed wrapper-owned Unity PID `17776` was verified and closed; accepted corrected wrapper `%TEMP%\warline-vrp060-state-owner-indices-rerun.log`; `[OperationMapRenderVirtualizationValidation] result=Passed tests=44`; explicit wrapper exit code `0`; `git diff --check` | Passed; deterministic state-owner index contract accepted | Stateful logical rows must already agree on one nonnegative index per collision-checked building identity. The post-baking replacement gate now additionally sorts those owner identities by unsigned `(Low, High)` and requires exact contiguous indices `0..N-1`, rejecting sparse, arbitrary, or reordered mappings before any resident presentation component is removed. Focused coverage proves input enumeration order does not affect the accepted mapping and a reversed mapping fails closed; the real-authoring bake copies index `0` to the canonical building while preserving its authoritative destroyed-state component and gameplay entity. This step establishes index ownership only: the current Vegetation/Prop pilot remains render-only, no building family or protected asset was mutated, and VRP-061 destruction adaptation remains open. Checklist progress is `47 / 80`; production, accepted/frozen assets, source ownership, Addressables, Jenkins/Unity paths, Android artifacts, and `productionCutover=0` remain unchanged. |
| 2026-07-31 | `VRP-061` resident/virtualized canonical building destruction split | Rejected first checked wrapper `%TEMP%\warline-vrp061-building-destruction.log` because the new test retained a buffer safety handle across structural changes; accepted corrected wrapper `%TEMP%\warline-vrp061-building-destruction-rerun.log` and final overflow-guard rerun `%TEMP%\warline-vrp061-building-destruction-final.log`; `[OperationMapBuildingDestructionValidation] result=Passed tests=4`; ECS/Burst guardrail `%TEMP%\warline-vrp061-ecs-architecture.log` `[EcsBurstHotPathArchitectureValidation] result=Passed tests=11`; explicit wrapper exit code `0`; `git diff --check` | Passed; canonical destruction semantics preserved for both presentation modes | Resident buildings still execute the accepted render-root scale transition and enable canonical destroyed state once. The disjoint virtualized Burst job receives no transform or proxy lookup: at zero health it enables the same authoritative `OperationMapBuildingDestroyedComponent` and appends one `Destroyed` event with the building's deterministic state-owner index and a monotonic nonzero version to the exact map-owned buffer. It does not search bindings, write render transforms, instantiate visuals, or structurally change gameplay entities. Focused coverage proves one event only, unchanged entity/blocker ownership, unchanged unrelated proxy transform, and fail-closed missing buffer; overlap, duplicate buffer ownership, invalid version, and conservative version overflow reject before scheduling. Event bounding/consumption and visible/off-camera synchronization remain VRP-062/064. Checklist progress is `48 / 80`; the current pilot remains render-only and production, accepted/frozen assets, source ownership, Addressables, Jenkins/Unity paths, Android artifacts, and `productionCutover=0` remain unchanged. |
| 2026-07-31 | `VRP-062` bounded state changes and jobified synchronization | Rejected compile `%TEMP%\warline-vrp062-state-sync.log` (`CS1654`); rejected test-fixture ECB-owner assumptions `%TEMP%\warline-vrp062-state-sync-rerun.log` and `-final.log`; accepted dependency-chain and final focused wrappers `%TEMP%\warline-vrp062-state-sync-dependency-clear.log` and `-steady-state-final.log` 4/4; destruction producer `%TEMP%\warline-vrp062-building-destruction-accepted.log` 4/4; rejected cell-selection wrapper `%TEMP%\warline-vrp062-cell-selection.log` after its 7/7 pass marker because Unity crashed during shutdown, then accepted clean `%TEMP%\warline-vrp062-cell-selection-rerun.log` 7/7; initialization `%TEMP%\warline-vrp062-initialization-final.log` 9/9; virtualization contract `%TEMP%\warline-vrp062-contract.log` 44/44; rejected incorrect architecture execute-method name `%TEMP%\warline-vrp062-ecs-architecture.log`; accepted `%TEMP%\warline-vrp062-ecs-architecture-accepted.log` 11/11; explicit wrapper success markers; `git diff --check` | Passed; bounded canonical state synchronization accepted | The map owner now carries exact canonical states, a persistent producer sequence, and synchronization revision/count state. Initialization validates and rebuilds canonical state from contiguous virtualized gameplay buildings once per map generation, resets stale generation events, and leaves the no-event steady-state path without placement/cell rescans. Valid batches are bounded by state-owner count, strictly sequential, unique by owner, and applied by Burst before a dependency-chained buffer clear. Only affected placements and their containing cells are marked dirty. A revision change re-runs selection/assignment for the currently materialized envelope, releasing intact rows and assigning destroyed rows when visible; off-camera state remains canonical without a proxy. Simultaneous camera and state changes honor a newly required envelope. No Android run was required for this bounded ECS contract. Checklist progress is `49 / 80`; the current pilot remains render-only and production, accepted/frozen assets, source representation ownership, Addressables, Jenkins/Unity paths, Android artifacts, and `productionCutover=0` remain unchanged. |
| 2026-07-31 | `VRP-063` complete generated House-family ownership | Host-access Windows GUI-licensing inventory `%TEMP%\warline-vrp063-house-inventory-host.log`; database determinism `%TEMP%\warline-vrp063-database-determinism.log`; two-pass direct bake `%TEMP%\warline-vrp063-direct-bake.log`; state synchronization `%TEMP%\warline-vrp063-state-sync.log` 4/4; building destruction `%TEMP%\warline-vrp063-building-destruction.log` 4/4; initialization `%TEMP%\warline-vrp063-initialization.log` 9/9; ECS/Burst architecture `%TEMP%\warline-vrp063-ecs-architecture.log` 11/11; rejected stale 704-slot contract expectation `%TEMP%\warline-vrp063-contract-final.log`; accepted corrected `%TEMP%\warline-vrp063-contract-final-rerun.log` 45/45 | Passed; one complete stateful building family is candidate-virtualized | The eligibility/bake contract selects the complete generated House family only, discovers 3,638 canonical gameplay-building source owners, and requires one intact plus one destroyed recipe for every owner. Those owners map deterministically to contiguous state indices and 7,276 state-linked logical placements; the prior 9,721 render-only placements retain independent source ownership. The database now contains 22,058 eligible renderer/material rows, 7,291 prototypes, 10,778 parts, 16,997 placements, 1,651 cells, and 929 slots; exact direct-bake ownership removes 22,058 of 82,009 source render rows and leaves 59,951 resident. Another 7,572 non-House building-family rows remain explicitly deferred. Two unchanged builds reproduced content hash `936bf85c...3709`, ordering hash `31dac1ed...719d`, config SHA-256 `c522c52f...c507`, and direct-bake fingerprint `4a436bd6...e785`. Direct bake passed despite an Editor-only unique-Entity-name capacity diagnostic; no acceptance gate was weakened. No Android run was required for this ownership step. Checklist progress is `50 / 80`; VRP-064 and final 16.667 ms/60 FPS acceptance remain open, while production, accepted/frozen assets, Addressables, Jenkins/Unity paths, and `productionCutover=0` remain unchanged. |
| 2026-07-31 | `VRP-064` visible/off-camera compound state and recycling acceptance | Initial single-part focused wrapper `%TEMP%\warline-vrp064-state-scenarios.log` 8/8; strengthened compound wrapper `%TEMP%\warline-vrp064-compound-state-scenarios.log` 8/8 and final name-cleanup rerun `%TEMP%\warline-vrp064-compound-state-scenarios-final.log` 8/8; building destruction `%TEMP%\warline-vrp064-building-destruction.log` 4/4; initialization `%TEMP%\warline-vrp064-initialization.log` 9/9; virtualization contract `%TEMP%\warline-vrp064-contract.log` 45/45; ECS/Burst architecture `%TEMP%\warline-vrp064-ecs-architecture.log` 11/11; checked host-access GUI-licensing wrappers with explicit success markers; `git diff --check` | Passed; bounded Windows/editor state-transition and recycling gate accepted | The strengthened fixture models two distant canonical building owners sharing one bucket of three fixed proxy slots. Each intact recipe owns three parts and each destroyed recipe owns two. Visible destruction removes every intact command and publishes only the destroyed compound; off-camera destruction leaves the currently visible owner unchanged and materializes the destroyed recipe on return; camera recycle/return reuses exact slots `0..2` deterministically; alternating destruction of both owners proves no intact/destroyed state or part identity leaks through recycled slots. Canonical states remain independently indexed and authoritative throughout. This bounded acceptance changes tests only: runtime behavior, candidate database, production, accepted/frozen assets, Addressables, Jenkins/Unity paths, Android artifacts, and `productionCutover=0` remain unchanged. Checklist progress is `51 / 80`; gameplay-consumer independence is VRP-065 and real-device destruction/recycling remains VRP-095. |
| 2026-07-31 | `VRP-065` canonical gameplay-consumer independence | Checked Windows GUI-licensing wrappers `%TEMP%\warline-vrp065-architecture.log` 12/12, `%TEMP%\warline-vrp065-selection.log` 12/12, `%TEMP%\warline-vrp065-targeting.log` 4/4, `%TEMP%\warline-vrp065-minimap.log` 4/4, `%TEMP%\warline-vrp065-blockers.log`, and `%TEMP%\warline-vrp065-contract-final.log` 45/45; rejected wrong execute-method attempt `%TEMP%\warline-vrp065-contract.log`; explicit success markers; `git diff --check` | Passed; selection, targeting, minimap, navigation, and blockers remain canonical-state consumers | The fail-closed ECS architecture suite now discovers the five gameplay-consumer families under Systems/Composition, requires concrete canonical consumer files and their stable gameplay tokens, and rejects references to proxy slots, commands, databases, virtualization metrics/state, canonical render state/change events, virtualized presentation, or `MaterialMeshInfo`. Functional suites independently pass selection caching, AI targeting, minimap marker retention/projection, and movement/blocker behavior, while the full virtualization contract remains 45/45. This bounded step changes tests only and required no Android run; runtime behavior, candidate database, production, accepted/frozen assets, Addressables, Jenkins/Unity paths, Android artifacts, and `productionCutover=0` remain unchanged. Checklist progress is `52 / 80`; complete-family expansion is next at VRP-066 and real-device destruction/recycling remains VRP-095. |
| 2026-07-31 | `VRP-066` complete eligible generated-building-family expansion | Inventory `%TEMP%\warline-vrp066-building-family-inventory.log`; rejected first database determinism pass on a transient atomic-replace `IOException`, accepted rerun `%TEMP%\warline-vrp066-database-determinism-rerun.log`; rejected initial real bake on the obsolete single-submesh pilot restriction and rejected first reconciled bake on stale logical-row/physical-renderer accounting; accepted `%TEMP%\warline-vrp066-direct-bake-reconciled.log` two-pass fingerprint `a9d4ec5b...e99f`; final virtualization `%TEMP%\warline-vrp066-contract-final.log` 45/45; readiness `%TEMP%\warline-vrp066-readiness.log` 11/11; state sync `%TEMP%\warline-vrp066-state-sync.log` 8/8; destruction `%TEMP%\warline-vrp066-building-destruction.log` 4/4; `git diff --check` | Passed; all currently eligible repeated generated building families are candidate-virtualized | Family-atomic selection retains complete House (3,638 owners / 10,759 rows) and adds complete Shop (1,295 owners / 7,210 rows). CityHall stays wholly resident: all 406 rows carry the stable `gameplay-building-family-unsupported-render-policy` reason because 44 family rows violate the closed policy; the current dense generator emits no Tent or Military family. The database deterministically reproduces content hash `f944cc7a...f5f5`, ordering hash `71a0dc3b...a7d4`, and config SHA-256 `9cb26b02...48ac`, with 29,268 logical rows, 19,587 placements, 9,875 prototypes, 17,982 parts, 1,660 cells, and 1,364 slots. Unity converts 187 selected multi-material Shop rows to additional per-submesh render entities, so readiness now records both 29,268 logical rows and 29,081 physical source renderers; exact asset-disambiguated reconciliation removes every selected renderer and leaves 52,928 of 82,009 physical source renderers resident. No Android run was required for this bounded ownership step; VRP-067 owns the real-device House/Shop destruction-transition visual/FPS matrix. Production, accepted/frozen assets, Addressables, Jenkins/Unity paths, Android artifacts, and `productionCutover=0` remain unchanged. Checklist progress is `53 / 80`; final 60 FPS acceptance remains open. |
| 2026-08-01 | `VRP-067` Android destruction-transition matrix | Recorder `%TEMP%\warline-vrp067-instrumentation-rerun.log` 21/21; runtime binding `%TEMP%\warline-vrp067-runtime-binding-movedfrom.log` 10/10; destruction/AOT `%TEMP%\warline-vrp067-destruction-aot-final.log` 4/4; checked profiler APK `%TEMP%\warline-vrp067-profiler-apk-final2.log`; APK SHA-256 `b957be05...3952`; paired Xiaomi `24090RA29G`; exact House/Shop launch markers, 120-frame before/after samples, and settled screenshots; structured JSON; `git diff --check` | Rejected; canonical state advanced but capacity/visual gates failed | House baseline/post p95 was `32.784/32.780 ms`, transition `16.367 ms`, sequence `1`, 106 active cells, 1,352 enabled slots, 875 overflow, and 876 highest deficit. Shop baseline/post p95 was `32.769/32.756 ms`, transition `49.129 ms`, sequence `1`, 108 active cells, 1,352 enabled slots, 790 overflow, and 801 highest deficit. Both settled captures retained the intact visible building, so neither variant passes. Tent and Military are honestly unavailable because the current generator emits neither family. The existing capacity report is explicitly provisional (`visible=0`, safety `1`, guard `1`, 5x5 envelope; camera/projection sweep pending), while these real views selected 106/108 cells and demanded at least 2,227/2,142 rows from enabled slots plus overflow. VRP-067 stays unchecked, VRP-070 stays blocked, checklist progress stays `53 / 80`, and no fail-closed gate, protected/production asset, Addressables/Jenkins/Unity path, or `productionCutover=0` changed. |
| 2026-08-01 | `VRP-067` exact materialized-envelope diagnostic substep | Initial resolver launch rejected before Unity because the parent shell blocked scripts; corrected checked wrapper `%TEMP%\warline-vrp067-envelope-instrumentation.log` `[AndroidPerformanceRecorderValidation] result=Passed tests=22`; rejected checked Android build `%TEMP%\warline-vrp067-envelope-profiler-apk.log` on Windows path length; rejected short-path build `%TEMP%\warline-vrp067-envelope-profiler-apk-shortpath.log` on stale duplicate Gradle compressed-assets intermediates; accepted clean checked short-path build `%TEMP%\warline-vrp067-envelope-profiler-apk-clean-shortpath.log` `[DenseCityCandidateAndroidPackage] result=Passed`; APK SHA-256 `463bac7d...befcd`; `git diff --check` | Passed bounded instrumentation/build step; Android envelope capture remains pending | The opt-in matrix output now includes exact inclusive `ActiveEnvelopeMin:ActiveEnvelopeMax` coordinates alongside cells, placements, slots, overflow, and deficit. Focused ECS coverage proves the recorder reads all values from the single map-owned metrics/state owner. The repository wrapper remained mandatory; a temporary `W:` alias avoided the confirmed 260-character Gradle/Ninja path, and only the verified generated `compressed_assets` intermediate was cleared to recover the second failure. The fresh APK is 696,278,899 bytes and passed the candidate package/production-safety gate. The Xiaomi remained reachable at `192.168.2.171`, but mDNS advertised no adb service and its two open high ports could not be safely distinguished as pairing versus connect; no new Android result is claimed until the main Wireless debugging screen's current `IP address & port` reconnects. VRP-067 stays unchecked, checklist progress stays `53 / 80`, and production/protected assets, Addressables/Jenkins/Unity paths, and `productionCutover=0` remain unchanged. |
| 2026-08-01 | `VRP-067` normalized intact visuals, exact capacity, and accepted Android matrix | Focused intact normalization `%TEMP%\warline-vrp067-intact-normalization-focused-final.log` 3/3; exact inventory `%TEMP%\warline-vrp067-intact-normalized-inventory-accepted.log`; unchanged database determinism `%TEMP%\warline-vrp067-intact-normalized-database-determinism.log`; two-pass direct bake `%TEMP%\warline-vrp067-intact-normalized-direct-bake-final.log`; virtualization `%TEMP%\warline-vrp067-intact-normalized-virtualization-final.log` 45/45; runtime-content rollback `%TEMP%\warline-vrp067-runtime-content-rollback-focused.log` 42/42; checked profiler APK `%TEMP%\warline-vrp067-intact-normalized-profiler-apk-final.log`; APK SHA-256 `34347e15...9cbf`; paired Xiaomi `24090RA29G`; exact House/Shop 120-frame before/after markers and reviewed captures; structured schema-2 JSON; `git diff --check` | Passed; VRP-067 state, visual, and zero-overflow capacity gates accepted | Source prefab inspection found every House/Shop intact prefab co-located active direct `Model` and `Destroyed` branches. Candidate-only realization now filters and removes the embedded destroyed branch while readiness rejects any unnormalized pair; source/accepted/frozen/production prefabs remain unchanged. Exact reconciliation removes 4,396 duplicate packed rows (`82,797 -> 78,401`) and accepts 24,904 eligible logical rows, 24,717 selected physical renderers, 52,928 resident physical renderers, 19,614 placements, 9,879 prototypes, 13,595 parts, 1,660 cells, and 3,858 slots. The schema-2 12x9/9x12 all-grid sweep retains at least 20% per-bucket headroom. Android House reports p95 `24.954 -> 24.951 ms`, transition `16.638 ms`, 1,856 slots, 106 cells, 1,260 placements, sequence `1`, zero overflow/deficit, and a visible intact-to-destroyed target change. Shop reports p95 `33.277 -> 33.284 ms`, transition `16.531 ms`, 1,765 slots, 108 cells, 1,225 placements, sequence `1`, zero overflow/deficit, and the same visual-state pass. Tent/Military remain unavailable. Checklist progress is `54 / 80`; VRP-070 is dependency-ready, but final 60 FPS acceptance remains open and no protected/production asset, Addressables/Jenkins/Unity path, or `productionCutover=0` changed. |
| 2026-08-02 | `VRP-070` generated Infrastructure and noninteractive-attachment expansion | Rejected initial focused fixture `%TEMP%\warline-vrp070-infrastructure-policy.log` on invalid synthetic stable ids; accepted corrected `%TEMP%\warline-vrp070-infrastructure-policy-rerun.log` 46/46; exact inventory `%TEMP%\warline-vrp070-infrastructure-inventory.log`; unchanged-input database determinism `%TEMP%\warline-vrp070-database-determinism.log`; rejected first direct bake `%TEMP%\warline-vrp070-direct-bake.log` on a stale expectation that counted 304 supported-component owners whose rows all use unsupported render policy; accepted two-pass rerun `%TEMP%\warline-vrp070-direct-bake-rerun.log`; final nested-identity contract `%TEMP%\warline-vrp070-contract-final2.log` 46/46; unchanged final inventory `%TEMP%\warline-vrp070-infrastructure-inventory-final.log`; `git diff --check` | Passed; all component-safe supported-policy generated Infrastructure is candidate-virtualized | A closed owner policy admits only generated `RenderOnly + Infrastructure` hierarchies containing transforms, mesh filters, and plain mesh renderers without property blocks. Animation, lights, particles, physics, LOD ownership, custom behaviours, special/unsupported renderers, missing or nested identities/scripts, and per-instance material overrides fail closed to stable resident reasons. All 22,172 generated Infrastructure owners pass that component policy; 21,868 have at least one supported-policy row and contribute exactly 38,675 virtualized rows, while 304 unsupported-policy rows stay resident. The deterministic database has content hash `4781c9ab...692c`, ordering hash `da64461e...9364`, config SHA-256 `ef35a206...80f`, 63,579 eligible logical rows, 63,392 selected physical renderers, 41,482 placements, 9,909 prototypes, 13,673 parts, 1,934 cells, two buckets, and 7,075 fixed slots. Two direct bakes reproduce fingerprint `0a6ebfc7...0724`, remove every selected renderer, leave 14,222 packed resident physical rows, and retain `productionCutover=0`. Current bucket capacities retain at least 20% measured-envelope headroom and are below 8,000 slots; the provisional 41,482 logical-placement MMI proxy remains over the later 24,000 VRP-075 target and is recorded, not weakened. No Android run was required for this ownership/classification step. Reported buildings-over-roads, non-blue canal, and road-over-water-without-bridge defects remain separate open correctness work. Checklist progress is `55 / 80`; VRP-071 is next. |
| 2026-08-02 | `VRP-071` alpha-clipped/transparent policy classification | Rejected first checked wrapper `%TEMP%\warline-vrp071-policy-contract.log` on one missed internal call-site argument; after verifying the exact wrapper-started process tree, closed only that failed Editor; accepted corrected `%TEMP%\warline-vrp071-policy-contract-rerun.log` 46/46; exact inventory `%TEMP%\warline-vrp071-policy-inventory.log`; final labelled inventory `%TEMP%\warline-vrp071-policy-inventory-final.log`; `git diff --check` | Passed; unsupported transparent content remains resident by stable policy reason | The canonical inventory now publishes exact `byMaterialSurface` and `byPolicyDisposition` audit tables without changing any source-row identity, eligibility, database, capacity, candidate scene, or production output. It classifies 74,123 opaque rows (`59,950` eligible / `14,173` excluded), 3,769 alpha-clipped rows (`3,629` eligible / `140` excluded), and 509 transparent rows (`0` eligible / `509` excluded). The 509 transparent rows all request cast/static shadows, which the closed transparent bucket forbids, so they remain resident under `unsupported-render-policy` and the explicit audit disposition `Resident:TransparentShadowPolicyUnsupported`. Alpha-clipped content remains in the accepted fixed `AlphaClippedShadowsOn` bucket; no transparent bucket or new proxy capacity is emitted. The worktree proves only the inventory report changes beyond the classifier audit code; database hash `4781c9ab...692c`, 63,579 eligible rows, 7,075 slots, protected/production assets, `productionCutover=0`, and Android evidence are unchanged. Checklist progress is `56 / 80`; VRP-072 is next. |
| 2026-08-02 | Independent correctness: clearly blue candidate canal water | Android ADB availability probe found no connected device; rejected sandboxed checked-wrapper launch `%TEMP%\warline-canal-blue-presentation.log` before Unity on duplicate `Path`/`PATH`; rejected first host-access Unity execution on exact floating-point equality; accepted corrected checked wrapper `%TEMP%\warline-canal-blue-presentation-rerun.log` 2/2; `git diff --check` | Windows/Editor asset contract passed; Android visual confirmation pending | The candidate-owned converted material for the canonical canal-water source now uses a saturated blue base color `(0.025, 0.2, 0.55, 0.82)`, while retaining transparent surface type, render queue `3000`, and `RenderType=Transparent`. The candidate conversion transaction reapplies this exact override on regeneration, and the focused gate proves the override is source-path-specific plus checks the persisted candidate material. Source Synty material, accepted/frozen/production assets, render-policy classification, capacity, database, `productionCutover=0`, and VRP-072 remain unchanged. Buildings-over-roads and road-over-water-without-bridge remain open. Checklist progress stays `56 / 80`. |
| 2026-08-02 | Independent correctness: building-road clearance generator correction | Focused checked wrapper `%TEMP%\warline-building-road-clearance.log` 3/3; rejected first full generator `%TEMP%\warline-building-road-clearance-integration.log` after the corrected layout produced 3,539 eligible open-ground patches against the prior exact 3,870 compatibility count; accepted reconciled full generator `%TEMP%\warline-building-road-clearance-integration-rerun.log`; source-scene byte preservation; `git diff --check` | Windows/Editor generator passed; persisted-candidate regeneration/bake and Android visual confirmation pending | Standard-road clearance bounds were centered at the grid corner while rendered road tiles use the cell center, offsetting the rejection footprint by 5 m on both axes. All road types now share the canonical cell center, invalid extents fail closed, and the full deterministic generator audits 4,817 reserved building owners with `buildingOverlaps=0` and measured `roadOverlaps=0`. The exact open-ground compatibility gate is retained at the corrected-layout baseline of 3,539 rather than removed or loosened. The run produced 4,836 semantic buildings, 33,230 surfaces, and 41,865 presentations without changing source-scene bytes. Current persisted candidate scenes/database were not regenerated, so the reported device defect is not yet claimed fixed. VRP-072, production/protected assets, and checklist progress `56 / 80` remain unchanged. |
| 2026-08-02 | Independent correctness: building-road transactional candidate/database reconciliation | Rejected first checked regeneration `%TEMP%\warline-building-road-candidate-regeneration.log` at the unreconciled protected-Autobahn open-ground count `3,456 / 3,539`; accepted forced transactional regeneration `%TEMP%\warline-building-road-candidate-regeneration-rerun.log`; rejected first fresh inventory `%TEMP%\warline-building-road-fresh-inventory-probe.log` at exact stale row count `76,517 / 78,401`; accepted reconciled inventory `%TEMP%\warline-building-road-fresh-inventory-rerun.log`; database source parity/determinism `%TEMP%\warline-building-road-database-reconciliation.log`; two-pass direct bake `%TEMP%\warline-building-road-direct-bake-reconciliation.log`; persisted canal material audit; `git diff --check` | Windows/Editor candidate, inventory, database, capacity, and direct-bake gates passed; Android package/visual confirmation pending | Added an explicit force-regeneration entry point that keeps the existing generation identity while retaining transactional scene/proxy rollback and protected source/config checks. The protected-Autobahn candidate contract keeps its separately measured exact `3,456` open-ground count while the non-Autobahn deterministic fixture remains exactly `3,539`; neither gate became a lower bound. Regeneration produced 4,545 building owners, 31,759 render-only owners, 32,733 surfaces, 2,304 proxy partitions, and 1,108 protected Autobahn tiles. Fresh schema-7 inventory reconciles the historical 4,396 destroyed-duplicate removal plus 1,884 corrected-layout render-row removal to 76,517 total rows, 61,925 eligible rows, 40,460 placements, 9,107 prototypes, 12,293 parts, 1,934 cells, and 6,804 slots with at least 20% per-bucket headroom. Database bytes were identical across two builds at content hash `798728c5...197f`; direct bake was identical across two passes at fingerprint `e45bba84...9a1f`, 61,783 selected physical renderers, 14,017 resident rows, and 4,530 building state owners. The regenerated shared canal material still contains the exact blue `(0.025, 0.2, 0.55, 0.82)` override. Production/protected assets and `productionCutover=0` remain unchanged; the reported Android building-road defect is not claimed closed until a rebuilt device visual passes, and checklist progress stays `56 / 80`. |
| 2026-08-02 | Independent correctness: building-road Android package and visual acceptance | Rejected first checked launch `%TEMP%\warline-building-road-android-profiler-apk.log` because the execute method omitted the `Game.Editor` namespace; accepted corrected checked wrapper `%TEMP%\warline-building-road-android-profiler-apk-rerun.log` `[DenseCityCandidateAndroidPackage] result=Passed`; APK SHA-256 `ad735746...a06a`; paired Xiaomi `24090RA29G`; streamed ADB install; cold `am start -W -S`; readiness/metrics log; reviewed `2026-08-02_BuildingRoadClearance_Android_Base.png` and `2026-08-02_BuildingRoadClearance_Android_Urban.png`; `git diff --check` | Passed; reported buildings-over-roads defect closed on the rebuilt Android candidate | The exact 696,278,899-byte development APK installed successfully and cold-launched in `839 ms` (`846 ms` wait). The candidate reached full operation-map readiness and stayed foreground. Runtime metrics report 40,460 placements, 61,925 parts, 14,017 resident rows, 1,178 active of 6,804 fixed slots, 50 active cells, 645 active placements, and zero overflow/deficit. The base view and a separately zoomed urban district both show buildings ending beside continuous rendered road surfaces rather than covering them. The known optional Play Asset Delivery `ClassNotFoundException` remains nonfatal for this embedded APK. The urban view exposes harsh black shadow patches; those are retained as Android visual evidence for VRP-072 and are not claimed fixed here. Candidate canal color and road-over-water-without-bridge remain separately tracked, protected/production assets and `productionCutover=0` remain unchanged, and checklist progress stays `56 / 80`. |
| 2026-08-02 | Independent correctness: canonical EntityScene building selection | Initial sandboxed batch/GUI wrappers `%TEMP%\warline-canonical-building-selection-final.log` and `-gui.log` rejected before compilation on the known Hub `1.17.4` / Editor `1.18.1` licensing and machine-identity boundary; accepted host-access checked GUI wrappers `%TEMP%\warline-canonical-building-selection-host.log` 7/7, `%TEMP%\warline-canonical-building-selection-lifecycle.log` 13/13, and `%TEMP%\warline-canonical-building-selection-suite-final.log` aggregate selection suite; `Tundra build success`; `git diff --check` | Windows/Editor selection correction passed; Android confirmation pending | The click lookup now includes live canonical `OperationMapBuildingComponent` entities by exact faction/grid/footprint/health/transform data and excludes prefab, destroyed, and zero-health entities. Focus lifecycle may apply `SelectedUnitTag` and HUD focus to that building, but never adds `UnitMove` or caches it as a movable selection. Existing unit/vehicle selection suites remain green. This corrects only canonical building focus: latest device confirmation is pending, the visible-tent/soldier-production path still consumes legacy managed producers and remains open, and the user-reported blue Autobahn plus road-over-water-without-bridge remain separate defects. No candidate database, render policy, capacity, protected/production asset, Android artifact, or `productionCutover=0` changed; checklist progress stays `56 / 80`. |
| 2026-08-02 | Independent correctness: canonical ECS producer discovery foundation | Checked Windows GUI-licensing wrappers `%TEMP%\warline-operation-map-producer-lookup.log` 3/3 and `%TEMP%\warline-production-camera-focus-regression.log` 10/10; `git diff --check` | Windows/Editor producer lookup passed; queue/spawn integration and Android acceptance remain open | Added a deterministic read-only resolver over live canonical `OperationMapBuildingComponent` producers and their baked `OperationMapBuildingProductionPrefab` rows. It prefers the lowest-placement player producer, then a neutral producer, and rejects prefabs, destroyed/dead buildings, missing prefab entities, and source-key mismatches. This is deliberately not wired to the build popup and cannot spend resources or queue/spawn a soldier yet; that next bounded path must remain ECS-backed and must not restore managed map buildings. The blue Autobahn and missing-bridge defects remain separate. No candidate database, render policy, protected/production asset, Android artifact, or `productionCutover=0` changed; checklist progress stays `56 / 80`. |
| 2026-08-02 | Independent correctness: canonical ECS production-queue contract | Initial checked wrapper `%TEMP%\warline-operation-map-producer-queue.log` 3/3; legacy request regression `%TEMP%\warline-operation-map-production-request-regression.log` 31/31; rejected final wrapper `%TEMP%\warline-operation-map-producer-queue-final.log` timed out after compiler error `CS0177`; corrected final wrapper `%TEMP%\warline-operation-map-producer-queue-final2.log` 3/3; `git diff --check` | ECS queue ownership passed; popup integration, queue consumption/spawn, and Android acceptance remain open | Canonical buildings with baked production prefabs now own an empty inline-capacity ECS request buffer plus a monotonic local request id. The bounded enqueue helper records the exact production index, baked prefab entity, source key, finite queue time, and pending state; it retains the configured combined legacy/canonical global pending limit and rejects missing queue ownership or request-id overflow. The rejected final run exposed unassigned output values on the new non-finite-time early return; explicit fail-closed defaults corrected the compile and the exact rerun passed. The queue is intentionally not connected to UI/economy and has no consumer yet, so it cannot spend resources, finish a request, or spawn a soldier. Legacy managed production remains unchanged and its 31-test request suite is green. The blue Autobahn and missing bridge remain separate. No candidate database, production/protected asset, Android artifact, or `productionCutover=0` changed; checklist progress stays `56 / 80`. |
| 2026-08-02 | Independent correctness: canonical ECS production readiness/FIFO consumption | Checked Windows GUI-licensing wrappers `%TEMP%\warline-operation-map-producer-queue-consumer.log` 3/3 and `%TEMP%\warline-operation-map-producer-queue-timing-regression.log` 3/3; `git diff --check` | Ready-time and exactly-once FIFO completion passed; spawn/UI/economy and Android acceptance remain open | Every canonical request now stores a finite ready time derived from the existing unit-production duration contract. Read-only consumption exposes only the lowest positive pending request id after that time, revalidates the live non-destroyed producer plus exact baked prefab/source/index ownership, and refuses missing prefab entities. Completion removes only that currently ready FIFO request, preventing early, reordered, or duplicate completion. No runtime system invokes completion and no entity is instantiated yet, so resources cannot be spent and soldier production is not claimed fixed. The blue Autobahn and missing bridge remain separate. No candidate database, production/protected asset, Android artifact, or `productionCutover=0` changed; checklist progress stays `56 / 80`. |
| 2026-08-02 | Independent correctness: canonical ECS soldier spawn transaction | Checked Windows GUI-licensing wrapper `%TEMP%\warline-operation-map-producer-spawn-transaction.log` 3/3; `git diff --check` | Explicit spawn transaction passed; runtime scheduling/cell resolution, UI/economy, and Android acceptance remain open | Given a validated ready FIFO request plus an explicit nonnegative cell and finite world position, the transaction instantiates the exact baked unit prefab, applies unit grid/transform/previous-position/faction/source/respawn/cooldown state, resets air/idle state when present, removes stale movement/target/selection state, and only then removes the request. Early time, invalid placement, missing required unit health/footprint, or any completion mismatch leaves the queue intact; a failed post-instantiation completion destroys the provisional instance. No runtime system chooses a cell or invokes this transaction yet, and UI/economy remains disconnected, so device-visible soldier production is not claimed fixed. The blue Autobahn and missing bridge remain separate. No candidate database, production/protected asset, Android artifact, or `productionCutover=0` changed; checklist progress stays `56 / 80`. |
| 2026-08-02 | Independent correctness: canonical ECS occupancy-aware production spawn-cell resolution | Rejected checked wrapper `%TEMP%\warline-operation-map-producer-spawn-cell.log` timed out after compiler errors `CS1654`/`CS1657` in disposable test-container declarations; corrected checked wrappers `%TEMP%\warline-operation-map-producer-spawn-cell-rerun.log` and final `%TEMP%\warline-operation-map-producer-spawn-cell-final.log` 3/3; existing spawn-transaction regression `%TEMP%\warline-operation-map-producer-spawn-transaction-regression.log` 3/3; `git diff --check` | Deterministic valid-cell ownership passed; runtime scheduling, UI/economy, and Android acceptance remain open | The canonical helper now derives a deterministic search origin immediately outside the live producer footprint and resolves a ready FIFO request through the shared grid spawn utility. Exact grid dimensions, finite cell size/origin, and exact walkable/blocked/occupied/reserved mask lengths fail closed; the chosen unit footprint cannot overlap blocked, occupied, reserved, unwalkable, out-of-bounds, or producer-owned cells. The rejected first run was a test-harness compile defect only; explicit `try/finally` disposal corrected it, and the final safeguard temporarily reserves the producer footprint while preserving its prior reservation bits. The checked gates prove deterministic placement, occupied preferred-cell avoidance, producer exclusion, and malformed-grid rejection. No runtime system supplies the live grid masks or invokes resolution/spawn, and the popup/economy path remains disconnected, so device-visible soldier production is not claimed fixed. The blue Autobahn and missing bridge remain separate. No candidate database, production/protected asset, Android artifact, or `productionCutover=0` changed; checklist progress stays `56 / 80`. |
| 2026-08-02 | Independent correctness: canonical ECS production runtime scheduler | Rejected checked wrapper `%TEMP%\warline-operation-map-production-runtime-scheduler.log` before execution on compiler error `CS1654` from deterministic native-list sorting; corrected checked wrapper `%TEMP%\warline-operation-map-production-runtime-scheduler-rerun.log` 3/3; `git diff --check` | Live-grid ready-request scheduling passed; popup/economy and Android acceptance remain open | The building runtime update now processes ready canonical queues against exactly one live grid owner. It orders producers by placement index/entity/request id, resolves at most 16 placements into a shared reservation mask before any structural changes, and then executes the previously accepted spawn transaction. The gate proves a ready request creates one player unit outside its Tent and completes exactly once, a not-ready request stays queued, and duplicate grid ownership preserves the request and spawns nothing. The first run was a compile-only disposal declaration defect; explicit native-list disposal corrected it. The build popup still does not enqueue through the canonical path or charge/refund its economy contract, so the visible-Tent soldier defect and Android interaction acceptance remain open. Blue-Autobahn isolation and the missing bridge remain separate. No candidate database, production/protected asset, Android artifact, or `productionCutover=0` changed; checklist progress stays `56 / 80`. |
| 2026-08-02 | Independent correctness: build-drawer canonical Tent production bridge | Checked Windows wrapper `%TEMP%\warline-operation-map-camp-production-bridge.log` 3/3; existing building-production request regression `%TEMP%\warline-operation-map-camp-production-regression.log` 31/31; `git diff --check` | Build-drawer preflight/economy enqueue passed; Android interaction acceptance remains open | Camp/build-drawer unit preflight now recognizes a live friendly canonical producer with exact baked prefab/source ownership instead of requiring a managed `RuntimeBuildingEntity`. The request keeps legacy producers preferred when present, otherwise spends materials once, enqueues the validated ECS request, refunds on enqueue failure, records the order only on success, and counts both legacy and canonical pending rows against the unchanged global limit. Focused coverage proves a canonical Tent satisfies preflight, one request spends exactly once and writes one pending ECS row, and an existing canonical request rejects at the global limit before spending; the full 31-test managed production suite remains green. Runtime scheduling is already present, but no rebuilt Android package or device interaction was run for this code, so the visible-Tent soldier defect is not yet device-accepted. Blue-Autobahn isolation and the missing bridge remain separate. No candidate database, production/protected asset, Android artifact, or `productionCutover=0` changed; checklist progress stays `56 / 80`. |
| 2026-08-02 | Independent correctness: canonical Tent Android interaction rerun | Rejected long-path checked package on Gradle/Ninja path length; rejected first `W:` short-path wrapper after one ARM64 compile exceeded its timeout; accepted exact short-path retry `%TEMP%\warline-canonical-tent-android-profiler-apk-shortpath-retry.log` with `[DenseCityCandidateAndroidPackage] result=Passed`; APK SHA-256 `93e8c063...c52dcc`; streamed install to paired Xiaomi `24090RA29G`; cold exact-activity launch; main-menu Deploy route; Build/Soldiers/Recruit interaction; 55-second completion observation | Rejected as end-to-end soldier-production acceptance; canonical Tent preflight and spend/enqueue pass, visible spawn remains unproven | The fresh 696,278,899-byte profiler APK installed successfully and cold-launched in `1,088 ms`. The correctly scaled main-menu Deploy control opened the playable base map. A visible Tent satisfied the Soldiers tab: Recruit remained enabled, one Advanced Rifleman request reduced materials from `120` to `95`, and the UI reported that the unit was added to the recruitment queue. However, the queue panel simultaneously displayed `NO PRODUCTION QUEUED`, and after 55 seconds (beyond the shown `00:45` duration) the visible roster remained the same five cards with no confirmed new soldier. Therefore the authored-building production/interaction row stays open; no Android spawn, selection, or lifecycle acceptance is claimed. The on-screen diagnostic was `36 FPS` on initial map arrival and `46 FPS` after the wait, below the 60 FPS target and not a Phase 9 capture. Blue-Autobahn isolation, canal confirmation, and the missing bridge remain separate. Production/protected assets and `productionCutover=0` remain unchanged; checklist progress stays `56 / 80`. |
| 2026-08-02 | Independent correctness: canonical production authored-grid selection | Initial checked batch/GUI wrappers were rejected on the Hub `1.17.4` / Editor `1.18.1` IPC mismatch; after verifying the sole wrapper Editors and restarting only stale Hub/licensing processes, accepted host-access checked GUI wrapper `%TEMP%\warline-operation-map-production-grid-selection-escalated.log` 4/4; `git diff --check` | Windows/Editor scheduler ownership correction passed; rebuilt Android completion remains pending | The Android rejection showed that the canonical Tent accepted and charged the request but never produced a visible soldier. The scheduler previously required exactly one entity carrying the complete grid/mask contract, so a legitimate authored grid plus temporary `RuntimeGridBootstrapGridTag` fallback suppressed all ready work. It now selects the sole complete non-bootstrap grid, falls back to exactly one complete grid only when no authored owner exists, and still fails closed on multiple authored owners. Focused coverage proves ready spawn, early preservation, authored-over-bootstrap selection, and ambiguous-authored rejection. The drawer queue remains legacy-only and no fresh Android package was built for this correction, so the authored-building production/interaction row stays open and no device-visible soldier claim is made. The reported oversized building marker, vehicle selection, blue Autobahn, canal confirmation, and missing bridge remain separate. Production/protected assets and `productionCutover=0` remain unchanged; checklist progress stays `56 / 80`. |
| 2026-08-02 | Independent correctness: candidate runtime-binding component identity and fresh Tent APK | Rejected checked package `%TEMP%\warline-canonical-tent-authored-grid-profiler-apk.log` before Gradle because the candidate binding lost its decoration-baker reference on reload; rejected focused diagnostics `%TEMP%\warline-candidate-runtime-binding-reuse-diagnostic.log`, `-detailed.log`, and rejected strict rebind `%TEMP%\warline-dense-candidate-runtime-binding-rebind.log`; accepted corrected dense layout `%TEMP%\warline-dense-candidate-runtime-binding-rebind-corrected.log`; accepted focused suite `%TEMP%\warline-candidate-runtime-binding-reuse-corrected.log` 23/23; accepted checked profiler package `%TEMP%\warline-canonical-tent-authored-grid-profiler-apk-repaired.log`; APK SHA-256 `0b578c22...e269`; `git diff --check` | Windows/Editor binding repair and fresh package passed; Android interaction remains pending | The baker's migration metadata incorrectly named `Game.Runtime` as its legacy source assembly even though the original global component lived in `Assembly-CSharp`, causing the serialized component to remain present in YAML while Unity loaded the typed `OperationMapSceneView` reference as null. The migration identity now names `Assembly-CSharp`; candidate regeneration also resolves exactly one `CombinedMeshBaker`, requires it on the existing decoration root, and explicitly rebinds it before the unchanged post-reload validator. The validator now identifies each missing required reference without weakening the gate. Both persisted candidate binding scenes pass the 23-test no-op/rollback suite, and the fresh 696,278,899-byte APK includes the authored-grid scheduler correction and passes the candidate package/production-safety marker. This heartbeat's single ADB probe discovered `192.168.2.171:39049` but the endpoint refused the connection, so install, main-menu Deploy, Tent Recruit, visible soldier completion, canal/Autobahn color, marker scale, and vehicle selection are not claimed. The authored-building production/interaction row stays open, VRP-072 remains unstarted, production/protected assets and `productionCutover=0` remain unchanged, and checklist progress stays `56 / 80`. |
| 2026-08-02 | Independent correctness: repaired Tent APK Android completion rerun | Exact 696,278,899-byte checked profiler APK SHA-256 `0b578c22...e269`; bundled-SDK ADB connection to paired Xiaomi `24090RA29G` at `192.168.2.171:46849`; streamed `install -r -d`; cold exact-activity launch; main-menu Deploy; `%TEMP%\warline-fresh-tent-recruit-queued.png`; `%TEMP%\warline-fresh-tent-closed.png`; Android `FrameRateDiag` before/after unit counts | Rejected; Recruit acknowledgement did not produce a canonical soldier | The repaired package installed successfully, cold-launched the exact activity in `835 ms` (`842 ms` wait), reached full dense-map readiness through the main-menu Deploy control, and exposed a selectable canonical Contractor Tent whose Soldiers tab enabled Recruit. Recruiting Heavy Gunner Male I displayed the success acknowledgement, but the materials display remained `120/60`, the drawer simultaneously retained `NO PRODUCTION QUEUED`, and after more than the shown `00:45` duration the runtime remained exactly `units=5005`, `sourceKeys=5005` with no visible soldier completion. The fresh authored-grid and runtime-binding corrections therefore do not close the Android production defect; the parent interaction row remains open and VRP-072 remains unstarted. The selected Tent's cyan marker was bounded to that Tent footprint in the observed frame, but this single Tent view does not close the separately reported oversized building-marker defect; vehicle selection, blue Autobahn/canal isolation, and the missing bridge also remain separate and unaccepted. Observed diagnostics were typically about `39-50 FPS` with GPU samples near `18-27 ms`, below the target but not a formal Phase 9 route. Production/protected assets and `productionCutover=0` remain unchanged; checklist progress stays `56 / 80`. |
| 2026-08-02 | Independent correctness: canonical production scheduler stall diagnostics | Checked Windows GUI-licensing wrapper `%TEMP%\warline-operation-map-production-scheduler-diagnostics-rerun.log` 5/5; `git diff --check` | Passed bounded diagnostic contract; fresh Android cause capture remains pending | The Android production path previously collapsed invalid runtime, absent/ambiguous grid ownership, invalid grid masks, unresolved spawn cells, and rejected spawn transactions into the same silent zero-spawn result. The scheduler now publishes a closed diagnostic outcome with producer, pending, ready, authored/complete-grid, resolved, and spawned counts; it skips grid work when no canonical request is pending, preserves not-ready requests without warnings, and emits only a rate-limited warning when a ready request is actually stalled. Focused coverage retains the four accepted spawn/timing/authored-grid/ambiguous-grid cases and proves an ambiguous ready request reports exactly once per five-second interval. No package was rebuilt, so the repaired-APK rejection remains open until a fresh Android run identifies the exact outcome and a subsequent fix visibly increases the canonical unit count. The newly reported incorrect Tent selection-panel portrait is queued as a separate UI/presentation-mapping defect alongside oversized markers, vehicle selection, blue Autobahn/canal isolation, and the missing bridge; none is claimed fixed here. VRP-072 remains unstarted, production/protected assets and `productionCutover=0` remain unchanged, and checklist progress stays `56 / 80`. |
| 2026-08-02 | Independent correctness: candidate binding normalization and Tent transport rejection | Checked short-path profiler package `%TEMP%\warline-canonical-tent-scheduler-diagnostics-profiler-apk-shortpath.log` with `[DenseCityCandidateAndroidPackage] result=Passed`; exact 696,278,899-byte APK SHA-256 `cb24795d...e012d`; bundled-SDK ADB to paired Xiaomi `24090RA29G`; streamed install; exact-activity cold launch; main-menu Deploy; Build/Soldiers/Recruit; `Build/AndroidEvidence/TentSchedulerBinding` screenshots; Android `FrameRateDiag` | Candidate binding and canonical completion passed; required helicopter/drop interaction rejected | Candidate-only layout generation now fail-closes on exactly one recognized `CombinedMeshBaker` serialized identity and temporarily restores the legacy `Assembly-CSharp` identity before import so a cold package build can bind the retained component without touching the production scene. The checked package passed and the device run selected the canonical Contractor Tent, enabled Recruit, changed materials `120 -> 95`, and increased stable canonical units/source keys `5005 -> 5006`, models `22 -> 23`, and impostors `4 -> 5`. No helicopter arrived and no rope/drop delivery was visible; the canonical ECS scheduler completed the request directly, so the reopened parent interaction row remains unchecked. The same run independently reproduces the incorrect Tent portrait and a cyan marker much larger than the Tent footprint. Focused/base performance ranged roughly `19-46 FPS`, adding concrete VRP-072 evidence without starting or accepting that item. Production/protected assets and `productionCutover=0` remain unchanged; checklist progress stays `56 / 80`. |
| 2026-08-02 | Independent correctness: canonical Tent transport-presentation bridge | User-directed normal Unity GUI validation; `[CanonicalTentTransportPresentationValidation] result=Passed transportVisible=1 ropeVisible=1 dropVisualVisible=1 ownership=canonical`; visible `Build/EditorEvidence/CanonicalTentTransportDrop.png`; GUI scheduler regression `[OperationMapProductionRuntimeSchedulerValidation] result=Passed tests=6`; `git diff --check` | Canonical presentation component and scheduler deferral passed; full in-map Tent interaction remains pending | The canonical ECS scheduler can now hand a ready Tent request to the existing pooled helicopter/drop presenter without creating managed `RuntimeBuildingEntity` ownership. The delivery session keeps the request queued while the helicopter arrives and the rope/drop visual runs, returns `ReadyToSpawn` only after the drop completes, and lets the existing canonical spawn transaction consume the request. A new non-error `DeliveryInProgress` diagnostic prevents expected presentation time from being reported as `SpawnRejected`. Focused Editor evidence visibly distinguishes the helicopter body, rope, descending unit, and target ground; scheduler coverage proves no unit materializes during delivery and exactly one unit materializes after completion. This is a component/integration substep only: the normal map Tent button has not yet been visibly exercised through the complete Editor UI route, so the six-row Editor-first queue remains unchecked, Android remains deferred, VRP-072 remains unstarted, and checklist progress stays `56 / 80`. No candidate database, package, protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |
| 2026-08-02 | Independent correctness: full in-map canonical Tent delivery acceptance | Rejected checked GUI-wrapper attempts before project code on the generic licensing-channel timeout; accepted user-directed Hub-opened Unity GUI route `Tools/Validation/Canonical Tent Full In-Map Recruit`; global Editor log `[CanonicalTentProductionPlayModeValidation] result=Passed`; visible `Build/EditorEvidence/CanonicalTentFullInMapRecruitDrop.png`; `git diff --check` | Editor-first unit-production row accepted; spawned-unit interaction and the other queue rows remain pending | The focused validation opens the real Menu scene, invokes the actual main-menu Deploy control, waits for the authored map and canonical unit population to stabilize, then invokes the actual Build, Soldiers, and Recruit controls. The accepted run observed the pooled helicopter, an active drop visual, and a live rope extended at least `3 m` before capturing evidence; only after the presentation completed did canonical `UnitSourcePrefabKey` ownership increase exactly `460 -> 461`. This closes only the Editor-first helicopter/rope/drop/completion row. It does not prove the new unit is selectable, does not accept portrait mapping, marker scale, vehicle selection, or building placement, does not authorize another Android package yet, and does not advance the `56 / 80` VRP checklist. No candidate database, package, protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |
| 2026-08-02 | Independent correctness: spawned canonical soldier visibility and normal selection | User-directed Hub-opened Unity GUI route `Tools/Validation/Canonical Spawned Soldier Interaction`; global Editor log `[CanonicalSpawnedSoldierInteractionPlayModeValidation] result=Passed`; visible `Build/EditorEvidence/CanonicalSpawnedSoldierSelected.png`; `git diff --check` | Editor-first spawned-unit visibility/interaction row accepted; four queue rows remain pending | The validation repeats the actual Menu Deploy and in-map Build/Soldiers/Recruit path against a stable `460`-unit baseline, identifies the new canonical soldier by entity identity, and accepts only after canonical unit ownership reaches `461`, its ECS render tree is enabled and visible, and the entity is centered inside the world camera viewport. It then closes the drawer and invokes the actual `RIFLE SQUAD` card; the new entity must receive `SelectedUnitTag`, become the exact `FocusedUnitUiReadModelComponent.FocusedUnit`, and expose the visible selection panel before evidence capture. This closes only spawned-unit visibility and normal interaction. Portrait mapping, marker footprint, vehicle selection, and building placement remain independently open; Android remains deferred and the `56 / 80` VRP checklist does not advance. No runtime code/content, candidate database, package, protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |

| 2026-08-02 | Independent correctness: Contractor Tent selection-panel portrait acceptance | User-directed Hub-opened Unity GUI route `Tools/Validation/Canonical Contractor Tent Portrait`; global Editor log `[CanonicalContractorTentPortraitPlayModeValidation] result=Passed`; visible `Build/EditorEvidence/CanonicalContractorTentSelectionPortrait.png`; exact selected managed owner `id=9`, prefab `Tent_Contractor`, title `Contractor Tent (911,371)`, portrait `Portrait_Building_Tent_Contractor_Action_512`; `git diff --check` | Editor-first selection-panel image row accepted; three queue rows remain pending | The focused validation opens Menu, invokes the actual Deploy control, resolves the real managed runtime-building owner rather than assuming an absent `OperationMapBuildingComponent` projection, focuses its world instance, and submits the normal building-click path. The resulting visible panel is bound to Contractor Tent owner `9` and exactly matches the configured action portrait required by the selection-summary contract. The previously reported wrong Tent image is not reproduced in this fresh Editor route. The same evidence clearly retains the oversized cyan building marker, so marker scale remains independently open; vehicle selection and building placement also remain open, Android remains deferred, and checklist progress stays `56 / 80`. No runtime code/content, candidate database, package, protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |
| 2026-08-02 | Independent correctness: canonical building selection-marker footprint acceptance | Rejected checked batch wrapper `%TEMP%\warline-building-selection-marker-focused.log` after the known generic licensing channel timed out; exact wrapper-owned Editor PID `32412` was the only process closed after the 600-second timeout; accepted Hub-opened GUI focused route `Tools/Validation/Building Selection Marker Focused` `[BuildingSelectionMarkerFocusedValidation] result=Passed tests=8`; accepted full in-map route `Tools/Validation/Canonical Building Selection Marker` `[CanonicalBuildingSelectionMarkerPlayModeValidation] result=Passed`; visible `Build/EditorEvidence/CanonicalBuildingSelectionMarkerFootprint.png`; `git diff --check` | Editor-first marker-footprint row accepted; two queue rows remain pending | Map-authored selection previously centered and sized the cyan ground marker from the selected object's aggregate renderer bounds, allowing a broad visual hierarchy to inflate it beyond gameplay ownership. Marker center, yaw, and planar size now come only from the selected runtime building's canonical origin, footprint, grid origin, and cell size; renderer bounds remain limited to surface height, vertical frame height, and the mesh-bound object outline. The focused regression includes an intentionally `30 x 18` renderer hierarchy and still resolves its canonical `4 x 3` footprint. The real Menu Deploy and normal building-click route selects Contractor Tent owner `9` at origin `(911,371)` and proves the marker is exactly its `11 x 13` footprint, centered at `(916.5,377.5)`, with the configured prefab base reconciled to scale `(10.72,1,12.67)`. Vehicle selection and building placement remain independently open, Android stays deferred, VRP progress remains `56 / 80`, and no candidate database, package, protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |

| 2026-08-02 | Independent correctness: canonical vehicle world and roster selection acceptance | User-directed Hub-opened Unity GUI route `Tools/Validation/Canonical Vehicle World And Roster Selection`; global Editor log `[CanonicalVehicleSelectionPlayModeValidation] result=Passed`; visible `Build/EditorEvidence/CanonicalVehicleWorldSelection.png` and `Build/EditorEvidence/CanonicalVehicleRosterSelection.png`; exact target `Entity(674:3)` / `Unit_Veh_Tank_USA`; `git diff --check` | Editor-first vehicle world/roster row accepted; one queue row remains pending | The live validator opens Menu, invokes the actual Deploy control, waits for the canonical map vehicle population to stabilize at seven selectable player combat vehicles, and centers the camera on a named player battle tank. A normal runtime focus command at the tank's projected world position resolves exactly one `SelectedUnitTag`, the same focused-unit read-model entity, a visible Battle Tank panel, and a visible cyan vehicle outline. It then invokes the actual `ARMOR` card, which clears the single focus and resolves two canonical player ground-combat vehicles including the same tank, with the visible `2 VEHICLES` panel and both outlines. The reported Editor selection failure is not reproduced in this fresh canonical route, so no gameplay policy is changed. Building placement remains the sole open Editor-first row; Android stays deferred, checklist progress remains `56 / 80`, and no candidate database, package, protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |
| 2026-08-02 | Independent correctness: canonical building placement/spawn acceptance | User-directed Hub-opened Unity GUI route `Tools/Validation/Canonical Building Placement And Spawn`; global Editor log `[CanonicalBuildingPlacementSpawnPlayModeValidation] result=Passed`; visible `Build/EditorEvidence/CanonicalBuildingPlacementSpawn.png`; exact selected owner `Barracks (883,376)`, runtime id `434`, footprint `8 x 14`; `git diff --check` | Final Editor-first functional row accepted; Android final confirmation is dependency-ready | The validator uses the real Menu Deploy, Build drawer, Buildings tab, affordable Barracks item, Place control, and placement confirmation bar. Player placement now assigns canonical player ownership through the placement-only registration handoff without changing map-load or generic runtime-spawn ownership. Acceptance requires the exact catalog building among concurrent runtime additions, owner faction `1`, the authored `8 x 14` footprint, an active renderer, `RuntimeBuildingEntityLink`, a normal projected world click, exact active-building identity, and the visible Barracks selection panel. The evidence frame visibly shows the placed Barracks selected and its panel populated. All six Editor-first rows now pass; Android remains reserved for final functional confirmation and performance evidence. VRP progress stays `56 / 80`, and no candidate database, package, protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |
| 2026-08-03 | Independent correctness: fresh Editor-accepted Android profiler package | Hub-opened Unity GUI route `Tools/Validation/Build Dense City Candidate Android Profiler APK`; rejected first GUI build on Windows `MAX_PATH`; bounded workspace-local `GRADLE_USER_HOME=.g`; rejected cached `.utmp` CMake path and stale generated `compressed_assets`; exact generated-output cleanup after worker/process verification; `[DenseCityCandidateAndroidPackage] result=Passed`; exact APK file inspection; bundled-SDK ADB and mDNS probe; `git diff --check` | Fresh current-state package passed; Android final functional confirmation pending | The GUI-only build entry retains the checked candidate package contract while using a workspace-short Gradle cache and restoring the caller environment afterward. After removing only the verified generated caches that retained the long path or duplicate compressed-asset entries, the package gate passed and produced `Build/AndroidDenseCandidateProfiler/WarlineCapture-DenseCityCandidate-Profiler.apk`, exactly `696,278,899` bytes with SHA-256 `0ce4dfd1c3a773b8f2aa216505fccd06c4cd954e8160d0af4e2d91a7cfddcdc1`. The regenerated Addressables layout/runtime-content reports reconcile the fresh payload. No ADB device or mDNS service was visible, so install, main-menu Deploy, and six-behavior Android confirmation did not run and are not claimed. VRP-072 remains unstarted; blue Autobahn/canal isolation and the missing bridge remain separate; production/protected assets and `productionCutover=0` are unchanged; checklist progress stays `56 / 80`. |
| 2026-08-03 | Independent correctness: Samsung final-functional rejection | Exact accepted 696,278,899-byte APK SHA-256 `0ce4dfd1...ddcdc1`; bundled-SDK wireless ADB to Samsung `SM-S918B` / `R5CTC1J02VB`; streamed install; exact-activity 499 ms cold launch; first-run English/Skip; main-menu Deploy; `Build/AndroidDenseCandidateProfiler/Evidence/samsung-*.png`; Android `FrameRateDiag` and selection-hitch diagnostics | Rejected; Android six-behavior closure remains open and checklist progress stays `56 / 80` | The current package installed, cold-launched, and reached the dense tactical map through the normal visible controls. Owner labels/images appeared for Soldier Tent and Contractor Tent/build previews, but Armor roster selection and a visible world-vehicle hit produced no visible selected state. A central Barracks selection reproduced the reported massive cyan marker/selection region; the same region later captured hit tests intended for a newly placed Contractor Tent. Contractor Tent placement accepted a visible valid `13x11` preview, deducted materials exactly `120/60 -> 100/60`, displayed the expected construction image, and increased the stable runtime diagnostic baseline from `units/sourceKeys=5005` to `5006`. After more than its declared 30-second duration it still exposed no completed health state or Recruit control, and its visible model could not be reselected after deselection. Helicopter/rope/drop delivery and spawned-soldier interaction were consequently unreachable and are not accepted. Samsung performance ranged about 38-55 FPS with GPU samples around 12-17 ms; selection produced a 585.6 ms update hitch. Editor acceptance remains truthful and unchanged, but Android final confirmation is rejected. VRP-072 remains unstarted until the functional rejection is fixed or explicitly deferred; blue Autobahn/canal isolation and the missing bridge remain separate. Production/protected assets and `productionCutover=0` are unchanged. |

| 2026-08-03 | Independent correctness: Android-rejected building marker, portrait, and click-ownership Editor repair | Hub-opened Unity GUI `Tools/Validation/Canonical Building Selection Marker`; `[CanonicalBuildingSelectionMarkerPlayModeValidation] result=Passed`; visible `Build/EditorEvidence/CanonicalBarracksSelectionPresentation.png` and `Build/EditorEvidence/CanonicalContractorTentClickOwnership.png`; focused GUI regression `[BuildingSelectionMarkerFocusedValidation] result=Passed tests=8`; independent GUI portrait gate `[CanonicalContractorTentPortraitPlayModeValidation] result=Passed`; visible `Build/EditorEvidence/CanonicalContractorTentSelectionPortrait.png`; `git diff --check` | Editor repair accepted; affected Android rerun requires a fresh checked APK; checklist progress stays `56 / 80` | The earlier Editor marker acceptance encoded the wrong contract: it forced map-authored markers to gameplay-cell footprints and never proved that a smaller visible building wins a direct click over a nearby broad selection area. Runtime definitions now retain exact configured selection/card sprites; the panel prefers the selected definition's exact action sprite. Map-authored marker center and planar size now come from transformed presentation bounds (child renderers or canonical transformed local bounds), while visual hit ownership is resolved independently before gameplay-cell fallback, with deterministic smaller-visible-owner and canonical-cell tie breaking. The full Deploy route selects Barracks `18`, proves `Portrait_Building_Barrack_Action_512` and presentation-bounded marker geometry, then deliberately passes Barracks origin `(869,396)` with a screen click on Contractor Tent `9`; the Tent owns the marker and panel at `(911,371)` with `Portrait_Building_Tent_Contractor_Action_512`. The old Samsung APK is not rerun and is stale after this code change. Vehicle selection, Tent completion/Recruit, helicopter/drop, and spawned-unit interaction remain separate Android-rejected items; VRP-072 remains unstarted. No candidate database, protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |
| 2026-08-03 | Independent correctness: fresh post-repair Samsung marker/portrait/hit rejection | Checked Windows GUI-licensing package wrapper `%TEMP%\warline-selection-presentation-profiler-apk.log`; `[DenseCityCandidateAndroidPackage] result=Passed`; exact 696,278,899-byte APK SHA-256 `21c1201c40a43b4aa8cc60f2f242a2aff0998eea614eaef35834b48cc588ee44`; bundled-SDK wireless ADB to Samsung `SM-S918B` / `R5CTC1J02VB`; streamed install; device-side size/hash reconciliation; exact-activity 430 ms cold launch; correct main-menu Deploy; retained evidence under `Build/AndroidDenseCandidateProfiler/Evidence/2026-08-03-selection-fix` | Rejected; static-reuse presentation path remains outside prior Editor acceptance; checklist progress stays `56 / 80` | The fresh package proved the prior Editor repair did not reach the Android composition path. Barracks retained a massive cyan frame; a nearby visible tent tap retained Barracks ownership; and periodic HUD refresh replaced the exact action portrait with the generic building portrait. Android uses a map-authored managed owner without child renderers beside retained static ECS presentation, so the new fallback to aggregate `BuildingDefinition.LocalBounds` still enlarged marker and direct-hit geometry. The HUD's periodic building model also resolved the prefab again instead of retaining the selected runtime definition's exact sprite. Other functional rows were not rerun after this affected-gate rejection. Initial-load performance was about 20 FPS and stabilized near 54-55 FPS; this is retained evidence only, and VRP-072 remains unstarted. Blue Autobahn/canal isolation and road-over-water-without-bridge remain separate. No candidate database, protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |
| 2026-08-03 | Independent correctness: static-reuse building selection presentation Editor acceptance | Checked GUI-licensing wrappers `%TEMP%\warline-static-reuse-shared-renderer-marker.log` `[BuildingSelectionMarkerFocusedValidation] result=Passed tests=10` and `%TEMP%\warline-static-reuse-nearest-center-runtime-rerun.log` `[RuntimeBuildingSystemFocusedValidation] result=Passed tests=7`; Hub-opened D3D11 `Tools/Validation/Canonical Building Selection Marker`; `[CanonicalBuildingSelectionMarkerPlayModeValidation] result=Passed`; freshly inspected `Build/EditorEvidence/CanonicalBarracksSelectionPresentation.png` and `Build/EditorEvidence/CanonicalContractorTentClickOwnership.png`; `git diff --check` | Editor repair accepted; one fresh checked APK and affected Samsung gate rerun required; checklist progress stays `56 / 80` | Map-authored static-reuse owners now keep their baked presentation center and derive marker and direct-hit planar geometry only from their canonical footprint. Shared packed faction renderers and aggregate local bounds cannot claim a direct tap. When canonical screen rectangles overlap, the visible owner whose baked screen center is nearest the tap wins before area/depth/id ties. A regression shares one broad renderer across Barracks, Contractor Tent, and a smaller overlapping decoy; the exact Tent-center click still resolves the Tent. The real Deploy route visibly proves Barracks `18` with its exact `Portrait_Building_Barrack_Action_512`, then supplies Barracks cell `(869,396)` while clicking Contractor Tent `9`; the Tent owns the bounded marker and distinct portrait. Periodic HUD refresh preserves the selected definition's exact action sprite. Android has not been rerun for this revision; other rejected functional rows remain separate, and VRP-072 remains unstarted. Blue Autobahn/canal isolation and road-over-water-without-bridge remain separate. No candidate database, protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |
| 2026-08-03 | Independent correctness: static-reuse Samsung affected-gate rejection | Hub-opened D3D11 GUI package route `Tools/Validation/Build Dense City Candidate Android Profiler APK`; `[DenseCityCandidateAndroidPackage] result=Passed`; exact 696,278,899-byte APK SHA-256 `e6644876bd68ece1a40d50b7ff28de63a3723dd0884afa372ffb1fb1bda41ff5`; bundled-SDK wireless ADB to Samsung `SM-S918B` / `R5CTC1J02VB` at `192.168.2.33:44569`; streamed install; exact-activity 515 ms cold launch; correctly scaled main-menu Deploy; `Build/AndroidDenseCandidateProfiler/Samsung-fresh-barracks.png` and `Samsung-fresh-nearby.png` | Rejected; Android marker and click ownership remain open; checklist progress stays `56 / 80` | The fresh post-`2af246d14` package installed and reached the dense tactical map through the required Deploy route. Selecting the central Barracks still produced a cyan frame spanning a large road-and-building region rather than the selected structure. A subsequent direct tap on the nearby beige Tent area retained the Barracks panel and expanded frame, so the packaged static-reuse path still allows Barracks to capture the neighboring visual hit. The visible Barracks portrait remained populated and consistent, but that cannot close the independently rejected marker and hit-ownership contract. No vehicle, Contractor completion/Recruit, helicopter/drop, or spawned-unit interaction gate was rerun. VRP-072 remains unstarted; blue Autobahn/canal isolation and road-over-water-without-bridge remain separate. No candidate database, protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |
| 2026-08-03 | Independent correctness: exact placement-prefab static-reuse selection Editor acceptance | Rejected checked GUI-licensing wrapper attempts `%TEMP%\warline-placement-prefab-marker.log`, `%TEMP%\warline-placement-prefab-geometry.log`, and `%TEMP%\warline-static-reuse-selection-geometry.log` on licensing or shutdown fail-closed behavior despite later pass markers; user-directed Hub-opened DX11 GUI `Tools/Validation/Static Reuse Selection Geometry Focused`; global Editor log `[StaticReuseSelectionGeometryValidation] result=Passed placementTests=5 markerTests=10 runtimeTests=7`; real Deploy `[CanonicalBuildingSelectionMarkerPlayModeValidation] result=Passed`; freshly inspected `Build/EditorEvidence/CanonicalBarracksSelectionPresentation.png` and `Build/EditorEvidence/CanonicalContractorTentClickOwnership.png`; `git diff --check` | Editor repair accepted; one fresh checked APK and only the affected Samsung gate rerun required; checklist progress stays `56 / 80` | The previous Editor fixture and packaged implementation both used broad canonical gameplay footprints for static-reuse presentation. Map-authored owners now derive exact visual planar size and center from their referenced placement prefab renderer-local bounds, retain placement yaw, and do so without cloning the prefab. Marker rendering and direct screen-hit ownership use this exact oriented presentation geometry; canonical cells remain authoritative for gameplay occupancy and as a fail-closed geometry fallback. Focused coverage proves no-child static reuse retains exact geometry, the Barracks marker uses `10.72 x 12.67` at `90 degrees`, and a nearby Tent wins despite a deliberately huge Barracks gameplay footprint and shared renderer. The real Deploy evidence retains exact Barracks/Tent portraits and independent bounded markers. Android has not been rerun for this revision; vehicle selection, Tent completion/Recruit, helicopter/drop, and spawned-unit interaction remain separate. VRP-072 remains unstarted; blue Autobahn/canal isolation and road-over-water-without-bridge remain separate. The generated runtime-content report and recovery scene remain untouched, and no protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |
| 2026-08-03 | Independent correctness: exact placement-prefab Samsung affected-gate rejection | User-directed Hub-opened DX11 GUI `Tools/Validation/Build Dense City Candidate Android Profiler APK`; `Build Finished, Result: Success`; `[DenseCityCandidateAndroidPackage] result=Passed`; exact 758,603,643-byte APK SHA-256 `002eeb0f41f361ce6ba92ccc48172db41b51a2dfc1a4bed40c77857dd86e0cec`; bundled-SDK wireless ADB to Samsung `SM-S918B` / `R5CTC1J02VB` at `192.168.2.33:44569`; streamed install; device-side size/hash equality; exact-activity 408 ms bounded retry cold launch; correctly scaled main-page Deploy; `Build/AndroidDenseCandidateProfiler/Evidence/2026-08-03-exact-geometry-fix/barracks-test.png` and `nearby-tent-test.png`; Android frame/hitch diagnostics | Rejected; packaged marker and nearby visual-hit ownership remain open; checklist progress stays `56 / 80` | The fresh post-`4d28bca0e` package installed byte-identically and reached the live dense map through the main-page Deploy control. The first unscaled tap entered Skirmish Setup and a later Launch Mission remained in pregame with `initializedSpawnConfigs=0`; one ordinary cold-launch retry used the verified physical-to-render coordinate scale and the main-page Deploy control entered live gameplay directly. Selecting the central Barracks still drew a cyan frame across the adjacent road and multiple structures rather than tightly matching the visible Barracks. A direct tap on the beige Tent inside that region left the Barracks portrait, panel, and oversized marker unchanged, so exact placement-prefab bounds as currently calculated do not fix packaged static reuse. Selection samples exposed 256.5 ms and 322.8 ms update hitches; stable samples were about 53.5-55 FPS with GPU around 16.5-17.4 ms. No other Android functional row was rerun or accepted. VRP-072 remains unstarted; blue Autobahn/canal isolation and road-over-water-without-bridge remain separate. The generated runtime-content report and recovery scene remain unstaged, and no protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |
| 2026-08-03 | Independent correctness: owned-renderer fallback Editor acceptance and Samsung rejection | Hub-opened GUI aggregate `[StaticReuseSelectionGeometryValidation] result=Passed placementTests=5 markerTests=10 runtimeTests=7`; GUI-only `Tools/Validation/Static Reuse Owned Renderer Fallback Focused` `[StaticReuseOwnedRendererFallbackValidation] result=Passed tests=2`; implementation commit `c98b6f1ca`; Hub-opened GUI `Tools/Validation/Build Dense City Candidate Android Profiler APK`; `[DenseCityCandidateAndroidPackage] result=Passed`; exact 758,603,643-byte APK SHA-256 `7f6623da57ba4cc27060d9421204fd30af11f7da9d90bba1109c5b34ea0628c2`; wireless install to Samsung `SM-S918B` / `R5CTC1J02VB`; exact-activity 601 ms cold launch; correctly scaled main-page Deploy; `Build/AndroidDenseCandidateProfiler/Evidence/2026-08-03-owned-renderer-fallback/barracks-selected.png` and `nearby-tent-tap.png` | Editor fallback accepted but affected Android gate rejected; checklist progress stays `56 / 80` | The bounded repair aggregates only active renderers owned beneath the selected map-authored building, rejects implausibly broad/shared aggregates, and excludes selection-outline renderers; marker presentation and direct-hit ownership share that plausible fallback before gameplay-footprint fallback. The fresh package installed successfully and entered live gameplay through the main-page Deploy control after an initial unscaled tap merely opened Skirmish Setup and the visible Back control returned to the main page. Android still drew the Barracks cyan frame across the road and adjacent presentation, and a direct tap on the nearby beige Tent retained the Barracks portrait, panel, and oversized frame. The packaged runtime therefore either has no usable owned-renderer geometry or rejects it and reaches the broad fallback; the next bounded item is one-shot runtime geometry-source/rejection diagnostics before any further policy mutation or rebuild. No other functional row was rerun or accepted. VRP-072 remains unstarted; blue Autobahn/canal isolation and road-over-water-without-bridge remain separate. The generated runtime-content report and recovery scene remain unstaged, and no protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |
| 2026-08-03 | Independent correctness: packaged building-selection diagnostic rerun | Hub-opened GUI focused `[StaticReuseOwnedRendererFallbackValidation] result=Passed tests=4`; diagnostic commits `a6730af1b` and `bd1067f65`; Hub-opened GUI package route twice after the respective code changes; final `[DenseCityCandidateAndroidPackage] result=Passed`; exact final 758,603,643-byte APK SHA-256 `bc81c510e5485fc1a1698ef616829ff7f530a259c587bf8c5b72b2df7bd7a2e1`; streamed wireless install to Samsung `SM-S918B` / `R5CTC1J02VB`; exact-activity 507 ms cold launch; correctly scaled main-page Deploy; evidence under `Build/AndroidDenseCandidateProfiler/Evidence/2026-08-03-geometry-diagnostics` and `2026-08-03-unconditional-geometry-diagnostics` | Diagnostic code and package passed; affected Android marker gate remains rejected and checklist progress stays `56 / 80` | The first diagnostic revision records the marker and direct-hit geometry source, exact-presentation flags, owned-renderer acceptance/rejection metrics, footprint, and chosen center/size/yaw; the second removes the `Debug.isDebugBuild` guard after the first Development APK emitted no records. Both focused Editor runs emitted the expected one-shot marker record. On Samsung, the first fresh package still showed the oversized Barracks frame; placing a nearby Contractor Tent spent materials `120/60 -> 100/60`, selected the Tent with the correct portrait, and still exposed no health or Recruit control after more than 30 seconds, preserving that separate completion rejection. The final exact package again selected Barracks with the frame extending off-screen and across the road, while neither unconditional marker nor winning-hit record appeared in complete logcat even though the diagnostic string is present in generated IL2CPP metadata and ordinary Unity performance logs are present. This rejects the assumption that another geometry fallback tweak inside the currently instrumented helper is evidence-driven. The next bounded item is direct one-shot instrumentation at the rendered `PremiumWorldSelectionFrameView.Configure` boundary plus the outer packaged hit-arbitration owner, followed by one measured presentation/hit fix; no further speculative bounds policy is accepted. No other Android functional row is closed. VRP-072 remains unstarted; blue Autobahn/canal isolation and road-over-water-without-bridge remain separate. The unrelated runtime-content report and recovery scene remain unstaged, and no protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |

| 2026-08-03 | Independent correctness: shared packed-renderer outline suppression | Direct packaged one-shot diagnostics at the outer hit owner and rendered-frame boundary; implementation commits `9d2dfeca5` and `07b1e498a1`; user-directed Hub-opened DX11 GUI `Tools/Validation/Canonical Building Selection Marker`; `[CanonicalBuildingSelectionMarkerPlayModeValidation] result=Passed barracksMarker=placementPrefabGeometry presentationSize=(7.43, 5.14, 13.55) presentationYaw=90.00 barracksPortrait=Portrait_Building_Barrack_Action_512 contractorTentClickOwner=9`; freshly inspected `Build/EditorEvidence/CanonicalBarracksSelectionPresentation.png` and `Build/EditorEvidence/CanonicalContractorTentClickOwnership.png`; `git diff --check` | Measured Editor fix accepted; fresh checked APK and affected Samsung rerun required; checklist progress stays `56 / 80` | The direct Samsung evidence separated two systems that the earlier screenshots conflated: the outer normal hit route reported `handled=0`, while the massive cyan presentation was an object-outline overlay copied from a shared packed static-reuse renderer, not the already bounded selection-frame geometry. Map-authored static-reuse owners now suppress that object-outline overlay and retain the bounded placement-prefab frame; managed/runtime-spawned buildings keep the existing object-outline path. The real Deploy validation visibly selects Barracks `18` with its exact action portrait and bounded footprint frame, then resolves the nearby Contractor Tent as independent click owner `9` with its distinct portrait and bounded frame. The gate now also fails if any active `SelectionObjectOutline` renderer exists beneath the selected map-authored owner. A post-pass Addressables invalid-handle exception occurred during Editor teardown after the required pass marker and evidence writes; it is recorded rather than treated as an additional pass. No fresh APK was built and Android is not yet accepted. Vehicle selection, Tent completion/Recruit, helicopter/drop, and spawned-unit interaction remain separate; VRP-072 remains unstarted. Blue Autobahn/canal isolation and road-over-water-without-bridge remain separate. The unrelated runtime-content report and recovery scene remain unstaged, and no protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |

| 2026-08-03 | Independent correctness: shared-outline fresh Samsung rejection | Checked Windows GUI-licensing package wrapper `%TEMP%\warline-shared-outline-profiler-apk.log`; `Build Finished, Result: Success`; `[DenseCityCandidateAndroidPackage] result=Passed`; exact 696,278,899-byte APK SHA-256 `64ca360f09f2704ed2f9a561b561cf6e9a13139a4bb976dc2ac54a3b982fe49a`; device-side size/hash equality; wireless Samsung `SM-S918B` / `R5CTC1J02VB`; exact-activity 542 ms cold launch; visible main-page Deploy; evidence under `Build/AndroidDenseCandidateProfiler/Evidence/2026-08-03-shared-outline-fix`; Android selection and frame-rate diagnostics | Package passed but affected Android marker gate rejected; checklist progress stays `56 / 80` | The exact fresh post-`5871ad36c` package installed byte-identically and reached the live tactical map through the main-page Deploy control after returning from one harmless input-scale miss into Skirmish Setup. Barracks selection retained its intended panel image, but the cyan presentation still outlined the upper shared building and continued as a long segment across the road, visibly reproducing the oversized defect. The attempted suppression therefore did not remove the packaged overlay that owns the massive presentation. The outer route again reported `handled=0`; the successful Barracks click projected to cell `(874,397)` and incurred a 290.9 ms update hitch. Stable samples were roughly 42-48 FPS with GPU near 22 ms, retained only as performance evidence. Nearby Tent ownership was not accepted because the marker sub-gate already failed. Vehicle selection, Tent completion/Recruit, helicopter/drop, and spawned-unit interaction remain separate. VRP-072 remains unstarted; blue Autobahn/canal isolation and road-over-water-without-bridge remain separate. The unrelated runtime-content report and recovery scene remain unstaged, and no protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |

| 2026-08-03 | Independent correctness: generic ECS building-outline suppression and fresh Samsung rejection | Implementation commit `48baeb813`; checked Windows GUI-licensing wrappers `%TEMP%\warline-operation-map-building-outline-playmode-accepted.log` `[OperationMapBuildingSelectionOutlineValidation] result=Passed tests=6 owner=OperationMapBuildingComponent footprintMarker=preserved sharedRendererOutline=suppressed repeatFrames=stable` and `%TEMP%\warline-canonical-building-selection-marker-post-ecs-outline.log` `[CanonicalBuildingSelectionMarkerPlayModeValidation] result=Passed`; visible Editor evidence `Build/EditorEvidence/CanonicalBarracksSelectionPresentation.png` and `CanonicalContractorTentClickOwnership.png`; rejected canonical-path Android build on Windows MAX_PATH; accepted short-path checked wrapper `%TEMP%\warline-operation-map-building-outline-profiler-apk-shortpath.log`; `Build Finished, Result: Success`; `[DenseCityCandidateAndroidPackage] result=Passed`; exact 696,278,899-byte APK SHA-256 `3e3a7d6cfcdb1a2e04ae0abc5074502391c82dacfb00481af55849b6db615f58`; device-side size/hash equality; wireless Samsung `SM-S918B` / `R5CTC1J02VB` at `192.168.2.33:44569`; exact-activity 417 ms cold launch; visible main-page Deploy; `Build/AndroidDenseCandidateProfiler/Evidence/2026-08-03-generic-ecs-outline-fix/barracks-selected.png`; Android selection/frame diagnostics | Editor contract and package passed, but affected Android marker gate rejected; checklist progress stays `56 / 80` | The generic ECS presentation system now destroys copied object-outline instances for selected `OperationMapBuildingComponent` entities while preserving their dedicated footprint marker. Synthetic live-group coverage proves a stale outline entity is removed and stays absent across repeated frames; the real Editor Deploy route still shows distinct bounded Barracks/Tent markers and portraits. The exact fresh package installed byte-identically and reached live gameplay through the required main-page Deploy control, but selecting Barracks still showed the intended portrait alongside a cyan outline on the upper shared presentation that continued as a long road segment. The packaged overlay therefore is not owned by either the earlier helper path or the newly suppressed generic selected-building ECS path. The outer route again reported `handled=0` at projected cell `(874,397)`; the selection frame incurred a 220.4 ms update hitch, followed by a 51.0 FPS sample with 14.3 ms GPU. Nearby Tent ownership was not rerun because the marker sub-gate already failed. The next bounded step must trace the exact live cyan renderer/outline instance and owner at the packaged presentation boundary before another mutation; no speculative geometry or outline policy change is accepted. Vehicle selection, Tent completion/Recruit, helicopter/drop, and spawned-unit interaction remain separate. VRP-072 remains unstarted; blue Autobahn/canal isolation and road-over-water-without-bridge remain separate. The two generated runtime-content/layout reports and recovery scene remain unstaged, and no candidate database, protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |

| 2026-08-04 | Independent correctness: cold candidate binding migration and Samsung marker/hit acceptance | Implementation commits `58d242c13` and `ba29ba817`; checked GUI-licensing focused `%TEMP%\warline-candidate-binding-migration-final-focused.log` `[OperationMapEntitySceneCandidateBakeAllValidation] result=Passed tests=24`; checked GUI-licensing package `%TEMP%\warline-marker-hitbox-short-profiler-apk-final.log`; `[DenseCityCandidateAndroidPackage] result=Passed`; exact 758,604,112-byte APK SHA-256 `d5c676c4bc6b73de84de010b7450104c776c62051aaf9afe31e02b5705cd2ab5`; streamed wireless install to Samsung `SM-S918B` / `R5CTC1J02VB`; exact-activity 544 ms cold launch; correctly scaled main-page Deploy; evidence under `Build/AndroidDenseCandidateProfiler/Evidence/2026-08-04-marker-hitbox-migration`; Android marker/hit and frame diagnostics | Accepted for the bounded Barracks marker and nearby Tent/Contractor click-ownership gate; checklist progress stays `56 / 80` | Cold candidate binding now durably migrates the retained `CombinedMeshBaker` and `MapSurfaceAuthoring` identities before package validation, allowing the fresh package to exercise the intended marker implementation. On Samsung, central Barracks selection no longer produced the road-spanning shared cyan overlay; its retained ECS marker reported `source=baked-building-hitbox`, `footprint=(20,10)`, and bounded scale `(6.292,12.500)`. A direct nearby beige-tent tap changed the visible owner from Barracks to `Soldier Tent` with a separate bounded marker. For the stricter Contractor case, a valid `13x11` Contractor Tent placement at `(874,374)` spent materials `120/60 -> 100/60`; after selecting Barracks, direct Tent hits emitted `kind=hit-selected id=7 instance=Contractor Tent_7`, `handled=1`, and moved the cyan marker to the Tent's own `13x11` gameplay footprint. The Contractor action image and footprint marker changed, while the panel title remained stale as `Barracks`; that label-refresh defect and Contractor completion/Recruit remain separate and are not accepted here. Vehicle selection, helicopter/drop, and spawned-unit interaction were not rerun. Selection hitches of about `195-358 ms` and roughly `50-51.5 FPS` samples remain performance evidence only; VRP-072 remains unstarted. Blue Autobahn/canal isolation and road-over-water-without-bridge remain separate. Generated reports and `_Recovery` scenes remain unstaged; no candidate database, protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |

| 2026-08-04 | Independent correctness: fresh Contractor panel refresh Editor repair | Checked GUI-licensing focused `%TEMP%\warline-contractor-panel-rts-selection-after-preflight.log` `[RtsSelectionInputSystemValidation] result=Passed tests=62`; checked GUI-licensing real-HUD transition `%TEMP%\warline-fresh-contractor-panel-self-contained-visible-title.log` `[FreshContractorSelectionPanelEditorValidation] result=Passed`; freshly inspected `Build/EditorEvidence/FreshContractorSelectionPanelTransition.png`; `git diff --check` | Editor repair accepted; one fresh checked APK and only the affected Samsung panel gate are required; checklist progress stays `56 / 80` | The stale Barracks label was caused by the release half of an already handled building click clearing `SuppressNextWorldClick` and then falling through to ECS unit focus, allowing the overlapping Barracks entity to replace the just-selected Contractor model. The release is now consumed unconditionally and clears pending pointer/move state without invoking unit focus. Deterministic coverage proves the suppressed release cannot fall through. A separate real `MatchHudSelectionPanelView` gate transitions Barracks to a fresh Contractor label and atomically proves `Contractor Tent (113,100)`, the exact Contractor action portrait, `Base Structure`, `Structure selected`, and health `-`; the rendered evidence visibly shows the same state. The full canonical match route was attempted but rejected before gameplay by the existing Addressables catalog typing defect (`StaticMapPresentationManifest` GUID resolves as `System.Object`), so it is not claimed as a pass. No APK was rebuilt and Samsung was not rerun. The already accepted marker/hit gate is not reopened; Contractor completion/Recruit, vehicle selection, helicopter/drop, and spawned-unit interaction remain separate. VRP-072 remains unstarted; blue Autobahn/canal isolation and the missing bridge remain separate. The generated runtime-content report and all `_Recovery` scenes remain unstaged; no protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |

| 2026-08-04 | Independent correctness: first Contractor panel APK rejection and explicit-building panel priority repair | Rejected checked 758,604,112-byte profiler APK SHA-256 `7508272b78a2ef411e0aae370dc7b9e04ad1a19568b34d882b9d3d7ee9ad1b27`; wireless Samsung `SM-S918B` / `R5CTC1J02VB`; streamed install; exact-activity 568 ms cold launch; main-page Deploy; evidence under `Build/AndroidDenseCandidateProfiler/Evidence/2026-08-04-contractor-panel-refresh`; checked GUI-licensing `%TEMP%\warline-selection-summary-building-priority-ipc.log` `[SelectionSummaryFocusedValidation] result=Passed tests=21`; checked visible GUI gate `%TEMP%\warline-fresh-contractor-panel-stale-focus.log` `[FreshContractorSelectionPanelEditorValidation] result=Passed`; freshly inspected `Build/EditorEvidence/FreshContractorSelectionPanelTransition.png`; `git diff --check` | First APK rejected; stronger Editor repair accepted and one new checked APK plus only the affected Samsung panel gate are required; checklist progress stays `56 / 80` | The first post-input-fix package proved that the Contractor hit and marker ownership were correct but the periodic HUD refresh still chose a stale focused Barracks entity first: after Barracks, a direct Contractor hit logged `kind=hit-selected id=7 instance=Contractor Tent_7` and `handled=1`, yet the panel combined the Contractor action portrait with the stale `Barracks` title and `1200/1200` health. Explicit managed building selection now owns the panel before selected/focused ECS summaries, so title, portrait, subtitle, order, health, storage, and actions change atomically even while stale ECS selection cleanup lags. The focused regression keeps a selected/focused Barracks entity alive while selecting `Contractor Tent (113,100)` and proves the complete Contractor model wins. The real-HUD visible gate now reproduces that same mixed state and renders the correct full Contractor panel. GUI licensing initially failed inside the sandboxed local IPC path; after the verified sole failed Editors were removed and Hub/licensing was cleanly restarted signed in, the same checked wrappers passed with permitted IPC access. The rejected APK is not reused. The accepted marker/hit gate remains closed; Contractor completion/Recruit, vehicle selection, helicopter/drop, and spawned-unit interaction remain separate. VRP-072 remains unstarted; blue Autobahn/canal isolation and road-over-water-without-bridge remain separate. The generated runtime-content report and all `_Recovery` scenes remain unstaged; no protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |

| 2026-08-04 | Independent correctness: second Contractor panel APK rejection and cache round-trip repair | Checked GUI-licensing package `%TEMP%\warline-contractor-panel-priority-profiler-apk-retry.log` `[DenseCityCandidateAndroidPackage] result=Passed`; exact 758,604,112-byte APK SHA-256 `a0be94e7ccedefa8352d6d7aea982897a0462bb2512ef8af61213c3c778f5ade`; streamed wireless install to Samsung `SM-S918B` / `R5CTC1J02VB` at `192.168.2.33:44569`; exact-activity 537 ms cold launch; main-page Deploy; direct Contractor `kind=hit-selected id=7` / `handled=1`; rejected final frame `Build/AndroidDenseCandidateProfiler/Evidence/2026-08-04-contractor-panel-priority/10-contractor-reselected-after-barracks.png`; corrected checked GUI-licensing `%TEMP%\warline-selection-summary-cache-transition-rerun.log` `[SelectionSummaryFocusedValidation] result=Passed tests=21`; checked visible GUI gate `%TEMP%\warline-fresh-contractor-panel-cache-transition.log` `[FreshContractorSelectionPanelEditorValidation] result=Passed`; freshly inspected `Build/EditorEvidence/FreshContractorSelectionPanelTransition.png`; `git diff --check` | Second APK rejected; exact Editor cache round-trip repair accepted and one new checked APK plus only the affected Samsung panel gate are required; checklist progress stays `56 / 80` | The second package included explicit-building priority and reached the correct Contractor managed owner, but after Contractor placement had cached that label, an intervening focused Barracks panel replaced the visible model without invalidating the selected-building cache. Reselecting the same Contractor therefore treated its unchanged label as cached, skipped the full model apply, and only the immediate portrait callback changed: after five seconds the Samsung frame still showed `Barracks`, `1200/1200`, and `Idle` with the Contractor portrait. Non-building focused/summary models now explicitly invalidate the selected-building cache. The corrected focused regression performs the exact `Contractor -> focused Barracks -> same Contractor` round trip and proves the final title, portrait, subtitle, order, and health refresh atomically. The real HUD validator performs the same three-state transition and visibly renders `Contractor Tent (113,100)`, the configured Contractor portrait, `Base Structure`, `Structure selected`, and health `-`. An initial wrapper run using a nonexistent namespaced test entry point was rejected for a missing pass marker; the corrected global entry point is the recorded pass. The rejected APK is not reused or accepted. The previously accepted marker/hit gate remains closed; Contractor completion/Recruit, vehicle selection, helicopter/drop, and spawned-unit interaction remain separate. VRP-072 remains unstarted; blue Autobahn/canal isolation and road-over-water-without-bridge remain separate. The generated runtime-content report and all `_Recovery` scenes remain unstaged; no protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |

| 2026-08-04 | Independent correctness: Contractor panel cache round-trip Samsung acceptance | Implementation commit `22c2772a7`; checked GUI-licensing focused `%TEMP%\warline-selection-summary-cache-transition-rerun.log` `[SelectionSummaryFocusedValidation] result=Passed tests=21`; checked visible GUI `%TEMP%\warline-fresh-contractor-panel-cache-transition.log` `[FreshContractorSelectionPanelEditorValidation] result=Passed`; checked GUI-licensing package `%TEMP%\warline-contractor-panel-cache-roundtrip-profiler-apk.log` `[DenseCityCandidateAndroidPackage] result=Passed`; exact 758,604,112-byte APK SHA-256 `db9240ba8577b27967729186ac114340ec2d0f1ca7d44152f0c5987b33dbabbc`; streamed wireless install to Samsung `SM-S918B` / `R5CTC1J02VB`; exact-activity 551 ms cold launch; main-page Deploy; evidence under `Build/AndroidDenseCandidateProfiler/Evidence/2026-08-04-contractor-panel-cache-roundtrip`; Android direct-hit and frame diagnostics | Accepted for the bounded freshly spawned Contractor selection-panel round trip; checklist progress stays `56 / 80` | The exact post-fix package repeated the prior failure sequence rather than retesting the already closed marker policy. A valid `13x11` Contractor placement spent materials `120/60 -> 100/60` and seeded the Contractor panel; an intervening direct world selection showed `Barracks`, `1200/1200`, and `Idle`; the same Contractor was then directly hit with `kind=hit-selected id=7 instance=Contractor Tent_7` and `handled=1`. After seven seconds and multiple periodic HUD updates, the final frame visibly showed `Contractor Tent`, the configured Contractor action portrait, `Base Structure`, health `-`, and `Structure selected`, proving the full model returned atomically instead of retaining Barracks data. The affected panel gate is closed; the rejected prior APKs are not reused. The selection frame incurred a 239.9 ms update hitch and stable samples were roughly 51-52 FPS, retained only as performance evidence. Contractor completion/Recruit is the next independent functional gate; vehicle selection, helicopter/drop, and spawned-unit interaction remain separate. VRP-072 remains unstarted; blue Autobahn/canal isolation and road-over-water-without-bridge remain separate. The generated runtime-content report retained SHA-256 `859426892e8fbb26678245d5d8f48ecbf71ac0137e7be6658b441b4c46885cda`, all `_Recovery` scenes remain unstaged, and no protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |

| 2026-08-04 | Independent correctness: selected-building health panel Editor repair | Host-access checked Windows GUI-licensing `Build/Logs/contractor-health-selection-summary-host.log` `[SelectionSummaryFocusedValidation] result=Passed tests=21`; checked visible GUI `Build/Logs/fresh-contractor-health-visible-host.log` `[FreshContractorSelectionPanelEditorValidation] result=Passed title=Contractor Tent origin=(113,100) portrait=Portrait_Building_Tent_Contractor_Action_512 subtitle=Base Structure order=Structure selected health=350/350`; freshly inspected `Build/EditorEvidence/FreshContractorSelectionPanelTransition.png`; `git diff --check` | Editor repair accepted; one fresh checked APK and only the affected Samsung health/Recruit gate are required; checklist progress stays `56 / 80` | The accepted Contractor title/portrait round trip exposed a narrower HUD defect: explicit managed building selection hardcoded health `-` and fill `0` even though the canonical runtime building owns valid `UnitHealth`. The selection panel now reads the existing selected-building health query, clamps and renders current/max health, and includes health in the selected-building cache key so damage refreshes without changing selection. Focused coverage proves `350/350` refreshes to `225/350`; the real HUD evidence visibly shows the correct Contractor title, action portrait, full health bar, `350/350`, and `Structure selected`. Sandboxed GUI licensing repeatedly failed before compilation; after the verified sole Hub/licensing recovery, the unchanged checked wrappers passed with host IPC access. No Android acceptance is claimed, no APK was rebuilt, and the accepted marker/hit and panel-title gates remain closed. Contractor Recruit/completion remains the affected device gate; vehicle selection, helicopter/drop, and spawned-unit interaction remain separate. VRP-072 remains unstarted; blue Autobahn/canal isolation and road-over-water-without-bridge remain separate. The unrelated runtime-content report and all `_Recovery` scenes remain unstaged; no protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |

| 2026-08-04 | Independent correctness: consolidated Editor-first production and selection batch | Host-access checked Windows GUI-licensing `Build/Logs/editor-first-functional-batch-host-sync.log`; wrapper exit code `0`; `[OperationMapCampProductionBridgeValidation] result=Passed tests=3`; `[OperationMapProducerQueueConsumerValidation] result=Passed tests=3`; `[OperationMapProductionRuntimeSchedulerValidation] result=Passed tests=6`; `[OperationMapProducerSpawnTransactionValidation] result=Passed tests=3`; `[CanonicalTentTransportPresentationValidation] result=Passed transportVisible=1 ropeVisible=1 dropVisualVisible=1 ownership=canonical`; `[BuildingProductionCameraFocusValidation] result=Passed tests=10`; `[RtsSelectionInputSystemValidation] result=Passed tests=62`; `[MatchHudSquadTraySelectionFocusedValidation] result=Passed tests=4`; aggregate `[EditorFirstProductionFunctionalBatchValidation] result=Passed suites=8 tests=92`; freshly inspected `Build/EditorEvidence/CanonicalTentTransportDrop.png`; `git diff --check` | Remaining Editor-first functional rows accepted as one fail-closed batch; Android consolidated confirmation is next; checklist progress stays `56 / 80` | Added one Editor-only menu/execute-method runner that composes the existing focused suites under suppressed nested process exits and rejects any nonzero suite result. The accepted batch proves canonical Contractor preflight/spend/queue and strict completion, live-grid spawn scheduling and configured unit instantiation, helicopter arrival/rope/drop ownership, produced-unit focus, world selection, and roster selection. The evidence visibly shows the canonical helicopter, rope, and drop unit. This validation does not itself advance a VRP checklist item: VRP-072 remains unstarted until the consolidated Android functional pass. No APK was built, and the accepted marker/hit and Contractor panel gates were not reopened. Blue Autobahn/canal isolation and road-over-water-without-bridge remain separate. Generated reports, performance settings artifacts, and all `_Recovery` scenes remain unstaged; no protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |

| 2026-08-04 | Independent correctness: consolidated Samsung functional rejection | Implementation commit `7ae86f350`; host-access checked Windows GUI-licensing package `Build/Logs/editor-first-consolidated-profiler-apk-retry.log` `[DenseCityCandidateAndroidPackage] result=Passed`; exact 696,278,899-byte APK SHA-256 `4059228900347253b3208feb355b4a1eff2ae29a6715bfc8a6482e07b9d2047e`; wireless Samsung `SM-S918B` / `R5CTC1J02VB` at `192.168.2.33:44569`; exact-activity 493 ms cold launch; correctly scaled main-page Deploy; screenshots and timed sequence under `Build/AndroidDenseCandidateProfiler/Evidence/2026-08-04-editor-first-functional-batch`; Android scheduler and frame diagnostics | Contractor completion/health and Recruit command accepted; vehicle selection and production completion rejected; checklist progress stays `56 / 80` | The fresh package reached the rendered operation map through the required Deploy button. A valid `13x11` Contractor placement spent materials `120/60 -> 100/60`; after 38 seconds the independently selected Contractor panel showed the correct portrait and `350/350`. Its Soldiers tab exposed Recruit, accepted `Advanced Rifleman Male II`, and spent materials `100 -> 75`. The queue then failed to execute: repeated `[OperationMapProductionScheduler] result=Stalled outcome=SpawnRejected producers=20 pending=1 ready=1 authoredGrids=1 completeGrids=1 resolved=1 spawned=0` diagnostics accompanied stable `units=5006`, `sourceKeys=5006`, and `models=22` across more than 56 seconds. No helicopter, rope, drop, or spawned soldier appeared, so spawned-unit interaction was unreachable. Independently, both the Armor roster card and a visible world-vehicle tap produced no visible selected state or panel change. The already accepted marker/hit and Contractor title/portrait cache-roundtrip gates were not retested. Observed functional-run samples ranged roughly `34-54 FPS`, with the stalled production interval roughly `43-50 FPS`; these remain VRP-072 evidence only and do not authorize a policy mutation. VRP-072 remains unstarted. Blue Autobahn/canal isolation and road-over-water-without-bridge remain separate. The generated reports and all `_Recovery` scenes remain unstaged; no protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |

| 2026-08-04 | Independent correctness: canonical production delivery-key normalization | Samsung rejection evidence above; checked Windows GUI-licensing `Build/Logs/operation-map-production-normalized-delivery.log` `[OperationMapProductionRuntimeSchedulerValidation] result=Passed tests=6`; checked aggregate `Build/Logs/editor-first-functional-batch-normalized-delivery.log` `[EditorFirstProductionFunctionalBatchValidation] result=Passed suites=8 tests=92`; `git diff --check` | Exact `SpawnRejected` cause repaired and Editor-accepted; Android batch confirmation remains pending; checklist progress stays `56 / 80` | The operation-map request retained the configured unit prefab's mixed-case name, while the real runtime unit lookup is canonically keyed by trimmed lowercase text. Delivery resolution performed a raw dictionary lookup, rejected the otherwise valid request before starting the helicopter session, and produced the Samsung `resolved=1 spawned=0` stall. Delivery now normalizes the stored source key through the same canonical key contract before lookup. The shared Editor test context now builds the production-realistic normalized dictionary, so the existing scheduler delivery regression would reproduce the packaged failure without the fix. The focused scheduler suite and complete 92-test production/selection batch pass. No APK was built under the Editor-first batching strategy; vehicle selection remains the next separate Editor defect, and helicopter/drop/spawn Android acceptance remains pending until the consolidated package. VRP-072 remains unstarted. The generated reports and all `_Recovery` scenes remain unstaged; no protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |
| 2026-08-04 | Independent correctness: consolidated profiler selection diagnostics | Checked Windows GUI-licensing `Build/Logs/editor-first-functional-batch-profiler-diagnostics.log`; `[EditorFirstProductionFunctionalBatchValidation] result=Passed suites=8 tests=92`; `git diff --check` | Development-player diagnostic prerequisite accepted; consolidated APK/device confirmation remains pending; checklist progress stays `56 / 80` | Selection-click diagnostics now activate only in non-Editor development players, which includes the candidate profiler APK, while release and performance builds retain the prior disabled behavior. This makes the next single Samsung batch self-identifying for roster-card delivery, UI release suppression, screen-to-grid lookup, and focused-entity resolution instead of requiring another blind rebuild. The full Editor-first batch still passes 92/92. No APK was built by this bounded prerequisite. VRP-072 remains unstarted, and no render policy, protected/production asset, Jenkins/Unity path, generated report, or recovery scene changed. |
| 2026-08-04 | Independent correctness: normalized-production consolidated Samsung partial acceptance | Implementation commits `bfce6be37` and `a58918105`; checked Windows GUI-licensing `Build/Logs/editor-first-functional-batch-profiler-diagnostics.log` `[EditorFirstProductionFunctionalBatchValidation] result=Passed suites=8 tests=92`; checked package `Build/Logs/consolidated-production-selection-profiler-apk-short-gradle-regenerated.log` `[DenseCityCandidateAndroidPackage] result=Passed`; exact 696,278,899-byte APK SHA-256 `62576f58c1d1207a5977d8b565bb324680d4813c40336564dfcb8f3177c69ee8`; wireless Samsung `SM-S918B` / `R5CTC1J02VB` at `192.168.2.33:44569`; exact-activity 363 ms first cold launch; correctly scaled main-page Deploy; evidence under `Build/AndroidDenseCandidateProfiler/Evidence/2026-08-04-consolidated-functional`; Android selection and frame diagnostics | Helicopter delivery/selection and Contractor identity/health accepted; vehicle selection, Contractor Recruit, and visible rope/drop confirmation rejected; checklist progress stays `56 / 80` | The exact fresh package reached the live map through the required Deploy control. A valid `13x11` Contractor placement spent materials `120/60 -> 100/60` and, after 35 seconds, visibly showed the correct Contractor title, action portrait, bounded marker, and `350/350`; however, its completed panel still exposed only Return, Destroy, and Camera, with no Recruit control. In an independent fresh gameplay session, Light Attack Helicopter production spent `104` materials (`120 -> 16`) and emitted the success toast while the production pane incorrectly remained `NO PRODUCTION QUEUED`. After more than 50 seconds, the Gunship roster selected a live `Light Attack Helico` at `520/520`, and `[SelectionClick] result=SquadTraySelect slot=AttackHelicopter selected=1` proves normalized delivery, spawned existence, and normal roster interaction. The transient rope/drop sequence was hidden by the stale queue pane and was not visibly accepted. Independently, Armor emitted `result=SquadTraySelectSkipped slot=CombatVehicles reason=NoCandidates`; a visible missile-launcher world hit resolved successfully but then emitted `result=RejectFocus ... reason=NonPlayer`, so neither vehicle route is accepted. Stable samples were roughly `49-54 FPS`; this is retained for VRP-072 evidence only and authorizes no render-policy mutation. VRP-072 remains unstarted; blue Autobahn/canal isolation and road-over-water-without-bridge remain separate. The unrelated generated report and all `_Recovery` scenes remain unstaged; no protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |
| 2026-08-04 | Independent correctness: authored vehicle ownership adoption | Checked Windows GUI-licensing `Build/Logs/map-vehicle-authored-ownership-adoption-final.log`; `[MapVehiclePlacementStartupCompletionValidation] result=Passed tests=3`; `git diff --check` | Authored neutral vehicle ownership is repaired and Editor-accepted; consolidated Android confirmation remains pending; checklist progress stays `56 / 80` | Startup now adopts an exact-position neutral authored vehicle into its canonical configured placement instead of leaving a neutral live duplicate and instantiating a second player copy. The bounded adoption accepts only non-prefab, non-disabled, non-static, non-passenger vehicle-motion entities with no respawn owner, reserves each adopted entity once, and applies faction, grid, transform, source key, respawn, cooldown, and reset state atomically. Focused coverage proves the real entity becomes player-owned and rejects already-owned or distant candidates. No APK was built or installed; this remains part of the user-directed Editor-first batch. VRP-072 remains unstarted, and unrelated generated reports and all `_Recovery` scenes remain unstaged. |
| 2026-08-04 | Independent correctness: building placement live-occupancy exclusion | Checked Windows GUI-licensing `Build/Logs/building-placement-live-occupancy-editor-rerun.log`; `[BuildingPlacementLiveOccupancyValidation] result=Passed tests=2`; checked aggregate `Build/Logs/editor-first-functional-batch-vehicle-occupancy.log`; `[EditorFirstProductionFunctionalBatchValidation] result=Passed suites=8 tests=92`; `git diff --check` | Building/building, building/unit, and building/vehicle overlap rejection is Editor-accepted; consolidated Android confirmation remains pending; checklist progress stays `56 / 80` | Placement already rejected canonical runtime-building rectangles but deliberately excluded dynamic occupancy from its cached blocker prefix and never performed a live unit-footprint check, allowing a Tent to be confirmed over another owner. Preview and confirmation now share one placement-occupant boundary: runtime buildings retain their canonical rectangle owner, while live infantry and full multi-cell vehicle footprints are queried directly; runtime-building ECS proxies and static blockers remain excluded from that dynamic layer to avoid double ownership. The first broader test entry point was rejected by an unrelated pre-existing harness setup null; the isolated fail-closed occupancy entry point passed, and the full Editor-first batch remained green. No APK was built or installed under the Editor-first batching strategy. VRP-072 remains unstarted, and unrelated generated reports and all `_Recovery` scenes remain unstaged. |
| 2026-08-04 | Independent correctness: operation-map production queue drawer read model | Checked Windows GUI-licensing `Build/Logs/building-ui-query-operation-map-queue.log` `[BuildingUiQueryValidation] result=Passed tests=11`; checked real-prefab view `Build/Logs/build-drawer-operation-map-queue-view.log` `[BuildDrawerProductionQueueSnapshotValidation] result=Passed tests=1`; rejected aggregate drawer run `Build/Logs/build-drawer-operation-map-queue-regression.log` on an unrelated order-dependent legacy placement-price assertion after 13 passing steps; rejected malformed one-test Test Framework invocation `Build/Logs/build-drawer-queue-snapshot-isolated.log` at its wrapper timeout; `git diff --check` | Stale `NO PRODUCTION QUEUED` read-model cause repaired and Editor-accepted; consolidated Android confirmation remains pending; checklist progress stays `56 / 80` | The build drawer queried only managed `RuntimeBuildingEntity.PendingProductions`, while canonical Contractor/operation-map producers enqueue into `OperationMapBuildingUnitProductionRequest`; a successful Recruit or helicopter request could therefore remain invisible in the queue pane. The friendly production query now joins live player-owned, healthy, non-destroyed operation-map producer buffers, resolves their configured preview prefabs, and projects bounded progress/timing without enabling unsupported cancellation. Focused coverage proves a freshly queued Contractor-style request appears at 25% with the correct producer and prefab; the real drawer prefab independently proves active/queued/empty presentation. The unrelated aggregate failure and malformed test invocation are recorded rather than accepted. No APK was built or installed under the Editor-first batching strategy. VRP-072 remains unstarted, and unrelated generated reports and all `_Recovery` scenes remain unstaged. |
| 2026-08-04 | Independent correctness: current-head Editor batch and consolidated Samsung partial acceptance | Checked Windows GUI-licensing `Build/Logs/editor-first-functional-batch-current-head-rerun.log` `[EditorFirstProductionFunctionalBatchValidation] result=Passed suites=8 tests=92`; checked package `Build/Logs/editor-first-consolidated-current-head-profiler-apk.log` `[DenseCityCandidateAndroidPackage] result=Passed`; exact 696,278,899-byte APK SHA-256 `8dd5d7581d5afc26fec9679877959f6f25a911bfc9d233d12d98d02d6e2ada70`; wireless Samsung `SM-S918B` / `R5CTC1J02VB`; exact-activity cold launches `524`, `604`, and `498 ms`; correctly scaled main-page Deploy; evidence under `Build/AndroidDenseCandidateProfiler/Evidence/2026-08-04-editor-batch-final`; Android selection, production, occupancy, and frame diagnostics | Contractor completion/Recruit and attack-helicopter production/selection accepted; placement overlap, Armor selection, Transport/drop visibility, and spawned-soldier interaction rejected; checklist progress stays `56 / 80` | The full current-head Editor batch passed 92/92 before exactly one fresh package was built. On Samsung, a clear-ground Contractor completed with the correct portrait and `350/350`; Advanced Rifle recruitment visibly progressed, cleared, and increased runtime ownership `units/sourceKeys 5006 -> 5007` and rendered models `22 -> 23`. Light Attack Helicopter production likewise progressed and cleared, selected through the Gunship roster at `520/520`, and accepted direct world focus on `Unit_Veh_Helicopter_Attack_Small`. The consolidated gate remains rejected because an initial `13x11` Contractor preview over an existing Tent incorrectly reported valid placement, Armor reported `SquadTraySelectSkipped ... NoCandidates`, Transport reported the same with no visible rope/drop control, and likely spawned-soldier taps were consumed as `SuppressNextWorldClick` without focused ownership. Samples were roughly `34-55 FPS`, with one `122.4 ms` placement/update hitch; this is evidence only and does not authorize a render-policy mutation. VRP-072 remains unstarted. Blue Autobahn/canal isolation and road-over-water-without-bridge remain separate. The unrelated generated report and all `_Recovery` scenes remain unstaged; no protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |
| 2026-08-04 | Independent correctness: operation-map building placement occupancy ownership | Checked Windows GUI-licensing `Build/Logs/building-placement-operation-map-occupancy-editor.log`; `[BuildingPlacementLiveOccupancyValidation] result=Passed tests=2`; `git diff --check` | Packaged overlap cause repaired and Editor-accepted; consolidated Android confirmation remains pending; checklist progress stays `56 / 80` | The prior live-occupancy repair excluded every ECS entity carrying `RuntimeBuildingCombatTag`, assuming the managed runtime-building dictionary owned all such footprints. Map-authored operation-map buildings carry that tag but are not managed runtime-building instances, so an existing Tent could be invisible to preview and confirmation. The occupancy boundary now excludes only non-operation-map runtime-building proxies; canonical `OperationMapBuildingComponent` entities block through their `UnitGrid` and full `UnitFootprint`. Focused coverage distinguishes a managed proxy, static blocker, live infantry, full vehicle footprint, and map-authored building. No APK was built or installed under the Editor-first batching strategy. Armor selection, Transport/drop visibility, and spawned-soldier interaction remain independent Editor-first defects; VRP-072 remains unstarted. The unrelated generated report and all `_Recovery` scenes remain unstaged; no protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |
| 2026-08-04 | Independent correctness: stable-identity authored vehicle adoption and Armor selection | Checked Windows GUI-licensing `Build/Logs/map-vehicle-stable-identity-selection-editor.log`; `[MapVehiclePlacementStartupCompletionValidation] result=Passed tests=4`; checked real candidate `Build/Logs/operation-map-vehicle-stable-identity-readiness-editor.log`; `[OperationMapEntityPresentationReadiness] result=Passed buildings=432 vehicles=22 renderOnly=9090`; checked aggregate `Build/Logs/editor-first-functional-batch-stable-vehicle-identity.log`; `[EditorFirstProductionFunctionalBatchValidation] result=Passed suites=8 tests=92`; rejected visible full-match Editor attempt in `Logs/Editor.log` at `[CanonicalVehicleSelectionPlayModeValidation] result=Failed timed out deploy=1 matchReady=0` after the pre-existing typed Addressables manifest preload failure; `git diff --check` | Migrated authored-vehicle ownership and Armor roster selection are repaired and Editor-accepted; consolidated Android confirmation remains pending; checklist progress stays `56 / 80` | The prior adoption repair still selected candidates only by a one-metre transform heuristic. Entity-presentation migration may preserve visible bounds while changing gameplay/render pivots, so the exact canonical vehicle could remain neutral and the packaged Armor card then reported `NoCandidates`. Startup now resolves migrated vehicles first through `OperationMapAuthoredVehiclePresentation -> UnitDetailedVisualReference.Root -> OperationMapEntityPresentationIdentity.PlacementIndex`; only legacy vehicles without the migrated marker may use proximity fallback, and a mismatched migrated identity is rejected fail-closed. Focused coverage places the migrated vehicle six metres from its compatibility transform, adopts the exact stable identity, applies player ownership, and selects it through the real Armor tray helper. The real candidate readiness gate independently confirms all 22 gameplay vehicles and legacy-placement parity, while the complete 92-test production/selection batch remains green. The full Menu-to-Deploy Editor route did not reach gameplay because GUID `2d7b...` again resolved as `System.Object` instead of `StaticMapPresentationManifest`; no full-match pass is claimed from that rejected route. No APK was built or installed under the Editor-first batching strategy. Transport/drop visibility and spawned-soldier interaction remain separate; VRP-072 remains unstarted. The unrelated generated report and all `_Recovery` scenes remain unstaged; no protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |
| 2026-08-04 | Independent correctness: Transport identity selection and post-UI spawned-unit focus | Rejected restricted-host GUI-wrapper attempts `Build/Logs/rts-selection-captured-ui-release-editor.log` and `-rerun.log` before project load on licensing IPC denial; checked host-access GUI wrappers `Build/Logs/rts-selection-captured-ui-release-editor-host.log` `[RtsSelectionInputSystemValidation] result=Passed tests=63`, `Build/Logs/map-vehicle-stable-identity-transport-selection-editor.log` `[MapVehiclePlacementStartupCompletionValidation] result=Passed tests=5`, and `Build/Logs/editor-first-functional-batch-ui-release-transport-rerun.log` `[EditorFirstProductionFunctionalBatchValidation] result=Passed suites=8 tests=92`; rejected aggregate invocation `Build/Logs/editor-first-functional-batch-ui-release-transport.log` because its shorthand class did not exist; `git diff --check` | Remaining Transport roster and spawned-unit input causes are repaired and Editor-accepted; one consolidated Android confirmation remains pending; checklist progress stays `56 / 80` | Gameplay UI click capture cleared its input-side release flags but left `RuntimeGameplayState.SuppressNextWorldClick` set, so the first legitimate world tap after Recruit was consumed before unit focus. Captured release completion now clears all release-scoped input flags and runtime suppression atomically; the new regression proves the immediately following world-unit click invokes focus exactly once. Independently, stable-identity adoption coverage now includes a migrated transport helicopter eight metres from its compatibility transform, applies player ownership through placement identity `18`, and selects it through the real Transport roster slot. The complete production/selection batch remains green at 92/92, including the existing visible helicopter/rope/drop/completion contract. No APK was built or installed under the Editor-first batching strategy. The next Android work is one consolidated functional pass only; VRP-072 remains unstarted. The unrelated generated report and all `_Recovery` scenes remain unstaged; no render policy, protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |
| 2026-08-04 | Independent correctness: post-UI/Transport consolidated Samsung pass | Implementation commit `b5133b9f7`; checked Windows GUI-licensing `Build/Logs/rts-selection-captured-ui-release-editor-host.log` `[RtsSelectionInputSystemValidation] result=Passed tests=63`, `Build/Logs/map-vehicle-stable-identity-transport-selection-editor.log` `[MapVehiclePlacementStartupCompletionValidation] result=Passed tests=5`, and `Build/Logs/editor-first-functional-batch-ui-release-transport-rerun.log` `[EditorFirstProductionFunctionalBatchValidation] result=Passed suites=8 tests=92`; checked package `Build/Logs/editor-batch-b5133b9f7-profiler-apk.log` `[DenseCityCandidateAndroidPackage] result=Passed`; exact 696,278,899-byte APK SHA-256 `3209bf05532f2a4adc463b03d63f5b9409c6e0b7f13eac7d46125dd150f66090`; wireless Samsung `SM-S918B` / `R5CTC1J02VB` at the currently discovered `192.168.2.33:44569`; exact activity cold launch `361 ms`; correctly scaled main-page Deploy; evidence under `Build/AndroidDenseCandidateProfiler/Evidence/2026-08-04-post-ui-transport-batch`; Android screenshots, selection diagnostics, and frame diagnostics | Contractor completion/Recruit and spawned-soldier existence/roster/direct-world interaction accepted; placement overlap and Armor/Transport roster selection rejected; visible helicopter/rope/drop remains unconfirmed; checklist progress stays `56 / 80` | The single fresh post-Editor-batch APK installed successfully and reached the live map through the required main-page Deploy control. A clear-ground `13x11` Contractor spent materials `120/60 -> 100/60`, completed, and visibly showed the correct `Contractor Tent (7)` title, action portrait, bounded marker, and `350/350`. The Soldiers tab exposed Recruit; two Advanced Rifleman requests spent `25` materials each and raised `units/sourceKeys 5006 -> 5007 -> 5008` and rendered models `22 -> 23 -> 24`. The first spawned rifleman selected through the Rifle Squad card with the correct portrait, `135/135`, visible world model, and footprint-scaled cyan marker. Its subsequent direct world tap independently traversed the repaired input path with `suppress=False`, resolved `unitLookup result=True` to `Unit_Chr_Soldier_Male_01_Alt_02`, and emitted both `focusAttempt result=True` and `clickFocus result=True`. The consolidated gate nevertheless remains rejected: the initial preview directly over an existing beige Tent still reported `VALID PLACEMENT (874,374) 13X11`; Armor and Transport each emitted `SquadTraySelectSkipped ... reason=NoCandidates`; and no helicopter/rope/drop sequence was visibly captured during the second Recruit. Stable samples were roughly `44-54 FPS`, with GPU samples about `9.7-20.6 ms`; these remain evidence only and authorize no VRP-072 render-policy mutation. VRP-072 remains unstarted. Blue Autobahn/canal isolation and road-over-water-without-bridge remain separate. The unrelated generated report and all `_Recovery` scenes remain unstaged; no protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |

| 2026-08-04 | Independent correctness: baked operation-map static-blocker occupancy correction | Checked Windows GUI-licensing `Build/Logs/building-placement-baked-operation-map-occupancy-editor.log`; `[BuildingPlacementLiveOccupancyValidation] result=Passed tests=2`; checked aggregate `Build/Logs/editor-first-functional-batch-operation-map-occupancy.log`; `[EditorFirstProductionFunctionalBatchValidation] result=Passed suites=8 tests=92`; `git diff --check` | Exact packaged-Tent archetype is repaired and Editor-accepted; consolidated Android confirmation remains pending; checklist progress stays `56 / 80` | The prior operation-map occupancy regression modeled `RuntimeBuildingCombatTag + OperationMapBuildingComponent` but omitted the `StaticGridBlocker` that `OperationMapBuildingAuthoring` adds to every baked building. Production therefore skipped the beige Tent at the static-blocker guard before reaching the operation-map exception, matching the Samsung-valid `13x11` overlap. The occupancy predicate now retains baked operation-map buildings even when they carry `StaticGridBlocker`, while still excluding ordinary static blockers, prefabs, and non-operation-map runtime-building proxies. Focused coverage now mirrors the exact baked component combination and the complete 92-test Editor-first functional batch remains green. No APK was built or installed under the Editor-first batching strategy. Armor/Transport Android selection and visible rope/drop confirmation remain independent consolidated gates; VRP-072 remains unstarted. The unrelated generated report and all `_Recovery` scenes remain unstaged; no render policy, protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |

| 2026-08-04 | Independent correctness: late-packed authored-vehicle readiness and ownership reconciliation | Checked host-access Windows wrapper `Build/Logs/map-vehicle-late-packed-readiness-editor-host.log`; `[MapVehiclePlacementStartupCompletionValidation] result=Passed tests=6`; checked aggregate `Build/Logs/editor-first-functional-batch-late-packed-readiness-host.log`; `[EditorFirstProductionFunctionalBatchValidation] result=Passed suites=8 tests=92`; `git diff --check`; bundled-SDK ADB/mDNS discovery | Packaged Armor/Transport `NoCandidates` startup cause repaired and Editor-accepted; consolidated Android confirmation remains pending; checklist progress stays `56 / 80` | Packed operation-map vehicles can arrive after the managed match shell's one-shot placement cursor. Startup now waits for the baked readiness contract's expected authored-vehicle count before consuming that cursor, preserves normal neutral-vehicle adoption, and only then reconciles stable-placement faction and canonical source identity. This prevents late presentation from remaining neutral/source-stale without causing a duplicate prefab spawn. The strengthened regression proves readiness is false before the expected authored marker, true afterward, and that the reconciled player tank selects through the real Armor roster path; the complete production/selection batch remains green. The first sandboxed rerun exceeded its 600-second licensing timeout and was rejected; an initial aggregate invocation used a nonexistent class and was also rejected. The recorded host-access wrapper runs passed unchanged code. No APK was built or installed under the Editor-first batching strategy. Samsung wireless service `adb-R5CTC1J02VB-lJmfCF` was discoverable at `192.168.2.33:44569` but not connected. VRP-072 remains unstarted; blue Autobahn/canal isolation and road-over-water-without-bridge remain separate. Both generated reports and all `_Recovery` scenes remain unstaged; no render policy, protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |

| 2026-08-04 | Consolidated post-Editor-batch Android package preparation | Implementation commit `4fe833c18`; checked GUI-licensing wrapper `Build/Logs/editor-batch-4fe833c18-profiler-apk-shortpath.log`; `[DenseCityCandidateAndroidPackage] result=Passed`; exact 696,278,899-byte APK SHA-256 `4bab342b42eebfbaa1f9e0e31ea1014a2e39daa18ddf68858cc0bedf22e66f05`; temporary verified `W:\` project mapping removed after success | One fresh consolidated profiler package is accepted and ready for the Samsung functional pass; checklist progress stays `56 / 80` | The package was built only after the late-packed vehicle readiness gate passed 6/6 and the complete Editor-first functional batch passed 92/92. An initial invocation using the non-namespaced build class was rejected before building; the first real build was rejected at Gradle on Windows MAX_PATH; the unchanged checked build then passed through the previously accepted short project mapping, including the package ownership gate, and the mapping was removed. No APK installation or device acceptance is claimed here. This exact package is the only authorized input for the next consolidated Samsung pass. VRP-072 remains unstarted, and no render policy, protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. Both generated reports and all `_Recovery` scenes remain unstaged. |

| 2026-08-05 | Independent correctness: consolidated post-Editor-batch Samsung functional rejection | Implementation/package head `4fe833c18`; exact checked 696,278,899-byte APK SHA-256 `4bab342b42eebfbaa1f9e0e31ea1014a2e39daa18ddf68858cc0bedf22e66f05`; streamed wireless install only to Samsung `SM-S918B` / `R5CTC1J02VB` at user-provided `192.168.2.33:45281`; exact activity cold launch `421 ms`; correctly scaled main-page Deploy; evidence under `Build/AndroidDenseCandidateProfiler/Evidence/2026-08-05-consolidated-functional`; Android screenshots, selection diagnostics, ownership counters, and frame diagnostics | Light Attack Helicopter production/completion and Gunship roster selection accepted; baked-building overlap rejection and Armor/Transport roster ownership rejected; Recruit completion accepted but the helicopter/rope/drop presentation remains visually rejected; checklist progress stays `56 / 80` | The authorized current-head package installed and reached the live map through the required Deploy control. Light Attack Helicopter production visibly queued and completed, raised runtime ownership, and selected through Gunship with the correct portrait and `520/520`. In an independent fresh session, a clear-ground `13x11` Contractor completed with the correct title, portrait, bounded marker, and `350/350`; Recruit spent `25` materials and raised `units/sourceKeys 5006 -> 5007` plus rendered models `22 -> 23`. The consolidated gate remains rejected because a `13x11` preview visibly intersecting an existing beige Tent still reported `VALID PLACEMENT`; correctly scaled Armor and Transport taps each emitted `SquadTraySelectSkipped ... reason=NoCandidates`; and twelve approximately 1.3-second post-Recruit frames showed no helicopter, rope, or drop presentation despite successful completion. Previously accepted Contractor and spawned-soldier interaction evidence is retained rather than rerun. Functional samples were roughly `43-55 FPS`; this evidence does not authorize VRP-072 render-policy changes. VRP-072 remains unstarted. Blue Autobahn/canal isolation and road-over-water-without-bridge remain separate. Both generated reports and all `_Recovery` scenes remain unstaged; no protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |

| 2026-08-05 | Independent correctness: canonical operation-map combat-origin placement occupancy | Checked GUI-licensing focused `Build/Logs/building-placement-canonical-combat-origin-editor.log` `[BuildingPlacementLiveOccupancyValidation] result=Passed tests=2`; rejected aggregate invocation `Build/Logs/editor-first-functional-batch-canonical-combat-origin.log` because the shorthand class did not exist; checked corrected aggregate `Build/Logs/editor-first-functional-batch-canonical-combat-origin-rerun.log` `[EditorFirstProductionFunctionalBatchValidation] result=Passed suites=8 tests=92`; `git diff --check` | The baked-building overlap cause exposed by Samsung is repaired and Editor-accepted; Armor/Transport ownership and visible rope/drop remain Editor-first; checklist progress stays `56 / 80` | The prior occupancy repair still derived an operation-map building rectangle from compatibility `UnitGrid` and `UnitFootprint`. Those fields can be stale or center-encoded differently after packed presentation adoption, while the canonical placement owner is `RuntimeBuildingCombatInfo.OriginCell/FootprintCells`. Placement occupancy now uses that canonical combat origin for operation-map buildings and retains the existing unit-grid path for infantry, vehicles, and legacy entities. The focused regression deliberately gives the baked building a stale compatibility center and proves its canonical rectangle blocks placement; the complete 92-test Editor-first functional batch remains green. No APK was built or installed under the user's batching strategy. VRP-072 remains unstarted. Both generated reports and all `_Recovery` scenes remain unstaged; no render policy, protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |

| 2026-08-05 | Independent correctness: headless packed-vehicle readiness-contract race | Checked GUI-licensing focused `Build/Logs/map-vehicle-packed-contract-race-editor.log` `[MapVehiclePlacementStartupCompletionValidation] result=Passed tests=6`; checked aggregate `Build/Logs/editor-first-functional-batch-packed-vehicle-contract-race.log` `[EditorFirstProductionFunctionalBatchValidation] result=Passed suites=8 tests=92`; `git diff --check` | The packaged Armor/Transport `NoCandidates` startup race is repaired and Editor-accepted; visible helicopter/rope/drop remains the final Editor-first defect; checklist progress stays `56 / 80` | The earlier readiness repair waited for the expected authored-vehicle count only after a readiness contract already existed. In a headless packed build, the managed startup tick can run before the gameplay-buildings EntityScene supplies that contract; the empty contract query was treated as ready, permanently consuming the one-shot vehicle placement cursor while all vehicles were absent. Headless startup with configured placements now requires the packed readiness contract before progressing, while a live authoring root retains legacy compatibility. The strengthened regression proves missing-contract headless startup is fail-closed, then proves the existing contract/count, stable Armor identity, Transport identity, ownership reconciliation, and both roster selection paths. The complete 92-test Editor-first batch remains green. No APK was built or installed under the user's batching strategy. VRP-072 remains unstarted. Both generated reports and all `_Recovery` scenes remain unstaged; no render policy, protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |

| 2026-08-05 | Independent correctness: late-packed production-transport registry cache repair | Rejected asynchronous full Menu-to-Deploy reproduction `Build/Logs/canonical-tent-full-in-map-repro-20260805-noquit2.log` at the pre-existing typed Addressables manifest mismatch (`2d7b...` resolved as `System.Object`) before gameplay readiness; checked GUI-licensing focused `Build/Logs/canonical-tent-late-packed-transport-editor.log` `[CanonicalTentTransportPresentationValidation] result=Passed transportVisible=1 ropeVisible=1 dropVisualVisible=1 ownership=canonical latePackedTransport=1`; refreshed visible `Build/EditorEvidence/CanonicalTentTransportDrop.png`; checked aggregate `Build/Logs/editor-first-functional-batch-late-packed-transport.log` `[EditorFirstProductionFunctionalBatchValidation] result=Passed suites=8 tests=93`; `git diff --check` | The packaged Recruit direct-spawn cause is repaired and Editor-accepted; one fresh consolidated APK and Samsung pass are dependency-ready; checklist progress stays `56 / 80` | The transport settings cache previously treated unchanged registry collection identities as unchanged contents. Packed startup fills those same collection objects after an early empty lookup, preserving a null default helicopter and causing Recruit delivery to return `NotRequired` and spawn directly. Cache validity now includes collection counts plus exact canonical helicopter/plane resolutions, invalidating per-unit settings as soon as late-packed transport content arrives. The focused regression proves empty-to-packed mutation on the same objects and the visible presentation gate proves helicopter, rope, drop visual, deferred spawn, and canonical ownership; the aggregate remains green at 93 tests. All three Samsung-rejected causes are now Editor-accepted, so the next bounded step is exactly one fresh checked profiler APK followed by one consolidated Samsung pass. VRP-072 remains unstarted. Both generated reports and all `_Recovery` scenes remain unstaged; no render policy, protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |

| 2026-08-05 | Independent correctness: zero-count bootstrap readiness-contract race | Fresh checked package head `8ae22f48f`; exact 696,278,899-byte APK SHA-256 `1c6563a495998654eb9f1807c71e34aead1c79ce4e55788af3306dd4f978e395`; streamed only to Samsung `SM-S918B` / `R5CTC1J02VB`; main-page Deploy succeeded; rejected Armor diagnostic `SquadTraySelectSkipped slot=CombatVehicles reason=NoCandidates`; rejected Transport diagnostic `SquadTraySelectSkipped slot=Transport reason=NoCandidates`; checked host-access GUI-licensing focused `Build/Logs/map-vehicle-zero-contract-readiness-editor.log` `[MapVehiclePlacementStartupCompletionValidation] result=Passed tests=6`; checked corrected aggregate `Build/Logs/editor-first-functional-batch-zero-contract-readiness.log` `[EditorFirstProductionFunctionalBatchValidation] result=Passed suites=8 tests=93`; `git diff --check` | The fresh package disproved functional closure; the newly exposed zero-count bootstrap-contract race is repaired and Editor-accepted; one replacement checked package and consolidated Samsung pass remain pending; checklist progress stays `56 / 80` | The exact current package installed and reached the candidate map, with visible Armor and Transport roster cards, but both taps still found no player candidates. The packed readiness helper was fail-closed while no contract existed, yet returned ready when an earlier base-stream contract existed with `ExpectedGameplayVehicleCount=0`. Because configured vehicle placements were present, that placeholder contract allowed the one-shot placement cursor to finish before the candidate stream's positive 22-vehicle contract arrived. EntityScene startup now treats a non-positive maximum expected count as not ready whenever the packed contract is required; legacy authoring-root startup remains compatible. The focused regression inserts a zero-count bootstrap contract before the positive map contract and proves startup remains closed. Two sandboxed GUI attempts failed licensing IPC and timed out; after precise wrapper-owned cleanup and a verified stale-client restart, the checked wrappers passed unchanged code through the documented host-access workaround. An aggregate shorthand class invocation was rejected before the corrected 93/93 pass. No replacement APK or Android acceptance is claimed yet. VRP-072 remains unstarted; blue Autobahn/canal isolation and road-over-water-without-bridge remain separate. Both generated reports and all `_Recovery` scenes remain unstaged; no protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |

| 2026-08-05 | Independent correctness: candidate runtime-binding MonoScript serialization | Rejected checked package head `ddb904691`; exact 696,278,808-byte APK SHA-256 `fd8bb448ed2c036c90dce297d2e47933660ebf89491009b579ea0f978fe407c7`; streamed only to Samsung `SM-S918B` / `R5CTC1J02VB`; main-page Deploy stalled at 90% with `OperationMapSourceScene code=MetadataBindFailed` and missing `decorationCombinedMeshBaker`; package log warned that `CombinedMeshBaker` was missing from the candidate scene; checked GUI-licensing diagnostic `Build/Logs/combined-mesh-baker-monoscript-without-movedfrom.log` `[CombinedMeshBakerMonoScriptValidation] result=Passed`; repair `Build/Logs/candidate-runtime-binding-resolvable-monoscript-repair.log` `[CandidateRuntimeBindingColdRepairValidation] result=Passed scenes=2`; cold focused `Build/Logs/candidate-runtime-binding-resolvable-monoscript-final-focused.log` `[OperationMapEntitySceneCandidateBakeAllValidation] result=Passed tests=25`; aggregate `Build/Logs/editor-first-functional-batch-resolvable-monoscript-final.log` `[EditorFirstProductionFunctionalBatchValidation] result=Passed suites=8 tests=93`; `git diff --check` | The replacement package disproved functional closure before gameplay; candidate binding serialization is repaired and Editor-accepted; one fresh checked package and consolidated Samsung pass remain pending; checklist progress stays `56 / 80` | The candidate builder previously deleted the valid copied `CombinedMeshBaker`, recreated it through a transient embedded `MonoScript`, externalized that row, and serialized a migration-only identity. Android Addressables stripped the component, so Deploy could not bind the operation-map view. Direct Unity diagnostics proved the script GUID existed but `MonoScript.GetClass()` returned null while the live component carried `MovedFrom`; removing that migration restored exact resolution to `Game.Runtime.CombinedMeshBaker`. The builder now preserves the typed component from the production `SaveScene` copy, both candidate bindings retain the concrete current identity, and focused coverage fails closed on GUID/class drift. Rejected identity-only attempts and one compile-diagnostic timeout are not counted as validation. No fresh APK or Android functional acceptance is claimed yet. VRP-072 remains unstarted; blue Autobahn/canal isolation and road-over-water-without-bridge remain separate. Both generated reports and all `_Recovery` scenes remain unstaged; no render policy, Jenkins/Unity path, or `productionCutover=0` changed. |

| 2026-08-05 | Independent correctness: packed vehicle ownership-completion fence | Rejected real Editor Menu-to-Deploy reproduction `%TEMP%\canonical-vehicle-packed-runtime-diagnostic-nonquit.log` before Match readiness on the pre-existing typed Addressables manifest mismatch (`2d7b...` resolved as `System.Object`); checked diagnostic profiler package `Build/Logs/packed-vehicle-runtime-diagnostic-profiler-apk.log` `[DenseCityCandidateAndroidPackage] result=Passed`; exact diagnostic APK 738,752,587 bytes, SHA-256 `4e9b1134df8bb9e82c6c8b1186e41d8097877c72eaa93434f7d863cae867c631`; Samsung `SM-S918B` / `R5CTC1J02VB` main-page Deploy and Armor/Transport diagnostics; checked GUI-licensing focused `Build/Logs/map-vehicle-packed-ownership-completion-focused.log` `[MapVehiclePlacementStartupCompletionValidation] result=Passed tests=8`; checked aggregate `Build/Logs/editor-first-functional-batch-packed-vehicle-ownership-completion.log` `[EditorFirstProductionFunctionalBatchValidation] result=Passed suites=8 tests=93`; `git diff --check` | Exact Android cause is proven and the ownership-completion fix is Editor-accepted; one clean replacement package and consolidated Samsung pass remain open; checklist progress stays `56 / 80` | Device diagnostics inspected all 22 actual packed vehicle entities. Every entity was non-prefab/non-disabled and carried the expected `UnitGrid`, `UnitMove`, `LocalToWorld`, motion/transport classification, `UnitDetailedVisualReference`, role-2 `OperationMapEntityPresentationIdentity`, correct operation-map id, placement index, and canonical source key; every one remained `Faction.Id=0`, so Armor/Transport correctly found no player candidates. The prior completed-cursor reconciliation existed, but the outer building-startup owner stopped invoking the vehicle startup tick as soon as queued/hidden progress reported complete. Packed-mode startup completion now additionally requires every readiness-contract vehicle to match its placement faction and canonical source identity, keeping the existing startup tick alive until reconciliation succeeds. The regression proves neutral packed ownership blocks completion, reconciliation assigns player ownership, and completion then opens. The diagnostic package is evidence only and is not final acceptance. VRP-072 remains unstarted; blue Autobahn/canal isolation and road-over-water-without-bridge remain separate. Both generated reports and all `_Recovery` scenes remain unstaged; no protected/production asset, Jenkins/Unity path, render policy, or `productionCutover=0` changed. |
| 2026-08-05 | Independent correctness: ownership-completion fence fresh Samsung rejection | Implementation/package head `cd1eed5fd`; recovered cold binding checks `Build/Logs/combined-mesh-baker-cd1eed5fd-restored-resolution.log` `[CombinedMeshBakerMonoScriptValidation] result=Passed` and `Build/Logs/candidate-runtime-binding-cd1eed5fd-reimport-repair.log` `[CandidateRuntimeBindingColdRepairValidation] result=Passed scenes=2`; checked GUI-licensing short-path package `Build/Logs/editor-batch-cd1eed5fd-profiler-apk-shortpath-rerun.log` `[DenseCityCandidateAndroidPackage] result=Passed`; exact 738,748,891-byte APK SHA-256 `0825b8a3a3506ad0e9682093d8c1d4f4bdcd8a5f6f8a4e606c11e0f07b4fee5f`; streamed only to Samsung `SM-S918B` / `R5CTC1J02VB`; corrected display-scaled main-page Deploy; evidence under `Build/AndroidDenseCandidateProfiler/Evidence/2026-08-05-packed-ownership-completion` | Fresh Android package disproves the Editor-only ownership fence; Armor roster selection remains rejected and checklist progress stays `56 / 80` | The valid main-page Deploy route passed operation-map readiness and logged `[LoadingGate] ready ... playRequested=1 simulationActive=1`; the loaded map retained 22 vehicle models. Armor still emitted `SquadTraySelectSkipped slot=CombatVehicles reason=NoCandidates`, and the exact live diagnostic again showed every packed vehicle as `faction=0 player=0`. The completion callback therefore did not keep the live startup branch open. Code audit identifies the remaining branch mismatch: the packed-mode requirement is captured once from the managed compatibility `sceneView` during initialization, while the compatibility vehicle authoring root remains assigned; the later positive packed readiness contract does not promote that frozen flag, and simulation ticks no longer invoke map-vehicle reconciliation. Transport/world selection and helicopter/rope/drop were not rerun after the Armor gate rejected; earlier accepted overlap, Contractor, and spawned-soldier evidence remains retained. The APK will not be reinstalled. The next bounded correction must make the live positive readiness contract promote reconciliation independently of the stale managed-mode snapshot and must cover the post-startup late-contract route. VRP-072 remains unstarted; blue Autobahn/canal isolation and road-over-water-without-bridge remain separate. Both generated reports and all `_Recovery` scenes remain unstaged; no protected/production asset, Jenkins/Unity path, render policy, or `productionCutover=0` changed. |
| 2026-08-05 | Independent correctness: live packed-contract ownership promotion | Checked GUI-licensing focused `Build/Logs/map-vehicle-live-contract-promotion-focused.log` `[MapVehiclePlacementStartupCompletionValidation] result=Passed tests=9`; rejected aggregate shorthand `Build/Logs/editor-first-functional-batch-live-packed-contract-promotion.log` before tests because `BuildingProductionSystemTests` was not the actual class; checked corrected aggregate `Build/Logs/editor-first-functional-batch-live-packed-contract-promotion-rerun.log` `[EditorFirstProductionFunctionalBatchValidation] result=Passed suites=8 tests=93`; `git diff --check` | The proven stale-mode/post-startup ownership branch is repaired and Editor-accepted; one fresh checked package and consolidated Samsung pass remain required; checklist progress stays `56 / 80` | A positive live `OperationMapEntityPresentationReadinessContract` now promotes packed vehicle handling even when the managed startup snapshot was compatibility mode and a compatibility authoring root remains assigned. If that contract arrives after startup has already opened, the simulation path invokes only the existing vehicle placement/reconciliation action while canonical ownership is incomplete, then stops once all packed vehicles match placement faction and source identity. The regression starts from a completed compatibility cursor with a positive late contract and a neutral/source-stale packed vehicle, proving the real update path promotes packed mode and assigns player ownership/canonical source. The first focused wrapper command host timed out after launching Unity; the exact wrapper-started Editor passed 9/9 and logged complete shutdown before its lingering process was terminated. No APK or Android acceptance is claimed yet. VRP-072 remains unstarted; blue Autobahn/canal isolation and road-over-water-without-bridge remain separate. Both generated reports and all `_Recovery` scenes remain unstaged; no protected/production asset, Jenkins/Unity path, render policy, or `productionCutover=0` changed. |
| 2026-08-05 | Independent correctness: live packed-contract fresh Samsung rejection | Implementation/package head `6158e1388`; first checked short-path package `Build/Logs/editor-batch-6158e1388-profiler-apk-shortpath.log` rejected before Gradle on the known cold `CombinedMeshBaker` importer-cache condition; reversible source-touch recovery `Build/Logs/combined-mesh-baker-6158e1388-reimport-touch.log` passed, exact restored source `Build/Logs/combined-mesh-baker-6158e1388-restored-resolution.log` passed, and candidate repair `Build/Logs/candidate-runtime-binding-6158e1388-reimport-repair.log` passed `scenes=2`; checked package retry `Build/Logs/editor-batch-6158e1388-profiler-apk-shortpath-rerun.log` `[DenseCityCandidateAndroidPackage] result=Passed`; exact 738,752,365-byte APK SHA-256 `c1a2d3ee49bdf1c0e147e2505052438658d1e1faa6f21639228a81975461215b`; streamed only to Samsung `SM-S918B` / `R5CTC1J02VB`; evidence under `Build/AndroidDenseCandidateProfiler/Evidence/2026-08-05-live-packed-contract-promotion` | Fresh Android package disproves the live-contract Editor fixture; Armor selection remains rejected and checklist progress stays `56 / 80` | The required main-page Deploy route reached `[LoadingGate] ready ... playRequested=1 simulationActive=1` with 22 rendered vehicle models. Armor still emitted `SquadTraySelectSkipped slot=CombatVehicles reason=NoCandidates`; exact live diagnostics still show every packed vehicle neutral, beginning with `placement=11 ... faction=0 player=0`. Therefore the new simulation reconciliation branch is not reached in the actual packaged composition, despite the focused 9/9 and aggregate 93/93 Editor gates. Transport/world selection and helicopter/rope/drop were not rerun after Armor rejected; earlier accepted overlap, Contractor, and spawned-soldier evidence remains retained. This APK will not be reinstalled. The next step must instrument the actual packaged building-runtime composition/callback invocation and live config/contract state before another ownership change; synthetic helper fixtures are insufficient. VRP-072 remains unstarted; blue Autobahn/canal isolation and road-over-water-without-bridge remain separate. Both generated reports and all `_Recovery` scenes remain unstaged; no protected/production asset, Jenkins/Unity path, render policy, or `productionCutover=0` changed. |
| 2026-08-05 | Independent correctness: late operation-map vehicle config resolution | Diagnostic implementation head `d2a9dfe75`; checked diagnostic package `Build/Logs/editor-batch-d2a9dfe75-packaged-composition-diagnostic-rerun.log` `[DenseCityCandidateAndroidPackage] result=Passed`; exact 738,751,031-byte APK SHA-256 `d007025695afcf08faf63a942b1bce0cef9d2277a6ba8dbc13e957834cae172f`; streamed only to Samsung `SM-S918B` / `R5CTC1J02VB`; packaged markers `[MapVehicleOwnershipRuntime] phase=CompositionInit configAssigned=0 placements=-1 authoringRootAssigned=1 configuredPacked=1`, `phase=RuntimeContextReady ... positiveContract=1`, and `phase=LiveContractObserved ... authoredVehicles=22`; checked host-access GUI-licensing focused `Build/Logs/map-vehicle-live-config-provider-focused-host-final.log` `[MapVehiclePlacementStartupCompletionValidation] result=Passed tests=10`; checked aggregate `Build/Logs/editor-first-functional-batch-live-config-provider-host.log` `[EditorFirstProductionFunctionalBatchValidation] result=Passed suites=8 tests=93`; `git diff --check` | The actual packaged composition cause is proven and the live-config correction is Editor-accepted; one fresh checked package and consolidated Samsung pass remain required; checklist progress stays `56 / 80` | The diagnostic APK proved that all 22 baked vehicle presentations and the positive readiness contract were live, while managed building composition had captured `MapVehiclePlacementConfig=null`; consequently the existing ownership callback had no placement/faction/source contract to reconcile and every rendered vehicle remained neutral. Match bootstrap now supplies bounded live providers for the operation-map vehicle config and authoring root; every building-runtime vehicle update, ownership-completion check, and startup-completion check resolves the current values instead of the stale initialization snapshot. The new integration regression initializes composition with a null snapshot, publishes the config later, invokes the real simulation update, and proves the neutral packed tank becomes player-owned with canonical source identity. Restricted GUI attempts were rejected on licensing IPC; after precise wrapper-owned cleanup, verified stale-client restart, official Unity-log review, and the documented checked host-access recovery, the focused and aggregate wrappers returned success. No corrected APK or Android acceptance is claimed yet. The diagnostic APK will not be reinstalled. VRP-072 remains unstarted; blue Autobahn/canal isolation and road-over-water-without-bridge remain separate. Both generated reports and all `_Recovery` scenes remain unstaged; no protected/production asset, Jenkins/Unity path, render policy, or `productionCutover=0` changed. |
| 2026-08-05 | Independent correctness: baked vehicle faction ownership | Checked GUI-licensing focused `Build/Logs/vehicle-baked-faction-dedicated-authoring-focused.log` `[OperationMapEntityPresentationIdentityValidation] result=Passed tests=11`; transactional candidate migration `Build/Logs/vehicle-baked-faction-dedicated-candidate-migration.log` `vehicles=22`; final real candidate bake `Build/Logs/vehicle-baked-faction-dedicated-real-candidate-bake.log`; dense propagation `Build/Logs/vehicle-baked-faction-dedicated-dense-propagation.log`; focused real dense bake `Build/Logs/vehicle-baked-faction-dedicated-real-dense-focused.log` `[DenseCityCandidateVehicleOwnershipBake] result=Passed vehicles=22 placementFactionParity=22/22`; aggregate `Build/Logs/editor-first-functional-batch-vehicle-baked-faction-dedicated.log` `[EditorFirstProductionFunctionalBatchValidation] result=Passed suites=8 tests=93` | The proven packaged neutral-ownership defect is corrected in the baked source contract and Editor-accepted; one new checked APK and consolidated Samsung pass remain required; checklist progress stays `56 / 80` | A dedicated ownership authoring exists only on the 22 gameplay-vehicle models and carries each authoritative placement faction into the `UnitGridAuthoring` bake. The owner entity receives matching `Faction` and `OperationMapAuthoredVehiclePresentation { PlacementIndex, FactionId }`, so runtime selection no longer depends on the deliberately absent managed placement config. Both protected candidate scenes were updated only through rollback-backed Editor transactions. The final protected candidate bake passed 432 buildings and 22 vehicles, and the exact dense scene Android consumes passed 22/22 placement/faction parity, including the intentionally neutral placement 9. The broad dense bake remains separately rejected before vehicle validation on its pre-existing authoring count (`GameplayBuildings 4977`, expected `5403`); no broad dense acceptance is claimed. An earlier aggregate invocation used a nonexistent shorthand class; the final corrected dedicated-component invocation passed 93/93. No APK or Android acceptance is claimed yet. VRP-072 remains unstarted; blue Autobahn/canal isolation and road-over-water-without-bridge remain separate. Both generated reports and all `_Recovery` scenes remain unstaged; accepted/production sources, Jenkins/Unity paths, render policy, and `productionCutover=0` remain unchanged. |
| 2026-08-05 | Independent correctness: placement confirmation parent-canvas ownership | Samsung InputDispatcher/Unity pointer diagnostics and evidence `24-pointer-location-probe.png`, `28-confirm-transform-candidates.png`, `31-confirm-yflip-probe.png`, and `33-confirm-safeinset-probe.png`; checked GUI-licensing focused `Build/Logs/build-placement-parent-canvas-narrow-focused-live.log` `[BuildPlacementParentCanvasValidation] result=Passed`; checked aggregate `Build/Logs/editor-first-functional-batch-placement-parent-canvas-rerun.log` `[EditorFirstProductionFunctionalBatchValidation] result=Passed suites=8 tests=93`; `git diff --check` | The Android placement-confirmation click-through cause is repaired and Editor-accepted; one fresh checked APK and remaining consolidated Samsung gates are required; checklist progress stays `56 / 80` | A raw tap at the visible Confirm center reached Unity at the correct bottom-left coordinate, but UI selection diagnostics reported `anyUi=False gameplayUi=False blockingUi=False` and selected the world owner behind the bar. The regression was introduced when the transient placement bar acquired its own nested Canvas/GraphicRaycaster: rendered geometry and interactive geometry diverged on Android. The bar now retains the parent shell Canvas/raycaster while all other HUD batching remains unchanged. The focused live-component regression proves `Awake`/layout caching creates no local Canvas or GraphicRaycaster and resolves the parent shell Canvas. Two earlier focused fixtures were honestly rejected on an unrelated null serialized menu router and a cold prefab type-resolution quirk before reaching the bounded assertion; the direct live-component gate then passed. No replacement APK or Android acceptance is claimed yet. VRP-072 remains unstarted; blue Autobahn/canal isolation and road-over-water-without-bridge remain separate. Both generated reports and all `_Recovery` scenes remain unstaged; no protected/production asset, Jenkins/Unity path, render policy, or `productionCutover=0` changed. |
| 2026-08-06 | Independent functional acceptance: consolidated Samsung ownership, placement, and delivery | Implementation/package head `8a2643b4e`; corrected full reimport with separate `-quit`/`-reimportAll`; APH-808 `Build/Logs/aph808-after-bounded-source-touch.log` passed; focused `Build/Logs/build-placement-parent-canvas-after-import-recovery.log` passed; aggregate `Build/Logs/editor-first-functional-batch-after-import-recovery.log` passed 93/93; checked package `Build/Logs/editor-batch-8a2643b4e-profiler-apk-after-import-recovery.log` passed with zero missing-script warnings; exact 777,444,713-byte APK SHA-256 `376018de87667295184fe968de027751c5aeac3b3227dae0bba22b2916cf95ec`; evidence under `Build/AndroidDenseCandidateProfiler/Evidence/2026-08-06-placement-parent-canvas-import-recovery` | The Android functional dependency lane is accepted; checklist progress remains `56 / 80` because no VRP checklist row closes; VRP-072 remains unstarted | The clean package was streamed exactly once only to authenticated Samsung `SM-S918B` / `R5CTC1J02VB`. After the user-authorized single cold app restart, correctly transformed main-page Deploy reached `[LoadingGate] ready ... playRequested=1 simulationActive=1`. Normal visible UI then accepted Armor roster selection (`selected=2`), Transport roster selection (`selected=1`), and direct world selection of `Unit_Veh_APC_Heavy`. A Contractor preview overlapping occupied terrain visibly reported `BLOCKED BY ROAD OR BLOCKER (870,382) 13X11`, and Cancel was consumed by the parent shell UI rather than clicking through. `Tent_Contractor` resolved with canonical ownership and `350/350`; Recruit delivery spent 25 materials per invocation and increased stable unit/model/source ownership. `focus-delivery-07.png` visibly captures the helicopter with its vertical rope and suspended drop, `day-delivery-05.png` clearly captures the helicopter over the focused Contractor Tent, and `day-delivery-08.png` captures the completed ground spawn. Earlier accepted Contractor and spawned-soldier interaction evidence remains retained. The initial wrong unscaled Skirmish route was rejected and not counted; the later safety pause was procedural because automation explicitly prohibited a cold restart until user approval, not a gameplay, package, or licensing failure. Both generated reports and all `_Recovery` scenes remain unstaged; no reinstall, second restart, protected/production mutation, Jenkins/Unity path change, render-policy change, or `productionCutover=0` change occurred. Blue Autobahn/canal isolation and road-over-water-without-bridge remain separate. |

| 2026-08-07 | `VRP-072` Android-evidence shadow/small-detail policy Editor checkpoint | Checked GUI-licensing inventory `Build/Logs/vrp072-android-visual-policy-inventory.log`; two-pass database determinism `Build/Logs/vrp072-android-visual-policy-database-determinism.log`; focused `Build/Logs/vrp072-android-visual-policy-focused-final-rerun.log` `48/48`; Android visual quality `Build/Logs/vrp072-android-visual-quality-focused.log` `17/17`; two-pass direct bake `Build/Logs/vrp072-android-visual-policy-direct-bake.log`; aggregate `Build/Logs/vrp072-editor-first-functional-batch-rerun.log` `93/93`; `git diff --check` | Editor-first checkpoint accepted; Android package/visual/GPU comparison remains open and checklist progress stays `56 / 80` | The frozen inventory proves all `12,293` virtualized prototype parts are LOD0-only, so no synthetic LOD chain was introduced. The Android-evidence policy moves exactly `12,185` eligible logical part rows to shadow-off buckets while preserving receive-shadows: `5,684` Vegetation, `5,619` Prop, and `882` sub-meter building-detail rows. Large structures remain shadow-on. The regenerated schema-7 inventory retains `76,517` total rows, `61,925` eligible rows, `40,460` placements, `9,107` prototypes, `12,293` parts, and `1,934` cells. Four fixed policies require `7,784` slots, remain below the provisional `8,000` limit, and retain the exact measured-envelope 20% ceiling without VRP-074 tuning. Database content hash `bfb350f0c8d1474aa05252dc04c87eede4c1210adcee9c92dcdbecc35897896e` and ordering hash `a43040ee38b9e8cfe752f1e52848cfa523e453fa2ebcde0cadc4142510d79318` reproduced byte-for-byte; two direct bakes reproduced fingerprint `0e6e4746a45d3c8de4216ce1a94dfc2d85324df4dd51982661fc42987b25acaa`, `61,783` selected physical renderers, `14,017` resident rows, zero production cutover, and protected/production isolation. Initial wrapper launches were rejected before test acceptance by duplicate `Path`/`PATH` and a stale Bee `W:` cache reference; the checked scoped elevated project-only mapping compiled and passed, and every temporary mapping was removed. No APK or Android after-evidence is claimed yet. Blue Autobahn/canal isolation and road-over-water-without-bridge remain separate. The two pre-existing modified generated package reports and all `_Recovery` scenes remain unstaged. |
| 2026-08-07 | `VRP-072` fresh checked profiler package | Pushed implementation head `2c43c1f75df6f6e8d9bcaa3563a9f2d063874fdb`; checked GUI-licensing package `Build/Logs/vrp072-2c43c1f75-profiler-apk-qualified.log` `[DenseCityCandidateAndroidPackage] result=Passed`; exact `Build/AndroidDenseCandidateProfiler/WarlineCapture-DenseCityCandidate-Profiler.apk`, `777,444,713` bytes, SHA-256 `e07e68af43a80fe6c540ac40ea6466285c35802711b04491fb3fc014119876f2` | Package accepted; install and matched Android visual/GPU evidence remain open, so checklist progress stays `56 / 80` | The first launch performed only the required Qualcomm D3D11 reopen and the next shorthand invocation was rejected before build because `BuildScript` now requires its `Game.Editor` namespace. The fully qualified checked invocation built and validated one fresh APK with the candidate EntityScene and zero production EntityScene inclusion. The scoped short `W:` project mapping was removed after the build. Bundled-SDK ADB discovery found the prior Samsung endpoint `192.168.2.33:33675` TCP-offline; two locally started bundled ADB daemons failed their local protocol handshake and were terminated only after executable-path verification. The APK was not installed and no device evidence is claimed. The automation retains bounded discovery/reconnect and authorizes installation only to exact Samsung `SM-S918B` / `R5CTC1J02VB`. The two modified package reports and all `_Recovery` scenes remain preserved and unstaged. |
| 2026-08-07 | `VRP-072` matched Samsung visual/GPU acceptance | Official Google-signed Platform Tools ADB `37.0.1-15733141`; authenticated endpoint `192.168.2.33:43479`; exact Samsung `SM-S918B` / `R5CTC1J02VB`; one explicitly authorized `install --no-streaming -r` of the checked `777,444,713`-byte APK SHA-256 `e07e68af43a80fe6c540ac40ea6466285c35802711b04491fb3fc014119876f2`; push install `Success` in `62.210 s` at `11.9 MB/s`; evidence `Build/AndroidDenseCandidateProfiler/Evidence/2026-08-07-vrp072-shadow-policy` | Android visual/GPU regression gate accepted; `VRP-072` advances the checklist to `57 / 80`; stop before VRP-073 | The fresh package reached the normal main-page Deploy route with `playRequested=1` and `simulationActive=1`. Matched daylight urban captures `13-city-phase-check.png`, `16-urban-matched-adjust.png`, and `17-urban-historical-route.png` preserve complete geometry without gross holes, camera-edge popping, or shadow/material leakage. A fixed gameplay crop measured pitch-black pixels (`max RGB <= 8`) at `10.527%` in historical `2026-08-02_BuildingRoadClearance_Android_Urban.png` versus `4.925%`, `4.323%`, and `4.932%` after the policy, a greater-than-half reduction; higher thresholds reproduced the same conclusion. On-screen base samples were `52-56 FPS`, daylight urban samples `45-56 FPS`, and the metrics-enabled steady camera traversal held `54-55 FPS` after rebuilds without a gross frame/GPU regression. Runtime metrics reported `overflow=0 deficit=0` at initialization (`1178/7784` slots, 50 cells) and through three camera rebuilds ending at command version `4` (`1469/7784` slots, 64 cells, 941 active placements). The retained consolidated Samsung functional evidence remains accepted; no unrelated functional gate was rerun. Large structures/resident exceptions remain shadow-on, LOD0 remains intact, production remains unchanged, and Phase 9's full sustained-60-FPS contract remains open. Blue Autobahn/canal isolation and road-over-water-without-bridge remain separate. The two generated reports and all `_Recovery` scenes remain preserved and unstaged. |
| 2026-08-07 | User-authorized final-performance duration/threshold amendment | Explicit user direction; contract-only change; no implementation, package, or validation claim | Forward-looking Phase 9 acceptance changes to average `>=54 FPS`, 10th-percentile `>=50 FPS`, average frame time `<=18.6 ms`, and a final two-minute thermal route; checklist progress remains `57/80` | The existing p95 `<20 ms`, p99 `<25 ms`, CPU-main, GPU, zero-allocation, zero-overflow, lifecycle, visual, and gameplay-equivalence gates remain unchanged. Representative diagnostics-disabled routes remain 120 seconds; only the final thermal route changes from ten minutes to two. The filename remains stable and historical evidence rows retain the thresholds that applied when they were recorded. This amendment does not itself pass VRP-092 or VRP-097; fresh exact-revision evidence must satisfy the amended values. |
| 2026-08-08 | `VRP-073` exact metric regeneration and packed reconciliation | Checked host-access GUI-licensing inventory `Build/Logs/vrp073-exact-metrics-inventory-host.log`; database determinism `Build/Logs/vrp073-exact-metrics-database-host.log`; real two-pass candidate bake `Build/Logs/vrp073-exact-metrics-direct-bake-host.log`; deterministic report SHA-256 values `777c16ef...2c97`, `31141d55...72e`, and `d33cda10...1df8`; `git diff --check` | Passed; exact current representation/package metrics are frozen and checklist advances to `58 / 80` | Regeneration reconciles `76,517` authored source rows, `61,925` eligible logical rows, `61,783` selected packed physical renderers, `14,017` packed resident physical rows, `40,460` placements, `9,107` prototypes, `12,293` parts, `1,934` occupied cells, four policy buckets, and `7,784` slots. Bucket peak/capacity/headroom is exact: opaque shadow-on `4092/4911/819`, opaque shadow-off `1876/2252/376`, alpha-clipped shadow-on `495/594/99`, and alpha-clipped shadow-off `22/27/5`, each across `14,592` canonical samples. The database reproduced content hash `bfb350f0c8d1474aa05252dc04c87eede4c1210adcee9c92dcdbecc35897896e` and ordering hash `a43040ee38b9e8cfe752f1e52848cfa523e453fa2ebcde0cadc4142510d79318`; two packed bakes reproduced fingerprint `0e6e4746a45d3c8de4216ce1a94dfc2d85324df4dd51982661fc42987b25acaa`, zero packed eligible source rows, and `61,783` removed selected renderers. Sandboxed GUI launches were rejected only because host named-pipe/WMI access was unavailable; the authorized licensing recovery used the verified Editor-bundled `1.18.1` client and the same checked wrappers with host access. No capacity value was tuned, no APK/device acceptance is claimed, and VRP-074 owns any later representative-route change. Production/protected assets, `productionCutover=0`, the blue Autobahn/canal defect, and road-over-water-without-bridge remain unchanged/separate; the two modified generated package reports and all `_Recovery` scenes remain preserved and unstaged. |
| 2026-08-08 | `VRP-074` bounded tactical-follow transition Editor checkpoint | User-authorized scope amendment; exact Samsung before-route evidence under `Build/AndroidDenseCandidateProfiler/Evidence/2026-08-08-vrp074-capacity-route`; checked GUI-licensing focused `Build/Logs/vrp074-tactical-follow-bounded-transition-focused.log` `33/33`; checked aggregate `Build/Logs/vrp074-editor-first-functional-batch-corrected.log` `93/93`; `git diff --check` | Editor-first repair accepted; fresh package and matched Android chained-route acceptance remain open, so progress stays `58 / 80` | Steady max zoom used `2,552/7,784` slots with zero overflow, and isolated tactical follow also stayed within the existing envelope. Only the supported far-view-to-follow chain created a transient `4,853` deficit: camera position eased toward the selected vehicle while the effective look target crossed the full map. Raising capacity would violate VRP-075's `8,000` representative-proxy bound. The bounded repair now initializes a virtual look point on the current view ray and damps it with the same timing as camera position, keeping the transition footprint downward and local while preserving final pose, explicit restore rotation, selection ownership, and all existing capacity values. The new focused regression drives a `900 m` transition for 180 frames and requires a finite bounded ground footprint every frame. No APK or Android after-evidence is claimed yet. Production/protected assets, `productionCutover=0`, both generated reports, all `_Recovery` scenes, blue Autobahn/canal isolation, and road-over-water-without-bridge remain unchanged/separate. |
| 2026-08-08 | `VRP-074` fresh-package matched Samsung chained-route acceptance | Pushed implementation head `a8cbe99c08d5213c3a8cf4f2c449bd1157e9fd21`; checked package `Build/Logs/vrp074-a8cbe99c0-profiler-apk.log`; exact `777,444,713`-byte APK SHA-256 `c63ca8a3d1a9723d43b3c726a5dfdf92abe0ede9e742c093c35a569868c888e6`; one official ADB 37 `install --no-streaming -r` to verified Samsung `SM-S918B` / `R5CTC1J02VB`; after-evidence under `Build/AndroidDenseCandidateProfiler/Evidence/2026-08-08-vrp074-bounded-transition-after` | Accepted; `VRP-074` advances the checklist to `59 / 80` and VRP-075 is dependency-ready | Cold main-page Deploy reached `playRequested=1 simulationActive=1`. Normal visible controls exercised max zoom-out, repeated drag to a remote map district, max zoom-in, Armor roster selection (`selected=2`), and Camera Follow. All five nonempty metrics command versions report `overflow=0 deficit=0`: initialization `1178/7784`, remote traversal `1167`, `1193`, and `1089`, then the repaired far follow `489/7784`. Early, mid, settled, and final captures preserve a finite downward local view with intact presentation and no corridor-wide holes, stale slots, popping, or material leakage. Capacity remains exactly `7,784`; no bucket, cell size, envelope guard, gameplay ownership, production/protected asset, or `productionCutover=0` value changed. The retained 20% measured-envelope headroom therefore remains valid without capacity inflation. Both generated reports and all `_Recovery` scenes remain preserved and unstaged; blue Autobahn/canal isolation and road-over-water-without-bridge remain separate. |
| 2026-08-08 | `VRP-075` exact packed `MaterialMeshInfo` and proxy-limit gate | Rejected raw query `Build/Logs/vrp075-packed-mmi-two-pass.log`; rejected corrected-assumption runs `Build/Logs/vrp075-packed-mmi-two-pass-corrected.log` and `Build/Logs/vrp075-packed-mmi-two-pass-final.log`; accepted checked host-access GUI-licensing two-pass bake `Build/Logs/vrp075-packed-mmi-two-pass-accepted.log`; generated schema-4 report `Design/AgentReports/2026-07-30_dense_city_render_virtualization_pilot_enabled.json`; `git diff --check` | Passed; `VRP-075` advances the checklist to `60 / 80`, completes Phase 7, and makes VRP-080 dependency-ready | The first diagnostic query counted `76,517` baking-world `MaterialMeshInfo` entities, including the `61,783` eligible source-conversion entities intentionally marked `BakingOnlyEntity`; it was rejected as a packed-runtime count. Excluding bake-only entities produced the exact shipped count `14,592`, but an intermediate assertion incorrectly assumed `14,017` resident logical rows plus `7,784` proxy owners were one-to-one render entities, and a second incorrectly assumed proxy ownership and `MaterialMeshInfo` co-locate on the same converted entity. Both failed closed and are not acceptance evidence. The final gate measures the actual non-baking-only `MaterialMeshInfo` archetype directly, requires a positive count no greater than `24,000`, independently requires the existing fixed pool no greater than `8,000`, and reproduces fingerprint `d08de8ef11048a8bbafe34f7f429daf03de89a7ff9625db6fa3db9a530980bac` across two real bakes. Accepted results are `14,592/24,000` shipped render entities and `7,784/8,000` fixed slots; because every enabled proxy is drawn from that fixed pool and matched Samsung VRP-074 peaked at `1,193` with zero overflow/deficit, the representative enabled bound also passes. A restricted-host `PATH`/WMI launcher failure and detached host wrapper were recovered by continuing the same checked GUI-licensing route and monitoring the exact process/log; no wrapper, pass marker, limit, production/protected asset, or `productionCutover=0` value was bypassed or weakened. The two unrelated modified package reports and all `_Recovery` scenes remain preserved and unstaged; blue Autobahn/canal isolation and road-over-water-without-bridge remain separate. |
| 2026-08-08 | `VRP-080` current-source direct logical and packed resident parity | Checked host-access GUI-licensing source gate `Build/Logs/vrp080-current-source-logical-parity.log`; `[OperationMapRenderEligibilityInventoryProbe] result=Passed`; two `[OperationMapRenderDatabaseBuilder] result=Passed`; deterministic bytes/hash pass; `[OperationMapRenderDatabaseBuilder] sourceParity=Passed`; current-head packed gate `Build/Logs/vrp080-current-packed-resident-parity.log`; `[OperationMapRenderVirtualizationCandidateValidation] result=Passed passes=2`; unchanged generated reports; `git diff --check` | Passed; `VRP-080` advances the checklist to `61 / 80` and makes VRP-081 dependency-ready | The live candidate inventory reconciles every `76,517` material/submesh row into exactly `61,925` eligible logical rows or `14,592` named resident exceptions, with no third disposition. The source-derived config then compares every persisted mesh/material identity, prototype/part range, renderer-path identity, submesh, local-to-placement/world matrix, local bounds, linear color, state owner/visual state, semantic category, full render policy, LOD/shadow flag, cell membership, and fixed bucket field by exact identity/index or IEEE-754 bits. Two builds reproduce content hash `bfb350f0...96e`, ordering hash `a43040ee...318`, `36,888,667` config bytes, and `1,100` report bytes. The separate real packed layer reconciles `75,800` renderer owners into `61,783` exact stripped virtualized renderers plus `14,017` unique retained owner/path render-entity mappings and reproduces fingerprint `d08de8ef...0bac` twice. The `717` difference between material/submesh rows and renderer owners is explicitly `142` virtualized plus `575` resident multi-material/submesh expansion, not missing data. No implementation or generated byte changed, so closure is documentation-only; no new APK/device run is required before VRP-081. Production/protected assets, `productionCutover=0`, both unrelated modified package reports, all `_Recovery` scenes, blue Autobahn/canal isolation, and road-over-water-without-bridge remain unchanged/separate. |
| 2026-08-08 | `VRP-081` exhaustive current-data materialized parity | Checked compile gate `Build/Logs/vrp081-materialized-parity-compile-gate-rerun.log`; checked host-access GUI-licensing focused gate `Build/Logs/vrp081-canonical-envelope-materialized-parity-final.log`; generated `Design/AgentReports/2026-08-08_dense_city_render_materialized_parity.json`; `[OperationMapRenderMaterializedParityValidation] result=Passed envelopes=14592 states=2 samples=29184 maxEnabled=4773`; `git diff --check` | Passed; `VRP-081` advances the checklist to `62 / 80` and makes VRP-082 dependency-ready | The validator builds the live `bfb350f0...96e` database and runs the actual production required-cell selection, state-aware placement gather, deterministic retain/release/assignment, and parallel slot-apply jobs for every accepted schema-2 `12 x 9` / `9 x 12` envelope at every grid coordinate in both intact and destroyed states. Every selected logical row has one reciprocal slot binding; every changed slot is checked by IEEE-754 bits for matrix, bounds, and linear color plus exact material/mesh/submesh/bucket/placement/part identity; every released slot is disabled and reset; active accounting equals the selected row count; overflow is zero. Intact peaks at `4,773` slots and destroyed at `4,396`, within the unchanged `7,784` pool; fingerprint is `7674a2c0...0b18`. Rejected diagnostics remain rejected: the whole-map baseline and historical sparse-pilot five-camera route overflow the expanded current database and are not the accepted materialization-capacity contract; two compile-fixture omissions also failed closed before the final checked pass. No product runtime algorithm, capacity, production/protected asset, or `productionCutover=0` value changed. The two unrelated modified package reports and all `_Recovery` scenes remain preserved and unstaged; blue Autobahn/canal isolation and road-over-water-without-bridge remain separate. |

| 2026-08-08 | `VRP-082` current packed readiness, failure/reset/retry, and two-cycle lifecycle | Accepted readiness sync `Build/Logs/vrp082-current-readiness-contract-role-sync-recovered-space.log`; accepted transactional runtime-content rebuild `Build/Logs/vrp082-current-overlap-runtime-content-role-sync-retry.log`; accepted PlayMode `Build/TestResults/vrp082-current-packed-lifecycle-overlap-accepted.xml` `2/2` and `Build/Logs/vrp082-current-packed-lifecycle-overlap-accepted.log` with `[DenseVirtualizedPackedLifecycle] result=Passed cycles=2 ... staleEntities=0` plus `[DenseVirtualizedPackedFailureResetRetry] result=Passed failures=1 resets=1 retries=1 staleEntities=0`; focused `Build/Logs/vrp082-render-virtualization-focused-global.log` `48/48`; aggregate `Build/Logs/vrp082-editor-first-functional-batch-final.log` `93/93`; `git diff --check` | Passed; `VRP-082` advances the checklist to `63 / 80` and makes VRP-083/084/085 dependency-ready | Exact retained/virtualized identity overlap is now baked and subtracted from readiness union counts: `10,288` retained overlaps (`4,530` generated-building, `5,758` generated render-only), yielding current contract counts `9,544` accepted, `36,304` generated, `4,977` buildings, `22` vehicles, and `40,849` render-only. The first real lifecycle XML rejected the stale `5,403/41,065` role contract; the source-derived synchronizer now validates every actual candidate identity and writes all four counts atomically. Rejected follow-ups exposed two test-fixture defects—the legacy default `ResidentEntities` overload and a hard-coded database schema `7`; the accepted test passes `VirtualizedProxyPool` and binds to `OperationMapRenderDatabaseBakeConfig.CurrentSchemaVersion` (`1`). One otherwise-correct retry was rejected only for the exact Unity Editor Relay `Connecting -> Failed / process exited` tooling diagnostic; a strict condition-only exclusion follows the existing editor-tooling precedent and all gameplay errors remain fail-closed. Windows `1224` memory-mapped YAML writes now use bounded cached-handle release/retry. `InvokeUnity.ps1` reads live logs with shared access, detects licensing/test fatal markers early, recognizes current successful test-run text, and retains explicit wrapper-owned timeouts. Licensing recovered through the authorized no-Editor/version-matched checked-wrapper route. A zero-byte system drive was recovered by deleting only verified rebuildable `Library/Bee` and obsolete `vrp054`, `vrp053`, and `vrp002_dense_city.raw` outputs; current package/evidence/source were preserved. The documented focused execute method is corrected to the actual global class. Production/protected assets, pool capacity, render policy, and `productionCutover=0` remain unchanged; unrelated generated reports and all `_Recovery` scenes remain unstaged; blue Autobahn/canal and road-over-water-without-bridge remain separate. |

| 2026-08-08 | `VRP-092` optimized Android frame-pacing correction | Rejected fixed-route evidence `Build/AndroidDenseCandidate/Evidence/2026-08-08-vrp092-gc-fallback-f0d70aff2`; checked focused `Build/Logs/vrp092-optimized-frame-pacing-focused-versioned-mutex-recovery.log` `[AndroidVisualQualityValidation] result=Passed tests=18`; checked GUI-licensing aggregate `Build/Logs/vrp092-optimized-frame-pacing-aggregate-gui.log` passed `93/93`; APH-808 `Build/Logs/vrp092-optimized-frame-pacing-aph808-gui.log` passed; `git diff --check` | Editor-accepted bounded correction; checklist remains `69 / 80` and VRP-092 remains open pending a fresh package and exact-device result | The rejected clean release route renders the intended Match and passes average FPS/frame time plus GPU, allocation, battery-drain, and ordinary-log gates, but fails p10, p95/p99, and CPU with `327` >=25 ms spikes recurring at an exact 12-frame gap. Live Samsung display state is 120 Hz while the player targets 60 FPS; gameplay Update averages about `0.5 ms` and GPU p95 is `12.943 ms`, isolating the dominant cadence from gameplay/ECS and GPU load. The Android player had `androidUseSwappy=0`; it now enables Unity `PlayerSettings.Android.optimizedFramePacing`, whose documented purpose is even Android frame distribution, and an Editor gate prevents regression. The first aggregate attempt is rejected because the Hub client reacquired the generic licensing mutex; the same checked GUI wrapper then passed without bypassing markers. No performance threshold, capacity, render policy, production/protected asset, or `productionCutover=0` value changed. The AC-powered authorization remains in force; the preserved generated report and all `_Recovery` scenes remain unstaged; blue Autobahn/canal and road-over-water-without-bridge remain separate. |

| 2026-08-08 | `VRP-092` frame-pacing package rejection and periodic-barrier correction | Pushed/package head `01f6d5021`; checked clean build `Build/Logs/vrp092-01f6d5021-optimized-frame-pacing-release-apk.log`; exact `543,628,944`-byte APK SHA-256 `7df43ff0dcafc6e246d49349942a27be97d23b493afb4481be6557ab022a7f02`; installed once with official ADB 37 only to verified Samsung `SM-S918B` / `R5CTC1J02VB`; rejected evidence `Build/AndroidDenseCandidate/Evidence/2026-08-08-vrp092-optimized-frame-pacing-01f6d5021`; corrected focused recorder `Build/Logs/vrp092-periodic-barrier-recorder-focused.log` `28/28`; minimap `Build/Logs/vrp092-periodic-barrier-minimap-focused.log` `4/4`; aggregate `Build/Logs/vrp092-periodic-barrier-aggregate.log` `93/93`; APH-808 `Build/Logs/vrp092-periodic-barrier-aph808.log` passed; `git diff --check` | Package rejected; bounded follow-up is Editor-accepted; checklist remains `69 / 80` and the rejected APK will not be reinstalled/reused | Optimized Android pacing improves average to `17.910 ms` / `55.836 FPS` and p10 to `59.934 FPS`, while GPU `12.889/14.433 ms`, zero allocation, battery, visible Match, and ordinary/fatal logs pass; frame p95/p99 `33.331/33.353 ms` still fail. Raw correlation proves `118/120` one-second `Debug.getPss` samples are followed by a >=25 ms frame; this VRP-092 timing route now samples PSS only at capture boundaries, while the independent VRP-094 peak-memory gate remains unchanged. The remaining >=25 ms spike mode is `11-12` frames, matching `MatchHudMinimapMarkerSystem`'s exact `0.2 s` synchronous `state.Dependency.Complete()` barrier; marker collection and deterministic player-priority publication now remain jobified through an asynchronous publish/dispose dependency. Unity's official frame-timing contract defines `cpuFrameTime` as total cadence including waits and `cpuMainThreadFrameTime` as active main-thread work excluding present/VSync/target-FPS wait, so the CPU-main gate now reads the latter without changing frame-time thresholds. No protected/production asset, capacity, render policy, memory acceptance, or `productionCutover=0` value changed. AC-powered testing remains authorized; preserved files and the two map defects remain separate/unstaged. |

| 2026-08-08 | `VRP-092` residual 5 Hz threat-scan candidate prefilter | Pushed/package head `4595370a8`; checked clean package `Build/Logs/vrp092-4595370a8-periodic-barrier-release-apk.log`; exact `543,618,152`-byte APK SHA-256 `3beb35a01c3de3eec2ea67ea34fe184367388e2be5fbccfe69932f4e78ac136a`; one exact-Samsung install; rejected fixed-route evidence `Build/AndroidDenseCandidate/Evidence/2026-08-08-vrp092-periodic-barrier-4595370a8`; focused threat-warning validation `Build/Logs/vrp092-threat-candidate-prefilter-focused-recovery.log` passed; aggregate `Build/Logs/vrp092-threat-candidate-prefilter-aggregate-rerun.log` passed `93/93`; APH-808 `Build/Logs/vrp092-threat-candidate-prefilter-aph808.log` passed; `git diff --check` | Prior package rejected; bounded replacement is Editor-accepted; checklist remains `69 / 80` and a fresh package/exact-device result is required | Removing timed PSS and the minimap completion improves the fixed route to `17.459 ms` / `57.278 FPS`, p10 `59.948 FPS`, frame p95 `16.804 ms`, GPU `12.862/14.397 ms`, zero allocation, zero battery drain, and visible/correct Match, but p99 `33.340 ms` and active CPU-main `13.158/30.213 ms` still fail. All `323` remaining >=25 ms frames are >=33 ms and recur at the exact `0.2 s` cadence of `ThreatDetectionWarningSystem`. That system previously scanned every target chunk once for every player detector and again for every close-contact player sensor, even though soldiers, civilians, buildings, dead units, and friendly units can never be warning targets. One Burst prefilter now builds the exact hostile live air/vehicle candidate list once per scan; detector kind, radius, approach, ETA, close-contact, new-threat suppression, and warning/audio behavior remain unchanged, while each sensor iterates only eligible candidates. A source regression prevents reintroducing full target-chunk traversal inside the per-sensor loop. The first GUI run was rejected on the known stale generic licensing mutex; after verifying no Editor, only the exact stale client was restarted and the same checked GUI wrapper passed. One aggregate command using a nonexistent shorthand class was rejected before validation; the correct checked method then passed. No threshold, route duration, protected/production asset, capacity, render policy, `productionCutover=0`, preserved file, or independent map defect changed. |

| 2026-08-09 | `VRP-092` threat-prefilter near-pass and close-contact sensor cache | Pushed/package head `3328bc9c0`; checked clean release build `Build/Logs/vrp092-3328bc9c0-threat-prefilter-release-apk-versioned-license.log`; exact `543,650,072`-byte APK SHA-256 `fa2b1e0f78b15fad1ad24941cc7ab73c4a4e27ef60b05c62da7f22d43a299dc6`; one exact-Samsung install; rejected fixed-route evidence `Build/AndroidDenseCandidate/Evidence/2026-08-09-vrp092-threat-prefilter-3328bc9c0`; checked focused `Build/Logs/vrp092-threat-close-sensor-cache-focused-host-gui.log` passed; aggregate `Build/Logs/vrp092-threat-close-sensor-cache-aggregate-host-gui.log` passed `93/93`; APH-808 `Build/Logs/vrp092-threat-close-sensor-cache-aph808-host-gui-rerun.log` passed | Prior package rejected by the unchanged CPU-average gate; bounded replacement is Editor-accepted; checklist remains `69 / 80` pending a fresh package/exact-device result | The prefilter removes the 5 Hz spikes: the 120-second route records `7,196` samples, `16.677501 ms` / `59.961 FPS`, p10 `59.978 FPS`, p95/p99 `16.676/16.752 ms`, only two frames at or above `20 ms`, GPU `14.324/14.538 ms`, and zero allocated bytes. It fails closed only because active CPU-main average is `12.010514 ms`, exceeding the unchanged `12.000 ms` gate by `0.010514 ms`; no rounding or threshold change is accepted. The follow-up gathers the same live player close-contact sensors while the existing target pass builds eligible hostile candidates, then reuses that compact sensor list instead of traversing all roughly 5,000 target entities a second time. Close-contact range/type semantics and all warning behavior remain unchanged, and the source regression forbids target-chunk traversal in both per-sensor paths. Restricted GUI licensing hit the known stale generic mutex; the authorized host-access checked GUI wrapper reached the signed-in Hub and passed every required marker. The first APH wrapper used an incorrect expected marker even though the validator logged success; the corrected exact-marker rerun passed. The rejected APK will not be reinstalled or reused. No threshold, route duration, protected/production asset, capacity, render policy, `productionCutover=0`, preserved file, or independent map defect changed. |

| 2026-08-09 | `VRP-092` close-contact cache package rejection and candidate-first exact scan | Pushed/package head `756d8df75`; first full-path build rejected on Windows native-header path length; checked session-local `W:\.` probe `Build/Logs/vrp092-shortpath-dot-probe-versioned-host.log` passed; checked short-path/version-matched build `Build/Logs/vrp092-756d8df75-close-sensor-cache-release-apk-shortpath-versioned.log` passed with zero missing-script matches; exact `543,985,027`-byte APK SHA-256 `c1d73343a4563926815502a6929d8fe6e642bca45a895c73600f69dd9e867974`; one exact-Samsung install; rejected evidence `Build/AndroidDenseCandidate/Evidence/2026-08-09-vrp092-close-sensor-cache-756d8df75`; focused `Build/Logs/vrp092-threat-candidate-inverted-close-focused-host-gui.log` passed; aggregate `Build/Logs/vrp092-threat-candidate-inverted-close-aggregate-host-gui.log` passed `93/93`; recovered APH-808 `Build/Logs/vrp092-threat-candidate-inverted-close-aph808-import-recovery-host-gui.log` passed | Package rejected only by unchanged CPU-average gate; stronger candidate-first replacement is Editor-accepted; checklist remains `69 / 80` | The exact 120-second release route again removes periodic spikes and passes average/p10/frame/GPU/GC/battery/correctness gates: `7,196` samples, `16.677639 ms` / `59.961 FPS`, p10 `59.974 FPS`, p95/p99 `16.678/16.770 ms`, two frames at or above `20 ms`, GPU `14.335/14.541 ms`, zero allocated bytes, zero fatal/overflow/deficit/ordinary-diagnostic matches, thermal status `0`, and visible correct Match. CPU p95 passes at `13.705 ms`, but CPU average `12.021921 ms` exceeds the unchanged `12.000 ms` limit, so the APK is rejected and was stopped. The stronger exact-semantic correction deduplicates live friendly sensor cells, visits each eligible hostile candidate once, stops at the first close sensor for previously detected threats, and for new threats retains the minimum ETA across close sensors before one registration. The first APH-808 run exposed one cold-null `AudioPlaybackPresentationRuntimeView` association after the short-path build; a timestamp-only touch of that exact script produced no git content change and the same checked validator passed. All temporary `W:` mappings were removed. No metric is rounded, no threshold, route duration, protected/production asset, capacity, render policy, `productionCutover=0`, preserved file, or independent map defect changed. |

| 2026-08-09 | `VRP-092` candidate-first package rejection and measurement-overhead correction | Pushed/package head `4db303dfb`; checked build `Build/Logs/vrp092-4db303dfb-candidate-first-close-release-apk-shortpath-versioned.log` passed with zero missing scripts; exact `543,996,087`-byte APK SHA-256 `34612b24b899b3183ac002f2ad3295aeec3d347fa1e7d1f020d4619211aae3fc`; one exact-Samsung install; rejected evidence `Build/AndroidDenseCandidate/Evidence/2026-08-09-vrp092-candidate-first-close-4db303dfb`; checked recorder focused `Build/Logs/vrp092-low-overhead-recorder-focused-host-gui.log` passed `30/30`; aggregate `Build/Logs/vrp092-low-overhead-recorder-aggregate-host-gui.log` passed `93/93`; recovered APH-808 `Build/Logs/vrp092-low-overhead-recorder-aph808-import-recovery-host-gui.log` passed | Package rejected only by unchanged CPU-average gate; bounded recorder-overhead correction is Editor-accepted; checklist remains `69 / 80` | Candidate-first matching keeps the fixed route smooth and correct but confirms the remaining CPU value is a stable measurement floor rather than threat-scan work: `7,196` samples, `16.677604 ms` / `59.961 FPS`, p10 `59.978 FPS`, frame p95/p99 `16.676/16.744 ms`, two frames at or above `20 ms`, CPU `12.031435/13.655 ms`, GPU `14.349/14.543 ms`, zero allocation, zero fatal/overflow/deficit/ordinary-diagnostic matches, and thermal status `0`. The APK fails closed on CPU average only and was stopped. The VRP-092 recorder still samples official CPU/GPU timing and main-thread allocation every frame, but no longer performs four known-unavailable render-counter reads per timed frame; managed-memory evidence moves from two profiler calls every frame to capture boundaries plus 1 Hz because VRP-094 remains the unchanged peak-memory owner. APH-804 retains its per-frame memory and render-counter contract. The same single cold-null audio association recovered through an exact timestamp-only source refresh with zero content diff. No threshold, route duration, CPU/GPU/GC/memory correctness gate, protected/production asset, capacity, render policy, `productionCutover=0`, preserved file, or independent map defect changed. |

| 2026-08-09 | `VRP-092` low-overhead-recorder package rejection and baked-helicopter dependency correction | Pushed/package head `cbf0297c9`; checked build `Build/Logs/vrp092-cbf0297c9-low-overhead-release-apk-shortpath-versioned.log` passed with zero missing scripts; exact `543,998,003`-byte APK SHA-256 `67a207e09b0e6df333816a17a0146a81dc0300a96897a1c08261e974c5ec68ad`; one exact-Samsung install; rejected evidence `Build/AndroidDenseCandidate/Evidence/2026-08-09-vrp092-low-overhead-cbf0297c9`; checked focused `Build/Logs/vrp092-async-baked-helicopter-blades-focused-shortpath-host-versioned-rerun.log` passed `9/9`; corrected aggregate `Build/Logs/vrp092-async-baked-helicopter-blades-aggregate-shortpath-host-versioned-rerun.log` passed `93/93`; checked GUI-licensing APH-808 `Build/Logs/vrp092-async-baked-helicopter-blades-aph808-shortpath-host-gui-final.log` passed | Package rejected only by unchanged CPU-average gate; bounded helicopter correction is Editor-accepted; checklist remains `69 / 80` | The fixed route remains visually correct and passes every unchanged non-CPU-average gate: `7,197` samples over `120.012 s`, `16.675311 ms` / `59.969 FPS`, p10 `59.979 FPS`, frame p95/p99 `16.676/16.761 ms`, two frames at or above `20 ms`, CPU p95 `13.658 ms`, GPU `14.373/14.625 ms`, zero allocation, zero overflow/deficit, zero ordinary diagnostics, and thermal status `0`. CPU average `12.043045 ms` still exceeds `12.000 ms`, so the APK was stopped and will not be reused. The accepted correction removes the canonical baked-blade path's immediate main-thread completion and keeps it in the ECS dependency chain; only legacy no-buffer helicopters retain the direct hierarchy fallback and its explicit safety completion. Canonical aircraft with baked or intentionally empty buffers no longer enter the legacy descendant scan. The first aggregate shorthand was rejected before tests, and two batchmode APH-808 runs passed the validator but were rejected because Unity returned neither a readable exit code nor an explicit shutdown marker; the same unchanged APH-808 gate then passed through the authorized checked GUI-licensing route. A fresh package remains required; no threshold, route duration, correctness gate, protected/production asset, capacity, render policy, `productionCutover=0`, preserved file, or independent map defect changed. |

| 2026-08-09 | `VRP-092` async-helicopter package route acceptance, clean zoom rejection, and mobile overview cap | Pushed/package head `927f2b544`; checked release build `Build/Logs/vrp092-927f2b544-async-helicopter-release-apk-shortpath-host-gui.log` passed with zero missing scripts; exact `543,998,979`-byte APK SHA-256 `378a6b335af8ac2e85c550471ae2b523e315da34fb6e0eb382825a014fbb0bbf`; installed once with official ADB 37 only to verified Samsung `SM-S918B` / `R5CTC1J02VB`; evidence `Build/AndroidDenseCandidate/Evidence/2026-08-09-vrp092-async-helicopter-927f2b544`; focused `Build/Logs/vrp092-mobile-zoom-cap-focused.log` passed `34/34`; corrected aggregate `Build/Logs/vrp092-mobile-zoom-cap-aggregate-corrected.log` passed `93/93`; recovered APH-808 `Build/Logs/vrp092-mobile-zoom-cap-aph808-import-recovery.log` passed; `git diff --check` | Fixed, representative slow-pan, and fast-pan routes are accepted; package is rejected by the unchanged clean zoom GPU/frame gates and will not be reinstalled/reused; bounded mobile correction is Editor-accepted; checklist remains `69 / 80` | Fixed, slow-pan, and fast-pan each completed the exact 120-second recorder contract at about `59.96 FPS`, with frame p95/p99 near `16.68 ms`, passing CPU/GPU, zero-allocation, correctness, capacity, and ordinary-log gates. The first zoom attempt began at thermal status `1` and is explicitly invalid. A clean status-`0` rerun completed `6,687` samples / `120.012 s`: average `17.947 ms` / `55.719 FPS`, p10 `59.940 FPS`, CPU `11.875/14.642 ms`, GPU average `14.087 ms`, zero allocation, and post-route thermal status `0` pass, while frame p95/p99 `33.333/33.356 ms` and GPU p95 `20.261 ms` fail. Timeline isolation proves default/inward segments remain near `16.67 ms`, while the final zoomed-out hold averages `21.129 ms` with `380` frames at or above `25 ms`. Android/mobile camera normalization now caps maximum overview height at `40`, matching the already configured fullscreen overview height instead of exposing the failing `45`-height footprint; desktop behavior and every performance/visual threshold remain unchanged. One malformed aggregate shorthand was rejected before tests. APH-808's single cold-null `AudioPlaybackPresentationRuntimeView` association recovered through an exact timestamp-only source refresh with identical SHA-256 and no content diff. A fresh package and clean route continuation are required. No protected/production asset, pool capacity, render policy, `productionCutover=0`, preserved file, or independent map defect changed. |

| 2026-08-09 | `VRP-092` mobile-overview package route continuation and fullscreen single-owner correction | Pushed/package predecessor `c48ecb3c7`; checked release build `Build/Logs/vrp092-mobile-zoom-cap-release-apk.log`; exact `543,998,951`-byte APK SHA-256 `40085348fb49dd60956156b1e40a0b431a3d7120b6b06a10730a6952c1d320cc`; installed once with official ADB 37 only to verified Samsung `SM-S918B` / `R5CTC1J02VB`; evidence `Build/AndroidDenseCandidate/Evidence/2026-08-09-vrp092-mobile-zoom-cap-c48ecb3c`; pushed replacement implementation `327af8eb7`; focused `Build/Logs/vrp092-fullscreen-single-owner-focused-rerun.log` passed `19/19`; corrected aggregate `Build/Logs/vrp092-fullscreen-single-owner-aggregate-rerun.log` passed `93/93`; recovered APH-808 `Build/Logs/vrp092-fullscreen-single-owner-aph808-after-bounded-touch.log` passed; `git diff --check` | Zoom, tactical-follow, and canonical selected-building destruction routes are accepted; fullscreen-map remains rejected by unchanged CPU-average/thermal gates; bounded single-owner correction is Editor-accepted; checklist remains `69 / 80` pending a fresh package and exact-device result | Clean zoom passes at `59.960 FPS`, frame p95/p99 `16.678/16.831 ms`, CPU `11.694/13.407 ms`, GPU `11.994/13.807 ms`, and zero allocation. Tactical-follow passes at `59.969 FPS`, CPU `11.508/13.212 ms`, GPU `13.409/13.739 ms`, zero allocation, and visible selected Armor follow. The accepted destruction interaction visibly selects a canonical Refugee Tent and the second command clears its panel through the real Destroy control; its route passes at `59.960 FPS`, CPU `11.812/13.495 ms`, GPU `12.562/12.674 ms`, and zero allocation. Two invalid destruction input probes remain explicitly rejected. The first fullscreen route passed FPS/frame/GPU/allocation but rejected CPU average `12.331 ms` and ended thermal status `1`; a cooled status-`0` rerun improved CPU average to `12.193 ms` but still exceeded the exact `12.000 ms` limit and again ended status `1`. Runtime inspection proved both the hidden compact minimap and visible fullscreen map kept updating while the popup was open. The correction suspends only the obscured compact update while fullscreen owns presentation; its timer remains due so compact refresh resumes immediately after close. Fullscreen visuals, markers, viewport input, cadence, every metric threshold, protected/production assets, capacity, render policy, and `productionCutover=0` remain unchanged. The first focused GUI attempt failed closed on the known stale licensing mutex; after its checked wrapper cleanup was denied by Windows, only that exact expired wrapper-owned Unity tree was stopped, no Editor was active, and only the path/command-line/start-time-verified stale licensing client was restarted before the identical wrapper passed. One aggregate shorthand class was rejected before tests. APH-808's exact `46` scoped MonoScript associations recovered through timestamp-only refresh with zero content change. The preserved reports/recovery scenes remain unstaged, and both independent map defects remain separate. |

| 2026-08-09 | `VRP-092` representative-route acceptance on fullscreen single-owner package | Pushed source/documentation head `05a969892`; checked release build `Build/Logs/vrp092-05a969892-fullscreen-single-owner-release-apk.log` passed with zero missing-script matches; exact `543,998,651`-byte APK SHA-256 `e4e239486de3ecb8900e07ea1fdc117292d9ef7ca5a6570243360ca99fdcfb1e`; installed exactly once with official ADB 37 only to verified Samsung `SM-S918B` / `R5CTC1J02VB`; evidence `Build/AndroidDenseCandidate/Evidence/2026-08-09-vrp092-fullscreen-single-owner-05a969892`; retained accepted predecessor evidence for fixed/slow-pan/fast-pan and zoom/tactical-follow/destruction | Accepted; checklist advances to `70 / 80`; VRP-093 is next | The corrected same-package fullscreen-map route completed `7,183` samples / `120.014 s` at `16.708 ms` / `59.852 FPS`, p10 `59.976 FPS`, frame p95/p99 `16.676/16.757 ms`, CPU `11.561/13.308 ms`, GPU `14.297/14.517 ms`, zero allocation, zero failure matches, and thermal status `0 -> 0`. Its visible start/end captures retain the full tactical map, markers, and viewport without white screen or UI loss. The same package's steady route completed `7,196` samples / `120.012 s` at `16.678 ms` / `59.961 FPS`, p10 `59.978 FPS`, frame p95/p99 `16.676/16.746 ms`, CPU `11.277/12.868 ms`, GPU `13.873/14.032 ms`, zero allocation, zero failure matches, and thermal status `0 -> 0`; start/end captures retain stable dense geometry, HUD, roster, command bar, and minimap without holes, leakage, or popping. The bounded fullscreen fix cannot execute while the popup is closed, so the already accepted fixed/slow-pan/fast-pan package routes and mobile-cap zoom/tactical-follow/destruction package routes remain valid and together cover every representative route. No gate, route duration, protected/production asset, capacity, render policy, `productionCutover=0`, preserved file, or independent map defect changed. |

| 2026-08-09 | `VRP-093` zero-allocation/capacity/rebuild/no-sync acceptance | Exact-device accepted VRP-092 route JSON/log evidence; checked GUI logs `Build/Logs/vrp093-initialization-bounded-rebuild-final.log` `9/9`, `Build/Logs/vrp093-proxy-apply-no-sync-final.log` `8/8`, and `Build/Logs/vrp093-state-sync-pure-gate-final.log` `8/8`; aggregate `Build/Logs/vrp093-editor-first-functional-batch-corrected.log` passed `93/93`; APH-808 `Build/Logs/vrp093-aph808.log` passed; pushed test-seam head `b7fc940e1`; source completion audit; `git diff --check` | Accepted; checklist advances to `71 / 80`; VRP-094 is next | Every accepted Android route records allocation source `main-thread-allocated-bytes`, `0` total bytes, and `0 B/frame`; route failure scans contain zero overflow/deficit/fatal matches, consistent with the accepted `7,784`-slot capacity sweep and traversal evidence. Initialization tests prove first/map-generation rebuilds, one camera publication followed by stable-envelope no-op, exact bounded native state, and disposal. Apply tests prove unchanged command versions schedule no job, allocate `0 B`, write nothing, and leave rebuild count unchanged; jobs remain ordered through `state.Dependency`. State-sync tests prove visible/off-camera state changes and deterministic recycling while the negative pure gate fails before mutation. The only four `.Complete()` calls in `OperationMapRender*` production sources are guarded initialization replacement or owner-removal/system-destroy disposal boundaries; no `Schedule(...).Complete()` exists in the recurring virtualization path. The state-sync test seam changed only the pure gate from `private` to `internal` because `SystemHandle.Update` logs system exceptions instead of rethrowing them to execute-method tests. The first direct runtime negative assertion and its `LogAssert` execute-method replacement remain rejected; the pure-gate rerun passed. A separate broad ECS architecture ratchet remains rejected on 13 pre-existing array snapshots across five managed helper files and was not weakened or used for this bounded acceptance. No production behavior, parent row, gate, protected/production asset, `productionCutover=0`, preserved file, or independent map defect changed. |

| 2026-08-09 | `VRP-094` exact Phase 0/current-candidate memory comparison | Frozen report `Design/AgentReports/2026-07-21_dense_city_phase0_android_baseline.json`; current report `Design/AgentReports/2026-08-09_dense_city_vrp094_android_memory_comparison.json`; accepted installed APK SHA-256 `e4e239486de3ecb8900e07ea1fdc117292d9ef7ca5a6570243360ca99fdcfb1e`; exact Samsung `SM-S918B` / `R5CTC1J02VB`; corrected evidence `Build/AndroidDenseCandidate/Evidence/2026-08-09-vrp094-memory-b7f90349c-rerun`; Android `dumpsys meminfo`; fresh 120-second structured recorder; retained screenshot/logcat | Accepted; checklist advances to `72 / 80`; VRP-095 is next | At route readiness the current package used `2,128,729 KiB` PSS, `2,219,812 KiB` RSS, `901,472 KiB` graphics, and `64,732 KiB` native heap. After the full route it retained `2,140,028 KiB` PSS, `2,232,260 KiB` RSS, `901,816 KiB` graphics, and `65,584 KiB` native heap; the recorder observed peak allocated/Mono/RSS of `904.107/26.523/2,090.092 MiB`. Against the exact frozen Phase 0 steady PSS and match RSS/graphics/native artifacts, retained current PSS is `352,192 KiB` (`14.132%`) lower, RSS `394,688 KiB` (`15.025%`) lower, graphics `194,716 KiB` (`17.757%`) lower, and native heap `2,124 KiB` (`3.347%`) higher. The current route completed `7,199` samples / `120.007 s` at `16.670 ms` average, `16.670/16.734 ms` p95/p99, and `0 B/frame`; the retained screen is complete with no white screen, gross hole, or UI loss. The first launch probe is rejected because its activity arguments were malformed; only the corrected rerun is accepted. The frozen baseline Xiaomi and current Samsung differ, so these are exact artifact deltas rather than identical-device causal attribution. No reinstall, unplugged test, parent row, threshold, protected/production asset, `productionCutover=0`, preserved file, or independent map defect changed. |

| 2026-08-09 | `VRP-095` exact Android state-scenario diagnostics checkpoint | Final checked GUI focused `Build/Logs/vrp095-state-scenario-focused-final.log` passed `32/32`; final aggregate `Build/Logs/vrp095-state-scenario-aggregate-final.log` passed `93/93`; APH-808 `Build/Logs/vrp095-state-scenario-aph808.log` passed; `git diff --check` | Editor-accepted diagnostic implementation; checklist remains `72 / 80` pending a fresh checked package and exact-device result | The opt-in release diagnostic selects three distant intact virtualized buildings with identical intact/destroyed bucket masks. It centers the real gameplay camera, records applied proxy-slot ownership, destroys the first while visible, travels far enough to prove its intact slots are rebound to a second building, destroys the third while it has zero assigned slots, verifies that canonical destroyed state advances without disturbing the visible second building, travels to the third and requires only its destroyed recipe, then returns to the first and requires its destroyed recipe to reproduce the same deterministic slot set. Every terminal state additionally requires positive materialization and zero overflow/deficit; failure retains the exact phase and a bounded wait prevents terminal metric readiness from stalling silently. The flag is exact, case-insensitive, opt-in, and independent of performance recording; it changes no ordinary runtime behavior. Android acceptance, screenshots, and logs remain open until this pushed source is packaged and installed once on the exact Samsung. No gate, route duration, parent row, protected/production asset, capacity, render policy, `productionCutover=0`, preserved file, or independent map defect changed. |

| 2026-08-09 | `VRP-095` fresh checked Android package | Pushed package head `28d2595b2`; rejected full-path build `Build/Logs/vrp095-28d2595b2-release-candidate-apk.log`; accepted checked short-path build `Build/Logs/vrp095-28d2595b2-release-candidate-apk-shortpath.log` with `[DenseCityCandidateAndroidPackage] result=Passed`; exact `Build/AndroidDenseCandidate/WarlineCapture-DenseCityCandidate.apk`, `543,993,843` bytes, SHA-256 `246d19ee83d5619f8bc270818128d4c771bca395ada5bf873a55a2f1d886f2a7` | Package accepted; checklist remains `72 / 80` pending one exact-Samsung install and state-scenario result | The first checked build failed closed because the retained Bee graph referenced its previously accepted `W:\Library` project root while no mapping existed. After confirming no Editor owned the project, the identical checked GUI-licensing build ran through a verified project-only `W:` mapping, compiled the new diagnostics, passed candidate EntityScene/production-isolation ownership, and removed the mapping immediately after success. The APK has not been installed. Official ADB 37 discovery first found a stale `_adb-tls-connect` advertisement at `192.168.2.33:33675`, but every known Samsung port refused TCP and subsequent isolated discovery advertised no service; each newly started path-verified isolated ADB server was stopped. Device acceptance therefore waits for a new phone-side wireless-debugging port and will issue at most one install to exact Samsung `SM-S918B` / `R5CTC1J02VB`. No other device, reinstall, gate, parent row, protected/production asset, `productionCutover=0`, preserved file, or independent map defect changed. |

| 2026-08-09 | `VRP-095` rejected first Android scenario and actual recycled-owner correction | Official ADB 37 authenticated `192.168.2.33:39713` as Samsung `SM-S918B` / `R5CTC1J02VB`; the exact `543,993,843`-byte APK installed once with `--no-streaming -r`; rejected evidence `Build/AndroidDenseCandidate/Evidence/2026-08-09-vrp095-state-scenario-28d2595b2-rerun`; corrected checked focused `Build/Logs/vrp095-recycled-owner-focused-host-gui.log` `32/32`; aggregate `Build/Logs/vrp095-recycled-owner-aggregate-host-gui.log` `93/93`; rejected stale-import APH-808 plus accepted `Build/Logs/vrp095-recycled-owner-aph808-after-bounded-touch.log`; `git diff --check` | Installed package is rejected and will not be reinstalled; corrected opt-in diagnostic is Editor-accepted; checklist remains `72 / 80` pending one fresh checked replacement package and exact-device pass | Android proved the visible building transition itself: owner `0` materialized `12` intact slots, then only its one destroyed slot, with normal Match HUD and no white screen. The run failed closed at `VerifyRecycle` because the preselected travel-anchor building owned none of those released physical IDs; that is a diagnostic selection error, not evidence that the slots remained stale. The correction keeps the exact physical-slot-reuse gate: after camera travel it resolves the active intact canonical building that actually received the released IDs, chooses the largest overlap with a stable lowest-state-owner tie break, and uses that real owner for the subsequent no-disturbance/state-leak checks. Exact visible destruction, off-camera zero materialization, destroyed-only arrival, deterministic destroyed return, overflow/deficit, and bounded-wait gates remain unchanged. GUI and version-matched sandboxed attempts failed closed because Hub `3.20.0` still supplied licensing client `1.17.4` while the Editor requested LocalIPC `1.18.1`, and sandboxed hardware identity access was denied; the checked host-access GUI wrapper then compiled and passed without changing paths or markers. Official [Unity license troubleshooting](https://docs.unity3d.com/6000.0/Documentation/Manual/ActivationFAQ.html) and [Unity 6000.5.0f1 release notes](https://unity.com/releases/editor/whats-new/6000.5.0f1) were reviewed. APH-808's first host run found the known stale `Menu.unity` MonoScript association; timestamp-only refresh of `AudioPlaybackPresentationRuntimeView.cs` kept source/meta SHA-256 unchanged and the identical rerun passed. No production behavior, gate threshold, protected/production asset, capacity, render policy, `productionCutover=0`, preserved worktree file, or independent map defect changed. |

| 2026-08-09 | `VRP-095` replacement-package rejection and stable recycled-owner follow-up | Pushed package source `0aa70a8d7`; checked build `Build/Logs/vrp095-0aa70a8d7-replacement-release-apk.log` passed; exact `544,008,871`-byte APK SHA-256 `733abd36adcca889322de1d05ad4f206caec7def0eabb106af5f758523987acc`; installed once with official ADB 37 only to verified Samsung `SM-S918B` / `R5CTC1J02VB`; rejected evidence `Build/AndroidDenseCandidate/Evidence/2026-08-09-vrp095-recycled-owner-0aa70a8d7-rerun2` | Package is rejected and will not be reinstalled; checklist remains `72 / 80`; stable-owner correction requires checked Editor validation and a fresh package | Android passes visible intact-to-destroyed replacement and exact physical reuse: owner `3383` received two released intact slot IDs. It then fails closed because that recipient was a one-frame transient and no longer materialized when the off-camera destruction advanced. Two preceding controller attempts never launched the app because PowerShell stripped the quoted Unity argument string; they are rejected controller evidence, not package runs. The follow-up keeps exact physical reuse and adds a full `60`-frame unchanged-slot stability requirement before triggering the off-camera destruction; if the transient owner disappears, the diagnostic re-resolves the actual current recycled owner instead of claiming a state leak. All canonical-destruction, zero-off-camera-materialization, no-disturbance, destroyed-only arrival, deterministic return, overflow/deficit, and bounded-wait gates remain unchanged. No production behavior, threshold, protected/production asset, capacity, `productionCutover=0`, preserved file, or independent map defect changed. |

| 2026-08-09 | `VRP-095` stable recycled-owner Editor acceptance | Rejected compile log `Build/Logs/vrp095-stable-recycled-owner-focused.log`; accepted focused `Build/Logs/vrp095-stable-recycled-owner-focused-rerun.log` `32/32`; aggregate `Build/Logs/vrp095-stable-recycled-owner-aggregate.log` `93/93`; APH-808 `Build/Logs/vrp095-stable-recycled-owner-aph808.log` passed; `git diff --check` | Editor-accepted; checklist remains `72 / 80` pending one fresh checked package and exact-device pass | The first focused wrapper failed closed on a local-name collision and reached its explicit `900`-second timeout; the wrapper terminated only its own verified Unity tree. After ownership cleared, the new local was renamed from `recycle` to `stableRecycle` with no behavioral change. The identical focused gate and unchanged aggregate/serialized-reference gates then passed. The diagnostic now requires the same actual recycled owner and exact physical slot set to remain intact for `60` frames before off-camera destruction, while a transient recipient returns to deterministic actual-owner resolution. No ordinary runtime behavior, threshold, protected/production asset, capacity, `productionCutover=0`, preserved file, or independent map defect changed. |

| 2026-08-09 | `VRP-095` stable-owner package rejection and bounded recipient wait | Pushed/package head `29ad0dcaf`; checked build `Build/Logs/vrp095-29ad0dcaf-stable-owner-release-apk.log`; exact `544,009,963`-byte APK SHA-256 `a4bd9d53abf6e1352df147bac785267b174a261acec862323b5506cacf46510e`; installed once only to verified Samsung; rejected evidence `Build/AndroidDenseCandidate/Evidence/2026-08-09-vrp095-stable-owner-29ad0dcaf` | Rejected APK will not be reinstalled; checklist remains `72 / 80`; bounded recipient-wait correction is implementation-open | The visible owner again passes exact intact-to-destroyed replacement. After camera travel, the released slot IDs briefly belonged to no active building, so immediate recipient resolution failed before the new stability gate could run. The correction replaces that immediate rejection with the existing fail-closed `240`-frame bound: it waits for an intact travel anchor and an active building that actually owns positive exact overlap, then resets the wait and still requires `60` unchanged stable frames before off-camera destruction. No zero-overlap state can pass, and every later canonical-state, no-materialization, no-disturbance, deterministic-return, overflow/deficit, and terminal metric assertion remains unchanged. No production behavior, threshold, protected/production asset, capacity, `productionCutover=0`, preserved file, or independent map defect changed. |

| 2026-08-09 | `VRP-095` bounded recipient-wait Editor acceptance | Focused `Build/Logs/vrp095-bounded-recipient-wait-focused.log` `32/32`; aggregate `Build/Logs/vrp095-bounded-recipient-wait-aggregate.log` `93/93`; rejected cold-null APH-808 plus accepted `Build/Logs/vrp095-bounded-recipient-wait-aph808-after-bounded-touch.log`; `git diff --check` | Editor-accepted; checklist remains `72 / 80` pending a fresh checked package and exact-device pass | The diagnostic waits within the existing `240`-frame fail-closed bound for positive exact released-slot overlap instead of rejecting a normal transient no-recipient interval, then requires the same owner and exact slot set for `60` frames. The first APH-808 run found the known stale `Menu.unity` association; timestamp-only refresh of `AudioPlaybackPresentationRuntimeView.cs` preserved source/meta SHA-256 and the identical rerun passed. No ordinary runtime behavior, threshold, exact-reuse assertion, protected/production asset, capacity, `productionCutover=0`, preserved file, or independent map defect changed. |

| 2026-08-09 | `VRP-095` bounded-wait package rejection and anchor-independent actual-owner resolution | Pushed/package head `b004bb717`; checked build `Build/Logs/vrp095-b004bb717-bounded-recipient-release-apk.log`; exact `544,014,699`-byte APK SHA-256 `ae0831cb8d86a285da9eeba26bdc08c1de553034d6c8686b598532b193f9cfe1`; installed once only to verified Samsung; rejected evidence `Build/AndroidDenseCandidate/Evidence/2026-08-09-vrp095-bounded-recipient-b004bb717` | Rejected APK will not be reinstalled; checklist remains `72 / 80`; anchor-independent resolution is implementation-open | The visible intact-to-destroyed transition passes, but the chosen travel-anchor building never materializes within the existing `240`-frame bound. That anchor exists only to move the camera and is not the exact-reuse subject; nearby active owners can receive the released physical IDs. The correction removes only the anchor-materialization prerequisite: direct overlap is used when the anchor is intact, otherwise the same deterministic slot scan waits for an active intact owner with positive exact overlap. The `240`-frame recipient bound, `60`-frame unchanged-owner/slot stability gate, and every later canonical-state, no-materialization, no-disturbance, deterministic-return, overflow/deficit, and terminal metric assertion remain unchanged. No production behavior, threshold, protected/production asset, capacity, `productionCutover=0`, preserved file, or independent map defect changed. |

| 2026-08-09 | `VRP-095` anchor-independent actual-owner Editor acceptance | Focused `Build/Logs/vrp095-anchor-independent-owner-focused.log` `32/32`; aggregate `Build/Logs/vrp095-anchor-independent-owner-aggregate.log` `93/93`; rejected cold-null APH-808 plus accepted `Build/Logs/vrp095-anchor-independent-owner-aph808-after-bounded-touch.log`; `git diff --check` | Editor-accepted; checklist remains `72 / 80` pending fresh checked package/exact-device pass | The camera anchor is now optional for slot ownership: when it is intact its direct overlap is considered, otherwise the deterministic active-owner scan waits within the existing bound for positive exact released-slot overlap. The same selected actual owner must still retain the exact slot set intact for `60` frames. The known cold-null audio association recovered through timestamp-only exact-script refresh with unchanged source/meta hashes. No production behavior, threshold, exact-reuse/canonical-state gate, protected/production asset, capacity, `productionCutover=0`, preserved file, or independent map defect changed. |

| 2026-08-09 | `VRP-095` anchor-independent package rejection and cross-owner destroyed-slot semantics correction | Pushed/package head `7d2c3f5fd`; checked build `Build/Logs/vrp095-7d2c3f5fd-anchor-independent-release-apk.log`; exact `544,012,763`-byte APK SHA-256 `e9d11bc46d7040a48d4c78f4a65d38016f206f61db06713a4be52a1647da667b`; installed once only to verified Samsung; rejected evidence `Build/AndroidDenseCandidate/Evidence/2026-08-09-vrp095-anchor-independent-7d2c3f5fd` | Rejected APK will not be reinstalled; checklist remains `72 / 80`; destroyed-slot semantics correction is implementation-open | Android passes visible intact-to-destroyed replacement, exact intact physical-slot reassignment, transient-owner re-resolution, `60` stable frames, off-camera canonical destruction with no materialization/disturbance, and destroyed-only materialization on arrival. It rejects only because the third destroyed owner receives a different free destroyed-pool physical ID than the first owner. That cross-owner ID-equality assumption is outside the checklist and contradicts accepted VRP-086 semantics, where exact physical IDs can differ under legitimate fixed-pool recycling. The correction removes only that assertion. Positive exact intact cross-owner reuse remains required; the third owner must still materialize positive destroyed-only slots; returning to the first owner must still reproduce its exact deterministic destroyed slot set; all overflow/deficit and terminal metric gates remain unchanged. No production behavior, threshold, protected/production asset, capacity, `productionCutover=0`, preserved file, or independent map defect changed. |

| 2026-08-09 | `VRP-095` destroyed-slot semantics Editor acceptance and Windows GUI-licensing recovery | Focused `Build/Logs/vrp095-destroyed-slot-semantics-focused.log` `32/32`; aggregate `Build/Logs/vrp095-destroyed-slot-semantics-aggregate-atomic-version-match.log` `93/93`; rejected cold-null `Build/Logs/vrp095-destroyed-slot-semantics-aph808-atomic-version-match.log` plus accepted `Build/Logs/vrp095-destroyed-slot-semantics-aph808-after-bounded-touch.log`; `git diff --check` | Editor-accepted; checklist remains `72 / 80` pending a fresh checked package and exact-device pass | The only diagnostic change removes the cross-owner destroyed physical-slot equality assertion; positive destroyed-only materialization and exact same-owner return remain fail-closed. Hub `3.20.0` repeatedly reclaimed the generic `Unity-LicenseClient-zfoul` pipe with client `1.17.4`, while Editor `6000.5.2f1` requires LocalIPC/client `1.18.1`. Two rejected GUI-wrapper attempts failed before tests with `Licensing initialization failed after 74.82s`; their exact wrapper-owned orphan Editor trees were path/command-line verified and cleaned without touching Hub or unrelated processes. The accepted recovery verified no Editor, stopped only the generic `1.17.4` client, and launched the checked GUI wrapper in the same bounded command so the Editor-bundled `1.18.1` client won the pipe race. APH-808's known `Menu.unity :: GameUIRoot[0]` cold-null association recovered through a timestamp-only exact-script refresh; source SHA-256 `A89400D0A14C2ABB629C62E9CB56888606542C370ED37EBD728E5E965A7EC3A3` and meta SHA-256 `2F5121BD6E6CADC7BD0457EEB439E778657E0ECBFCDBFA8FF90C6AEE3887174D` remained unchanged. No threshold, production behavior, protected/production asset, capacity, `productionCutover=0`, preserved file, or independent map defect changed. |

| 2026-08-09 | `VRP-095` `d749e8675` package rejection and intact cross-owner slot-semantics correction | Checked build `Build/Logs/vrp095-d749e8675-destroyed-slot-semantics-shortpath-gui-android-apk.log`; exact `544,013,547`-byte APK SHA-256 `08e07807a1b34ee5b930f94a0b27b670f35cbc20b346a3205e26919415e6637a`; installed once with official ADB 37 and `--no-streaming -r` only to verified Samsung `SM-S918B` / `R5CTC1J02VB`; rejected evidence `Build/AndroidDenseCandidate/Evidence/2026-08-09-vrp095-destroyed-slot-semantics-d749e8675` | Rejected APK will not be reinstalled; checklist remains `72 / 80`; corrected diagnostic requires fresh Editor/package/device acceptance | Android again passes visible owner `0` materializing `12` intact slots and then only its one destroyed slot. It rejects at `VerifyRecycle` solely because the active intact travel owner uses a compatible fixed-pool allocation without sharing the first owner's exact numeric physical slot IDs. That cross-owner equality is not a VRP-095 requirement and contradicts accepted VRP-086 fixed-pool semantics. The correction accepts any positive intact materialization at the travel anchor, records released-slot overlap only as diagnostics, retains deterministic exact-overlap owner resolution as a fallback when that anchor is absent, and requires the selected same owner and exact slot set to remain stable for `60` frames. Destroyed-only transitions, off-camera zero materialization/no disturbance, exact same-owner destroyed return, compatible bucket masks, bounded waits, and zero overflow/deficit remain fail-closed. No production behavior, threshold, protected/production asset, capacity, `productionCutover=0`, preserved file, or independent map defect changed. |

| 2026-08-09 | `VRP-095` intact cross-owner slot-semantics Editor acceptance | Focused `Build/Logs/vrp095-intact-cross-owner-semantics-focused.log` `32/32`; aggregate `Build/Logs/vrp095-intact-cross-owner-semantics-aggregate.log` `93/93`; rejected cold-null APH-808 plus accepted `Build/Logs/vrp095-intact-cross-owner-semantics-aph808-after-bounded-touch.log`; `git diff --check` | Editor-accepted; checklist remains `72 / 80` pending a fresh checked package and exact-device pass | The diagnostic now treats positive intact materialization and same-owner stability as the acceptance contract instead of requiring unrelated owners to share numeric slot IDs. The exact-overlap scan remains available when the travel anchor is absent. The same selected owner must retain its exact slot set for `60` frames, and all canonical destruction, off-camera, deterministic-return, bounded-wait, overflow, and deficit assertions remain unchanged. APH-808's known `Menu.unity :: GameUIRoot[0]` cold-null association recovered through timestamp-only refresh of `AudioPlaybackPresentationRuntimeView.cs`; source/meta SHA-256 remained `A89400D0A14C2ABB629C62E9CB56888606542C370ED37EBD728E5E965A7EC3A3` / `2F5121BD6E6CADC7BD0457EEB439E778657E0ECBFCDBFA8FF90C6AEE3887174D`. No threshold, production behavior, protected/production asset, capacity, `productionCutover=0`, preserved file, or independent map defect changed. |

| 2026-08-09 | `VRP-095` `4498791a3` package rejection and same-owner logical-return correction | Checked build `Build/Logs/vrp095-4498791a3-intact-slot-semantics-shortpath-gui-android-apk.log`; exact `544,012,567`-byte APK SHA-256 `54e2ca1a00aa5a7c263244ad27c99f58a5ed43f83fbbfe949b348cd2a2bd354f`; installed once with official ADB 37 and `--no-streaming -r` only to verified Samsung; rejected evidence `Build/AndroidDenseCandidate/Evidence/2026-08-09-vrp095-intact-slot-semantics-4498791a3` | Rejected APK will not be reinstalled; checklist remains `72 / 80`; logical-return correction is implementation-open | Android passes visible owner `0` intact-to-destroyed replacement, positive compatible intact materialization for owner `2553`, `60` stable frames, off-camera canonical destruction with zero materialization/no disturbance, and destroyed-only materialization for owner `4084`. It fails only because returning owner `0` receives a different free destroyed-pool physical ID after intervening camera allocations. Physical slot numbers are disposable presentation storage, not owner identity. The correction still requires the same canonical owner to return positively, requires every returned part to be destroyed-only, and requires its complete destroyed recipe to reproduce the original part count; only numeric physical-ID equality is removed. Bounded waits and terminal zero overflow/deficit remain fail-closed. No production behavior, threshold, protected/production asset, capacity, `productionCutover=0`, preserved file, or independent map defect changed. |

| 2026-08-09 | `VRP-095` same-owner logical-return Editor acceptance | Rejected GUI licensing-start log `Build/Logs/vrp095-logical-return-semantics-focused.log`; accepted focused version-matched `Build/Logs/vrp095-logical-return-semantics-focused-version-match.log` `32/32`; aggregate `Build/Logs/vrp095-logical-return-semantics-aggregate.log` `93/93`; rejected cold-null and two pass-marker/shutdown-detached APH-808 runs; accepted GUI `Build/Logs/vrp095-logical-return-semantics-aph808-gui-final.log`; `git diff --check` | Editor-accepted; checklist remains `72 / 80` pending fresh package/exact-device pass | The final-return gate now validates canonical owner identity, positive destroyed-only materialization, and exact destroyed-recipe part-count completeness without treating disposable physical slot indices as owner identity. The first GUI focused run lost the Hub generic-client pipe race; the documented version-matched wrapper fallback passed. APH-808 first exposed the known cold-null association; timestamp-only refresh preserved exact source/meta hashes. Two subsequent APH executions produced the validator pass marker but were rejected because batchmode detached during shutdown; the checked GUI route then produced both the pass marker and acceptable wrapper exit. No production behavior, threshold, protected/production asset, capacity, `productionCutover=0`, preserved file, or independent map defect changed. |

| 2026-08-09 | `VRP-095` exact Samsung destruction/recycling acceptance | Pushed/package head `f6accf53e`; checked build `Build/Logs/vrp095-f6accf53e-logical-return-shortpath-gui-android-apk.log`; exact `544,011,811`-byte APK SHA-256 `65a4a57c8014ff99a06151a6adb2b4046a7304bfb1f1b817ad49c764128b452d`; one official-ADB-37 `--no-streaming -r` install to verified Samsung `SM-S918B` / `R5CTC1J02VB`; evidence `Build/AndroidDenseCandidate/Evidence/2026-08-09-vrp095-logical-return-f6accf53e` | Passed; checklist advances to `73 / 80`; VRP-096 is next | The exact device passes visible owner `0` at `12` intact slots and then one destroyed-only slot; owner `2553` remains intact on `8` slots for `60` frames; owner `4084` advances canonical destruction while off camera with zero materialization and no disturbance, then materializes only its destroyed recipe; owner `0` returns positively with its complete destroyed recipe. Terminal metrics report `2,779` enabled slots, `68` active cells, `1,480` active placements, `overflow=0`, `deficit=0`, and envelope `50,18:58,25`. Phase screenshots retain the live Match HUD and city geometry without white screen, gross holes, stale state, or UI loss; existing residual dark shadow patches are unchanged and are not claimed as a new visual-quality result. No reinstall, performance rerun, threshold, protected/production asset, capacity, `productionCutover=0`, preserved file, or independent map defect changed. |
| 2026-08-09 | `VRP-096` exact Samsung two-cycle lifecycle | Accepted installed/package head `f6accf53e`; official ADB 37 verified Samsung `SM-S918B` / `R5CTC1J02VB`; evidence `Build/AndroidDenseCandidate/Evidence/2026-08-09-vrp096-lifecycle-f6accf53e` | Passed; checklist advances to `74 / 80`; VRP-097 is next | The visible route completes Menu -> Match -> Menu -> Match -> Menu without reinstall or network dependency. Both entries independently resolve the packaged dense-city definition, stream support EntityScene `b57de3fe43d8a4dcb9eefc6ef149ee66` and candidate EntityScene `c00140f2e94a04c3084c8dcb0c18cbd0`, and publish `[LoadingGate] ready` with `playRequested=1` / `simulationActive=1`. Both exits remove the Match HUD and return to the complete main menu; the two post-unload menu PNGs are byte-identical. The full Unity/IL2CPP log has exactly two ready markers, two streams of each EntityScene, and zero Unity fatal, `result=Failed`, positive overflow, or positive deficit matches. No package reinstall, unplugged requirement, threshold, protected/production asset, `productionCutover=0`, preserved file, or independent map defect changed. |
| 2026-08-09 | `VRP-097` final cooled Samsung thermal acceptance | Accepted installed/package head `f6accf53e`; official ADB 37 verified Samsung `SM-S918B` / `R5CTC1J02VB`; accepted evidence `Build/AndroidDenseCandidate/Evidence/2026-08-09-vrp097-final-thermal-f6accf53e-cool-rerun2`; structured recorder `complete=true`, `7,198` samples, `120.007 s`; start/end screenshots; battery/thermal snapshots; full logcat | Passed; checklist advances to `75 / 80`, completes Phase 9, and makes VRP-100 dependency-ready | The accepted cooled run starts and ends at thermal status `0` (`AP 36.5 -> 50.2 C`, `BAT 35.9 -> 37.0 C`, `SKIN 35.7 -> 39.4 C`) with `0%` battery drain. It sustains `59.980` average FPS, `59.997` p10 FPS, `16.672 ms` average frame time, `16.670 ms` p95, `16.740 ms` p99, CPU `11.568/13.218 ms` average/p95, GPU `13.849/14.001 ms` average/p95, and `0 B/frame` managed allocation; all amended and unchanged timing budgets pass. Start/end frames retain the complete live Match HUD and dense-city presentation without white screen, gross holes, stale proxy state, or UI loss, and logcat has no failed result, positive overflow/deficit, fatal exception, SIGSEGV, or OOM match. The earlier completed warm run remains rejected because CPU average was `12.025 ms` against the unchanged `12.000 ms` limit; a malformed quoted launch and the first cooled isolated-ADB launcher both failed before measurement and remain rejected. No reinstall, unplugged requirement, gate weakening, protected/production asset, `productionCutover=0`, preserved file, or independent map defect changed. |
| 2026-08-09 | `VRP-100` accepted architecture/workflow/output-ownership closeout | Updated `dense_city_author_workflow.md`, `dense_city_generated_output_ownership.md`, `operation_map_runtime_ownership_chain.md`, `operation_map_authored_ecs_workflow.md`, and `operation_map_scene_split_rollback_recipe.md`; source/report inspection; `git diff --check` | Passed; checklist advances to `76 / 80`; VRP-101 is next | Documentation now records the exact editor types (`OperationMapRenderDatabaseBakeConfig`, `OperationMapVirtualizedPresentationAuthoring`, `OperationMapRenderDatabaseBuilder`), runtime blob/components/systems, and the candidate-only ownership chain. It records content/order/config/logical/packed/materialized hashes, `76,517 = 61,925 + 14,592` source reconciliation, `40,460` placements, `1,934` cells, four buckets, `7,784` slots, `61,783` stripped packed renderers, and `14,017` retained packed owner/path rows. The file transaction, protected-production snapshot, complete-output regeneration/invalidation rules, cutover path ledger, and virtualized rollback verification are explicit. The accepted Samsung package/thermal result is linked without claiming production. No generated evidence byte, implementation, protected/production asset, Addressables setting, package, threshold, `productionCutover=0`, preserved file, or independent map defect changed. |
| 2026-08-09 | `VRP-101` parent parity/residency semantic closure | Parent architecture clauses and Phase 6/9 summaries updated from accepted current source/report/device evidence; `git diff --check` | Passed; checklist advances to `77 / 80`; VRP-102 is next but remains dependency-gated by open parent production prerequisites | Parent rules now distinguish direct render-entity parity in `ResidentEntities` from complete logical-row plus exhaustive materialized-proxy parity in `VirtualizedProxyPool`; off-camera lack of a slot is valid only after logical parity. Map-lifetime residency now means one immutable database, resident exceptions, and one fixed slot set, not permanent materialization of every eligible renderer. The superseded future camera-section loader is explicitly rejected in favor of zero-operation fixed-slot reassignment, and building/attachment/render-only clauses name accepted canonical-state recipe semantics. Parent Phase 6 and Phase 9 summaries now record current `76,517 = 61,925 + 14,592` reconciliation, `40,460` placements, `7,784` slots, exact Samsung acceptance, and unchanged production gating. No parent checklist checkbox, production authorization, implementation, generated byte, protected asset, Addressables setting, package, threshold, `productionCutover=0`, preserved file, or independent map defect changed. |
| 2026-08-09 | Phase 10 exact-package inventory gate and installed-artifact reconciliation | Official ADB `37.0.1` verified `SM-S918B` / `R5CTC1J02VB` at `192.168.2.33:42611`; installed `base.apk` and local `Build/AndroidDenseCandidate/WarlineCapture-DenseCityCandidate.apk` both `544,011,811` bytes / SHA-256 `65a4a57c8014ff99a06151a6adb2b4046a7304bfb1f1b817ad49c764128b452d`; focused `Build/Logs/parent-android-package-exact-dense-plan-focused-host-gui.log` `13/13`; expected real-package rejection `Build/Logs/parent-android-package-stale-bundle-real-apk-rejection.log`; `git diff --check` | Final package ownership is rejected/open; checklist remains `77 / 80`; accepted behavioral/performance evidence is retained but this APK must not be reinstalled or used for final production-package acceptance | Read-only APK inventory proved the current dense build plan/report owns exactly four bundles totaling `915,827` bytes, while the installed APK carries six dense bundles totaling `1,070,729` bytes. The two unreachable incremental-build leftovers are `059ad5323b1112826e7d3560ced80254.bundle` (`152,869` bytes) and `c2059c47c5fbfa12fb10b95f9419f34c.bundle` (`2,033` bytes). The package validator now requires the packaged dense catalog, hash, and bundle path set to equal the current deterministic file plan exactly; focused fixtures pass and the real APK rejects on the first unexpected stale entry instead of silently accepting it. The stale files are not referenced by the accepted catalog, so prior runtime/frame/visual evidence is not reclassified, but final package-size/ownership and cutover acceptance require a fresh clean checked artifact. GUI licensing first failed before compilation; after exact orphan cleanup, the identical checked host-access GUI wrapper passed. No install, app launch, production/protected mutation, `productionCutover=0` change, preserved-file change, or independent defect reclassification occurred. |
| 2026-08-09 | Phase 10 dense-package clean-cache root-cause correction | Checked GUI-licensing focused `Build/Logs/parent-android-package-clean-cache-focused-host-gui.log` `[DenseCityCandidateAndroidPackageValidation] result=Passed tests=15`; `git diff --check` | Implementation and Editor gate pass; checklist remains `77 / 80`; a fresh checked artifact is still required after production cutover | Dense-candidate release and profiler player builds now always add `BuildOptions.CleanBuildCache`. Their content-hash bundle filenames change as the deterministic Addressables plan changes, so permitting an incremental player cache allowed unreachable prior bundles to survive under `assets/aa/DenseCityCandidate/`. The focused suite proves both variants retain their required release/development flags and force the clean cache. The current installed/local APK remains rejected and will not be reinstalled; no replacement APK, device run, production/protected mutation, `productionCutover=0` change, preserved-file change, or independent defect reclassification occurred. |
| 2026-08-09 | `VRP-102` authorized staged production cutover | Checked Windows GUI-licensing wrapper `Build/Logs/vrp102-production-cutover-host-gui-r2.log`; `[OperationMapDenseCityProductionCutover] result=Passed`; rollback checkpoint `Design/AgentReports/2026-08-09_dense_city_production_cutover_rollback_checkpoint.json`; `git diff --check` | Passed; checklist advances to `78 / 80`; VRP-103 is next | The byte-transactional cutover changed the production definition and thin runtime binding to `EntityScene + VirtualizedProxyPool`, bound EntityScene GUID `c00140f2e94a04c3084c8dcb0c18cbd0`, removed legacy placement/static references, and reduced production operation-map Addressables to catalog `2`, core `4`, shared `0`, presentation `0`. The checkpoint binds source revision `b9383b6a12e7b4ef6b9494a05a8a8788db90b859`, pre-cutover definition/runtime/Addressables hashes, and exact frozen static ownership of `545` files / `269` scenes / `42,765,094` bytes with aggregate SHA-256 `8cb615f4a82b312da806d5124261ff2e8b1af0e0cb6fdb2f0307331f80403764`. The first attempt failed compilation before mutation and is rejected; the corrected wrapper passed all in-transaction definition/layout/runtime checks. The static package remains tracked and untouched pending VRP-103. Preserved generated reports and `_Recovery` scenes remain unstaged. |
| 2026-08-09 | `VRP-103` production clean-package entry point | Checked GUI-licensing package contract `Build/Logs/vrp103-production-build-entry-package-focused-gui.log` `15/15`; checked Android report contract `Build/Logs/vrp103-production-build-entry-report-focused-gui-r2.log` `14/14`; `git diff --check` | Implementation passes; checklist remains `78 / 80` pending the fresh artifact and exact-device/rollback evidence | `BuildScript.BuildProductionAndroidApk` is now the narrow checked execute method for production acceptance. It forces `CleanBuildCache`, retains the release detailed-build report, runs the production Addressables report and EntityScene package gate, and emits `[ProductionAndroidPackage] result=Passed` only after all reports complete. The relevant source-contract assertion now follows the existing centralized dense-candidate option helper and separately proves this production entry forces clean cache. The first report-suite run is rejected because that stale assertion expected the former inline expression; the corrected same wrapper passes. No package exists from this step, and the frozen static output remains untouched. |
| 2026-08-09 | `VRP-103` first clean production APK rejection and EntityScene build-input correction | Rejected checked build `Build/Logs/vrp103-production-9299cc962-clean-apk-gui.log`; build succeeded but package gate rejected missing `c00140f2e94a04c3084c8dcb0c18cbd0.entityheader`; accepted focused recovery `Build/Logs/vrp103-production-entityscene-build-addition-versioned.log` `2/2`; fresh production Addressables report | APK rejected and will not be installed; checklist remains `78 / 80`; one replacement clean package is required | ZIP inspection proved the rejected APK included support `b57de3f...` and old rollback `d50925a...` EntityScenes but omitted the cutover `c00140f...` scene. The player-build additions hook still defaulted to the historical source SubScene even after production definition cutover. It now derives the production EntityScene GUID from the current definition when `PresentationKind=EntityScene`, retains the historical source-path fallback for a static rollback, and keeps the candidate-only override unchanged. The production Build Layout records six stable map addresses, five bundles / `135,568,624` bytes, and zero duplicated dependencies. The first focused GUI recovery reached entitlement but stalled before project load and timed out; the same checked wrapper passed `2/2` through the authorized version-matched path. No rejected APK install, static deletion, preserved-file staging, or independent defect reclassification occurred. |
| 2026-08-09 | Independent blue-Autobahn/canal material isolation correction | Checked focused `Build/Logs/autobahn-canal-material-isolation-focused-version-matched-host.log` `[DenseCityCanalPresentationMaterialValidation] result=Passed tests=2`; checked read-only full-scene `Build/Logs/autobahn-original-road-material-scene-validation-host.log`; `git diff --check` | Editor correction accepted; checklist remains `78 / 80`; exact-device confirmation is folded into the fresh VRP-103 production package route | Candidate blue coloring is no longer selected by material path alone: it requires the exact canonical canal-water material and exact canonical canal-surface prefab, and a separate non-canal conversion variant prevents shared-source recoloring. The protected Autobahn validator now fails closed on changed material-slot count, any water/transparent material, or any slot that differs from the approved source road prefab. The complete current scene passes for `2` protected anchors, `1,108` road tiles, `962` lane cells, `146` connector cells, `29` connector columns, `4` approved prefab GUIDs, and all `5,540` original opaque road-material slots. Rejected sandboxed GUI/version-matched licensing launches are retained; after user-authorized cleanup of only their wrapper-created trees and verified stale-client restart, the identical checked host-access wrapper passed. Canal presentation stays blue; road-over-water-without-bridge remains a separate open defect. No protected/production asset, static package, preserved report, or `_Recovery` scene changed. |
| 2026-08-09 | `VRP-103` regenerated production candidate and exact Editor-to-bake material/transform parity | Rejected first installed clean production APK `d641769022064dadc242d49fba28e780b70fe1d59a4545c18644db82bd11e66f`; transactional regeneration log `Build/Logs/autobahn-material-regenerate-production-candidate-d12884e64.log`; protected Autobahn rerun `Build/Logs/autobahn-regenerated-candidate-scene-material-parity.log`; rejected obsolete full-count route `Build/Logs/autobahn-regenerated-candidate-baked-material-transform-parity.log`; accepted narrow post-cutover bake `Build/Logs/production-dense-city-material-transform-parity.log`; `Design/AgentReports/2026-07-24_dense_city_generated_transform_parity.json`; `git diff --check` | Regenerated Editor candidate and baked-runtime parity pass; checklist remains `78 / 80`; no replacement APK is authorized until this stable step is pushed | The first package launched normal gameplay rather than a white screen, but user visual review exposed blue roads and dark ground, so it was rejected and stopped. Root cause was sequencing: the corrected converter had passed against source content, but the production candidate scenes had not been regenerated before packaging. The candidate is now regenerated, and the protected audit again proves all `1,108` Autobahn tiles / `5,540` slots retain approved opaque road materials. The historical full validator remains honestly rejected before parity on its obsolete `5,403`-building assumption versus the current accepted `4,977`; that assumption was not weakened. A new read-only post-cutover gate first proves production is bound to the exact accepted `EntityScene + VirtualizedProxyPool`, then bakes that scene and fails on any mesh, material, base-color, matrix, scale, bounds, missing, or unexpected row. It passes `36,304/36,304` identities and `62,455/62,455` renderer entries with zero unresolved mesh/material rows, zero mesh/material/base-color mismatch, identical candidate/baked identity SHA-256 `5e8eef95...e186`, and zero rejected rows. Android visual confirmation, rollback restore, and VRP-103 closure remain open; canal water stays blue and road-over-water-without-bridge remains separate. Preserved generated reports and all `_Recovery` scenes remain unstaged. |

## 19. Evidence References

- `Design/AgentReports/2026-07-27_dense_city_candidate_android_minimap_performance_rerun.json`
- `Design/AgentReports/2026-07-25_dense_city_packed_asset_sharing.json`
- `Design/AgentReports/2026-07-27_dense_city_candidate_android_runtime_content.json`
- `Design/AgentReports/2026-07-28_dense_city_render_virtualization_eligibility_inventory.json`
- `Design/AgentReports/2026-07-28_dense_city_render_virtualization_source_rows.json.gz`
- `Design/AgentReports/2026-07-28_dense_city_render_virtualization_prototype_recipes.json`
- `Design/AgentReports/2026-07-28_dense_city_render_virtualization_logical_placements.json`
- `Design/AgentReports/2026-07-28_dense_city_render_virtualization_spatial_cells.json`
- `Design/AgentReports/2026-07-28_dense_city_render_virtualization_capacity_budget.json`
- `Design/AgentReports/2026-07-28_dense_city_render_virtualization_database.json`
- `Design/AgentReports/2026-07-29_dense_city_vrp002_android_raw_profiler_evidence.json`
- `Design/AgentReports/2026-07-29_dense_city_vrp002_android_raw_profiler_summary.md`
- `Design/AgentReports/2026-07-29_dense_city_vrp004_fixed_camera_route_baseline.json`
- `Design/AgentReports/2026-07-29_dense_city_render_virtualization_direct_bake.json`
- `Design/AgentReports/2026-07-30_dense_city_render_virtualization_pilot_freeze.json`
- `Design/AgentReports/2026-07-30_dense_city_render_virtualization_pilot_enabled.json`
- `Design/AgentReports/2026-07-30_dense_city_vrp053_android_after_profiler_summary.md`
- `Design/AgentReports/2026-07-30_dense_city_vrp053_android_before_after.json`
- `Design/AgentReports/2026-07-31_dense_city_vrp054_windows_remediation.md`
- `Design/AgentReports/2026-08-01_dense_city_vrp067_android_destruction_matrix.json`
- `Design/AgentReports/2026-07-21_dense_city_phase0_android_baseline.md`
- `Design/AgentReports/2026-07-19_operation_map_android_performance_characterization.md`
- `Design/Architecture/dense_city_editor_bake_hybrid_runtime_implementation_tracker.md`
- `Design/Architecture/performance_regression_contract.md`

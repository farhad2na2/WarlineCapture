# Match HUD V3 Sharp Solid V09 Generation Prompt

```text
Use case: ui-mockup
Asset type: WarlineCapture mobile tactical match HUD visual target, v09 tutorial indicator pass
Input images: Image 1 is the edit target and layout source; Image 2 is the sharp-solid main-menu style reference; Image 3 is the ARIA portrait/personality reference.
Primary request: Revise the v08 Match HUD target by adding visible tutorial indicator examples showing how FTUE guidance appears on a squad button and on the MOVE command.

Keep the v08 HUD composition and all major UI lockups unchanged: dry Sahrin battlefield, tall selected-unit panel on the left, five squad cards at bottom-left with title text, bottom command rail with SELECT MOVE ATTACK HOLD STOP SCAN, BUILD and SUPPORT only on the right rail, medium ARIA panel top-right, resource/status header left of ARIA, HOSTILE CELL SPOTTED warning panel, attack-unavailable feedback above command rail, and the moderately sized bottom-right minimap. Do not add a top menu button, standalone objective panel, or top MOVE ORDER pill.

Add tutorial indicator treatment:
- Show a tutorial target highlight on the first bottom-left squad card, Rifle Squad.
- Show a second tutorial target highlight on the MOVE command button in the bottom command rail.
- Use clear ARIA-themed tactical guidance visuals: cyan/teal pulsing outline, bright corner brackets, soft glow, small amber numbered step badge, and a thin pointer arrow anchored to the target.
- The first badge reads exactly: 1 SELECT SQUAD.
- The second badge reads exactly: 2 TAP MOVE.
- Include one compact dark translucent ARIA tutorial card near the bottom HUD, not covering gameplay or labels. It should feel like a small coach tip, with a tiny ARIA face/icon, sharp corners, solid shadow, cyan accent line, and short text: SELECT A SQUAD, THEN MOVE.
- Make the indicators large enough for mobile readability but do not shrink or hide the target buttons.
- The highlight should sit on top of the UI as a tutorial overlay, not as a replacement button design.

Style: high-end colorful mobile RTS HUD, sharp rectangles, solid action colors, strong drop shadows, tactical sci-fi military polish, brighter than the old black/gold style.
Constraints: preserve existing button labels and icon scale; do not cover the MOVE label or Rifle Squad title; no old ornate gold borders; no sea/water/naval scenery; no diamonds/gems/supply resources; no tiny unreadable tutorial UI; no full-screen dark tutorial overlay; no extra bottom command buttons.
```

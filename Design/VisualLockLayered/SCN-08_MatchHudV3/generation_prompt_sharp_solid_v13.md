# Match HUD V3 Sharp Solid V13 Generation Prompt

```text
Use case: ui-mockup
Asset type: WarlineCapture mobile tactical match HUD visual target, v13 final cleanup
Input images: Image 1 is the corrected draft to revise; Image 2 is the current sharp-solid UI and newer ARIA area reference; Image 3 is the ARIA portrait/personality reference.
Primary request: Keep the corrected header, expanded ARIA with attached map, and moderate far-right BUILD button from Image 1, but remove the old floating bottom tutorial coach card. All FTUE explanation must live inside the ARIA expanded panel.

Keep exactly:
- Top resource/status header visible: Materials, Oil, Fuel, Civilian Risk, Settings, Pause.
- HOSTILE CELL SPOTTED warning panel below/near the header without covering it.
- Expanded top-right ARIA FTUE panel with a smaller modern ARIA portrait, "TUTORIAL 1/3", tutorial message, DO IT button, SHOW ME button, and attached tactical map at the bottom.
- ARIA map remains attached to ARIA, with city grid, pips, viewport rectangle, and small zoom controls.
- Bottom command bar contains exactly: SELECT, MOVE, ATTACK, HOLD, STOP, SCAN, SUPPORT, BUILD.
- BUILD is far-right and only moderately larger than SUPPORT, about 25-35 percent wider, same row, not oversized, not double height.
- SUPPORT is normal-size immediately left of BUILD.
- Tutorial highlights on Rifle Squad and MOVE with amber badges 1 and 2.
- Tall selected-unit panel, squad cards, dry battlefield, and attack-unavailable feedback.

Remove/change:
- Remove the separate floating bottom ARIA coach card that says "SELECT A SQUAD, THEN MOVE".
- Do not replace it with another floating tutorial card.
- Do not add a bottom MAP tile.
- Do not add a standalone minimap.
- Do not use old compact ARIA button styling; keep the newer expanded ARIA area with modern portrait treatment.

Composition constraints:
- Freed space where the floating coach card was should show playable battlefield, path/selection markers, or clean negative space.
- ARIA must not cover the top header, warning panel, bottom command bar, BUILD, SUPPORT, MOVE label, or Rifle Squad title.
- Keep labels readable and mobile-friendly.

Style: high-end colorful mobile RTS HUD, sharp rectangles, solid action colors, strong drop shadows, tactical sci-fi military polish, ARIA cyan holographic accents, brighter than old black/gold.
Avoid: floating coach card, old ARIA button, missing top header, oversized BUILD, bottom MAP tile, standalone minimap, vertical right rail, old ornate gold borders, water/sea/naval scenery, fake resources.
```

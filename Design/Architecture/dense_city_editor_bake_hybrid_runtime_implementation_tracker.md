# Operation Map ECS Presentation And Dense City Editor Bake Implementation Tracker

Date: 2026-07-21
Status: Complete. Production owns the accepted deterministic dense-city `EntityScene + VirtualizedProxyPool` representation with exact source/baked/package parity, lifecycle and rollback acceptance, zero live static-package ownership, and final exact-Samsung Android performance/thermal/correctness evidence under the amended contract.
Parent tracker: `operation_map_scene_split_and_generator_tracker.md`
Related contracts: `file_naming_architecture_contract.md`, `gameplay_solid_ecs_contract.md`, `performance_regression_contract.md`, `operation_map_runtime_ownership_chain.md`, `operation_map_scene_split_rollback_recipe.md`
Related R&D only: `runtime_operation_map_generation_rnd_implementation_tracker.md`
Measured Android render-residency amendment: `dense_city_virtualized_render_proxy_android_60fps_implementation_tracker.md`

## 1. Objective

Migrate all existing visual content on `opmap.skirmish.desert_base_01` and all future generated dense-city content to one ECS-first map SubScene presentation path. Existing and generated buildings, vehicles, roads, bridges, terrain, mountains, vegetation, props, infrastructure, and background visuals must bake from editor GameObjects to runtime entities without shipping or streaming their GameObject hierarchies.

The migration preserves the existing military base, handmade city, placement behavior, surface/blocker data, camera/minimap/runway/helipad ownership, and deterministic map identity. The current 514-scene static-presentation package remains rollback evidence until ECS visual/runtime parity is accepted, then leaves the production Addressables/build path.

The production result is hybrid:

```text
Editor authoring scene
  -> classify and migrate existing map visuals to protected SubScene entity-authoring roots
  -> deterministic dense-city generation under one logical disposable ownership set
  -> generation-time semantic ownership and collider removal
  -> all existing and generated visual placements baked to ECS entities and Entities Graphics in resident mode
  -> accepted VirtualizedProxyPool semantics instead bake eligible placements to immutable logical data plus fixed ECS render slots; production selection remains gated
  -> coarse ECS surface/blocker data plus compact entity-scene presentation data
  -> thin generated runtime-binding scene
  -> local Addressables package

Runtime
  -> load thin operation-map binding scene additively
  -> load the map SubScene containing existing and generated gameplay/render-only entities once
  -> publish immutable map/surface/blocker ECS data
  -> use Entities Graphics batching, frustum culling, and LOD without per-camera scene streaming
  -> unload all map entities, map ECS state, subscene, and binding scene in the accepted order
```

This tracker does not authorize runtime procedural city generation. Existing-map migration and dense-city generation are editor operations. Runtime only loads deterministic baked entity output.

## 2. Accepted Decisions

- Regenerate the city correctly instead of converting the previously generated 25,000-object hierarchy.
- Migrate the current map's existing renderers to the same ECS presentation path before adding the dense city.
- Treat the existing 451 building and 29 vehicle placement records as migration inputs, not permanent justification for managed runtime visual GameObjects.
- Move existing map visual authoring into protected map SubScene roots through a transactional editor migration; do not manually reparent thousands of objects.
- Classify content while generating it. A post-pass validates ownership but never guesses ownership from names.
- Keep every generated object beneath one of two scene-scoped roots in the same disposable generation ownership set: immutable/proxy source in the operation-map authoring scene and damageable building authoring in the existing map SubScene.
- Keep migrated military, handmade city, runway, resources, mountains, roads, and authored mission content outside disposable generated ownership roots but inside protected authored ECS presentation ownership.
- Keep persistent hand edits outside the generated ownership roots under an authored override root.
- Remove collider and rigidbody components from generated instances immediately. Do not merely disable them.
- Reject every production map bake that contains any `Collider`, `Collider2D`, `Rigidbody`, or `Rigidbody2D` in map-owned build content.
- Use simplified semantic bake proxies for terrain, roads, bridges, ramps, water exclusion, and blockers. Never rasterize detailed presentation meshes as gameplay navigation geometry.
- Treat every generated house/building that can receive damage as an ECS gameplay entity with Entities Graphics presentation. Do not create or retain a runtime building GameObject for it.
- Bake intact and destroyed render hierarchies to entity children and switch entity visual state when health reaches zero. Runtime building destruction must not instantiate a replacement GameObject.
- Treat roof equipment, interior dressing, shop goods, tent contents, signs, awnings, lamps, furniture, and every other visual attached to a damageable building as part of that building's presentation ownership. Bake those descendants beneath either its intact or destroyed visual root so the complete intact set disappears and the complete destroyed set appears in one destruction transition.
- Bake only independent trees, rocks, lights, props, modular road/sidewalk segments, bridge segments, vegetation items, and background modules as render-only ECS entities sharing mesh/material assets. A prop that must disappear with a building is never an independent render-only entity.
- Bake unique generated terrain, canal, mountain, road, or bridge visuals as one or more render-only entities unless measured Android evidence approves a specific heavyweight exception.
- Do not pool permanent map visuals. Pooling remains for transient spawned effects; permanent placements are baked entity instances with no spawn/despawn loop.
- Keep all generated city entities resident for the lifetime of the loaded map. Camera movement changes rendering visibility through Entities Graphics only; it does not load/unload generated city scenes or simulation entities.
- Apply the same resident entity/culling rule to existing map visuals. Existing and generated visual entities load once and unload once with the map.
- Amendment authorized 2026-07-28 for the candidate only: the corrected Android candidate proved whole-map render-entity residency unacceptable at `15.7-16.3 FPS` with CPU main `56.7-59.8 ms` while GPU remained `15.6-16.4 ms`. `dense_city_virtualized_render_proxy_android_60fps_implementation_tracker.md` may replace eligible permanent render entities with immutable resident placement/prototype data plus one preallocated job-driven ECS render-proxy slot set. All simulation/gameplay entities remain resident; no GameObjects, runtime generation, entity spawn/despawn loop, scene streaming, production cutover, or frozen-rollback mutation is authorized by this amendment.
- Reuse the accepted operation-map runtime binding scene, map SubScene, local Addressables layout, readiness, failure, and teardown owners.
- Keep the existing static-presentation manifest/streamer only during migration rollback. After ECS parity acceptance, `opmap.skirmish.desert_base_01` uses entity-scene presentation mode, its manifest/514 chunk scenes leave production Addressables/build ownership, and the streamer is not bound for this map.
- Do not introduce another map loader, streamer, runtime-generation host, update-loop `MonoBehaviour`, manager, controller, facade, service, provider, or service locator.
- Do not ship existing-map or dense-city authoring GameObject hierarchies. Ship the thin runtime binding scene and baked ECS entity scene data/metadata; art remains transitive EntityScene build content unless a built-content layout proves that a leaf requires one explicit cross-partition shared owner.
- Preserve the accepted Editor placement exactly through migration, baking, Addressables packaging, and runtime load. The authoritative source world matrix for every renderer owner must match its candidate-SubScene authoring matrix, baked ECS matrix, and Addressables-loaded runtime matrix within the tolerances in Section 12.0A. Runtime systems must not recompute, offset, normalize, ground-snap, or otherwise overwrite permanent map-visual transforms.

## 3. Baseline Audit

### 3.1 Existing Map Baseline

Accepted current-map evidence reports:

- `16,542` static-presentation sources serialized into `514` generated scene chunks;
- `451` building and `29` vehicle placement records;
- map-specific static manifest, integrity ledger, local Addressables groups, and camera-driven scene streamer;
- existing building placement startup that creates managed `RuntimeBuildingEntity`/GameObject presentation;
- existing vehicle/unit authoring that already demonstrates ECS prefab/render conversion and must be audited rather than reimplemented;
- a thin runtime binding scene plus one map SubScene that currently owns grid/initial-unit authoring but not the full visual map.

These are migration baselines and rollback inputs, not the accepted final runtime representation.

### 3.2 Dense City Audit

The first giant-city generation was cleared and is retained only as audit evidence. It produced:

| Metric | Before generation | After generation | Delta |
|---|---:|---:|---:|
| Authoring scene bytes | `45,755,844` | `225,703,659` | `+179,947,815` (`+393.3%`) |
| Scene GameObjects | `129` | `25,411` | `+25,282` |
| Scene MeshRenderers | `5` | `13,928` | `+13,923` |
| Prefab instances | `13,313` | `49,282` | `+35,969` |
| Generated buildings | N/A | `5,759` | `+5,759` |
| Generated road tiles | N/A | `4,650` | `+4,650` |
| Generated road chunks | N/A | `83` | `+83` |
| Generated parks | N/A | `15` | `+15` |
| Enabled MeshColliders in saved scene | N/A | `931` | unacceptable |
| Enabled BoxColliders in saved scene | N/A | `161` | unacceptable |
| New generated bake-group ownership markers | `0` | `0` | generated content unclassified |

Additional findings:

- The generator used 21 `DenseCity_*` roots that mixed presentation, surface, and blocker responsibilities.
- The generated city extent was approximately `2560 x 1280`, while the active gameplay grid was `2048 x 1024` at `1 m` cells.
- The generator extended approximately `512 m` west and `128 m` north/south outside the active grid.
- Expanding the gameplay grid to the full generated rectangle would increase cells from `2,097,152` to `3,276,800`, approximately `56%`.
- Surface authoring used `2 x 2` samples per cell. Full-grid expansion would increase nominal sample positions from approximately `8.39 million` to `13.11 million`.
- Static presentation currently uses `32 m` chunks. A `2560 x 1280` rectangle permits up to `3,200` logical chunk coordinates before occupancy filtering.
- Generated facade material variants already enabled GPU instancing, but renderer, shadow, entity-scene, Entities Graphics batch, and LOD budgets were not validated.
- The static-presentation baker's `LODGroup` source exclusion does not govern generated city entity presentation. Generated LOD authoring must instead prove correct Entities Graphics baking and device behavior.

## 4. Scope

### 4.1 In Scope

- Existing-map renderer/placement ownership inventory and explicit ECS migration classification.
- Transactional movement of existing map visual authoring into protected map SubScene roots.
- ECS conversion of current authored buildings, vehicles, roads, bridges, terrain, mountains, vegetation, props, infrastructure, and background visuals.
- Conditional operation-map content validation/readiness for entity-scene presentation without a required static manifest.
- Retirement of the current map's 514 static presentation scenes, manifest binding, source suppression, and camera-streamer dependency after accepted rollback-safe cutover.
- Dense-city generator semantic hierarchy.
- Disposable generated-root identity and deterministic regeneration.
- Authored-content protection and persistent override ownership.
- Zero-collider enforcement for generated and map-owned production content.
- Generation-time surface and blocker records.
- Simplified surface/blocker proxy generation.
- Dense-city bake-readiness validation.
- One-button integration with `Game/Operation Maps/Bake Current Map (All)`.
- ECS entity-scene ownership, shared render-asset validation, Entities Graphics batching, LOD/impostor policy, and entity-scene size/runtime budgets.
- Isolation from and eventual retirement of the existing static-presentation package so no existing or generated renderer is serialized twice.
- Thin runtime binding scene and local Addressables packaging already owned by the operation-map pipeline.
- ECS building authoring/baking, health/destruction state, entity visual switching, and target/blocker integration.
- Editor, PlayMode, Android, build-layout, deterministic-output, and performance validation.

### 4.2 Out Of Scope

- Runtime procedural city generation for production.
- A second physical operation map.
- Remote Addressables delivery.
- Manual reparenting of thousands of generated objects.
- Name-substring or prefab-name ownership inference.
- Unity Physics, collider-based pathing, raycast-based map blocking, or Rigidbody gameplay.
- Shipping the authoring scene as the runtime map scene.
- Replacing the existing operation-map loader. The existing streamer remains implemented for legacy/future compatibility but is unbound from this ECS-presented map after cutover.
- A GameObject pool for permanent generated map visuals. The accepted fixed ECS render-slot representation is not a GameObject pool; its production selection remains governed by `dense_city_virtualized_render_proxy_android_60fps_implementation_tracker.md`.
- Generated-city static-presentation scene chunks without a separately approved, measured heavyweight-content exception.

## 5. Ownership And Scene Hierarchy

The required logical authoring hierarchy spans the two existing editor source scenes because only objects authored in a SubScene are converted to entity scene data:

```text
Operation-map authoring scene
  OperationMapSceneView
  MapSurfaceAuthoring
  GridAuthoringConfig
  EmptyLegacyPlacementRootsDuringMigration
  MissionAndInfrastructureMetadataNotOwnedByTheSubScene

  AuthoredCityOverrides
    PersistentPresentation
    PersistentSurfaceAuthoring
    PersistentGameplayAuthoring
    GenerationExclusions

  Generated_GiantDenseMiddleEasternCity_MapBakeSource
    BakeSources
      Terrain
      Roads
      Bridges
      Ramps
      Blockers

Existing map SubScene authoring scene
  Grid
  InitialUnitsSpawnerAuthoring
  AuthoredOperationMapEntityPresentation
    GameplayBuildings
      MilitaryBase
      HandmadeCity
      Infrastructure
    GameplayVehicles
    RenderOnly
      Terrain
      RoadsAndBridges
      Mountains
      Vegetation
      Props
      Infrastructure
      Horizon
  Generated_GiantDenseMiddleEasternCity_EntityPresentation
    GameplayBuildings
      Buildings
      CivicAndMarket
    RenderOnly
      Infrastructure
      Vegetation
      Props
      Horizon
```

Rules:

- `AuthoredOperationMapEntityPresentation` is identified by `OperationMapEntityPresentationRootAuthoring`, belongs to the current operation-map id, and is never deleted by dense-city regeneration.
- Existing visual migration is transactional and stable-identity-driven. It moves/copies only reviewed renderer/placement owners, validates the candidate SubScene, and leaves the accepted source/static package untouched until cutover.
- Existing objects with unresolved gameplay, script, light, animation, or cross-scene reference ownership fail migration and require an explicit disposition. Names, prefab filenames, and renderer appearance are not classification evidence.
- After cutover, existing authored building/vehicle visuals and placements are owned by SubScene entities. Legacy roots may remain empty only while serialized compatibility fields exist; they do not spawn runtime GameObjects.
- Both generated roots are identified by `DenseCityGeneratedRootAuthoring`, a shared generation id, and distinct `DenseCityGeneratedRootRole` values, not by names alone.
- Exactly one `MapBakeSource` root must exist in the operation-map authoring scene and exactly one `EntityPresentationSource` root must exist in its referenced map SubScene.
- Regeneration deletes only those two marked roots and recreates them transactionally as one ownership set.
- `AuthoredCityOverrides` is never deleted or reparented by generation.
- Existing map roots are immutable protected-area inputs. Generation may inspect their precomputed renderer bounds during the editor operation but may not move, disable, rename, or modify them.
- Every damageable generated building belongs beneath `EntityPresentationSource/GameplayBuildings`, has explicit entity-building authoring metadata, and is excluded from static-presentation source collection.
- Every generated non-gameplay visual belongs beneath `EntityPresentationSource/RenderOnly`. In `ResidentEntities`, it bakes to renderer entities with no gameplay components or update system. In `VirtualizedProxyPool`, eligible rows bake to immutable logical records plus fixed proxy slots while explicit resident exceptions remain renderer entities.
- No generated city renderer may exist beneath `MapBakeSource`, the operation-map authoring scene, or any static-presentation source root.
- Every generated surface proxy belongs beneath exactly one `BakeSources` role root.
- Proxy roots may contain `MeshFilter` data needed by the surface baker, but must not contain `MeshRenderer`, collider, Rigidbody, gameplay component, or runtime update behavior.
- Detailed presentation objects must not exist beneath a terrain, road, bridge, ramp, or blocker bake root.
- `OperationMapSceneView.BuildingAuthoringRoot`, `VehicleAuthoringRoot`, `MapBuildingPlacementConfig`, and `MapVehiclePlacementConfig` are migration inputs. In `EntityScene` presentation mode, current-map startup does not spawn from them and validation does not require non-empty placement configs.

## 6. Semantic Generation Contract

### 6.1 Existing Map Migration Contract

| Existing content | Final SubScene ownership | Runtime treatment |
|---|---|---|
| Authored map building | `AuthoredOperationMapEntityPresentation/GameplayBuildings` | ECS health/faction/footprint/targeting plus intact/destroyed Entities Graphics children; no `RuntimeBuildingEntityLink` or visual GameObject |
| Authored map vehicle | `AuthoredOperationMapEntityPresentation/GameplayVehicles` | Reuse current unit/vehicle ECS Baker and render-prefab path; prove no source visual GameObject remains or is duplicated |
| Terrain, road, sidewalk, bridge, runway visual | `AuthoredOperationMapEntityPresentation/RenderOnly` | Render-only entities; existing ECS surface/runway metadata remains authoritative |
| Mountain, rock, vegetation, prop, infrastructure, horizon visual | `AuthoredOperationMapEntityPresentation/RenderOnly` | Render-only entities sharing mesh/material assets |
| Building/vehicle placement configs | migration evidence only after cutover | No current-map runtime spawning in `EntityScene` mode; retained temporarily for parity/rollback, then archived or removed through accepted ownership cleanup |
| Existing collider/Rigidbody | none | Removed from map-owned instances; ECS surface/blocker/occupancy remains authoritative |
| Existing script/light/animation behavior | explicit reviewed ECS, baked-lighting, or approved managed-boundary disposition | Never silently stripped or converted by renderer-only inference |

Existing static-presentation manifest source identity and 514 scene chunks remain byte-stable rollback evidence until entity-scene visual/gameplay parity passes. They are not regenerated from migrated ECS source content.

### 6.2 Generated City Contract

The generator knows semantic intent at creation time and must place output accordingly.

| Generated content | Presentation ownership | Surface/blocker output | Gameplay treatment |
|---|---|---|---|
| Dense building shell | `EntityPresentation/GameplayBuildings/Buildings` | coarse footprint in `BakeSources/Blockers` | ECS simulation entity plus Entities Graphics intact/destroyed children |
| Building foundation/ground patch | `EntityPresentation/RenderOnly/Infrastructure` | simplified walkable patch in `BakeSources/Terrain` | render-only entity |
| Asphalt road | `EntityPresentation/RenderOnly/Infrastructure` | road deck in `BakeSources/Roads` | render-only entity plus surface metadata |
| Sidewalk | `EntityPresentation/RenderOnly/Infrastructure` | terrain or road mask according to approved movement policy | render-only entity plus surface metadata |
| Road shoulder/natural patch | `EntityPresentation/RenderOnly/Infrastructure` | `BakeSources/Terrain` | render-only entity |
| Bridge visual | `EntityPresentation/RenderOnly/Infrastructure` | deck in `BakeSources/Bridges`; approach in `BakeSources/Ramps` | render-only entity plus typed bridge surface |
| Canal water | `EntityPresentation/RenderOnly/Infrastructure` | non-ground exclusion in `BakeSources/Blockers` until water movement exists | render-only entity; no Unity Physics |
| Canal bank/park ground | `EntityPresentation/RenderOnly/Infrastructure` | `BakeSources/Terrain` | render-only entity |
| Courtyard wall/solid perimeter | gameplay-building child when damageable; otherwise `EntityPresentation/RenderOnly/Infrastructure` | coarse wall footprint in `BakeSources/Blockers` | entity; no collider |
| Courtyard props/wells | `EntityPresentation/RenderOnly/Props` | blocker only when gameplay clearance requires it | render-only entity |
| Rocks inside playable bounds | `EntityPresentation/RenderOnly/Props` | simplified blocker footprint | render-only entity; no collider |
| Rocks outside playable bounds | `EntityPresentation/RenderOnly/Horizon` | none | render-only entity |
| Horizon mountains | `EntityPresentation/RenderOnly/Horizon` | none unless they intersect playable bounds | render-only entity; measured unique-mesh exception only if required |
| Trees, bushes, grass | `EntityPresentation/RenderOnly/Vegetation` | no blocker by default | repeated render-only entities sharing art assets |
| Power poles, lines, streetlights | `EntityPresentation/RenderOnly/Infrastructure` | no blocker by default | repeated render-only entities sharing art assets |
| Roof, interior, shop, or tent props attached to a damageable building | child of the owning building's intact or destroyed visual root | none | explicit building identity and visual-state ownership at generation/migration time; intact descendants disappear atomically with the intact shell |
| Independent rooftop tanks and utility props | `EntityPresentation/RenderOnly/Props` only when they are proven independent of every damageable building | none | explicit independent identity; proximity or hierarchy name is not ownership evidence |
| Civic/market hero building | `EntityPresentation/GameplayBuildings/CivicAndMarket` | coarse blocker footprint | ECS building entity with explicit health/destruction policy |

No semantic decision may be recovered from `GameObject.name`, prefab filename, material name, hierarchy substring, or renderer shape after generation.

## 7. Collider-Free Contract

This game does not use Unity colliders for operation-map gameplay.

Generation requirements:

- Immediately after any prefab or primitive is instantiated, remove `Collider`, `Collider2D`, `Rigidbody`, and `Rigidbody2D` components from that instance hierarchy.
- Removal is instance-scoped. Never mutate a shared third-party prefab asset during city generation.
- Generated primitives must be created without retaining their automatically added colliders.
- Do not keep disabled collider components as documentation or future fallback.
- Surface height, walkability, blocking, building placement, vehicle occupancy, soldier pathing, and air clearance continue through ECS grid/surface data.

Bake requirements:

- Dense-city readiness fails if any prohibited physics component exists beneath either generated ownership root.
- Operation-map readiness fails if any prohibited physics component exists in the source map hierarchy or runtime binding scene.
- Build validation scans the dependency closure of production operation-map scenes and generated presentation scenes. Imported third-party assets that are not part of the build closure do not fail this gate.
- Tests must cover inactive objects, nested prefab instances, removed-component prefab overrides, generated SubScene authoring, baked entity-scene dependencies, and the frozen rollback static-presentation set while it exists.

## 8. Hand-Authored Content And Override Contract

Persistent map edits must not be made inside the disposable generated ownership roots unless losing them during regeneration is intentional.

`AuthoredCityOverrides` owns:

- persistent handmade buildings and props;
- deliberate removal/exclusion regions;
- replacement landmarks;
- mission-specific objectives and staging areas;
- authored roads or district corrections;
- typed spawn, deployment, camera, runway, helipad, and objective anchors;
- persistent authored ECS buildings and legacy interactive production-building/vehicle placements.

Generation behavior:

1. Validate that the current scene is `opmap_skirmish_desert_base_01` and that exactly one `OperationMapSceneView` exists.
2. Capture protected bounds from existing military, handmade, runway, resource, mountain, road, and override owners before deleting generated content.
3. Validate override/exclusion records for finite values, stable ids, grid intersection, and duplicates.
4. Delete only the two marked roots in the accepted generation ownership set.
5. Generate only in cells not reserved by protected or override records.
6. Fail rather than move or suppress existing authored content.
7. Save generation evidence containing the protected-owner counts and overlap result.

Manual edit workflow:

- Use the generated city for broad procedural composition.
- Promote any persistent change into `AuthoredCityOverrides` before regenerating.
- Keep mission/gameplay authoring outside generated presentation.
- Regenerate and run readiness validation after override changes.
- Run Bake All only after generation and readiness pass.

## 9. Architecture And Assembly Contract

No new asmdef is approved.

| Assembly | Responsibility | Allowed changes | Forbidden changes |
|---|---|---|---|
| `Game.Authoring` | passive generated-root and override authoring markers | serialized data only; no updates | scene loading, Addressables, editor API, runtime policy |
| `Game.Editor` | city generation, semantic hierarchy, proxy creation, bake validation, budget reports | editor-only static builders/validators and immutable generation records | player runtime behavior, update loops, runtime generation |
| `Game.Components` | existing unmanaged surface, blocker, grid, map metadata | extend only if current blobs cannot represent required data | Unity objects, managed collections, Addressables |
| `Game.Rendering` | Entities Graphics integration plus existing compatibility static-presentation metadata | reuse shared render data and current rendering contracts | mission policy, map selection, loading lifecycle |
| `Game.Composition` | existing runtime binding/SubScene loading and presentation-kind selection | consume the accepted entity scene/runtime binding output; retain the existing streamer only for maps explicitly using `StaticSceneChunks` | city generation, source hierarchy inspection, another streamer |
| `Game.Configs` | existing operation-map definition and small immutable metadata references | per-map bake settings only when required | concrete rendering dependencies or hot runtime policy |
| `Game.Tests.Editor` | generation, ownership, deterministic, bake, build-layout, architecture tests | focused test fixtures | production behavior hidden in tests |
| `Game.Tests.PlayMode` | runtime bind, entity readiness, rendering, teardown, and allocation evidence | production-route probes | alternate runtime path |

Assembly direction remains:

```text
Game.Editor -> Game.Authoring / Game.Components / Game.Configs / Game.Rendering / Game.Composition
Game.Composition -> Game.Rendering / Game.Runtime / Game.Components / Game.Configs
Game.Rendering -> Game.Components / Game.Configs
Game.Runtime -> Game.Components / Game.Configs / Game.Rendering.Contracts
```

Runtime assemblies must never reference `Game.Editor`.

## 10. Existing Types To Reuse Or Extend

| Existing type | Kind / assembly | Required disposition |
|---|---|---|
| `DenseMiddleEasternCityEditModeBuilder` | internal static editor class / `Game.Editor` | Extend generation so every creation call receives explicit presentation and bake-record ownership. Split responsibilities into new bounded editor collaborators before this file grows further. |
| `RuntimeCityRAndDEditModeBuilder` | internal static editor class / `Game.Editor` | Keep as the editor command coordinator. It creates/replaces only the two marked roots in the generation ownership set and invokes validation after generation. |
| `RuntimeCityRAndDMapViewEditor` | sealed custom Editor / `Game.Editor` | Keep buttons thin. `Build Giant Dense City` calls the coordinator; no bake logic in inspector GUI. |
| `MapBakeGroupAuthoring` | sealed passive MonoBehaviour / `Game.Authoring` | Reuse for `IgnoredDecoration`, `Terrain`, `Road`, `Bridge`, `Ramp`, and `Blocker` ownership. Do not add dense-city-specific duplicate role components. |
| `MapSurfaceAuthoringEditor` | static editor class / `Game.Editor` | Continue consuming explicit nearest-group ownership. Do not add name detection. |
| `MapSurfaceBakeSystem` | sealed editor class / `Game.Editor` | Continue spatially indexed surface baking. Feed simplified proxy meshes, not detailed city art. |
| `OperationMapDefinition` | sealed ScriptableObject / `Game.Configs` | Add explicit presentation kind. Require static manifest/legacy placements only for `StaticSceneChunks`; require the authored SubScene and reject static chunk ownership for `EntityScene`. |
| `OperationMapCanonicalPresentationMode` | byte enum / `Game.Rendering` | Add `EntityScene` for renderer-free binding scenes whose visual ownership is the loaded map SubScene rather than static chunk scenes. |
| `OperationMapCurrentMapBaker` | public static editor class / `Game.Editor` | Add existing-map migration/readiness before dense-city readiness, surface, entity baking, and Addressables build. Preserve transactional report behavior. |
| `StaticMapPresentationBaker` | public static editor class / `Game.Editor` | Freeze current 514-scene output as rollback evidence during migration. In accepted `EntityScene` mode, skip source baking and verify zero production Addressables/build ownership rather than regenerating chunks. |
| `OperationMapRuntimeBindingSceneBuilder` | public static editor class / `Game.Editor` | Generate the thin renderer-free runtime binding scene in `EntityScene` mode and bind the map SubScene. Never copy either authoring hierarchy. |
| `OperationMapAddressablesLayoutBuilder` | public static editor class / `Game.Editor` | Package runtime binding, map SubScene entity data/dependencies, metadata, and surface. In `EntityScene` mode, remove static manifest/chunk labels and legacy placement references from production ownership. |
| `OperationMapSceneView` | sealed passive MonoBehaviour / `Game.Composition` | Validate presentation-kind-specific references. `EntityScene` accepts empty legacy roots/configs and requires the protected entity-presentation root in the referenced SubScene. |
| `UnitHealth` | unmanaged `IComponentData` / `Game.Components` | Reuse for generated building current/max health. Do not create a parallel managed health model. |
| `UnitDestroyedVisualReference` | unmanaged `IComponentData` / `Game.Components` | Reuse the alive/destroyed render-root entity references and visible scales. |
| `UnitDestroyedVisualSystem` | Burst `ISystem` / `Game.Runtime` | Reuse initialization of intact/destroyed entity visual state. Extend only through general ECS data, never through building GameObjects. |
| `UnitGridAuthoring.Baker` | existing Baker pattern / `Game.Authoring` | Use as the reference pattern for converting intact/destroyed GameObject authoring hierarchies into render entities. Do not attach `RuntimeBuildingEntityLink`. |
| `RuntimeBuildingEntityLink` and `BuildingDestroyedVisualPresentationSystemHelper` | legacy managed building presentation / `Game.Runtime` | Retain only as rollback/migration code until existing map buildings pass ECS parity. No existing or generated map-authored building may use this path after cutover. |
| `OperationMapSceneLoadingSceneSystemHelper` | managed composition boundary / `Game.Composition` | Branch on presentation kind: load/await entity SubScene ownership and do not request a static manifest/streamer for `EntityScene`. |
| `StaticMapPresentationStreamer` | managed presentation boundary / `Game.Composition` | Remain available for other presentation kinds but stay unbound for `opmap.skirmish.desert_base_01` after ECS cutover. |
| `StaticMapPresentationManifest` | sealed ScriptableObject / `Game.Rendering` | Preserve the current map artifact only as rollback evidence after cutover; it is not an `EntityScene` content requirement. |

## 11. Approved New Types

Names below are normative. A different name, kind, file, or assembly requires updating this tracker before implementation. No new type may use `Manager`, `Controller`, `Facade`, `Service`, `Provider`, `Runtime`, `Utility`, or a bare non-ECS `System` suffix.

| Type | C# kind | Namespace / assembly | Planned file | Responsibility |
|---|---|---|---|---|
| `OperationMapPresentationKind` | public byte enum | `Game.Configs` / `Game.Configs` | `Assets/Game/Scripts/Configs/OperationMapPresentationKind.cs` | Closed `StaticSceneChunks` and `EntityScene` content ownership modes used by definition, build, validation, and loading policy. |
| `OperationMapEntityPresentationRole` | public byte enum | `Game.Authoring` / `Game.Authoring` | `Assets/Game/Scripts/Authorings/OperationMapEntityPresentationRootAuthoring.cs` | Closed authored `GameplayBuildings`, `GameplayVehicles`, and `RenderOnly` role identities. |
| `OperationMapEntityPresentationRootAuthoring` | `sealed MonoBehaviour`, no update methods | `Game.Authoring` / `Game.Authoring` | same file | Marks the protected authored ECS presentation root with operation-map id, schema, and deterministic migration hash. |
| `OperationMapEntityPresentationMigrationRecord` | internal readonly struct | `Game.Editor` / `Game.Editor` | `Assets/Game/Scripts/Editor/OperationMapEntityPresentationMigrationEditor.cs` | Stable source identity, approved role, transform, prefab/asset identity, component disposition, and destination identity for one existing map owner. |
| `OperationMapEntityPresentationMigrationEditor` | internal static editor class | `Game.Editor` / `Game.Editor` | same file | Creates and validates a candidate SubScene migration transaction without mutating accepted source/static rollback artifacts before cutover. |
| `OperationMapEntityPresentationReadinessValidator` | public static editor class | `Game.Editor` / `Game.Editor` | `Assets/Game/Scripts/Editor/OperationMapEntityPresentationReadinessValidator.cs` | Fail-closed existing-map entity ownership, parity, duplicate-asset, legacy-spawn, manifest retirement, and rollback validation with menu/batch entry points. |
| `DenseCityGeneratedRootRole` | public enum | `Game.Authoring` / `Game.Authoring` | `Assets/Game/Scripts/Authorings/DenseCityGeneratedRootAuthoring.cs` | Closed `MapBakeSource` and `EntityPresentationSource` ownership roles. |
| `DenseCityGeneratedRootAuthoring` | `sealed MonoBehaviour`, no update methods | `Game.Authoring` / `Game.Authoring` | same file | Marks one scene-scoped root in the disposable generation set and stores role, shared generation id, generator schema/version, seed, and deterministic generation hash. No scene search or policy. |
| `DenseCityAuthoredOverrideAuthoring` | `sealed MonoBehaviour`, no update methods | `Game.Authoring` / `Game.Authoring` | `Assets/Game/Scripts/Authorings/DenseCityAuthoredOverrideAuthoring.cs` | Marks persistent override/exclusion roots with stable id, local center/size, and exclusion flags without colliders. |
| `DenseCityPresentationCategory` | internal enum | `Game.Editor` / `Game.Editor` | `Assets/Game/Scripts/Editor/MapPrototypes/DenseCityGenerationRecords.cs` | Closed presentation categories used at the generation call site; never inferred from names. |
| `DenseCitySurfaceRecordKind` | internal enum | `Game.Editor` / `Game.Editor` | same file | Closed terrain, road, bridge, ramp, and blocked proxy kinds. |
| `DenseCityRecordIdentity` | internal readonly struct | `Game.Editor` / `Game.Editor` | same file | Generator schema, seed, district id, kind, deterministic sequence, source GUID/local id, and stable key. |
| `DenseCityBuildingBakeRecord` | internal readonly struct | `Game.Editor` / `Game.Editor` | same file | Building transform, footprint, foundation, blocker, frontage, presentation, and gameplay disposition. |
| `DenseCitySurfaceBakeRecord` | internal readonly struct | `Game.Editor` / `Game.Editor` | same file | Surface kind, deterministic polygon/rectangle geometry, elevation, movement mask, layer, and chunk identity. |
| `DenseCityPresentationBakeRecord` | internal readonly struct | `Game.Editor` / `Game.Editor` | same file | Prefab/mesh/material identities, transform, ECS gameplay/render-only disposition, shadow policy, batching eligibility, LOD importance, and stable spatial identity. |
| `DenseCityGenerationRecordSet` | internal sealed editor-lifetime class | `Game.Editor` / `Game.Editor` | same file | Owns pre-sized, stable-ordered record lists for one editor generation and clears them before disposal. It is never serialized into or referenced by player runtime code. |
| `DenseCitySemanticHierarchyBuilder` | internal static editor class | `Game.Editor` / `Game.Editor` | `Assets/Game/Scripts/Editor/MapPrototypes/DenseCitySemanticHierarchyBuilder.cs` | Creates and validates the generated hierarchy and its `MapBakeGroupAuthoring` roles. Does not generate districts. |
| `DenseCityPhysicsComponentStripper` | internal static editor class | `Game.Editor` / `Game.Editor` | `Assets/Game/Scripts/Editor/MapPrototypes/DenseCityPhysicsComponentStripper.cs` | Removes prohibited physics components from newly instantiated instance hierarchies and reports counts. Never edits shared assets. |
| `DenseCitySurfaceProxyBuilder` | internal static editor class | `Game.Editor` / `Game.Editor` | `Assets/Game/Scripts/Editor/MapPrototypes/DenseCitySurfaceProxyBuilder.cs` | Converts generation records into simplified terrain/road/bridge/ramp/blocker proxy meshes grouped by spatial chunk and role. |
| `DenseCityBakeReadinessValidator` | public static editor class | `Game.Editor` / `Game.Editor` | `Assets/Game/Scripts/Editor/DenseCityBakeReadinessValidator.cs` | Fail-closed validation for root identity, ownership, colliders, mixed roles, bounds, records/proxies, protected content, and deterministic hashes. Exposes menu and batch entry points. |
| `DenseCityPresentationBudgetValidator` | public static editor class | `Game.Editor` / `Game.Editor` | `Assets/Game/Scripts/Editor/DenseCityPresentationBudgetValidator.cs` | Produces deterministic authoring/entity/render-batch/mesh/material/triangle/shadow/entity-scene-size reports and enforces accepted Android budgets. |
| `OperationMapBuildingAuthoring` | `sealed MonoBehaviour` with nested `Baker`, no update methods | `Game.Authoring` / `Game.Authoring` | `Assets/Game/Scripts/Authorings/OperationMapBuildingAuthoring.cs` | Serializes stable id, faction, health, footprint, destruction/blocker policy, and intact/destroyed render roots. Recursively bakes each root's shell and attached roof/interior/shop/tent prop descendants into the same entity visual state; bakes only unmanaged ECS data and render-entity references. |
| `OperationMapBuildingBlockerPolicy` | public byte enum | `Game.Components` / `Game.Components` | `Assets/Game/Scripts/Components/OperationMapBuildingComponents.cs` | Closed blocker disposition. Initial production value is `RubbleRemainsBlocked`; passable destruction remains rejected until dynamic-grid ownership is implemented. |
| `OperationMapBuildingComponent` | unmanaged `IComponentData` | `Game.Components` / `Game.Components` | `Assets/Game/Scripts/Components/OperationMapBuildingComponents.cs` | Stable map-building identity and deterministic destruction/blocker policy. No Unity object, string, or managed collection. |
| `OperationMapBuildingDestroyedComponent` | unmanaged tag `IComponentData` | `Game.Components` / `Game.Components` | same file | Marks completed destruction so the transition executes once. |
| `OperationMapBuildingDestructionSystem` | Burst-compatible `ISystem` | `Game.Runtime` / `Game.Runtime` | `Assets/Game/Scripts/Systems/OperationMapBuildingDestructionSystem.cs` | Observes `UnitHealth`, applies the approved blocker disposition, and records destroyed state without GameObject creation/destruction. Resident mode switches baked entity visuals; accepted virtualized semantics emit the bounded canonical state transition consumed by the fixed render-slot path. |
| `OperationMapRenderMaterialBaseColorBakingSystem` | bake-only `ISystem` | `Game.Rendering` / `Game.Rendering` | `Assets/Game/Scripts/Rendering/Baking/OperationMapRenderMaterialBaseColorBakingSystem.cs` | After Entities Graphics renderer conversion, writes each accepted/generated operation-map render entity's exact linear authored `_BaseColor` into `URPMaterialPropertyBaseColor` before EntityScene serialization. It performs no player-runtime work or structural changes. |

The original resident-mode runtime addition is limited to presentation-kind selection, ECS building state, and its Burst destruction transition. In `ResidentEntities`, all persistent existing/generated map rendering uses Entities Graphics data produced by baking; no custom per-frame instance renderer, managed registry, GameObject pool, or new scene streamer is approved. The accepted `VirtualizedProxyPool` representation uses fixed ECS render slots and Burst assignment systems; it still forbids a managed registry, GameObject pool, runtime entity spawn/despawn loop, or new scene streamer. The existing static-presentation streamer remains available for `StaticSceneChunks` maps but is unbound from this map after an authorized cutover.

## 12. Migration And Generation-Time Data Contract

### 12.0 Existing Map Migration Record

Every accepted existing owner requires a committed deterministic disposition containing:

- repository-relative source scene and stable serialized object identity;
- current authoritative owner and approved `OperationMapEntityPresentationRole`;
- source prefab GUID/local id or mesh/material identities;
- exact world/local transform and hierarchy dependency;
- gameplay components, scripts, lights, animation, cross-object references, and collider removal disposition;
- destination SubScene path and stable destination identity;
- building/vehicle placement-config identity when applicable;
- expected ECS root/render-child/archetype ownership;
- rollback source/static-manifest/chunk identity;
- explicit decision owner for every mixed or unresolved case.

The migration cannot classify through object names, prefab filenames, material names, or renderer shape. Mixed/unresolved rows block candidate publication.

### 12.0A Editor-To-Runtime Transform Parity Contract

The current static bake/Addressables path is known to allow model positions at runtime to differ from their accepted Editor placements. The EntityScene migration must fix this defect rather than reproduce it. Visual similarity alone is not acceptance evidence.

For every migrated or generated gameplay/render-only presentation owner, Bake All must emit one deterministic transform-parity row keyed by stable source identity. Each row contains:

- authoritative accepted Editor source world matrix and local matrix;
- source parent stable identity and the ordered parent transform chain needed to explain the world matrix;
- prefab GUID/local id, renderer-relative transform, and mesh pivot/bounds identity;
- candidate SubScene authoring world/local matrices after transactional copy or generation;
- baked ECS root/render-child matrices, including `LocalTransform`, `Parent`, and `PostTransformMatrix` contributions where present;
- Addressables-loaded runtime world matrix after entity-scene readiness and one settled frame for resident rows; virtualized rows instead compare the complete immutable logical record and every required materialized proxy sample;
- position, rotation, scale, and matrix residuals for each boundary;
- an explicit reason code for any approved non-visual helper/proxy that is exempt from renderer parity.

Required default tolerances are position `<= 0.001 m`, rotation `<= 0.01 degrees`, per-axis scale `<= 0.0001`, and world-matrix element residual `<= 0.0001`. A larger tolerance or intentional transform change requires a named, stable-identity exception in this tracker with before/after evidence; no broad category exception is allowed.

Validation must fail closed when:

- a source row has zero or multiple candidate/baked/runtime matches;
- hierarchy flattening changes the resulting world matrix;
- a nested prefab, negative/non-uniform scale, pivot, or `PostTransformMatrix` contribution is lost;
- a building/vehicle legacy placement config or startup system applies a second offset after ECS load;
- ground snapping, terrain sampling, floating-origin logic, or runtime normalization moves permanent presentation entities;
- an Addressables-loaded entity differs from the same candidate baked directly in Editor;
- a renderer bounds center/corners move beyond tolerance even when the owning root matrix appears equal.

The validator must compare matrices and transformed renderer bounds numerically, then capture fixed-camera screenshots as secondary visual proof. `ResidentEntities` compares every loaded render entity directly. The accepted `VirtualizedProxyPool` semantics require complete immutable logical-row parity plus exact materialized-proxy parity for every accepted canonical envelope and intact/destroyed state as defined by `dense_city_virtualized_render_proxy_android_60fps_implementation_tracker.md`; off-camera lack of a proxy is valid only after logical parity passes. The current accepted evidence covers `61,925` logical rows, `14,592` resident exceptions, `29,184` materialized samples, and zero packed eligible source rows at database content/order hashes `bfb350f0...96e` / `a43040ee...318`. Production still cannot flip to `EntityScene`, and rollback static presentation cannot be retired, until the separately authorized cutover and exact cutover-revision validation pass.

Every generated placement must emit explicit records before visual realization:

### 12.1 Common Record Identity

- generator schema version;
- deterministic seed;
- stable district id;
- stable record kind;
- deterministic sequence index within kind/district;
- source prefab GUID and local file id when applicable;
- world transform encoded with invariant finite values;
- presentation category;
- gameplay-grid relationship: inside, intersecting, or outside;
- protected-area decision and reason code;
- stable hash input excluding timestamps, instance ids, absolute paths, and editor session data.

### 12.2 Building Record

- footprint center, half extents, orientation, minimum/maximum height;
- presentation prefab identity and visual scale;
- foundation terrain patch dimensions;
- blocker footprint dimensions and clearance margin;
- damageable/static disposition, max health, faction, and destroyed-blocker policy;
- intact and destroyed visual-root identities;
- stable identities and state ownership for every attached roof, interior, shop, tent, sign, awning, lamp, furniture, and utility visual; each belongs to exactly one building and exactly one visual state;
- frontage/road relationship;
- map-grid inclusion.

### 12.3 Surface Record

- role: terrain, road, bridge, ramp, or blocked;
- polygon/rectangle vertices in deterministic winding order;
- layer id and movement mask;
- minimum/maximum elevation and slope contract;
- source stable key;
- chunk coordinates.

### 12.4 Presentation Record

- mesh/prefab identity;
- material identities and approved overrides;
- transform;
- shadow policy;
- Entities Graphics batching eligibility;
- near/far importance class;
- stable spatial cell used for diagnostics and optional future entity-section analysis;
- renderer exclusion reason when not baked.

Records are editor-transition data. They do not become managed runtime lists. In `ResidentEntities`, all generated presentation records become baked entity data in the map SubScene. In accepted `VirtualizedProxyPool`, eligible records become one immutable unmanaged blob plus fixed ECS render slots while canonical gameplay entities stay resident; surfaces/blockers remain existing immutable map metadata. Generated ECS buildings do not become `MapBuildingPlacementConfigEntry` objects, and generated render-only placements do not become static-presentation source records.

## 13. Proxy Geometry Contract

- Proxy geometry must be deterministic and generated from records, not renderer bounds rediscovered after generation.
- Detailed prefab meshes are forbidden as `Blocker` sources.
- Building blockers use footprint rectangles or reviewed convex outlines, not wall-by-wall mesh triangles.
- Adjacent coplanar road and terrain rectangles are merged within one spatial bucket when semantic metadata matches.
- Proxy meshes are partitioned by role, layer id, movement mask, and chunk.
- A single proxy mesh must not cross operation-map ownership or incompatible movement masks.
- Proxy meshes carry `MeshFilter` only. They have no `MeshRenderer`, collider, Rigidbody, Animator, AudioSource, particle system, or script other than the owning bake-group marker on an ancestor.
- Generated proxy asset paths are map-scoped and transaction-owned. Candidate output is validated before replacing accepted output.
- Mesh vertex/index format is selected deterministically; use 16-bit indices where the partition permits it.
- Proxy output must be idempotent and byte-stable across two runs from unchanged input.

## 14. ECS Presentation Optimization Contract

The complete current operation-map runtime representation is ECS-first:

- existing and generated damageable buildings: map SubScene gameplay entities rendered by Entities Graphics;
- existing and generated vehicles: current ECS unit/vehicle entity rendering path;
- existing and generated trees, props, vegetation, modular infrastructure, roads, sidewalks, bridges, mountains, terrain, water, and background pieces: map SubScene render-only entities;
- transient explosion/smoke VFX: existing pooled managed VFX boundary where required;
- production static-presentation GameObject chunks for this map: none after accepted cutover.

All map visual entities load once with the map SubScene and remain loaded until map teardown in `ResidentEntities` mode. Under the accepted `VirtualizedProxyPool` semantics, the immutable placement/state database, named resident exceptions, and all fixed proxy slots load/unload once; only logical-to-slot assignment and enable state change with the camera envelope. The Samsung acceptance proves this representation meets the amended 54-FPS-class contract without camera-driven scene loading. The exact implementation and evidence remain owned by `dense_city_virtualized_render_proxy_android_60fps_implementation_tracker.md`; production selection remains separately gated.

### 14.1 ECS Building Runtime Contract

- Existing and generated building GameObjects exist only in the editor map SubScene as selectable authoring inputs. The player consumes Unity entity-scene data produced by baking; it never loads those authoring GameObjects.
- Each building root entity owns stable map identity, `LocalTransform`, `UnitHealth`, faction, grid cell/footprint, and `OperationMapBuildingComponent` destruction/blocker policy.
- In `ResidentEntities`, intact and destroyed model hierarchies bake to render entities and their root references/state select the visible hierarchy. In accepted `VirtualizedProxyPool`, eligible intact/destroyed hierarchies bake to immutable prototype/placement recipes keyed to the canonical building state owner; fixed proxy slots materialize only the currently required recipe.
- In `ResidentEntities`, every building-attached prop is a descendant render entity of exactly one visual root. In accepted `VirtualizedProxyPool`, the same exclusive intact/destroyed ownership is encoded in the compound prototype/placement recipe and canonical state-owner index. Intact roof/interior/shop/tent props must not be separately parented, independently streamed, or classified as global render-only props. Destroyed-state debris or replacement props belong only to the destroyed state.
- `OperationMapBuildingDestructionSystem` performs a one-time Burst-compatible state transition when health reaches zero: mark `OperationMapBuildingDestroyedComponent`, apply the explicit blocker policy, and either switch the resident entity hierarchy or publish the bounded virtualized presentation-state change. Both paths must hide the complete intact presentation including all attached props and show only the complete destroyed presentation.
- If an intact attached prop has no destroyed equivalent, it simply disappears with the intact hierarchy. No orphan visual may remain at the building position. A genuinely independent gameplay object requires its own explicit entity identity and cannot be inferred from proximity.
- The default blocker policy is `RubbleRemainsBlocked`: the precomputed ECS blocker footprint is unchanged after destruction. A passable-after-destruction policy is forbidden until it has a stable building-to-dynamic-grid mapping and focused pathing tests.
- Persistent buildings do not use `RuntimeBuildingEntity`, `RuntimeBuildingEntityLink`, `BuildingVisualSystem`, `BuildingDestroyedVisualPresentationSystemHelper`, `Object.Instantiate`, or `Object.Destroy`.
- Temporary explosion, fire, and smoke may cross the existing pooled VFX presentation boundary. Those effects do not own building health, identity, transforms, or persistent destroyed state.
- Building render entities share baked mesh/material assets. No mesh, material, or texture is cloned per placement.
- All building simulation entities remain available to targeting, damage, save/load, and scenario systems even when offscreen. Camera visibility affects rendering only.
- The accepted remedy for unacceptable whole-map render residency is the immutable logical database plus fixed pre-baked proxy pool, not a camera-driven EntityScene/Addressables/render-section loader. Simulation state remains resident, camera travel starts zero map-content operations, and canonical intact/destroyed state is applied before a required recipe becomes visible.

### 14.2 Render-Only Entity Contract

- In `ResidentEntities`, existing and generated permanent visual placements are baked directly into entity scene chunks. In accepted `VirtualizedProxyPool`, eligible placements bake into immutable logical data and a fixed preallocated ECS render-slot set. Neither mode instantiates from a GameObject pool, and proxy assignment never becomes a gameplay system.
- Repeated placements share imported mesh, material, texture, and approved render configuration assets. Per-placement runtime data is limited to entity identity required by Unity, transform/hierarchy, render bounds, LOD data, and Entities Graphics references.
- Modular roads, sidewalks, bridges, trees, rocks, lights, props, vegetation, and skyline modules remain separate placements when transforms permit asset sharing. Do not combine them merely to reduce authoring object count.
- Unique continuous terrain/canal/road geometry may be deterministically combined into bounded entity-render meshes when it reduces renderer count without duplicating source assets or creating a whole-city mesh.
- Render-only entities receive no health, faction, target, occupancy, managed component, authoring marker, or update behavior unless their semantic record explicitly promotes them to gameplay ownership.
- Precomputed ECS surface/blocker data remains authoritative. Render entities do not provide collision, raycast, navigation, or placement geometry.
- The player must contain no map visual GameObject hierarchy, map-visual chunk scene handle, or current-map production entry from `StaticMapPresentationManifest` after cutover.

### 14.3 Shared Asset And Package Contract

- A repeated prefab placement must reference the same baked mesh/material assets as every compatible placement; no per-placement mesh/material/texture copy is allowed.
- Addressables Build Layout must report each existing/generated map art dependency in one accepted local production ownership location. Rollback static artifacts must not be labeled, cataloged, or bundled with the entity-scene package; any duplicate production dependency bytes fail readiness.
- The generated SubScene source `.unity` file is editor authoring input. Runtime packages contain its entity scene output and referenced art dependencies, not its GameObject YAML hierarchy.
- Entity scene data, shared dependencies, and catalog entries are bundled locally with the application for the initial release and require no network access.
- Reintroducing a static-presentation chunk for this map requires a tracker amendment naming the unique asset, measured entity-residency failure, expected memory saving, ownership path, unload policy, and Android validation. Repetition alone never justifies an exception.

Required source policy:

- Include every existing renderer beneath `AuthoredOperationMapEntityPresentation` and every generated renderer beneath `EntityPresentationSource`; prove each bakes to exactly one gameplay or render-only entity hierarchy.
- Exclude authored/generated entity-presentation roots and their dependencies from static-presentation source collection/manifest ownership.
- Exclude bake proxies, authoring gizmos, override volumes, gameplay placement previews, and runtime binding objects.
- Exclude every prohibited physics component from generated authoring and baked dependency closure.
- Preserve shared mesh/material/texture references. Do not duplicate third-party binary art into entity scene outputs.
- Keep repeated mesh/material combinations Entities Graphics batching-compatible; do not clone mesh/material assets per entity.
- Merge unique road, shoulder, foundation, terrain, or ground geometry only when measured renderer reduction exceeds the loss of repeated-asset sharing.
- Do not combine the entire city into one giant mesh.

LOD/impostor policy:

- Source `LODGroup` or approved entity LOD authoring must bake to Entities Graphics-compatible LOD data and pass focused conversion tests.
- Near tier keeps repeated entity meshes and required hero detail.
- Mid tier removes rooftop props, wires, small vegetation, and minor facade detail.
- Far tier uses entity-rendered district aggregate meshes or impostors with no shadow casting.
- Exactly one visual tier is active for a placement/district at a time.
- LOD/impostor generation must preserve deterministic bounds, hashes, shared material ownership, and intact/destroyed building state.
- LOD/impostor behavior is not accepted until real Android camera traversal proves transitions, batching, visual continuity, and memory residency.

## 15. Authoring Scene And Runtime Package Separation

The following paths retain their current ownership:

- Authoring source scene: `Assets/Game/Scenes/OperationMaps/Skirmish/opmap_skirmish_desert_base_01.unity`
- Thin runtime binding output root: `Assets/Game/GeneratedOperationMaps/RuntimeBinding/opmap.skirmish.desert_base_01`
- Frozen rollback-only static presentation root after cutover: `Assets/Game/GeneratedStaticMapPresentation/OperationMaps/opmap/skirmish/desert_base_01`

Rules:

- The authoring scene is an editor source and must not be an Addressable runtime source-scene entry.
- `OperationMapAddressablesLayoutBuilder.SourceScenePath` remains `OperationMapRuntimeBindingSceneBuilder.OutputPath`.
- The runtime binding scene remains renderer-free in `EntityScene` mode.
- The map SubScene owns every existing/generated map gameplay and render-only entity.
- The static manifest, integrity ledger, and 514 scenes remain frozen rollback artifacts on disk until closeout; they are absent from current-map production Addressables labels, catalogs, dependencies, Android build scenes, and runtime loading.
- Map surface, minimap raster, definition, runtime binding, and entity SubScene data are package-owned according to the revised `EntityScene` Addressables contract. Legacy placement configs remain migration evidence, not runtime content references.
- Build Layout validation must prove that source-scene GameObject YAML is absent, entity scene data is included once, shared art dependencies are not duplicated, and no map visual hierarchy appears in runtime binding or static chunk scenes.

## 16. One-Button Bake Sequence

`Game/Operation Maps/Bake Current Map (All)` remains the authoritative command. Its accepted sequence becomes:

1. Save/confirm modified scenes using current editor behavior.
2. Open the operation-map authoring scene and referenced map SubScene transactionally.
3. Run `entity-presentation-readiness` and validate complete existing-map migration/parity with zero mixed or unresolved owners.
4. Run `dense-city-readiness` when a generated ownership set exists; otherwise record an explicit not-generated state.
5. Validate legacy 451/29 placement parity against authored ECS entities without publishing runtime placement spawns.
6. Run `surface-data` from existing and generated semantic proxy groups.
7. Validate zero prohibited physics components and save accepted authoring/SubScene candidates.
8. Close source scenes, force entity-scene baking, and validate expected gameplay/render-only/render-child entities.
9. Validate the frozen static manifest/chunk rollback set without regenerating it.
10. Run `presentation-budget` across all existing/generated entity/render/batch/dependency data.
11. Run `runtime-definition` with `OperationMapPresentationKind.EntityScene` and no required static manifest/legacy placement runtime references.
12. Run `minimap-raster`.
13. Run `spatial-bindings`.
14. Run `runtime-binding-scene` in `EntityScene` mode and validate renderer-free/collider-free output.
15. Run `addressables-layout`, removing current-map static manifest/chunk and legacy placement runtime entries.
16. Run `local-addressables` and inspect Build Layout/entity-scene/shared-art dependencies for single ownership and zero current-map static scenes.
17. Publish a passed report only after every stage succeeds.

Failure behavior:

- Fail before mutating accepted generated output when readiness fails.
- Existing transaction owners restore prior definition, Addressables layout, runtime binding, SubScene candidate, manifest/integrity/chunk rollback set, and metadata on publication failure.
- Never leave a stale successful report after failure.
- Restore the editor scene setup in `finally`.
- Never delete the authoring source or authored override root.
- A failed bake does not silently load prior stale city output as current evidence.

## 17. Runtime Loading And Teardown

No new runtime lifecycle is introduced.

Accepted load order:

1. Resolve scenario and `OperationMapDefinition`.
2. Load the thin runtime binding scene through local Addressables.
3. Resolve exactly one `OperationMapSceneView`.
4. Require `OperationMapPresentationKind.EntityScene` and load/await the map SubScene containing all existing/generated gameplay and render-only entities.
5. Bind map surface and immutable operation-map metadata.
6. Validate expected gameplay/render-only/render-child entity counts, stable identities, vehicle state, and intact building visual state.
7. Assert no current-map static manifest or presentation streamer binding exists.
8. Publish readiness only after required metadata and entity scene data pass.
9. Begin gameplay.

Accepted teardown order:

1. Clear gameplay readiness and stop map-dependent work.
2. Clear map-owned ECS entities, including all existing/generated render-only and intact/destroyed building visual entities, buffers, anchors, and blobs through their accepted owners.
3. Unload/release SubScene content.
4. Unload/release the thin runtime binding scene handle.
5. Release definition/map asset handles.
6. Verify no stale map id, entity, scene, static-presentation handle, or retained authoring reference remains.

The authoring scene never participates in runtime load or teardown.

## 18. Determinism And Hashing

The following must be deterministic for unchanged source/config/seed:

- generated hierarchy paths;
- generated record ordering;
- protected-owner counts and decisions;
- prefab selection sequence;
- transforms and material variants;
- semantic role assignment;
- proxy mesh vertices and indices;
- generated-root content hash;
- surface payload hash;
- authored migration record/source/destination hashes;
- authored/generated entity presentation source hash, entity scene dependency identity, and shared-art ownership set;
- frozen rollback manifest/chunk identities and proof that production Addressables contain none of them;
- runtime binding ledger;
- minimap raster hash;
- Build Layout ownership set.

Forbidden hash inputs:

- wall-clock time;
- absolute workstation/worktree paths;
- Unity instance ids;
- editor session ids;
- unordered dictionary/hash-set enumeration;
- transient progress values;
- random GUID creation during repeat generation.

Two complete generate-and-bake runs from identical input must produce byte-identical accepted artifacts and a clean second `git status` for generated ownership paths.

## 19. Android Performance And Size Gates

No ECS migration or city budget is accepted without baseline and target-device evidence. Phase 0 records the current shipped map baseline first. Existing-map cutover and dense-city acceptance use both absolute device limits from `performance_regression_contract.md` and explicit deltas from that baseline.

Required metrics:

- compressed APK/AAB and installed size;
- operation-map local Addressables bytes by group and bundle;
- duplicate dependency GUIDs/bytes;
- authoring scene exclusion proof;
- runtime binding scene bytes;
- production static manifest/chunk count, required to be zero for this map after cutover, plus frozen rollback bytes reported separately;
- existing/generated gameplay/render-only/render-child entity counts, archetype/entity-chunk counts, shared mesh/material count, and entity-scene bytes;
- SubScene entity integration/GPU upload average, p95, p99, and maximum;
- peak transition memory and retained post-load memory;
- CPU main/render thread average, p95, p99, and maximum;
- GPU frame average, p95, p99, and maximum;
- sustained FPS and thermal behavior;
- draw calls, SetPass calls, visible renderers, triangles, and shadow casters for representative cameras;
- steady-state GC allocation and transition allocation spikes;
- surface bake duration and peak editor memory;
- Addressables build duration and peak editor memory.

Mandatory qualitative gates:

- `0 B/frame` managed allocation from operation-map orchestration after readiness.
- No frame-time spike caused by integrating/uploading thousands of resident building render entities or applying a virtualized camera-envelope rebuild in one frame.
- No existing-map or generated-city source GameObject hierarchy resident at runtime.
- No collider or Rigidbody in production map dependency closure.
- No visible LOD/impostor holes, duplicate buildings, missing roads, or entity-section seams.
- No map-visual scene load/unload or visibility pop caused by camera travel.
- No navigation through buildings, canals, walls, or mountains.
- No blocked roads caused by presentation-only props.
- No expansion of the gameplay grid until missions require and device gates accept it.

Optimization order when a gate fails:

1. Remove accidental source duplication and prohibited components.
2. Verify every repeated visual shares baked mesh/material data and forms expected Entities Graphics batches.
3. Add/fix entity LOD/impostors and reduce shadow casters/small distant detail.
4. Deterministically combine only unique continuous terrain/road geometry where batching cannot help.
5. Reduce city density only after entity representation optimizations are measured.
6. Propose render-only entity section streaming only if measured residency remains unacceptable; never partition simulation state by camera.
7. Propose a static-presentation exception only for named unique heavyweight content with measured memory benefit and no repeated-asset alternative.

Runtime procedural generation is not the fallback for a rendering or packaging failure.

## 20. Progress Summary

Progress is checklist based. Every checkbox counts. Update the count, phase status, and validation log in the same stable change that completes an item.

Current progress: **149 / 149 (100%)**

| Phase | Status |
|---|---|
| Phase 0: Baseline and contracts | Complete |
| Phase 0A: Existing map ECS presentation migration | Complete. Transactional candidate Bake All plus direct and packed runtime matrix/bounds parity and fixed-camera parity are accepted. Exact-Samsung functional evidence confirms canonical authored-building selection, Contractor Tent ownership/health, blocked placement interaction, Recruit production, exact material spending, stable spawned-unit ownership, and visible helicopter/rope/drop delivery. |
| Phase 1: Generated ownership authoring | Complete; protected candidate scene-pair transaction, exact semantic hierarchy, and fail-closed role/scene ownership validation complete |
| Phase 2: Generator semantic output | Complete; building/attachment, infrastructure, canal/park, civic/market, and combined courtyard/detail records replay into the protected candidate hierarchy; generated building recovery, footprint collection, material policy, sealed-output pruning, canal-underpass overlap ownership, and readiness category ownership use explicit records/components with no production name/category inference; deterministic seed/order is guarded as generator schema v1; full-map feature records and exact hierarchy-owner routing pass focused Windows Unity validation |
| Phase 3: Collider-free enforcement | Complete for current production. Stripper coverage and source/candidate gates are complete; every known direct or indirect dense-editor primitive/prefab path strips before further realization; focused stripper and full-map generated-root validation pass on Windows; the separately authorized retirement removed the frozen static package that owned the historical collider-bearing transitive prefabs. |
| Phase 4: Surface and blocker proxies | Complete; deterministic terrain, road, bridge, ramp, canal, building, wall, and rock proxies are validated without physics components |
| Phase 5: Readiness and Bake All integration | Complete; generation, renderer/proxy ownership, protected authored-content identity/overlap, presentation-kind publication ordering, deterministic dense candidate-to-baked-to-Addressables-runtime transform/bounds gates, packed dense fixed-camera parity, and exact two-run generation plus complete Bake All no-op validation pass. |
| Phase 6: ECS presentation optimization | Complete for the accepted production representation. Deterministic authoring geometry, complete existing/generated EntityScene ownership, shared mesh/material preservation, bounded combination policy, baked role/state ownership, exact logical/materialized parity, and fixed render-slot ownership are accepted across eligible Vegetation, Prop, generated Infrastructure, complete House/Shop state families, and supported sub-meter details. Current reconciliation is `76,517` material/submesh rows = `61,925` eligible logical rows + `14,592` resident exceptions; packed output strips `61,783` eligible physical renderers, retains `14,017` unique resident owner/path rows, and owns `40,460` placements, `1,934` cells, four buckets, and `7,784` slots. CityHall and unsupported/transparent policies remain explicit resident exceptions. Shadow/mobile budgets and exhaustive materialization pass; production cutover selects this representation. |
| Phase 7: Runtime package separation | Complete. Production selects the accepted dense EntityScene as the sole map-render presentation owner; production Addressables contain zero current-map static manifest/chunk entries; the clean package and transactional rollback restore pass through child `VRP-103`; child `VRP-104` retires exactly `543` tracked legacy files while preserving the minimap pair and Git-backed rollback tree; the six-root production dependency closure passes; and final package/install/content accounting is accepted. |
| Phase 8: Runtime lifecycle validation | Complete. Packed transform/bounds and dense fixed-camera parity, virtualized camera/state routes, exact return bindings, zero overflow, fail-closed readiness, authored-vehicle movement, resident camera culling, complete unload, post-bind ownership isolation, ECS-only lifecycle, Editor PlayMode validation, failure/reset/retry, zero-operation camera travel, full Bake All determinism, five-view/edge-traversal continuity, and final-package Menu -> Match -> Menu -> Match -> Menu behavior all pass. |
| Phase 9: Android acceptance | Complete for the exact final production APK/source revision `6b4d268b8`. Exact Samsung `SM-S918B` / `R5CTC1J02VB` passes final-package cold launch, two consecutive load/readiness/unload cycles, all nine diagnostics-disabled 120-second representative/performance routes, the amended FPS contract, unchanged frame/CPU/GPU/allocation gates, the final cooled two-minute thermal route at thermal status `0 -> 0`, qualified immutable Android-target transform/bounds parity reconciliation, and representative House/Shop/Contractor Tent/Barracks destruction-family replacement with zero target-owned intact leakage. Blue Autobahn/canal isolation and road-over-water-without-bridge remain separate defects. |
| Phase 10: Documentation and closeout | Complete; author workflow, authored ECS workflow, ownership/rollback, parent evidence, architecture/naming guardrails, production package/device/thermal/rollback acceptance, verified static retirement, dependency closure, package accounting, lifecycle, final route/thermal acceptance, Android parity, destruction-family acceptance, deterministic evidence indexing, and generated-artifact ownership closeout all pass. |

## 21. Implementation Checklist

### Phase 0: Baseline And Contracts

- [x] Record a clean post-clear authoring-scene hash, size, hierarchy counts, renderer counts, and existing bake-group counts.
- [x] Record current accepted surface payload, static manifest, chunk count, source count, integrity ledger, minimap, runtime binding, and Addressables Build Layout identities.
- [x] Capture current Android APK/installed size, runtime memory, draw calls, frame timings, GC, load time, and unload time on the target device.
- [x] Confirm the authoring scene is excluded from current Addressables/runtime build ownership.
- [x] Confirm existing military, handmade, runway, road, mountain, resource, placement, and override roots that generation must preserve.
- [x] Approve generated hierarchy names and exact semantic ownership table from this tracker.
- [x] Approve whether city content outside the current grid is presentation-only; default is presentation-only.
- [x] Add focused test fixtures that can build a small deterministic city without opening or modifying the full map scene.

**Exit:** Regressions can be measured against a clean, cleared-city baseline.

### Phase 0A: Existing Map ECS Presentation Migration

- [x] Inventory all current static-presentation sources by repository-relative scene/object identity, prefab GUID/local id, mesh/material identity, transform, components, and current static chunk ownership. Historical pre-clear baseline was `16,542` sources / `514` chunks; post-clear accepted package is measured by the inventory probe.
- [x] Join all current building and vehicle placement identities to their exact authored source objects and reject zero, multiple, reused, mixed, or unresolved matches. Historical pre-clear counts were `451` / `29`; post-clear counts are measured by the inventory probe.
- [x] Classify every existing owner as gameplay building, gameplay vehicle, render-only entity, map metadata/proxy, approved managed boundary, or rejected/unresolved without name-based inference.
- [x] Audit scripts, lights, animation, particles, cross-object references, and source suppression behavior; record an explicit ECS/baked-lighting/VFX/metadata disposition for each non-renderer dependency.
- [x] Prove which current vehicle placements already produce ECS entities/render entities and identify only the missing conversion/duplication cleanup.
- [x] Inventory every managed `RuntimeBuildingEntity`/GameObject dependency required by current authored buildings and define equivalent ECS component/buffer/system ownership before visual cutover.
- [x] Inventory every roof, interior, shop, tent, sign, awning, lamp, furniture, and utility visual attached to each current damageable building; assign each stable identity to exactly one building and one intact/destroyed state without proximity or name inference.
- [x] Add `OperationMapPresentationKind` and set the candidate definition to `EntityScene` only after migration validation succeeds.
- [x] Extend `OperationMapCanonicalPresentationMode`, `OperationMapDefinition`, and content-reference validation for an entity-presented map with no required static manifest or runtime placement references.
- [x] Add `OperationMapEntityPresentationRootAuthoring` and exact protected authored hierarchy roots in the existing map SubScene.
- [x] Add deterministic `OperationMapEntityPresentationMigrationRecord` capture and `OperationMapEntityPresentationMigrationEditor` candidate transaction ownership.
- [x] Move/copy reviewed existing render-only visual authoring into the candidate SubScene while preserving world transforms, prefab/source asset references, hierarchy semantics, and shared art identity.
- [x] Convert all current authored building placements to `OperationMapBuildingAuthoring` ECS ownership with health, faction, footprint, targeting, production/interaction parity where applicable, and intact/destroyed entity visuals.
  - Reopened 2026-08-02 after Android user review and accepted 2026-08-09 from the retained consolidated exact-device proof. The corrected production consumer uses the canonical ECS producer contract, and the checked package at implementation head `8a2643b4e` was exercised on Samsung `SM-S918B` / `R5CTC1J02VB`: `Tent_Contractor` resolved with canonical ownership and `350/350`; occupied-terrain Contractor placement visibly reported `BLOCKED BY ROAD OR BLOCKER`; Recruit spent exactly 25 materials per invocation and increased stable unit/model/source ownership; retained captures show the helicopter, rope, suspended drop, and completed ground spawn. Canonical selection, health/faction/footprint/targeting/destruction data, and the intact/destroyed presentation gates remain covered by the accepted Editor and child Android evidence. No rerun was needed because no contradictory production/interaction evidence exists.
- [x] Convert/verify all current authored vehicle placements through the existing unit/vehicle Baker path and eliminate duplicate source/static/runtime GameObject visuals.
- [x] Remove prohibited physics components from migrated instances without mutating shared third-party prefab assets.
- [x] Bake the candidate SubScene and validate expected gameplay/render-only/render-child entity counts, finite transforms/bounds, archetypes, and zero managed map visual components.
- [x] Prove repeated meshes/materials/textures have one shared package owner and placements contribute only compact entity/transform/render-reference data.
- [x] Add deterministic source-to-candidate-to-baked transform parity records and fail-closed Editor validation for every gameplay/render-only owner, including nested prefab, hierarchy, pivot, negative/non-uniform scale, `Parent`, and `PostTransformMatrix` cases.
  - Stable identity foundation and existing-candidate backfill passed on 2026-07-21: building, vehicle, and render-only candidate populators emit one passive `OperationMapEntityPresentationIdentityAuthoring`, which bakes to unmanaged `OperationMapEntityPresentationIdentity` data containing operation-map id, exact source `GlobalObjectId`, role, and placement index where applicable. The protected candidate contains and bakes exactly 9,544 unique identities. Backfill proved that all 9,090 old render-only candidates had source-world-matrix mismatches because the old root-copy path serialized source-local transforms as candidate-world transforms.
  - Accepted-source-to-candidate repair passed on 2026-07-21: render-only migration now mirrors deterministic transform-only source parent chains and parents each cloned owner with its original local transform, preserving inherited shear and negative/non-uniform scale without approximation. The transaction rebuilt all 9,090 render-only owners and validated owner matrices plus every cloned renderer matrix/world bounds at matrix `0.0001` and bounds `0.001 m` tolerances before save. In-memory ECS bake and all nine Bake All stages remain green at 9,544 identities / 14,212 render meshes / zero non-finite transforms / zero managed map visual companions. Remaining work is deterministic source/candidate/baked matrix and transformed-renderer-bounds report rows for all roles, special baked `Parent`/`PostTransformMatrix` fixtures, and the real Addressables-loaded runtime comparison.
  - Source/candidate/baked owner-matrix manifest landed fail-closed on 2026-07-21: all 9,544 stable identities join exactly once and every accepted-source → candidate → baked owner world matrix passes the `0.0001` residual after explicit ECS `Parent * LocalTransform * PostTransformMatrix` reconstruction. The first bounds pass correctly rejected 938 rows and exposed two defects rather than weakening acceptance: package-generated render entities needed deterministic authored-renderer ownership matching, and candidate vehicles had lost accepted scene-instance child overrides by being rebuilt from prefab defaults.
  - Direct baked visual parity passed twice on 2026-07-21 after repairing those defects. The validator now matches every accepted authored renderer bake entry to its material entity using Entities Graphics mesh/skinned-mesh bounds semantics and deterministic world-matrix/local-bounds keys; a bounded join tolerance is used only to recover package-generated render-entity ownership and never changes the `0.0001` owner-matrix or `0.001 m` transformed-bounds acceptance tolerances. Vehicle migration clones accepted scene instances to preserve child overrides, then reapplies authoritative placement root transforms. Final evidence is 9,544 candidate identities / 9,544 baked identities / 14,209 render meshes / zero rejected rows / zero non-finite transforms / zero managed map visual companions. A full transactional candidate Bake All also passes with production cutover disabled. The Addressables-loaded runtime comparison remains a separate open acceptance item below; Android parity is user-triggered and does not block other Editor work.
  - Runtime-comparison manifest foundation passed on 2026-07-21: parity schema v2 records all 14,209 baked render entities with deterministic world matrices, local bounds, transformed world bounds, and accepted owner identity where the package retains one. The manifest intentionally includes all 315 package-generated rows without an authored owner so the runtime gate must reproduce the complete render-entity multiset rather than silently discarding extras. This is dependency evidence only; the real Addressables-loaded candidate comparison remains open and checklist progress does not advance.
- [x] Capture fixed-camera visual parity for terrain, roads, bridges, buildings, vehicles, mountains, vegetation, props, infrastructure, lighting, and minimap before changing production ownership.
  - Accepted 2026-07-23 for the project-owner-approved `NotGenerated` candidate state: the existing 1280x720 fixed-camera report passes overview, minimap, and three deterministic detail views over `13,691` accepted-source and `13,691` candidate renderers. All ten tracked PNG hashes still match the report, and the current candidate SubScene SHA-256 `31a2638471ed23fa5dfe39c3544454ff79af126b219fa3813de6cbd09c8f4314` matches the fingerprinted packed runtime-content report used by the later 9,544-identity/14,249-renderer matrix and transformed-bounds acceptance. The earlier generated-city condition is obsolete because the project owner explicitly cleared generated-city content; this acceptance does not claim parity for content absent from the accepted candidate. Production remains `StaticSceneChunks`, production cutover remains zero, and Android visual parity remains user-triggered.
- [x] Extend runtime binding/loading/readiness/teardown so `EntityScene` loads the map SubScene once, skips static-manifest/streamer binding, and unloads all map entities once.
- [x] Extend `OperationMapSceneView` and startup validation so empty legacy placement roots/configs are accepted only in validated `EntityScene` mode and cannot trigger managed map visual spawning.
- [x] Extend Bake All with fail-closed entity migration/readiness/bake/budget stages and rollback of source scenes, SubScene, definition, runtime binding, and Addressables layout.
- [x] Build a candidate local Addressables layout containing one runtime binding, one map entity-scene ownership path, map-surface metadata, minimap raster, transitive EntityScene art, zero authoring-derived explicit shared entries, and zero current-map static manifest/chunk/legacy-placement runtime entries. Promote an explicit shared leaf only after built-content layout evidence proves cross-partition use.
  - Dense dependency profile accepted on 2026-07-24: a distinct candidate definition and renderer-free thin runtime binding reference only the protected dense EntityScene candidate plus the accepted map surface and minimap. The exact five planned roots are definition, source scene, EntityScene, surface, and minimap, with zero static-manifest, legacy-placement, presentation, or explicit shared entries. Dense definition, thin binding, and EntityScene GUIDs are pairwise distinct and cannot collide with production or the accepted packed candidate. This does not claim built-content duplication or runtime-load parity; those Phase 7/8 gates remain open.
- [x] Run Editor load, gameplay interaction, building damage/destruction, vehicle behavior, camera traversal, menu return, and two-cycle teardown with zero map visual GameObjects or static handles.
  - Accepted Windows completion 2026-07-25: the schema-9 complete-generated-city package passes exact two-cycle load/readiness/matrix/bounds/fixed-camera/unload, one-time ECS-only generated-building destruction, readiness failure/reset/retry, and the production Menu-to-Match authored-vehicle route. The vehicle resolves a real path and moves with its baked detailed visual while retaining source identity, hierarchy, EntityScene section ownership, zero managed map-visual ownership, and an idle static streamer; normal Menu return removes all scene/section/map metadata. Together with the accepted resident-camera culling and two-cycle production-route fixtures, this covers the complete Editor lifecycle without a map-visual GameObject or static-presentation handle.
- [x] Run Android offline package, size, memory, frame, batching, visual parity, navigation, and two-cycle lifecycle acceptance against the exact candidate revision.
  - Accepted 2026-08-09 from the completed child Phase 9 evidence. The final checked candidate package/source head `f6accf53e` is a `544,011,811`-byte APK with SHA-256 `65a4a57c8014ff99a06151a6adb2b4046a7304bfb1f1b817ad49c764128b452d`, installed only to Samsung `SM-S918B` / `R5CTC1J02VB`. The accepted diagnostics-disabled representative set and final cooled route pass the amended FPS and unchanged frame/CPU/GPU/allocation/correctness gates; retained memory is below the frozen Phase 0 PSS/RSS/graphics artifacts; fixed-slot capacity ends at zero overflow/deficit; visual/state routes prove intact/destroyed materialization and recycling without holes, leakage, popping, stale state, or white screen; retained functional evidence covers normal navigation, selection, placement, production, delivery, and authored-vehicle movement; the offline Menu -> Match -> Menu -> Match -> Menu route independently reloads both EntityScenes and fully unloads twice. Packed sharing evidence retains one shared `RenderMeshArray`, one EntityScene archive, and zero duplicated dependency bytes. Evidence from unaffected predecessor packages remains valid only where the child tracker explicitly proved the later bounded opt-in/closed-popup correction could not execute on that route; the final-package lifecycle, destruction/recycling, and thermal proofs close the current revision.
- [x] After acceptance, remove the tracked static scenes/manifest/integrity artifacts from production labels/build ownership and delete them from tracked generated output only in a separately reviewable cleanup commit with rollback archive/hash evidence.
  - Accepted 2026-08-10 through child `VRP-104` after explicit user/parent authorization. The current frozen package contained `545` tracked files / `269` `.unity` scenes / `42,765,094` bytes (the earlier `514` wording was a pre-regeneration planning count), bound to rollback revision `b9383b6a...` and SHA-256 `8cb615...3764`. Checked preflight passed before mutation. The cleanup removes exactly `543` files / all `269` scenes / `42,628,450` bytes and retains only the unchanged minimap plus meta (`136,644` bytes). Checked postflight proves current EntityScene production, zero production static manifest/chunk entries, the complete Git-backed rollback archive, accepted rollback restore, and accepted production Android evidence. Report: `Design/AgentReports/2026-08-10_dense_city_static_retirement_validation.json`.

**Exit:** The existing physical map is fully presented by SubScene entities, no current-map static scene or managed map visual is loaded, and the dense-city generator can target the same accepted path.

### Phase 1: Generated Ownership Authoring

- [x] Add `DenseCityGeneratedRootAuthoring` and its Unity `.meta` file in `Game.Authoring`.
- [x] Add `DenseCityAuthoredOverrideAuthoring` and its Unity `.meta` file in `Game.Authoring`.
- [x] Add `OperationMapBuildingAuthoring` and bake intact/destroyed authoring roots to entity references using the existing `UnitGridAuthoring.Baker` pattern.
- [x] Make the building Baker recursively include shell and attached-prop descendants under their declared visual root; reject descendants shared across buildings, present under both states, outside both states, or parented to an independent render-only owner.
- [x] Add unmanaged `OperationMapBuildingComponent` and `OperationMapBuildingDestroyedComponent` data in `Game.Components`.
- [x] Add finite-value, stable-id, duplicate-id, size, and scene-ownership validation for all generated/override/building authoring types.
- [x] Add `DenseCitySemanticHierarchyBuilder` in `Game.Editor`.
- [x] Create the exact proxy hierarchy in the operation-map authoring scene and gameplay/render-only entity presentation hierarchy in the existing map SubScene with identity transforms.
  - Accepted 2026-07-24 through a protected candidate-only copy transaction. The operation-map authoring candidate and entity-presentation candidate are independent scene assets copied from the accepted map and current accepted `NotGenerated` entity candidate before the paired generated roots are created. The accepted map, accepted SubScene, current packed candidate, production definition, Addressables ownership, and rollback package remain unchanged. This establishes empty semantic ownership only; no dense-city districts or visuals have been realized yet.
- [x] Add exactly one nearest `MapBakeGroupAuthoring` role owner to each proxy semantic group; do not classify generated render entities as static-presentation inputs.
- [x] Reject duplicate/missing role roots, mismatched generation ids, misplaced override roots, role overlap, and roots outside the active operation-map scene pair.
- [x] Prove regeneration deletes only the two marked role roots and preserves all other objects in both scenes by identity/hash.

**Exit:** One logical disposable generation set spans two explicit scene-scoped roots, one persistent override domain remains protected, and generated building authoring has an explicit ECS bake contract.

### Phase 2: Generator Semantic Output

- [x] Add bounded `DenseCityGenerationRecords` value types and stable ordering rules.
- [x] Refactor building placement to emit ECS-building, foundation, blocker, intact-visual, and destroyed-visual records before realization.
- [x] Emit stable building-owned attachment records for roof/interior/shop/tent props during generation and realize them beneath the declared intact or destroyed root rather than the global render-only prop hierarchy.
- [x] Refactor road generation to emit distinct road, shoulder, terrain-patch, bridge, and ramp records.
- [x] After packed dense-city fixed-camera acceptance, replace the pre-dense-city main Autobahn presentation in the protected candidate with the shared dense-city two-lane road family; preserve its route and gameplay surface semantics, connect both ends and every crossing to the existing generated two-lane road graph, and fail validation on visible model/material discontinuity, overlap, or any open gap.
  - Dependency accepted 2026-07-24: all five canonical Editor views pass against the real packed Addressables-loaded dense candidate in two complete load/capture/unload cycles. This task is now the next dependency-ready Phase 2 item.
  - Accepted 2026-07-24 in the protected candidate only: the two exact legacy Autobahn owners remain as renderer/collider-free stable-identity anchors while 962 adjacent two-lane cells and 146 deterministic connector cells use the approved dense asphalt road family across 29 crossing columns. One generated-root manifest binds all 1,108 cells to their stable presentation identities and source prefab GUIDs without retaining temporary tile components. Strict scene validation, direct ECS bake, isolated macOS Addressables/Entities packaging, and two complete packed load/readiness/capture/unload cycles pass at 36,946 generated identities and 82,797 render rows with production cutover disabled. The accepted map/SubScene, production definition/Addressables, frozen rollback package, and Android outputs remain unchanged.
- [x] Refactor canal/park generation to emit separate water exclusion, bridge, bank, terrain, vegetation, and light/prop records.
- [x] Refactor civic/market generation to separate hero presentation, terrain, road, and blocker records.
- [x] Refactor courtyard, wall, rock, mountain, vegetation, rooftop, power, and street-detail generation according to the semantic table.
- [x] Place every damageable building under `EntityPresentation/GameplayBuildings` and every other generated visual under its explicit `EntityPresentation/RenderOnly` parent at creation time.
- [x] Prove every repeated generated prefab placement shares source mesh/material identity and produces only transform/render entity instance data.
  - Accepted 2026-07-25 at the direct candidate-to-baked-ECS renderer join and reconfirmed through the isolated packed runtime package. All 68,735 generated candidate renderer/submesh rows resolve exactly once to 68,735 baked Entities Graphics rows. The proof records 50,360 prefab-backed and 18,375 mesh-backed rows, 59 persistent prefab sources, 57 repeated prefab sources across 31,973 placements, and 377 repeated resolved presentation signatures across 68,722 rows. Every repeated signature resolves one exact shared baked `Mesh`/`Material` object pair; unresolved mesh/material rows, source failures, baked asset mismatches, placement-specific managed components, unconsumed rows, and repeated-signature pair mismatches are all zero. Signed nonzero Unity local file IDs are accepted so the canal water prefab's valid negative FBX mesh subasset identity remains persistent evidence rather than a false transient allocation. Two complete direct bakes produced byte-identical report and manifest hashes, and the rebuilt isolated package passed both Addressables-loaded cycles without production cutover.
- [x] Prove generated buildings never enter `MapBuildingPlacementConfig`, `RuntimeBuildingEntityLink`, or managed destroyed-visual instantiation.
  - Accepted 2026-07-25 across generation input, direct bake, and the real isolated packed dense runtime. Dense candidate realization now snapshots the protected legacy `MapBuildingPlacementConfig` before generation and rejects with rollback after asset save unless both its exact 451-row count and SHA-256 `26973214...3464d` remain unchanged. Focused mutation coverage proves one injected placement is rejected. Existing dense readiness rejects any generated `RuntimeBuildingEntityLink`; direct bake contains 4,971 generated gameplay buildings, 5,403 total building presentations, and zero managed map-visual companions. Both complete packed load cycles select a `densecity.<sha256>` building, drive its production Burst ECS destruction transition, and prove intact/destroyed root state changes with zero total-entity delta, zero loaded-GameObject identity delta, and zero runtime link components. Production remains `StaticSceneChunks`, the protected placement config and accepted sources remain unchanged, and Android validation remains user-triggered.
- [x] Remove all post-generation name/category detection from the production path.
  - Accepted 2026-07-25: generated presentation records now carry a closed semantic category and explicit protected-overlap flag into authoring and baked ECS identities. Canal underpass realization retains exact renderer ownership during generation, while authoring and packed readiness consume only the serialized semantic component. The accepted older candidate was upgraded transactionally from its stable semantic roots with a geometry-only, exact-count migration for its 12 paired bed/water underpass owners; this one-time editor migration is absent from generation, bake readiness, Addressables loading, and runtime systems. The migration resolves all 36,946 identities, is byte-stable on a second run, and direct bake proves 36,946 exact transform rows, 82,797 render rows, zero non-finite transforms, and zero managed map-visual companions.
- [x] Preserve the exact deterministic seed and RNG call order unless a reviewed generator schema/version migration explicitly changes output.
- [x] Add tests proving each generated feature kind emits the expected semantic records and hierarchy owner.
  - Accepted 2026-07-25 on the approved Windows wrapper lane: the full-map fixture validated two building kinds, 18 surface kinds, and 36 presentation kinds, and the focused hierarchy-owner EditMode test passed 1/1. The first full-map run exposed mixed `urban-rock-visual` ownership: map-clipped rock blockers were hardcoded as infrastructure while presentation-only rocks were props. Visual-blocker inputs now require an explicit category; the record-set gate accepts only infrastructure or prop blockers and focused transaction coverage proves vegetation still fails closed. The final full-map run passed with 5,371 semantic buildings, 33,536 surfaces, 42,730 presentations, and 25 consistently prop-owned urban rocks without changing the accepted source scene bytes.

**Exit:** The generator never produces mixed semantic roots, building runtime ownership is ECS-only, and no ownership must be inferred later.

### Phase 3: Collider-Free Enforcement

- [x] Add `DenseCityPhysicsComponentStripper` with instance-only removal semantics.
- [x] Invoke physics-component removal immediately after every generated prefab/primitive realization path.
  - Direct-prefab implementation completed 2026-07-25: all 16 `DenseMiddleEasternCityEditModeBuilder` `PrefabUtility` call sites and all three record-replay prefab call sites route through `DenseCityPhysicsComponentStripper.InstantiatePrefabWithoutPhysics`; the only remaining direct `PrefabUtility.InstantiatePrefab` in dense editor code is inside that exception-safe factory. Focused coverage proves recursive strip, parent ownership, removed-component overrides, and persistent prefab non-mutation.
  - Accepted 2026-07-25 on the approved Windows wrapper lane: the one remaining `RuntimeCityVisualPresentationSystemHelper.SpawnVisualOnlyPrefab` call is stripped at the editor boundary immediately after its nullable return check and before transform, bounds, reservation, foundation, or record-realization work. `DenseCityPhysicsComponentStripperTests.RunFocusedValidation` passed 6/6, and the full-map fixture scanned the complete generated root, including inactive descendants, with zero prohibited 2D/3D colliders or Rigidbodies.
- [x] Remove primitive-created colliders before parent assignment and record publication completes.
  - Accepted 2026-07-25 on the approved Windows wrapper lane: `RuntimeCityRAndDEditModeBuilder` obtains road/shoulder cubes only through `DenseCityPhysicsComponentStripper.CreatePrimitiveWithoutPhysics`, which strips before returning an unparented primitive and destroys rejected partial objects. The focused 6/6 suite proves the primitive retains its mesh/renderer while containing zero active or inactive 2D/3D colliders or Rigidbodies.
- [x] Remove nested `Collider`, `Collider2D`, `Rigidbody`, and `Rigidbody2D` components, including inactive descendants.
- [x] Add generated-root validation requiring zero prohibited physics components.
- [x] Add operation-map source-scene validation requiring zero prohibited physics components in map-owned content.
- [x] Add runtime binding and generated entity-scene dependency validation requiring zero prohibited physics components.
- [x] Add build-dependency-closure validation for production scenes and Addressable map content.
  - Accepted 2026-08-10 after the authorized EntityScene cutover/static retirement. The checked validator traverses all `1,665` dependency assets from six production roots, rejects retired authoring/static entries, opens both production scenes, and validates `898` directly owned/generated map GameObject assets / `905` roots with zero prohibited physics. It separately reports `220` transitive gameplay-unit GameObject assets that are packaged through valid production configs but are not map-owned dense-city presentation; those keep their gameplay physics contract. The candidate redirects `501,925` serialized references across `78` unique physics-bearing visual prefab mappings plus three building definitions to deterministic physics-free generated mirrors while retaining mesh/material identities and mutating zero shared source prefabs. Evidence: `Design/AgentReports/2026-08-10_dense_city_production_dependency_closure.json`, `Build/Logs/parent-physics-free-prefab-dependency-repair-gui-r9.log`, and `Build/Logs/parent-production-dependency-closure-gui-r10.log`.
- [x] Add tests covering prefab instances, inactive children, primitive objects, removed-component overrides, and shared-prefab non-mutation.

**Exit:** No production operation-map path contains or depends on Unity collider/Rigidbody gameplay.

### Phase 4: Surface And Blocker Proxies

- [x] Add `DenseCitySurfaceProxyBuilder` and map-scoped candidate-output ownership.
- [x] Build simplified terrain proxy meshes from terrain/foundation/park records.
- [x] Build road proxies separately from shoulders and natural ground patches.
- [x] Build bridge deck and ramp proxies with correct layer and movement masks.
- [x] Build canal/water exclusion data without colliders.
- [x] Build coarse building/wall/rock blocker proxies from deterministic footprints.
- [x] Merge compatible proxy geometry by semantic role and spatial chunk.
- [x] Enforce deterministic winding, vertex/index ordering, finite values, and map bounds.
- [x] Require zero renderers and zero prohibited components beneath proxy roots.
- [x] Validate representative soldier, vehicle, building-placement, and aircraft-grounded surface queries against generated proxies.
- [x] Validate generated content outside the current gameplay grid contributes presentation only unless explicitly approved otherwise.

**Exit:** ECS surface/blocker data represents city gameplay without detailed-mesh rasterization or Unity Physics.

### Phase 5: Readiness And Bake All Integration

- [x] Add `OperationMapEntityPresentationReadinessValidator` with menu and batch entry points.
- [x] Add `DenseCityBakeReadinessValidator` with menu and batch entry points.
- [x] Validate exactly one generated root per required role, matching generation id/schema/version/seed, and deterministic content hashes.
- [x] Validate zero unclassified generated renderers and zero detailed renderers beneath proxy roots.
- [x] Validate each proxy has exactly one nearest bake-group owner.
- [x] Validate protected authored content was not moved, disabled, renamed, deleted, or overlapped.
- [x] Validate all legacy placement entries map one-to-one to accepted authored ECS entities and are absent from current-map runtime spawning/content references.
- [x] Validate every generated ECS building has finite transform/footprint/health, one intact root, one destroyed root, shared mesh/material references, and no managed runtime link.
- [x] Validate every attached prop resolves to exactly one building and one visual state, every intact attachment is hidden by the building transition, and no attachment is orphaned beneath an independent render-only owner.
- [x] Insert transform-parity validation after candidate population, after ECS baking, and after candidate Addressables runtime load; any matrix/bounds mismatch must roll back candidate outputs and invalidate stale success evidence.
  - Dense three-checkpoint acceptance completed on 2026-07-24: the protected generated candidate bakes in memory to exactly 5,803 gameplay buildings, 22 gameplay vehicles, 3 presentation roots, 9,544 legacy identities, 35,796 unique generated identities, 5,803 building presentations, and 78,325 render-mesh entities with zero non-finite transforms or managed map-visual companions. All 35,796 generated owners and all 64,076 generated renderer/material entities join exactly once at the direct-bake checkpoint. The source-fingerprinted packed manifest composes `LocalTransform`, `Parent`, and `PostTransformMatrix` in the same `float4x4` order as `LocalToWorldSystem`, preventing bake-snapshot/runtime one-ULP drift without weakening matrix `0.0001` or bounds `0.001` tolerances. The isolated dense Addressables and Entities content then passes two consecutive load/capture/unload cycles across all 9,544 legacy identities, 35,796 generated identities, and 78,325 renderer rows with zero matrix, local-bounds, transformed-world-bounds, duplicate, missing, unexpected, or teardown failures.
- [x] Insert `entity-presentation-readiness` as the first content gate and `dense-city-readiness` as the conditional generated-content gate in `OperationMapCurrentMapBaker`.
- [x] Insert `presentation-budget` after entity-scene baking and before runtime definition/Addressables publication; invoke the existing static-presentation bake only for definitions explicitly using `StaticSceneChunks`.
- [x] Preserve stage report, stale-success invalidation, scene restoration, and transaction semantics.
- [x] Add failure tests proving no accepted output changes after readiness, proxy, surface, presentation, or budget failure.
  - Proxy failure coverage implemented 2026-07-25: the focused candidate-authoring transaction test uses the same proxy-folder move/restore helpers as realization, injects failure after a partial replacement is imported, and requires restoration of the accepted folder/file GUIDs and bytes with deletion of the partial output and transaction backup. The broader item remains open for readiness, surface, presentation, and budget failures and pending wrapper-driven execution of this focused test.
  - Surface failure coverage implemented 2026-07-25: the focused surface-placement transaction test seeds a previously accepted terrain record, injects a later surface-realization exception, and requires every accepted identity, geometry, elevation, movement, layer, and chunk field to remain unchanged while the rejected surface is removed. The broader item remains open for readiness, presentation, and budget failures and pending wrapper-driven execution of this focused test.
  - Presentation failure coverage implemented 2026-07-25: the existing late render-only replay failure now begins with accepted building and prop hierarchy objects and requires those exact objects to survive while every object created by the rejected replay is removed. The broader item remains open for readiness and budget failures and pending wrapper-driven execution of this focused test.
  - Readiness failure coverage implemented 2026-07-25: the focused entity-presentation readiness suite captures the complete accepted hierarchy state, forces a count-contract rejection, and requires the recursive names, active state, local transforms, component types, serialized authoring data, and child structure to remain identical. The broader item remains open for budget failure and pending wrapper-driven execution of this focused test.
  - Budget failure coverage implemented 2026-07-25: the focused presentation-budget test rejects incomplete geometry through the production budget predicate after candidate-owned outputs are changed, then requires the candidate file transaction to restore accepted bytes and remove the partial output.
  - Accepted 2026-07-25 through one approved Windows-wrapper entry point: proxy folder replacement, surface-record realization, late presentation replay, readiness-count rejection, and presentation-budget rejection all preserve their seeded accepted outputs while removing partial replacements. The readiness fixture now supplies the complete production-required non-negative contract before forcing its intended count mismatch.
- [x] Add two-run no-op validation for generation plus complete Bake All.
  - Transactional two-run harness implemented 2026-07-25 in `OperationMapEntitySceneCandidateBakeAllTests.RunTwoRunNoOpValidation`: each pass executes real dense realization, direct dense ECS bake, dense candidate layout, and complete existing-candidate Bake All; the harness compares SHA-256 manifests of the exact candidate scenes, definitions, thin bindings, generated dense asset root, and legacy dense-road output. An outer disk-backed checkpoint restores every listed file/directory and root `.meta` on any exception or second-run mismatch, while intentionally excluding timing-bearing reports from the no-op fingerprint.
  - Windows execution remains open. The first full attempt exposed and rolled back a three-ULP nested attachment replay error; `ApplyWorldMatrix` now applies a bounded parent-space world-position residual correction without changing the existing matrix tolerances, and the focused wrapper regression passes. The next full attempt completed deterministic realization at 4,971 generated buildings, 30,886 render-only owners, 32,845 surfaces, and 2,468 proxy partitions, then failed closed before ECS baking because the accepted direct-bake baseline still requires 31,975 render-only identities (36,946 total). The outer checkpoint restored accepted candidate output; this item remains unchecked until the 1,089-owner semantic-baseline discrepancy is reconciled and two complete byte-stable passes succeed.
  - Fresh-versus-accepted identity diagnosis completed 2026-07-25 through the approved Windows wrapper. The transactional diagnostic captured 35,857 fresh versus 36,946 accepted identities, with 35,804 stable IDs shared. All 1,089 net missing owners are `Infrastructure` instances of `SM_Env_Ground_Round_01.prefab`; the other 53 accepted-only and 53 fresh-only entries are the same seven horizon-prefab groups with shifted generated sequence names/stable IDs after the missing ground records. The checkpoint restored candidate outputs, and the frozen baseline remains unchanged pending repair of the round-ground semantic-record path.
  - A transactional 2026-07-25 causality probe replaced realized-renderer footprint bounds with each committed building record's validated blocker bounds only for the probe run. This produced 2,770 open-ground patches and 35,846 fresh identities—11 fewer than the unchanged implementation and 1,100 below the accepted identity total—so blocker-record bounds are not the compatibility repair. The checkpoint restored candidate outputs; the experimental source and regenerated diagnostic report were reverted byte-for-byte, and no count gate or baseline changed.
  - The round-ground semantic baseline was reconciled 2026-07-25 without restoring name inference. Realized building owners now explicitly record whether their placement reserves open-ground clearance; frontage-snapped buildings preserve the accepted no-clearance behavior that previously existed only because their generated wrapper names no longer ended in `_Visual`. The original generator accepted exactly the schema-locked deterministic prefix of 3,870 open-ground patches and failed closed if eligible generation underproduced that count. The 2026-08-02 building-road clearance correction moved standard-road rejection bounds from the grid corner to the same canonical cell center as the rendered tile; its full deterministic generator produced exactly 3,539 eligible patches, so the exact fail-closed baseline is now 3,539. This is a measured layout-contract reconciliation, not a removal or lower-bound relaxation. Persisted-candidate regeneration and a fresh identity comparison remain pending. The checklist item remains open pending two complete byte-stable Bake All passes.
  - A fail-closed realization no-op prerequisite was added 2026-07-26 after the line-level diagnostic proved semantically identical regeneration changes Unity YAML local file IDs. Realization now reuses an existing candidate only when both loaded scenes pass the complete ownership/readiness validator, the generated roots exactly match the current SHA-256 contract over both source scenes, config, schema/version, and seed, the matching proxy folder exists, and realized proxy/building/render-only populations are non-empty. Stale, incomplete, or invalid output still enters the existing transactional rebuild. Focused Windows GUI-wrapper coverage passed 20/20, including exact-contract acceptance and stale-hash rejection. The checklist remains open until the complete two-pass candidate fingerprint succeeds.
  - The first complete no-op rerun on 2026-07-27 proved both realization passes return `AlreadyComplete` with the exact 4,971-building, 31,975-render-only, and 2,468-proxy contract, and its first complete Bake All passed every content gate. The second direct bake completed but dense layout then failed closed with Windows mapped-file error 1224 because the builder reapplied identical serialized fields, saved the candidate definition, and only afterward discovered its YAML normalization was required. Candidate-definition configuration now uses `SerializedObject.ApplyModifiedPropertiesWithoutUndo()` as the earlier no-op boundary: an unchanged definition is not dirtied, saved, normalized, or reimported, while creation and real GUID changes retain the full persistence path. Focused Windows GUI-wrapper coverage passed 21/21, including changed/unchanged/changed serialized-property cases. The complete two-pass retry remains required.
  - The next complete retry on 2026-07-27 passed realization and dense layout without the prior definition write, then failed closed inside first-pass complete Bake All when the accepted runtime-binding builder recopied and resaved an already-valid scene before normalization encountered Win32 mapped-file error 1224. The builder now opens and validates an existing binding against the exact candidate definition and entity SubScene before any mutation; a valid scene is a complete no-op, while missing/invalid output retains the production-copy repair path and explicitly unloads the main scene asset before overwrite. Focused Windows GUI-wrapper coverage passed 22/22, including read-only reuse validation for both accepted and dense runtime-binding scenes. The full two-pass retry remains required.
  - The following complete retry on 2026-07-27 passed realization and reached first-pass Bake All without the prior runtime-binding rewrite, then failed closed while truncating the sole Git-LFS-tracked transform-parity JSON report because a lingering LFS filter process held a Windows memory mapping. The report writer now compares exact UTF-8 bytes and, when content changes without changing length, overwrites the existing bytes through a shared fixed-length stream rather than truncating/resizing the mapped file; genuine length changes retain normal fail-closed replacement. Focused Windows GUI-wrapper coverage passed 23/23 with a live `MemoryMappedFile` held across the write. No Git/LFS process was terminated, and the complete retry remains required.
  - Accepted 2026-07-27 through the approved Windows GUI wrapper. Both realization passes returned `AlreadyComplete` for the identical generation hash with 4,971 buildings, 31,975 render-only owners, 2,468 proxies, and the same protected 451-placement SHA-256. Both dense layouts passed with five entries, zero explicit shared dependencies, and zero production Addressables mutation. Both complete Bake All passes passed at 432 legacy buildings, 22 vehicles, 9,090 legacy render-only owners, 9,544 core identities, 14,249 render entities, 670 meshes, 39 materials, 6,064,174 triangles, and zero production cutover. The exact candidate-output manifest fingerprint matched at `699a32b8e5101346948dcce2c42009853a84d67b0bcd959d3475359057374430`.

**Exit:** One button produces or rejects the complete map package without partial/stale success evidence.

### Phase 6: ECS Presentation Optimization

- [x] Add `DenseCityPresentationBudgetValidator` and deterministic JSON evidence outside transient project folders.
- [x] Report source/baked entity/render counts, unique meshes/materials/textures, triangles, shadows, batching eligibility, bounds, and spatial density.
- [x] Report gameplay/render-only/render-child entity counts, archetype/entity-chunk counts, shared asset identities, entity-scene bytes, and intact/destroyed visual ownership.
  - Dense direct-bake metrics accepted 2026-07-25: the generated-candidate bake report now fail-closed captures 5,403 gameplay buildings, 31,975 generated render-only identities, 20,390 render-child entities, 78 archetypes, 4,692 chunks, one shared `RenderMeshArray` identity, 1,769 shared mesh identities, 80 shared material identities, 5,403 intact roots, 5,237 destroyed roots, 166 explicitly missing destroyed variants, zero missing intact roots, and zero shared intact/destroyed roots. The item remains open only for exact packed entity-scene bytes.
  - Exact-byte reporting foundation accepted 2026-07-25: schema 2 of the isolated runtime-content report now separates the sum of `.archive` payload bytes (`entitySceneArchiveBytes`) from catalog/metadata overhead (`entityContentMetadataBytes`) and preserves the total as `entityContentBytes`. The builder requires exactly one nonempty archive for the one requested dense EntityScene and rejects missing catalogs, empty archives, negative inventories, and ambiguous multi-archive output before report publication. The checked-in schema-1 report cannot be retroactively relabeled because its local packed output is absent on Windows; this row remains open until the macOS content lane rebuilds and publishes the exact schema-2 archive value.
  - EntityScene archive fingerprint foundation accepted 2026-07-25: schema 8 of the next isolated runtime-content report records a machine-independent SHA-256 over each sorted relative `.archive` path, exact byte length, and individual file SHA-256. Windows fixtures prove identical archive sets under different roots hash equally and a changed archive changes the aggregate. The row remains open for the real packed archive hash and exact bytes.
  - Accepted Windows packed evidence 2026-07-25: the approved substitute content lane produced exactly one EntityScene archive at 137,862,756 bytes with archive-set SHA-256 `4e4ab7bfd23911f64b4e5a7d08d25211e9c8e52eaf9e446acd86ea0208a91c6f`, 49,947,710 bytes of catalog/metadata, and 187,810,466 total Entities content bytes. Combined with the already accepted gameplay/render-only/render-child, archetype/chunk, shared `RenderMeshArray`, and intact/destroyed ownership metrics, this completes the row.
- [x] Report entity-scene/shared-dependency bytes, frozen rollback chunk bytes, duplicated dependency bytes, and production static-manifest/chunk counts required to be zero.
  - Frozen rollback byte evidence accepted 2026-07-25: the read-only Windows inventory reports the immutable rollback manifest at 10,097,753 bytes and its 269 `.unity` chunk payloads at 32,381,589 bytes, excluding `.meta`, integrity-ledger, and minimap bytes by contract. The isolated runtime-content schema now carries these values on future packed builds and rejects a missing/empty manifest or scene set. The combined item remains open for exact packed EntityScene/shared/duplicate bytes and the production zero-count proof after authorized cutover.
  - Production static-entry baseline accepted 2026-07-25: the same read-only report records production kind `StaticSceneChunks`, one static-manifest Addressables entry, 269 presentation-chunk entries, explicit post-cutover targets of zero/zero, and `postCutoverZeroCountsSatisfied=0`. The reusable gate rejects any retained static entry as soon as production kind becomes `EntityScene`; it does not misrepresent the authorized pre-cutover package as already cut over.
  - Packed dependency accounting foundation accepted 2026-07-25: a deterministic GUID/bundle occurrence reducer now reports physical shared-dependency bytes and duplicated excess bytes without double-counting repeated rows inside one bundle. Duplicate bytes are the sum of physical copies beyond the largest retained copy per GUID; missing requested shared GUIDs, blank ownership, and nonpositive byte rows fail closed. No accepted dense Build Layout exists on Windows, so the exact packed values remain open for the next macOS content build.
  - Addressables Build Layout adapter accepted 2026-07-25: the reducer now consumes Unity's real `BuildLayout` graph, including explicit assets, file-owned implicit assets, and implicit assets referenced by explicit owners, and maps serialized plus streamed bytes to stable GUID/bundle occurrences. A synthetic two-bundle graph proves exact shared and excess-copy totals. Build Layout capture/publication remains open for the next isolated macOS build.
  - Transactional Build Layout capture accepted 2026-07-25: the isolated builder now temporarily enables JSON Build Layout generation, requires exactly one new readable report, restores the prior Addressables report list/format/enabled preference, deletes only the newly generated global report, and retains a copy under the transaction-owned dense output. Runtime-content schema 4 will publish the layout hash, shared GUID/byte counts, duplicate GUID/excess-byte counts, and a complete marker. The actual values remain open until the macOS build runs.
  - Cross-bundle duplication gate accepted 2026-07-25: the isolated builder now rejects its real Build Layout before publication unless duplicated dependency GUID and excess-byte counts are both zero. Same-bundle repeated rows remain deduplicated, while a physical copy of one GUID in two bundles fails closed with its exact excess bytes. Focused Windows fixtures pass 28/28; exact accepted packed values still require the macOS content build.
  - Final combined acceptance 2026-08-11: the accepted packed EntityScene evidence reports `137,862,756` archive bytes, zero explicit shared-dependency GUIDs/bytes, and zero duplicated dependency GUIDs/bytes. The frozen rollback inventory remains exact at a `10,097,753`-byte manifest plus `269` immutable scene chunks / `32,381,589` chunk bytes. The final redirected Android Addressables report records five bundles / `135,348,884` bytes with an empty duplicate-dependency set, while the checked production dependency closure and post-retirement validation independently report zero production static-manifest entries and zero production static-chunk entries. These are separately owned measurements: rollback bytes are retained recovery evidence and are not counted as live production content.
- [x] Verify every existing/generated map visual is included through entity-scene ownership while proxy/source authoring objects and all current-map static chunk inputs are excluded.
  - Accepted 2026-07-25 through one aggregate fail-closed ownership gate over the existing-map bake parity, dense generated bake parity, and exact dense candidate Addressables entry allowlist. The evidence joins 9,544 existing visual identities with 14,249 baked render entities and 36,946 generated identities with 68,735 generated renderer entities, requiring exact candidate/baked identity-set equality, zero missing/unexpected/duplicate identities, zero unresolved/unconsumed generated renderers, and zero managed map-visual companions. The five-entry candidate package is restricted to its definition, renderer-free runtime binding, sole dense EntityScene, map-surface data, and minimap raster; source/candidate authoring scenes, explicit shared owners, static manifest/chunks, and legacy placements are all zero. This is candidate ownership proof only and does not claim production cutover.
- [x] Preserve repeated building, tree, prop, road-module, bridge-module, vegetation, and infrastructure mesh/material references for Entities Graphics batching.
  - Accepted 2026-07-25 through schema 3 of the dense direct-bake parity report. Each candidate renderer signature now carries its explicit generation-time semantic category and must resolve after baking to exactly one mesh/material asset pair. The accepted bake reports 18,364 repeated building renderer entries, 39,005 repeated road/bridge/infrastructure entries, 5,423 repeated tree/vegetation entries, and 5,876 repeated prop entries, with zero asset-pair mismatches in every category. Category classification uses the serialized semantic contract, not object names; packed binary duplication remains a separate open item.
- [x] Prove no existing/generated map mesh/material/texture binary is copied per entity, entity section, or retired presentation bundle.
  - Packed Addressables foundation accepted 2026-07-25: every explicit, file-owned implicit, and explicit-owner-referenced implicit Build Layout asset is reduced by GUID and physical bundle, and the isolated build now rejects any cross-bundle GUID copy before publication. This proves the candidate Addressables half of the contract once a real Build Layout is captured; the row remains open for the separate Entities archive/section binary proof and accepted macOS packed evidence.
  - Accepted Windows aggregate 2026-07-25: the fail-closed packed-sharing report joins the complete bake, generated transform/asset-pair parity, and schema-9 runtime-content evidence. All `82,797` render entities share exactly one `RenderMeshArray` containing `1,769` mesh and `80` material identities; `57` repeated prefab sources cover `31,973` placements and `377` repeated presentation signatures cover `68,722` rows with zero unresolved mesh/material references, pair mismatches, managed per-instance components, or persistent-source failures. The real package contains one `137,862,756`-byte EntityScene archive, zero explicit shared-dependency GUIDs/bytes, zero duplicated dependency GUIDs/bytes across every Build Layout asset type (including textures), zero static runtime entries, and unchanged production settings. This proves placement/section reuse through ECS shared references and one archive rather than placement-specific or retired-bundle binary copies.
- [x] Combine only measured unique continuous terrain/road geometry; reject a whole-city mesh and combinations that destroy repeated-asset sharing.
  - Accepted 2026-07-25 with no continuous terrain/road combination currently authorized. Schema 4 measures every generated renderer against the 5,195.93 × 2,103.09 m aggregate candidate footprint, rejects any mesh-backed renderer spanning at least 50% on both horizontal axes, and rejects every mesh-backed semantic family except the existing building geometry. The accepted maximum is 2.19% × 5.41%; all 18,375 mesh-backed entries are buildings, while infrastructure/road/bridge, vegetation, prop, and horizon mesh-backed counts are zero. Any future continuous terrain/road combination therefore requires measured authorization instead of silently replacing shared modules.
- [x] Add Burst `OperationMapBuildingDestructionSystem` and focused tests proving one-time intact-to-destroyed entity visual transition with no GameObject creation.
- [x] Add destruction tests containing nested roof, interior, shop, and tent props; prove the complete intact hierarchy becomes hidden, the complete destroyed hierarchy becomes visible, missing destroyed equivalents leave no orphan, and the second update is a no-op.
- [x] Keep rubble blockers unchanged by default; require an explicit per-record policy and deterministic dynamic-grid update before any destroyed building becomes passable.
- [x] Add entity-compatible LOD/impostor authoring only where Android geometry/shadow evidence requires it.
  - Accepted as not required for the current representation on 2026-08-09. The complete VRP-072 inventory proves all `12,293` virtualized prototype parts are LOD0-only and no alternate source recipes exist; Android evidence instead approved a bounded shadow/small-detail policy without synthesizing geometry or introducing an impostor transition. LOD0 remains intact, and any future alternate recipe reopens this conditional row.
- [x] Add deterministic LOD/impostor source/output hashes and transition validation when introduced.
  - Accepted as conditionally not applicable on 2026-08-09 because no LOD/impostor output was introduced. Determinism remains fail-closed at the complete current representation through database content hash `bfb350f0c8d1474aa05252dc04c87eede4c1210adcee9c92dcdbecc35897896e`, ordering hash `a43040ee38b9e8cfe752f1e52848cfa523e453fa2ebcde0cadc4142510d79318`, and accepted packed/materialized fingerprints; adding any alternate LOD or impostor source/output requires new hashes and transition validation before acceptance.
- [x] Reject current-map static-presentation production chunks unless a named unique-heavyweight exception is added to this tracker with measured Android evidence.
  - Accepted 2026-07-27 from the existing fail-closed production inventory gate and its previously accepted Windows-wrapper fixtures. `MeasureProductionStaticAddressables` permits the authorized `StaticSceneChunks` rollback baseline to be reported without claiming cutover, but rejects `EntityScene` production when either the static manifest or any presentation chunk remains and accepts it only at exact zero/zero. No exception is currently named or authorized, so the gate is intentionally stricter than the conditional allowance. The accepted `%TEMP%\warline-dense-production-static-counts-tests-2.log` run passed all 9/9 fixtures. A 2026-07-27 audit rerun did not reach compilation because the GUI Editor encountered the known stale licensing-client IPC state; that failed startup is not counted as new validation evidence and changed no project or production asset.
- [x] Validate shadows, small-detail stripping, transparent materials, and lighting against mobile budgets.
  - Deterministic evidence foundation accepted 2026-07-27: schema 2 of the presentation-budget report now inventories active shadow casters and receivers, unique transparent and alpha-clipped materials, renderers whose maximum world-bounds extent is at most the explicit 1 m small-detail threshold, lightmapped renderers, and light-probe users. The core gate requires an exact completeness marker and rejects negative or out-of-range counts, so schema-1/default evidence cannot silently satisfy this mobile-quality prerequisite. The final Windows GUI-licensing wrapper run passed all 23/23 focused tests and exited `0`; an intermediate run correctly rejected the test-only schema-2 assertion after it exposed that only invalidation reports had been version-bumped, and the final assignment was corrected. The checklist row remains open for a regenerated complete candidate report plus measured Android visual/performance budgets; no shadow, detail, material, or lighting production setting changed.
  - Android render-policy closure accepted 2026-07-27: the existing mobile-quality suite already fixes the Android pipeline at 0.50 FSR render scale, 1x MSAA, 16 m shadow distance, disabled light cookies/layers, Forward+, GPU Resident Drawer instanced drawing, and preserved BRG/instancing shader variants. Its URP global-settings check now parses the complete `m_RuntimeSettings` and managed-reference blocks, rejects duplicate or dangling runtime RIDs, and requires `GPUResidentDrawerResources` to be in the runtime list rather than merely serialized as an unused definition. The documented Windows GUI-licensing wrapper compiled and passed all 17/17 focused cases with exit code `0`. The row remains open for candidate-specific measured shadow/detail/transparency/lighting evidence on Android; this step changed no rendering or production setting.
  - Final candidate-specific acceptance completed through VRP-072 and retained Phase 9 evidence. Exactly `12,185` eligible logical part rows stop casting/static shadows while preserving receive-shadows (`5,684` Vegetation, `5,619` Prop, and `882` sub-meter building-detail rows); large structures and named resident/transparent exceptions remain unchanged. The deterministic report/database/bake gates passed, matched Samsung daylight crops reduced pitch-black coverage by more than half without holes, leakage, or popping, and later representative/final thermal routes passed unchanged GPU/frame/correctness budgets. No lighting or transparency exception was inferred from names, and no production setting changed.

**Exit:** Presentation output is bounded, deterministic, visually complete, and suitable for Android profiling.

### Phase 7: Runtime Package Separation

- [x] Prove both operation-map and map-SubScene source GameObject YAML hierarchies are absent from Addressables runtime entries and player build scenes.
  - Accepted Windows foundation 2026-07-25: the isolated dense builder now fails closed unless its actual temporary group contains exactly the candidate definition and renderer-free thin runtime binding and unless none of the accepted/candidate operation-map or map-SubScene source hierarchy paths appears as an explicit Addressables entry or enabled player build scene. The live report records two explicit entries, two enabled build scenes, and zero forbidden entries/scenes. `packedImplicitDependencyEvidenceComplete=0`, so this row remains open until the accepted macOS Build Layout proves neither raw YAML hierarchy is present as an implicit packed dependency.
  - Packed Build Layout gate accepted 2026-07-25: schema 5 of the next isolated runtime-content build enumerates both explicit assets and file-owned/internal implicit assets, normalizes and deduplicates their asset-path/bundle/ownership tuples, and rejects the build before publication if any accepted/candidate operation-map or map-SubScene source hierarchy path is present. Focused Windows fixtures pass 26/26, including explicit and implicit rejection through the real Addressables `BuildLayout` adapter. The row remains open because the macOS content build that supplies the real packed asset-path evidence has not run.
  - Accepted Windows packed evidence 2026-07-25: the approved substitute content lane recorded five packed asset paths, zero source-hierarchy explicit assets, and zero source-hierarchy implicit assets after the earlier explicit-entry and enabled-player-scene gate reported zero forbidden paths. The fail-closed Build Layout gate passed before publication.
- [x] Prove `OperationMapAddressablesLayoutBuilder.SourceScenePath` remains the thin runtime binding scene.
- [x] Regenerate the runtime binding scene and require renderer-free, collider-free `EntityScene` validation.
- [x] Verify operation-map definition references the accepted runtime binding scene, surface, minimap, and entity SubScene with no required static manifest or current-map legacy placement runtime references.
- [x] Verify the map SubScene is the sole runtime owner of every existing/generated map renderer and production Addressables contain zero current-map static manifest/chunk entries.
  - Accepted 2026-08-09 at staged cutover: the production definition and thin binding select the accepted dense EntityScene GUID `c00140f2e94a04c3084c8dcb0c18cbd0` with canonical `EntityScene` mode and no legacy building/vehicle/static references. The checked production layout validator passes with catalog `2`, core `4`, shared `0`, presentation `0`, and the retained exact logical/resident/materialized parity proves all current render ownership is represented by this single map SubScene path. Fresh package closure remains separate.
- [x] Verify source meshes/materials/textures are shared by GUID and entity-scene render references rather than copied into placement-specific binary assets.
  - Accepted Windows packed evidence 2026-07-25: the schema-9 report joins the already accepted one-shared-`RenderMeshArray`/shared mesh/material identity proof to one packed dense EntityScene archive and reports zero duplicated dependency GUIDs and zero duplicated dependency bytes. Placements remain compact entity/render-reference data; no placement-specific Addressables art copy was found.
- [x] Verify Build Layout reports no authoring hierarchy duplication, exactly one map entity-scene ownership path, and zero duplicated existing/generated map art dependencies.
  - Accepted Windows packed evidence 2026-07-25: the retained Build Layout SHA-256 is `4cf9be9229b48e4ec76c24debc4025a0232ea8695af202b1ac67c4ee40d10cf4`. Its validated graph contains the single planned dense EntityScene ownership path, five runtime asset paths, zero source-hierarchy explicit/implicit assets, zero duplicated dependency GUIDs, and zero duplicated dependency bytes.
- [x] Verify local bundles remain included with the application and require no network access.
  - Accepted Windows contract foundation 2026-07-25: the isolated dense Addressables builder now explicitly disables remote catalogs, disables startup catalog updates, requires `IncludeInBuild`, binds both schema paths to the Addressables local build/load variables, and rejects any configured non-file absolute load URI before creating content. Focused fixtures accept the local built-in contract and reject a remote catalog, an excluded group, and an HTTPS load path. This row remains open because actual player-package inclusion and a network-disabled runtime launch were not run.
  - Published-output gate accepted 2026-07-25: after the transaction publishes isolated Addressables output, schema 6 now requires a nonempty `catalog.bin` inside that exact output root plus at least one nonempty `.bundle`, and records catalog bytes, bundle count, and bundle bytes. Missing/out-of-root catalogs and empty bundle sets fail before report/output commit. Focused Windows fixtures pass 31/31. The row remains open for player-package inclusion and network-disabled launch against the real build.
  - Final acceptance 2026-07-27 joins the already accepted Xiaomi network-disabled cold launch with direct read-only inspection of the current rebuilt APK. `Build/AndroidDenseCandidate/WarlineCapture-DenseCityCandidate.apk` is 555,823,365 bytes / SHA-256 `613aeeecdde3cae594e831b30200f2566d2a0687d61e6d9eba6763719b23ebcb` and contains exactly one `assets/aa/DenseCityCandidate/catalog.bin` (2,977 bytes), its 32-byte hash, and four local Android bundles totaling 915,795 bytes. Those values exactly reconcile with the current schema-9 Android runtime-content report, and the ZIP contains zero HTTP/remote-catalog-named entries. The candidate runtime loads the catalog from `Application.streamingAssetsPath/aa/DenseCityCandidate/catalog.bin`; its build contract keeps remote catalogs disabled, startup catalog updates disabled, and the group included in build. The earlier exact candidate installed and reached the rendered Deploy menu with Wi-Fi and mobile data both disabled, zero fatal/ANR matches, and no network fallback. This proves local application ownership without claiming the still-open dense Match performance/minimap/device gates.

**Exit:** The player ships optimized runtime output, not the 225 MB-style editor hierarchy.

### Phase 8: Runtime Lifecycle Validation

- [x] Run focused load, readiness, damage/destruction, vehicle behavior, failure, retry, teardown, and sequential-load tests with the complete ECS-presented map.
  - Accepted 2026-07-23 for current packed authored-vehicle behavior: `UnitGridSnapSystem` derives each `OperationMapAuthoredVehiclePresentation` grid cell from its accepted baked `LocalTransform` without changing position, rotation, or scale, while ordinary spawned units retain grid-to-transform snapping. The packed candidate selects a real section-owned authored ground vehicle, provisions test-only ECS fuel for its neutral faction, issues the production grouped manual move order, resolves a production path over the real map surface, moves the gameplay entity, and proves its baked detailed visual follows without changing source identity, hierarchy, EntityScene ownership, section residency, static-streamer state, or managed map-visual ownership. The complete packed fixture passes all six cases. This does not close the umbrella item because packed failure/reset/retry and complete generated-city coverage remain open.
  - Accepted 2026-07-23 for packed readiness failure/reset/retry: the real fingerprinted candidate is loaded until all packed sections and presentation identities pass normal readiness, then a test-only API wrapper rejects that loaded state once with a deterministic wrong-map readiness check without mutating any entity or accepted asset. Failure begins owned EntityScene and thin-scene cleanup immediately; reset cannot discard operation ownership while asynchronous cleanup is pending, and the same helper, definition, Addressables catalog, and Entities catalog then retry successfully. The failed scene entity and every resolved section entity are destroyed before retry; the retry passes the real readiness contract and complete unload. The complete packed fixture passes all seven cases. This does not close the umbrella item because complete generated-city coverage is absent from the project-owner-approved `NotGenerated` candidate.
  - Accepted Windows complete-generated-city load/parity step 2026-07-25: the packed fixture now fails closed on the accepted runtime-content schema-9 target/platform, local publication, bundle-set hash, Build Layout hash, zero duplicated dependency bytes/GUIDs, zero explicit/implicit source-hierarchy assets, and single EntityScene archive/byte-accounting evidence before loading. It then loads the real isolated Windows Addressables/Entities package twice, matches all `9,544` legacy identities, `36,946` generated identities, and `82,797` renderer rows against the accepted manifest, captures all five canonical views in both cycles, and tears down completely. The refreshed five-view Editor baseline and ten runtime captures pass with maximum mean-channel delta `0.0021497` against `0.0025` and maximum non-edge changed-pixel ratio `0.0018816` against `0.01`. This does not close the umbrella item because complete-generated-city vehicle behavior remains open.
  - Accepted Windows complete-generated-city failure/reset/retry step 2026-07-25: the schema-9 packed fixture loads until every dense section and exact readiness count is present, injects the existing deterministic wrong-map readiness fault once, and requires `MetadataBindFailed` without mutating content. Reset remains unavailable while owned asynchronous cleanup is pending. Once reset succeeds, the failed EntityScene root and every captured section entity are absent; the same loader, definition, Addressables catalog, and Entities catalog then retry successfully with exactly `9,544` legacy identities, `36,946` generated identities, and `82,797` render rows before complete unload. This does not close the umbrella item because complete-generated-city vehicle behavior remains open.
  - Accepted Windows complete-generated-city vehicle step 2026-07-25: the schema-9 package enters through the production Menu-to-Match route with exact `9,544` legacy identities, `36,946` generated identities, and `82,797` render rows. The established production validator selects a real section-owned authored ground vehicle, provisions test-only ECS fuel, issues the grouped manual move order, resolves a path from cell `(862,386)` to `(865,386)`, and moves both vehicle and baked detailed visual `2.03230166` units. Source identity, detailed-visual reference, transform hierarchy, scene/section residency, scene-load requests, single map root, zero managed map visuals, and idle static streamer remain exact. The move order is cleared, test fuel is removed, and normal Menu return releases all dense scene/section/map ownership. This closes the complete Editor lifecycle umbrella; Android acceptance remains separate.
- [x] Validate map readiness requires all expected existing/generated gameplay and render-only entities and no presentation preload queue.
  - Accepted 2026-07-23: the candidate EntityScene bakes exactly one unmanaged readiness contract on the gameplay-building presentation root. `OperationMapSceneLoadingSceneSystemHelper` does not publish ready until the loaded sections contain the contract, exactly three unique role roots, `432` gameplay-building identities and building-presentation components, `22` gameplay-vehicle identities and authored vehicle components, `9,090` render-only owner identities, zero generated-city identities for the accepted not-generated state, one shared migration hash, unique source identities, and `RequiresStaticPresentationPreload=0`. Missing, duplicate, stale, wrong-map, wrong-role, count-mismatched, or static-preload content fails closed as `MetadataBindFailed`.
- [x] Load the candidate through its real Addressables/runtime-binding route and prove all permanent map-visual ECS matrices and transformed renderer bounds match the accepted Editor transform-parity manifest before gameplay systems update them.
  - Accepted 2026-07-23: `OperationMapEntityScenePackedRuntimeParityPlayModeTests.PackedCandidate_TwoLoadCyclesMatchBakedMatricesAndBounds` loaded the candidate definition and thin runtime binding from the packed macOS Addressables catalog, loaded/unloaded the packed EntityScene twice, matched all `9,544` source identities and all `14,249` render entities against the accepted schema-v3 matrix/local-bounds/transformed-world-bounds manifest, and left no loaded candidate EntityScene after either cycle. The test suspends only known gameplay transform mutators during the pre-simulation capture and restores their prior enabled state in `finally`; production systems are unchanged. Android validation remains user-triggered and is not a blocker for this Editor proof.
- [x] Validate camera traversal causes no map-visual scene load/unload; Entities Graphics culling/LOD changes visibility while entity identities remain stable.
  - Accepted 2026-07-23: the graphics-enabled packed candidate test issued explicit URP render requests through the real Match world camera at a map-detail view and an off-map view. Entities Graphics retained the same three batches and `696` chunks while visible instances changed from `13,933-13,949` to zero; the detail view performed `868` instance tests, produced `2,139` draw commands, and fully culled `348` chunks, while the off-map view culled all `696` chunks. The candidate EntityScene root, resolved section entities, every section-tagged entity index/version, every `RequestSceneLoaded` owner, loaded Unity scene handles/paths, operation-map ECS metadata, and idle static-streamer state remained exact after both views. The candidate contains `104` section `MeshLODComponent` records but reported no LOD recalculation in this route, so Android LOD transition and visual-continuity acceptance remains explicitly open.
- [x] Validate map unload releases entity scene/subscene/map metadata without a static-presentation drain step.
  - Accepted 2026-07-23: the packed candidate completed the real production Menu-to-Match-to-Menu route. Teardown destroyed the EntityScene metadata entity, every resolved section metadata entity, every entity tagged to those sections, the published operation-map root/metadata/readiness components, the thin runtime-binding scene, and the Match scene. The retained `MatchSceneView` preserved its immutable EntityScene presentation mode after its active map view was cleared, so the Menu path neither started nor waited for `StaticMapPresentationStreamer` drain; the streamer remained unbound with zero active/pending operations and `DrainComplete=0`. The default ECS World and exactly one scene-lifecycle root survived. The production `StaticSceneChunks` Menu-to-Match-to-Menu regression also passed.
- [x] Validate two consecutive Deploy-to-Match-to-Menu cycles without retained map entities, static chunks, invalid handles, or stale ECS state.
  - Accepted 2026-07-23: `OperationMapEntityScenePackedRuntimeParityPlayModeTests.PackedCandidate_TwoMatchToMenuCyclesReleaseAllOwnershipWithoutStaticDrain` drove two consecutive production Menu-to-Match-to-Menu cycles through the fingerprinted packed candidate catalog. Each Match loaded the same candidate EntityScene, published exactly one operation-map root/metadata/readiness owner, and contained section-tagged entities; each Menu return destroyed the EntityScene metadata entity, every resolved section metadata entity, all section-tagged entities, all operation-map ECS metadata, the thin runtime-binding scene, and the Match scene. The static streamer remained unbound with zero active/pending operations and `DrainComplete=0` throughout both cycles. The ordinary production-catalog `TwoSequentialMatches_ReleaseMapStateBeforeTheNextLoad` regression also passed with graphics enabled, covering stale gameplay state and handles independently of the candidate override.
- [x] Validate `0 B/frame` managed allocation from operation-map orchestration after warmup.
  - Accepted 2026-07-23: the fingerprinted packed candidate reached the production Match route with EntityScene readiness already published, then exercised the exact recurring `MenuBootstrapCompositionSystemHelper.UpdateStaticMapPresentationForLoadedMatch` boundary for `256` warmup calls and `2,048` measured calls. Total and maximum per-call main-thread managed allocation were both `0 B`. The production Match owner was already bound, so its loader polling path was inactive; the recurring boundary covered the unbound static-streamer update and unchanged-readiness publication fast path. All `16,998` section-tagged entities, the loaded EntityScene, the single operation-map root/readiness owner, and zero active/pending static-streamer operations remained unchanged. Whole-frame Editor/UI/gameplay allocation is intentionally excluded from this operation-map-attributed gate and remains separate from Android frame capture.
- [x] Validate no runtime hierarchy search, source-scene access, generator call, or collider query occurs.
  - Accepted 2026-07-23: the packed EntityScene production route does not construct `RuntimeCityCompositionSystemHelper`, while the legacy `StaticSceneChunks` route retains its existing owner. The accepted operation-map authoring scene and accepted authoring SubScene remain unloaded in stable Match and after teardown. Static method-boundary guards reject hierarchy/global searches, source asset or scene access, dense-city generation, and collider/physics queries from the recurring post-bind readiness and presentation path. The loader exits before its thin runtime-binding scene resolution once ready, and `MatchSceneView` gates source-scene transition work behind `!matchRuntimeBound`. One-time hierarchy resolution of the authorized renderer-free thin runtime-binding scene remains transition-only as permitted by the parent architecture contract; it is not part of post-bind steady state.
- [x] Validate no existing/generated map visual runtime GameObject, GameObject pool entry, `RuntimeBuildingEntityLink`, managed registry entry, or instantiated destroyed replacement exists.
  - Accepted 2026-07-23: EntityScene Match startup now excludes the legacy runtime city, blocker, and decoration presentation helpers, while `StaticSceneChunks` retains all three. The real packed route contains zero managed runtime buildings, entity-link registry entries/components, authored-building visual components, candidate building/attachment authoring components, building visual pool roots, or renderers beneath the allowed empty `RuntimeCity`, `RuntimeBuildings`, and `RuntimeBlockers` gameplay shells. Damaging a real packed operation-map building changes only its baked ECS intact/destroyed root scales and enableable destroyed state; the complete loaded GameObject identity set and total ECS entity count remain exact, proving no instantiated destroyed replacement. Player-created runtime building gameplay remains available but owns no permanent EntityScene map content.
- [x] Validate editor PlayMode completely before any Android deployment.
  - Accepted 2026-07-23: the complete graphics-enabled PlayMode assembly passed all `43/43` tests in one ordered `280.21 s` run with zero failures, inconclusive cases, or skips. The run includes repeated Menu-to-Match lifecycle stress, first-launch Addressables narrative playback, transport/scan/initial-unit gameplay, both Menu/Match music transitions, and all five packed operation-map EntityScene parity/lifecycle cases. The gate exposed stale fixture assumptions for canonical authored construction cost, scan combat capability, initial runtime camera-focus state, intentional parachute scale, and asynchronous narrative presentation; focused reruns passed after aligning those fixtures with current contracts. It also exposed a real persistent-ECS/recreated-presentation defect: scene reload stopped the old audio sources while `AudioMusicStateComponent` retained the active event, causing the new view to suppress playback. The presentation now reconciles that ECS-owned music state once on enable and replays only when no source for the same event hash is active. Audio reconciliation passed `9/9`, assembly boundaries passed `31/31`, and ECS/Burst guardrails passed `11/11`. Sandboxed Unity could not write its licensing database; guarded no-Editor IPC recovery followed by the same project wrapper with approved full access restored validation without changing project content. Production remains `StaticSceneChunks`, production cutover remains zero, and Android remains user-triggered.

**Exit:** Existing map/SubScene loading handles the complete ECS-presented physical map without a parallel lifecycle or static map-visual streamer.

### Phase 9: Android Acceptance

- [x] Build local Addressables and Android APK/AAB from a clean checkout using the documented Unity wrapper/CI route.
  - Windows isolated-content lane enabled 2026-07-25: the dense candidate runtime-content builder now accepts only supported active `StandaloneOSX` or `StandaloneWindows64` targets, derives the shared Addressables cache namespace as `OSX` or `Windows`, and passes that exact target through Addressables and Entities content creation. Focused Windows validation passes 37/37 with `StandaloneWindows64` active. No editor installation path is embedded, so Jenkins may continue resolving Unity from `D:`. The row remains open for the real isolated Windows content build and Android APK/AAB.
  - First real Windows attempt reached Addressables SBP and failed closed because Unity's Windows build extension reported `Unable to build with the current configuration, please check the Build Settings.` before bundle creation. The Windows standalone module is installed and `StandaloneWindows64` is active; licensing recovered normally and is not the cause. The failed run produced no accepted runtime-content report/output. Its one absolute-path layout-report rewrite was restored, and the builder now journals the complete five-file dense layout owner set across all later content stages. A replay reproduced the SBP rejection and restored every tracked layout output automatically. The row remains open pending correction of the Windows player build configuration.
  - Android candidate-content target lane enabled 2026-07-27: the same isolated owner now accepts Android only when Unity reports the exact Android build-target group and installed module, maps its protected shared cache to `Library/com.unity.addressables/aa/Android`, passes Android through Addressables and Entities content creation, and does not enter the Windows-only temporary Standalone scripting-backend scope. Unsupported targets and missing modules remain fail-closed. The complete focused owner suite passed 42/42 through the approved Windows GUI wrapper, and a live read-only `-buildTarget Android` probe passed with `activeTarget=Android`, `targetGroup=Android`, and `addressablesPlatform=Android`. The row remains open for the real isolated Android content build and clean APK/AAB; no production cutover is claimed.
  - Real isolated Android content accepted 2026-07-27 from a clean tracked worktree at `9f8a55a02`. The final report-owned rerun produced four local bundles totaling 915,794 bytes plus a 3,041-byte catalog, with bundle-set SHA-256 `0fdef1a9b92ed47de8bc4c22ad29f1a4d6c83613d14a8e3c9b912bc33b57e531`. The Android EntityScene contains exactly one 115,924,427-byte archive with archive-set SHA-256 `886adc6d16e6334de07f80ba23955724b1f11d9858638486806dcca07b3ac0b4`, 49,947,710 metadata bytes, and 165,872,137 total Entities bytes. Build Layout validation reports five packed runtime asset paths, zero explicit or implicit source hierarchies, and zero shared or duplicated dependency GUIDs/bytes. A preceding successful build had the same topology, archive bytes/hash, metadata, and zero-count gates but its compressed bundle total was one byte larger and its Addressables/Build Layout hashes differed; this step therefore records the final artifact fingerprint without claiming cross-build Addressables byte determinism. Production remains `StaticSceneChunks` with one manifest and 269 chunks, `productionSettingsMutated=0`, `sharedOutputRestored=1`, and `productionCutover=0`. The combined row remains open for the clean Android APK/AAB that includes this candidate content.
  - Clean APK provenance prerequisite accepted 2026-07-27: the first two production APK attempts failed closed before any build mutation because the wrapper's process-local duplicate-`PATH` cleanup made `git rev-parse HEAD` undiscoverable inside Unity, even after the parent shell could resolve Git. `AndroidBuildReportGenerator` now accepts an optional absolute `WARLINE_GIT_EXECUTABLE` override, rejects relative or missing configured paths, and preserves the existing `git`/PATH behavior when no override is supplied. Focused Android-active GUI-wrapper coverage passed 13/13. The override is invocation-only and no Unity, Jenkins, project, or production setting embeds this machine's Git path. The row remains open for the clean rerun.
  - Real candidate APK evidence 2026-07-27: after canonicalizing the duplicate process `Path` entries without serializing any machine path, the documented wrapper built `Build/AndroidDenseCandidate/WarlineCapture-DenseCityCandidate.apk` from clean pushed revision `963713b8c`. The APK is 555,819,265 bytes with SHA-256 `c1600f5797eb9469ee1fb817215143f6ffedc4779c5ed98d8925fb4081e299da`; direct ZIP inspection finds three official Entities `.archive` payloads, four embedded candidate bundles, the dense GUID in `archive_dependencies.txt`, and no production GUID. The post-build gate initially rejected the valid package because it recognized only extensionless archive filenames; it now accepts Unity's optional `.archive` suffix. The focused 8/8 marker and three existing-APK gate markers all passed, but every wrapper run was rejected because this Windows-on-ARM Editor crashed or stalled during shutdown after the marker. The checklist row therefore remains open; no wrapper pass, device launch, offline runtime, or installed-size claim is made.
  - Final wrapper acceptance 2026-07-27: the documented Windows GUI-licensing wrapper started from clean pushed revision `a3946c7b052cf7b5f87f37c09e005a244a3f7fb5`, with build provenance reporting `dirty=False`, and completed `Game.Editor.BuildScript.BuildDenseCityCandidateAndroidApk` with process exit code `0` plus the required `[DenseCityCandidateAndroidPackage] result=Passed` marker. The isolated content gate rebuilt four local Android bundles, one 115,924,427-byte dense EntityScene archive, 49,947,710 metadata bytes, zero shared or duplicated dependency bytes, `productionSettingsMutated=0`, and `sharedOutputRestored=1`. The resulting APK is 555,817,853 bytes with SHA-256 `dec15d022c10eda2f3e3e4cb397f37871fac69656af5d7cdccd77741b32ae079`; read-only ZIP inspection finds exactly one candidate catalog, four candidate bundles, explicit dense EntityScene ownership, and zero production EntityScene entries. This closes the clean wrapper-build row without claiming device-side minimap, FPS, installed-size, lifecycle, or production-cutover acceptance.
- [x] Verify application launch and Deploy entry with network disabled.
  - Accepted on Xiaomi `24090RA29G` / Android 16 on 2026-07-27: the candidate APK installed successfully, Wi-Fi and mobile data both read `0` during a cold launch, and `am start -W` reported `Status: ok`, `LaunchState: COLD`, `TotalTime: 755`, and `WaitTime: 783` ms. The process remained foreground/resumed after 25 seconds; the language selector, narrative, skip flow, and final main menu with the rendered `DEPLOY` control all completed offline with zero fatal/ANR matches. Wi-Fi and mobile data were restored to their original enabled state after every probe. The one missing optional Play Asset Delivery class did not terminate or block the local APK flow and is recorded explicitly in the device report.
- [x] Capture APK/AAB, installed size, bundle bytes, duplicate dependencies, and authoring-scene exclusion.
  - Candidate-only Windows evidence 2026-07-27: `Design/AgentReports/2026-07-27_dense_city_candidate_android_package_evidence.json` records the 555,819,265-byte APK and SHA-256, launchable package/activity, exactly Menu and Match as player scenes, three official Entities archives, four embedded local candidate bundles / 915,795 bytes, zero production EntityScene archive ownership, and the existing Build Layout's zero duplicated dependency GUIDs/bytes and zero source-hierarchy explicit/implicit assets. Six duplicate ZIP resource names are listed separately and classified as Android resource-merge names rather than candidate packed-dependency duplication. The evidence reconciles 9/9 against the retained APK. A connected device reports 712,775 KiB for the readable installed code path, but ADB cannot read its `oat` directory; complete installed size and device-side authoring absence remain open.
  - Final production-package acceptance 2026-08-11: `Design/AgentReports/2026-08-11_dense_city_final_android_package_evidence.json` binds the exact checked `557,478,145`-byte APK / SHA-256 `25e8dae2e4f19d93a2a3c994dab3dd898a8cc4cb3b1d7b0dfb608ff5afa71ff9` from source revision `6b4d268b837f085987d4287e2ca2898f0db5b064` to the Samsung-installed base APK with byte/hash equality. Android package storage accounting reports `741,172,224` code bytes, `16,035,840` data bytes, `1,085,440` cache bytes, and `758,293,504` reported total bytes; exact base length/hash are independently verified with device `stat`/`sha256sum`. The final package owns five bundles / `135,348,884` bytes, zero duplicated dependencies, zero static partitions/manifests/chunks, and zero dependencies on either protected operation-map authoring scene. The checked non-Unity evidence validator emitted `[DenseCityFinalAndroidPackageEvidence] result=Passed`; its ignored log is `Build/Logs/parent-final-android-package-evidence-validation.log`.
- [x] Capture map load/preload/unload timings and progress behavior.
  - Corrected-APK partial device evidence 2026-07-27: the exact wrapper-accepted APK cold-launched in `644 ms` (`653 ms` wait), submitted the deferred Match load, resolved candidate catalog/definition/EntityScene `c00140...cbd0`, and reached gameplay-ready `3,041 ms` after catalog load began / `3,231 ms` after the deferred Match request. Dense presentation and loading feedback were visible. Preload decomposition, unload, and the required second lifecycle remain open, so the combined row is not accepted.
  - Final production-package acceptance 2026-08-11: `Design/AgentReports/2026-08-11_dense_city_final_android_lifecycle_evidence.json` binds the exact installed `557,478,145`-byte APK / SHA-256 `25e8dae...1ff9` to two consecutive visible Menu -> Match -> Menu cycles on Samsung `SM-S918B` / `R5CTC1J02VB` without reinstall. Cold launch is `230/238 ms` total/wait. Deferred Match request to fail-closed gameplay readiness is `3,039 ms` then `2,851 ms`; the second cycle visibly records `Loading map presentation`, `90%`, and `LOADING REQUIRED DATA` before the ready map. Release logging has no teardown-duration marker, so unload timing is recorded honestly as screenshot-polling upper bounds of `5,605 ms` and `8,297 ms`; both cycles visibly return to the complete Menu and the accepted child lifecycle supplies the exact `512/2,132 ms` then `449/2,098 ms` support/candidate EntityScene decomposition and teardown contract. No fatal/ANR or package mutation occurred.
- [x] Capture peak/retained memory and verify source authoring hierarchy is absent.
  - Accepted 2026-08-09 from VRP-094 and the packed Build Layout/package ownership gates. Exact-Samsung readiness/retained PSS, RSS, graphics, native heap, and recorder peaks are recorded in `Design/AgentReports/2026-08-09_dense_city_vrp094_android_memory_comparison.json`; retained current PSS/RSS/graphics are `14.132%/15.025%/17.757%` below the frozen Phase 0 artifacts. The checked package layout contains zero explicit or implicit operation-map/SubScene authoring hierarchy assets and the baked blob owns virtualization data without a runtime bake-config dependency.
- [x] Capture CPU/GPU frame distributions, draw/SetPass calls, visible renderers, triangles, and shadow casters across representative routes.
  - First dense Match observation on Xiaomi `24090RA29G` / Android 16 on 2026-07-27 is rejected performance evidence, not acceptance: the rebuilt candidate loaded dense EntityScene `c00140f2e94a04c3084c8dcb0c18cbd0` and restored the city/building/tent presentation, but sustained only approximately `16.3-16.5 FPS` / `61 ms` per frame with about `56-74 ms` CPU main time, `2 ms` render-thread time, and `16-20 ms` GPU time. The frame contained 5,431 units, 22 models, 5 impostors, about 1.12 million triangles / 2.05 million vertices, and approximately 53 draw calls; HUD presentation averaged only `0.3-0.5 ms`. Inspection found `MatchHudMinimapMarkerSystem` scanning the complete live-unit set through three jobs and forcing dependency completion every frame. The bounded correction refreshes at the existing `0.2 s` HUD cadence, preserves player-first priority and the 1,024-marker cap, and completes the scan only on refresh frames. Windows D3D11 compilation and the focused four-test Editor suite pass; Android distributions and sustained FPS remain open until the corrected APK is rebuilt and measured on the reconnected device.
  - Corrected-APK rerun 2026-07-27 remains rejected for performance: the exact 555,817,853-byte / SHA-256 `dec15d02...ae079` APK sustained only `15.7-16.3 FPS` / `61.5-63.7 ms`, with typical CPU-main `56.7-59.8 ms`, GPU `15.6-16.4 ms`, CPU render-thread `1.8-2.3 ms`, 50 draw calls, 31 SetPass calls, 785,844 triangles, 1,423,203 vertices, 5,431 units, 22 models, and four impostors. The instrumented gameplay update averaged only `0.4 ms`; an opt-in marker rerun placed the missing cost inside `PlayerLoop` while reporting only `1.1-1.8 ms` for `RenderLoop`, but its diagnostic overhead makes it non-acceptance evidence. Android `gfxinfo` reported zero SurfaceView frames and is inapplicable. A raw Unity profiler capture was not recovered because wireless ADB disconnected. The CPU-main owner, representative-route distributions, visible-renderer count, and shadow-caster count remain open.
  - Final production-package acceptance 2026-08-11: `Design/AgentReports/2026-08-11_dense_city_final_android_performance_acceptance.json` binds nine diagnostics-disabled `120 s` routes to exact APK SHA-256 `25e8dae...1ff9` and Samsung `SM-S918B` / `R5CTC1J02VB`. Every route passes average/p10 FPS, average/p95/p99 frame, CPU average/p95, GPU average/p95, and `0 B/frame`; the final cooled route records `59.979` average FPS / `59.997` p10, CPU `11.861/13.516 ms`, GPU `14.243/14.420 ms`, and thermal status `0 -> 0`. Release profiler batch/SetPass/triangle/vertex counters are explicitly unavailable rather than misreported as zero workload; the report therefore combines the accepted diagnostic `50` draw / `31` SetPass / `785,844` triangle reference, the accepted source inventory of `13,943` active renderers / `13,901` shadow casters, and the current packed `61,925 + 14,592 = 76,517` render disposition with `7,784` fixed slots, one shared render array, and one EntityScene archive.
- [x] Capture Entities Graphics batches, gameplay/render-only entity counts, culling/LOD behavior, entity-scene integration cost, and destruction-transition frame cost.
  - Accepted 2026-08-09 from the packed sharing/parity reports, VRP-080 through VRP-086, and exact-Samsung VRP-092/VRP-095/VRP-096 evidence. Packed output owns one shared `RenderMeshArray` and one EntityScene archive; direct and packed reports record complete gameplay/resident/logical/render identities and exact `76,517 = 61,925 + 14,592` source disposition. Camera traversal changes fixed-slot materialization without simulation-count or gameplay-state changes, starts zero map-content operations, and retains LOD0-only visual continuity. The accepted lifecycle log records support/candidate EntityScene stream latency on both cycles (`512/2132 ms` then `449/2098 ms`), while representative selected-building destruction passes the full frame/CPU/GPU/allocation gates and the dedicated state route proves visible/off-camera destruction plus deterministic destroyed-only return with zero overflow/deficit.
- [x] Compare the Android Addressables-loaded transform/bounds parity report with the accepted Editor manifest and reject any moved, rotated, scaled, duplicated, or missing permanent map visual.
  - Final production-package acceptance 2026-08-11: `Design/AgentReports/2026-08-11_dense_city_final_android_transform_bounds_parity.json` reconciles the exact Android-target five-bundle package and dense EntityScene `c00140...cbd0`, streamed twice from the installed APK, against the exhaustive accepted Editor-to-baked manifest (`36,304/36,304` identities and `62,455/62,455` renderers, zero mesh/material/base-color/matrix/scale/bounds/missing/unexpected/rejected mismatch), current logical database parity (`76,517 = 61,925 + 14,592`, `40,460` placements, `7,784` slots), accepted two-state materialization, and nine final Android visual routes. The release build intentionally emits no 62,455-row device dump; the report states this qualification and relies on immutable package provenance, checked exhaustive manifests, two-cycle load identity, the absence of an Android-specific transform mutation owner, and noncontradictory Android visuals. No moved, rotated, scaled, duplicated, missing, or bounds-mismatched permanent visual is accepted.
- [x] Capture steady-state GC, transition allocations, thermal behavior, and sustained FPS.
  - Accepted 2026-08-09 through VRP-092, VRP-093, and VRP-097. Every accepted representative route records `0 B/frame`; focused gates prove stable-envelope/stable-command no-op allocation and asynchronous apply ownership; the final `120.007 s` cooled route sustains `59.980` average FPS / `59.997` p10 FPS with thermal status `0 -> 0`, `0%` battery drain, and passing frame/CPU/GPU distributions.
- [x] Validate navigation, placement, targeting, aircraft clearance, camera, minimap, runway, helipad, and scenario anchors.
  - The same 2026-07-27 dense Match observation rejected the minimap sub-gate: `Build/AndroidDenseCandidate/validation/zoomout.png` showed the map frame/compass but no unit markers. The marker producer cleared its buffer every frame while `[WithChangeFilter(typeof(LocalTransform))]` skipped unchanged units, so most frames published an empty buffer. The correction removes that incompatible change filter, retains the previous marker snapshot between bounded refreshes, replaces unsafe parallel `NativeList` writes with ordered player/enemy/scan-intel collection, and caps before every add. `MatchHudMinimapMarkerSystemTests` passes 4/4 in the Windows Unity Test Runner, including a regression that updates the system twice without a transform change and requires the marker to remain. Device visual acceptance remains open.
  - Minimap sub-gate accepted 2026-07-27 on the corrected APK: both the initial dense Match frame and a later sustained capture visibly retain the compact minimap frame, orientation indicator, map raster, and a nonempty player-marker set. The later 2712x1220 PNG is 2,730,474 bytes with SHA-256 `fe3fb954...c0c48`; it is retained as transient build evidence while its identity and measurements are committed in `Design/AgentReports/2026-07-27_dense_city_candidate_android_minimap_performance_rerun.json`. Navigation, placement, targeting, aircraft, camera traversal, runway, helipad, and scenario anchors remain open, so the combined checklist row is not accepted.
  - Accepted 2026-08-09 from retained exact-Samsung Phase 0A/Phase 9 interaction and representative-route evidence plus fresh checked focused gates. The device evidence retains selection/targeting, blocked and successful placement, helicopter delivery, camera traversal, and the corrected minimap without a contradictory gameplay or visual result. `Build/Logs/parent-navigation-current-map-anchors-canonical-hash.log` passes `4/4` for the current production definition, deployment cells, runtime catalog identity, and deterministic runway/helipad anchors; `Build/Logs/parent-navigation-aircraft-scenario-focused.log` passes `3/3` for fixed-wing runway initialization/teardown, helipad spawn consumption/teardown, and typed scenario deployment anchors; `Build/Logs/parent-navigation-helipad-pathing-atomic-version-matched.log` passes `2/2` for helipad walkability/blocker exclusion. The July 19 runtime cutover had left the focused test and compatibility-definition rebuild helper pointing at the retired Match SubScene, and the Windows checkout exposed a CRLF-versus-LF hash false failure. Both tooling owners now resolve the established production operation-map SubScene and hash text inputs with canonical LF bytes. The production definition asset and gameplay content remain unchanged. The broad initial-base fixture remains rejected because its unrelated automatic-base-count expectation is stale, and three Unity Test Framework invocations that failed before tests on the known Hub licensing pipe race contribute no pass claim.
- [x] Validate entity LOD/impostor transitions, seams, missing geometry, and visual parity when applicable; confirm camera travel performs no map-visual scene streaming.
  - Accepted 2026-08-09 from VRP-072, VRP-083, and VRP-086 plus retained Android routes. No alternate LOD/impostor recipe exists, so transition validation is not applicable to the LOD0-only representation. Five fixed views, edge traversal, chained zoom/pan/follow, and final Android captures retain complete geometry without holes, seams, leakage, popping, or stale state. Instrumented camera travel starts zero map-scene, Addressables, or static-streamer operations and changes no simulation entity count/gameplay state.
- [x] Capture before/after destruction views for representative house, shop, tent, and military building variants and verify no intact shell, roof prop, interior dressing, sign, awning, or tent content remains visible after replacement.
  - Final closeout accepted 2026-08-11 in `Design/AgentReports/2026-08-11_dense_city_final_destruction_family_acceptance.json`. The accepted VRP-067 House/Shop pairs retain their exact image hashes and complete generated-family intact/destroyed recipe ownership. A fresh no-reinstall route on the exact final Samsung package selects the authored canonical `Contractor Tent` at `350/350` and visibly replaces its canvas/interior/complete target with rubble; the same-package `Barracks` pair selects the intact `1200/1200` military target and replaces it with its ruined presentation. Paired review plus the fail-closed state-owner contract reports zero target-owned intact shell, roof prop, interior, sign, awning, or tent-content leakage. Adjacent tents, containers, vehicles, and the second barracks remain because they are independent owners, not leaked children. Historical profiler performance is not reused; final performance remains independently accepted. The app was force-stopped after capture and no package or protected/production asset changed.
- [x] Compare against Phase 0 baseline and resolve every failed budget before acceptance.
  - Accepted 2026-08-09. VRP-094 records exact frozen/current memory deltas, VRP-092 accepts every representative route under the amended average/p10 and unchanged p95/p99/CPU/GPU gates, VRP-093 accepts allocation/capacity/rebuild ownership, and VRP-097 accepts the final cooled thermal route. Historical rejected packages and warm/measurement runs remain rejected and were not reused as acceptance evidence.

**Exit:** The existing ECS-migrated map and giant city are accepted on the target Android device, not merely in Editor or by successful build.

### Phase 10: Documentation And Closeout

- [x] Document the exact author workflow: edit overrides, clear/regenerate city, validate, Bake All, review report, device test.
  - Accepted 2026-07-25 in `Design/Architecture/dense_city_author_workflow.md`: the workflow separates persistent authored overrides from disposable generated ownership, uses the transactional candidate hierarchy/realization/material commands, enumerates the fail-closed validation order and candidate-only Bake All command, identifies tracked evidence, preserves production `StaticSceneChunks`, and keeps Android/device acceptance explicitly open.
- [x] Document existing-map authored ECS editing, building/vehicle authoring, SubScene workflow, static rollback artifacts, and entity-scene cutover/rollback.
  - Accepted 2026-07-25 in `Design/Architecture/operation_map_authored_ecs_workflow.md`: the workflow identifies source/candidate/runtime boundaries, exact presentation roles, building/vehicle/render-only authoring contracts, candidate migration and validation commands, frozen static ownership, cutover prerequisites, and both candidate and committed-production rollback paths. It explicitly states that candidate success does not authorize production cutover or static-package deletion.
- [x] Document generated versus authored ownership and how to preserve hand edits.
  - Accepted 2026-07-25 in `Design/Architecture/dense_city_generated_authored_ownership.md`: an explicit ownership matrix and hierarchy contract distinguish persistent authored sources, two disposable generated roots, derived candidate/evidence outputs, and the frozen rollback package. The promotion procedures cover generator-family changes, exclusion/correction overrides, and persistent visual/gameplay objects without preserving candidate-instance edits.
- [x] Document every generated output owner and rollback path.
  - Accepted 2026-07-25 in `Design/Architecture/dense_city_generated_output_ownership.md`: the owner matrix covers candidate scenes and marked roots, proxy meshes, shared/generated material and legacy road support folders, candidate SubScene/definitions/thin bindings/layout, isolated Addressables and Entities content, runtime parity binary, tracked report/capture families, transient logs/caches, and the frozen static rollback package. It distinguishes transaction-journaled rollback from deterministic regeneration plus version-control rollback where no journal exists.
- [x] Update the parent operation-map tracker with accepted dense-city implementation evidence.
  - Accepted 2026-07-25 in `Design/Architecture/operation_map_scene_split_and_generator_tracker.md`: the parent now records the dense candidate's ECS presentation, semantic/proxy, isolated packaging, parity, and ECS-only destruction evidence together with its exact non-cutover boundary. Parent checklist counts remain unchanged because production remains `StaticSceneChunks` and Android/package/performance/static-retirement gates are still open.
- [x] Update architecture/naming guardrails for all new types and exceptions, if any.
  - Accepted 2026-07-25 in `Design/Architecture/gameplay_solid_ecs_contract.md` and `Design/Architecture/file_naming_architecture_contract.md`: the legacy managed map-building conversion rule is now scoped to `StaticSceneChunks`, while the candidate `EntityScene` branch is explicitly ECS-only across authoring, unmanaged data, Burst destruction, baking, composition/readiness, and editor production boundaries. The naming contract classifies every new suffix family and confirms that the two bare `*System` names are real `ISystem` implementations; no new product-prefix, broad-shell, non-ECS system, static-registry, or naming-escape exception is approved.
- [x] Record final deterministic hashes, counts, package sizes, performance evidence, logs, and screenshots.
  - Local-bundle fingerprint foundation accepted 2026-07-25: schema 7 of the next isolated runtime-content report records a machine-independent SHA-256 over every sorted relative `.bundle` path, exact byte length, and individual file SHA-256. Focused fixtures prove identical bytes produce the same 64-character hash and a changed bundle changes the aggregate. The row remains open for the real packed bundle hash, final package/device counts, performance logs, and screenshots.
  - EntityScene archive fingerprint foundation accepted 2026-07-25: schema 8 applies the same relative-path/byte/file-hash manifest contract to the sole packed `.archive` set. The report remains prospective until the real packed content lane runs, so final package/device evidence is still open.
  - Final closeout accepted 2026-08-11 in `Design/AgentReports/2026-08-11_dense_city_final_evidence_index.json`. The index binds source revision `6b4d268b8`, exact host/device APK SHA-256 `25e8dae...1ff9`, five final map bundles / `135,348,884` bytes with package-derived aggregate SHA-256 `d631e2b3...bf07e`, and three APK ContentArchives / `118,765,607` bytes with aggregate SHA-256 `30e4ad6b...b7846`. It records the current database content/order/config hashes, `76,517 = 61,925 + 14,592` render disposition, `40,460` placements, `7,784` slots, exhaustive `36,304`-identity / `62,455`-renderer zero-mismatch parity, package/storage sizes, frozen rollback bytes, all nine accepted route recorder hashes/metrics, final lifecycle report, representative route logs and screenshots with exact bytes/SHA-256, and the separate destruction-family evidence. The aggregate hashes use the repository's sorted `path|length|sha256` plus LF contract; rejected route samples remain excluded.
- [x] Confirm all generated artifacts are committed through their accepted ownership policy and transient logs/build caches are excluded.
  - Repository-audit foundation accepted 2026-07-25: `Tools/CI/dense_city_generated_artifact_ownership_audit.py` binds six required tracked ownership-contract/candidate paths, all tracked persistent candidate-output families, all dated dense-city evidence, forbidden transaction-backup/temp-owner names, and eight `.gitignore` probes for dense runtime content, runtime-content transactions, runtime parity, Unity temp/log output, Android build scratch, and Bee scratch. The accepted report has zero missing required paths, tracked transient paths, forbidden transaction artifacts, or unignored probes. This row remains open because the final artifact/evidence set and Android acceptance are incomplete.
  - Final closeout accepted 2026-08-11. The audit now has an explicit fail-closed `--final-closeout` mode that additionally requires eleven committed final trackers/reports and five Android acceptance reports while preserving the original foundation behavior. Seven focused Python tests pass, including complete and missing-final-evidence cases. `Design/AgentReports/2026-08-10_dense_city_final_generated_artifact_ownership_audit.json` reports `PASS`, `finalArtifactSetComplete=true`, `androidAcceptanceComplete=true`, six required ownership paths present, eleven final paths present, five Android paths present, `4,803` tracked persistent-output files, `111` tracked dense evidence files including the staged final audit itself, zero tracked transient or forbidden transaction artifacts, and all eight transient ignore probes accepted. Build/log/cache/device evidence remains ignored. The five pre-existing modified generated reports and fourteen untracked `_Recovery` scene/meta files remain preserved and are not claimed as closeout-owned changes.
- [x] Mark this tracker complete only after every required checklist item and Android gate passes.
  - Final completion accepted 2026-08-11 after the child tracker reached `80/80` with status `Complete`, all other parent rows reached `148/149`, and a zero-open-row audit proved this final row was the sole remaining checklist item. The exact `557,478,145`-byte APK / SHA-256 `25e8dae...1ff9` on Samsung `SM-S918B` / `R5CTC1J02VB` passes two lifecycle cycles, nine diagnostics-disabled `120 s` routes, the amended average `>=54 FPS` / p10 `>=50 FPS` / average frame `<=18.6 ms` contract, unchanged p95/p99/CPU/GPU/GC gates, and final thermal status `0 -> 0`. Deterministic package/database/parity/destruction/rollback evidence is indexed; final generated-artifact ownership reports both completion booleans true with zero violations. The app is force-stopped and device display settings are restored. Preserved modified reports and `_Recovery` scenes remain untouched. Blue Autobahn/canal isolation and road-over-water-without-bridge remain separately classified evidence and are not conflated by this tracker closeout.

**Exit:** Another agent can reproduce, validate, maintain, and roll back the complete workflow without undocumented assumptions.

## 22. Required Focused Tests

Planned test files in `Assets/Tests/Editor`:

- `DenseCityDeterministicFixtureTests.cs`
- `DenseCityGeneratedRootAuthoringTests.cs`
- `DenseCitySemanticHierarchyBuilderTests.cs`
- `DenseCityGenerationRecordTests.cs`
- `DenseCityPhysicsComponentStripperTests.cs`
- `DenseCitySurfaceProxyBuilderTests.cs`
- `DenseCityBakeReadinessValidatorTests.cs`
- `DenseCityPresentationBudgetValidatorTests.cs`
- `DenseCityBakeAllIntegrationTests.cs`
- `DenseCityRuntimePackageOwnershipTests.cs`
- `OperationMapBuildingAuthoringTests.cs`
- `OperationMapBuildingAttachedVisualOwnershipTests.cs`
- `OperationMapBuildingDestructionSystemTests.cs`
- `OperationMapEntityPresentationMigrationEditorTests.cs`
- `OperationMapEntityPresentationReadinessValidatorTests.cs`
- `OperationMapEntityPresentationTransformParityTests.cs`
- `OperationMapEntitySceneAddressablesOwnershipTests.cs`

Extend existing tests rather than duplicating them for:

- static presentation input, structural validation, ownership, rollback, integrity, and no-op behavior, including freeze/retirement and rejection of every existing/generated renderer after cutover;
- Entities Graphics baking for existing/generated gameplay and render-only placements, stable identities, intact/destroyed references, shared asset ownership, and map SubScene ownership;
- source/candidate/baked/runtime transform matrix and transformed-renderer-bounds parity, including nested hierarchy, pivot, negative/non-uniform scale, `Parent`, and `PostTransformMatrix` fixtures;
- operation-map Addressables layout and Build Layout validation;
- runtime binding scene generation/validation;
- scene/SubScene loading, complete entity readiness, absence of static presentation binding, handle lifetime, teardown, and sequential map loads;
- architecture naming, assembly direction, source growth, and ECS/Burst classification.

## 23. Required Validation Commands And Evidence

- Use `Tools/CI/invoke_unity_macos.sh` for every Unity batch validation.
- Keep Unity Hub open and signed in according to `AGENTS.md`.
- Store verbose logs under `/private/tmp`; record concise paths/results in the validation log.
- Run `git diff --check` before every stable commit.
- Run focused EditMode tests for the changed slice.
- Run affected architecture/naming/source-growth tests.
- Run compile validation with zero compiler errors.
- Run deterministic generate/bake twice and compare accepted output hashes.
- Run PlayMode lifecycle before Android.
- Run Android build/device validation according to the phase gates.
- Never claim visual or performance acceptance from static tests alone.

## 24. Validation Log

| Date | Slice | Evidence | Result | Notes |
|---|---|---|---|---|
| 2026-08-05 | Post-cold-binding fresh profiler APK preparation | Child package-readiness gate; implementation/package head `aee4403d0`; checked GUI-licensing short-path build `Build/Logs/editor-batch-aee4403d0-profiler-apk-shortpath.log` `[DenseCityCandidateAndroidPackage] result=Passed`; exact final 696,278,899-byte APK SHA-256 `1c6563a495998654eb9f1807c71e34aead1c79ce4e55788af3306dd4f978e395`; verified temporary `W:\` mapping removed; bundled ADB 36.0.0 restart/discovery | Fresh package accepted; exact Samsung install and consolidated confirmation remain; parent progress stays `126/149 (85%)` | The package was built from local/origin `aee4403d0` only after the cold candidate repair passed both stages and the aggregate remained 93/93. Samsung `SM-S918B` / `R5CTC1J02VB` advertises `adb-R5CTC1J02VB-lJmfCF` at `192.168.2.33:33675`; the port is reachable, but ADB authentication/connect currently fails. No install, launch, Deploy interaction, or functional acceptance is claimed. The active heartbeat automation retries only that exact device and will install only this hash. VRP-072 remains unstarted. Both generated reports and all `_Recovery` scenes remain unstaged; no protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |
| 2026-08-05 | Cold candidate runtime-binding component migration | Child package-readiness gate; accepted checked GUI repair stage `Build/Logs/candidate-runtime-binding-correct-order-repair.log` `[CandidateRuntimeBindingColdRepairValidation] result=Passed scenes=2`; separate fresh-Editor focused `Build/Logs/candidate-runtime-binding-correct-order-cold-focused.log` `[OperationMapEntitySceneCandidateBakeAllValidation] result=Passed tests=24`; checked aggregate `Build/Logs/editor-first-functional-batch-candidate-cold-repair.log` `[EditorFirstProductionFunctionalBatchValidation] result=Passed suites=8 tests=93`; `git diff --check` | Cold candidate binding is repaired and Editor-accepted; fresh checked APK and consolidated Samsung confirmation remain; parent progress stays `126/149 (85%)` | The first post-`b63d0836c` package attempt rejected before Android build because a cold Editor could not resolve the candidate Decorations `CombinedMeshBaker`; licensing was not the cause. Repository scenes/prefabs identify the serialized source as global `CombinedMeshBaker` in `Assembly-CSharp`, so the runtime class's incorrect `MovedFrom` source assembly is corrected. Candidate-only regeneration now copies the typed production binding without saving production, removes exactly one stale baker row, creates exactly one baker, normalizes its embedded script to the checked-in GUID, and avoids stale-offset YAML edits. Both candidate scenes carry the established serialized identity and pass a separate cold Editor at 24/24; the aggregate remains 93/93. No APK or device acceptance is claimed yet. VRP-072 remains unstarted. Both generated reports and all `_Recovery` scenes remain unstaged; no protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |
| 2026-08-05 | EntityScene vehicle-readiness propagation and licensing recovery | Child independent correctness gate; user-authorized Windows licensing recovery ladder recorded in `AGENTS.md`; rejected checked GUI compile log `Build/Logs/map-vehicle-entityscene-readiness-editor-authorized-recovery.log`; rejected nonexistent namespaced execute-method log `Build/Logs/map-vehicle-entityscene-readiness-editor-recovery-rerun.log`; accepted checked GUI focused `Build/Logs/map-vehicle-entityscene-readiness-editor-recovery-corrected.log` `[MapVehiclePlacementStartupCompletionValidation] result=Passed tests=6`; accepted checked GUI aggregate `Build/Logs/editor-first-functional-batch-entityscene-readiness-recovery.log` `[EditorFirstProductionFunctionalBatchValidation] result=Passed suites=8 tests=93`; `git diff --check` | Packaged Armor readiness is repaired and Editor-accepted; fresh checked APK and consolidated Samsung confirmation remain; parent progress stays `126/149 (85%)` | EntityScene mode now explicitly requires the packed presentation-readiness contract even when the compatibility vehicle authoring root remains assigned, closing the condition disproved by the prior Samsung `NoCandidates` result. The recovery ladder kept licensing from becoming a passive blocker; once the recovered GUI path reached compilation, a value-type test fixture argument was corrected from `null` to `default`, and the stale namespaced execute-method spelling was replaced by the actual global class. Focused 6/6 and aggregate 93/93 then passed. No APK was built or installed yet. VRP-072 remains unstarted. Both generated reports and all `_Recovery` scenes remain unstaged; no protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |
| 2026-07-21 | Initial audit and tracker | Scene YAML/source audit; editor generation log | Design only | Previous generated city was cleared. No implementation or bake output is accepted by this tracker yet. |
| 2026-07-21 | ECS-first generated presentation decision | Sections 1-16, 19, and 21-25 of this tracker; parent tracker direction link | Design only | All generated permanent visuals target map SubScene entities with shared Entities Graphics assets. Generated-city GameObject chunks/pools are rejected; the existing static package is frozen rollback evidence pending the full-map cutover. |
| 2026-07-21 | Full current-map ECS presentation decision | Existing-map 16,542-source / 514-chunk / 451-building / 29-vehicle accepted baseline; revised sections 1-25 | Design only | Existing and generated map visuals now share one ECS SubScene presentation target. Current static scenes remain frozen rollback evidence during migration and leave production package/runtime ownership after accepted cutover. |
| 2026-07-21 | Building-attached visual ownership decision | Building authoring/data/runtime contracts plus Phases 0A, 1, 2, 5, 6, and 9 | Design only | Roof, interior, shop, tent, sign, awning, lamp, furniture, and utility visuals attached to a damageable building belong to exactly one intact/destroyed entity hierarchy and transition atomically with it. |
| 2026-07-21 | Editor-to-runtime transform parity defect accepted | Section 12.0A; Phase 0A/5/7/9 gates; audit of `OperationMapBuildingCandidateMigrationEditor`, `OperationMapVehicleCandidateMigrationEditor`, and `OperationMapRenderOnlyCandidateMigrationEditor` | Design gate added; implementation pending | The current candidate path has no source-to-baked-to-Addressables matrix proof. Render-only copies preserve world transforms, while building/vehicle candidates reconstruct placement transforms. Production cutover is now blocked until deterministic matrix and transformed-bounds parity passes twice in Editor and once on the user-triggered Android build. |
| 2026-07-21 | Phase 0 deterministic editor baseline | `OperationMapPhase0BaselineProbeTests`: 8/8 passed; two `/private/tmp/warline-phase0-baseline-*.json` captures differed only by their declared report path; concise logs at `/private/tmp/warline-phase0-baseline-tests.log` and `/private/tmp/warline-phase0-probe-{a,b}.log` | Partial pass | Schema v2 recorded the clean canonical authoring scene and map SubScene identities, 15,792 GameObjects, 15,671 renderers, 15,584 mesh renderers, 13,317 prefab-instance roots, 27 bake groups, zero generated roots, zero prohibited physics components, 269 static chunks, 11,892 static sources, protected-root candidate GlobalObjectIds, and current surface/manifest/integrity/minimap/runtime-binding identities. The checked-in Addressables Build Layout predates this manifest state, so package identity and authoring-scene exclusion rows remain open pending a post-commit rebuild. Android evidence and protected-root approval also remain open. |
| 2026-07-21 | Phase 0 post-commit Addressables baseline | `OperationMapAddressablesBuildReportBuilder.Run`; `/private/tmp/dense-city-phase0-addressables.log`; `/private/tmp/dense-city-phase0-post-addressables.json`; `OperationMapAddressables*`: 17/17 passed | Passed | Initial rebuild against `d4ad9cc38` proved authoring-scene exclusion for the 269-chunk/11,892-source package. Later superseded on `main` by `8f93dafe0` shader-bundle dedupe: build hash `872c797d60c0cbd26e4e430bb1b210f1`, 119 bundles, 119,993,045 aggregate bundle bytes. Android device evidence remains open. |
| 2026-07-21 | Isolated deterministic-city fixture | `DenseCityDeterministicFixtureTests`: 1/1 passed; `/private/tmp/dense-city-deterministic-fixture-tests.xml`; `/private/tmp/dense-city-deterministic-fixture-tests.log` | Passed | Built and cleared a bounded one-city fixture twice from an in-memory config clone, produced identical hierarchy hashes, and verified the canonical operation-map scene SHA-256 did not change. The fixture uses an isolated batch scene or additive saved-scene path and creates no project asset. |
| 2026-07-21 | Phase 0 protected-root confirmation | `Design/AgentReports/2026-07-21_dense_city_phase0_protected_roots_and_approvals.md`; baseline probe protected-root GlobalObjectIds | Passed | Confirmed eight present protected roots (`Buildings`, `Vehicles`, `Runways`, `Roads`, `Mountains` x2, `ResourceAreas`, `DenseCity_GradingArchive`) by GlobalObjectId. `AuthoredCityOverrides` is absent and remains a Phase 1 create-once root. The hierarchy/semantic and outside-grid decisions were subsequently approved by the project owner. |
| 2026-07-21 | Phase 0A inventory, GPT 5.6 cutover review, and non-mutating planner | Focused inventory/record/planner/root-marker tests: 37/37 passed; inventory SHA-256 `a0b1c332ce715a5346785c0727cee9dad1b70f78e1895a2618df682edfa8c66d`; dry-run record-set hash `6e771d490511963753ad32cc8018f8952de947b6d56bf71e9c1badc1d84bdda2`; placement-join set hash `fd4679d4f07d2a82e058c9617e467ce9120fa151fdaa19ba35ef11eaa4c20709`; `Design/AgentReports/2026-07-21_gpt56_phase0a_inventory_review_and_grok_handoff.md` | Passed | Resolved all 11,892 renderer-component identities, partitioned them into 9,090 non-overlapping migration owners, and joined all current 432 building / 22 vehicle placements with zero unresolved, ambiguous, mixed, or reused identities. Classified 10,683 static render-only and 1,209 protected authored sources. Dispositioned 696 controller-free inert Animators for omission; found zero blocking dependencies or external scene-object references. The real inventory produces `StaticOwnersReadyGameplayOwnersPending`; no accepted scene, SubScene, presentation mode, Addressables, or rollback artifact was mutated. Production mutation remains blocked on the Android baseline and later GPT 5.6 cut-point reviews. |
| 2026-07-21 | Phase 0 owner design approvals | Project-owner decisions captured in `Design/AgentReports/2026-07-21_dense_city_phase0_protected_roots_and_approvals.md` | Passed | Approved tracker sections 5 and 6 as the generated hierarchy/semantic ownership contract and approved presentation-only as the default for generated city content outside the gameplay grid. Android current-revision device evidence is the only remaining Phase 0 exit item. |
| 2026-07-21 | Phase 0 Android current-revision baseline | `adb install -r -d` Success on `R4M7PZEQZ58T59ZH`; `Tools/CI/dense_city_phase0_android_baseline_capture.py`; `Design/AgentReports/2026-07-21_dense_city_phase0_android_baseline.{md,json}`; APK SHA-256 `dab470fd296b9f2ca2866ba6042940137cce36beabaa886bdf2ce7dab9f8bc44` | CharacterizationCaptured | Dirty `8f93dafe0` release APK 459,188,380 bytes; installed ~631.6 MiB via `du` base.apk+lib. Match TOTAL PSS 2,489,021 KiB; Graphics 1,096,532 KiB; menu-after-unload 1,268,879 KiB. SurfaceFlinger 60-window average FPS 58.42 (min 30.59). Match-ready upper bound 40.79 s; unload-to-menu 16.08 s. Release logcat lacked draw/GC diagnostic markers (null). Phase 0 exit complete; not Phase 9 acceptance. |
| 2026-07-21 | Phase 0A vehicle ECS already-produced proof | `OperationMapVehicleEcsConversionInventoryProbeTests`: 4/4 passed; probe result `AllPlacementsAlreadyProduceEcs`; `Design/AgentReports/2026-07-21_dense_city_phase0a_vehicle_ecs_conversion_inventory.{md,json}` | Passed | All 22 vehicle placements exact-join to authored sources, resolve prefabs with `UnitGridAuthoring` vehicle motion + Model renderers + destroyed visuals, and hide authoring after spawn. Zero unresolved joins and zero conversion cleanup rows. Runtime path is `MapVehiclePlacementSpawnPrefabSystemHelper` → `RuntimeUnitPrefabSystem` → `UnitGridAuthoring.UnitGridBaker`. No scene/SubScene/Addressables/presentation-mode mutation. |
| 2026-07-21 | Phase 0A RuntimeBuildingEntity dependency inventory | `OperationMapRuntimeBuildingEntityDependencyInventoryProbeTests`: 5/5 passed; probe result `AllPlacementsRequireManagedRuntimeBuildingEntity`; `Design/AgentReports/2026-07-21_dense_city_phase0a_runtime_building_entity_dependency_inventory.{md,json}` | Passed | All 432 building placements exact-join and still require managed `RuntimeBuildingEntity` presentation. Catalogued 10 managed dependency surfaces with ECS ownership proposals. Feature counts: destroyed visuals 266, production 20, resource 70, runway 1. No scene/SubScene/Addressables/presentation-mode mutation. |
| 2026-07-21 | Phase 0A building attachment ownership inventory | `OperationMapBuildingAttachmentOwnershipInventoryProbeTests`: 6/6 passed; probe result `AttachmentOwnershipInventoryComplete`; `Design/AgentReports/2026-07-21_dense_city_phase0a_building_attachment_ownership_inventory.{md,json}` | Passed | Assigned 1,324 intact + 272 destroyed renderer attachments across 432 exact building joins with zero orphans, shared claims, or dual-state conflicts. Ownership uses exact source ancestry and configured destroyed-prefab references only; no name/proximity role inference. No scene/SubScene/Addressables/presentation-mode mutation. |
| 2026-07-21 | Phase 0A six-role owner classification | `OperationMapEntityPresentationOwnerClassificationProbeTests`: 11/11 passed; probe result `OwnerClassificationComplete`; `Design/AgentReports/2026-07-21_dense_city_phase0a_owner_classification.{md,json}`; mutation handoff `Design/AgentReports/2026-07-21_gpt56_phase0a_mutation_ready_handoff.md` | Passed | Classified 9,544 owners: 432 GameplayBuilding, 22 GameplayVehicle, 9,090 RenderOnlyEntity, plus MapMetadataProxy/ApprovedManagedBoundary catalogs; zero RejectedUnresolved. Authored buildings/vehicles are classified from exact placement joins because they are absent from the static-presentation manifest. All 432 buildings require managed RuntimeBuildingEntity until GPT ECS cutover. |
| 2026-07-21 | Phase 0A presentation-validation scaffolding + mutation readiness gate | `OperationMapPresentationKindContractTests`: 3/3 passed; `OperationMapEntityPresentationMutationReadinessTests`: 3/3 passed; `Design/AgentReports/2026-07-21_dense_city_phase0a_mutation_readiness_gate.md` | Passed | Confirmed fail-closed `EntityScene` content-reference/canonical-mode scaffolding remains inactive on production. Added `TryEvaluateMutationReadiness`; live Phase 0A evidence evaluates to `CandidateTransactionReadyPendingMutation`. No scene/SubScene/Addressables/presentation-mode mutation. |
| 2026-07-21 | Phase 0A protected candidate + building ECS transaction implementation | Roslyn compile using Unity Bee response files: `Game.Components`, `Game.Configs`, `Game.Authoring`, `Game.Runtime`, `Game.Editor`, and `Game.Tests.Editor` passed; accepted scene SHA-256 `f210e53df16b9dad1c96457b4e72c03260ac8ab215a3f4e959b97d54c904be88`; accepted SubScene SHA-256 `eff3ce6992d234c7438a321f0f9f552c2abebcc0a4738445014bc8f86579965d`; handoff `Design/AgentReports/2026-07-21_gpt56_phase0a_candidate_building_conversion_grok_handoff.md` | Implementation passed; mutation blocked | Added a fail-closed separate-GUID candidate scene transaction, exact 432-placement building copy transaction, explicit intact/destroyed baked visual roots, ECS identity/combat/resource/production data, and a static-blocker-safe destruction visual transition. The required wrapper reached Unity but failed before `executeMethod` because headless licensing rejected protocol `1.18.1`; no candidate asset was created and production remains `StaticSceneChunks`. Checklist count remains 17/144 until Unity executes, bakes, and validates the candidate. |
| 2026-07-21 | Phase 0A candidate mutation retry + render-only plan | `/private/tmp/dense-city-candidate-hierarchy.log`; restored inventory SHA-256 `a0b1c332ce715a5346785c0727cee9dad1b70f78e1895a2618df682edfa8c66d`; `OperationMapRenderOnlyCandidateMigrationPlanner(.cs)` + tests; `Design/AgentReports/2026-07-21_dense_city_phase0a_candidate_mutation_retry_and_render_only_plan.md` | Mutation still blocked; planner ready | Re-ran `CreateProtectedCandidateHierarchy` through `invoke_unity_macos.sh`; same headless licensing protocol `1.18.1` failure before `executeMethod`. Restored the accepted inventory onto the probe default tmp path. Added a fail-closed Map-child-folder → `RenderOnly/*` bucket planner covering all 9,090 owners (Props 6880, Vegetation 928, RoadsAndBridges 825, Terrain 442, Mountains 13, Horizon 2). Accepted scene/SubScene hashes unchanged; candidate folder still absent; progress remains 17/144. |
| 2026-07-21 | Phase 0A protected candidate + 432 building populate | Non-batchmode Unity executeMethod logs `/private/tmp/dense-city-candidate-hierarchy-gui.log`, `/private/tmp/dense-city-populate-buildings-gui.log`; focused EditMode `/private/tmp/dense-city-candidate-tests-gui4.xml` 18/18; `Design/AgentReports/2026-07-21_dense_city_phase0a_candidate_hierarchy_and_buildings_executed.md` | Passed | Stopped stuck batchmode; GUI licensing path succeeded. Created candidate GUID `0f9ecd54a7f0f467fa35556af7d28f1d` ≠ source `d50925a18e9164ce782536576cb833d8`; populated 432 `OperationMapBuildingAuthoring` with 432 IntactVisual + 266 DestroyedVisual; zero Collider/Rigidbody in candidate YAML; accepted scene/SubScene hashes unchanged; production not referenced. Fixed four focused test fixtures (canonical opmap ids, job Complete, untitled-scene SetUp). Production remains `StaticSceneChunks`. |
| 2026-07-21 | Phase 0A candidate render-only owner copy | `OperationMapRenderOnlyCandidateMigrationEditor.PopulateCandidateRenderOnlyOwners`; `/private/tmp/dense-city-populate-render-only-gui.log`; planner tests 3/3; `Design/AgentReports/2026-07-21_dense_city_phase0a_candidate_render_only_owners_copied.md` | Passed | Copied all 9,090 render-only owners into candidate `RenderOnly/*` buckets (Props 6880, Vegetation 928, RoadsAndBridges 825, Terrain 442, Mountains 13, Horizon 2, Infrastructure 0) using authored Map-child-folder identity only. Preserved 432 buildings; candidate ~35 MiB; zero Collider/Rigidbody; accepted hashes unchanged; productionCutover=0. |
| 2026-07-21 | Phase 0A candidate bake validation | `OperationMapEntityPresentationCandidateBakeValidator.BakeAndValidateCandidateEntityPresentation`; `/private/tmp/dense-city-candidate-bake-validate3.log`; `Design/AgentReports/2026-07-21_dense_city_phase0a_candidate_bake_validation.json` | Passed | In-memory bake: `CandidateBakeValidationPassed` — gameplay buildings 432, presentation roots 3, building presentations 432, building render children 698, render-mesh entities 13,909, non-finite transforms 0, managed building companions 0. Accepted hashes unchanged; productionCutover=0. |
| 2026-07-21 | Phase 0A candidate vehicles + shared art ownership | `PopulateCandidateGameplayVehicles`; `/private/tmp/dense-city-populate-vehicles-gui.log`; `ProveSharedArtOwnership`; shared-art tests 1/1; `Design/AgentReports/2026-07-21_dense_city_phase0a_candidate_vehicles_populated.md`; `Design/AgentReports/2026-07-21_dense_city_phase0a_shared_art_ownership.{json,md}` | Passed | Populated 22 GameplayVehicles prefab instances on the UnitGridAuthoring baker path; GameplayVehicles has 22 children. Shared art proof: 11,892 sources → 670 meshes / 39 materials / 671 prefabs, 0 missing assets, `SharedArtOwnershipProven`. Production remains `StaticSceneChunks`. |
| 2026-07-21 | Phase 0A EntityScene binding/load/streamer skip scaffolding | Non-batchmode EditMode `/private/tmp/dense-city-entityscene-scaffold-tests-gui4.xml` 19/19; `Design/AgentReports/2026-07-21_dense_city_phase0a_entityscene_binding_scaffolding.md` | Passed | Added `OperationMapEntityScenePresentationPolicy`; SceneView accepts empty placements only for EntityScene; loading helper skips manifest load and rejects bound static manifest GUIDs; ownership Initialize already clean-skips; MenuBootstrap skips streamer bind and treats EntityScene preload ready. Production definition unchanged (`StaticSceneChunks`). |
| 2026-07-21 | Phase 0A candidate EntityScene definition + Addressables ownership layout | Non-batchmode executeMethod `/private/tmp/dense-city-candidate-entityscene-addressables-gui9.log`; ownership tests `/private/tmp/dense-city-candidate-entityscene-addressables-tests2.xml` 2/2; `Design/AgentReports/2026-07-21_dense_city_phase0a_candidate_entityscene_addressables_layout.{json,md}` | Passed | Candidate definition set to `EntityScene`; candidate runtime binding points at entity scene GUID `0f9ecd54…`; layout plan 1846 entries (1841 shared art) with zero static-manifest/chunk/legacy-placement runtime rows; production Addressables mutated=0; production remains `StaticSceneChunks`. |
| 2026-07-21 | Phase 0A transactional candidate Bake All implementation | `OperationMapEntitySceneCandidateBakeAll`; focused `/private/tmp/warline-phase0a-bake-all-focused-fix.log` 8/8; successful complete runs `/private/tmp/warline-phase0a-bake-all-run4.log` and `/private/tmp/warline-phase0a-bake-all-run5.log`; `git diff --check`; `Design/AgentReports/2026-07-21_dense_city_phase0a_candidate_bake_all.{json,md}` | Passed | Recovered Unity licensing IPC with explicit approval. Fixed empty batch scene-setup restoration and preserved the candidate runtime-binding `.meta` GUID across rebuilds. Settled repeated runs are byte-identical for candidate SubScene, definition, runtime binding, and runtime-binding meta. Budgets pass at 432 buildings, 22 vehicles, 9,090 render-only owners, 3 roots, 14,212 render meshes, 1,841 shared dependencies, zero production cutover. Accepted scene/SubScene hashes remain `f210e53d...` / `eff3ce69...`; production remains `StaticSceneChunks`. Exact transform parity, fixed-camera parity, Editor lifecycle, and Android acceptance remain open. Progress is 29/148 (20%). |
| 2026-07-21 | Phase 0A per-owner transform identity foundation | `OperationMapEntityPresentationIdentityAuthoringTests.RunFocusedValidation`: `/private/tmp/warline-operation-map-identity-contract-after-reset.log` 5/5; `ScriptArchitectureAlignmentContractTests.RunAssemblyBoundaryValidation`: `/private/tmp/warline-operation-map-identity-architecture.log` 31/31; `EcsBurstHotPathArchitectureTests.RunFocusedValidation`: `/private/tmp/warline-operation-map-identity-ecs-architecture.log` 11/11; isolated Unity Bee/Roslyn compile of `Game.Components`, `Game.Authoring`, `Game.Editor`, and `Game.Tests.Editor`; `git diff --check` | Passed; parity gate remains open | Added a passive deterministic identity marker to all three candidate population paths and baked it into an unmanaged ECS component. Strict validation rejects malformed operation-map/source identity, unknown role, and role/placement mismatch. No runtime system, managed registry, service locator, hierarchy search, update loop, production definition, Addressables ownership, source scene, or candidate scene changed. Initial headless and windowed validations hit the known licensing protocol loop; the project recovery script was run after explicit user authorization, then all focused gates passed. Progress remains 29/148 (20%) until the full source/candidate/baked/runtime matrix-and-bounds parity checkbox is accepted. |
| 2026-07-21 | Phase 0A existing-candidate identity backfill and bake gate | Backfill `/private/tmp/warline-operation-map-identity-backfill-execute-4.log`; in-memory bake `/private/tmp/warline-operation-map-identity-bake-validation.log`; schema-v2 full Bake All `/private/tmp/warline-operation-map-identity-bake-all-schema2.log`; identity tests `/private/tmp/warline-operation-map-identity-backfill-focused-5.log` 8/8; assembly boundaries `/private/tmp/warline-operation-map-identity-backfill-architecture.log` 31/31; ECS/Burst `/private/tmp/warline-operation-map-identity-backfill-ecs-architecture.log` 11/11; candidate SHA-256 `9009f26b...18adc5`; accepted source/SubScene SHA-256 `f210e53d...` / `eff3ce69...`; `git diff --check` | Passed identity recovery; parity rejected pending correction | Transactionally backfilled exactly 9,544 unique source identities into the existing candidate (432 buildings, 22 vehicles, 9,090 render-only), then proved all 9,544 bake to unmanaged ECS components with 14,212 render meshes, zero non-finite transforms, and zero managed map visual companions. Full Bake All reran with the identity stage idempotently `AlreadyComplete`; all stages passed and productionCutover remained 0. The diagnostic join proved the old render-only copy path uses source-local transform values as candidate-world values: 9,090/9,090 render-only owners mismatch accepted source world matrices. This is now a quantified fail-closed migration defect and the next priority. Progress remains 29/148 (20%). |
| 2026-07-21 | Phase 0A render-only source-to-candidate transform repair | Transaction `/private/tmp/warline-operation-map-transform-repair-execute.log`; identity/hierarchy tests `/private/tmp/warline-operation-map-transform-repair-focused-elevated.log` 10/10; in-memory ECS bake `/private/tmp/warline-operation-map-transform-repair-bake-validation-2.log`; full Bake All `/private/tmp/warline-operation-map-transform-repair-bake-all.log`; assembly boundaries `/private/tmp/warline-operation-map-transform-repair-architecture.log` 31/31; ECS/Burst `/private/tmp/warline-operation-map-transform-repair-ecs.log` 11/11; candidate SHA-256 `b36cfb4d...ca7081f7`; accepted source/SubScene SHA-256 `f210e53d...` / `eff3ce69...`; `git diff --check` | Passed source-to-candidate render-only parity; full parity gate remains open | Replaced the lossy root-copy route with deterministic transform-only mirrors of the accepted source parent chains, preserving local transforms beneath those chains instead of decomposing world matrices. Transactionally rebuilt all 9,090 render-only owners and fail-closed validated each owner matrix plus cloned renderer matrices/world bounds before save. Candidate still bakes 9,544 identities and 14,212 render meshes with zero non-finite transforms or managed map visual companions; all nine Bake All stages pass; productionCutover remains 0. Baked matrix/bounds manifest rows and real Addressables-loaded runtime parity remain the next priority. Progress remains 29/148 (20%). |
| 2026-07-21 | Phase 0A source/candidate/baked transform manifest | `OperationMapEntityPresentationTransformParityValidator`; `/private/tmp/warline-operation-map-baked-transform-parity-5.log`; `Design/AgentReports/2026-07-21_dense_city_phase0a_transform_parity.json`; `git diff --check` | Owner matrices passed; renderer bounds rejected fail-closed | Emitted 9,544 deterministic identity-keyed rows with accepted source local/world matrices and parent identity chain, candidate local/world matrices, reconstructed baked world/local/`PostTransformMatrix` data, and transformed renderer bounds. Candidate and baked identity joins are both exactly 9,544. All owner matrices pass `0.0001`; 938 bounds rows remain rejected (9 GameplayVehicles and 929 RenderOnly: 777 residual, 161 presence). Production remains `StaticSceneChunks`, productionCutover=0, and Android validation is deferred without blocking further Editor work. Progress remains 29/148 (20%). |
| 2026-07-21 | Phase 0A direct baked renderer ownership and bounds parity | Candidate vehicle refresh `/private/tmp/warline-operation-map-refresh-candidate-vehicles-2.log`; consecutive parity passes `/private/tmp/warline-operation-map-baked-transform-parity-final-2.log` and `/private/tmp/warline-operation-map-baked-transform-parity-final-3.log`; full transactional Bake All `/private/tmp/warline-operation-map-candidate-bake-all-parity-passed.log`; assembly boundaries `/private/tmp/warline-operation-map-parity-architecture.log` 31/31; ECS/Burst `/private/tmp/warline-operation-map-parity-ecs.log` 11/11; `Design/AgentReports/2026-07-21_dense_city_phase0a_transform_parity.json`; `git diff --check` | Passed direct source/candidate/baked matrix and transformed-bounds parity twice; runtime parity remains open | Deterministically matched package-generated render entities back to accepted authored renderer bake entries using world matrix plus local bounds, without weakening the `0.0001` owner-matrix or `0.001 m` transformed-bounds acceptance tolerances. Rebuilt 22 candidate vehicles from accepted scene instances so per-instance child transforms survive ECS conversion, while placement config remains authoritative for each vehicle root. Final proof: 9,544 candidate identities, 9,544 baked identities, 14,209 render meshes, zero rejected rows, zero non-finite transforms, and zero managed map visual companions. Full candidate Bake All passes with productionCutover=0; production remains `StaticSceneChunks`. Addressables-loaded runtime/fixed-camera/lifecycle gates remain open, and Android remains user-triggered. Progress is 30/148 (20%). |
| 2026-07-21 | Phase 0A runtime-comparison render manifest foundation | Direct candidate bake `/private/tmp/warline-operation-map-runtime-manifest-direct-bake.log`; assembly boundaries `/private/tmp/warline-operation-map-runtime-manifest-architecture.log` 31/31; ECS/Burst `/private/tmp/warline-operation-map-runtime-manifest-ecs.log` 11/11; `Design/AgentReports/2026-07-21_dense_city_phase0a_transform_parity.json`; `dotnet build Game.Editor.csproj --no-restore`; `git diff --check` | Passed dependency foundation; Addressables runtime comparison remains open | Advanced the transform-parity report to schema v2 and recorded all 14,209 baked render entities with world matrix, local bounds, transformed world bounds, and accepted owner identity where available. The 315 package-generated unowned rows remain explicit, so the future runtime validator must match the complete multiset. Direct parity remains 9,544/9,544 identities with zero rejected rows. No runtime system, production Addressables entry, source scene, candidate scene, presentation mode, or production cutover changed. Progress remains 30/148 (20%). |
| 2026-07-21 | Phase 8 transactional candidate runtime-content harness | `OperationMapEntitySceneCandidateRuntimeContentBuilder`; `OperationMapEntitySceneBuildAdditions.UseCurrentProcessSceneOverride`; compile `/private/tmp/warline-operation-map-candidate-runtime-builder-compile.log`; Android-target content probe `/private/tmp/warline-operation-map-candidate-runtime-content-build.log`; macOS-target rejection `/private/tmp/warline-operation-map-candidate-runtime-content-standalone-build.log`; active-target restore `/private/tmp/warline-restore-android-active-target-no-build.log`; `git diff --check` | Implementation compile-clean; packed Editor runtime validation blocked by missing macOS Standalone Build Support | Added a scoped build-process-only EntityScene override and a temporary local Addressables group transaction for the candidate definition/runtime binding. The transaction restores Addressables settings byte-for-byte and never moves or relabels production entries. An initial content-only probe inherited the Android active target and was discarded as acceptance evidence; no APK/AAB/player build ran. The corrected harness now fails immediately unless the target is StandaloneOSX and the macOS playback engine is installed. This Unity installation lacks that module, so real packed Editor runtime parity remains open with owner `local Unity installation`; other non-Android work continues. Progress remains 30/148 (20%). |
| 2026-07-21 | Phase 1 generated ownership authoring contracts | Focused EditMode `/private/tmp/warline-dense-city-authoring-contract-tests-2.xml` 3/3; assembly boundaries `/private/tmp/warline-dense-city-authoring-contract-architecture.log` 31/31; Unity compile `/private/tmp/warline-dense-city-authoring-contract-tests-2.log`; `git diff --check` | Passed | Added passive `DenseCityGeneratedRootAuthoring` with closed MapBakeSource/EntityPresentationSource roles, shared generation identity, schema/version, seed, and lowercase SHA-256 generation hash. Added collider-free `DenseCityAuthoredOverrideAuthoring` with stable identity, finite positive local bounds, and explicit presentation/surface/blocker exclusions. Both live in `Game.Authoring`, have no Update methods or runtime ownership, and include Unity metadata plus focused malformed-input rejection. Progress is 32/148 (22%). |
| 2026-07-21 | Phase 1 operation-map building ECS state contract | Focused EditMode `/private/tmp/warline-operation-map-building-components-tests.xml` 2/2; assembly boundaries `/private/tmp/warline-operation-map-building-components-architecture.log` 31/31; Unity compile `/private/tmp/warline-operation-map-building-components-tests.log`; `git diff --check` | Passed | Audited the existing `OperationMapBuildingAuthoring` root-reference baker and advanced it to the approved ECS contract. Added closed `OperationMapBuildingBlockerPolicy` with only `RubbleRemainsBlocked`, unmanaged stable `OperationMapBuildingComponent`, and an enableable `OperationMapBuildingDestroyedComponent` baked disabled to avoid a future per-destruction structural add. Unsupported blocker policy fails authoring validation. No destruction behavior or production presentation mode changed; recursive descendant ownership validation remains open. Progress is 34/148 (23%). |
| 2026-07-21 | Phase 1 building visual hierarchy ownership | Focused EditMode `/private/tmp/warline-operation-map-building-ownership-tests.xml` 4/4; protected candidate bake `/private/tmp/warline-operation-map-building-ownership-candidate-bake.log`; assembly boundaries `/private/tmp/warline-operation-map-building-ownership-architecture.log` 31/31; `git diff --check` | Passed | The building baker now explicitly includes every transform beneath intact/destroyed roots and fails closed on overlapping state roots, renderers outside both states, nested/shared building owners, incorrect presentation-role ancestry, and independent or mismatched descendant presentation identities. The actual protected candidate remains valid with 432 gameplay buildings, 698 building render children, 9,544 identities, 14,209 render entities, zero non-finite transforms, zero managed map visual companions, and `productionCutover=0`. Progress is 35/148 (24%). |
| 2026-07-21 | Phase 1 semantic hierarchy builder | Focused EditMode `/private/tmp/warline-dense-city-semantic-hierarchy-tests-3.xml` 2/2; assembly boundaries `/private/tmp/warline-dense-city-semantic-hierarchy-architecture.log` 31/31; Unity compile `/private/tmp/warline-dense-city-semantic-hierarchy-tests-3.log`; `git diff --check` | Passed | Added editor-only `DenseCitySemanticHierarchyBuilder` with explicit operation-map and entity-presentation scene inputs. It creates identity-transformed marked roots, the five exact `MapBakeGroupAuthoring` proxy roles, generated gameplay-building buckets, and render-only buckets; validates shared deterministic generation identity and nearest proxy ownership; and rejects same-scene use or overwrite of an existing marked root. It does not generate districts, save scenes, delete accepted content, search at runtime, or change production presentation. Progress is 36/148 (24%). |
| 2026-07-22 | Phase 1 authoring and scene-ownership readiness | Focused validator `/private/tmp/warline-dense-city-authoring-readiness-tests-2.xml` 4/4; authoring regression `/private/tmp/warline-dense-city-authoring-contract-regression-tests.xml` 17/17; assembly boundaries `/private/tmp/warline-dense-city-authoring-readiness-architecture.log` 31/31; Unity logs; `git diff --check` | Passed; full Phase 5 readiness remains open | Centralized strict source `GlobalObjectId` validation; added finite/nonzero transform and positive-size validation for generated roots, authored overrides, and buildings; and added an editor-only scene-pair ownership validator that rejects duplicate override ids, duplicate building source/placement identities, misplaced buildings, wrong generated-root identities, and invalid building presentation ownership. This validates the Phase 1 authoring/ownership slice only; collider, proxy, bake-budget, package, and full readiness checks remain explicit Phase 3-5 work. Progress is 37/148 (25%). |
| 2026-07-22 | Phase 1 fail-closed semantic role ownership | Focused EditMode `/private/tmp/warline-dense-city-semantic-ownership-tests-2.xml` 4/4; assembly boundaries `/private/tmp/warline-dense-city-semantic-ownership-architecture.log` 31/31; Unity compile logs; `git diff --check` | Passed | Tightened semantic-path validation from first-name lookup to exact direct-child traversal, so duplicate path segments cannot be accepted. Proxy groups now require one component on the exact role node and reject overlapping ancestor role ownership. Existing generation-id, root-role, scene-pair, override-placement, and generated-building owner checks remain fail-closed. No accepted scene, candidate scene, production package, or runtime system changed. Progress is 38/148 (26%). |
| 2026-07-22 | Phase 1 transactional generated-root replacement | Focused EditMode `/private/tmp/warline-dense-city-semantic-replacement-tests-2.xml` 5/5; assembly boundaries `/private/tmp/warline-dense-city-semantic-replacement-architecture.log` 31/31; Unity compile logs; `git diff --check` | Passed in isolated scene pair; accepted assets intentionally unchanged | Added the approved editor coordinator operation that accepts only zero roots or one already-valid paired generation set, destroys only the two marked roots through one Undo transaction, recreates and validates the replacement pair, marks both scenes dirty, and reverts the transaction on failure. The focused fixture saves both scenes and proves unmarked roots retain their exact `GlobalObjectId` while both marked roots are replaced. This is mutation-safety evidence, not authorization to alter the protected accepted source/candidate before generator semantic output is wired. Progress is 39/148 (26%). |
| 2026-07-22 | Phase 2 bounded semantic generation records | Focused EditMode `/private/tmp/warline-dense-city-generation-records-tests-final.xml` 5/5; assembly boundaries `/private/tmp/warline-dense-city-generation-records-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/warline-dense-city-generation-records-ecs.log` 11/11; Unity compile logs; `git diff --check` | Passed | Added the tracker-approved editor-only closed presentation/surface categories, deterministic record identity, immutable building/surface/presentation bake records, and editor-lifetime `DenseCityGenerationRecordSet`. The set requires explicit bounded capacities, rejects duplicate stable identities across all record kinds, cannot be read before sealing or mutated afterward, sorts each record kind ordinally by deterministic identity, exposes copied collections read-only, and clears owned collections on disposal. Geometry, asset identity, transform, health, blocker policy, and attachment ownership validate fail-closed. No generator realization, RNG order, runtime assembly, scene, SubScene, or package changed. Progress is 40/148 (27%). |
| 2026-07-22 | Phase 2 atomic building-record transaction foundation | Focused EditMode `/private/tmp/warline-dense-city-building-record-batch-tests.xml` 7/7; assembly boundaries `/private/tmp/warline-dense-city-building-record-batch-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/warline-dense-city-building-record-batch-ecs.log` 11/11; Unity compile logs; `git diff --check` | Passed; generator placement integration remains open | Added an editor-only atomic building-record group operation covering simulation, explicit foundation ownership, explicit blocker ownership, intact presentation, and destroyed presentation. Capacity, identity, category, and duplicate checks complete before mutation; failed preflight leaves the record set unchanged, and explicit group removal supports rollback after a later realization rejection without name or stable-key-prefix inference. No generator, RNG order, scene, SubScene, runtime assembly, Addressables ownership, or production presentation mode changed. The full building-placement checklist item remains incomplete until `SpawnBuilding` emits and rolls back these records around realization. Progress remains 40/148 (27%). |
| 2026-07-22 | Phase 2 explicit placement semantic input | Focused EditMode `/private/tmp/warline-dense-city-placement-semantic-tests-2.xml` 2/2; assembly boundaries `/private/tmp/warline-dense-city-placement-semantic-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/warline-dense-city-placement-semantic-ecs.log` 11/11; Unity compile logs; `git diff --check` | Passed; record emission remains open | Made placement presentation category a required `PrefabFootprint` input and assigned it explicitly where palettes and direct placements are constructed: houses, shops, halls, clock towers, and landmarks are gameplay-building intact inputs; trees are vegetation; fountains are props. Unsupported/unclassified categories fail construction. No prefab-name detection, default category, generated output, RNG call, realization order, scene, SubScene, or production path changed. This removes the mixed-use `SpawnBuilding` ambiguity before record emission is introduced. Progress remains 40/148 (27%). |
| 2026-07-22 | Phase 2 deterministic building-record factory | Focused EditMode `/private/tmp/warline-dense-city-building-record-factory-tests.xml` 2/2; assembly boundaries `/private/tmp/warline-dense-city-building-record-factory-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/warline-dense-city-building-record-factory-ecs.log` 11/11; Unity compile logs; `git diff --check` | Passed; asset extraction and placement integration remain open | Added a pure editor factory that converts explicit generator schema/seed/district/sequence, source prefab identities, material identities, world transform, footprint, foundation, blocker, faction, health, movement, layer, and chunk inputs into one linked five-record group. It preserves separate intact/destroyed source identities, emits a deterministic rotated world-space footprint polygon, and commits through the already-validated atomic record-set operation. No AssetDatabase lookup, generator realization, RNG call/order, scene, SubScene, runtime system, or production ownership changed. Progress remains 40/148 (27%). |
| 2026-07-22 | Phase 2 stable visual asset metadata extraction | Focused EditMode `/private/tmp/warline-dense-city-visual-asset-metadata-tests.xml` 2/2 against the real `SM_Bld_Shop_04` asset; assembly boundaries `/private/tmp/warline-dense-city-visual-asset-metadata-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/warline-dense-city-visual-asset-metadata-ecs.log` 11/11; Unity compile logs; `git diff --check` | Passed; destroyed-visual policy and placement integration remain open | Added an editor-only extraction boundary for persistent prefab GUID/local ID and ordinal unique material GUIDs. It rejects scene-only objects, missing persistent materials, and ambiguous material subassets that share a GUID but require local-id identity, avoiding runtime asset discovery and name inference. The extractor does not inspect or mutate scenes, instantiate visuals, alter RNG, or change runtime/Addressables ownership. Progress remains 40/148 (27%). |
| 2026-07-22 | Phase 2 explicit generated-building role/destruction policy | Production-config EditMode `/private/tmp/warline-dense-city-generated-building-policy-tests.xml` 1/1; footprint-role EditMode `/private/tmp/warline-dense-city-placement-role-tests.xml` 3/3; assembly boundaries `/private/tmp/warline-dense-city-generated-building-policy-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/warline-dense-city-generated-building-policy-ecs.log` 11/11; Unity compile logs; `git diff --check` | Passed; placement record emission remains open | Added the closed `GeneratedCityBuildingRole` contract and required every gameplay-building footprint to declare House, Shop, Civic, or Other while forbidding building roles on vegetation/props. The existing runtime-city config now owns explicit serialized destroyed-visual prefab references for all four roles and resolves them through one fail-closed API. Production config tests verify exact persistent assets. This removes destroyed-visual and role name inference without changing generated transforms, RNG order, scenes, SubScenes, runtime presentation, or Addressables ownership. Progress remains 40/148 (27%). |
| 2026-07-22 | Phase 2 deterministic material-variant selection | Focused EditMode `/private/tmp/warline-dense-city-material-selector-tests.xml` 3/3; assembly boundaries `/private/tmp/warline-dense-city-material-selector-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/warline-dense-city-material-selector-ecs.log` 11/11; Unity compile logs; `git diff --check` | Passed; material asset library/application and placement integration remain open | Extracted the existing position/seed material decision into a pure editor-only selector with an explicit generated-building role and exact material-family inputs. It preserves the established hash salt, six facade tint choices, Shop_05 20% original-material rule, and five recolor tones while rejecting unclassified/non-finite inputs and skipping civic or unsupported material families. The selector performs no hierarchy scan, name inference, AssetDatabase lookup, material mutation, RNG consumption, scene mutation, or runtime work. Progress remains 40/148 (27%). |
| 2026-07-22 | Phase 2 persistent building-material library extraction | Focused EditMode `/private/tmp/warline-dense-city-material-library-tests-2.xml` 2/2; deterministic fixture `/private/tmp/warline-dense-city-material-library-fixture.xml` 1/1 over two builds; assembly boundaries `/private/tmp/warline-dense-city-material-library-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/warline-dense-city-material-library-ecs.log` 11/11; Unity compile logs; `git diff --check` | Passed; pre-realization placement integration remains open | Moved the persistent source loading, exact source/variant family classification, six-by-three facade material update, five Shop_05 recolored texture/material update, and selected-material resolution into one editor-only library. The builder's post-pass now delegates to the pure selector and exact material identity instead of Shop_05 wrapper-name detection, while preserving Hall exclusion, palette counters, visible-slot fail-closed checks, checked-in asset paths, source-art sharing, and mixed Shop_05/generic-facade slot behavior. The isolated fixture built identically twice and left the canonical map unchanged. No runtime assembly, RNG order, generated transform, scene/SubScene, Addressables ownership, or production presentation changed. Progress remains 40/148 (27%). |
| 2026-07-22 | Phase 2 building placement record transaction | Focused EditMode `/private/tmp/warline-dense-city-building-placement-transaction-tests.xml` 3/3; assembly boundaries `/private/tmp/warline-dense-city-building-placement-transaction-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/warline-dense-city-building-placement-transaction-ecs.log` 11/11; Unity compile logs; `git diff --check` | Passed; generator call-graph integration remains open | Added one editor-only coordinator that atomically adds the linked building/foundation/blocker/intact/destroyed record group before invoking realization, keeps it only after an accepted realization, removes all five records after a rejected realization, and removes all five before rethrowing a realization exception. Tests prove all three outcomes and prevent orphan semantic records. The coordinator performs no scene lookup, hierarchy scan, asset discovery, runtime work, or visual cleanup; generator integration remains responsible for cleaning partially realized visuals and supplying stable deterministic inputs. Progress remains 40/148 (27%). |
| 2026-07-22 | Phase 2 bounded generation transaction context | Focused EditMode `/private/tmp/warline-dense-city-generation-transaction-context-tests.xml` 2/2; assembly boundaries `/private/tmp/warline-dense-city-generation-transaction-context-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/warline-dense-city-generation-transaction-context-ecs.log` 11/11; Unity compile logs; `git diff --check` | Passed; builder call-graph wiring remains open | Added one disposable editor-generation context that exclusively owns the bounded record set and deterministic five-record sequence allocation per explicit district. Accepted placements retain one complete group; rejected attempts consume their deterministic attempt sequence but leave no records, preventing later identities from depending on realization acceptance side effects. The context has no static state, hierarchy/asset discovery, inferred district/default surface data, runtime dependency, or visual ownership. Placement sites must still supply their exact district, transform, surface, chunk, metadata, and realization cleanup behavior. Progress remains 40/148 (27%). |
| 2026-07-22 | Phase 2 pre-realization resolved material metadata | Focused EditMode `/private/tmp/warline-dense-city-visual-asset-resolver-tests.xml` 4/4 against persistent production art/material assets; assembly boundaries `/private/tmp/warline-dense-city-visual-asset-resolver-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/warline-dense-city-visual-asset-resolver-ecs.log` 11/11; Unity compile logs; `git diff --check` | Passed; full placement input construction remains open | Extended the existing editor-only visual metadata extractor with an explicit material resolver. It now records the sorted unique persistent GUIDs of the materials selected for a placement before realization, while preserving the original prefab GUID/local-id identity and rejecting null or nonpersistent resolved materials. A production Shop_04 test proves the selected checked-in facade variant GUID is captured without instantiating or scanning a generated hierarchy. No name inference, asset creation, scene mutation, runtime lookup, RNG consumption, or production presentation change was introduced. Progress remains 40/148 (27%). |
| 2026-07-22 | Phase 2 complete pre-realization building record input | Focused EditMode `/private/tmp/warline-dense-city-placement-record-builder-tests.xml` 1/1 against the production runtime-city config, Shop_04 source prefab, configured Shop destroyed visual, and selected checked-in facade material; assembly boundaries `/private/tmp/warline-dense-city-placement-record-builder-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/warline-dense-city-placement-record-builder-ecs.log` 11/11; Unity compile logs; `git diff --check` | Passed; `SpawnBuilding` integration remains open | Added an editor-only bridge that accepts every semantic placement field explicitly, extracts persistent intact/destroyed prefab and material identities before realization, and produces the pure linked five-record group. It carries generator schema/seed/district/sequence, transform, footprint, foundation, blocker bounds, frontage, faction, health, movement mask, surface layer, and chunk without guessed defaults. The production-asset test verifies configured destruction policy and selected material identity. No hierarchy scan, scene mutation, runtime work, RNG consumption, or production presentation change was introduced. Progress remains 40/148 (27%). |
| 2026-07-22 | Phase 2 exact pre-realization building placement plan | Focused EditMode `/private/tmp/warline-dense-city-placement-plan-focused.log` 2/2; assembly boundaries `/private/tmp/warline-dense-city-placement-plan-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/warline-dense-city-placement-plan-ecs.log` 11/11; Unity compile logs; `git diff --check` | Passed; `SpawnBuilding` transaction wiring remains open | Added a pure editor-only planner that reproduces the existing grid-origin clamp, footprint-cell sizing, quarter-turn orientation, authored foundation elevation, frontage-edge alignment, wrapper world matrix, blocker bounds, footprint dimensions, frontage direction, and presentation chunk before any visual instantiation. It rejects non-finite, non-quarter-turn, and out-of-grid inputs. This gives the transaction an exact semantic placement candidate that realization can verify before records are retained; it does not instantiate objects, inspect hierarchies, consume RNG, mutate scenes, or change runtime/Addressables ownership. Progress remains 40/148 (27%). |
| 2026-07-22 | Phase 2 atomic building-placement integration | Full in-memory generation `/private/tmp/warline-dense-city-building-transaction-integration-12.log`; planner `/private/tmp/warline-dense-city-placement-plan-focused-4.log` 4/4; destroyed-visual policy `/private/tmp/warline-dense-city-building-policy.log` 2 configs; assembly boundaries `/private/tmp/warline-dense-city-building-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/warline-dense-city-building-ecs.log` 11/11; accepted scene SHA-256 before/after comparison; `git diff --check` | Passed; building placement record emission complete | Wired the existing `SpawnBuilding` call graph through one bounded generation transaction: every accepted gameplay building now commits its linked ECS-building, foundation, blocker, intact-presentation, and destroyed-presentation records before visual realization, while rejected or exceptional realization removes the complete group and destroys partial wrappers. The full map-wide fixture produced 5,759 district buildings and 150 civic/market placements for exactly 5,909 linked building groups, 11,818 surface records, and 11,818 presentation records with zero placement-parity rejection. The disposable generated root now moves to scene-root ownership and resolves to an exact identity world transform before regeneration, preventing the transformed `Map` parent from introducing Editor/runtime placement offsets or one-ULP matrix drift. Realization uses the precomputed parent-local transform once, validates matrix residual plus conservative blocker containment, and does not re-snap permanent visuals after record commitment. The accepted operation-map scene bytes remained unchanged, production remains `StaticSceneChunks`, Addressables ownership was untouched, and no Android build was run. Progress is 41/148 (28%). |
| 2026-07-22 | Phase 2 building-attachment ownership and transaction foundation | Focused owner registry `/private/tmp/warline-dense-city-owner-registry-4.xml` 3/3; focused attachment transaction `/private/tmp/warline-dense-city-attachment-transaction.xml` 4/4; assembly boundaries `/private/tmp/warline-dense-city-attachment-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/warline-dense-city-attachment-ecs.log` 11/11; Unity compile logs; `git diff --check` | Passed; generated attachment realization integration remains open | Accepted building transactions now publish an explicit editor-lifetime owner entry containing the committed building identity, source prefab, declared building role, and intact presentation root. The urban attachment collector consumes that registry instead of rediscovering owners and House/Shop roles from generated wrapper names. Added a bounded attachment transaction that validates the owner is already committed, publishes one attachment presentation before acceptance, and removes it on rejected or exceptional realization. Presentation capacity now explicitly reserves bounded attachment headroom. No generated attachment path is switched yet, no scene/SubScene/Addressables/runtime ownership changed, and progress remains 41/148 (28%). |
| 2026-07-22 | Phase 2 generated building-owned attachment integration | Focused attachment suite `/private/tmp/warline-dense-city-owned-attachments-focused-3.xml` 5/5; full in-memory map generation `/private/tmp/warline-dense-city-owned-attachments-integration-2.log`; two-build deterministic fixture `/private/tmp/warline-dense-city-owned-attachments-determinism.xml` 1/1; assembly boundaries `/private/tmp/warline-dense-city-owned-attachments-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/warline-dense-city-owned-attachments-ecs.log` 11/11; accepted scene hash preserved; `git diff --check` | Passed; generated building-owned attachment item complete | Added district-stable attachment identity allocation, persistent prefab/material metadata extraction, record-to-realized-transform tracking, exact matrix parity checks, and immediate-parent ownership validation. All generated shop roof caps, rooftop water tanks, rooftop utility props, shop-wall props, and civic cloth-cover/umbrella tent props now instantiate beneath the exact committed intact building root and emit `BuildingAttachmentIntact` presentations; the former global rooftop/shop attachment roots were removed. Full generation produced 5,909 semantic buildings and 2,781 owned attachments for 14,599 total presentations with no orphan, owner, or transform mismatch. The generator has no standalone interior-detail pass; interior renderers embedded in source prefabs remain part of the intact prefab asset and therefore share its declared building ownership instead of being duplicated as inferred records. No destroyed-state generated attachments currently exist, so the context rejects that category until an explicit destroyed root is introduced. Production remains `StaticSceneChunks`, scenes/SubScenes/Addressables were not mutated, no Android build ran, and progress is 42/148 (28%). |
| 2026-07-22 | Phase 2 infrastructure transaction foundation | Focused EditMode `/private/tmp/warline-dense-city-infrastructure-transaction-recovered.xml` 5/5; assembly boundaries `/private/tmp/warline-dense-city-infrastructure-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/warline-dense-city-infrastructure-ecs.log` 11/11; Unity compile logs; `git diff --check` | Passed; generator road emission remains open | Added an atomic surface-plus-infrastructure-presentation transaction for terrain patches, roads, bridges, and visually realized ramps plus a separate surface-only rollback transaction so approach-ramp proxies never require invented visuals. Both operations preflight category, semantic kind, capacity, and duplicate identity before mutation and remove complete committed state after rejected or exceptional realization. The initial unsupported-protocol licensing loop was recovered only after confirming no Editor was running, using the guarded repository IPC reset with `--confirm-no-editors --quit-hub`, and reopening Hub; no active Editor was interrupted. No generator output, RNG order, scene, SubScene, package, runtime path, or production ownership changed. Progress remains 42/148 (28%). |
| 2026-07-22 | Phase 2 deterministic infrastructure record factory | Focused EditMode `/private/tmp/warline-dense-city-infrastructure-record-factory.xml` 3/3; assembly boundaries `/private/tmp/warline-dense-city-infrastructure-record-factory-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/warline-dense-city-infrastructure-record-factory-ecs.log` 11/11; Unity compile logs; `git diff --check` | Passed; generator road emission remains open | Added a pure editor-only factory that converts explicit schema, seed, district, sequence, semantic kind, source prefab identity, material identity, exact world matrix, surface dimensions/elevation, movement mask, layer, and chunk into a linked surface-plus-infrastructure-presentation pair. A separate ramp-only operation emits exactly one surface proxy and rejects non-ramp use, preventing fake render records. Rotated footprint tests prove the surface polygon is derived from normalized world axes while presentation preserves the exact realized matrix. No AssetDatabase lookup, hierarchy scan, realization, RNG consumption, scene/SubScene mutation, runtime code, or production ownership changed. Progress remains 42/148 (28%). |
| 2026-07-22 | Phase 2 pre-realization infrastructure asset metadata | Focused production-asset EditMode `/private/tmp/warline-dense-city-infrastructure-placement-record-builder.xml` 2/2; assembly boundaries `/private/tmp/warline-dense-city-infrastructure-placement-record-builder-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/warline-dense-city-infrastructure-placement-record-builder-ecs.log` 11/11; Unity compile logs; `git diff --check` | Passed; generator road emission remains open | Added an editor-only placement-record builder that extracts persistent source prefab GUID/local-id and resolved persistent material GUIDs before realization, then delegates to the pure infrastructure factory. Production tests verify the asphalt road prefab records its own persistent identity/materials and a natural ground patch records the explicit checked-in ground-variation material rather than inspecting the generated instance after material replacement. The same boundary supports bridge visuals and surface-only ramps with an explicit source identity. No generated hierarchy scan, name inference, transient material identity, RNG consumption, realization, scene/SubScene mutation, runtime code, or production ownership changed. Progress remains 42/148 (28%). |
| 2026-07-22 | Phase 2 bounded infrastructure sequence context | Focused EditMode `/private/tmp/warline-dense-city-infrastructure-context.xml` 4/4; assembly boundaries `/private/tmp/warline-dense-city-infrastructure-context-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/warline-dense-city-infrastructure-context-ecs.log` 11/11; Unity compile logs; `git diff --check` | Passed; generator road emission remains open | Extended the disposable generation context with one explicit per-district infrastructure identity stream. Visualized placements reserve two sequence values and surface-only placements reserve one; rejected attempts consume their reserved range while rollback removes every record, so later identities cannot depend on realization acceptance. The stream is instance-owned, bounded, cleared on disposal, and separate from building/attachment identity streams. No static state, hierarchy/asset discovery, inferred district, realization-order change, scene/SubScene mutation, runtime code, or production ownership changed. Progress remains 42/148 (28%). |
| 2026-07-22 | Phase 2 transactional road-tile record integration | Full in-memory generation `/private/tmp/warline-dense-city-road-record-integration.log`; two-build deterministic fixture `/private/tmp/warline-dense-city-road-record-determinism.xml` 1/1; assembly boundaries `/private/tmp/warline-dense-city-road-record-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/warline-dense-city-road-record-ecs.log` 11/11; accepted-scene byte-hash guard; `git diff --check` | Passed; terrain-patch, bridge, shoulder, and ramp emission remain open | Moved the bounded generation transaction context before road generation and wired every existing road tile through pre-realization semantic commitment. Persistent metadata is cached per source prefab; each tile emits one `Road` surface and one `Infrastructure` presentation from its exact planned world matrix, cell footprint, movement mask, layer, and chunk before instantiation. Realization preserves the existing dictionary iteration, prefab selection, placement, elevation, scale, naming, hierarchy, and RNG behavior, validates every matrix element against the committed presentation, and destroys partial visuals on exceptions. Full generation retained 5,909 buildings and 2,781 owned attachments while adding exactly 4,650 road surfaces/presentations: totals moved from 11,818/14,599 to 16,468/19,249. The accepted scene hash remained unchanged; no SubScene, Addressables, runtime system, or production ownership changed. Progress remains 42/148 (28%). |
| 2026-07-22 | Phase 2 transactional road terrain-patch integration | Full in-memory generation `/private/tmp/warline-dense-city-road-patch-integration.log`; two-build deterministic fixture `/private/tmp/warline-dense-city-road-patch-determinism.xml` 1/1; assembly boundaries `/private/tmp/warline-dense-city-road-patch-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/warline-dense-city-road-patch-ecs.log` 11/11; accepted-scene byte-hash guard; `git diff --check` | Passed; bridge, shoulder, and ramp emission remain open | Split natural ground patch creation into deterministic plan and realization operations. The plan resolves the existing hash-selected persistent prefab, persistent ground-variation material, exact y-axis rotation, nonuniform scale, and vertical renderer-bound alignment before instantiation; the existing helper now delegates to the same operations for all non-road callers. Every accepted road patch commits one `Terrain` surface and one `Infrastructure` presentation before realization, validates exact matrix parity, and removes records plus partial visuals on failure. Full generation added exactly 4,650 patch surfaces/presentations, bringing totals to 21,118 surfaces and 23,899 presentations while retaining 5,909 buildings and 2,781 owned attachments. Visual selection/hash inputs, hierarchy, iteration/RNG order, accepted-scene bytes, SubScenes, Addressables, runtime systems, and production ownership remain unchanged. Progress remains 42/148 (28%). |
| 2026-07-22 | Phase 2 transactional bridge and approach-ramp integration | Focused bridge transaction `/private/tmp/dense-city-bridge-transaction-final.xml` 9/9; bridge factory `/private/tmp/dense-city-bridge-factory-final.xml` 5/5; shared sequence context `/private/tmp/dense-city-bridge-context-final.xml` 5/5; full in-memory generation `/private/tmp/dense-city-bridge-integration-final-2.log`; two-build deterministic fixture `/private/tmp/dense-city-bridge-determinism-final.xml` 1/1; assembly boundaries `/private/tmp/dense-city-bridge-architecture-final.log` 31/31; ECS/Burst guardrails `/private/tmp/dense-city-bridge-ecs-final.log` 11/11; accepted-scene SHA-256 before/after `f210e53df16b9dad1c96457b4e72c03260ac8ab215a3f4e959b97d54c904be88`; generated editor/test project compile 0 errors; `git diff --check` | Passed; shoulder emission remains open | Added one atomic bridge group containing a typed bridge surface, one infrastructure presentation, and two explicit surface-only approach ramps. Preflight and rollback cover all four records, while the generation context reserves one contiguous four-slot district-local infrastructure sequence range even after rejection. Bridge planning now resolves persistent prefab/material metadata, exact source-local renderer bounds including inactive prefab assets, root transform, semantic deck footprint, approach footprints, elevation, movement mask, layer, and chunks before realization. Realization preserves the accepted bridge placement and validates exact matrix plus transformed renderer-center parity, destroying partial visuals on failure. Full generation emitted 14 bridge presentations, 14 bridge surfaces, and 28 ramp surfaces, moving totals from 21,118/23,899 to 21,160 surfaces and 23,913 presentations while retaining 5,909 buildings and 2,781 owned attachments. No scene, SubScene, Addressables, runtime system, RNG order, production ownership, or Android build changed. The combined road/shoulder/terrain-patch/bridge/ramp checklist item remains incomplete only for explicit shoulder records, so progress remains 42/148 (28%). |
| 2026-07-22 | Phase 2 transactional authored road-shoulder integration | Focused road factory/transaction/context/production-prefab planner `/private/tmp/dense-city-road-shoulder-focused-final-3.xml` 29/29; full in-memory generation `/private/tmp/dense-city-road-shoulder-integration.log`; two-build deterministic fixture `/private/tmp/dense-city-road-shoulder-determinism.xml` 1/1; assembly boundaries `/private/tmp/dense-city-road-shoulder-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/dense-city-road-shoulder-ecs.log` 11/11; accepted-scene SHA-256 before/after `f210e53df16b9dad1c96457b4e72c03260ac8ab215a3f4e959b97d54c904be88`; Unity compile 0 errors; `git diff --check` | Passed; combined road semantic-output item complete | Added one variable-sized atomic road group containing the typed road surface, one prefab-root infrastructure presentation, and zero or more typed terrain shoulder surfaces. The planner consumes the existing pre-realization authored `RoadFootprintKind.Sidewalk` bounds, sorts them geometrically for stable sequence allocation, records the exact transformed upper walkable elevation and footprint, and emits no shoulder for the dirt-only road family. Rejected or exceptional realization removes the road, presentation, and every shoulder together; declared-count mismatch fails before advancing the district-local sequence. Full generation emitted 11,612 explicit shoulder surfaces, moving totals from 21,160/23,913 to 32,772 surfaces and 23,913 presentations while retaining 5,909 buildings and 2,781 owned attachments. Presentation count is unchanged because the existing prefab-root presentation owns its baked road and sidewalk child renderers, avoiding duplicate visuals. Road realization, hierarchy, prefab/material sharing, RNG order, scenes/SubScenes, Addressables, runtime systems, production ownership, and Android builds were unchanged. Progress is 43/148 (29%). |
| 2026-07-22 | Phase 2 canal-water semantic transaction foundation | Focused canal/generation/building/infrastructure/context EditMode `/private/tmp/dense-city-canal-water-foundation.xml` 33/33; assembly boundaries `/private/tmp/dense-city-canal-water-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/dense-city-canal-water-ecs.log` 11/11; Unity compile 0 errors; `git diff --check` | Passed; generator realization integration remains open | Added one atomic canal-water group with a non-traversable blocker surface plus separate canal-bed and canal-water infrastructure presentations. The group preflights capacity, category, and all three identities before mutation; rejected or exceptional realization removes the complete group, and the generation context reserves one contiguous three-slot infrastructure sequence range. Blocker records now require `MovementMask.None`, while every traversable surface still requires a nonzero movement mask; generated building blockers were corrected to the same invariant without changing stable identity or visual realization. The foundation has no asset lookup, transient primitive identity, scene mutation, generator integration, RNG change, SubScene/Addressables/runtime change, or Android validation. Progress remains 43/148 (29%) until canal water, bank/park terrain, vegetation, and light/prop records are integrated and parity-validated. |
| 2026-07-22 | Phase 2 transactional canal-water generator integration | Full in-memory map generation `/private/tmp/dense-city-canal-water-integration.log`; focused canal plus two-build deterministic fixture `/private/tmp/dense-city-canal-water-final-tests.xml` 5/5; assembly boundaries `/private/tmp/dense-city-canal-water-integration-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/dense-city-canal-water-integration-ecs.log` 11/11; accepted-scene SHA-256 before/after `f210e53df16b9dad1c96457b4e72c03260ac8ab215a3f4e959b97d54c904be88`; Unity compile 0 errors; `git diff --check` | Passed; bank/park terrain, vegetation, and light/prop integration remain open | Replaced transient cube water/bed realization with two instances of one persistent checked-in water-plane prefab while preserving separate checked-in bed and water materials. Every tile now plans persistent asset/material identities and exact root world transforms before realization, commits one non-ground blocker plus separate bed/water presentations atomically, removes partial visuals on failure, validates both realized matrices, and fails the full build unless tile/exclusion/bed/water counts match exactly. Full generation emitted 304 exclusions and 608 presentations, moving totals from 32,772/23,913 to 33,076 surfaces and 24,521 presentations while retaining 5,909 buildings, 2,781 owned attachments, and 11,612 road shoulders. No source scene, SubScene, Addressables/runtime ownership, RNG order, production mode, or Android build changed. Progress remains 43/148 (29%) until the complete canal/park semantic-output checklist item is accepted. |
| 2026-07-22 | Phase 2 bank/park terrain-visual transaction foundation | Focused terrain/canal/infrastructure/context EditMode `/private/tmp/dense-city-terrain-visual-foundation.xml` 28/28; assembly boundaries `/private/tmp/dense-city-terrain-visual-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/dense-city-terrain-visual-ecs.log` 11/11; Unity compile 0 errors; `git diff --check` | Passed; bank/park generator integration remains open | Added one bounded variable-size atomic group containing a traversable terrain surface and 1-16 explicit persistent infrastructure presentations. Capacity, category, source identity, and duplicate identity checks complete before mutation; rejection or realization failure removes the entire group. The generation context reserves one contiguous shared infrastructure sequence range and rejects declared-presentation-count mismatch before advancing that sequence. This supports the existing three-patch organic canal banks and five-patch pocket parks without collapsing distinct source assets into a fake presentation. No generator, hierarchy, asset lookup, visual realization, RNG, scene/SubScene, Addressables/runtime ownership, or Android build changed. Progress remains 43/148 (29%) until bank/park integration and remaining canal details are accepted. |
| 2026-07-22 | Phase 2 transactional canal-bank generator integration | Full in-memory map generation and exact semantic parity `/private/tmp/dense-city-canal-bank-final-integration.log`; terrain transaction plus two-build deterministic fixture `/private/tmp/dense-city-canal-bank-determinism.xml` 5/5; assembly boundaries `/private/tmp/dense-city-canal-bank-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/dense-city-canal-bank-ecs.log` 11/11; accepted-scene SHA-256 before/after `f210e53df16b9dad1c96457b4e72c03260ac8ab215a3f4e959b97d54c904be88`; Unity compile 0 errors; `git diff --check` | Passed; pocket-park terrain, vegetation, and light/prop integration remain open | Replaced every transient cylinder bank base with a second planned instance of the same selected persistent round-ground prefab at the existing 104% footprint and lower elevation, retaining the organic three-patch layering and green material override. Each bank segment now plans one non-buildable traversable terrain surface plus six explicit persistent presentations before realization, commits them atomically, removes partial visuals on failure, and validates every realized matrix. Full generation preserved 1,104 bank patches and emitted exactly 368 terrain surfaces, 1,104 base presentations, and 1,104 top presentations, moving totals from 33,076/24,521 to 33,444 surfaces and 26,729 presentations. The build fails closed on any patch/base/top/terrain count mismatch. No RNG order, source scene, SubScene, Addressables/runtime ownership, production mode, or Android build changed. Progress remains 43/148 (29%) until the complete canal/park semantic-output item is accepted. |
| 2026-07-22 | Phase 2 transactional canal pocket-park generator integration | Full in-memory map generation and exact semantic parity `/private/tmp/dense-city-canal-park-integration.log`; terrain transaction plus two-build deterministic fixture `/private/tmp/dense-city-canal-park-determinism.xml` 5/5; assembly boundaries `/private/tmp/dense-city-canal-park-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/dense-city-canal-park-ecs.log` 11/11; accepted-scene SHA-256 before/after `f210e53df16b9dad1c96457b4e72c03260ac8ab215a3f4e959b97d54c904be88`; Unity compile 0 errors; `git diff --check` | Passed; vegetation and light/prop integration remain open | Replaced the pocket parks' transient cylinder bases and unrecorded round-ground instances with ten preplanned persistent prefab presentations under one traversable, non-buildable terrain record per park. The existing five-patch organic layout, hash-selected source prefabs, green material override, elevations, hierarchy names, reserved cells, and detail placement remain unchanged. Each park commits its terrain, five base presentations, and five top presentations atomically before realization, removes the complete park hierarchy on failure, validates every realized matrix, and fails the full build on any park/terrain/base/top count mismatch. Full generation preserved 2 parks and emitted exactly 2 terrain surfaces plus 20 presentations, moving totals from 33,444/26,729 to 33,446 surfaces and 26,749 presentations while retaining 5,909 buildings and 2,781 owned attachments. No RNG order, source scene, SubScene, Addressables/runtime ownership, production mode, or Android build changed. Progress remains 43/148 (29%) until vegetation and light/prop records close the combined canal/park semantic-output item. |
| 2026-07-22 | Phase 2 render-only presentation transaction foundation | Focused render-only/canal/terrain EditMode `/private/tmp/dense-city-render-only-foundation-2.xml` 17/17; assembly boundaries `/private/tmp/dense-city-render-only-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/dense-city-render-only-ecs.log` 11/11; accepted-scene SHA-256 before/after `f210e53df16b9dad1c96457b4e72c03260ac8ab215a3f4e959b97d54c904be88`; Unity compile 0 errors; `git diff --check` | Passed; canal vegetation and light/prop generator integration remain open | Added a single-record transactional ownership path for independent render-only presentations without inventing gameplay terrain or misusing building attachments. The factory and record set accept only `Infrastructure`, `Vegetation`, `Prop`, or `Horizon`; gameplay-building and building-attachment categories are rejected. The transaction commits before realization and removes the presentation after rejection or exception. The generation context allocates the record from the existing district-local infrastructure sequence so stable identities remain collision-free across roads, surfaces, canal groups, vegetation, and props. No generator call site, hierarchy, asset lookup, visual realization, RNG, source scene, SubScene, Addressables/runtime ownership, or Android build changed. Progress remains 43/148 (29%) until canal detail integration is accepted. |
| 2026-07-22 | Phase 2 transactional canal vegetation and light integration | Full in-memory map generation and exact semantic parity `/private/tmp/dense-city-canal-detail-integration.log`; render-only transaction plus terrain and two-build deterministic fixture `/private/tmp/dense-city-canal-detail-determinism.xml` 14/14; assembly boundaries `/private/tmp/dense-city-canal-detail-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/dense-city-canal-detail-ecs.log` 11/11; accepted-scene SHA-256 before/after `f210e53df16b9dad1c96457b4e72c03260ac8ab215a3f4e959b97d54c904be88`; Unity compile 0 errors; `git diff --check` | Passed; combined canal/park semantic-output item complete | Preloaded persistent prefab/material metadata for the existing canal tree, bush, and streetlight assets, then routed every bank-side and pocket-park detail through the render-only transaction. Grounding is now planned from persistent prefab-local renderer bounds before realization while preserving the existing selected prefab, yaw, uniform scale, support elevation, object name, and parent hierarchy. Trees and bushes emit `Vegetation` records; streetlights emit `Infrastructure` records. Every realized root validates exact matrix parity and is removed with its record after rejection or exception. Full generation emitted exactly 85 tree, 81 bush, and 61 light presentations, moving presentation totals from 26,749 to 26,976 while surfaces remained 33,446 and buildings/attachments remained 5,909/2,781. Full-build parity now fails on any mismatch across water, bank, park, vegetation, or light counts. No RNG order, source scene, SubScene, Addressables/runtime ownership, production mode, or Android build changed. The combined canal/park item is complete; progress is 44/148 (30%). |
| 2026-07-22 | Phase 2 civic/market semantic ownership integration | Focused building transaction/context/deterministic fixture `/private/tmp/dense-city-civic-semantic-focused.xml` 12/12; full in-memory generation and exact semantic parity `/private/tmp/dense-city-civic-semantic-integration-final.log`; assembly boundaries `/private/tmp/dense-city-civic-semantic-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/dense-city-civic-semantic-ecs.log` 11/11; accepted-scene SHA-256 before/after `f210e53df16b9dad1c96457b4e72c03260ac8ab215a3f4e959b97d54c904be88`; Unity compile 0 errors; `git diff --check` | Passed; civic/market semantic-output item complete | Preserved the existing atomic five-record gameplay-building transaction and added source-time civic identity for the hall and roadside bazaar buildings: building simulation, foundation terrain, blocker, intact presentation, and destroyed presentation are labeled together without hierarchy/name inference. Explicit civic-road cells now carry civic road and terrain-patch identity through the existing road transaction while retaining shoulder ownership and the same road prefab, transform, iteration, and realization path. Full generation emitted exactly 24 civic building groups and 78 civic roads; generation fails closed unless all five building records match and every civic road/terrain record has its paired presentation. Generic buildings and roads retain their existing identities. No RNG order, visual output, source scene, SubScene, Addressables/runtime ownership, production mode, or Android build changed. Progress is 45/148 (30%). |
| 2026-07-22 | Phase 2 transactional horizon-mountain semantic integration | Deterministic fixture `/private/tmp/dense-city-horizon-semantic-focused.xml` 1/1; full in-memory generation and exact semantic parity `/private/tmp/dense-city-horizon-semantic-integration.log`; assembly boundaries `/private/tmp/dense-city-horizon-semantic-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/dense-city-horizon-semantic-ecs.log` 11/11; accepted-scene SHA-256 before/after `f210e53df16b9dad1c96457b4e72c03260ac8ab215a3f4e959b97d54c904be88`; Unity compile 0 errors; `git diff --check` | Passed; combined courtyard/detail semantic-output item remains open | Replaced temporary-instance planning with deterministic prefab-local renderer-bound planning for the existing horizon perimeter. Scale, yaw, outward retry offsets, protected-area rejection, grounding, names, hierarchy, collider stripping, and visual output remain unchanged. Every accepted mountain now commits one persistent `Horizon` presentation record before realization and validates exact realized world-matrix parity; rejected protected-area attempts emit nothing. Full generation emitted exactly 53 horizon presentations, moving presentation totals from 26,976 to 27,029 while surfaces/buildings remained 33,446/5,909. Generation fails closed on realized/semantic count mismatch. No RNG order, source scene, SubScene, Addressables/runtime ownership, production mode, or Android build changed. The broader courtyard/wall/rock/mountain/vegetation/rooftop/power/street-detail checkbox remains open, so progress remains 45/148 (30%). |
| 2026-07-22 | Phase 2 transactional boulevard and sidewalk-detail semantic integration | Deterministic fixture `/private/tmp/dense-city-street-detail-semantic-focused.xml` 1/1; full in-memory generation and exact semantic parity `/private/tmp/dense-city-street-detail-semantic-integration.log`; assembly boundaries `/private/tmp/dense-city-street-detail-semantic-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/dense-city-street-detail-semantic-ecs.log` 11/11; accepted-scene SHA-256 before/after `f210e53df16b9dad1c96457b4e72c03260ac8ab215a3f4e959b97d54c904be88`; Unity compile 0 errors; `git diff --check` | Passed; combined courtyard/detail semantic-output item remains open | Cached persistent prefab/material metadata for boulevard median trees and shared streetlights, then routed the existing boulevard-tree, boulevard-light, and sidewalk-light placements through the render-only transaction. The existing sorted road-cell traversal, hashes, side resolution, scales, rotations, support elevations, reserved areas, names, and hierarchy are unchanged. Trees emit `Vegetation`; both light paths emit `Infrastructure`. Each record commits before realization, validates exact grounded matrix parity, and rolls back with a rejected or failed instance. Full generation emitted exactly 155 boulevard trees, 74 boulevard lights, and 482 sidewalk lights, moving presentations from 27,029 to 27,740 while surfaces/buildings remained 33,446/5,909. Generation fails closed on any semantic/realized count mismatch. No RNG order, source scene, SubScene, Addressables/runtime ownership, production mode, or Android build changed. Progress remains 45/148 (30%) until all categories in the combined detail item are accepted. |
| 2026-07-22 | Phase 2 transactional free-ground landscaping semantic integration | Deterministic fixture `/private/tmp/dense-city-landscaping-semantic-focused.xml` 1/1; full in-memory generation and exact semantic parity `/private/tmp/dense-city-landscaping-semantic-integration.log`; assembly boundaries `/private/tmp/dense-city-landscaping-semantic-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/dense-city-landscaping-semantic-ecs.log` 11/11; accepted-scene SHA-256 before/after `f210e53df16b9dad1c96457b4e72c03260ac8ab215a3f4e959b97d54c904be88`; Unity compile 0 errors; `git diff --check` | Passed; combined courtyard/detail semantic-output item remains open | Added a shared transactional free-detail path that computes persistent prefab-local renderer bounds and the exact planned grounded world matrix before realization, performs the existing authored-core, road, building, and reserved-area rejection without creating a temporary object, then commits and realizes atomically. Main-street bushes and free-ground grass now use this path as `Vegetation` records while preserving the existing grid traversal, hashes, placement chances, side attempts, rotations, scales, occupied-area reservations, names, and hierarchy. Full generation emitted exactly 2,049 grass and 262 main-street-bush presentations, moving presentations from 27,740 to 30,051 while surfaces/buildings remained 33,446/5,909. Generation fails closed on semantic/realized count mismatch. No RNG order, source scene, SubScene, Addressables/runtime ownership, production mode, or Android build changed. Progress remains 45/148 (30%) until all categories in the combined detail item are accepted. |
| 2026-07-22 | Phase 2 transactional roadside power-network semantic integration | Deterministic fixture `/private/tmp/dense-city-power-semantic-focused.xml` 1/1; full in-memory generation and exact semantic parity `/private/tmp/dense-city-power-semantic-integration.log`; assembly boundaries `/private/tmp/dense-city-power-semantic-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/dense-city-power-semantic-ecs.log` 11/11; accepted-scene SHA-256 before/after `f210e53df16b9dad1c96457b4e72c03260ac8ab215a3f4e959b97d54c904be88`; Unity compile 0 errors; `git diff --check` | Passed; combined courtyard/detail semantic-output item remains open | Cached persistent pole/line asset metadata and prefab-local renderer bounds in the existing utility result shared by all sorted road corridors. Pole grounding and wire height are now derived from the exact planned transformed bounds before realization; each line derives its position, rotation, and z scale from the planned adjacent pole endpoints and persistent source length. Both paths commit `Infrastructure` records before instantiation, validate exact realized matrix parity, and roll back failed instances/records together. Existing road-run discovery, side resolution, spacing, chain flushing, reserved areas, names, hierarchy, and visual output are unchanged. Full generation emitted exactly 1,390 poles and 1,198 connecting lines, moving presentations from 30,051 to 32,639 while surfaces/buildings remained 33,446/5,909. Generation fails closed on semantic/realized count mismatch. No RNG order, source scene, SubScene, Addressables/runtime ownership, production mode, or Android build changed. Progress remains 45/148 (30%) until all categories in the combined detail item are accepted. |
| 2026-07-22 | Phase 2 atomic visual-blocker transaction foundation | Focused generation-context EditMode `/private/tmp/dense-city-visual-blocker-foundation.xml` 8/8; assembly boundaries `/private/tmp/dense-city-visual-blocker-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/dense-city-visual-blocker-ecs.log` 11/11; Unity compile 0 errors; `git diff --check` | Passed; courtyard-wall generator integration remains open | Added a dedicated bounded record group for a coarse non-traversable blocker plus one render-only `Infrastructure` presentation. The new factory creates deterministic paired identities and an oriented rectangular blocker footprint from explicit size and transform inputs. Record-set capacity/category/kind/identity checks complete before mutation, rejection or realization failure rolls back both records, and the shared district infrastructure sequence advances by exactly two. Existing terrain/road infrastructure continues to reject blocker surfaces, preserving that semantic invariant. No generator realization, visual output, RNG order, source scene, SubScene, Addressables/runtime ownership, production mode, or Android build changed. Progress remains 45/148 (30%) until courtyard walls and the remaining combined detail categories are integrated. |
| 2026-07-22 | Phase 2 transactional courtyard semantic integration | Deterministic fixture `/private/tmp/dense-city-courtyard-semantic-compile.xml` 1/1; full in-memory generation and exact semantic parity `/private/tmp/dense-city-courtyard-semantic-integration.log`; assembly boundaries `/private/tmp/dense-city-courtyard-semantic-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/dense-city-courtyard-semantic-ecs.log` 11/11; accepted-scene SHA-256 before/after `f210e53df16b9dad1c96457b4e72c03260ac8ab215a3f4e959b97d54c904be88`; Unity compile 0 errors; `git diff --check` | Passed; combined courtyard/detail semantic-output item remains open | Cached persistent asset/material metadata for the existing courtyard wall, pillar, well, and bush prefabs and planned every transform from prefab-local renderer bounds before realization. Each wall now atomically commits one oriented coarse non-traversable blocker and one render-only `Infrastructure` presentation; pillars emit `Infrastructure`, wells emit `Prop`, and bushes emit `Vegetation` records through the render-only transaction. Existing courtyard selection, side attempts, dimensions, gate segmentation, hashes, rotations, scales, grounding elevations, names, hierarchy, collider stripping, ground-detail cleanup, and reservations remain unchanged. Full generation emitted exactly 760 wall blockers and 760 wall presentations, 380 pillars, 152 wells, and 680 bushes, moving totals from 33,446/32,639 to 34,206 surfaces and 34,611 presentations. Generation fails closed on any realized/semantic count mismatch. No RNG order, source scene, SubScene, Addressables/runtime ownership, production mode, or Android build changed. Progress remains 45/148 (30%) until rooftop, general prop/tree/rock, and remaining combined detail categories are accepted. |
| 2026-07-22 | Phase 2 transactional street-prop, tree, and urban-rock semantic integration | Deterministic fixture `/private/tmp/dense-city-natural-detail-semantic-compile.xml` 1/1; full in-memory generation and exact semantic parity `/private/tmp/dense-city-natural-detail-semantic-integration.log`; assembly boundaries `/private/tmp/dense-city-natural-detail-semantic-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/dense-city-natural-detail-semantic-ecs.log` 11/11; accepted-scene SHA-256 before/after `f210e53df16b9dad1c96457b4e72c03260ac8ab215a3f4e959b97d54c904be88`; Unity compile 0 errors; `git diff --check` | Passed; combined courtyard/detail semantic-output item remains open | Cached persistent metadata for the existing street-prop, dense-tree, and urban-rock arrays. Exact prefab-local renderer bounds now drive pre-realization road/building/reserved-area rejection. Street props emit `Prop` records, trees emit `Vegetation`, and each playable urban rock atomically emits one coarse non-traversable blocker plus one `Infrastructure` presentation; realized transforms validate against the planned matrices. Existing grid/cluster traversal, hashes, placement chances, positions, rotations, scales, names, hierarchy, overlap rejection, and collider stripping remain unchanged. Full generation emitted exactly 4,812 street-prop presentations, 1,918 tree presentations, and 25 rock blocker/presentation pairs, moving totals from 34,206/34,611 to 34,231 surfaces and 41,366 presentations. Generation fails closed on any realized/semantic count mismatch. Rooftop caps, tanks, utilities, and shop-wall props were re-audited and are already covered by the validated building-attachment transaction. Civic fountains and open-ground terrain visuals remain direct realization paths, so the combined item is intentionally not closed. Progress remains 45/148 (30%). |
| 2026-07-22 | Phase 2 transactional civic-fountain semantic correction | Deterministic fixture `/private/tmp/dense-city-civic-fountain-semantic-focused.xml` 1/1; full in-memory generation and exact semantic parity `/private/tmp/dense-city-civic-fountain-semantic-integration.log`; assembly boundaries `/private/tmp/dense-city-civic-fountain-semantic-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/dense-city-civic-fountain-semantic-ecs.log` 11/11; accepted-scene SHA-256 before/after `f210e53df16b9dad1c96457b4e72c03260ac8ab215a3f4e959b97d54c904be88`; Unity compile 0 errors; `git diff --check` | Passed; open-ground terrain visuals remain before combined item closure | Re-audited the previously accepted civic/market slice and found its two standalone plaza fountains were still direct realizations while cloth covers and umbrellas were already hall-owned attachments. Both fountain prefab/material identities are now extracted before placement and each fountain commits one render-only `Prop` presentation before exact grounded realization. Existing positions, support elevation, rotation, scale, names, parent hierarchy, collider stripping, and plaza random ordering remain unchanged. Full generation emits exactly two civic-fountain presentations and fails closed otherwise, moving presentations from 41,366 to 41,368 while surfaces remain 34,231. No source scene, SubScene, Addressables/runtime ownership, production mode, or Android build changed. Progress remains 45/148 (30%). |
| 2026-07-22 | Phase 2 transactional open-ground terrain integration and combined-detail closure | Deterministic fixture `/private/tmp/dense-city-open-ground-semantic-focused.xml` 1/1; full in-memory generation and exact semantic parity `/private/tmp/dense-city-open-ground-semantic-integration-2.log`; assembly boundaries `/private/tmp/dense-city-open-ground-semantic-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/dense-city-open-ground-semantic-ecs.log` 11/11; accepted-scene SHA-256 before/after `f210e53df16b9dad1c96457b4e72c03260ac8ab215a3f4e959b97d54c904be88`; Unity compile 0 errors; `git diff --check` | Passed; combined courtyard/detail semantic-output item complete | Delayed open-ground realization until the existing courtyard and boulevard exclusion areas are known, while snapshotting pre-urban building footprints at the original generation point so later roof/wall attachments cannot alter placement. Every surviving round-ground patch now atomically commits one traversable terrain surface and one persistent-material `Infrastructure` presentation before realization, validates exact world-matrix parity, and rolls back as a unit. The prior final visual count is preserved exactly: 3,729 original candidates minus 183 courtyard and one boulevard overlap equals 3,545 surviving patches. Full semantic totals are 5,909 buildings, 37,776 surfaces, and 44,913 presentations. The combined category audit also confirms rooftop caps/tanks/utilities/shop-wall props are building-owned attachments; courtyard walls/props, mountains, street details, landscaping, power, street props, trees, rocks, and civic fountains now have explicit transactional records. No source scene, SubScene, Addressables/runtime ownership, production mode, or Android build changed. Progress is 46/148 (31%). |
| 2026-07-22 | Phase 2 typed presentation-hierarchy routing foundation | Focused EditMode `/private/tmp/dense-city-presentation-hierarchy-context-3.xml` 3/3; assembly boundaries `/private/tmp/dense-city-presentation-hierarchy-context-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/dense-city-presentation-hierarchy-context-ecs.log` 11/11; Unity compile 0 errors; `git diff --check` | Passed; generator call-site routing remains open | Added a fail-closed editor-only hierarchy context over the marked `EntityPresentationSource` root. It resolves gameplay buildings to `GameplayBuildings/Buildings` or `GameplayBuildings/CivicAndMarket` from the explicit generated building role, and resolves Infrastructure, Vegetation, Prop, and Horizon records to their exact `RenderOnly` buckets. Every required path must exist exactly once with identity transforms. Building attachments are deliberately rejected from independent routing and must remain beneath their declared intact/destroyed building visual-state owner. This adds no post-generation inference, scene mutation, runtime owner, static service, Addressables change, RNG change, or production cutover. The full creation-time placement checkbox remains open until all realization call sites consume this context. Progress remains 46/148 (31%). |
| 2026-07-22 | Phase 2 semantic render-only realization transaction | Focused hierarchy/transaction EditMode `/private/tmp/dense-city-presentation-hierarchy-transactions-3.xml` 8/8; assembly boundaries `/private/tmp/dense-city-presentation-hierarchy-transactions-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/dense-city-presentation-hierarchy-transactions-ecs.log` 11/11; Unity compile 0 errors; `git diff --check` | Passed; generator call-site routing remains open | Extended the bounded generation transaction context with a hierarchy-aware render-only realization overload. The committed presentation category selects its exact semantic parent before the callback creates an object; the returned root must be a direct child in the same scene and its world matrix must match the committed record within `0.0001`. Wrong-parent, nested-owner, null, rejected, exceptional, or transform-drift realization rolls back the semantic record and destroys the partial returned object. Existing legacy transaction overloads remain unchanged for the accepted editor-preview generator until all categories can switch atomically, avoiding a callable mixed-hierarchy candidate path. No source/candidate scene, runtime owner, RNG order, Addressables ownership, production mode, or Android build changed. Progress remains 46/148 (31%). |
| 2026-07-22 | Phase 2 record-driven render-only candidate realizer | Focused EditMode `/private/tmp/dense-city-render-only-realizer.xml` 2/2; assembly boundaries `/private/tmp/dense-city-render-only-realizer-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/dense-city-render-only-realizer-ecs.log` 11/11; Unity compile 0 errors; `git diff --check` | Passed; full record-set replay remains open | Added an editor-only realizer for one explicit render-only presentation record. It resolves and verifies the persistent prefab GUID plus local file id, instantiates directly beneath the category-selected semantic parent, decomposes only finite non-sheared TRS matrices, applies the exact recorded world transform, verifies all 16 matrix elements within `0.0001`, verifies the sorted persistent material GUID set, and retains the prefab connection so repeated placements share source mesh/material assets. Unsupported mesh-only records, missing assets, source mismatch, material drift, shear, hierarchy mismatch, or transform drift fail closed and remove the partial instance. Tests prove exact prefab/material/transform/parent parity and mismatch cleanup. No production scene, candidate asset, runtime owner, Addressables ownership, RNG order, production mode, or Android build changed. Progress remains 46/148 (31%). |
| 2026-07-22 | Phase 2 transactional render-only record-set replay | Focused `/private/tmp/dense-city-render-only-replay-focused.log` 2/2; assembly boundaries `/private/tmp/dense-city-render-only-replay-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/dense-city-render-only-replay-ecs.log` 11/11; Unity compile 0 errors; `git diff --check` | Passed; building/attachment replay and generator integration remain open | Added a bounded editor-only replay transaction over the sealed, stable-identity-sorted presentation records. It realizes only independent render-only categories through the record-driven realizer, skips building-owned presentation records for their separate ownership transaction, preserves deterministic record order, and destroys every object created by the replay in reverse order if any later source, material, transform, or hierarchy validation fails. Tests prove cross-category stable replay into exact semantic parents and complete rollback after a later material mismatch. The tracker summary was corrected to the measured 46 checked items out of 148; no checklist item was newly closed by this dependency step. No accepted/candidate scene, source/static rollback artifact, runtime owner, Addressables ownership, RNG order, production mode, or Android build changed. Progress remains 46/148 (31%). |
| 2026-07-22 | Phase 2 generated-building stable identity foundation | Focused `/private/tmp/dense-city-generated-building-identity-focused-2.log` 4/4; assembly boundaries `/private/tmp/dense-city-generated-building-identity-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/dense-city-generated-building-identity-ecs.log` 11/11; Unity compile 0 errors; `git diff --check` | Passed; building visual-root realization remains open | Extended the existing `OperationMapBuildingAuthoring` and unmanaged building identity/state components with one canonical stable id while preserving the exact authored `SourceGlobalObjectId` field used by accepted-source migration and parity joins. Existing buildings automatically use their valid Unity GlobalObjectId as the stable id; generated buildings instead require a mutually exclusive `densecity.<64 lowercase SHA-256>` id derived from the complete deterministic generation record key. Missing, malformed, or mixed authored/generated identities fail closed. Dense-city readiness now rejects duplicate canonical stable ids rather than treating an intentionally empty generated source GlobalObjectId as ownership. Tests prove authored fallback, generated acceptance, mixed rejection, deterministic hashing, bounded length, and lowercase format. No scene/prefab serialization, source/candidate scene, runtime system, Addressables ownership, RNG order, production mode, or Android build changed. No checklist item closes until generated building visuals and attachments realize beneath this authoring owner. Progress remains 46/148 (31%). |
| 2026-07-22 | Phase 2 generated-building authoring configuration boundary | Focused `/private/tmp/dense-city-generated-building-configure-focused.log` 5/5; assembly boundaries `/private/tmp/dense-city-generated-building-configure-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/dense-city-generated-building-configure-ecs.log` 11/11; Unity compile 0 errors; `git diff --check` | Passed; building visual-root realization remains open | Added one `UNITY_EDITOR`-only configuration method to the existing `OperationMapBuildingAuthoring`. The future record realizer can now assign operation-map id, generated stable id, placement index, faction, origin cell, building definition, blocker policy, and immediate intact/destroyed visual roots directly through the owning authoring contract instead of reflection, a duplicate authoring type, or a managed runtime bridge. The method clears authored `SourceGlobalObjectId` ownership and leaves all acceptance to the same fail-closed `TryValidate` path. Tests prove complete field assignment and valid intact/destroyed ownership. No source/candidate scene, prefab, runtime system, ECS hot path, Addressables ownership, RNG order, production mode, or Android build changed. No checklist item closes until the record-driven building and attachment realization transaction consumes this boundary. Progress remains 46/148 (31%). |
| 2026-07-22 | Phase 2 generated-building gameplay record ownership | Focused record/authoring EditMode `/private/tmp/warline-generated-building-record-tests.xml` 19/19; generated authoring entry point `/private/tmp/warline-generated-building-gameplay-values.log` 6/6; assembly boundaries `/private/tmp/warline-generated-building-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/warline-generated-building-ecs.log` 11/11; Unity compile 0 errors; `git diff --check` | Passed; building visual-root realization remains open | Preserved deterministic grid origin and footprint cells from `DenseCityBuildingPlacementPlan` through the immutable placement request, building record input, and committed building bake record instead of dropping them before authoring. Generated `OperationMapBuildingAuthoring` now requires and exposes the committed footprint and maximum health as mutually exclusive generated gameplay values; its Baker consumes those values for grid, blocker, footprint, and health components. Existing authored buildings remain definition-driven and reject stale generated overrides. Tests prove record propagation, editor configuration, generated acceptance, malformed override rejection, authored fallback, and unchanged factory/material behavior. No source/candidate scene, prefab, runtime system, hot-loop work, Addressables ownership, RNG order, production mode, or Android build changed. No checklist item closes until record-driven building visual roots and attachments realize atomically beneath this owner. Progress remains 46/148 (31%). |
| 2026-07-22 | Phase 2 explicit generated-building role and definition identity | Focused record/transaction EditMode `/private/tmp/dense-city-building-role-definition.xml` 28/28; assembly boundaries `/private/tmp/dense-city-building-role-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/dense-city-building-role-ecs.log` 11/11; Unity compile 0 errors; `git diff --check` | Passed; building visual-root realization remains open | Added explicit `GeneratedCityBuildingRole` and persistent building-definition config GUID to every immutable generated-building request/input/record. A bounded editor-only definition library resolves House/Other, Shop, and Civic roles to the existing House, Shop, and Hall config assets once per generation context; missing assets fail before placement. The future replay can now select `GameplayBuildings/Buildings` versus `CivicAndMarket` and configure the existing building authoring from record data without hierarchy-name, prefab-name, or identity-kind inference. Tests prove role/GUID propagation and persistent asset resolution while preserving semantic record counts and generator RNG order. No source/candidate scene, runtime system, Addressables ownership, production mode, or Android build changed. No checklist item closes until the record-driven building and attachment replay consumes this metadata. Progress remains 46/148 (31%). |
| 2026-07-22 | Phase 2 record-driven generated-building presentation realizer | Focused building/render-only EditMode `/private/tmp/dense-city-building-realizer.xml` 4/4; assembly boundaries `/private/tmp/dense-city-building-realizer-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/dense-city-building-realizer-ecs.log` 11/11; Unity compile 0 errors; `git diff --check` | Passed; sealed record-set building/attachment replay remains open | Added an editor-only realizer for one committed generated-building group. It creates one owner directly beneath the role-selected `GameplayBuildings` parent, applies the exact recorded world matrix, resolves the persistent definition config by recorded GUID, instantiates prefab-connected intact and destroyed visual roots as immediate children, validates source/material/world-matrix parity, and configures the existing `OperationMapBuildingAuthoring` with stable id, faction, origin, footprint, and health. A late mismatch destroys the complete partial owner hierarchy. The shared matrix helper now correctly decomposes parent-relative TRS while retaining world-matrix parity for existing render-only replay. No source/candidate scene, runtime owner, Addressables ownership, generator integration, production mode, or Android build changed. No checklist item closes until the sealed building/attachment replay and generator routing use this realizer transactionally. Progress remains 46/148 (31%). |
| 2026-07-22 | Phase 2 transactional generated-building and attachment replay | Focused building/attachment replay EditMode `/private/tmp/dense-city-building-replay.xml` 6/6; assembly boundaries `/private/tmp/dense-city-building-replay-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/dense-city-building-replay-ecs.log` 11/11; Unity compile 0 errors; `git diff --check` | Passed; candidate generator integration remains open | Added a sealed-record-set replay transaction for generated gameplay buildings. It indexes strictly stable-sorted presentation records, resolves each building's exact linked intact/destroyed records, realizes one authoring owner through the existing building realizer, and instantiates every building-owned attachment directly beneath the declared intact or destroyed visual-state root. Attachment source, material, parent, scene, and world-matrix parity are validated. Any missing/mismatched building record or later attachment failure destroys all previously realized building owners in reverse order, including their visual descendants. No attachment is routed through global render-only ownership. No source/candidate scene, runtime owner, generator integration, Addressables ownership, production mode, or Android build changed. The hierarchy checklist remains open until generation/candidate replacement invokes both building and independent render-only replays atomically. Progress remains 46/148 (31%). |
| 2026-07-22 | Phase 2 atomic complete-presentation replay boundary | Focused `/private/tmp/dense-city-presentation-replay-focused.log` 2/2; assembly boundaries `/private/tmp/dense-city-presentation-replay-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/dense-city-presentation-replay-ecs.log` 11/11; Unity compile 0 errors; `git diff --check` | Passed; candidate generator integration remains open | Added one editor-only coordinator over the existing generated-building/attachment and independent render-only replay transactions. A sealed record set now has a single all-or-nothing realization boundary: buildings realize first under their explicit gameplay owners, render-only records then realize under their typed semantic parents, and any later render-only mismatch removes both its own partial output and every previously realized building owner in reverse order. Tests prove successful mixed-category replay and cross-transaction rollback after a late material-identity failure. No accepted/candidate scene, generator call site, runtime owner, Addressables ownership, production mode, or Android build changed. The hierarchy checklist remains open until the candidate replacement path consumes this combined transaction. Progress remains 46/148 (31%). |
| 2026-07-22 | Phase 3 instance-only recursive physics stripping | Focused stripper `/private/tmp/dense-city-physics-stripper-focused-2.log` 3/3; render-only replay regression `/private/tmp/dense-city-physics-stripper-render-replay.log` 2/2; building/attachment replay regression `/private/tmp/dense-city-physics-stripper-building-replay.log` 2/2; assembly boundaries `/private/tmp/dense-city-physics-stripper-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/dense-city-physics-stripper-ecs.log` 11/11; Unity compile 0 errors; `git diff --check` | Passed for record-driven candidate presentation paths; legacy generator-wide invocation remains open | Added the editor-only `DenseCityPhysicsComponentStripper`, which rejects persistent assets and removes every nested active or inactive `Collider`, `Collider2D`, `Rigidbody`, and `Rigidbody2D` from an instantiated hierarchy with exact removal counts. Record-driven independent visuals, generated intact/destroyed building visuals, and building-owned attachments invoke it immediately after prefab instantiation. Tests prove recursive 2D/3D removal, primitive collider removal, persistent-prefab rejection, and source-prefab non-mutation; replay regressions prove the integration. This closes the stripper and recursive-removal checklist items only. Whole legacy-generator timing, generated-root/dependency validation, and removed-component override coverage remain open. Progress is 48/148 (32%). |
| 2026-07-22 | Phase 3 generated-root prohibited-physics readiness gate | Focused readiness `/private/tmp/dense-city-generated-root-physics-readiness-2.log` 5/5; assembly boundaries `/private/tmp/dense-city-generated-root-physics-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/dense-city-generated-root-physics-ecs.log` 11/11; Unity compile 0 errors; `git diff --check` | Passed; broader operation-map and package dependency gates remain open | Extended the instance-only physics utility with a non-mutating recursive audit and integrated it into the existing dense-city authoring readiness validator for both the `MapBakeSource` and `EntityPresentationSource` generated roots. Any active or inactive nested 3D/2D collider or rigidbody now fails readiness with its exact type and hierarchy path before surface or entity baking. The focused fixture proves inactive descendant rejection. It also aligned one stale test assertion with the already-established canonical building stable-id duplicate diagnostic; production behavior did not change. Progress is 49/148 (33%). |
| 2026-07-22 | Phase 4 map-scoped deterministic surface-proxy builder foundation | Focused proxy validation `/private/tmp/warline-dense-city-surface-proxy-focused-5.log` 3/3; semantic hierarchy regression `/private/tmp/warline-dense-city-semantic-partitions.xml` 5/5; assembly boundaries `/private/tmp/warline-dense-city-surface-proxy-architecture-2.log` 31/31; ECS/Burst guardrails `/private/tmp/warline-dense-city-surface-proxy-ecs-2.log` 11/11; Unity compile 0 errors; `git diff --check` | Passed; geometry merging, map bounds, surface-query parity, and candidate integration remain open | Added the editor-only `DenseCitySurfaceProxyBuilder`. It consumes only sealed deterministic surface records, canonicalizes convex polygon winding and starting vertex, partitions by role/layer/movement mask/chunk in stable order, selects 16- or 32-bit indices deterministically, writes candidate mesh assets only beneath the validated operation-map id and exact generation hash, and creates bake-only `MeshFilter` partition owners with matching nearest `MapBakeGroupAuthoring` metadata. The semantic hierarchy retains exactly five canonical role roots while accepting only direct same-role partition owners. Any invalid geometry or late failure removes created hierarchy and candidate mesh output. Tests prove all five roles, metadata partitioning, upward faces, deterministic replay, invalid-polygon rollback, zero renderers, and zero physics components. No accepted scene, SubScene, production map definition, Addressables ownership, runtime system, production mode, or Android build changed. Progress is 50/148 (34%). |
| 2026-07-22 | Phase 4 deterministic compatible proxy rectangle merging | Focused proxy merge validation `/private/tmp/warline-dense-city-surface-proxy-merge-focused-3.log` 4/4; assembly boundaries `/private/tmp/warline-dense-city-surface-proxy-merge-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/warline-dense-city-surface-proxy-merge-ecs.log` 11/11; Unity compile 0 errors; `git diff --check` | Passed; arbitrary convex polygons remain unmerged by design and production candidate integration remains open | Added a deterministic bounded sweep that recognizes exact axis-aligned proxy rectangles, merges only full-edge-adjacent coplanar rectangles, and repeats horizontal/vertical passes until no compatible reduction remains. Grouping still occurs first by role, layer, movement mask, and chunk, so merges cannot cross semantic or spatial ownership. Different elevations remain separate even when edges touch, while arbitrary convex outlines retain their canonical original geometry. Focused tests prove adjacent terrain and road reduction, elevation separation, expected vertex/triangle reductions, and unchanged deterministic replay. No scene, SubScene, accepted output, Addressables ownership, runtime path, production mode, or Android build changed. Progress is 51/148 (34%). |
| 2026-07-22 | Phase 4 deterministic proxy map-bounds enforcement | Focused proxy validation `/private/tmp/warline-dense-city-surface-proxy-bounds-focused.log` 5/5; assembly boundaries `/private/tmp/warline-dense-city-surface-proxy-bounds-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/warline-dense-city-surface-proxy-bounds-ecs.log` 11/11; Unity compile 0 errors; `git diff --check` | Passed; production grid-bound derivation and candidate integration remain open | Made approved map-surface bounds an explicit required builder input. Bounds must be finite with positive dimensions, and every canonical source polygon vertex must remain inside the inclusive rectangle before any folder, mesh asset, or hierarchy object is created. Existing surface-record constructors already reject non-finite coordinates/elevations; focused deterministic signatures prove canonical vertex/index ordering, and the new fixture proves an out-of-bounds record fails before mutation. This closes the combined winding/order/finite/bounds checklist item without treating generated presentation outside the gameplay grid as pathable surface data. No scene, SubScene, accepted asset, Addressables ownership, runtime system, production mode, or Android build changed. Progress is 52/148 (35%). |
| 2026-07-22 | Phase 4 strict bake-only proxy component ownership | Focused proxy validation `/private/tmp/warline-dense-city-surface-proxy-components-focused.log` 6/6; assembly boundaries `/private/tmp/warline-dense-city-surface-proxy-components-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/warline-dense-city-surface-proxy-components-ecs.log` 11/11; Unity compile 0 errors; `git diff --check` | Passed; operation-map dependency-closure validation remains open | Replaced the limited renderer/physics count check with a fail-closed component allowlist over the complete `BakeSources` hierarchy. Only `Transform`, the canonical or partition `MapBakeGroupAuthoring`, and the exact expected generated `MeshFilter` count are permitted. This excludes all renderers, colliders, rigidbodies, animation, audio, particles, arbitrary scripts, and missing-script components before candidate output is created and after it is populated. A pollution fixture proves an `AudioSource` under a canonical role root aborts with its hierarchy path and leaves no candidate mesh folder or generated mesh owner. No accepted scene, SubScene, Addressables ownership, runtime code, production mode, or Android build changed. Progress is 53/148 (36%). |
| 2026-07-22 | Phase 4 representative generated-proxy surface queries | Focused proxy/runtime query validation `/private/tmp/warline-dense-city-surface-proxy-runtime-query-after-reset.log` 7/7; assembly boundaries `/private/tmp/warline-dense-city-surface-query-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/warline-dense-city-surface-query-ecs.log` 11/11; `dotnet build Game.Editor.csproj --no-restore` 0 errors; `dotnet build Game.Tests.Editor.csproj --no-restore` 0 errors; `git diff --check` | Passed after user-authorized documented licensing IPC recovery | Added an editor-only collector that maps the generated Terrain/Road/Bridge/Ramp/Blocker proxy owners and meshes into the real `MapSurfaceMeshBakeSource` contract in stable hierarchy order. The focused fixture sends those sources through the production `MapSurfaceBakeSystem` and proves representative infantry, wheeled, tracked, building-placement, and grounded-aircraft acceptance/rejection against the generated proxies. The recovery was performed only after the dry run confirmed no active Unity Editor; Hub was reopened signed in and the versioned Unity 6000.5.2 licensing channel then initialized correctly. No scene, SubScene, production map definition, Addressables ownership, runtime system, production mode, or Android build changed. Progress is 54/148 (36%). |
| 2026-07-22 | Phase 4 outside-grid presentation-only contract | Focused proxy validation `/private/tmp/warline-dense-city-presentation-only-surface-query.log` 8/8; assembly boundaries `/private/tmp/warline-dense-city-presentation-only-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/warline-dense-city-presentation-only-ecs.log` 11/11; `dotnet build Game.Tests.Editor.csproj --no-restore` 0 errors; `git diff --check` | Passed | Added a mixed sealed-record-set regression proving that a Horizon presentation translated beyond both approved gameplay-grid axes remains in the presentation record set but contributes no generated proxy or `MapSurfaceMeshBakeSource`. The existing fail-closed out-of-bounds surface fixture continues to prove that the same location cannot enter gameplay surface ownership. This locks the default policy without adding an exception path, runtime owner, managed bridge, scene mutation, Addressables change, production cutover, or Android build. Progress is 55/148 (37%). |
| 2026-07-22 | Phase 4 terrain/foundation/park proxy integration | Focused proxy validation `/private/tmp/warline-dense-city-terrain-proxy-factory.log` 9/9; assembly boundaries `/private/tmp/warline-dense-city-terrain-proxy-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/warline-dense-city-terrain-proxy-ecs.log` 11/11; `dotnet build Game.Tests.Editor.csproj --no-restore` 0 errors; `git diff --check` | Passed | Added an integration fixture over the actual terrain-visual and building record factories. District terrain, canal-park terrain, and generated-building foundation records flow through their existing immutable `Terrain` records into one simplified terrain proxy partition, while the linked building blocker remains a separate blocker partition. The fixture validates record counts, reduced proxy geometry, movement-mask ownership, and no dependence on detailed presentation meshes. No duplicate terrain builder, runtime owner, managed bridge, scene mutation, Addressables change, production cutover, or Android build was added. Progress is 56/148 (38%). |
| 2026-07-22 | Phase 4 road/shoulder/natural-ground proxy separation | Focused proxy validation `/private/tmp/warline-dense-city-road-proxy-factory.log` 10/10; assembly boundaries `/private/tmp/warline-dense-city-road-proxy-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/warline-dense-city-road-proxy-ecs.log` 11/11; `dotnet build Game.Tests.Editor.csproj --no-restore` 0 errors; `git diff --check` | Passed | Added an integration fixture over `DenseCityInfrastructureRecordFactory.CreateRoadWithShoulders` plus the terrain-visual factory used by natural ground. One road, two shoulders, and one natural-ground record produce exactly one `Road` proxy partition and one separate `Terrain` partition. Geometry counts prove the road mesh contains only the road polygon while shoulders and natural ground remain terrain-owned; both retain their explicit movement-mask metadata. No duplicate road builder, detailed-mesh rasterization, runtime owner, scene mutation, Addressables change, production cutover, or Android build was added. Progress is 57/148 (39%). |
| 2026-07-22 | Phase 4 bridge-deck/approach-ramp proxy separation | Focused proxy validation `/private/tmp/warline-dense-city-bridge-ramp-proxy.log` 11/11; assembly boundaries `/private/tmp/warline-dense-city-bridge-ramp-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/warline-dense-city-bridge-ramp-ecs.log` 11/11; `dotnet build Game.Tests.Editor.csproj --no-restore --disable-build-servers` 0 errors; `git diff --check` | Passed | Added an integration fixture over the real `DenseCityInfrastructureRecordFactory.CreateBridgeWithApproaches` path. One bridge transaction emits one `Bridge` deck record and two `Ramp` approach records, and the proxy builder preserves them as separate semantic partitions with explicit layer 2 and movement mask 3 metadata. Geometry counts prove the deck remains one quad while the approaches form the independent ramp mesh; no presentation mesh is rasterized into gameplay data. No duplicate bridge builder, runtime owner, scene mutation, Addressables change, production cutover, or Android build was added. Progress is 58/148 (39%). |
| 2026-07-22 | Phase 4 collider-free canal-water exclusion integration | Focused proxy validation `/private/tmp/warline-dense-city-canal-water-proxy.log` 12/12; assembly boundaries `/private/tmp/warline-dense-city-canal-water-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/warline-dense-city-canal-water-ecs.log` 11/11; `dotnet build Game.Tests.Editor.csproj --no-restore --disable-build-servers` 0 errors; `git diff --check` | Passed | Added an integration fixture over the real canal-water record factory. Its zero-movement exclusion produces exactly one layer-4 `Blocker` proxy, while the bed and water remain two presentation-only records and never become gameplay meshes. The production map-surface bake path classifies every sampled canal cell as blocked for infantry, and the complete generated proxy hierarchy contains no collider or rigidbody. No duplicate water owner, presentation-mesh rasterization, runtime system, managed bridge, scene mutation, Addressables change, production cutover, or Android build was added. Progress is 59/148 (40%). |
| 2026-07-22 | Phase 4 building/wall/rock coarse-blocker integration and phase closure | Focused proxy validation `/private/tmp/warline-dense-city-building-wall-rock-proxy.log` 13/13; assembly boundaries `/private/tmp/warline-dense-city-building-wall-rock-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/warline-dense-city-building-wall-rock-ecs.log` 11/11; `dotnet build Game.Tests.Editor.csproj --no-restore --disable-build-servers` 0 errors; `git diff --check` | Passed; Phase 4 complete | Added one integration fixture over the real generated-building factory and the shared visual-blocker factory used by courtyard walls and urban rocks. Their deterministic rectangle/oriented-rectangle footprints produce three coarse blockers in one explicit layer-3 zero-movement partition while the building foundation remains independently terrain-owned. Geometry counts prove all three footprints are represented without detailed presentation-mesh rasterization, and the proxy hierarchy remains free of colliders and rigidbodies. Every Phase 4 checklist item is now accepted. No runtime owner, managed bridge, scene mutation, Addressables change, production cutover, or Android build was added. Progress is 60/148 (41%). |
| 2026-07-22 | Phase 5 existing-map entity-presentation readiness entry point | Focused readiness `/private/tmp/warline-operation-map-presentation-readiness-focused.log` 4/4; live protected-candidate batch entry `/private/tmp/warline-operation-map-presentation-readiness-live.log`; assembly boundaries `/private/tmp/warline-operation-map-presentation-readiness-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/warline-operation-map-presentation-readiness-ecs.log` 11/11; `dotnet build Game.Tests.Editor.csproj --no-restore --disable-build-servers` 0 errors; `git diff --check` | Passed; broader Phase 5 gates remain open | Added the approved `OperationMapEntityPresentationReadinessValidator` with a `Game/Operation Maps/EntityScene Migration` menu command and public batch entry point. It non-mutatingly requires exactly one valid root for each of the three closed roles, one shared migration hash, unique valid source identities, matching nearest role ownership, unique role-scoped placement indices, accepted identity counts, and zero prohibited physics components including inactive descendants. Focused fixtures reject duplicate identities, wrong role ancestry, and inactive colliders. The real protected candidate passes with 432 building, 22 vehicle, and 9,090 render-only identities. Protected-content parity, legacy-spawn retirement, and package publication remain separate open checklist items. No scene save, source/candidate mutation, Addressables change, production cutover, or Android build occurred. Progress is 61/148 (41%). |
| 2026-07-22 | Phase 5 conditional dense-city readiness entry point | Focused readiness `/private/tmp/warline-dense-city-readiness-entry-focused.log` 7/7; live source/candidate batch entry `/private/tmp/warline-dense-city-readiness-entry-live.log`; assembly boundaries `/private/tmp/warline-dense-city-readiness-entry-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/warline-dense-city-readiness-entry-ecs.log` 11/11; `dotnet build Game.Tests.Editor.csproj --no-restore --disable-build-servers` 0 errors; `git diff --check` | Passed; current state explicitly NotGenerated | Added `Game/Operation Maps/EntityScene Migration` menu and public batch entry points to the existing approved dense-city readiness validator. The command transactionally opens the accepted operation-map/candidate scene pair, derives a shared generation id only when exactly one correctly assigned generated root exists in each scene, invokes the strict ownership validator when generated, and records a successful explicit `NotGenerated` state only when neither scene contains a generated root. A partial or duplicated pair fails closed. The live project correctly reports `NotGenerated`, matching the tracker’s cleared-city baseline. No scene was saved, generated output was invented, or source/candidate/Addressables/production ownership was changed. Progress is 62/148 (42%). |
| 2026-07-22 | Phase 5 generated-root generation contract | Focused readiness `/private/tmp/warline-dense-city-generation-contract-focused.log` 9/9; assembly boundaries `/private/tmp/warline-dense-city-generation-contract-architecture.log` 31/31; `dotnet build Game.Tests.Editor.csproj --no-restore --disable-build-servers` 0 errors; ECS/Burst rerun `/private/tmp/warline-dense-city-generation-contract-ecs.log` did not load the project because Unity licensing initialization timed out; immediately preceding unchanged ECS boundary passed 11/11; `git diff --check` | Passed for the editor-test-only contract stage; licensing rerun unavailable | Confirmed the production semantic-hierarchy/readiness path already fails closed unless each scene has exactly one required generated role root and both roots share the expected generation id, schema, schema version, deterministic seed, and valid SHA-256 generation hash. Added direct regression coverage for a duplicate role root and for every individual generation-contract mismatch. No production code, runtime boundary, scene, Addressables definition, source/candidate content, or Android output changed. Progress is 63/148 (43%). |
| 2026-07-22 | Phase 5 generated-renderer ownership gate | Focused readiness `/private/tmp/warline-dense-city-renderer-ownership-focused.log` 12/12; assembly boundaries `/private/tmp/warline-dense-city-renderer-ownership-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/warline-dense-city-renderer-ownership-ecs.log` 11/11; `dotnet build Game.Tests.Editor.csproj --no-restore --disable-build-servers` 0 errors; `git diff --check` | Passed | Extended the existing fail-closed dense-city readiness validator so the map-bake/proxy hierarchy rejects every detailed `Renderer`, while every renderer beneath the generated entity-presentation root must resolve either to a validated `OperationMapBuildingAuthoring` or one of the four closed render-only categories. Added positive classified-renderer coverage and negative proxy-renderer/unclassified-renderer regressions. Validation initially exposed the documented Unity licensing `505 Unsupported protocol version '1.18.1'` condition; the approved no-editor IPC/Hub recovery restored host-access wrapper validation. No runtime system, scene, generated output, Addressables ownership, production cutover, or Android build changed. Progress is 64/148 (43%). |
| 2026-07-22 | Phase 5 per-proxy bake-group ownership gate | Focused readiness `/private/tmp/warline-dense-city-proxy-owner-focused.log` 14/14; assembly boundaries `/private/tmp/warline-dense-city-proxy-owner-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/warline-dense-city-proxy-owner-ecs.log` 11/11; `dotnet build Game.Tests.Editor.csproj --no-restore --disable-build-servers` 0 errors; `git diff --check` | Passed | Added a direct readiness check over every generated proxy `MeshFilter`: the proxy object itself must carry exactly one `MapBakeGroupAuthoring`, and that component must be the nearest bake-group owner. This closes the inherited-owner gap left by role-container validation and matches the existing bake-source collector contract. Added acceptance coverage for an explicitly owned terrain proxy and rejection coverage for a proxy that only inherits the terrain group. No generator transaction, scene, runtime system, Addressables definition, production cutover, or Android output changed. Progress is 65/148 (44%). |
| 2026-07-22 | Phase 5 protected authored-content identity and overlap gate | Live accepted-map readiness `/private/tmp/warline-dense-city-protected-content-live.log` result `NotGenerated`; focused readiness `/private/tmp/warline-dense-city-protected-content-focused.log` 21/21; assembly boundaries `/private/tmp/warline-dense-city-protected-content-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/warline-dense-city-protected-content-ecs.log` 11/11; `dotnet build Game.Editor.csproj --no-restore` 0 errors; `git diff --check` | Passed | Locked the eight approved authored roots by stable `GlobalObjectId`, indexed hierarchy path, active state, and identity local transform. Protected manifest renderers now fail closed on deletion/disable, hierarchy movement, world-bounds drift, mesh identity drift, or material identity drift. Generated presentation is checked against margin-expanded protected XZ footprints through a bounded spatial hash, with only the existing named subgrade canal-underpass exception. Regression fixtures deliberately cover stable identity, rename, disable, move, reparent, delete, and overlap/separation behavior. The real accepted source passes without mutation and remains explicitly `NotGenerated`; no scene, SubScene, Addressables definition, runtime system, production cutover, or Android output changed. Progress is 66/148 (45%). |
| 2026-07-22 | Phase 5 legacy placement to authored ECS ownership and runtime-reference retirement gate | Candidate vehicle transaction `/private/tmp/warline-populate-candidate-gameplay-vehicles.log` 22/22 with pre-save accepted matrix/bounds parity; focused readiness `/private/tmp/warline-operation-map-readiness-focused.log` 6/6; live candidate readiness `/private/tmp/warline-operation-map-readiness-live.log` 432 buildings, 22 vehicles, 9,090 render-only owners; full in-memory ECS bake and parity `/private/tmp/warline-candidate-bake-vehicle-gameplay.log`; candidate bake report `CandidateBakeValidationPassed` with 432 gameplay buildings, 22 gameplay vehicles, 9,544 identities, 14,249 render entities, zero non-finite transforms, and zero managed map companions; transform report `SourceCandidateBakedParityPassed` with 9,544/9,544 identities and zero rejected rows; assembly boundaries `/private/tmp/warline-legacy-placement-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/warline-legacy-placement-ecs.log` 11/11; `dotnet build Game.Editor.csproj` and `Game.Tests.Editor.csproj` 0 errors | Passed; production cutover remains 0 | The new gate joins every legacy building/vehicle config row to its accepted inventory source identity and candidate ECS Baker owner, confirms candidate definition/runtime binding/Addressables layout contain no current-map legacy placement spawning references, and rejects visual-only vehicle owners. Validation exposed that the prior candidate vehicle transaction cloned accepted visuals after merely checking the configured prefab Baker, thereby discarding `UnitGridAuthoring`. The repaired transaction keeps each real gameplay prefab/Baker root and replaces only its placeholder model with an exact candidate-only copy of the accepted visual hierarchy, assigning that hierarchy as `modelRoot` and preserving strict world-matrix/transformed-renderer-bounds parity. Candidate bake validation now requires exactly 22 unit-movement archetypes. Accepted source scenes, production StaticSceneChunks/Addressables, and Android outputs remain unchanged. Progress is 67/148 (45%). |
| 2026-07-22 | Phase 5 generated ECS building completeness gate | Focused dense-city readiness `/private/tmp/warline-dense-city-generated-building-readiness.log` 25/25; live accepted source/candidate readiness `/private/tmp/warline-dense-city-generated-building-live.log` result `NotGenerated`; assembly boundaries `/private/tmp/warline-generated-building-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/warline-generated-building-ecs.log` 11/11; `dotnet build Game.Editor.csproj` and `Game.Tests.Editor.csproj` 0 errors | Passed conditionally; generated city remains absent by accepted baseline | Extended the existing generated-root building ownership pass after stable-id/placement uniqueness validation. Every generated building must retain the existing finite transform, positive footprint, and positive maximum-health contract; additionally it must declare immediate intact and destroyed visual roots, contain renderers in both states, use only persistent asset-backed `sharedMesh`/`sharedMaterials`, and contain no `RuntimeBuildingEntityLink` descendant. Focused fixtures accept a complete generated building and fail closed for a missing destroyed root, managed runtime link, and scene-embedded mesh. Authored legacy buildings remain on their existing migration contract. No scene, candidate SubScene, runtime system, Addressables definition, production cutover, or Android output changed. Progress is 68/148 (46%). |
| 2026-07-22 | Phase 5 generated building attachment ownership/state gate | Focused readiness `/private/tmp/warline-dense-city-attachment-readiness.log` 29/29; replay ownership `/private/tmp/warline-dense-city-attachment-replay.log` 2/2; live source/candidate readiness `/private/tmp/warline-dense-city-attachment-live.log` result `NotGenerated`; assembly boundaries `/private/tmp/warline-building-attachment-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/warline-building-attachment-ecs.log` 11/11; `dotnet build Game.Editor.csproj` and `Game.Tests.Editor.csproj` 0 errors | Passed conditionally; generated city remains absent by accepted baseline | Added explicit `OperationMapBuildingAttachmentAuthoring` ownership/state metadata and an unmanaged `OperationMapBuildingAttachment` baked component. Record replay assigns each attachment to exactly one generated building and its intact or destroyed root; the existing root-scale transition therefore hides/shows the complete attachment subtree with its declared state. Readiness rejects wrong-state hierarchy, duplicate/nested claims, orphaned owners, independent presentation ownership, nonpersistent render assets, and unmarked added prefab overrides. No managed runtime link, new runtime system, scene mutation, production cutover, or Android output was added. Progress is 69/148 (47%). |
| 2026-07-22 | Phase 5 source/candidate transform-parity transaction checkpoint | Focused transaction/parity `/private/tmp/warline-source-candidate-parity-focused.log` 11/11; full candidate Bake All `/private/tmp/warline-source-candidate-parity-bake-all.log`; source/candidate parity 9,544/9,544; baked parity 9,544/9,544 across 14,249 render entities; assembly boundaries `/private/tmp/warline-source-candidate-parity-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/warline-source-candidate-parity-ecs.log` 11/11; `dotnet build Game.Editor.csproj` and `Game.Tests.Editor.csproj` 0 errors | Passed substep; combined parity item remains open for Addressables-loaded runtime parity | Candidate Bake All now invalidates stale transform-parity success evidence before mutation, validates exact source-to-candidate world matrices and transformed renderer bounds after population/backfill, and relies on the existing candidate file journal to restore candidate scene/definition/binding outputs on rejection. The following in-memory ECS bake parity also passed. The parity evidence schema records its current checkpoint. Runtime Addressables-loaded entity parity is not yet implemented, so checklist count and progress remain 69/148 (47%); production cutover remains zero. |
| 2026-07-22 | Phase 5 current-map readiness gate ordering | Focused readiness/order `/private/tmp/warline-current-map-readiness-gates-focused.log` 7/7; live entity readiness `/private/tmp/warline-current-map-entity-readiness-live.log` 432 buildings, 22 vehicles, 9,090 render-only owners; live dense-city readiness `/private/tmp/warline-current-map-dense-readiness-live.log` result `NotGenerated`; assembly boundaries `/private/tmp/warline-current-map-readiness-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/warline-current-map-readiness-ecs.log` 11/11; `dotnet build Game.Editor.csproj` and `Game.Tests.Editor.csproj` 0 errors; `git diff --check` | Passed; presentation-budget publication ordering remains open | `OperationMapCurrentMapBaker` now executes `entity-presentation-readiness` before opening the source scene for placement or surface mutation. Dense-city readiness still fails closed for partial/duplicated generated ownership, but reports a bake stage only when the validated scene pair actually contains generated content; the accepted cleared-city baseline therefore remains an explicit validated `NotGenerated` state. Existing validator scene-setup restoration remains the owner of temporary scene changes, and no runtime system, source/candidate scene, Addressables ownership, production definition, or Android output changed. Progress is 70/148 (47%). |
| 2026-07-22 | Phase 6 deterministic presentation-budget validator foundation | Focused budget validation `/private/tmp/warline-presentation-budget-focused.log` 3/3; live candidate budget `/private/tmp/warline-presentation-budget-live.log`; deterministic report `Design/AgentReports/2026-07-22_dense_city_presentation_budget.json`; assembly boundaries `/private/tmp/warline-presentation-budget-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/warline-presentation-budget-ecs.log` 11/11; `dotnet build Game.Tests.Editor.csproj` 0 errors; `git diff --check` | Core passed; packed content byte/duplication metrics remain explicitly pending | Added the editor-only `DenseCityPresentationBudgetValidator` under the existing `Game/Operation Maps/EntityScene Migration` menu and a batch entry point. It fail-closed aggregates the accepted candidate bake, transformed renderer-bounds parity, shared-art compactness, and isolated candidate layout evidence. Current proof records 9,544 presentation identities, 14,249 render entities, 670 unique meshes, 39 unique materials, zero non-finite transforms, zero managed map companions, and zero candidate static-manifest/chunk/legacy-placement entries. Because Unity `6000.5.2f1` lacks macOS Standalone Build Support, the report explicitly records `packedContentMetricsComplete=0` and `-1` entity-scene/shared/duplicate bytes; it cannot be mistaken for packed-content or production-cutover acceptance. Progress is 71/148 (48%). |
| 2026-07-22 | Phase 5/6 candidate Bake All presentation-budget transaction checkpoint | Focused ordering/invalidation validation `/private/tmp/warline-presentation-budget-bake-all-focused.log` 4/4; full transactional candidate Bake All `/private/tmp/warline-presentation-budget-candidate-bake-all-resumed.log` passed 11/11 stages with `rollbackApplied=0`; presentation budget core records 9,544 identities, 14,249 render entities, 670 unique meshes, and 39 unique materials; assembly boundaries `/private/tmp/warline-presentation-budget-bake-all-architecture-recovered.log` 31/31; ECS/Burst guardrails `/private/tmp/warline-presentation-budget-bake-all-ecs.log` 11/11; `dotnet build Game.Tests.Editor.csproj --no-restore` 0 errors | Passed substep; production publication ordering and packed/runtime proof remain open | Candidate Bake All now invalidates stale presentation-budget evidence before mutation, runs the fail-closed budget gate after the in-memory entity bake/bake-budget evidence and before postflight isolation, and records invalidation failure details in the transaction report. Rejection invalidates the budget report while the existing file journal remains the mutation rollback owner. The first architecture rerun reached compilation but entered a licensing reconnect loop; after confirming no Editor remained, the documented guarded IPC reset and Hub reopen restored validation. This does not invoke or alter current-map static presentation publication, production definitions, Addressables ownership, or Android output. Missing macOS Standalone Build Support still prevents packed-content byte metrics and Addressables-loaded runtime parity, so the checklist count remains 71/148 (48%). |
| 2026-07-22 | Current StaticSceneChunks runtime placement visual-parity repair | Focused placement validation `/private/tmp/warline-map-placement-static-presentation-production-asset.log` 4/4, including every production operation-map placement row; assembly boundaries `/private/tmp/warline-map-placement-static-presentation-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/warline-map-placement-static-presentation-ecs.log` 11/11; `dotnet build Game.Tests.Editor.csproj --no-restore --disable-build-servers` 0 errors; `git diff --check` | Passed repair; runtime Addressables parity gate remains open | The compatibility runtime-binding scene intentionally has an empty authored building hierarchy because the protected static presentation already owns the visible map. Previously, every unresolved child placement fell back to cloning its complete gameplay prefab; 61 refinery child rows could therefore create many full refineries that did not exist at the accepted Editor locations. Added an explicit config policy that preserves legacy fallback behavior by default but makes the current operation map create logical runtime wrappers without duplicate visuals when its authored child is unavailable. The production-asset regression executes all configured rows against the empty binding hierarchy and proves zero prefab children are created. Production remains `StaticSceneChunks`; no EntityScene cutover, source/candidate scene mutation, Addressables ownership change, runtime update loop, hierarchy search, or Android build was introduced. The documented guarded Hub-inclusive licensing recovery was required before focused Unity validation. Checklist count remains 71/148 (48%). |
| 2026-07-22 | Existing accepted-map fixed-camera parity evidence | Live capture `/private/tmp/warline-fixed-camera-existing-map-final.log` passed 5/5 views at 1280x720 over 13,691 accepted-source and 13,691 candidate renderers; focused comparison `/private/tmp/warline-fixed-camera-existing-map-focused.log` 4/4; report `Design/AgentReports/2026-07-22_operation_map_fixed_camera_parity.json`; ten paired capture PNGs under `Design/AgentReports/Captures/2026-07-22_operation_map_fixed_camera_parity`; assembly boundaries `/private/tmp/warline-fixed-camera-existing-map-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/warline-fixed-camera-existing-map-ecs.log` 11/11; serial `dotnet build Game.Tests.Editor.csproj --no-restore --disable-build-servers` 0 errors; `git diff --check` | Existing-map substep passed; combined checklist remains open | Added an editor-only capture command under `Game/Operation Maps/EntityScene Migration` that resolves every candidate presentation identity back to the exact accepted source owner, alternates the two renderer sets beneath one deterministic camera/light setup, captures overview, minimap, and three density-selected detail pairs, and rejects blank source evidence or pixel drift above the declared thresholds. The report explicitly records `contentScope=ExistingAcceptedMapOnly`, `generatedCityState=NotGenerated`, and `combinedGeneratedParityAccepted=0`; obsolete first-pass captures were removed. This evidence therefore cannot close the combined terrain/roads/buildings/generated-city fixed-camera row while the approved deterministic city roots are absent, and it does not prove Addressables-loaded runtime parity. Production remains `StaticSceneChunks`; no source/candidate scene, Addressables definition, runtime system, or Android output changed. Checklist count remains 71/148 (48%). |
| 2026-07-22 | Phase 5 presentation-kind publication ordering | Focused budget/publication policy `/private/tmp/warline-static-presentation-kind-focused.log` 5/5; assembly boundaries `/private/tmp/warline-static-presentation-kind-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/warline-static-presentation-kind-ecs.log` 11/11; `dotnet build Game.Tests.Editor.csproj --no-restore --disable-build-servers` 0 errors; `git diff --check` | Passed; packed metrics and runtime parity remain separate open gates | Kept the candidate `presentation-budget` checkpoint after the in-memory entity bake and before postflight/publication acceptance, and made `Bake Current Map (All)` select the legacy static-presentation stage from the definition's closed `OperationMapPresentationKind` contract. `StaticSceneChunks` executes the existing chunk baker, `EntityScene` skips it, and unknown enum values fail closed. Regression coverage proves all three outcomes, preventing a future EntityScene cutover from silently republishing the retired static chunks. The known licensing protocol mismatch required the documented no-Editor guarded IPC reset and Hub reopen before host-wrapper validation. No scene, generated output, production definition, Addressables ownership, static rollback artifact, runtime system, or Android output changed. Progress is 72/148 (49%). |
| 2026-07-22 | Phase 5 candidate transaction invariants | Focused candidate Bake All transaction `/private/tmp/warline-candidate-transaction-order-focused.log` 12/12; `dotnet build Game.Tests.Editor.csproj --no-restore --disable-build-servers -v:q` 0 errors; immediately preceding unchanged production boundary validation 31/31 assembly and 11/11 ECS/Burst; `git diff --check` | Passed | Extended the existing real file rollback/protected-production tests with an ordering regression over the candidate Bake All failure path. The contract now locks stale budget-evidence invalidation before rollback, candidate-owned file rollback before protected-production revalidation, persistent failure-report write before rethrow, and both source/candidate scene-opening paths restoring the prior scene setup or creating a clean batch-mode scene. This stage changes tests and tracker evidence only; candidate and accepted scenes, production definition, Addressables ownership, runtime systems, rollback artifacts, and Android output remain unchanged. Progress is 73/148 (49%). |
| 2026-07-22 | Phase 6 deterministic presentation geometry evidence | Focused budget validation `/private/tmp/warline-presentation-geometry-focused.log` 6/6; live accepted/candidate scan `/private/tmp/warline-presentation-geometry-live.log`; report `Design/AgentReports/2026-07-22_dense_city_presentation_budget.json`; assembly boundaries `/private/tmp/warline-presentation-geometry-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/warline-presentation-geometry-ecs.log` 11/11; `dotnet build Game.Tests.Editor.csproj --no-restore --disable-build-servers -v:q` 0 errors; `git diff --check` | Passed for existing accepted-map candidate; generated-city and packed metrics remain open | Extended the read-only budget gate to transactionally open and restore the accepted operation-map scene, accepted SubScene, and candidate SubScene, then report 15,671 accepted authored renderers, 14,003 candidate authored renderers, 13,943 active candidate renderers, 14,249 baked render entities, 1,220 candidate mesh identities, 45 material identities, 43 texture identities, 833,768 unique triangles, 6,064,174 instanced triangles, 13,901 active shadow casters, 14,003 batching-eligible renderers, finite aggregate bounds, and 261.02 active renderers/km2. The gate rejects missing persistent geometry/material/texture references, property-block batching exceptions, non-finite bounds, and incomplete geometry evidence; live evidence has zero missing references and zero non-finite bounds. Counts explicitly distinguish complete authored inventory from visible fixed-camera parity and static-manifest inventory. No scene was saved, no source/candidate object was mutated, production remains `StaticSceneChunks`, packed bytes remain `-1`, and no Android build ran. Progress is 74/148 (50%). |
| 2026-07-22 | Phase 6 baked entity layout and visual-ownership evidence | Live candidate bake and transform parity `/private/tmp/warline-dense-city-baked-layout-evidence.log`; aggregate budget `/private/tmp/warline-dense-city-presentation-budget-expanded.log`; focused budget validation `/private/tmp/warline-dense-city-presentation-budget-focused.log` 7/7; assembly boundaries `/private/tmp/warline-dense-city-baked-layout-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/warline-dense-city-baked-layout-ecs.log` 11/11; `dotnet build Game.Editor.csproj --no-restore` 0 errors; reports `Design/AgentReports/2026-07-21_dense_city_phase0a_candidate_bake_validation.json` and `Design/AgentReports/2026-07-22_dense_city_presentation_budget.json`; `git diff --check` | Passed non-packed substep; entity-scene bytes remain blocked by missing macOS Standalone Build Support | Extended the in-memory candidate bake gate to report 17,087 total entities, 72 nonempty archetypes, 777 entity chunks, 432 gameplay-building identities, 22 gameplay-vehicle identities, 9,090 render-only identities, 14,249 render entities, 2,015 render-child entities, one shared `RenderMeshArray` identity, 1,741 baked mesh identities, and 50 baked material identities. Building presentation ownership reports all 432 intact roots, 266 destroyed roots, 166 explicitly absent destroyed equivalents, zero missing intact roots, and zero intact/destroyed root collisions. Role drift, unknown roles, missing intact roots, root collisions, empty shared-render evidence, non-finite transforms, and managed visual companions fail closed. Packed `entitySceneBytes` remains explicitly `-1`; therefore the combined checklist row remains open and progress remains 74/148 (50%). No scene, SubScene, Addressables ownership, production definition, static rollback artifact, runtime system, or Android output changed. |
| 2026-07-23 | Phase 6 one-time ECS building destruction | Focused destruction `/private/tmp/warline-operation-map-building-destruction-focused.log` 2/2; assembly boundaries `/private/tmp/warline-operation-map-building-destruction-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/warline-operation-map-building-destruction-ecs.log` 11/11; `dotnet build Game.Runtime.csproj`, `Game.Tests.Editor.csproj`, and `Game.Editor.csproj` with `--no-restore --disable-build-servers` 0 errors; `git diff --check` | Passed | Replaced the reversible per-frame visual-state derivation with Burst `OperationMapBuildingDestructionSystem`, a `[WithDisabled]` enableable-marker query, one-time root-scale transition, null destroyed-root support, explicit rubble-blocker policy gating, and no GameObject/ECB creation path. Focused tests prove alive initialization, intact/destroyed root switching, roof/interior/shop/tent attachment ownership, missing destroyed equivalents, entity-count stability, marker latching, blocker preservation, second-update no-op behavior, and health-restoration non-reversal. The generic test-runner CLI refreshed and compiled the renamed assets but produced no result file, so the accepted deterministic `RunFocusedValidation` entry point was used. The documented no-Editor Hub-inclusive licensing recovery restored the required versioned client. No scene, SubScene, Addressables ownership, production definition, static rollback artifact, passability path, or Android output changed. Progress is 77/148 (52%). |
| 2026-07-23 | Phase 7 thin runtime-binding source-scene identity | Focused binding validation `/private/tmp/warline-operation-map-thin-runtime-binding-focused.log` 5/5; assembly boundaries `/private/tmp/warline-operation-map-thin-runtime-binding-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/warline-operation-map-thin-runtime-binding-ecs.log` 11/11; `dotnet build Game.Tests.Editor.csproj --no-restore --disable-build-servers` 0 errors; `git diff --check`; protected-output `git diff --exit-code` over runtime binding, production definition, and Addressables settings | Passed read-only | Added a regression locking `OperationMapAddressablesLayoutBuilder.SourceScenePath` to `OperationMapRuntimeBindingSceneBuilder.OutputPath`, distinct from both the accepted authoring scene and map SubScene, with a persistent scene asset at that path. The existing structural validation then proves that exact binding asset has two roots, one operation-map view, empty building/vehicle/decoration roots, and zero renderer, collider, Rigidbody, camera, or light content. Validation opened the asset read-only and did not invoke either mutating builder; protected production/runtime-binding/Addressables outputs remained unchanged. No production definition, package ownership, source/candidate scene, static rollback artifact, or Android output changed. Progress is 78/148 (53%). |
| 2026-07-23 | Phase 7 strict candidate EntityScene runtime binding | Candidate regeneration `/private/tmp/warline-operation-map-entityscene-binding-regenerate.log` (`CandidateEntitySceneAddressablesLayoutReady`, 1,849 entries, 1,844 shared dependencies, zero production Addressables mutation); focused binding validation `/private/tmp/warline-operation-map-entityscene-binding-focused.log` 9/9; assembly boundaries `/private/tmp/warline-operation-map-entityscene-binding-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/warline-operation-map-entityscene-binding-ecs.log` 11/11; `dotnet build Game.Editor.csproj` and `Game.Tests.Editor.csproj` with `--no-restore --disable-build-servers` 0 errors; `git diff --check`; protected-output `git diff --exit-code` over accepted source scene/SubScene, production binding/definition, Addressables settings, and frozen static-presentation root | Passed candidate-only | Generalized the runtime-binding structural validator without weakening the production `PresentationOnly` contract, added a strict candidate `EntityScene` contract for exact definition/SubScene identity, auto-load, null legacy placements, empty presentation roots, and zero renderer/collider/Rigidbody/camera/light content, and made the candidate builder enforce it after persisted-reference repair and deterministic YAML normalization. Regeneration removed stale copied `PresentationOnly` source identity while preserving the candidate scene GUID. Tests prove the candidate passes, rejects injected renderer and collider content, excludes accepted authoring/static-presentation scene dependencies, and leaves production validation intact. The first sandboxed attempt exposed the known read-only external licensing database boundary; the documented no-Editor IPC recovery plus host-access wrapper completed validation. No accepted source/candidate SubScene, production binding/definition, Addressables ownership, frozen rollback artifact, or Android output changed. Progress is 79/148 (53%). |
| 2026-07-23 | Phase 7 candidate EntityScene definition references | Focused ownership validation `/private/tmp/warline-operation-map-entityscene-definition-ownership-focused.log` 3/3; assembly boundaries `/private/tmp/warline-operation-map-entityscene-definition-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/warline-operation-map-entityscene-definition-ecs.log` 11/11; `dotnet build Game.Tests.Editor.csproj --no-restore --disable-build-servers` 0 errors; `git diff --check` | Passed read-only | Added a focused candidate ownership contract proving `EntityScene` presentation kind, valid local references, exact candidate thin runtime-binding GUID, exact shared map-surface and minimap GUIDs, exact candidate SubScene navigation GUID, and empty static-manifest/building-placement/vehicle-placement references. The same suite proves candidate roles remain separate and production remains `StaticSceneChunks`. No asset, scene, SubScene, Addressables setting, production definition, rollback artifact, or Android output changed. Progress is 80/148 (54%). |
| 2026-07-23 | Phase 3 accepted source-scene prohibited-physics gate | Focused source validation `/private/tmp/warline-operation-map-source-physics-focused.log` 3/3; candidate Bake All ordering `/private/tmp/warline-operation-map-source-physics-bake-all-focused.log` 13/13; presentation readiness `/private/tmp/warline-operation-map-readiness-focused.log` 7/7; assembly boundaries `/private/tmp/warline-operation-map-source-physics-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/warline-operation-map-source-physics-ecs.log` 11/11; `dotnet build Game.Editor.csproj --no-restore --disable-build-servers` and `dotnet build Game.Tests.Editor.csproj --no-restore --disable-build-servers -p:BuildProjectReferences=false` 0 errors; `git diff --check` | Passed read-only; runtime binding/dependency-closure gates remain open | Added a fail-closed editor validator for the accepted operation-map scene and accepted SubScene and inserted it after candidate preflight isolation but before candidate population. It opens only missing accepted scenes additively, scans every active or inactive descendant through the shared prohibited-physics audit, reports exact scene/hierarchy/type failures, closes only scenes it opened, and restores the prior active scene without saving. Focused coverage rejects inactive 3D/2D colliders and rigidbodies and proves both accepted scene files and editor scene setup remain unchanged. No accepted source/candidate scene, production definition, Addressables ownership, rollback artifact, runtime system, or Android output changed. Progress is 81/148 (55%). |
| 2026-07-23 | Phase 3 candidate runtime-binding and EntityScene physics gate | Focused runtime physics `/private/tmp/warline-operation-map-runtime-physics-focused.log` 2/2; strengthened binding validation `/private/tmp/warline-operation-map-runtime-binding-physics-focused-2.log` 9/9; candidate Bake All ordering `/private/tmp/warline-operation-map-runtime-physics-bake-all-focused.log` 14/14; assembly boundaries `/private/tmp/warline-operation-map-runtime-physics-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/warline-operation-map-runtime-physics-ecs.log` 11/11; `dotnet build Game.Editor.csproj --no-restore --disable-build-servers` and `dotnet build Game.Tests.Editor.csproj --no-restore --disable-build-servers -p:BuildProjectReferences=false` 0 errors; `git diff --check` | Passed read-only; full build-dependency closure remains open | Added a fail-closed gate after candidate binding/layout generation and before candidate budget acceptance. It validates the thin `EntityScene` runtime binding and its exact referenced candidate SubScene with the canonical inactive-aware 3D/2D collider and rigidbody policy, closes only scenes it opened, restores editor setup, and never saves. The existing binding validator now applies the same policy across both binding roots rather than checking only 3D components beneath `MapRoot`. Tests prove all four prohibited component classes fail with exact asset/hierarchy/type diagnostics and that the real binding and SubScene bytes remain unchanged. This item intentionally covers the runtime binding and generated EntityScene dependency only; the next checklist item owns transitive production/Addressables build-dependency closure. No accepted source scene/SubScene, candidate scene/SubScene, production definition, Addressables ownership, rollback artifact, runtime system, or Android output changed. Progress is 82/148 (55%). |
| 2026-07-23 | Phase 3 prohibited-physics coverage completion and production-closure audit | Focused stripper coverage `/private/tmp/warline-dense-city-physics-coverage-focused.log` 4/4; assembly boundaries `/private/tmp/warline-dense-city-physics-coverage-architecture.log` 31/31; `dotnet build Game.Tests.Editor.csproj --no-restore --disable-build-servers` 0 errors; read-only production GUID/YAML closure audit | Coverage passed; closure item blocked, owner Codex | Added the missing assertion that stripping an instantiated physics prefab records four removed-component overrides while preserving its exact source-prefab identity. Together with existing tests, coverage now proves active/inactive 3D and 2D removal, primitive removal, persistent-asset rejection, and shared-prefab non-mutation. The independent closure audit found zero directly serialized prohibited components across all 269 production chunk scenes, but seven collider-bearing transitive prefabs in the frozen operation-map package (`Tent_Regular`, `Tent_Contractor`, `Tent_Expert`, `SM_Prop_Parachute_01`, `SM_Prop_EmergencyDrop_01`, `Unit_Veh_Jet_01_Destroyed`, and `SM_Wep_RocketLauncher_MIssile_01`); the full enabled-player closure reaches 87. A loaded-scene-only gate would be misleading, so dependency-closure completion remains blocked until those reachable prefab dependencies can be removed without mutating the protected rollback package or weakening acceptance. No production/candidate scene, Addressables setting, definition, generated output, runtime system, or Android output changed. Progress is 83/148 (56%). |
| 2026-07-23 | Phase 7 candidate explicit-ownership remediation | Candidate layout regeneration `/private/tmp/warline-candidate-core-root-layout-regenerate.log` (`CandidateEntitySceneAddressablesLayoutReady`, 5 entries, zero explicit shared dependencies, zero production Addressables mutation); focused ownership `/private/tmp/warline-candidate-core-root-ownership-focused.log` 4/4; candidate transaction `/private/tmp/warline-candidate-core-root-bakeall-focused-v2.log` 15/15; assembly boundaries `/private/tmp/warline-candidate-core-root-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/warline-candidate-core-root-ecs.log` 11/11; `dotnet build Game.Editor.csproj` and `Game.Tests.Editor.csproj` with `--no-restore --disable-build-servers` 0 errors; `git diff --check` | Passed candidate-only; production dependency closure remains blocked | Removed the recursive authoring-scene dependency promotion that had incorrectly assigned separate candidate ownership to 1,844 prefab/config/art dependencies. The candidate plan now owns exactly the definition, thin runtime binding, EntityScene, map-surface metadata, and minimap raster. Meshes, materials, textures, shaders, animation, and other art remain transitive EntityScene build content; an explicit shared entry is now rejected unless a future built-content layout establishes a genuine cross-partition owner. This removes source GameObject prefab graphs, UI portraits, combat VFX, collision meshes, package internals, and redundant material children from the candidate ownership manifest without changing the candidate SubScene, accepted sources, production definition, production Addressables, frozen rollback package, runtime systems, or Android output. The separate Phase 3 production closure blocker is unchanged, so checklist count remains 83/148 (56%). |
| 2026-07-23 | Phase 8 macOS candidate packed-content unblock | `Game/Operation Maps/EntityScene Migration/Build Candidate Runtime Parity Content`; `/private/tmp/warline-candidate-runtime-content-after-module.log`; `Design/AgentReports/2026-07-21_dense_city_phase0a_candidate_runtime_content.json`; packed catalog `Library/com.unity.addressables/aa/OSX/catalog.bin`; assembly boundaries `/private/tmp/warline-candidate-runtime-content-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/warline-candidate-runtime-content-ecs.log` 11/11; `git diff --check` | Content build passed; Addressables-loaded runtime parity remains open | After project `Library` cleanup and installation of macOS Standalone Build Support for Unity `6000.5.2f1`, the transactional harness built the candidate definition/runtime-binding content for `StandaloneOSX`. The catalog contains the candidate operation-map identity, and the report records the expected EntityScene GUID, local output path, `productionCutover=0`, and `temporaryGroupRetained=0`. The clean-cache run crossed the wrapper's 1,800-second timeout after the builder logged `result=Passed` and while Unity was shutting down; the temporary Addressables group/settings were restored and the generated platform content-state asset remains ignored. The follow-up guardrails passed after the documented no-Editor Hub-inclusive recovery replaced conflicting generic/versioned licensing clients. This clears only the environment blocker. It does not yet prove packed runtime matrix/bounds parity, candidate-only bundle bytes, production cutover, or Android acceptance, so checklist count remains 83/148 (56%). |
| 2026-07-23 | Phase 8 packed runtime parity and EntityScene lifecycle foundation | Fingerprinted content rebuild `/private/tmp/warline-candidate-runtime-content-fingerprinted.log`; packed PlayMode `/private/tmp/warline-packed-runtime-parity-results-v6.xml` 1/1 in `23.97 s`; log `/private/tmp/warline-packed-runtime-parity-v6.log`; loader focused `/private/tmp/warline-operation-map-loader-focused-v2.log` 15/15; unit-render focused `/private/tmp/warline-unit-mass-render-focused.log` 35/35; assembly boundaries `/private/tmp/warline-packed-runtime-parity-architecture-v2.log` 31/31; ECS/Burst guardrails `/private/tmp/warline-packed-runtime-parity-ecs-v2.log` 11/11; `dotnet build Game.Runtime.csproj`, `Game.Tests.Editor.csproj`, and `Game.Tests.PlayMode.csproj` with `--no-restore --disable-build-servers -v:q` 0 errors; `git diff --check` | Passed fingerprinted packed two-cycle transform/bounds parity; broader gameplay lifecycle and production cutover remain open | The real packed macOS catalog now loads the candidate definition, thin runtime binding, and EntityScene through the production composition path. The runtime-content report binds the proof to SHA-256 fingerprints of the current candidate SubScene, definition, runtime binding, transform manifest, Addressables catalog, and Entities catalog, so stale content cannot satisfy the test. Both complete load/capture/unload cycles match all 9,544 operation-map identities and all 14,249 baked render rows within the accepted matrix and local/transformed-bounds tolerances. Pre-simulation capture suspends and restores only the air-movement, helicopter-blade, and transport-plane-door transform mutators; production behavior is unchanged. Tolerance-safe bucketed nearest pairing replaces raw-float lexicographic row pairing while retaining per-row tolerance assertions. The loader adopts existing requested scenes, owns only loads it initiates, starts owned unload once, waits until `DestroyMetaEntities` removes the EntityScene metadata before publishing unload complete, and focused coverage prevents the old early-completion race. Unit mass-render setup excludes operation-map identities/visual roots/prefabs and retries ordinary unit candidates until a visual reference exists. No accepted source/candidate SubScene, production definition, production Addressables ownership, frozen rollback artifact, or Android output changed. Progress is 84/148 (57%); production cutover remains zero and Android validation remains user-triggered. |
| 2026-07-23 | Phase 8 fail-closed packed EntityScene readiness | Readiness utility EditMode `/private/tmp/warline-readiness-utility-tests-v2.xml` 3/3; loader focused `/private/tmp/warline-readiness-loader-focused-v2.log` 17/17; candidate content rebuild `/private/tmp/warline-readiness-content-build.log`; packed PlayMode `/private/tmp/warline-readiness-packed-playmode.xml` 1/1 in `31.10 s`; `dotnet build Game.Composition.csproj` and `Game.Tests.Editor.csproj` with `--no-restore --disable-build-servers -v:q` 0 errors; `git diff --check` | Passed candidate-only readiness and packed regression; broader lifecycle and production cutover remain open | Added one unmanaged baked readiness contract to the gameplay-building role root and made the existing loader validate it only after the packed EntityScene reports loaded. Readiness now requires one contract, three unique role roots, one operation-map/migration identity, unique source identities, exactly 432 gameplay-building identities/presentation components, 22 gameplay-vehicle identities/authored components, 9,090 render-only owner identities, explicit zero generated-city identities for the accepted not-generated state, and no static presentation preload. Pending content delays readiness; mismatched content fails closed as `MetadataBindFailed`. The rebuilt fingerprinted candidate still passes both packed load/capture/unload cycles and all 9,544 identity plus 14,249 renderer matrix/bounds comparisons. `Game.Composition` remains independent of Entities Graphics; detailed renderer parity stays in the rendering-aware packed test. Production remains `StaticSceneChunks`, production cutover remains zero, accepted sources and frozen rollback content are unchanged, and Android remains user-triggered. Progress is 85/148 (57%). |
| 2026-07-23 | Phase 8 EntityScene menu-return lifecycle ownership | Focused menu transition `/private/tmp/warline-entityscene-menu-transition-focused-v3.xml` 1/1; streamer suite `/private/tmp/warline-entityscene-menu-streamer-suite.xml` 32/32; scene-wiring suite `/private/tmp/warline-entityscene-menu-wiring-suite-v2.xml` 4/4; assembly boundaries `/private/tmp/warline-entityscene-menu-unload-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/warline-entityscene-menu-unload-ecs.log` 11/11; `dotnet build Game.Tests.Editor.csproj --no-restore --disable-build-servers -v:q` 0 errors; `git diff --check` | Passed lifecycle-owner foundation; packed Match-to-Menu release proof remains open | EntityScene presentation now retains the loaded `MatchSceneView` lifecycle owner across the route transition so the existing menu-loading path can request and observe operation-map content teardown before unloading the Match shell. Only EntityScene bypasses the legacy static-presentation drain gate; `StaticSceneChunks` behavior remains unchanged. The common unload path queues Match-scene unload exactly once after operation-map content reports complete. Focused coverage proves no static scene operation starts for EntityScene and the retained owner remains available to teardown. This does not yet close the checklist item because a packed Match-to-Menu run must still prove EntityScene, SubScene, and metadata release end to end. Production remains `StaticSceneChunks`, production cutover remains zero, accepted sources and rollback content are unchanged, and Android remains user-triggered. Progress remains 85/148 (57%). |
| 2026-07-23 | Phase 8 strict packed EntityScene release evidence | Packed two-cycle release proof `/private/tmp/warline-packed-unload-release-proof.xml` 1/1 in `23.63 s`; log `/private/tmp/warline-packed-unload-release-proof.log`; scene-wiring suite `/private/tmp/warline-entityscene-release-wiring.xml` 5/5; assembly boundaries `/private/tmp/warline-packed-release-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/warline-packed-release-ecs.log` 11/11; `dotnet build Game.Tests.PlayMode.csproj --no-restore --disable-build-servers -v:q` 0 errors; `git diff --check` | Passed direct packed release proof; real Match-to-Menu route remains open | Strengthened both packed unload cycles from accepting a merely not-loaded EntityScene to requiring destruction of the EntityScene metadata entity, every resolved section metadata entity, and the thin runtime-binding scene. The route wiring regression also locks metadata bootstrap disposal before packed content release and Match-shell unload. This is real candidate Addressables/Entities content and retains the fingerprint gate, but it intentionally does not claim the checklist item because the production Match-to-Menu route still needs to drive the packed candidate without mutating the accepted production catalog. Production remains `StaticSceneChunks`, production cutover remains zero, accepted sources and rollback content are unchanged, and Android remains user-triggered. Progress remains 85/148 (57%). |
| 2026-07-23 | Phase 8 packed Match-to-Menu EntityScene teardown | Packed route `/private/tmp/warline-packed-match-menu-release-v5.xml` 1/1 in `15.25 s`; complete packed parity/lifecycle suite `/private/tmp/warline-packed-entityscene-suite-final.xml` 2/2 in `38.00 s`; production `StaticSceneChunks` lifecycle regression `/private/tmp/warline-production-menu-match-menu-regression-graphics.xml` 1/1 in `12.64 s`; streamer regression `/private/tmp/warline-entityscene-unload-streamer-final.xml` 32/32; assembly boundaries `/private/tmp/warline-entityscene-unload-route-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/warline-entityscene-unload-route-ecs.log` 11/11; `dotnet build Game.Composition.csproj`, `Game.Tests.Editor.csproj`, and `Game.Tests.PlayMode.csproj` with `--no-restore --disable-build-servers -v:q` 0 errors; `git diff --check` | Passed; unload checklist item complete | Added an Editor-only candidate-catalog override so the real serialized Match scene can load packed candidate content without mutating accepted catalog assets or player builds. The test drives the production UI route and verifies stable Match startup followed by complete Menu teardown: EntityScene root, resolved section metadata, section-tagged entities, operation-map root/metadata/readiness, thin binding scene, and Match scene are gone, while the default World and one lifecycle root survive. The run exposed and fixed a real deadlock: clearing the active operation-map view changed the Match view's presentation mode back to source-renderer mode, causing Menu teardown to wait forever on an unbound static streamer after EntityScene unload. The loaded immutable presentation mode now survives until loader disposal. Production remains `StaticSceneChunks`, production cutover remains zero, accepted sources and rollback content are unchanged, and Android remains user-triggered. Progress is 86/148 (58%). |
| 2026-07-23 | Phase 8 sequential packed EntityScene route lifecycle | Focused packed two-route-cycle proof `/private/tmp/warline-packed-two-route-cycles.xml` 1/1 in `26.53 s`; complete packed parity/lifecycle suite `/private/tmp/warline-packed-entityscene-suite-two-cycle.xml` 2/2 in `48.50 s`; production-catalog two-match lifecycle `/private/tmp/warline-production-two-sequential-matches-graphics.xml` 1/1 in `20.58 s`; streamer regression `/private/tmp/warline-packed-sequential-streamer.xml` 32/32; assembly boundaries `/private/tmp/warline-packed-sequential-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/warline-packed-sequential-ecs.log` 11/11; `dotnet build Game.Tests.PlayMode.csproj --no-restore --disable-build-servers -v:q` 0 errors; `git diff --check` | Passed; sequential lifecycle checklist item complete | Extended the real production Menu route over the fingerprinted packed candidate to two complete Match-to-Menu cycles without reinitializing the loaded catalogs. Each cycle requires a loaded candidate EntityScene, section-tagged content, and exactly one operation-map root/metadata/readiness owner, then proves complete destruction of the EntityScene root, all resolved section metadata and section-tagged entities, and all operation-map ECS metadata on Menu return. The static streamer remains unbound and idle through both EntityScene cycles. The independent production-catalog sequential-match regression passes with graphics enabled, covering stale gameplay state and handles outside the Editor-only candidate override. Production remains `StaticSceneChunks`, production cutover remains zero, accepted sources and rollback content are unchanged, and Android remains user-triggered. Progress is 87/148 (59%). |
| 2026-07-23 | Phase 8 packed camera traversal residency and culling | Focused graphics-enabled packed traversal `/private/tmp/warline-packed-camera-culling-v4.xml` 1/1 in `17.37 s`; complete graphics-enabled packed parity/lifecycle/traversal suite `/private/tmp/warline-packed-entityscene-suite-camera.xml` 3/3 in `62.60 s`; streamer regression `/private/tmp/warline-camera-culling-streamer.xml` 32/32; assembly boundaries `/private/tmp/warline-camera-culling-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/warline-camera-culling-ecs.log` 11/11; `dotnet build Game.Tests.PlayMode.csproj --no-restore --disable-build-servers -v:q` 0 errors; `git diff --check` | Passed; Editor camera-residency/culling checklist item complete | Added a graphics-enabled packed-candidate traversal using explicit URP render requests because batch PlayMode has no automatic Game View culling pass. The detail view retained three Entities Graphics batches and `696` chunks while rendering `13,933-13,949` instances with `868` instance tests, `2,139` draw commands, and `348` fully culled chunks; the off-map view retained the same batches/chunks while rendering zero and culling all `696` chunks. Exact EntityScene root, resolved sections, section-tagged entity handles, `RequestSceneLoaded` owners, loaded Unity scenes, operation-map metadata, and unbound static-streamer state remain unchanged at both views. The test restores camera state, other camera enablement, and shadow quality in `finally`. The `104` section `MeshLODComponent` records produced no LOD recalculation, so Android LOD-transition and visual-continuity acceptance remains open and is not claimed here. Production remains `StaticSceneChunks`, production cutover remains zero, accepted sources and rollback content are unchanged, and no Android build ran. Progress is 88/148 (59%). |
| 2026-07-23 | Phase 8 packed post-readiness operation-map allocation gate | Focused packed allocation `/private/tmp/warline-packed-operation-map-allocation-recovered.xml` 1/1 in `15.87 s`; complete packed parity/lifecycle/traversal/allocation suite `/private/tmp/warline-packed-entityscene-suite-allocation.xml` 4/4 in `72.32 s`; streamer regression `/private/tmp/warline-operation-map-allocation-streamer.xml` 32/32; assembly boundaries `/private/tmp/warline-operation-map-allocation-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/warline-operation-map-allocation-ecs.log` 11/11; `dotnet build Game.Tests.PlayMode.csproj --no-restore --disable-build-servers -p:BuildProjectReferences=false -v:minimal` 0 errors; `git diff --check` | Passed; operation-map-attributed allocation checklist item complete | The real packed Menu-to-Match route now measures the recurring EntityScene operation-map boundary after readiness instead of conflating it with unrelated whole-frame Editor, UI, rendering, and gameplay work. After 256 warmups, 2,048 synchronous production calls allocated zero total and zero maximum bytes on the main thread while the unbound static streamer stayed idle, readiness remained unchanged, and all 16,998 section-tagged entities stayed resident. The Match loader was already bound and therefore not polled in steady state. The first sandboxed Unity launch could not write Unity's licensing database; host execution then exposed the documented generic/versioned-client protocol mismatch, and the guarded no-Editor IPC recovery restored validation without changing project content. Production remains `StaticSceneChunks`, production cutover remains zero, accepted sources and rollback content are unchanged, and no Android build ran. Progress is 89/148 (60%). |
| 2026-07-23 | Phase 8 post-bind EntityScene ownership isolation | Focused static guard `/private/tmp/warline-operation-map-no-runtime-generator-focused.xml` 1/1; final static wiring suite `/private/tmp/warline-steady-ownership-wiring-suite.xml` 6/6; final packed parity/lifecycle/traversal/allocation suite `/private/tmp/warline-packed-steady-ownership-final.xml` 4/4 in `70.98 s`; production `StaticSceneChunks` lifecycle regression `/private/tmp/warline-production-runtime-city-ownership-regression.xml` 2/2; streamer regression `/private/tmp/warline-steady-ownership-streamer.xml` 32/32; assembly boundaries `/private/tmp/warline-steady-ownership-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/warline-steady-ownership-ecs.log` 11/11; Unity compile logs contain zero compiler-error signatures; `git diff --check` | Passed; post-bind hierarchy/source/generator/physics checklist item complete | EntityScene Match startup now excludes the legacy runtime-city helper entirely, making its generator path unreachable, while `StaticSceneChunks` preserves existing runtime-city ownership. Packed route assertions prove the accepted operation-map authoring scene and accepted authoring SubScene are absent during stable Match and after teardown. Static method-boundary guards reject hierarchy/global search, source asset/scene access, dense-city generation, and collider/physics queries from recurring post-bind operation-map work; they also lock source-scene transition work behind the unbound gate and require the ready loader to exit before thin-scene resolution. The transition-only renderer-free thin runtime-binding hierarchy resolution remains authorized by the parent contract and is not misreported as steady-state work. Production cutover remains zero, accepted sources and rollback content are unchanged, and no Android build ran. Progress is 90/148 (61%). |
| 2026-07-23 | Phase 8 ECS-only permanent map-visual lifecycle | Static ownership wiring `/private/tmp/warline-managed-map-ownership-wiring-v4.xml` 6/6; packed parity/lifecycle/allocation/destruction fixture `/private/tmp/warline-packed-managed-map-ownership-v2.xml` 4 passed with only the graphics-only camera case skipped under `-nographics`; real packed destruction case passed in `14.61 s`; graphics-enabled production `StaticSceneChunks` lifecycle `/private/tmp/warline-production-map-presentation-ownership-graphics.xml` 2/2 in `28.72 s`; assembly boundaries `/private/tmp/warline-managed-map-ownership-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/warline-managed-map-ownership-ecs.log` 11/11; Unity compile logs contain zero compiler-error signatures; `git diff --check` | Passed; ECS-only permanent map-visual lifecycle checklist item complete | EntityScene now omits all three legacy permanent-map presentation helpers: runtime city, runtime blockers, and runtime decorations. Packed stable Match proves zero managed runtime-building and link-registry entries, zero `RuntimeBuildingEntityLink`, authored-building visual, or candidate authoring components, zero building visual pool root, and zero renderers beneath the allowed empty legacy gameplay shell roots. A real packed building destruction switches only the baked intact/destroyed entity-root scales and enableable destroyed state while preserving the exact loaded GameObject identity set and total ECS entity count. The symmetric graphics-enabled production regression proves `StaticSceneChunks` still constructs all three legacy helpers. Accepted sources, candidate content, production cutover, and rollback content are unchanged, and no Android build ran. Progress is 91/148 (61%). |
| 2026-07-23 | Phase 8 complete Editor PlayMode acceptance | Full graphics-enabled PlayMode `/private/tmp/warline-phase8-full-playmode-v2.xml` 43/43 in `280.21 s`; log `/private/tmp/warline-phase8-full-playmode-v2.log`; focused audio reconciliation `/private/tmp/warline-audio-reconcile-focused-v3.log` 9/9; focused corrected-fixture run `/private/tmp/warline-phase8-five-fixes-v2.xml` 5/6 followed by narrative `/private/tmp/warline-narrative-async-v3.xml` 1/1; assembly boundaries `/private/tmp/warline-phase8-full-playmode-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/warline-phase8-full-playmode-ecs.log` 11/11; `git diff --check` | Passed; complete Editor PlayMode checklist item complete | The ordered full assembly now passes repeated Menu/Match lifecycle stress, narrative Addressables playback, gameplay command/transport/initial-spawn fixtures, both music transitions, and all packed EntityScene cases without a skip. Five fixtures were aligned with current authored/ECS contracts and asynchronous presentation timing. The remaining suite-order failure was fixed in production: a recreated audio presentation reconciles the persistent ECS-owned music state once on enable and restarts only a missing same-event source, without mutating request queues or adding steady-state allocation. Sandboxed licensing storage was read-only; guarded no-Editor IPC recovery plus the approved project wrapper under full access completed validation. Production remains `StaticSceneChunks`, production cutover remains zero, accepted sources and rollback content are unchanged, and no Android build ran. Progress is 92/148 (62%). |
| 2026-07-23 | Phase 0A accepted-state fixed-camera parity closure | Existing fixed-camera report `Design/AgentReports/2026-07-22_operation_map_fixed_camera_parity.json` 5/5; ten paired PNGs under `Design/AgentReports/Captures/2026-07-22_operation_map_fixed_camera_parity`; current candidate SubScene SHA-256 `31a2638471ed23fa5dfe39c3544454ff79af126b219fa3813de6cbd09c8f4314`; fingerprinted packed runtime report `Design/AgentReports/2026-07-21_dense_city_phase0a_candidate_runtime_content.json`; packed runtime parity `/private/tmp/warline-phase8-full-playmode-v2.xml` 5/5 packed cases inside 43/43 full PlayMode; assembly boundaries `/private/tmp/warline-phase8-full-playmode-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/warline-phase8-full-playmode-ecs.log` 11/11; capture-file hash audit; `git diff --check` | Passed for the accepted project-owner-approved `NotGenerated` candidate; Editor fixed-camera checklist item complete | The report's overview, minimap, and three detail pairs cover the accepted terrain, roads, bridges, buildings, vehicles, mountains, vegetation, props, infrastructure, lighting, and minimap state. Every tracked PNG still matches its recorded SHA-256, and the captured candidate scene is byte-identical to the candidate fingerprint used by the later packed Addressables/Entities runtime matrix and transformed-bounds proof. The earlier validation row left this open only for generated-city content that the project owner subsequently removed; absent content is no longer part of the accepted candidate and no parity is claimed for it. Production remains `StaticSceneChunks`, production cutover remains zero, rollback content is unchanged, and Android visual parity remains user-triggered. Progress is 93/148 (63%). |
| 2026-07-23 | Phase 8 packed authored-vehicle transform and movement parity | Authored-grid EditMode `/private/tmp/warline-authored-vehicle-grid-editmode-final.xml` 2/2; focused packed movement `/private/tmp/warline-packed-vehicle-v15.xml` 1/1 in `15.83 s`; complete packed fixture `/private/tmp/warline-packed-vehicle-full-fixture.xml` 6/6 in `109.82 s`; assembly boundaries `/private/tmp/warline-packed-vehicle-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/warline-packed-vehicle-ecs.log` 11/11; `dotnet build Game.Runtime.csproj` and `Game.Tests.PlayMode.csproj` with `--no-restore --disable-build-servers -v:q` 0 errors; `git diff --check` | Passed current packed vehicle-behavior sub-gate; failure/reset/retry and generated-city coverage remain open | Split initial grid ownership so accepted authored vehicles derive `UnitGrid` from their baked world transform without snapping the transform back to the default cell, while ordinary spawned units retain the existing grid-to-world initialization. The real packed route selects a section-owned authored ground vehicle, supplies temporary ECS-only neutral-faction fuel, issues the production grouped manual move, resolves a real surface-aware path, moves the gameplay entity, and proves its baked visual root follows with stable identity, hierarchy, EntityScene/section ownership, static-streamer state, and zero managed map-visual ownership. Pathfinding now declares its existing simulation-group ownership explicitly. The broader lifecycle checklist remains open because packed readiness failure/reset/retry and complete generated-city content are not yet accepted; progress remains 93/148 (63%). |
| 2026-07-23 | Phase 8 packed readiness failure/reset/retry lifecycle | Focused loader cleanup gate `/private/tmp/warline-operation-map-cleanup-gate-host.log` 18/18; focused real packed retry `/private/tmp/warline-packed-readiness-retry-7.xml` 1/1; complete packed fixture `/private/tmp/warline-packed-readiness-full-fixture.xml` 7/7 in `101.14 s`; assembly boundaries `/private/tmp/warline-packed-readiness-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/warline-packed-readiness-ecs.log` 11/11; `dotnet build Game.Composition.csproj`, `Game.Tests.Editor.csproj`, and `Game.Tests.PlayMode.csproj` with `--no-restore --disable-build-servers -v:q` 0 errors; `git diff --check` | Passed current packed failure/reset/retry sub-gate; generated-city coverage remains open | A real packed readiness failure now retains ownership until both the EntityScene release and thin source-scene unload complete. `TryReset` rejects pending cleanup instead of clearing handles and allowing overlapping ownership; synchronous completion remains valid. The test-only wrapper forces one deterministic wrong-map readiness rejection after real section load without modifying entities, then proves failed scene/section destruction, same-definition and same-catalog retry, normal readiness, and complete unload. The initial sandboxed launch could not write Unity's licensing database; guarded no-Editor IPC recovery followed by the approved project wrapper with host access restored validation without changing project content. Production remains `StaticSceneChunks`, production cutover remains zero, accepted sources and rollback content are unchanged, Android remains user-triggered, and progress remains 93/148 (63%). |
| 2026-07-24 | Phase 1 protected dense-city candidate hierarchy transaction | Candidate transaction `/private/tmp/warline-dense-city-candidate-transaction-v6.xml` 4/4; semantic hierarchy regression `/private/tmp/warline-dense-city-semantic-hierarchy-regression.xml` 5/5; assembly boundaries `/private/tmp/warline-dense-city-candidate-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/warline-dense-city-candidate-ecs.log` 11/11; `dotnet build Game.Tests.Editor.csproj --no-restore --disable-build-servers -v:q` 0 errors; protected-source SHA-256 audit; `git diff --check` | Passed; semantic ownership is ready for deterministic record replay, while dense-city realization remains open | Added `Game/Maps/Skirmish Desert Base/Create Dense City Candidate Hierarchy` as a fail-closed copy transaction over four distinct scene paths. It computes one deterministic generation identity from the protected source pair, config, schema, version, and seed; requires independent candidate GUIDs; creates and validates the paired generated roots; saves only the candidate copies; and deletes both outputs on any failure. The reopened real assets validate one shared generated state, five exact nearest map-bake role owners, and no entity-presentation bake groups. Accepted map hash `f210e53df16b9dad1c96457b4e72c03260ac8ab215a3f4e959b97d54c904be88`, accepted SubScene hash `eff3ce6992d234c7438a321f0f9f552c2abebcc0a4738445014bc8f86579965d`, and current packed candidate hash `31a2638471ed23fa5dfe39c3544454ff79af126b219fa3813de6cbd09c8f4314` remain unchanged. Production remains `StaticSceneChunks`, production cutover remains zero, Addressables/rollback ownership is unchanged, and no Android build ran. Progress is 95/148 (64%). |
| 2026-07-24 | Phase 2 sealed generation-record snapshot foundation | Focused records `/private/tmp/warline-dense-city-record-snapshot-v2.xml` 10/10; assembly boundaries `/private/tmp/warline-dense-city-record-snapshot-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/warline-dense-city-record-snapshot-ecs.log` 11/11; Unity compile 0 errors; `git diff --check` | Passed dependency evidence; generator snapshot publication and candidate replay remain open | Added an editor-only owned `DenseCityGenerationRecordSnapshot` over copies of the already-sealed, stable-sorted building, surface, and presentation record lists. Snapshot creation rejects unsealed input, and focused tests prove the snapshot retains deterministic order after its source transaction is disposed. This removes the record-lifetime blocker without changing generator RNG order, realization, scenes, runtime systems, production ownership, or Android output. Checklist progress remains 95/148 (64%) until the real generator publishes this snapshot and candidate replay is accepted. |
| 2026-07-24 | Phase 2 generator snapshot publication | Focused records `/private/tmp/warline-dense-city-builder-snapshot-publication.xml` 10/10; assembly boundaries `/private/tmp/warline-dense-city-builder-snapshot-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/warline-dense-city-builder-snapshot-ecs.log` 11/11; Unity compile 0 errors; `git diff --check` | Passed dependency evidence; candidate replay remains open | `DenseMiddleEasternCityEditModeBuilder.Result` now publishes the owned sealed snapshot created after all generation and semantic parity checks but before the transaction context is disposed. Existing callers, deterministic seed/RNG order, realization behavior, and result counters remain unchanged. This gives the candidate transaction a stable replay payload without retaining mutable generator state or introducing runtime ownership. No scene, Addressables definition, production mode, rollback content, or Android output changed; progress remains 95/148 (64%). |
| 2026-07-24 | Phase 2 snapshot-backed replay/proxy source contract | Presentation replay `/private/tmp/warline-dense-city-snapshot-replay-v2.xml` 2/2; surface-proxy regression `/private/tmp/warline-dense-city-snapshot-proxy-regression.xml` 13/13; assembly boundaries `/private/tmp/warline-dense-city-snapshot-source-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/warline-dense-city-snapshot-source-ecs.log` 11/11; Unity compile 0 errors; `git diff --check` | Passed dependency evidence; real candidate realization remains open | Added one internal read-only generation-record source contract implemented by both the mutable pre-seal transaction set and immutable snapshot. Presentation replay, building replay, render-only replay, and surface-proxy construction now consume only that read contract. The integration fixture disposes the mutable source before replay and still realizes the complete building/render-only set from the snapshot, while all 13 proxy cases remain green. No scene, production owner, runtime system, Addressables definition, rollback content, or Android output changed; progress remains 95/148 (64%). |
| 2026-07-24 | Phase 2/4 protected dense-city candidate realization | Complete transaction `/private/tmp/warline-dense-city-realize-candidate-v20.log`; focused placement/proxy `/private/tmp/warline-dense-city-global-placement-batched-proxy-focused.xml` 25/25; replay/readiness `/private/tmp/warline-dense-city-realized-replay-readiness-focused-v2.xml` 52/52; assembly boundaries `/private/tmp/warline-dense-city-realized-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/warline-dense-city-realized-ecs.log` 11/11; accepted-source SHA-256 audit; `git diff --check` | Passed candidate-only realization; generated-candidate ECS bake, Addressables load, transform/bounds parity, fixed-camera parity, and production cutover remain open | Replayed the sealed generator snapshot into the protected scene pair as 5,371 gameplay buildings and 30,425 independent render-only owners, generated 34,595 bounded gameplay surfaces into 2,517 merged renderer-free proxy partitions, and passed dense-city readiness before saving only candidate assets. Generated building placement indices are deterministic and map-global while stable identities retain district-local record sequences; out-of-grid rejected building attempts still reserve their five-record spans. Batched proxy asset creation preserves deterministic output while reducing complete realization from about nine minutes to under one minute. Dense candidate hashes are `8ea3d25a...c2b2fc6f` / `7e95c29b...55e93eb`; accepted map `f210e53d...04be88` and accepted packed candidate `31a26384...f4314` are unchanged. Production remains `StaticSceneChunks`, production Addressables and rollback content are untouched, and no Android build ran. Progress is 96/148 (65%). |
| 2026-07-24 | Phase 5 generated presentation identity prerequisite | Building realization `/private/tmp/warline-dense-city-generated-presentation-identity-focused.xml` 2/2; render-only realization `/private/tmp/warline-dense-city-generated-presentation-identity-renderonly.xml` 4/4; assembly boundaries `/private/tmp/warline-dense-city-generated-identity-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/warline-dense-city-generated-identity-ecs.log` 11/11; Unity compile 0 errors; protected-scene SHA-256 audit; `git diff --check` | Passed dependency evidence; generated-candidate bake parity remains open | Added one passive generated-owner authoring identity that bakes to unmanaged `DenseCityPresentationIdentity` data. Every generated gameplay-building owner and independent render-only owner receives its record-derived bounded `densecity.<sha256>` identity and explicit role; building attachments remain owned by their building and receive no independent identity. The existing populated candidate correctly rejected a second realization before mutation, and rollback retained dense candidate hashes `8ea3d25a...c2b2fc6f` / `7e95c29b...55e93eb` plus accepted hashes `f210e53d...04be88` / `31a26384...f4314`. Transactional candidate regeneration and the candidate-to-baked matrix/transformed-bounds join are the next steps. Production, accepted candidates, Addressables, rollback content, and Android outputs are unchanged; progress remains 96/148 (65%). |
| 2026-07-24 | Phase 5 realized-candidate generated identity backfill | Focused replay/backfill `/private/tmp/warline-dense-city-identity-backfill-focused-v3.log` 2/2; first candidate transaction `/private/tmp/warline-dense-city-identity-backfill-candidate-v2.log`; byte-stable readiness/no-op transaction `/private/tmp/warline-dense-city-identity-backfill-readiness-noop.log`; report `Design/AgentReports/2026-07-24_dense_city_generated_identity_backfill.json`; assembly boundaries `/private/tmp/warline-dense-city-identity-backfill-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/warline-dense-city-identity-backfill-ecs.log` 11/11; Unity compile 0 errors; protected SHA-256 audit; `git diff --check` | Passed dependency evidence; generated-candidate ECS bake and matrix/bounds parity remain open | Rebuilt the sealed record snapshot in memory, prevalidated every stable-id/prefab/material/name/world-matrix/semantic-parent join, and then added exactly 35,796 identities to the already-realized dense entity candidate: 5,371 gameplay-building owners plus 30,425 independent render-only owners. Attachments remain building-owned. The second full transaction reported `added=0` / `existing=35796`, passed dense readiness, and retained candidate SHA-256 `4214935c...534c2b` byte-for-byte. Dense map `8ea3d25a...c2b2fc6f`, accepted map `f210e53d...04be88`, accepted SubScene, accepted packed candidate `31a26384...f4314`, map-wide config, production Addressables, rollback content, and Android outputs are unchanged. Progress remains 96/148 (65%). |
| 2026-07-24 | Phase 5 dense generated candidate direct ECS bake and transform/bounds parity | Complete in-memory bake runs `/private/tmp/warline-dense-candidate-bake.log` and `/private/tmp/warline-dense-candidate-bake-repeat.log`; compact reports `Design/AgentReports/2026-07-24_dense_city_generated_candidate_bake_validation.json` and `Design/AgentReports/2026-07-24_dense_city_generated_transform_parity.json`; assembly boundaries `/private/tmp/warline-dense-candidate-bake-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/warline-dense-candidate-bake-ecs.log` 11/11; Unity compile 0 errors; protected SHA-256 and report byte-stability audit; `git diff --check` | Passed direct-bake dependency evidence; dense Addressables-loaded runtime parity remains open | Added `Game/Operation Maps/EntityScene Migration/Bake And Validate Dense City Candidate` as a candidate-only, in-memory bake gate. It validates exact authoring and baked populations: 5,803 gameplay buildings, 22 vehicles, 3 roots, 9,544 legacy identities, 35,796 generated identities (5,371 building and 30,425 render-only), 5,803 building presentations, 78,325 render meshes, 111,455 total entities, 4,437 chunks, zero non-finite transforms, and zero managed map-visual companions. Generated parity matched all 35,796 owner identities and all 64,076 eligible renderer/material entities with zero role, world-matrix, transformed-bounds, duplicate, missing, unexpected, unresolved, or unconsumed failures. Repeated report hashes are `ae775d8e...c87d2d` and `6f987994...2e509`; dense entity/map candidates remain `4214935c...534c2b` / `8ea3d25a...c2b2fc6f`, accepted map remains `f210e53d...04be88`, and accepted packed candidate remains `31a26384...f4314`. Reports retain aggregate counts/digests plus at most 64 rejected samples instead of serializing tens of thousands of passed rows. Production stays `StaticSceneChunks`; production Addressables, rollback content, and Android outputs are untouched. The three-checkpoint Phase 5 item remains unchecked, so progress remains 96/148 (65%). |
| 2026-07-24 | Phase 7 dense candidate definition/runtime-binding isolation | Dense layout builder `/private/tmp/warline-dense-city-layout-build-final4.log` and byte-stable repeat `/private/tmp/warline-dense-city-layout-build-repeat-final4.log`; focused ownership `/private/tmp/warline-dense-city-layout-ownership-final4.log` 7/7; report `Design/AgentReports/2026-07-24_dense_city_candidate_entityscene_addressables_layout.json`; assembly boundaries `/private/tmp/warline-dense-city-layout-architecture-final4.log` 31/31; ECS/Burst guardrails `/private/tmp/warline-dense-city-layout-ecs-final4.log` 11/11; Unity compile 0 errors; protected asset SHA-256 audit; `git diff --check` | Passed dependency evidence; dense built-content and Addressables-loaded runtime parity remain open | Added a dense-specific definition and renderer-free thin runtime binding without changing the accepted packed candidate or production route. The deterministic plan requires exactly five existing roots and zero optional-heavy/shared/static/presentation/placement roles. Dense definition `cbfc2f62e3142413485019bd83197539`, thin binding `dad0bd13fb20943dfb2f881cbe225f05`, and EntityScene `c00140f2e94a04c3084c8dcb0c18cbd0` GUIDs are distinct and collision-checked against every production, accepted-candidate, surface, and minimap root. Candidate outputs are journaled and restored on failure; all five package roots, production/accepted assets, and Addressables settings are hashed across the operation and global dirty-asset saving is prohibited. Two complete builds produced byte-identical normalized definition, scene, metadata, and report hashes. Production remains `StaticSceneChunks`; production Addressables, accepted sources/candidates, rollback content, and Android outputs are unchanged. Progress remains 96/148 (65%) until built-content ownership and real dense runtime parity are accepted. |
| 2026-07-24 | Phase 7 isolated dense macOS runtime content | Successful isolated builds `/private/tmp/warline-dense-city-runtime-content-build-v7.log` and `/private/tmp/warline-dense-city-runtime-content-build-v8.log`; report `Design/AgentReports/2026-07-24_dense_city_candidate_runtime_content.json`; dense Addressables output `Library/OperationMapDenseCityRuntimeContent/Addressables`; dense Entities output `Library/OperationMapDenseCityRuntimeContent/Entities`; accepted-output recovery `/private/tmp/warline-accepted-candidate-runtime-content-restore.log`; assembly boundaries `/private/tmp/warline-dense-runtime-content-architecture-final.log` 31/31; ECS/Burst guardrails `/private/tmp/warline-dense-runtime-content-ecs-final.log` 11/11; Unity compile 0 errors; `git diff --check` | Passed dependency evidence; real dense Addressables-loaded transform/bounds parity remains open | Added a dense-only macOS content builder that fails before mutation below 15 GiB free, persists its temporary settings only beneath the dense candidate directory for the duration of the build, owns exactly two explicit Addressable entries, publishes four packed bundles plus one Entities archive, and removes the temporary settings afterward. A directory transaction retains rollback copies until commit, restores the accepted `Library/com.unity.addressables/aa/OSX` output before protection checks, and restores production settings bytes after Unity's global Addressables modification listener observes the temporary group. Two accepted runs retained the same four bundle filenames, exact five-root plan, `187112286` Entity-content bytes, and Entities catalog SHA-256 `105bc0bb...98ebb`; the packed catalog is fingerprinted per build because transient Addressables serialization changes its catalog hash without changing addresses, bundle names, or package ownership. The current report records dense EntityScene `c00140f2e94a04c3084c8dcb0c18cbd0`, four bundles, one archive, `productionCutover=0`, `productionSettingsMutated=0`, and `sharedOutputRestored=1`. Production remains `StaticSceneChunks`; accepted sources/candidates, production Addressables settings, frozen rollback content, and Android outputs are unchanged. Progress remains 96/148 (65%) until the real dense packed two-cycle matrix/transformed-bounds proof is accepted. |
| 2026-07-24 | Phase 5/7 dense expected-runtime parity manifest | Repeated dense direct bakes `/private/tmp/warline-dense-runtime-manifest-bake-v4-escalated.log` and `/private/tmp/warline-dense-runtime-manifest-bake-v5-escalated.log`; summary `Design/AgentReports/2026-07-24_dense_city_runtime_parity_manifest.json`; binary `Library/OperationMapDenseCityRuntimeParity/dense_candidate_runtime_parity.bin`; assembly boundaries `/private/tmp/warline-dense-runtime-manifest-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/warline-dense-runtime-manifest-ecs.log` 11/11; Unity compile 0 errors; `git diff --check` | Passed dependency evidence; real dense Addressables-loaded comparison remains open | The direct dense bake now writes one fixed-width format-3 runtime comparison manifest while the bake world is alive. It contains exact matrices for all 9,544 legacy and 35,796 generated identities plus matrices, local bounds, and transformed world bounds for all 78,325 render rows. Stable ordinal sorting, duplicate/non-finite rejection, exact count gates, transactional publication, and source fingerprints prevent stale or partial evidence. Two full bakes produced byte-identical `15434582`-byte output with SHA-256 `520593be...78f3`, candidate SubScene SHA-256 `4214935c...534c2b`, and direct parity-report SHA-256 `6f987994...2e509`. The compact summary retains counts, tolerances, fingerprints, and manifest hash without serializing passed rows to JSON. The first sandboxed licensing attempts could not create the macOS IPC channel; guarded no-Editor recovery and the mandated wrapper with normal IPC access restored validation. Production remains `StaticSceneChunks`; accepted sources/candidates, production Addressables, frozen rollback content, and Android outputs are unchanged. Progress remains 96/148 (65%) until the real dense packed two-cycle matrix/transformed-bounds proof is accepted. |
| 2026-07-24 | Phase 5/7 dense packed-readiness contract correction | Focused generated-readiness EditMode `/private/tmp/warline-dense-readiness-generated-focused.xml` 5/5; no-op identity backfill `/private/tmp/warline-dense-readiness-contract-backfill.log`; direct dense bake `/private/tmp/warline-dense-readiness-direct-bake.log`; dense layout rebuild `/private/tmp/warline-dense-readiness-layout-rebuild.log`; isolated runtime-content rebuild `/private/tmp/warline-dense-readiness-runtime-content-rebuild-v2.log`; first real dense packed comparison `/private/tmp/warline-dense-packed-runtime-parity-v6.xml`; assembly boundaries `/private/tmp/warline-dense-readiness-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/warline-dense-readiness-ecs.log` 11/11; Unity compile 0 errors | Readiness correction passed; dense packed transform/bounds parity remains rejected on one renderer row | Extended the existing packed EntityScene readiness owner to count and validate unmanaged `DenseCityPresentationIdentity` rows, including non-empty unique stable IDs and exact generated role totals. The dense candidate contract now requires 5,803 gameplay buildings, 22 vehicles, 39,515 render-only owners, and 35,796 generated identities while preserving the accepted zero-generated contract. The no-op backfill retained all 35,796 identities and produced candidate SubScene SHA-256 `182e1975...6e03`; direct bake retained 9,544 legacy identities, 35,796 generated identities, and 78,325 render rows. Isolated runtime content rebuilt as four Addressables bundles (`921138` bytes) plus one Entities archive (`187126934` bytes), with production settings unchanged and shared output restored. The real two-cycle fixture now passes load/readiness and reaches row comparison, where one renderer matrix/world-bound row has residual `0.000122070313` against the Section 12.0A matrix limit `0.0001`; the limit was not weakened and no exception was added. Manifest SHA-256 is `b3ce9b3e...b9ff`. Production remains `StaticSceneChunks`; accepted sources/candidates, production Addressables, frozen rollback content, and Android outputs are unchanged. Progress remains 96/148 (65%) until the named renderer mismatch is resolved and both packed cycles pass. |
| 2026-07-24 | Phase 5/7 dense canonical-transform packed runtime parity | Diagnostic packed run `/private/tmp/warline-dense-packed-runtime-parity-v8.xml`; repeated canonical direct bakes `/private/tmp/warline-dense-canonical-transform-manifest-bake.log` and `/private/tmp/warline-dense-canonical-transform-manifest-bake-repeat.log`; accepted two-cycle packed comparison `/private/tmp/warline-dense-packed-runtime-parity-v9.xml` 1/1 in `18.95 s`; summary `Design/AgentReports/2026-07-24_dense_city_runtime_parity_manifest.json`; binary `Library/OperationMapDenseCityRuntimeParity/dense_candidate_runtime_parity.bin`; assembly boundaries `/private/tmp/warline-dense-canonical-parity-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/warline-dense-canonical-parity-ecs.log` 11/11; Unity compile 0 errors; `git diff --check` | Passed; dense candidate-to-direct-baked-to-Addressables-runtime matrix/transformed-bounds checklist item complete | Attributed the rejected row to generated owner `densecity.06e54af1...6a0`: only translation element `m03` differed (`1816.96741` direct-bake snapshot versus `1816.96753` runtime), exactly one float ULP at that magnitude, with identical local bounds and the corresponding one-ULP transformed-bound difference. The manifest writer now derives every identity and renderer world matrix from `LocalTransform`, `Parent`, and `PostTransformMatrix` using `float4x4`/`math.mul` in Unity's `LocalToWorldSystem` composition order rather than persisting the temporary bake world's pre-update `LocalToWorld`. No tolerance or runtime transform changed. Both complete bakes produced byte-identical canonical manifest SHA-256 `884685d8...9f6e`, retaining 9,544 legacy identities, 35,796 generated identities, 78,325 render rows, and `15434582` bytes. The real isolated Addressables/Entities package passed both complete load/readiness/capture/unload cycles with strict matrix `0.0001` and bounds `0.001` gates and complete teardown. Production remains `StaticSceneChunks`; accepted sources/candidates, production Addressables, frozen rollback content, and Android outputs are unchanged. Progress is 97/148 (66%). |
| 2026-07-24 | Phase 5/8 dense packed fixed-camera parity | Canonical Editor baseline `/private/tmp/warline-dense-editor-edgegate.log`; final real packed two-cycle PlayMode `/private/tmp/warline-dense-packed-fixed-camera-final.xml` 1/1 in `109.55 s`; comparator fixtures `/private/tmp/warline-dense-edge-uniform-focused.xml` 1/1 and `/private/tmp/warline-dense-edge-silhouette-focused.xml` 1/1; runtime report `Design/AgentReports/2026-07-24_dense_city_runtime_fixed_camera_parity.json`; five Editor PNGs and ten runtime PNGs under `Design/AgentReports/Captures/2026-07-24_dense_city_*fixed_camera*`; faction-tint regression `/private/tmp/warline-dense-fixed-camera-faction-tint.log` 3/3; assembly boundaries `/private/tmp/warline-dense-fixed-camera-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/warline-dense-fixed-camera-ecs.log` 11/11; `dotnet build Game.Tests.PlayMode.csproj --no-restore --disable-build-servers` 0 errors | Passed; protected-candidate Autobahn/two-lane-road harmonization is dependency-ready | Captured the protected dense candidate in the Editor and through the real isolated Addressables/Entities runtime route at 1280x720 overview, minimap, and three detail views. Both runtime load/capture/unload cycles pass all ten rows with maximum mean-channel delta `0.0020248` against `0.0025` and maximum non-edge changed-pixel ratio `0.0037696` against the unchanged `0.01` limit. Raw changed-pixel ratios `0.02247`-`0.05631` remain recorded as diagnostics: a source-or-runtime 3x3 RGB range above 16 classifies rasterization-edge pixels, while only changed non-edge pixels drive the existing changed-pixel acceptance limit. Focused fixtures prove a uniform color shift remains a full interior failure and a one-pixel silhouette shift remains visible in raw diagnostics but not interior drift. This secondary visual gate does not replace the exact 45,340 identity and 78,325 renderer matrix/local-bounds/transformed-bounds proof. Production remains `StaticSceneChunks`; production Addressables, accepted sources/candidates, frozen rollback content, and Android outputs are unchanged. Checklist progress remains 97/149 (65%). |
| 2026-07-24 | Phase 2 protected Autobahn two-lane-road harmonization | Transactional realization `/private/tmp/warline-dense-autobahn-realize-marker-clean-final.log`; strict scene gate `/private/tmp/warline-dense-autobahn-scene-validation-clean-final.log`; direct ECS bake `/private/tmp/warline-dense-autobahn-bake-clean-final.log`; Editor baseline `/private/tmp/warline-dense-autobahn-editor-baseline-final.log`; isolated runtime content `/private/tmp/warline-dense-autobahn-runtime-content-clean-final.log`; packed two-cycle matrix/bounds/fixed-camera PlayMode `/private/tmp/warline-dense-autobahn-packed-fixed-camera-clean-final-v2.xml` 1/1 in `58.90 s`; focused planner/scene validation `/private/tmp/warline-dense-autobahn-focused-editmode-final.xml` 19/19; realization/material validation `/private/tmp/warline-dense-autobahn-realizer-editmode-final.xml` 5/5; ECS/Burst guardrails `/private/tmp/warline-dense-autobahn-ecs-burst-final.xml` 11/11; broad architecture inventory `/private/tmp/warline-dense-autobahn-assembly-boundaries-final.xml` 50/58 with eight unchanged legacy UI/runtime inventory failures outside this change | Passed candidate-only; production cutover and Android acceptance remain open | Replaced the two legacy Autobahn renderer owners with empty stable-identity anchors and generated a continuous adjacent-lane route from columns `-196..284` on rows `29..30`, plus 146 cells that join every accepted crossing across 29 columns. All 1,108 cells resolve exactly once through a generated-root manifest to four approved dense asphalt prefab GUIDs; temporary markers are absent from the saved candidate. The strict gate passes `anchors=2`, `laneCells=962`, `connectorCells=146`, `prefabGuids=4`; direct and packed evidence retain 9,544 legacy plus 36,946 generated identities and 82,797 render rows at matrix `0.0001` and bounds `0.001` tolerances. The isolated package contains four Addressables bundles (`921139` bytes) and one Entities archive (`190463113` bytes), with `productionCutover=0`, `productionSettingsMutated=0`, and `sharedOutputRestored=1`. Both packed cycles pass all five views with maximum mean-channel delta `0.0015131` and maximum non-edge changed-pixel ratio `0.0020454`. No temporary generated marker component, managed runtime map visual, gameplay surface authoring, collider, Rigidbody, source-scene mutation, production Addressables mutation, rollback mutation, or Android output was introduced. Progress is 98/149 (66%). |
| 2026-07-25 | Phase 2 repeated generated prefab shared-instance proof | Diagnostic direct bakes `/private/tmp/warline-dense-shared-instance-direct-bake-v2.log` through `v5.log`; accepted schema-2 repeated direct bakes `/private/tmp/warline-dense-shared-instance-direct-bake-v8-schema2.log` and `/private/tmp/warline-dense-shared-instance-direct-bake-v9-schema2-repeat.log`; direct report `Design/AgentReports/2026-07-24_dense_city_generated_transform_parity.json`; isolated runtime-content rebuild `/private/tmp/warline-dense-shared-instance-runtime-content-schema2.log`; exact packed two-cycle PlayMode `/private/tmp/warline-dense-shared-instance-packed-runtime-schema2.xml` 1/1 in `36.10 s`; assembly boundaries `/private/tmp/warline-dense-shared-instance-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/warline-dense-shared-instance-ecs-burst.log` 11/11; `dotnet build Game.Editor.csproj --no-restore --disable-build-servers` 0 errors; Unity compile 0 errors; `git diff --check` | Passed candidate-only; production cutover and Android acceptance remain open | Extended the existing exact renderer join rather than creating a parallel ownership path. All 68,735 generated renderer/submesh rows resolve to the exact source and baked `Mesh`/`Material` pair. The schema-2 report proves 57 repeated persistent prefab sources cover 31,973 placements and 377 repeated resolved presentation signatures cover 68,722 rows, with zero persistent-source failures, unresolved renderers/assets, mesh/material mismatches, placement-specific managed components, unconsumed rows, or repeated-signature pair mismatches. Diagnostic rejection correctly attributed 608 apparent failures to the canal water prefab's valid negative FBX mesh local file ID; accepting signed nonzero Unity local IDs removed the false rejection without weakening persistence. Two complete bakes retained report SHA-256 `8f05ebc9...de4a`, manifest SHA-256 `067c03ec...9e5d`, 36,946 generated identities, and 82,797 total render rows. The rebuilt isolated package retained four Addressables bundles (`921139` bytes), one Entities archive (`190463113` bytes), `productionCutover=0`, `productionSettingsMutated=0`, and `sharedOutputRestored=1`; both real load/readiness/capture/unload cycles passed. Accepted sources/candidates, production Addressables, frozen rollback content, and Android outputs remain unchanged. Progress is 99/149 (66%). |
| 2026-07-25 | Phase 2 generated-building ECS-only runtime ownership proof | Protected placement guard `/private/tmp/warline-dense-building-placement-protection-focused.xml` 1/1 in `3.25 s`; real dense packed two-cycle matrix/bounds plus generated-building destruction `/private/tmp/warline-dense-generated-building-managed-ownership-packed.xml` 1/1 in `40.04 s`; direct bake report `Design/AgentReports/2026-07-24_dense_city_generated_candidate_bake_validation.json`; assembly boundaries `/private/tmp/warline-generated-building-managed-ownership-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/warline-generated-building-managed-ownership-ecs-burst.log` 11/11; `dotnet build Game.Tests.Editor.csproj` and `Game.Tests.PlayMode.csproj` with `--no-restore --disable-build-servers` 0 errors; `git diff --check` | Passed candidate-only; production cutover and Android acceptance remain open | Added transactional protection for the accepted 451-row building-placement migration input: realization captures count and SHA-256 `26973214...3464d`, validates them after `AssetDatabase.SaveAssets`, restores the asset on rejection, and reports both metrics on success. Focused coverage mutates the config and proves fail-closed rejection. Existing readiness rejects generated runtime links, while the direct bake proves 4,971 generated gameplay buildings, 5,403 building presentations, and zero managed companions. The real isolated packed fixture now damages one exact generated building in each complete cycle and proves the Burst ECS visual-state transition creates/destroys neither entities nor GameObjects and creates no `RuntimeBuildingEntityLink`. No accepted source/candidate, production definition/Addressables, frozen rollback package, or Android output changed. Progress is 100/149 (67%). |
| 2026-07-25 | Phase 2 explicit post-generation building ownership | Focused transaction ownership `/private/tmp/warline-dense-city-no-name-owner-focused-rerun.log` 8/8; deterministic two-build fixture `/private/tmp/warline-dense-city-no-name-deterministic-rerun.log` 1/1; assembly boundaries `/private/tmp/warline-dense-city-no-name-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/warline-dense-city-no-name-ecs.log` 11/11; `dotnet build Game.Editor.csproj --no-restore` 0 errors; `git diff --check` | Passed partial dependency step; post-generation name/category detection checklist item remains open | Civic ownership recovery, open-ground footprint collection, and the building material pass now consume the transaction's exact committed `DenseCityRealizedBuildingOwner` rows instead of rescanning `RuntimeCityVisuals`, `_Visual`, `_GroundPatch`, or `Hall` names. Hall-family material exclusion is explicit placement metadata carried with the accepted owner and does not alter RNG order. Removed the post-seal renderer-connectivity cleanup because it mutated temporary realized hierarchy after immutable records were published and could never be represented by record replay. The deterministic fixture produced identical hierarchy hashes across two builds and retained the canonical map hash. Canal-underpass protected-overlap exemptions and readiness category ownership still require explicit record/component ownership, so progress remains 100/149 (67%). |
| 2026-07-25 | Phase 2 explicit generated presentation semantics | Focused authoring readiness `/private/tmp/warline-dense-semantic-readiness-focused.log` 30/30; canal record propagation `/private/tmp/warline-dense-semantic-canal-focused.log` 5/5; packed ECS readiness `/private/tmp/warline-dense-semantic-runtime-readiness-focused.log` 7/7; accepted transactional migration `/private/tmp/warline-dense-semantic-identity-backfill-v8.log`; byte-stable no-op repeat `/private/tmp/warline-dense-semantic-identity-backfill-noop.log`; direct dense ECS bake `/private/tmp/warline-dense-semantic-direct-bake.log`; assembly boundaries `/private/tmp/warline-dense-semantic-architecture.log` 31/31; ECS/Burst guardrails `/private/tmp/warline-dense-semantic-burst.log` 11/11; `dotnet build Game.Editor.csproj` and `Game.Composition.csproj` 0 errors; `git diff --check` | Passed candidate-only; production cutover and Android acceptance remain open | Added closed semantic category and protected-overlap flags to immutable generation records, candidate authoring identities, and unmanaged baked identities. Generated canal underpasses retain exact renderer ownership through their transaction; readiness validates the explicit nearest identity instead of names, named category roots, or inferred bounds. The accepted older candidate migration resolved 4,971 building plus 31,975 render-only identities, classified its 12 paired underpass owners through a migration-only geometry/count gate, and preserved candidate SHA-256 `a78eb6d...5fc1` on the second run. Direct bake retained 5,403 total gameplay buildings, 36,946 generated identities, 82,797 render rows, 118,274 entities, zero duplicate/unknown identities, zero non-finite transforms, and zero managed map-visual companions; the runtime parity manifest is `16099896` bytes and all 36,946 transform rows pass. Production remains `StaticSceneChunks`; accepted source/SubScene, production Addressables, frozen rollback content, and Android outputs remain unchanged. Progress is 101/149 (68%). |
| 2026-07-25 | Phase 2 deterministic generator schema-v1 RNG contract | `DenseCityDeterministicFixtureTests.DenseBuilder_RandomContractMatchesGeneratorSchemaV1`; Unity Roslyn compile using the current `Game.Tests.Editor.rsp` with the assembly name preserved; independent normalized source extraction; prior two-build fixture `/private/tmp/warline-dense-city-no-name-deterministic-rerun.log` 1/1; `git diff --check` | Passed bounded deterministic contract; feature-kind semantic coverage remains open | Extended the existing deterministic fixture with a fail-closed source contract for the exact three dense-generator RNG stream constructors, their seed fallbacks and salts, and all 25 ordered `Random.Next`/`NextDouble` draws. Whitespace-only source formatting is ignored, while any added, removed, reordered, or argument-changed draw fails with an explicit requirement for a reviewed generator schema/version migration. The test compiles cleanly through Unity's current Roslyn response file; an independent extraction matched three streams and all 25 expected calls. The previously accepted two-build fixture remains the runtime output proof and canonical-map mutation guard. No generator output, accepted source/candidate, production ownership, rollback content, or Android output changed. Progress is 102/149 (68%). |
| 2026-07-25 | Phase 2 generated feature-kind semantic coverage implementation | `DenseCityBuildingPlacementIntegrationValidation`; `DenseCityPresentationHierarchyContextTests.GeneratedFeatureKinds_RouteToTheirExplicitPresentationOwner`; Unity Roslyn compile using current `Game.Tests.Editor.rsp`; `git diff --check` | Implementation compile-clean; checklist item remains open pending wrapper-driven focused Unity execution | Extended the existing full-map generation fixture to inspect its real immutable record snapshot and require nonzero, correctly typed output for two building kinds, 18 surface kinds, and 36 presentation kinds spanning ordinary/civic buildings, foundations/blockers, roads/shoulders/patches, canal/bridge/park output, attachments, utilities, courtyard/natural details, vegetation, props, and horizon. Extended the semantic hierarchy fixture to route every presentation feature kind through the production `DenseCityPresentationHierarchyContext` and assert its exact owner bucket, including declared-building attachment ownership. Unity's current Roslyn response file compiles the complete `Game.Tests.Editor` assembly with zero errors, and `git diff --check` passes. The mandated macOS wrapper cannot execute on this Windows workspace, so no focused Unity result is claimed and progress remains 102/149 (68%). No production generator, accepted source/candidate, Addressables ownership, rollback content, or Android output changed. |
| 2026-07-25 | Phase 3 strip-before-parent generated primitive implementation | `DenseCityPhysicsComponentStripper.CreatePrimitiveWithoutPhysics`; `RuntimeCityRAndDEditModeBuilder.CreateRoadSegment`; `DenseCityPhysicsComponentStripperTests.CreatePrimitiveWithoutPhysics_ReturnsUnparentedColliderFreePrimitive`; Unity Roslyn compile of `Game.Editor` and `Game.Tests.Editor`; direct production primitive-constructor inventory; `git diff --check` | Implementation compile-clean; primitive checklist item remains open pending wrapper-driven focused Unity execution | Added an exception-safe primitive factory to the approved instance-only stripper. It creates the Unity primitive, removes every prohibited physics component before returning control to a caller, and destroys the partial object if stripping fails. The deterministic editor road builder now obtains both road and shoulder cubes exclusively through that factory, so neither object can be parented, materially configured, or otherwise published while its automatic collider exists; its old late single-collider cleanup was removed. Focused coverage requires an unparented mesh/renderer primitive with zero active or inactive 2D/3D colliders and Rigidbodies. Production inventory leaves `GameObject.CreatePrimitive` only inside the guarded factory for this generation lane. Unity Roslyn compiles both affected assemblies with zero errors and `git diff --check` passes. The mandated macOS wrapper cannot execute on this Windows workspace, so progress remains 102/149 (68%) and no focused Unity pass is claimed. No accepted source/candidate, production Addressables, frozen rollback content, or Android output changed. |
| 2026-07-25 | Phase 3 direct prefab strip-before-return implementation | `DenseCityPhysicsComponentStripper.InstantiatePrefabWithoutPhysics`; 16 direct `DenseMiddleEasternCityEditModeBuilder` sites; `DenseCityBuildingPresentationRealizer`; `DenseCityRenderOnlyPresentationRealizer`; `DenseCityPhysicsComponentStripperTests.InstantiatePrefabWithoutPhysics_ParentsStrippedInstanceWithoutMutatingPrefab`; Unity Roslyn compile of `Game.Editor` and `Game.Tests.Editor`; dense-editor prefab-constructor inventory; `git diff --check` | Direct prefab paths compile-clean; broad immediate-strip checklist item remains open | Added an exception-safe prefab factory to the approved instance-only stripper. It validates prefab/parent inputs, instantiates beneath the requested owner, recursively removes all active/inactive 2D/3D colliders and Rigidbodies before returning control, and destroys the partial instance on rejection. All 16 direct giant-dense generator prefab calls plus building and render-only replay's three prefab calls now use it; direct inventory leaves `PrefabUtility.InstantiatePrefab` only inside the factory. Focused coverage verifies exact parent ownership, four removed-component overrides, complete recursive physics removal, and unchanged persistent prefab contents. Unity Roslyn compiles both affected assemblies with zero errors and `git diff --check` passes. The separate runtime-helper-backed building realization still clones prefabs indirectly and requires its own editor-boundary treatment; the mandated macOS wrapper also cannot execute in this Windows workspace. Therefore no focused Unity pass is claimed, the broad checkbox remains open, and progress stays 102/149 (68%). No accepted source/candidate, production Addressables, frozen rollback content, or Android output changed. |
| 2026-07-25 | Phase 3 indirect building immediate-strip and full-root gate | `DenseMiddleEasternCityEditModeBuilder.SpawnBuilding`; `DenseCityBuildingPlacementIntegrationValidation`; Unity Roslyn compile of `Game.Editor` and `Game.Tests.Editor`; dense-generation instantiation inventory; `git diff --check` | Immediate-strip implementation inventory complete; checklist item remains open pending wrapper-driven full generation | Closed the remaining indirect editor-generation gap without adding an editor dependency to `Game.Runtime`: immediately after the nullable `SpawnVisualOnlyPrefab` result, the dense editor builder recursively strips its complete wrapper hierarchy before applying realization transforms, calculating bounds, reserving placement, creating foundations, or accepting semantic records. The existing full-map integration entry point now also calls `DenseCityPhysicsComponentStripper.TryValidateNoProhibitedComponents` on the complete generated root, including inactive descendants, so any missed direct or indirect path fails closed. Combined inventory now covers the guarded primitive factory, guarded prefab factory, 16 direct giant-dense sites, three record-replay sites, and the single runtime-helper-backed building site. Unity Roslyn compiles both affected assemblies with zero errors and `git diff --check` passes. The mandated macOS wrapper cannot execute in this Windows workspace, so no full-generation or focused stripper pass is claimed, both immediate-strip/primitive acceptance items remain open, and progress stays 102/149 (68%). No accepted source/candidate, production Addressables, frozen rollback content, or Android output changed. |

| 2026-07-25 | Windows dense feature semantics and collider-free acceptance | Windows resolver `C:\Users\zfoul\AppData\Local\Temp\warline-unity-resolve-windows.log`; stripper `warline-dense-city-windows-physics-stripper.log` 6/6; visual-blocker transaction `warline-dense-city-windows-visual-blocker-contract.log` 9/9; full map `warline-dense-city-windows-building-integration-final.log`; feature-owner XML `warline-dense-city-windows-feature-owner.xml` 1/1 in `3.79 s`; `git diff --check` | Passed; Phase 2 complete and Phase 3 focused stripping items accepted | Fixed Windows Editor resolution to ignore absent roots while retaining `D:\Program Files` on Jenkins hosts where it exists. The first full-map run correctly rejected mixed urban-rock categories; visual-blocker inputs now carry an explicit category and the record-set gate permits only infrastructure or prop, with vegetation rejection covered. The final full-map run passed at 5,371 semantic buildings, 33,536 surfaces, 42,730 presentations, 25 urban rocks, and zero prohibited generated-root physics while preserving accepted scene bytes. The focused hierarchy test passed exact owner routing, and the stripper suite passed recursive prefab/primitive removal and persistent prefab non-mutation. No accepted source/candidate, production Addressables, frozen rollback content, or Android output changed. Progress is 105/149 (70%). |
| 2026-07-25 | Dense-city author workflow documentation | `Design/Architecture/dense_city_author_workflow.md`; source-code menu/path/report reference audit; tracker checkbox count `106/149`; `git diff --check` | Passed documentation step; device and production-cutover acceptance remain open | Documented the exact candidate-only override, regeneration, validation, Bake All, report-review, device-test, rejection, rollback, and commit flow. The workflow explicitly prevents saving disposable previews over accepted assets, identifies the candidate transaction as the acceptance path, preserves production `StaticSceneChunks`, and does not claim Android evidence that has not run. Progress is 106/149 (71%). |
| 2026-07-25 | Existing-map authored ECS and rollback workflow documentation | `Design/Architecture/operation_map_authored_ecs_workflow.md`; source-code menu/type/path reference audit; tracker checkbox count `107/149`; `git diff --check` | Passed documentation step; production cutover and static cleanup remain open | Documented the accepted source, candidate, and runtime boundaries; three presentation roles; proxy ownership; existing building, vehicle, and render-only authoring rules; exact migration/validation/Bake All sequence; frozen static rollback root; cutover prerequisites; and candidate versus committed-production rollback. The workflow does not mutate or authorize mutation of accepted sources, production Addressables, or rollback artifacts. Progress is 107/149 (72%). |
| 2026-07-25 | Generated-versus-authored ownership documentation | `Design/Architecture/dense_city_generated_authored_ownership.md`; source-code root/type/enum reference audit; tracker checkbox count `108/149`; `git diff --check` | Passed documentation step | Documented the persistent authored, disposable generated, derived evidence, candidate, and frozen rollback ownership classes; exact two-root hierarchy; authored override contract; and safe promotion paths for procedural, exclusion, visual, building, vehicle, and anchor edits. The document rejects name inference, collider exclusions, candidate-as-source edits, report edits, rollback mutation, and unauthorized production cutover. Progress is 108/149 (72%). |
| 2026-07-25 | Generated-output owner and rollback documentation | `Design/Architecture/dense_city_generated_output_ownership.md`; source-code producer/path/transaction audit; tracker checkbox count `109/149`; `git diff --check` | Passed documentation step | Assigned the candidate scene/root, proxy, material, definition/binding/layout, isolated runtime-content, evidence, transient, and frozen-static families to exact producers and rollback paths. The contract explicitly records the surface-proxy and runtime-content journals, candidate file transactions, protected production snapshots, and the weaker version-control rollback required for candidate shared materials and legacy support folders that have no complete transaction journal. Progress is 109/149 (73%). |
| 2026-07-25 | Parent operation-map tracker evidence synchronization | `Design/Architecture/operation_map_scene_split_and_generator_tracker.md`; dense tracker evidence/production-boundary audit; tracker checkbox count `110/149`; `git diff --check` | Passed documentation step | Added a parent-facing candidate-only dense-city evidence snapshot and validation-log row. It records the accepted ECS presentation, semantic/proxy, isolated packaging, parity, and ECS-only building proof while preserving the parent’s completion counts and explicit production boundary: `StaticSceneChunks`, production Addressables, frozen static rollback, and Android/package/performance/static-retirement gates remain unchanged/open. Progress is 110/149 (74%). |
| 2026-07-25 | EntityScene/dense-city architecture and naming guardrails | `Design/Architecture/gameplay_solid_ecs_contract.md`; `Design/Architecture/file_naming_architecture_contract.md`; 77-file added-source/meta audit; bare-system and broad-shell declaration audit; tracker checkbox count `111/149`; `git diff --check` | Passed documentation and static-contract step; no new Unity result claimed | Scoped the older managed map-building conversion rule to `StaticSceneChunks` and added the ECS-only `EntityScene` boundary for authoring, unmanaged data, Burst building destruction, baking-only material projection, composition/readiness, and editor-owned generation/packaging/evidence. All 77 audited added dense/operation-map C# files have tracked `.meta` files and zero product-prefix violations; the only two bare `*System` declarations are real `ISystem` implementations, and no new broad-shell declaration exists. A separate Windows wrapper attempt had not reached the test body while Unity retried its existing Hub licensing connection, so this documentation step does not claim a new Unity pass. Production cutover and Android acceptance remain open. Progress is 111/149 (74%). |

| 2026-07-25 | Phase 5 proxy-failure accepted-output rollback proof | `DenseCityCandidateAuthoringTransactionTests.ProxyFailure_RestoresAcceptedOutputAndRemovesPartialReplacement`; Unity Roslyn compile of `Game.Editor` plus the focused test using the current response file; `git diff --check`; Windows wrapper log `%TEMP%\warline-dense-guardrail-assembly.log` | Implementation compile-clean; focused Unity execution not reached; umbrella failure-test item remains open | Exposed only the existing internal proxy-folder journal helpers to the Editor test assembly and added a focused failure injection after a partial proxy replacement is imported. The proof requires exact accepted folder/file GUID and byte restoration, deletion of the partial replacement, and deletion of the transaction backup. The Editor and focused test compile together with zero errors (three unchanged obsolete-API warnings). The already-running wrapper-owned architecture validation never reached its test body because Unity repeatedly failed to reconnect to the existing Hub licensing client; after 900 seconds the wrapper timed out and Windows denied its attempt to terminate only PID 3540, which still owns the project, so no second Unity process was started and no Unity pass is claimed. Readiness, surface, presentation, and budget failure cases remain open; progress stays 111/149 (74%). No accepted source/candidate, production Addressables, frozen rollback content, Jenkins path resolution, or Android output changed. |
| 2026-07-25 | Phase 5 surface-failure accepted-record rollback proof | `DenseCityInfrastructurePlacementTransactionTests.SurfaceTransaction_FailurePreservesAcceptedOutput`; Unity Roslyn compile of `Game.Editor` plus the focused test using the current response file; `git diff --check` | Implementation compile-clean; focused Unity execution blocked by the existing wrapper-owned process; umbrella failure-test item remains open | Extended the existing surface transaction suite with an accepted-state regression instead of another empty-state cleanup test. The fixture commits one accepted terrain record, injects an exception while realizing a later ramp, and then proves the exact accepted stable identity, polygon, elevation, movement mask, layer, and chunk survive while the rejected record is absent. The Editor and focused test compile together with zero errors (three unchanged obsolete-API warnings). PID 3540 from the timed-out wrapper still owns the project, so no second Unity process was started and no Unity pass is claimed. Readiness, presentation, and budget failure cases remain open; progress stays 111/149 (74%). No source/candidate scene, generated proxy asset, production Addressables, frozen rollback content, Jenkins path resolution, or Android output changed. |
| 2026-07-25 | Phase 5 presentation-failure accepted-hierarchy rollback proof | `DenseCityPresentationReplayTransactionTests.Realize_LateFailurePreservesAcceptedAndRollsBackNewPresentationSet`; focused Unity Roslyn compile against the compile-clean `Game.Editor` reference; `git diff --check` | Focused test compile-clean; wrapper-driven Unity execution not reached; umbrella failure-test item remains open | Strengthened the existing late render-only material-failure fixture by seeding accepted building and prop presentation objects before replay. After the injected later failure, the test requires those exact accepted objects to remain as the only children while the transaction removes every building and render-only object it created. The focused test compiles with zero errors. The previously timed-out wrapper-owned Unity process later resumed compilation but failed broadly on unavailable UI/TMP and generated assembly references, returned to licensing reconnect attempts, and still owns the project; no second Unity process was started and no Unity pass is claimed. Readiness and budget failure cases remain open; progress stays 111/149 (74%). No source/candidate scene, generated asset, production Addressables, frozen rollback content, Jenkins path resolution, or Android output changed. |
| 2026-07-25 | Phase 5 readiness-failure accepted-hierarchy immutability proof | `OperationMapEntityPresentationReadinessValidatorTests.ReadinessFailure_DoesNotMutateAcceptedHierarchy`; focused Unity Roslyn compile against the compile-clean `Game.Editor` reference; `git diff --check` | Focused test compile-clean; wrapper-driven Unity execution not reached; umbrella failure-test item remains open | Extended the existing readiness suite with an accepted-state fingerprint around a forced expected-count rejection. The fingerprint recursively covers names, active state, invariant local transforms, component types, serialized authoring data, and child ordering before and after the validator, proving rejection is read-only for the accepted hierarchy. The focused test compiles with zero errors. The wrapper-owned Unity process still owns the project after its timeout/recovery failure, so no second Unity process was started and no Unity pass is claimed. The budget failure case remains open; progress stays 111/149 (74%). No source/candidate scene, generated asset, production Addressables, frozen rollback content, Jenkins path resolution, or Android output changed. |
| 2026-07-25 | Phase 5 budget-failure accepted-output rollback proof | `DenseCityPresentationBudgetValidatorTests.BudgetFailure_RestoresAcceptedCandidateOutput`; focused Unity Roslyn compile against the compile-clean `Game.Editor` reference; `git diff --check` | Focused test compile-clean; wrapper-driven Unity execution not reached; umbrella failure-test item remains open | Added the final named failure category without weakening the budget gate. The fixture mutates an accepted candidate output and creates a partial output, rejects incomplete geometry through `TryCreateReport`, then executes the same candidate file transaction used by Candidate Bake All and requires byte-for-byte restoration plus deletion of the partial output. The focused test compiles with zero errors. PID 3540 still owns the project after the prior wrapper timeout/recovery failure, so no second Unity process was started and no Unity pass is claimed. All readiness, proxy, surface, presentation, and budget implementation cases now exist; the umbrella remains pending wrapper-driven execution. Progress stays 111/149 (74%). No source/candidate scene, production Addressables, frozen rollback content, Jenkins path resolution, or Android output changed. |
| 2026-07-25 | Phase 5 transactional two-run no-op harness and nested transform precision repair | `OperationMapEntitySceneCandidateBakeAllTests.RunTwoRunNoOpValidation`; `DenseCityRenderOnlyPresentationRealizer.ApplyWorldMatrix`; `DenseCityRenderOnlyPresentationRealizerTests.RunWorldMatrixPrecisionValidation`; Unity Roslyn response-file compile of `Game.Editor` and `Game.Tests.Editor`; Windows wrapper `%TEMP%\warline-dense-world-matrix-precision-2.log`; full attempts `%TEMP%\warline-dense-city-two-run-no-op.log` and `%TEMP%\warline-dense-city-two-run-no-op-2.log`; `git diff --check` | Harness and rollback implementation compile-clean; focused precision regression passed; two-run item remains open on a fail-closed semantic-count discrepancy | Added a separate real-workflow entry point that runs dense realization, direct dense ECS bake, dense layout, and complete existing-candidate Bake All twice, hashes stable candidate-owned files/directories with streaming SHA-256, and restores them from an outer disk checkpoint on any exception or mismatch. The first run rejected a nested attachment whose replayed world Z exceeded the unchanged relative/absolute tolerance by roughly three float ULPs; a bounded parent-space residual correction now removes that accumulation and its focused wrapper regression passes. The next full run completed realization at 4,971 generated buildings, 30,886 render-only owners, 32,845 surfaces, and 2,468 proxy partitions, then correctly rejected the direct bake because accepted evidence requires 31,975 render-only identities/36,946 total. The checkpoint restored accepted candidate assets; no two-run pass or count-baseline change is claimed. Progress remains 111/149 (74%). Production Addressables, accepted sources, frozen rollback content, Jenkins path resolution, and Android outputs were not changed. |
| 2026-07-25 | Phase 5 fresh-versus-accepted identity-delta diagnosis | `OperationMapEntitySceneCandidateBakeAllTests.RunFreshIdentityDeltaValidation`; `Design/AgentReports/2026-07-25_dense_city_fresh_identity_delta.json`; Windows wrapper `%TEMP%\warline-dense-fresh-identity-delta.log`; Unity Roslyn response-file compile of `Game.Tests.Editor`; `git diff --check` | Passed diagnostic; two-run item remains open and accepted counts remain frozen | Added a transactional diagnostic that snapshots accepted stable identities, realizes a fresh candidate, emits deterministic grouped and per-owner differences, and restores every candidate-owned file/directory through the existing outer checkpoint. The wrapper run passed and captured 36,946 accepted, 35,857 fresh, and 35,804 shared stable IDs. The full 1,089 net loss is one group: `Infrastructure` owners using `SM_Env_Ground_Round_01.prefab`. The remaining symmetric 53/53 delta contains the same horizon prefab groups under shifted sequence names/stable IDs, downstream of the missing ground records. No count gate was weakened and no accepted candidate, source, production Addressables, frozen rollback content, Jenkins path resolution, or Android output changed. Progress remains 111/149 (74%). |
| 2026-07-25 | Phase 5 five-category accepted-output rollback proof | `OperationMapEntitySceneCandidateBakeAllTests.RunFailureRollbackValidation`; Windows wrapper `%TEMP%\warline-dense-bake-all-failure-rollback-3.log`; Unity Roslyn response-file compile of `Game.Tests.Editor`; `git diff --check` | Passed all five failure categories; checklist item accepted | One focused entry point now runs the exact proxy, surface, presentation, readiness, and budget rejection proofs in a single Editor session. The first diagnostic run exposed that the readiness fixture's nominally valid hierarchy omitted the production-required readiness counts; the fixture now supplies 1/1/1/0 before forcing the intended count mismatch. The accepted run reached `[DenseCityBakeAllFailureRollbackValidation] result=Passed categories=5`, proving seeded accepted folders/files, semantic records, hierarchy objects, and serialized hierarchy state survive while partial replacements are removed. No production gate was weakened and no accepted candidate/source, production Addressables, frozen rollback content, Jenkins path resolution, or Android output changed. Progress is 112/149 (75%). |
| 2026-07-25 | Phase 5 open-ground blocker-bounds causality probe | Temporary `DenseMiddleEasternCityEditModeBuilder.CollectGeneratedBuildingFootprints` substitution; Windows wrapper `%TEMP%\warline-dense-fresh-identity-blocker-bounds.log`; restored `Design/AgentReports/2026-07-25_dense_city_fresh_identity_delta.json`; Unity Roslyn response-file compile of `Game.Editor` and `Game.Tests.Editor`; `git diff --check` | Probe passed transactionally and rejected the proposed repair; two-run item remains open | Replaced realized renderer bounds with immutable building-record blocker bounds for one checkpointed fresh realization. The run completed at 2,770 open-ground patches, 32,834 surfaces, 43,153 presentation records, and 35,846 fresh identities versus 36,946 accepted (`acceptedOnly=1,153`, `freshOnly=53`). This moves eleven owners farther from the current 2,781-patch/35,857-identity result, proving the blocker record is a more conservative exclusion and cannot restore the frozen baseline. The transaction restored all candidate outputs; the source experiment and regenerated report were reverted to their exact HEAD hashes. No checklist count changed (112/149, 75%), and no accepted source/candidate, production Addressables, frozen rollback content, Jenkins path resolution, or Android output changed. |
| 2026-07-25 | Phase 6 dense ECS optimization-metric publication | `OperationMapEntityPresentationCandidateBakeValidator.ValidateDenseBakedWorld`; `Design/AgentReports/2026-07-24_dense_city_generated_candidate_bake_validation.json`; Windows wrapper `%TEMP%\warline-dense-optimization-metrics.log`; Unity Roslyn response-file compile of `Game.Editor`; `git diff --check` | Dense in-memory bake and new fail-closed metrics passed; entity-scene-byte sub-gate remains open | Extended the accepted dense report and rejection chain with archetype, render-child, shared render-array/mesh/material, and intact/destroyed visual-root ownership metrics. The wrapper bake passed at 118,274 entities, 78 archetypes, 4,692 chunks, 82,797 render entities, 20,390 render children, one shared render-array identity, 1,769 mesh identities, 80 material identities, 5,403 intact roots, 5,237 destroyed roots, 166 intentional missing destroyed variants, zero missing intact roots, and zero shared intact/destroyed roots. Unity rewrote ten generated material floats from `0.97` to its binary-equivalent serialized spelling; those wrapper-caused material changes were restored and excluded. Exact packed entity-scene bytes remain unreported, so the checklist item and progress remain 112/149 (75%). No candidate/source scene, production Addressables, frozen rollback content, Jenkins path resolution, or Android output changed. |
| 2026-07-25 | Phase 6 exact EntityScene archive-byte reporting foundation | `OperationMapDenseCityCandidateRuntimeContentBuilder.MeasureEntityContent`; `OperationMapDenseCityCandidateRuntimeContentBuilderTests.RunFocusedValidation`; Windows wrapper `%TEMP%\warline-dense-runtime-content-byte-inventory-host.log` 3/3; Unity Roslyn response-file compile of `Game.Editor` and `Game.Tests.Editor`; `git diff --check` | Implementation and focused byte inventory passed; accepted exact byte value remains open for the next macOS content rebuild | Replaced the ambiguous whole-directory-only metric with schema-2 fields that separately report the sole requested EntityScene archive payload, catalog/metadata overhead, and their total. The build now rejects any result without exactly one nonempty archive, and focused fixtures prove exact byte partitioning, missing-catalog rejection, and multi-archive detection. The first Windows wrapper attempt could not access the host licensing client and its own timeout cleanup was denied by the sandbox; only that wrapper-owned PID tree was then terminated, and the same checked-in wrapper passed with host access. No wrapper, Jenkins path setting, accepted report, candidate/source asset, production Addressables, frozen rollback content, or Android output changed. Because the prior macOS output is absent locally, the existing schema-1 `190463113` total was not mislabeled as archive bytes; progress remains 112/149 (75%). |
| 2026-07-25 | Phase 6 frozen rollback byte inventory | `OperationMapDenseCityCandidateRuntimeContentBuilder.MeasureFrozenRollbackContent`; `OperationMapDenseCityCandidateRuntimeContentBuilderTests.RunFocusedValidation`; Windows wrappers `%TEMP%\warline-dense-frozen-rollback-byte-inventory.log` 6/6 and `%TEMP%\warline-dense-frozen-rollback-byte-report.log`; `Design/AgentReports/2026-07-25_dense_city_frozen_rollback_byte_inventory.json`; Unity Roslyn response-file compile of `Game.Editor` and `Game.Tests.Editor`; `git diff --check` | Exact frozen manifest/chunk bytes passed; combined packed byte/count item remains open | Added a read-only deterministic inventory that includes only the frozen manifest payload and top-level `.unity` chunk payloads, excluding `.meta`, integrity-ledger, and minimap bytes. The accepted package reports a 10,097,753-byte manifest and 269 chunks totaling 32,381,589 bytes. Missing/empty manifest or chunk sets fail closed, fixture coverage proves `.meta` exclusion and exact partitioning, and the current accepted package is locked to the reported values. Schema 2 of the next isolated runtime-content build will carry the same fields. No frozen file, accepted source/candidate, production Addressables, Jenkins path resolution, or Android output changed. Exact packed EntityScene/shared/duplicate bytes and production zero-count proof remain open, so progress stays 112/149 (75%). |
| 2026-07-25 | Phase 6 production static Addressables count baseline and cutover gate | `OperationMapDenseCityCandidateRuntimeContentBuilder.MeasureProductionStaticAddressables`; `OperationMapDenseCityCandidateRuntimeContentBuilderTests.RunFocusedValidation`; Windows wrapper `%TEMP%\warline-dense-production-static-counts-tests-2.log` 9/9; report wrapper `%TEMP%\warline-dense-production-static-counts-report-schema2.log`; updated `Design/AgentReports/2026-07-25_dense_city_frozen_rollback_byte_inventory.json`; Unity Roslyn response-file compile of `Game.Editor` and `Game.Tests.Editor`; `git diff --check` | Pre-cutover baseline and fail-closed post-cutover gate passed; zero-count acceptance remains pending authorized cutover | The schema-2 report records production `StaticSceneChunks`, one static-manifest entry, 269 static presentation chunk entries, required post-cutover counts of zero/zero, `postCutoverZeroCountsSatisfied=0`, and `productionCutover=0`. Pure fixtures accept this explicit legacy baseline, reject an `EntityScene` production definition retaining even one retired static entry, and accept zero/zero after cutover. The first focused wrapper run emitted the 9/9 marker but crashed natively during shutdown, so it was not accepted; the identical second wrapper completed successfully. No Addressables setting, definition, frozen rollback asset, accepted source/candidate, Jenkins path resolution, or Android output changed. Exact packed EntityScene/shared/duplicate bytes and actual post-cutover zero evidence remain open, so progress stays 112/149 (75%). |
| 2026-07-25 | Phase 6 packed shared/duplicate dependency byte accounting foundation | `OperationMapDenseCityCandidateRuntimeContentBuilder.MeasurePackedDependencyBytes`; `OperationMapDenseCityCandidateRuntimeContentBuilderTests.RunFocusedValidation`; Windows wrapper `%TEMP%\warline-dense-packed-dependency-byte-accounting.log` 12/12; Unity Roslyn response-file compile of `Game.Editor` and `Game.Tests.Editor`; `git diff --check` | Deterministic accounting contract passed; accepted packed values remain pending macOS Build Layout evidence | Added a pure reducer over Build Layout-style GUID/bundle byte occurrences. It counts each physical GUID once per bundle, sums requested shared-dependency bytes, and reports only excess physical copies beyond the largest retained copy as duplicated bytes. Fixtures prove exact shared/duplicate totals, same-bundle row deduplication, shared-GUID list deduplication, and missing-shared-evidence rejection. The Windows workspace has no accepted dense Addressables Build Layout or packed output, so no zero/value claim is made for the real package and progress remains 112/149 (75%). No build settings, Addressables data, accepted source/candidate, frozen rollback asset, Jenkins path resolution, or Android output changed. |
| 2026-07-25 | Phase 6 Addressables Build Layout byte adapter | `OperationMapDenseCityCandidateRuntimeContentBuilder.MeasurePackedDependencyBytes(BuildLayout, ...)`; `OperationMapDenseCityCandidateRuntimeContentBuilderTests.MeasurePackedDependencyBytes_AdaptsAddressablesBuildLayout`; Windows wrapper `%TEMP%\warline-dense-build-layout-byte-adapter.log` 13/13; Unity Roslyn response-file compile of `Game.Editor` and `Game.Tests.Editor`; `git diff --check` | Build Layout adapter passed; accepted packed values remain pending macOS capture | Added a direct adapter from Unity Addressables `BuildLayout` to the accepted GUID/bundle byte reducer. It includes explicit assets, file-owned implicit assets, and implicit assets referenced by explicit owners, combining serialized and streamed bytes while ignoring zero-byte bookkeeping rows. A synthetic layout with one shared explicit dependency and one implicit dependency copied across two bundles reports the exact expected 100 shared bytes and 25 excess duplicate bytes. No build-layout preference, build output, Addressables setting, accepted source/candidate, frozen rollback asset, Jenkins path resolution, or Android output changed. Progress remains 112/149 (75%). |
| 2026-07-25 | Phase 6 transactional dense Build Layout capture contract | `OperationMapDenseCityCandidateRuntimeContentBuilder.BuildLayoutCaptureScope`; `SelectSingleGeneratedBuildLayoutPath`; `DenseRuntimeContentOutputTransaction`; `OperationMapDenseCityCandidateRuntimeContentBuilderTests.RunFocusedValidation`; Windows wrapper `%TEMP%\warline-dense-build-layout-capture-contract.log` 16/16; Unity Roslyn response-file compile of `Game.Editor` and `Game.Tests.Editor`; `git diff --check` | Capture/publication implementation and selection gates passed; real packed metrics await macOS build | The isolated builder now scopes JSON Build Layout generation around its temporary Addressables build, requires exactly one new readable report, restores the user's prior report list, format, and enabled preference, deletes only the newly generated global report, and copies the accepted report into a separately transaction-owned dense output. Schema 4 publishes its SHA-256 plus shared-dependency GUID/bytes and duplicated-dependency GUID/excess bytes, and rejects a packed shared-GUID count that differs from the plan. Fixtures reject missing and ambiguous reports. Windows cannot run the hard-coded StandaloneOSX content build, so no real packed values or checklist completion are claimed; progress remains 112/149 (75%). No preference, production setting, accepted source/candidate, frozen rollback asset, Jenkins path resolution, or Android output changed. |

| 2026-07-25 | Phase 6 complete candidate EntityScene visual ownership | `DenseCityPresentationBudgetValidator.TryCreateDenseOwnershipReport`; `DenseCityPresentationBudgetValidatorTests.RunFocusedValidation`; Windows wrappers `%TEMP%\warline-dense-entityscene-ownership-tests.log` 11/11 and `%TEMP%\warline-dense-entityscene-ownership-live.log`; `Design/AgentReports/2026-07-25_dense_city_entity_scene_ownership.json`; Unity Roslyn response-file compile of `Game.Editor` and `Game.Tests.Editor`; `git diff --check` | Passed candidate ownership; production cutover remains separate | Added one fail-closed aggregate over the accepted existing-map parity, dense generated parity, and exact five-entry dense candidate layout. The live report proves 9,544 existing visual identities with 14,249 baked render entities, 36,946 generated identities with 68,735 candidate and baked renderer entities, matching generated identity-set hashes, and zero missing, unexpected, duplicate, unresolved, or unconsumed generated ownership. It also records zero managed map-visual companions, authoring-scene entries, explicit shared owners, static presentation entries, and legacy placement entries. The candidate allowlist is exactly definition, renderer-free runtime binding, sole dense EntityScene, map-surface data, and minimap raster. Production remains `StaticSceneChunks`; no source/candidate scene, production Addressables, frozen rollback asset, Jenkins path resolution, or Android output changed. Progress is 113/149 (76%). |

| 2026-07-25 | Phase 6 semantic-family shared render references | `OperationMapDenseCityGeneratedTransformParityValidator.TryValidateRequiredCategorySharing`; `DenseCityPresentationBudgetValidatorTests.RunFocusedValidation`; accepted Windows wrapper `%TEMP%\warline-dense-category-sharing-tests-nographics-2.log` 14/14; live dense bake `%TEMP%\warline-dense-category-sharing-live.log`; `Design/AgentReports/2026-07-24_dense_city_generated_transform_parity.json`; Unity Roslyn response-file compile of `Game.Editor` and `Game.Tests.Editor`; `git diff --check` | Passed; packed binary duplication remains separate | Schema 3 of the dense parity report groups renderer signatures by the serialized semantic category already assigned at generation time. The live bake reports 18,364 repeated building entries across 263 repeated signatures, 39,005 road/bridge/infrastructure entries across 81, 5,423 tree/vegetation entries across 10, and 5,876 prop entries across 16, with every repeated signature resolving to one baked mesh/material pair. Two initial focused runs reached 14/14 but crashed natively during graphics shutdown and were rejected; one malformed low-level argument invocation timed out and its wrapper cleaned only its own process tree. The corrected `-nographics` wrapper then passed cleanly, as did the live bake. Ten wrapper-caused material float-spelling rewrites were restored; the parity report and its dependent runtime-manifest hashes were retained. No accepted scene, production Addressables, frozen rollback asset, Jenkins path resolution, or Android output changed. Progress is 114/149 (77%). |

| 2026-07-25 | Phase 6 bounded mesh-combination policy | `OperationMapDenseCityGeneratedTransformParityValidator.TryValidateMeshCombinationPolicy`; `DenseCityPresentationBudgetValidatorTests.RunFocusedValidation`; accepted Windows wrapper `%TEMP%\warline-dense-mesh-combination-tests-final.log` 17/17; live dense bake `%TEMP%\warline-dense-mesh-combination-live.log`; `Design/AgentReports/2026-07-24_dense_city_generated_transform_parity.json`; Unity Roslyn response-file compile of `Game.Editor` and `Game.Tests.Editor`; `git diff --check` | Passed; no continuous terrain/road combination is currently authorized | Schema 4 measures mesh-backed renderer world bounds against the aggregate generated candidate and fails closed on non-building mesh-backed geometry, any infrastructure/road/bridge combination without measured authorization, or a mesh spanning at least 50% of both horizontal axes. The accepted candidate spans 5,195.93 × 2,103.09 m; its largest mesh-backed ratios are 2.19% × 5.41%. All 18,375 mesh-backed renderer entries are existing building geometry, while infrastructure/road/bridge, vegetation, prop, and horizon counts are zero, preserving the already-proven repeated module sharing. The first focused run reached 17/17 but crashed natively during shutdown and was rejected; the identical second `-nographics` run, final refined-policy run, and live bake exited cleanly. Ten wrapper-caused material float-spelling rewrites were restored; the parity report and dependent runtime-manifest hashes were retained. No accepted scene, production Addressables, frozen rollback asset, Jenkins path resolution, or Android output changed. Progress is 115/149 (77%). |

| 2026-07-25 | Phase 7 explicit source-hierarchy and player-build-scene exclusion | `OperationMapDenseCityCandidateRuntimeContentBuilder.MeasureSourceHierarchyExclusion`; isolated Addressables pre-build fail-closed gate; `OperationMapDenseCityCandidateRuntimeContentBuilderTests.RunFocusedValidation`; accepted Windows wrapper `%TEMP%\warline-dense-source-hierarchy-exclusion-tests.log` 19/19; live report wrapper `%TEMP%\warline-dense-source-hierarchy-exclusion-live.log`; `Design/AgentReports/2026-07-25_dense_city_source_hierarchy_exclusion.json`; Unity Roslyn response-file compile of `Game.Editor` and `Game.Tests.Editor`; `git diff --check` | Explicit Addressables and enabled player-build-scene exclusion passed; packed implicit-dependency evidence remains open | The live report records the exact two dense explicit runtime entries, two enabled player build scenes, zero accepted/candidate operation-map or map-SubScene source hierarchy entries, and zero forbidden build scenes. The isolated builder applies the same reducer to the actual temporary Addressables group before content build and rejects a count other than two or any forbidden source path. Fixtures cover the clean contract plus fail-closed detection of a source hierarchy as an explicit entry or enabled build scene. The report deliberately records `packedImplicitDependencyEvidenceComplete=0` and `productionCutover=0`; the checklist row remains open until the accepted macOS Build Layout proves no implicit raw-hierarchy dependency. No build settings, Addressables setting, accepted source/candidate, frozen rollback asset, Jenkins path resolution, or Android output changed. Progress remains 115/149 (77%). |

| 2026-07-25 | Phase 7 offline local-bundle delivery foundation | `OperationMapDenseCityCandidateRuntimeContentBuilder.MeasureLocalBundleDelivery`; isolated Addressables pre-build fail-closed gate; `OperationMapDenseCityCandidateRuntimeContentBuilderTests.RunFocusedValidation`; accepted Windows wrapper `%TEMP%\warline-dense-offline-delivery-tests.log` 23/23; Unity Roslyn response-file compile of `Game.Editor` and `Game.Tests.Editor`; `git diff --check` | Local/offline build contract passed; player-package inclusion and network-disabled runtime acceptance remain open | The isolated dense builder now forces remote catalogs off, startup catalog updates off, the sole dense group included in build, local Addressables build/load variables, and a non-network load URI before content creation. Fixtures fail closed on a remote catalog, excluded group, or HTTPS load path. Two live production-layout report attempts emitted their intended 4/4 local-group marker but crashed natively during Unity shutdown and were rejected; their generated report was removed and no live-layout claim is made. No production Addressables setting, build setting, accepted source/candidate, frozen rollback asset, Jenkins path resolution, or Android output changed. Progress remains 115/149 (77%). |
| 2026-07-25 | Phase 10 generated-artifact ownership repository audit foundation | `Tools/CI/dense_city_generated_artifact_ownership_audit.py`; `Tools/CI/tests/test_dense_city_generated_artifact_ownership_audit.py`; bundled Python focused suite 5/5; `Design/AgentReports/2026-07-25_dense_city_generated_artifact_ownership_audit.json`; `git diff --check` | Repository ownership foundation passed; final artifact-set and Android closeout remain open | The deterministic audit reports six required tracked ownership/candidate paths present, 4,967 tracked persistent candidate-output files, 72 tracked dense-city evidence files, zero tracked transient paths, zero tracked forbidden transaction/temp-owner artifacts, and all eight `.gitignore` probes accepted. Pure fixtures fail closed on a missing required output, tracked Unity cache, tracked transaction backup, or unignored transient probe. The report explicitly records `finalArtifactSetComplete=false` and `androidAcceptanceComplete=false`, so the Phase 10 row and progress remain 115/149 (77%). No Unity process, source/candidate scene, generated production asset, production Addressables setting, frozen rollback content, Jenkins path resolution, or Android output changed. |
| 2026-07-25 | Phase 7 packed source-hierarchy exclusion gate | `OperationMapDenseCityCandidateRuntimeContentBuilder.MeasurePackedSourceHierarchy`; isolated Addressables Build Layout pre-publication gate; `OperationMapDenseCityCandidateRuntimeContentBuilderTests.RunFocusedValidation`; accepted Windows wrapper `%TEMP%\warline-dense-packed-source-hierarchy-tests.log` 26/26; Unity Roslyn response-file compile of `Game.Editor` and `Game.Tests.Editor`; `git diff --check` | Build Layout adapter and fail-closed gate passed; real macOS packed evidence remains open | The schema-5 builder now enumerates explicit and both file-owned and internal implicit Build Layout assets, deduplicates exact asset-path/bundle/ownership tuples, and rejects any accepted/candidate operation-map or map-SubScene source hierarchy before publishing the dense output. Fixtures prove runtime-only assets pass and both explicit and implicit source-scene paths fail through the pure reducer and Unity Addressables adapter. The Windows wrapper compiled cleanly and exited with the required 26/26 marker. No runtime-content build, accepted report, source/candidate scene, production Addressables setting, frozen rollback content, Jenkins path resolution, or Android output changed. The checklist row and progress remain 115/149 (77%) pending the real macOS Build Layout. |
| 2026-07-25 | Phase 6/7 zero cross-bundle dependency-copy gate | `OperationMapDenseCityCandidateRuntimeContentBuilder.RequireNoPackedDependencyDuplication`; isolated Addressables Build Layout pre-publication gate; `OperationMapDenseCityCandidateRuntimeContentBuilderTests.RunFocusedValidation`; accepted Windows wrapper `%TEMP%\warline-dense-packed-duplication-gate-tests.log` 28/28; Unity Roslyn response-file compile of `Game.Editor` and `Game.Tests.Editor`; `git diff --check` | Fail-closed packed duplication contract passed; real macOS values and Entities archive proof remain open | The existing deterministic GUID/bundle byte reducer is now an enforced publication gate: both duplicated GUID count and excess physical bytes must be zero. Repeated Build Layout rows for one GUID inside one physical bundle remain deduplicated, while a synthetic 40-byte/25-byte cross-bundle copy rejects with one duplicated GUID and 25 excess bytes. The Windows wrapper compiled cleanly and exited with the required 28/28 marker. No runtime-content build, accepted report, source/candidate scene, production Addressables setting, frozen rollback content, Jenkins path resolution, or Android output changed. The checklist rows and progress remain 115/149 (77%) pending accepted macOS Build Layout values and the separate Entities archive/section binary proof. |
| 2026-07-25 | Phase 7 published local-content output gate | `OperationMapDenseCityCandidateRuntimeContentBuilder.MeasurePublishedLocalContent`; isolated Addressables post-publication/pre-commit gate; `OperationMapDenseCityCandidateRuntimeContentBuilderTests.RunFocusedValidation`; accepted Windows wrapper `%TEMP%\warline-dense-published-local-content-tests.log` 31/31; Unity Roslyn response-file compile of `Game.Editor` and `Game.Tests.Editor`; `git diff --check` | Transaction-owned local output contract passed; application inclusion and offline launch remain open | After publishing the isolated Addressables output, schema 6 requires a nonempty `catalog.bin` contained by the exact dense output root and one or more nonempty `.bundle` files, then records catalog bytes, bundle count, and bundle bytes. Fixtures prove exact byte accounting and fail closed on an out-of-root catalog or missing bundle set. The Windows wrapper compiled cleanly and exited with the required 31/31 marker. No runtime-content build, accepted report, source/candidate scene, production Addressables setting, frozen rollback content, Jenkins path resolution, or Android output changed. The checklist row and progress remain 115/149 (77%) pending real player-package inclusion and network-disabled runtime validation. |
| 2026-07-25 | Phase 7 deterministic local-bundle-set fingerprint | schema-7 `OperationMapDenseCityCandidateRuntimeContentBuilder.MeasurePublishedLocalContent`; `OperationMapDenseCityCandidateRuntimeContentBuilderTests.RunFocusedValidation`; accepted Windows wrapper `%TEMP%\warline-dense-local-bundle-fingerprint-tests-final.log` 32/32; Unity Roslyn response-file compile of `Game.Editor` and `Game.Tests.Editor`; `git diff --check` | Deterministic fingerprint contract passed; real packed hash remains open | The published local-content result now hashes an unambiguous manifest of each sorted relative bundle path, byte length, and individual SHA-256, so the aggregate is independent of the checkout root. Fixtures prove identical relative bundle sets under two different roots hash equally and a changed bundle changes the aggregate. The first wrapper run emitted 32/32 but crashed during Unity shutdown and was rejected; the final cross-root fixture compiled cleanly and exited with the required marker. No runtime-content build, accepted report, source/candidate scene, production Addressables setting, frozen rollback content, Jenkins path resolution, or Android output changed. The checklist row and progress remain 115/149 (77%) pending the real packed bundle set and final package/device evidence. |
| 2026-07-25 | Phase 6 deterministic EntityScene archive-set fingerprint | schema-8 `OperationMapDenseCityCandidateRuntimeContentBuilder.MeasureEntityContent`; shared relative-file-set SHA-256 reducer; `OperationMapDenseCityCandidateRuntimeContentBuilderTests.RunFocusedValidation`; accepted focused Windows wrapper `%TEMP%\warline-dense-entity-archive-fingerprint-focused.log` 33/33; Unity Roslyn response-file compile of `Game.Editor` and `Game.Tests.Editor`; `git diff --check` | Deterministic archive fingerprint contract passed; real packed hash/bytes remain open | Entity content now hashes an unambiguous manifest of each sorted relative `.archive` path, byte length, and individual SHA-256, reusing the bundle-set reducer. Fixtures prove identical archive sets under two different roots hash equally and archive mutation changes the aggregate. Three low-level Windows wrapper attempts emitted 33/33 but crashed during Unity shutdown and were rejected; the repository's focused Windows execute-method wrapper then compiled cleanly, found the exact 33/33 marker, and exited successfully. No runtime-content build, accepted report, source/candidate scene, production Addressables setting, frozen rollback content, Jenkins path resolution, or Android output changed. The checklist rows and progress remain 115/149 (77%) pending the real packed archive hash/bytes and final package/device evidence. |
| 2026-07-25 | Phase 9 isolated Windows content-target lane | schema-9 `OperationMapDenseCityCandidateRuntimeContentBuilder`; `RequireSupportedValidationBuildTarget`; platform-derived shared Addressables output; `OperationMapDenseCityCandidateRuntimeContentBuilderTests.RunFocusedValidation`; accepted focused Windows wrapper `%TEMP%\warline-dense-windows-content-target-tests.log` 37/37 with `-buildTarget StandaloneWindows64`; Unity Roslyn response-file compile of `Game.Editor` and `Game.Tests.Editor`; `git diff --check` | Windows/macOS target-selection contract passed; real Windows content build is next | The isolated builder now accepts only installed `StandaloneWindows64` or `StandaloneOSX` support, maps their shared Addressables namespaces to `Windows` or `OSX`, passes the active target into Entities content creation, uses target-scoped build/load paths, and records the validation target/platform in schema 9. Unsupported Android/other active targets and missing standalone support fail closed. No Unity executable path is checked in, preserving Jenkins `D:` resolution. The focused Windows wrapper compiled cleanly, found the exact 37/37 marker, and exited successfully. No content build, accepted report, source/candidate scene, production Addressables setting, frozen rollback content, or Android output changed. Progress remains 115/149 (77%); the next bounded step is the real isolated Windows candidate-content build. |
| 2026-07-25 | Phase 9 outer dense layout-output rollback | `OperationMapDenseCityCandidateRuntimeContentBuilder.GetDenseLayoutOutputTransactionPaths`; complete definition/meta/binding/meta/layout-report journal; `OperationMapDenseCityCandidateRuntimeContentBuilderTests.RunFocusedValidation`; accepted focused Windows wrapper `%TEMP%\warline-dense-layout-outer-transaction-tests.log` 38/38; real failure replay `%TEMP%\warline-dense-windows-runtime-content-rollback-probe.log`; `git diff --check` | Outer rollback passed; Windows SBP player configuration remains open | The first real Windows build failed in Addressables SBP before bundle creation because Unity's build extension rejected the current player configuration. That run rewrote only the machine-absolute path in the dense layout report; its exact committed bytes were restored. The runtime-content owner now snapshots the dense definition, definition `.meta`, thin binding, binding `.meta`, and layout report before invoking the layout producer, restores them on layout/planning failure and every later content failure, and performs disk preflight before any layout mutation. The focused wrapper passed 38/38. A real failure replay reproduced the SBP rejection and left the worktree with only the two intended source changes, proving automatic tracked-output rollback. No accepted report/content output, production Addressables setting, frozen rollback content, Jenkins path resolution, or Android output changed. Progress remains 115/149 (77%) pending the Windows Build Settings correction and successful real package. |
| 2026-07-25 | Phase 9 reversible Windows content-build backend scope | `OperationMapDenseCityCandidateRuntimeContentBuilder.SelectTemporaryScriptingBackend`; reflected Windows build-extension preflight; `StandaloneScriptingBackendScope`; `OperationMapDenseCityCandidateRuntimeContentBuilderTests.RunFocusedValidation`; accepted focused Windows wrapper `%TEMP%\warline-dense-windows-backend-scope-tests-final.log` 41/41; accepted restoration wrapper `%TEMP%\warline-dense-windows-backend-restoration-serialized.log`; real packed-content probe `%TEMP%\warline-dense-windows-runtime-content-mono-scope.log`; `git diff --check` | Windows SBP player-configuration blocker passed; production Addressables-directory integrity remains open | The Windows build extension reported the exact rejection `Currently selected scripting backend (IL2CPP) is not installed.` for the local x64 Editor module, whose installed variations are Mono-only. The content-only owner now preserves an already-enabled backend, otherwise permits only that exact Windows64/IL2CPP-missing condition to switch temporarily to Mono, verifies the extension enables Build, and restores IL2CPP in a saved `finally` scope; every other disabled-build reason remains fail-closed. A dedicated wrapper proved the temporary switch and explicit IL2CPP restoration. The real isolated build advanced through Addressables packing and bundle compression instead of failing immediately in SBP, then correctly stopped at the next gate because `Assets/AddressableAssetsData` changed during packing. Its layout/report outputs rolled back. Unity startup/serialization rewrites to `ProjectSettings/ProjectSettings.asset` and `Assets/Settings/PC_RPAsset.asset` were inspected and restored to their clean pre-run bytes. No Unity executable path or drive is serialized, so Jenkins `D:` editor resolution is unchanged. No accepted runtime-content report, production Addressables setting, frozen rollback content, Android output, or checklist acceptance changed. Progress remains 115/149 (77%); the next bounded step is complete production Addressables directory rollback before the integrity assertion. |
| 2026-07-25 | Phase 9 production Addressables directory transaction and accepted Windows packed evidence | `OperationMapDenseCityCandidateRuntimeContentBuilder.CandidateDirectoryTransaction`; complete recursive production-settings byte journal; `OperationMapDenseCityCandidateRuntimeContentBuilderTests.RunFocusedValidation`; accepted focused Windows wrapper `%TEMP%\warline-dense-addressables-directory-transaction-tests-commit.log` 42/42; accepted real build `%TEMP%\warline-dense-windows-runtime-content-directory-transaction.log`; schema-9 `Design/AgentReports/2026-07-24_dense_city_candidate_runtime_content.json`; `git diff --check` | Passed; first complete Windows Addressables/Entities evidence accepted | The isolated owner now snapshots every file below `Assets/AddressableAssetsData`, restores modified and deleted bytes, removes newly created files, refreshes synchronously, and only then runs the existing protected-directory hash assertion. A focused fixture covers modified, deleted, added, nested, and repeated rollback. Its first run emitted 42/42 but crashed after a lingering import worker during shutdown and was rejected; the identical final wrapper run and the final commit-state rerun passed cleanly. The real build then passed all packed source-hierarchy, dependency-duplication, local-publication, single-archive, production-integrity, shared-output-restoration, and backend-restoration gates. Accepted values are four local bundles / 912,043 bundle bytes, zero duplicated GUIDs/bytes, zero packed source-hierarchy explicit/implicit assets, one 137,862,756-byte EntityScene archive, 49,947,710 metadata bytes, and 187,810,466 total Entities bytes. Unity serializer rewrites and the layout report's machine-only path were restored; production Addressables, frozen rollback content, source/candidate assets, Jenkins editor resolution, and Android output were not changed. Four evidence-complete checklist rows closed. Progress is 119/149 (80%). |
| 2026-07-25 | Phase 8 schema-9 Windows dense packed two-cycle and fixed-camera parity | Initial schema diagnostic `%TEMP%\warline-dense-windows-packed-two-cycle.log`; stale-baseline diagnostic `%TEMP%\warline-dense-windows-packed-two-cycle-schema9.xml`; transactional Editor recapture `%TEMP%\warline-dense-windows-editor-fixed-camera-recapture.log`; accepted graphics-enabled PlayMode `%TEMP%\warline-dense-windows-packed-two-cycle-fixed-camera.xml` 1/1 in `159.48 s`; refreshed baseline/runtime reports and 15 PNGs under `Design/AgentReports/2026-07-24_dense_city_*fixed_camera*`; `git diff --check` | Passed partial dependency step; complete-generated-city vehicle and failure/retry coverage remain open | Upgraded the real packed fixture from obsolete runtime-content schema 1 to fail-closed schema 9 evidence checks before package load. The first rerun correctly rejected the stale fixed-camera baseline fingerprint; the checked-in transactional capture owner refreshed five Editor views for candidate SHA-256 `a78eb6d8...fc1` with `82,261` renderers. The final graphics-enabled Windows run loaded the isolated Addressables/Entities package twice, matched `9,544` legacy identities, `36,946` generated identities, and `82,797` render rows, and passed all ten runtime views with zero rejected rows, maximum mean-channel delta `0.0021497`, and maximum non-edge changed-pixel ratio `0.0018816`. Both cycles unloaded completely. Incidental Unity material serialization rewrites were restored; production Addressables, accepted/frozen production assets, Jenkins editor resolution, and Android outputs were unchanged. Progress remains 119/149 (80%). |
| 2026-07-25 | Phase 8 schema-9 Windows dense failure/reset/retry lifecycle | Compile-only rejected invocation `%TEMP%\warline-dense-windows-failure-retry.log`; premature-quit rejected invocation `%TEMP%\warline-dense-windows-failure-retry-final.log`; post-retry matrix diagnostic `%TEMP%\warline-dense-windows-failure-retry-accepted.xml`; accepted focused PlayMode `%TEMP%\warline-dense-windows-failure-retry-counts.xml` 1/1 in `25.00 s`; `git diff --check` | Passed partial dependency step; complete-generated-city vehicle behavior remains open | Added the complete dense package counterpart of the established one-shot readiness-fault lifecycle fixture. The first invocation only compiled after an initial domain reload and emitted no result; a second invocation with explicit `-quit` shut down before the Test Framework and crashed in Unity's lingering import-worker cleanup, so both were rejected. The first real test run proved failure/reset/retry but correctly exposed that a full post-simulation renderer-matrix comparison included one already-advanced animated row; the separate pre-simulation parity gate remains authoritative. The accepted focused gate instead retains the full schema-9 evidence and readiness validations, requires failed root/section destruction, retries the same package with exact `9,544` legacy, `36,946` generated, and `82,797` render counts, and proves complete unload with zero stale scene ownership. No source/candidate scene, production Addressables setting, accepted/frozen production asset, Jenkins path, or Android output changed. Progress remains 119/149 (80%). |
| 2026-07-25 | Phase 0A/8 schema-9 Windows dense authored-vehicle production route | Rejected null-graphics diagnostic `%TEMP%\warline-dense-windows-vehicle.xml`; accepted graphics-enabled PlayMode `%TEMP%\warline-dense-windows-vehicle-graphics.xml` 1/1 in `54.76 s`; `[OperationMapPackedVehicle]` movement marker; `git diff --check` | Passed; complete Editor lifecycle umbrella closed | Added a loaded-content hook to the established packed production-route fixture and used it to require exact complete-city counts before vehicle interaction. The first null-graphics run proved movement but was rejected because Windows emitted an extra invalid-render-target error during Menu return beyond the two established null-device expectations. The graphics-enabled run passed cleanly: a real section-owned authored vehicle resolved a production path from `(862,386)` to `(865,386)`, moved `2.03230166` units, and its baked detailed visual moved the identical distance while source identity, hierarchy, scene requests, section residency, single map root, zero managed map visuals, and idle static streamer remained exact. Normal Menu return released all dense EntityScene, section, and map metadata. Incidental dense material serialization rewrites were restored; no source/candidate scene, production Addressables setting, accepted/frozen production asset, Jenkins path, or Android output changed. Two checklist rows closed. Progress is 121/149 (81%). |
| 2026-07-25 | Phase 6 packed ECS asset-sharing aggregate | `DenseCityPresentationBudgetValidator.TryCreateDensePackedAssetSharingReport`; rejected execute-method shutdown runs `%TEMP%\warline-dense-packed-asset-sharing-focused.log` and `-final.log`; accepted fixture-only EditMode `%TEMP%\warline-dense-packed-asset-sharing-editmode.xml` 20/20; accepted real-evidence EditMode `%TEMP%\warline-dense-packed-asset-sharing-evidence.xml` 21/21; `Design/AgentReports/2026-07-25_dense_city_packed_asset_sharing.json`; `git diff --check` | Passed; per-placement/per-section/retired-bundle binary-copy row closed | Added a deterministic fail-closed join over complete baked ECS sharing, generated persistent-source/asset-pair parity, and schema-9 packed Build Layout/archive evidence. Synthetic fixtures reject cross-bundle duplicated bytes and multiple EntityScene archives. Both focused execute-method attempts reached their exact 20/20 marker but crashed in Unity's lingering import-worker shutdown and were rejected. The Test Framework-owned EditMode route then exited cleanly, and the final 21-test run executed the real checked-in evidence and published the report. Accepted values are `82,797` render entities, one shared `RenderMeshArray`, `1,769` mesh identities, `80` material identities, `68,722` repeated-signature rows, one `137,862,756`-byte EntityScene archive, and zero shared/duplicated dependency GUIDs or bytes, static runtime entries, managed companions, or production mutations. No source/candidate scene, production Addressables setting, accepted/frozen production asset, Jenkins path, or Android output changed. Progress is 122/149 (82%). |
| 2026-07-25 | Phase 5 explicit open-ground compatibility baseline | `DenseCityRealizedBuildingOwner.ReservesOpenGroundClearance`; schema-locked 3,870-patch gate; focused Windows wrapper `%TEMP%\warline-dense-open-ground-owner-unit.log` 9/9 and `%TEMP%\warline-dense-clipped-open-ground-owner-unit.log` 9/9; complete generator `%TEMP%\warline-dense-open-ground-count-gate-integration.log`; transactional candidate comparison `%TEMP%\warline-dense-open-ground-count-gate-candidate-delta.log`; `Design/AgentReports/2026-07-25_dense_city_fresh_identity_delta.json`; `git diff --check` | Passed dependency step; full two-run Bake All remains open | Replaced the accidental `_Visual` suffix dependency with explicit per-building open-ground clearance ownership, preserving the accepted no-clearance behavior for frontage-snapped buildings. Two rejected transactional probes established the bounds: excluding all explicit owners produced 149 surplus ground identities, while carrying clipped blocker bounds produced 180 missing identities; both restored the accepted candidate cleanly. The accepted implementation takes the deterministic eligible prefix and fails closed below exactly 3,870 patches. The complete source fixture passed at 3,870 patches, and the real candidate comparison passed at 36,946 accepted, 36,946 fresh, and 36,946 shared identities with zero missing/extra rows. No accepted candidate, production Addressables setting, frozen rollback content, Jenkins path, or Android output changed. Progress remains 122/149 (82%) pending two complete byte-stable Bake All passes. |
| 2026-07-26 | Phase 5 self-contained two-run prerequisite transaction | `OperationMapEntitySceneCandidateBakeAllTests.RunTwoRunNoOpValidation`; focused Windows wrapper `%TEMP%\warline-dense-two-run-prerequisite-focused.log` 15/15; attempted complete run `%TEMP%\warline-dense-two-run-noop-windows-inventory-owned.log`; `git diff --check` | Focused prerequisite passed; complete two-run Bake All remains open | The two-run owner now creates its migration inventory and summary at unique machine-temporary paths, publishes both required environment variables only for the validation scope, and restores the previous environment and deletes both temporary files in `finally`. A nested byte checkpoint restores known serializer/report side effects on success and failure while retaining the candidate output only after two exact fingerprints match. The focused suite passed 15/15. The complete Windows run did not reach the execute method because the responsive Editor repeatedly lost/refused the Hub licensing channel and reported the unavailable headless entitlement; that infrastructure attempt is rejected and no two-run acceptance is claimed. The checklist and progress remain 122/149 (82%). |
| 2026-07-26 | Phase 5 current candidate-layout budget and Windows GUI-host validation route | `DenseCityPresentationBudgetValidator.TryCreateReport`; opt-in `-GuiLicensing` forwarding through `InvokeUnityExecuteMethodValidation.ps1`/`InvokeUnity.ps1`; wrapper contracts 10/10; focused budget `%TEMP%\warline-dense-layout-budget-current-contract-gui-host.log` 22/22; focused two-run harness `%TEMP%\warline-dense-two-run-harness-after-layout-budget-gui-host.log` 15/15; rejected full diagnostic `%TEMP%\warline-dense-two-run-noop-gui-host.log`; `git diff --check` | Passed dependency step; complete two-run Bake All remains open | The documented no-Editor host-access recovery selected the versioned `LicenseClient-zfoul-6000.5.2` channel and restored Windows validation. The Windows wrapper now supports opt-in GUI licensing while retaining batchmode as the unchanged default for Jenkins/current callers. The recovered full pass cleared migration inventory, realization, direct dense bake, Addressables layout, source physics, identity backfill, and transform-parity processing, then correctly failed closed because the legacy core presentation budget still required explicit shared Addressables entries. The budget now enforces the accepted current ownership contract exactly: five explicit definition/runtime-binding/entity-scene/surface/minimap entries and zero explicit shared dependencies; a regression rejects the legacy shared-entry form. The two-run incidental checkpoint now also restores the five legacy candidate reports rewritten before budget evaluation. The rejected run's report bytes were restored, production Addressables remained unmodified, and no full two-run acceptance is claimed. Progress remains 122/149 (82%). |
| 2026-07-26 | Phase 5 candidate-output fingerprint race stabilization | `OperationMapEntitySceneCandidateBakeAllTests.ComputeCandidateOutputFingerprint`; rejected full diagnostic `%TEMP%\warline-dense-two-run-noop-gui-host-layout-fixed.log`; focused Windows GUI-host wrapper `%TEMP%\warline-dense-two-run-fingerprint-race-focused-gui-host.log` 15/15; `git diff --check` | Passed dependency step; complete two-run Bake All remains open | The recovered full pass cleared the corrected five-entry/zero-shared layout budget and every preceding gate, then encountered an orphaned generated `.meta` disappearing between candidate-output enumeration and SHA-256 reading. Fingerprinting now performs a synchronous asset refresh before enumeration and records a deterministic `file-missing` manifest row if a file still vanishes during hashing. This preserves fail-closed behavior: a transient disappearance cannot crash the rollback owner, and two runs with different stable file sets cannot compare equal. The focused harness passed 15/15 through the documented versioned-client GUI licensing route. The rejected full run restored its incidental reports and left the worktree clean; no complete two-run acceptance is claimed. Progress remains 122/149 (82%). |
| 2026-07-26 | Phase 5 complete two-run stable-output mismatch diagnosis | Rejected full Windows GUI-host run `%TEMP%\warline-dense-two-run-noop-gui-host-fingerprint-stable.log`; `OperationMapEntitySceneCandidateBakeAllTests.DescribeManifestDifference`; focused Windows GUI-host wrapper `%TEMP%\warline-dense-two-run-manifest-difference-focused-gui-host.log` 15/15; `git diff --check` | Both complete passes cleared content gates; final stable-output equality failed closed and remains open | The first and second complete workflows each realized exactly 4,971 generated buildings, 31,975 render-only identities, 2,468 proxy partitions, and 33,934 surfaces; directly baked 36,946 generated identities and 82,797 render entities; and passed the exact five-entry/zero-shared layout, source-physics, identity-backfill, transform-parity, and 9,544-identity/14,249-renderer core presentation-budget gates. The synchronous refresh removed the transient `.meta` crash and allowed both fingerprints to complete, but the final stable-output SHA-256 values differed (`eb28634e...14ca` versus `ab30966c...78e5`), so the harness rejected the candidate and restored the clean pre-run worktree. Mismatch failures now retain exact equality while reporting both manifest entry counts and the first differing rows, allowing the next full run to identify the unstable path rather than return opaque hashes. No candidate output was accepted and progress remains 122/149 (82%). |
| 2026-07-26 | Phase 5 Windows memory-mapped rollback recovery | `OperationMapEntitySceneCandidateBakeAll.CandidateFileTransaction.Rollback`; `OperationMapEntitySceneCandidateAddressablesLayoutBuilder.BuildCandidateEntitySceneAddressablesLayout`; rejected complete GUI-host diagnostic `%TEMP%\warline-dense-two-run-noop-manifest-difference-gui-host.log`; focused Windows GUI-host wrapper `%TEMP%\warline-dense-windows-mapped-rollback-focused-gui-host.log` 15/15; `git diff --check` | Passed bounded rollback dependency; complete two-run equality remains open | The next full diagnostic passed first-pass realization and direct dense bake, then a failing layout stage attempted to restore the candidate definition while Unity still held a Windows memory-mapped importer handle; `File.WriteAllBytes` returned Win32 error 1224 and masked the original layout failure. The shared byte transaction now calls Unity's `AssetDatabase.ReleaseCachedFileHandles()` before restoring any checkpointed file. The layout owner also preserves both the original exception and a rollback exception in an aggregate if recovery still fails, so root-cause evidence cannot be replaced by a secondary restore error. The focused transaction/harness suite passed 15/15 through GUI licensing. The outer transaction restored the failed run cleanly; no full two-run acceptance is claimed and progress remains 122/149 (82%). |
| 2026-07-26 | Phase 5 byte-idempotent candidate YAML normalization | `OperationMapEntitySceneCandidateAddressablesLayoutBuilder.NormalizeAssetText`; `OperationMapEntitySceneCandidateBakeAllTests.NormalizeAssetText_ChangesOnceThenBecomesByteNoOp`; rejected complete GUI-host run `%TEMP%\warline-dense-two-run-windows-handle-release-gui-host.log`; focused Windows GUI-host wrapper `%TEMP%\warline-dense-idempotent-normalize-focused-gui-host.log` 16/16; `git diff --check` | Passed bounded second-pass write dependency; complete two-run equality remains open | The handle-release rerun completed both dense realizations and both direct ECS bakes at the exact frozen 4,971-building, 31,975-render-only, 36,946-generated-identity, and 82,797-render-entity counts. The second Addressables-layout pass then exposed the original Win32 1224 source: YAML normalization always rewrote the loaded dense candidate definition even when the normalized bytes were already identical, while Unity held the asset memory-mapped. Normalization now compares exact UTF-8 bytes, returns without touching an identical file, and releases Unity's cached handles only before a genuine byte-changing write. A regression proves trailing whitespace is normalized once and the second call is a byte no-op. The outer transaction restored the rejected run; no complete equality pass is claimed and progress remains 122/149 (82%). |
| 2026-07-26 | Phase 5 loaded-asset release before raw-byte mutation | `OperationMapEntitySceneCandidateAddressablesLayoutBuilder.NormalizeAssetText`; per-file loaded-asset release in `CandidateFileTransaction.Rollback`; `OperationMapEntitySceneCandidateBakeAllTests.NormalizeAssetText_UnloadsImportedAssetBeforeChangedWrite`; rejected complete GUI-host run `%TEMP%\warline-dense-two-run-idempotent-normalize-gui-host.log`; focused Windows GUI-host wrapper `%TEMP%\warline-dense-unload-before-raw-write-focused-gui-host.log` 17/17; `git diff --check` | Passed generalized Windows mapped-asset dependency; complete two-run equality remains open | The idempotent-normalization rerun cleared first-pass realization and direct dense bake, then the legacy runtime-binding scene required a genuine normalized-byte change after Unity had imported and loaded the `SceneAsset`; releasing only cached file handles could not release that native mapping, and Windows again returned 1224. Before any genuine raw-byte normalization, the builder now explicitly unloads the path's main Unity asset and releases cached handles. Rollback applies the same unload-and-release rule per checkpointed asset before restoring bytes, preventing a secondary mapped-file error from hiding the original failure. A focused fixture imports and loads a real `TextAsset`, then proves changed normalization succeeds and emits exact LF/no-trailing-whitespace bytes. The focused harness passed 17/17. The outer transaction restored the rejected run; no complete two-run acceptance is claimed and progress remains 122/149 (82%). |
| 2026-07-26 | Phase 5 outer checkpoint loaded-asset restore | `OperationMapEntitySceneCandidateBakeAllTests.CandidateOutputCheckpoint.Restore`; `CandidateOutputCheckpoint_UnloadsImportedAssetBeforeRestore`; rejected complete GUI-host run `%TEMP%\warline-dense-two-run-unload-mapped-assets-gui-host.log`; focused Windows GUI-host wrapper `%TEMP%\warline-dense-outer-checkpoint-unload-focused-gui-host.log` 18/18; `git diff --check` | Passed harness recovery dependency; complete two-run equality remains open | The generalized production mutation fix cleared dense layout and subsequent complete Bake All validation beyond the prior runtime-binding failure. A later body failure then entered the outer two-run checkpoint, whose raw `File.Copy` restore attempted to overwrite the still-loaded candidate presentation scene and masked that underlying exception with Windows user-mapped-file error 1224. The outer checkpoint now unloads each candidate-owned main asset and releases cached handles before file restore/delete; directory rollback unloads every contained asset before recursive replacement. A focused fixture imports and loads a real asset, then proves disposal restores its exact checkpointed bytes. The focused harness passed 18/18. This step does not claim the masked body failure is fixed or the two-run gate passed; progress remains 122/149 (82%). |
| 2026-07-26 | Windows wrapper PATH-independent timeout cleanup | `Tools/CI/InvokeUnity.ps1`; wrapper contract tests 11/11; timed-out complete GUI-host run `%TEMP%\warline-dense-two-run-outer-restore-unload-gui-host.log`; `git diff --check` | Passed CI-wrapper reliability dependency; the still-responsive Unity validation remains unclaimed | The complete two-run validation reached second-pass realization, direct dense bake, source physics, and later processing, but exceeded its explicit 7,200-second wrapper timeout. The process-local duplicate-PATH workaround used for GUI licensing left `taskkill.exe` undiscoverable, so the wrapper could not terminate its owned Unity process tree. Timeout cleanup now resolves `%SystemRoot%\System32\taskkill.exe` explicitly, verifies that exact executable exists, and retains exit code 124 semantics; default batchmode, editor resolution, and Jenkins invocation remain unchanged. Contract tests prove the timeout route contains no bare PATH-dependent `taskkill.exe` call. The existing Unity process remains responsive and CPU-active and is being allowed to complete its transaction; no complete two-run pass is claimed and progress remains 122/149 (82%). |
| 2026-07-26 | Phase 5 first differing authoring-scene YAML diagnostic | Completed post-timeout GUI-host run `%TEMP%\warline-dense-two-run-outer-restore-unload-gui-host.log`; `OperationMapEntitySceneCandidateBakeAllTests.DescribeTextFileDifference`; focused Windows GUI-host wrapper `%TEMP%\warline-dense-first-yaml-difference-focused-gui-host.log` 19/19; `git diff --check` | Exact unstable file identified; line-level cause remains open | The responsive post-timeout Unity process completed both full workflows and every content, physics, parity, layout, and core presentation-budget gate. Exact candidate manifests contained 4,969 entries on both passes, but failed closed because the dense authoring candidate scene changed from SHA-256 `9f6ce384...fcba4` to `0fb137bb...2060`; the overall fingerprints were `ffcc618f...0c0d` and `14eb8855...4112`. The harness now copies only the first-pass 50 MB authoring scene to a unique machine-temporary file, streams both YAML files line-by-line on mismatch, reports the first changed line with bounded escaped text, and deletes the snapshot in `finally`. A focused fixture proves exact line-two diagnostics. No generated snapshot or rejected candidate output is retained, item 905 remains open, and progress remains 122/149 (82%). |
| 2026-07-26 | Phase 5 fail-closed existing-realization no-op prerequisite | `DenseCityCandidateAuthoringTransaction.TryUseExistingRealization`; `MatchesGenerationContract`; focused Windows GUI-wrapper `%TEMP%\warline-dense-existing-realization-noop-focused.log` 20/20; `git diff --check` | Passed bounded prerequisite; complete two-run equality remains open | The authoring-scene mismatch was traced to Unity assigning new YAML local file IDs while recreating semantically identical candidate objects. Realization now avoids that mutation only when the loaded candidate pair passes the existing strict ownership/readiness gate, its schema/version/seed and deterministic source/config SHA-256 exactly match current inputs, its matching proxy asset folder exists, and proxy/building/render-only populations are non-empty. Any stale hash, incomplete structure, or failed readiness check retains the full transactional rebuild behavior. Focused coverage proves an exact generation contract is accepted and a stale hash is rejected. No fail-closed equality gate was weakened, Jenkins still defaults to its existing wrapper batchmode route, progress remains 122/149 (82%), and the complete two-pass run is still required. |
| 2026-07-27 | Phase 5 candidate-definition pre-save no-op prerequisite | Rejected complete Windows GUI-wrapper run `%TEMP%\warline-dense-two-run-existing-realization-noop.log`; `OperationMapEntitySceneCandidateAddressablesLayoutBuilder.ApplyCandidateDefinitionProperties`; focused Windows GUI-wrapper `%TEMP%\warline-dense-candidate-definition-presave-noop-focused-rerun.log` 21/21; `git diff --check` | Passed bounded mapped-file prerequisite; complete two-run equality remains open | Both full-run realization passes correctly reused the accepted generation contract at 4,971 buildings, 31,975 render-only owners, and 2,468 proxies. First-pass complete Bake All passed at 9,544 core identities, 14,249 render entities, 670 meshes, 39 materials, 6,064,174 triangles, zero shared dependencies, and zero production cutover. After the second direct bake, dense layout failed closed because identical serialized candidate-definition fields were reapplied and saved before YAML normalization encountered Win32 mapped-file error 1224; rollback preserved the accepted worktree. Candidate-definition persistence now skips dirty/save/normalize/import when `ApplyModifiedPropertiesWithoutUndo()` reports no field change, while asset creation and genuine GUID changes still persist. A regression proves first apply changes, identical second apply is a no-op, and a later GUID change is detected. The focused suite passed 21/21 after one rejected compile attempt missing the test-only `Game.Configs` import was corrected; its wrapper performed owned timeout cleanup. Progress remains 122/149 (82%), and the complete retry is still required. |
| 2026-07-27 | Phase 5 runtime-binding pre-save no-op prerequisite | Rejected complete Windows GUI-wrapper retry `%TEMP%\warline-dense-two-run-definition-presave-noop.log`; `OperationMapEntitySceneCandidateAddressablesLayoutBuilder.TryReuseExistingCandidateRuntimeBinding`; focused Windows GUI-wrapper `%TEMP%\warline-dense-runtime-binding-presave-noop-focused.log` 22/22; `git diff --check` | Passed bounded mapped-scene prerequisite; complete two-run equality remains open | The candidate-definition pre-save guard prevented its prior mapped-file failure. The retry then reached first-pass complete Bake All and failed closed when accepted candidate layout recopied and resaved its already-correct runtime-binding scene before raw YAML normalization hit Win32 error 1224; the outer checkpoint restored the accepted worktree. Runtime-binding generation now first opens the existing scene and runs the production validator against the exact candidate definition and entity SubScene. A valid binding returns without copy, save, normalization, or import. Missing or invalid output retains the repair path, which unloads the main scene asset and releases cached handles before an overwrite. Focused coverage proves both accepted and dense existing bindings validate as read-only no-ops. No equality, production-isolation, or rollback gate was weakened; progress remains 122/149 (82%) pending the complete retry. |
| 2026-07-27 | Phase 5 Git-LFS mapped parity-report write prerequisite | Rejected complete Windows GUI-wrapper retry `%TEMP%\warline-dense-two-run-runtime-binding-noop.log`; `OperationMapEntityPresentationTransformParityValidator.WriteReportText`; focused Windows GUI-wrapper `%TEMP%\warline-dense-lfs-mapped-parity-report-focused-rerun.log` 23/23; `git diff --check`; `git check-attr filter` confirms the report alone uses `filter=lfs` | Passed bounded mapped-report prerequisite; complete two-run equality remains open | The runtime-binding reuse guard prevented its prior mapped-scene failure. The next retry then failed closed in first-pass Bake All because `File.WriteAllText` attempted to truncate the LFS-tracked transform-parity JSON while a lingering Git LFS filter held a Windows mapping. The writer now performs an exact byte no-op comparison and uses a shared fixed-length stream when replacement bytes have the same length, avoiding truncate/resize while preserving full content updates; real length changes still take the normal replacement path and fail closed if the external mapping forbids them. The first focused attempt proved the write succeeded but used an invalid 19-byte-versus-4-KB mapped-view-capacity assertion; the corrected regression holds a live `MemoryMappedFile`, verifies it remains readable, and proves exact replacement bytes. No LFS process was terminated, no report was untracked, and progress remains 122/149 (82%) pending a complete retry. |
| 2026-07-27 | Phase 5 complete two-run no-op acceptance | Complete Windows GUI-wrapper `%TEMP%\warline-dense-two-run-lfs-parity-report.log`; exact fingerprint `699a32b8e5101346948dcce2c42009853a84d67b0bcd959d3475359057374430`; prior focused prerequisites 20/20 through 23/23; `git diff --check` | Passed; checklist item accepted | Both complete workflows reused the exact current generation contract without recreating YAML objects, then passed direct dense ECS bake, dense five-entry/zero-shared layout, source/runtime physics, legacy identity backfill, source/candidate/baked transform and bounds parity, complete accepted-candidate Bake All, and core presentation budgets. Both passes retained 4,971 generated buildings, 31,975 generated render-only identities, 2,468 proxy partitions, 9,544 legacy core identities, 14,249 legacy render entities, 670 meshes, 39 materials, 6,064,174 triangles, zero explicit shared dependencies, zero production Addressables mutation, and zero production cutover. Exact candidate manifests matched at fingerprint `699a32b8...74430`; item 905 is complete and progress advances to 123/149 (83%). |
| 2026-07-27 | Phase 6 production static-presentation rejection acceptance audit | Existing `OperationMapDenseCityCandidateRuntimeContentBuilder.MeasureProductionStaticAddressables`; previously accepted Windows wrapper `%TEMP%\warline-dense-production-static-counts-tests-2.log` 9/9; tracker and focused-test source audit; `git diff --check` | Passed from existing accepted evidence; checklist item accepted | The production gate reports the authorized `StaticSceneChunks` baseline without claiming cutover, rejects `EntityScene` ownership when either the retired manifest or any static presentation chunk remains, and accepts only exact zero/zero. No named exception is currently authorized. A fresh GUI-wrapper audit attempt did not reach compilation because Unity encountered the known stale licensing-client IPC state; it is recorded only as a rejected startup and contributes no validation claim. No production definition, Addressables setting, frozen rollback asset, source/candidate output, Jenkins path, or Android output changed. Progress advances to 124/149 (83%). |
| 2026-07-27 | Phase 9 isolated Android candidate-content target prerequisite | `OperationMapDenseCityCandidateRuntimeContentBuilder.RequireSupportedValidationBuildTarget`; Android platform output mapping; Windows-only backend-scope exclusion; final Android-active GUI wrapper `%TEMP%\warline-dense-android-content-target-final.log` 42/42; live Android target probe `%TEMP%\warline-dense-android-content-target-live.log`; `git diff --check` | Passed bounded prerequisite; real Android candidate content and APK/AAB remain open | The isolated content owner now accepts installed Android support using the exact Android build-target group, maps the protected shared output to `Library/com.unity.addressables/aa/Android`, and avoids mutating Standalone scripting backend settings on the Android lane. Unsupported targets and missing modules still fail closed. The initial sandboxed focused attempt was rejected before compilation because licensing IPC and BIOS identity access were denied; under the previously approved recovery policy, only that wrapper-owned Editor was terminated and the identical checked-in GUI wrapper passed 42/42 with full access. A separate read-only live probe switched to Android, compiled platform assemblies, and proved `activeTarget=Android`, `targetGroup=Android`, and `addressablesPlatform=Android`; the final exact-code suite then passed 42/42 with Android active. No content build, APK/AAB, production definition, Addressables setting, frozen rollback asset, accepted candidate output, or Jenkins editor path changed. Progress remains 124/149 (83%). |
| 2026-07-27 | Phase 9 real isolated Android candidate content | Initial approved GUI wrapper `%TEMP%\warline-dense-android-runtime-content-build.log`; report-ownership rerun `%TEMP%\warline-dense-android-runtime-content-report-owned.log`; final focused ownership suite `%TEMP%\warline-dense-android-report-ownership-final.log` 42/42; Android schema-9 `Design/AgentReports/2026-07-27_dense_city_candidate_android_runtime_content.json`; retained Build Layout under `Library/OperationMapDenseCityRuntimeContent/BuildLayout`; `git diff --check` | Android Addressables/Entities content passed; clean APK/AAB remains open | From clean tracked revision `9f8a55a02`, the isolated owner built four local Android bundles / final 915,794 bytes and one 3,041-byte catalog, then one 115,924,427-byte EntityScene archive plus 49,947,710 metadata bytes. Final bundle-set SHA-256 is `0fdef1a9...57e531`; archive-set SHA-256 is `886adc6d...ac0b4`. The real Build Layout contains five packed runtime asset paths, zero source-hierarchy explicit/implicit assets, and zero shared or duplicated dependency GUIDs/bytes. The first successful build differed by one compressed bundle byte and transient Addressables/Build Layout hashes while retaining the exact archive and ownership metrics, so cross-build bundle-byte determinism is not claimed. Android evidence now has a target-owned report while the canonical desktop packed report remains byte-identical to `HEAD`. Production settings and the shared Android output were restored exactly; the tracked machine-only candidate-layout report path was restored before staging. Production stays `StaticSceneChunks`, frozen rollback remains 269 chunks, and cutover stays zero. Progress remains 124/149 (83%) because the combined checklist row also requires the candidate APK/AAB. |
| 2026-07-27 | Phase 9 clean APK Git-provenance executable prerequisite | Rejected pre-mutation wrappers `%TEMP%\warline-dense-android-clean-production-apk.log` and `%TEMP%\warline-dense-android-clean-production-apk-path-fixed.log`; `AndroidBuildReportGenerator.ResolveGitExecutable`; focused Android-active GUI wrapper `%TEMP%\warline-android-git-provenance-override-focused.log` 13/13; `git diff --check` | Passed bounded prerequisite; clean APK rerun remains open | Both APK attempts correctly failed before target/settings/Addressables/player build mutation when Unity could not resolve `git` after duplicate-`PATH` cleanup. Provenance capture now supports an invocation-only `WARLINE_GIT_EXECUTABLE` absolute path, rejects relative/missing overrides, and otherwise retains PATH lookup. No Git or Unity install path is serialized or checked in, preserving Jenkins drive independence. Progress remains 124/149 (83%). |
| 2026-07-27 | Phase 9 isolated candidate Android package owner prerequisite | `BuildScript.BuildDenseCityCandidateAndroidApk`; `OperationMapDenseCityCandidateAndroidPackageDeployment`; embedded local Addressables runtime path; focused Android-active GUI wrapper `%TEMP%\warline-dense-candidate-android-package-remote-content-final.log` 8/8; candidate-content regression `%TEMP%\warline-dense-candidate-android-package-content-regression.log` 42/42; `git diff --check` | Passed bounded packaging prerequisite; real candidate APK remains open | The clean production APK correctly remains fail-closed on its stale frozen static manifest and is not used as a candidate bypass. A separate candidate-only build entry point now rebuilds the isolated Android catalog with an embedded `{Addressables.RuntimePath}` load root, adds the candidate catalog/bundles and remote Entities archive/catalog content through `BuildPlayerContext` without copying into `Assets`, rejects source/candidate authoring scenes and legacy generated static scenes from player-scene ownership, and validates the finished APK for the dense EntityScene, its exact remote-content archive, local catalog/bundles, and absence of the production EntityScene. The first focused compile attempt was rejected for one corrected namespace qualification; only its wrapper-owned GUI Editor was recovered, while Hub and licensing remained running. Exact-code focused validation passed 8/8 and the existing content suite passed 42/42. No APK was built, no production Addressables setting or frozen rollback asset changed, no checklist item is claimed, and progress remains 124/149 (83%). |
| 2026-07-27 | Phase 9 embedded Addressables dynamic-path investigation | Rejected clean candidate APKs `%TEMP%\warline-dense-candidate-android-clean-apk.log` and `%TEMP%\warline-dense-candidate-android-clean-apk-dynamic-path.log`; `EmbeddedAndroidAddressablesBuildPath`; focused Android-active GUI wrapper `%TEMP%\warline-dense-candidate-android-package-dynamic-path-final.log` 8/8; `git diff --check` | Rejected; exact dynamic build-path syntax corrected for retry | The first real candidate build reached the isolated Addressables owner and failed closed before bundle, Entities, player, or Gradle output because Addressables requires a dynamic `{Addressables.RuntimePath}` load root to pair with a dynamic build root. The first correction used braces on both paths and passed the focused contract but was rejected by the real Addressables builder because its build-side expansion specifically requires square brackets. Embedded packaging now uses the package-defined `[Addressables.BuildPath]/Android` build expression with `{Addressables.RuntimePath}/DenseCityCandidate/Android` for runtime, while desktop/editor parity builds retain their absolute local paths. Both rejected runs left no transient settings folder, production Addressables setting, frozen rollback asset, APK, or accepted checklist claim; progress remains 124/149 (83%). |
| 2026-07-27 | Phase 9 official Entities player-deployment ownership prerequisite | Rejected clean candidate APK `%TEMP%\warline-dense-candidate-android-clean-apk-canonical-path.log`; `OperationMapEntitySceneBuildAdditions.UseCurrentProcessSceneOverride`; candidate Addressables-only deployment plan; readable Entities archive-catalog APK gate; focused Android-active GUI wrapper `%TEMP%\warline-dense-candidate-android-official-entities-owner.log` 8/8; `git diff --check` | Passed bounded ownership prerequisite; clean candidate APK retry remains open | The canonical path retry passed the isolated four-bundle Addressables build and the complete one-archive candidate Entities build at 165,872,137 bytes. Player preparation then failed closed because the custom package processor attempted to add the candidate `archive_dependencies.bin` after Unity's official Entities processor had already registered that destination. Candidate packaging now leaves all Entities file deployment to Unity, scopes the existing `IEntitySceneBuildAdditions` owner to the dense candidate for this build only, and injects only the isolated Addressables catalog/bundles. The final APK gate reads the official archive dependency text, requires explicit dense-GUID ownership and archive payload, and rejects both production header and production archive-catalog ownership. The focused suite passed 8/8. Rejected-run report drift was restored exactly; no APK, production mutation, frozen rollback mutation, or checklist claim is retained, and progress remains 124/149 (83%). |
| 2026-07-27 | Phase 9 real candidate APK and archive-suffix gate correction | Clean canonical-environment wrapper `%TEMP%\warline-dense-candidate-android-clean-apk-canonical-env.log`; rejected focused marker `%TEMP%\warline-dense-candidate-android-archive-extension.log` 8/8; rejected existing-APK gates `%TEMP%\warline-dense-candidate-existing-apk-gate-explicit-exit.log`, `-headless-final.log`, and `-process-exit.log`; direct read-only ZIP inspection; SHA-256; `git diff --check` | APK/player build succeeded; checklist acceptance remains open because no wrapper validation exited successfully | From clean pushed revision `963713b8c`, canonicalizing the inherited duplicate `Path` keys preserved Git and Android SDK discovery without embedding this machine's `C:` paths or changing Jenkins resolution. Unity built the 555,819,265-byte candidate APK, but the post-build gate falsely rejected Unity's `.archive` payload filenames. The gate retains extensionless compatibility and the focused fixture now covers Unity's `.archive` form. Direct package inspection finds three archive payloads, four local candidate bundles, explicit dense GUID ownership, zero production GUID ownership, and SHA-256 `c1600f57...e299da`; the Unity gate logged the same 555,819,265-byte pass three times. The focused test marker reached 8/8, but GUI and headless wrapper runs crashed during shutdown, while the process-exit probe stalled after its pass marker and only its two wrapper-owned Unity processes were recovered; Hub and licensing were untouched. All those runs remain rejected, no checklist row closes, and progress remains 124/149 (83%). |
| 2026-07-27 | Windows wrapper native-crash fail-closed prerequisite | `Tools/CI/InvokeUnity.ps1`; isolated `Find-UnityLoggedFailure` semantic harness 8/8; rejected APK logs `%TEMP%\warline-dense-candidate-existing-apk-gate-explicit-exit.log` and `-headless-final.log`; `git diff --check` | Passed bounded wrapper hardening; APK acceptance remains open | Native Unity shutdown crashes previously produced no readable process exit code and were rejected only because no generic success marker existed; a completed Test Framework marker could therefore have hidden the same crash. The wrapper now treats both the observed `Crash!!!` sentinel and Unity's crash-handler sentence as fatal before considering any success marker. The focused source-semantic harness proves both native signatures plus managed execute-method, nonzero return, license, and batch-abort failures are detected, while return-code-zero and normal output remain nonfatal. Both rejected APK logs now classify as `Crash!!!`. No Unity run, production/candidate asset, package, Jenkins editor path, or checklist item changed; progress remains 124/149 (83%). |
| 2026-07-27 | Phase 9 candidate APK static package evidence | `aapt dump badging`; empty `adb devices -l`; retained candidate APK; `Design/AgentReports/2026-07-27_dense_city_candidate_android_package_evidence.json`; package-evidence reconciliation 9/9; `git diff --check` | Passed bounded candidate-only evidence; installed/device acceptance remains open | The retained APK is launchable as `com.warlinecapture.game/com.unity3d.player.UnityPlayerGameActivity`, targets SDK 36 with minimum SDK 26, and remains byte-identical to SHA-256 `c1600f57...e299da`. The checked-in evidence enumerates all three official Entities archives and four embedded local candidate bundles, records explicit dense and zero production archive ownership, joins the schema-9 Build Layout's zero duplicated dependency/source-hierarchy metrics, and distinguishes six duplicate Android resource-merge entry names from packed dependency duplication. The build log contains only Menu and Match player scenes. No ADB device is attached, so the report explicitly leaves installed bytes, offline launch, Deploy entry, runtime metrics, and device-side authoring absence unaccepted. No APK, production/candidate asset, frozen rollback content, Unity/Jenkins path, or checklist count changed; progress remains 124/149 (83%). |
| 2026-07-27 | Phase 9 network-disabled Android launch and Deploy entry | Xiaomi `24090RA29G` / Android 16 / arm64-v8a; successful ADB install; `am start -W`; process/activity checks; `%TEMP%\warline-dense-offline-*.png`; filtered logcat; updated candidate package evidence; `git diff --check` | Passed; offline application-launch/Deploy checklist item accepted | The exact 555,819,265-byte candidate APK installed after the user approved Xiaomi's Install-via-USB prompt. With Wi-Fi and mobile data both disabled, a cold start completed in 755 ms / 783 ms wait, stayed foreground/resumed after 25 seconds, rendered first-launch language and narrative UI, followed the explicit skip confirmation, and reached the main menu with the Deploy control visible. Repeated offline transitions retained the same process and produced zero fatal, fatal-signal, ANR, or activity-start matches. The missing optional Play Asset Delivery class was nonfatal for this embedded local APK and is retained in evidence rather than hidden. Wi-Fi and mobile data were restored to their original enabled state after every probe. The readable installed code path is 712,775 KiB, but complete installed size remains open because shell access to `oat` was denied. One checklist row closes; progress advances to 125/149 (84%). |
| 2026-07-27 | Phase 9 dense candidate APK runtime-definition selection fix | Android logcat from the retained candidate APK; Unity Hub GUI compile with final `Tundra build success` (363 updated items); GUI Test Runner `OperationMapDenseCityCandidateAndroidPackageTests` 12/12 in `0.088 s`; `git diff --check` | Passed bounded code/package-contract validation; rebuilt APK and device Match acceptance remain open | Device deployment reached Match but rendered only the sparse compatibility objects because the packaged candidate owned EntityScene `c00140f2e94a04c3084c8dcb0c18cbd0` while `MatchSceneView` still resolved the production catalog definition and requested missing EntityScene `d50925a18e9164ce782536576cb833d8`. The candidate APK now receives the build-scoped `WARLINE_DENSE_CITY_CANDIDATE` define, mounts only its embedded `aa/DenseCityCandidate/catalog.bin`, loads the exact dense definition address, and rejects any map-id, presentation-kind, or EntityScene-GUID mismatch instead of falling back to production. Production players retain full catalog validation and their existing definition path. The package/build contract fails closed unless the runtime GUID, definition address, catalog path, layout, and build define agree. The first GUI compile correctly exposed and rejected a wrong `IResourceLocator` namespace; the corrected code compiled successfully and all twelve focused tests passed. Standalone wrapper attempts did not reach compilation because of the documented Windows licensing-client mutex and contribute no pass claim. No accepted/frozen production asset, production Addressables setting, Jenkins editor path, or retained APK changed. Progress remains 125/149 (84%) until a rebuilt APK proves candidate catalog/definition/EntityScene loading and dense visuals on the Android device. |
| 2026-07-27 | Phase 9 dense Match minimap retention and marker-scan throttling | Rebuilt candidate APK `Build/AndroidDenseCandidate/WarlineCapture-DenseCityCandidate.apk`; Android logcat/frame diagnostics; `Build/AndroidDenseCandidate/validation/zoomout.png`; Unity Hub D3D11 compile; GUI Test Runner `MatchHudMinimapMarkerSystemTests` 4/4; rejected concurrent standalone wrapper `%TEMP%\warline-minimap-marker-focused-windows.log`; `git diff --check` | Windows fix validation passed; Android minimap/FPS reacceptance remains open | The rebuilt 555,823,365-byte APK (SHA-256 `613aeeec...3ebcb`) selected the candidate catalog/definition, loaded dense EntityScene `c00140...cbd0`, and restored the missing dense city/building/tent presentation, closing the runtime-selection diagnostic without closing an Android checklist row. The first dense Match view exposed an empty minimap and approximately 16.4 FPS. The marker producer cleared its singleton buffer on every frame, used a transform change filter that then skipped unchanged units, and synchronously scanned 5,431 units every frame outside the measured HUD view cost. It now retains markers between 0.2-second HUD-cadence refreshes, performs ordered bounded player/enemy/scan-intel collection into a 1,024-entry scratch list, and publishes only after collection completes. Windows D3D11 compilation succeeded and all four exact regression tests passed, including unchanged-frame marker retention. A concurrent standalone-wrapper attempt was rejected before compilation because the sandboxed licensing client could not read BIOS identity while the normal Editor was open; it contributes no validation claim and did not change project content. The device was disconnected before rebuilding this correction, so no Android FPS or rendered-minimap pass is claimed. No accepted/frozen production asset, production Addressables setting, Jenkins path, or rollback package changed. Progress remains 125/149 (84%). |
| 2026-07-27 | Phase 7 embedded local-bundle and offline application acceptance | Direct read-only ZIP inspection of `Build/AndroidDenseCandidate/WarlineCapture-DenseCityCandidate.apk`; schema-9 `Design/AgentReports/2026-07-27_dense_city_candidate_android_runtime_content.json`; accepted Xiaomi network-disabled cold-launch evidence; candidate local-load contract/source audit; `git diff --check` | Passed; local-bundle/no-network checklist item accepted | The current 555,823,365-byte APK / SHA-256 `613aeeec...3ebcb` contains exactly one 2,977-byte candidate catalog, its 32-byte hash, and four `assets/aa/DenseCityCandidate/Android/*.bundle` payloads totaling 915,795 bytes, exactly matching the current Android runtime-content report. No HTTP or remote-catalog-named ZIP entry exists. The build owner disables remote catalogs and startup updates, requires the candidate group in the build, and rejects network load paths; runtime resolution mounts the catalog directly beneath `Application.streamingAssetsPath`. The already accepted installed candidate cold-launched and reached the rendered Deploy menu with Wi-Fi and mobile data both disabled and zero fatal/ANR matches. This closes only local application ownership; it does not claim current-APK offline rerun, corrected minimap/FPS acceptance, installed-size completion, dense Match lifecycle, production cutover, or frozen-package retirement. No Unity run or asset mutation was required. Progress advances to 126/149 (85%). |
| 2026-07-27 | Phase 9 corrected candidate APK wrapper acceptance | Windows GUI-licensing wrapper `%TEMP%\warline-dense-candidate-android-minimap-rebuild-full.log`; `Game.Editor.BuildScript.BuildDenseCityCandidateAndroidApk`; required package/content pass markers; wrapper exit code 0; direct ZIP inventory; SHA-256; JSON parse; URP runtime-settings reference audit; `git diff --check` | Passed; clean wrapper-build checklist item accepted | From clean pushed revision `a3946c7b0`, the documented wrapper rebuilt isolated Android content and produced the corrected minimap/FPS candidate APK without a shutdown crash or stall. The 555,817,853-byte APK / SHA-256 `dec15d02...ae079` contains one candidate catalog, four candidate bundles, the dense EntityScene archive/header, and zero production EntityScene entries. The content gate reports one 115,924,427-byte archive, 49,947,710 metadata bytes, zero shared/duplicated dependency bytes, restored shared output, and no production Addressables mutation. Unity's build-time URP synchronization populated runtime settings with references already defined in the same asset; all fourteen added references resolve. Device-side minimap/FPS acceptance remains open because the Android device is disconnected. One checklist row closes; progress advances to 127/149 (85%). |
| 2026-07-27 | Phase 6 mobile material/lighting evidence prerequisite | `DenseCityPresentationBudgetValidator` schema 2; Windows GUI-licensing wrappers `%TEMP%\warline-dense-mobile-material-lighting-evidence.log`, rejected `-schema2.log`, and final `-schema2-final.log`; `[DenseCityPresentationBudgetValidation] result=Passed tests=23`; wrapper exit code 0; `git diff --check` | Passed bounded evidence prerequisite; Android mobile-budget acceptance remains open | Candidate geometry evidence now deterministically counts shadow casters/receivers, unique transparent/alpha-clipped materials, at-most-1 m detail renderers, lightmapped renderers, and light-probe users, and rejects incomplete or impossible count ranges before report publication. The first implementation passed 23/23; the schema-version review added a new assertion whose first rerun correctly failed because only the invalidation report had been bumped. The final corrected schema-2 report path compiled, passed all 23/23 tests, and exited through the documented wrapper with code `0`. No accepted/frozen asset, rendering setting, candidate output, or checklist count changed; progress remains 127/149 (85%). |
| 2026-07-27 | Phase 6 Android URP runtime-resource closure | `AndroidVisualQualityValidationTests.UniversalRenderPipelineGlobalSettingsRetainRuntimeResources`; Windows GUI-licensing wrapper `%TEMP%\warline-android-visual-quality-runtime-rids.log`; `[AndroidVisualQualityValidation] result=Passed tests=17`; wrapper exit code 0; `git diff --check` | Passed bounded render-policy prerequisite; device budgets remain open | The Android render policy remains fixed at the already accepted mobile render scale/MSAA/shadow/lighting/Forward+/GPU Resident Drawer settings. The strengthened fail-closed test parses runtime and definition RID sets, rejects duplicates and dangling runtime references, and proves the GPU Resident Drawer resource is runtime-owned rather than merely serialized. All 17/17 focused cases passed. No accepted/frozen asset, rendering setting, candidate output, or checklist count changed; progress remains 127/149 (85%). |
| 2026-07-27 | Phase 9 corrected-APK minimap/performance rerun | Paired wireless ADB on Xiaomi `24090RA29G`; exact APK install/cold launch; candidate runtime/readiness logs; 30-second stable Match observation; `dumpsys meminfo`; `top -H`; transient 2712x1220 screenshots plus SHA-256; opt-in Unity profiler-marker rerun; structured `2026-07-27_dense_city_candidate_android_minimap_performance_rerun.json`; JSON parse; `git diff --check` | Minimap passed; FPS/CPU/memory acceptance rejected | The exact wrapper-accepted APK installed without a prompt, cold-launched in 644 ms, resolved dense EntityScene `c00140...cbd0`, reached gameplay readiness, and visibly retained minimap markers. Stable Match remained CPU-main-bound at `15.7-16.3 FPS`; GPU time was near a 60 FPS frame but main-thread time remained about 57-60 ms, and total PSS was 2,209,278 KiB with 948,829 KiB graphics. Gameplay update instrumentation averaged only 0.4 ms. A diagnostic marker relaunch narrowed the missing cost to PlayerLoop but was not an acceptance sample; wireless ADB disconnected before a raw Unity profile could be recovered. No source, accepted/frozen asset, package, or checklist count changed; progress remains 127/149 (85%). |

| 2026-07-28 | Measured Android render-residency design amendment | Corrected-APK report; packed asset-sharing report; current bake/runtime/camera/job/assembly inventory; `dense_city_virtualized_render_proxy_android_60fps_implementation_tracker.md`; checklist/dependency consistency audit; `git diff --check` | Candidate-only design accepted; implementation and parent checklist counts remain open | The new tracker converts the measured whole-map render-entity failure into an 80-step dependency-ordered plan: immutable prototype/part/placement/cell data, one preallocated ECS render-slot set, Burst cell selection/assignment/apply, canonical building-state synchronization, two-level logical/materialized parity, exact lifecycle/package gates, and 60-FPS-class Android budgets. It labels bounded additive work for Terra and ownership/hot-path/cutover work for Sol. This authorizes investigation only: current candidate/production outputs, `StaticSceneChunks`, accepted/frozen assets, Android APK, and progress `127/149 (85%)` remain unchanged. |
| 2026-07-30 | VRP render-only candidate pilot enablement | Child `VRP-051`; Windows GUI-licensing contract 43/43, runtime initialization 8/8, proxy apply 8/8, ECS architecture 11/11; transactional two-pass candidate validator; `2026-07-30_dense_city_render_virtualization_pilot_enabled.json`; unchanged report SHA-256 `13800b4c...e8da` | Passed candidate-only amendment step; parent checklist count unchanged | The dense candidate definition is now `EntityScene + VirtualizedProxyPool` with exactly one persisted virtualization authoring root. Direct bake twice reproduces 9,721 logical placements, 22 prototypes/26 parts, 1,635 cells, 704 fixed slots, 11,299 stripped eligible rows, and 70,710 resident authoring rows; the runtime coordinator schedules selection/assignment/apply and no-ops inside its materialized camera guard. Production remains `StaticSceneChunks + ResidentEntities`, `productionCutover=0`, and accepted/frozen/production/Addressables assets are unchanged. Packed camera-motion/guard-band/overflow validation and all Android acceptance remain open, so parent progress stays `127/149 (85%)`. |
| 2026-07-30 | VRP packed virtualized camera-route acceptance | Child `VRP-052`; host-access Windows GUI-licensing PlayMode `%TEMP%\warline-vrp052-playmode-host.xml` 1/1; initialization EditMode 9/9; ECS architecture 11/11; route marker `placements=9721 rows=11299 capacity=704 active=319 rebuilds=4 overflow=0`; `git diff --check` | Passed child packed-runtime step; parent checklist count unchanged | The isolated candidate package now proves slow/fast pan, zoom, rotation, teleport, stable guard-band no-op behavior, exact deterministic return bindings, and zero overflow/deficit across four required rebuilds. Runtime initialization waits while the low-level packed fixture has zero active-map projection, then initializes once the exact map owner appears; duplicate and mismatched owners remain fail-closed. Production, accepted/frozen assets, Android artifacts, and source representation ownership remain unchanged. Android before/after measurement is still open, so parent progress stays `127/149 (85%)`. |
| 2026-07-30 | VRP Android before/after pilot rejection | Child `VRP-053`; Xiaomi `24090RA29G`; instrumented profiler APK SHA-256 `4eaf7858...269817b`; raw capture frames `0..900`; checked wrapper export `%TEMP%\warline-vrp053-after-profiler-export.log` frames 300..799; `2026-07-30_dense_city_vrp053_android_before_after.json` | Measurement complete; expansion and parent checklist progress unchanged | The same APH-804 stationary-input route measured 10.6 FPS average, 93.94 ms average / 110.68 ms p95 frame, 93.93 ms average / 110.66 ms p95 CPU active, and 15.53 ms average / 16.59 ms p95 GPU. Device metrics loaded 9,721 placements, 11,299 parts, 70,710 resident rows, and 704 slots but enabled zero with zero active cells/placements. Raw markers identify `UnitFactionTintTargetBackfillSystem` at 51.722 ms/frame and `UnitMassRenderSettingsSystem` at 17.318 ms/frame while Entities Graphics itself averaged 0.683 ms/frame. Exact camera-pose image parity remains qualified because the historical before artifact did not serialize its pose. VRP-054 must reject expansion, correct initial-view publication and unchanged whole-world presentation scans, then remeasure Android. Production, accepted/frozen assets, source representation ownership, and parent progress remain `127/149 (85%)`. |
| 2026-07-31 | VRP Windows initial-view and presentation-scan remediation | Child `VRP-054` partial; host-access GUI-licensing camera 9/9, unit-render 36/36, faction-tint 4/4; exact 70,710-child stationary proxy 300 samples / 0.0016 ms p95 for the two former CPU owners; `2026-07-31_dense_city_vrp054_windows_remediation.md` | Windows remediation passed; Android acceptance and parent checklist progress unchanged | Runtime camera residency now uses the projected frustum footprint across the baked presentation height range. Operation-map identity/authored-vehicle parents are terminally excluded before incomplete-unit retry, and the one-shot unit classifier owns missing tint defaults so the legacy backfill skips classified renderers. The interim 8.3333 ms 120-Hz Editor CPU-owner budget passes, but this is not whole-frame or Android evidence. `VRP-054` and Phase 6 remain open until the same Android route proves active slots, material CPU-main p95 improvement, parity, and no replacement owner. Production, accepted/frozen assets, source ownership, Android artifacts, and parent progress remain `127/149 (85%)`. |
| 2026-07-31 | VRP exact packed-resident Android expansion acceptance | Child `VRP-054`; resident-buffer focused rerun 38/38; exact 70,710-row Windows steady-state 300 samples / 0.0025 ms p95; profiler APK SHA-256 `36600231...0f2`; Xiaomi `24090RA29G`; 486,810,763-byte raw capture SHA-256 `805860c3...d866`; checked export `%TEMP%\warline-vrp054-resident-buffer-profiler-export.log` frames 300..799; Android summary and visual review | Passed child expansion gate; Phase 6 dependency-ready; parent checklist count unchanged | Exact packed resident-row classification replaces unreliable transform ancestry for the 70,710 resident render entities and runs in bounded 12,000-row passes. The corrected stationary Android route reports 122/704 active slots, 25 active cells, 114 active placements, and zero overflow/deficit. Compared with VRP-053, average frame time improves from 93.94 to 21.62 ms and CPU-main p95 from 110.66 to 35.88 ms; faction-tint backfill falls from 51.722 to 2.239 ms/frame and mass render settings from 17.318 to 2.171 ms/frame, with no replacement dominant owner. The retained Android view shows intact dense-city presentation, HUD, and minimap without a gross hole; exact historical pose equivalence remains qualified because the before artifact did not serialize its pose. This unblocks the child building/state phase but does not accept the final 16.667 ms/60 FPS gate. Production, accepted/frozen assets, source representation ownership, Addressables, Jenkins/Unity paths, `productionCutover=0`, and parent progress remain `127/149 (85%)`. |
| 2026-07-31 | VRP deterministic gameplay-building state-owner indices | Child `VRP-060`; rejected compile `%TEMP%\warline-vrp060-state-owner-indices.log`; accepted corrected checked wrapper `%TEMP%\warline-vrp060-state-owner-indices-rerun.log` 44/44; `git diff --check` | Passed child Phase 6 index contract; parent checklist count unchanged | Every virtualized gameplay-building mapping must now use contiguous zero-based indices in unsigned collision-checked owner-identity order. The post-baking gate rejects arbitrary, sparse, or reordered indices before removing resident render-root ownership, then retains the accepted index on the canonical gameplay building while its building, destroyed-state, health, faction, grid, blocker, targeting, and other authoritative ECS data remain resident. The current candidate pilot still contains no stateful building rows; no family, source renderer, accepted/frozen/production asset, Addressables ownership, Android artifact, or production cutover changed. Child destruction adaptation is next; parent progress remains `127/149 (85%)`. |
| 2026-07-31 | VRP resident/virtualized canonical building destruction split | Child `VRP-061`; rejected first checked wrapper `%TEMP%\warline-vrp061-building-destruction.log` because the test retained a buffer safety handle across structural changes; accepted corrected/final focused wrappers `%TEMP%\warline-vrp061-building-destruction-rerun.log` and `%TEMP%\warline-vrp061-building-destruction-final.log` 4/4; ECS/Burst architecture wrapper `%TEMP%\warline-vrp061-ecs-architecture.log` 11/11; `git diff --check` | Passed child Phase 6 destruction contract; parent checklist count unchanged | Resident buildings retain the accepted intact/destroyed render-root scale transition. Virtualized buildings now enable the same canonical destroyed state and append exactly one destroyed-state event carrying their deterministic owner index and a monotonic nonzero version to the unique map-owned buffer, without transform/proxy access, visual instantiation, or gameplay-entity structural changes. Missing or duplicate buffer ownership, resident/virtualized overlap, invalid version state, and conservative overflow fail closed. The current pilot still has no stateful building rows; bounded event consumption and proxy synchronization remain child `VRP-062`. No building family, source renderer, accepted/frozen/production asset, Addressables ownership, Android artifact, Jenkins/Unity path, or production cutover changed; parent progress remains `127/149 (85%)`. |
| 2026-07-31 | VRP bounded canonical building-state synchronization | Child `VRP-062`; rejected compile and test-fixture assumptions recorded in the child log; accepted checked wrappers for state sync 4/4, destruction producer 4/4, cell selection 7/7, initialization 9/9, virtualization contract 44/44, and ECS/Burst architecture 11/11; `git diff --check` | Passed child Phase 6 synchronization contract; parent checklist count unchanged | The candidate runtime now initializes exact map-owned canonical visual states from contiguous virtualized gameplay-building owners, consumes only owner-bounded strictly versioned unique-owner batches in a Burst job, marks only affected logical placements and containing cells, and clears events through the scheduled ECS dependency chain. Visible changes rebuild the current guard envelope and reassign intact/destroyed rows; off-camera changes retain state without proxy allocation, and simultaneous camera exits use the new envelope. The current Vegetation/Prop pilot still has zero state owners and no building family was mutated; child `VRP-063` remains the first complete-family ownership step. Production, accepted/frozen assets, source ownership, Addressables, Android artifacts, Jenkins/Unity paths, and `productionCutover=0` remain unchanged; parent progress remains `127/149 (85%)`. |
| 2026-07-31 | VRP complete generated House-family ownership | Child `VRP-063`; checked inventory and unchanged-input database determinism; two-pass direct bake; virtualization contract 45/45; state sync 4/4; destruction 4/4; initialization 9/9; ECS/Burst architecture 11/11 | Passed child Phase 6 ownership step; parent checklist count unchanged | The candidate database now virtualizes one complete generated House family under 3,638 canonical gameplay-building source owners, each with exactly one intact and one destroyed recipe. This adds 7,276 state-linked placements beside the existing 9,721 render-only placements for 16,997 total placements, 7,291 prototypes, 10,778 parts, 1,651 cells, and 929 slots. Exact direct-bake ownership removes 22,058 of 82,009 source render rows and leaves 59,951 resident; 7,572 rows belonging to other building families remain explicitly deferred. Determinism reproduced content hash `936bf85c...3709`, ordering hash `31dac1ed...719d`, and direct-bake fingerprint `4a436bd6...e785`. Child VRP-064 still owns visible/off-camera destruction, recycle/return, and state-leak acceptance; final Android 60 FPS acceptance remains open. Production, accepted/frozen assets, Addressables, Jenkins/Unity paths, and `productionCutover=0` remain unchanged; parent progress remains `127/149 (85%)`. |
| 2026-07-31 | VRP compound building-state recycling acceptance | Child `VRP-064`; strengthened checked state scenarios 8/8; destruction 4/4; initialization 9/9; virtualization contract 45/45; ECS/Burst architecture 11/11 | Passed child Phase 6 transition/recycling step; parent checklist count unchanged | A bounded Windows/editor fixture forces two distant canonical building owners through one three-slot bucket using three-part intact and two-part destroyed recipes. It proves visible swaps remove every prior-state part, off-camera destruction materializes correctly on return, recycle/return reproduces exact slot use, and alternating owner transitions cannot leak visual state or part identity through recycled slots. Canonical gameplay state remains independently indexed and authoritative. This adds acceptance coverage only; the candidate database, runtime behavior, production, accepted/frozen assets, Addressables, Jenkins/Unity paths, Android artifacts, and `productionCutover=0` remain unchanged. Gameplay-consumer independence remains child VRP-065 and real-device destruction/recycling remains VRP-095; parent progress remains `127/149 (85%)`. |
| 2026-07-31 | VRP canonical gameplay-consumer independence | Child `VRP-065`; checked architecture 12/12, selection 12/12, targeting 4/4, minimap 4/4, movement/blocker batch passed, virtualization contract 45/45; rejected wrong execute-method attempt retained in the child log; `git diff --check` | Passed child Phase 6 independence step; parent checklist count unchanged | A fail-closed source architecture test now requires concrete selection, targeting, minimap, navigation, and blocker consumers plus their canonical gameplay-data tokens, and rejects dependencies on render-proxy assignment/state contracts or `MaterialMeshInfo`. Existing functional suites prove those consumers still operate on canonical entity/component state while the virtualization contract remains intact. This changes tests only and required no Android run; runtime behavior, the candidate database, production, accepted/frozen assets, Addressables, Jenkins/Unity paths, Android artifacts, and `productionCutover=0` remain unchanged. Complete-family expansion is next at child VRP-066; parent progress remains `127/149 (85%)`. |
| 2026-07-31 | VRP complete eligible generated-building-family expansion | Child `VRP-066`; exact family inventory; deterministic database rerun; two-pass direct bake fingerprint `a9d4ec5b...e99f`; virtualization 45/45; readiness 11/11; state sync 8/8; destruction 4/4; `git diff --check` | Passed child Phase 6 expansion; parent checklist count unchanged | The candidate now virtualizes all currently eligible repeated generated building families: complete House (3,638 owners / 10,759 rows) and complete Shop (1,295 owners / 7,210 rows). CityHall remains wholly resident with all 406 rows reported under the stable unsupported-policy exclusion because 44 family rows violate the closed policy; the current dense generator has no Tent or Military family. The candidate database contains 19,587 placements, 29,268 logical rows, 9,875 prototypes, 17,982 parts, 1,660 cells, and 1,364 fixed slots. Exact multi-submesh reconciliation records 29,081 selected physical source renderers and leaves 52,928 of 82,009 physical renderers resident. Production, accepted/frozen assets, Addressables, Jenkins/Unity paths, Android artifacts, and `productionCutover=0` remain unchanged; the parent checklist remains `127/149 (85%)` because this child expansion closes no parent row. Child VRP-067 owns Android destruction-transition acceptance. |
| 2026-08-01 | VRP normalized building-state Android acceptance | Child `VRP-067`; focused normalization 3/3; exact inventory and unchanged database determinism; two-pass direct bake; virtualization 45/45; runtime-content rollback 42/42; checked profiler APK SHA-256 `34347e15...9cbf`; paired Xiaomi House/Shop 120-frame before/after samples and reviewed captures | Passed child Phase 6 state/visual/capacity gate; parent checklist count unchanged | Candidate-only intact-visual normalization removes the embedded `Destroyed` sibling from realized House/Shop intact visuals and fail-closes any remaining co-located pair without mutating source, accepted, frozen, or production prefabs. Exact reconciliation removes 4,396 duplicate packed rows (`82,797 -> 78,401`) and produces 24,904 eligible logical rows, 24,717 selected physical renderers, 52,928 resident physical renderers, 19,614 placements, 9,879 prototypes, 13,595 parts, 1,660 cells, and 3,858 fixed slots. The exact 12x9/9x12 all-grid capacity sweep retains at least 20% per-bucket headroom. Android House and Shop both advanced canonical sequence `1`, used nonzero cells/slots/placements, reported zero overflow/deficit, and visibly changed their logged target from intact to destroyed presentation. House post p95 was 24.951 ms (about 40 FPS) and Shop 33.284 ms (about 30 FPS), so the child state gate passes while final 60 FPS acceptance remains open. Production remains `StaticSceneChunks + ResidentEntities`, `productionCutover=0`, and parent progress remains `127/149 (85%)`. |
| 2026-08-02 | VRP generated Infrastructure expansion | Child `VRP-070`; closed component-ownership policy 46/46; exact inventory; unchanged-input database determinism; two-pass direct bake fingerprint `0a6ebfc7...0724`; final virtualization contract 46/46; `git diff --check` | Passed child Phase 7 coverage step; parent checklist count unchanged | The candidate now virtualizes all generated Infrastructure whose owner hierarchy is render-only, component-safe, and supported by the existing fixed renderer-policy buckets: 38,675 logical rows under 21,868 owners with eligible rows. Another 304 Infrastructure rows remain resident under the existing unsupported-policy exclusion; animation, lights, particles, physics, LOD owners, custom behaviours, special renderers, missing scripts, and property-block ownership fail closed. The deterministic candidate contains 63,579 eligible logical rows, 63,392 selected physical renderers, 41,482 placements, 9,909 prototypes, 13,673 parts, 1,934 cells, and 7,075 fixed slots; direct bake leaves 14,222 packed resident physical rows. Current measured-envelope buckets retain at least 20% headroom and stay below the provisional 8,000-slot limit. The later VRP-075 24,000 packed-`MaterialMeshInfo` gate remains open; no Android acceptance is claimed. Production, accepted/frozen assets, Addressables, Jenkins/Unity paths, `productionCutover=0`, and parent progress remain `127/149 (85%)`. |
| 2026-08-02 | VRP alpha-clipped/transparent policy audit | Child `VRP-071`; corrected checked render-policy contract 46/46; exact material-surface/policy-disposition inventory; `git diff --check` | Passed child Phase 7 classification step; parent checklist count unchanged | The candidate inventory now explicitly reports 74,123 opaque, 3,769 alpha-clipped, and 509 transparent renderer/material rows. All 509 transparent rows request cast/static shadows and therefore remain resident under the existing fail-closed unsupported-policy reason; no transparent pool bucket is materialized. Alpha-clipped rows continue through the fixed `AlphaClippedShadowsOn` bucket when otherwise eligible. Source-row identity, eligibility, database hash `4781c9ab...692c`, 7,075 fixed slots, production/protected assets, `productionCutover=0`, and Android evidence are unchanged. Parent progress remains `127/149 (85%)`; child VRP-072 owns evidence-based LOD/shadow/small-detail tuning. |
| 2026-08-02 | Candidate canal-water presentation correction | Child independent correctness gate; checked Windows GUI-licensing wrapper `%TEMP%\warline-canal-blue-presentation-rerun.log` 2/2 after one rejected exact-float assertion; `git diff --check` | Editor asset contract passed; Android visual confirmation pending; parent checklist count unchanged | The candidate-owned URP material converted from the canonical canal-water source now has an explicit saturated blue base color `(0.025, 0.2, 0.55, 0.82)`. The candidate material converter reapplies the source-path-specific override on regeneration. The focused gate verifies the persisted material and retains transparent surface, queue `3000`, and `RenderType=Transparent`, so no LOD/shadow/bucket policy is changed without VRP-072 Android evidence. Source Synty material, accepted/frozen/production assets, database/capacity, `productionCutover=0`, and parent progress remain `127/149 (85%)`. Android visual confirmation and the separate building/road and missing-bridge defects remain open. |
| 2026-08-02 | Building-road clearance generator correction | Child independent correctness gate; checked focused road bounds 3/3; rejected first full deterministic generator on the prior exact 3,870 open-ground baseline; accepted corrected-layout rerun with 3,539 exact eligible patches, 4,817 reserved building owners, and zero measured road overlaps; source-scene byte preservation; `git diff --check` | Windows/Editor generator passed; persisted-candidate regeneration/bake remains open; parent checklist count unchanged | Standard-road placement exclusion used the grid corner while every rendered road tile uses the cell center, creating a 5 m X/Z mismatch. The shared bounds helper now uses the canonical road-cell center for standard and dirt roads, rejects invalid extents, and the placement audit derives rather than hardcodes its zero-overlap result. The exact open-ground fail-closed count is reconciled to 3,539 for the corrected layout. The accepted generator produced 4,836 semantic buildings, 33,230 surfaces, and 41,865 presentations without changing the source scene. Current persisted candidate scenes/database and Android package remain unchanged, so device visual closure is pending. Production/protected assets, `productionCutover=0`, and parent progress remain `127/149 (85%)`. |
| 2026-08-02 | Building-road transactional candidate/database reconciliation | Child independent correctness gate; rejected/accepted checked candidate regeneration; rejected/accepted exact fresh inventory; deterministic database source parity; two-pass direct ECS bake; persisted blue-canal material audit; `git diff --check` | Windows/Editor persisted candidate and virtualization reconciliation passed; Android package/visual closure pending; parent checklist count unchanged | Forced regeneration now atomically replaces the current-generation candidate scenes and 2,304 surface-proxy partitions while retaining the same generation identity, protected source/config hashes, and rollback behavior. The candidate-only protected-Autobahn route has its exact measured 3,456 open-ground compatibility count, distinct from the unchanged non-Autobahn fixture's exact 3,539 gate. Schema-7 inventory reconciles 4,396 normalized destroyed duplicates plus 1,884 corrected-layout rows against the historical evidence, producing 76,517 total / 61,925 eligible rows, 40,460 placements, 9,107 prototypes, 12,293 parts, 1,934 cells, and 6,804 slots. Two database builds reproduced content hash `798728c5...197f`; two direct bakes reproduced fingerprint `e45bba84...9a1f`, 61,783 selected physical renderers, 14,017 resident rows, and 4,530 building state owners. The regenerated canal material retains the exact blue override. Production/protected assets, `productionCutover=0`, and parent progress remain `127/149 (85%)`; the reported overlap defect is not claimed closed before rebuilt Android visual confirmation. |
| 2026-08-02 | Canonical EntityScene building selection correction and interaction-parity reopening | Child independent correctness gate; accepted host-access checked GUI wrappers `%TEMP%\warline-canonical-building-selection-host.log` 7/7, `%TEMP%\warline-canonical-building-selection-lifecycle.log` 13/13, and `%TEMP%\warline-canonical-building-selection-suite-final.log`; `Tundra build success`; `git diff --check` | Building focus passed on Windows/Editor; authored-building production/interaction row reopened; parent progress `126/149 (85%)` | Android user review exposed that the EntityScene cutover retired legacy placement references while the click helper and soldier-production UI still depended on legacy managed building ownership. The bounded correction adds live canonical buildings to grid/screen selection and permits HUD focus/`SelectedUnitTag` without granting `UnitMove` or movable-cache ownership; existing unit/vehicle selection remains green. Production remains open because the build popup has not yet migrated from managed `RuntimeBuildingEntity` producers to baked `OperationMapBuildingProductionPrefab` ownership, so a visible player Tent cannot yet satisfy the soldier requirement. Device confirmation is pending. The same review reports the Autobahn incorrectly blue; the prior canal material asset contract is therefore insufficient at device level and material isolation remains separate. No candidate database, production/protected asset, Android artifact, Jenkins/Unity path, or `productionCutover=0` changed. |
| 2026-08-02 | Canonical ECS building-producer discovery foundation | Child independent correctness gate; checked Windows GUI-licensing wrappers `%TEMP%\warline-operation-map-producer-lookup.log` 3/3 and `%TEMP%\warline-production-camera-focus-regression.log` 10/10; `git diff --check` | Read-only canonical producer resolution passed; parent progress remains `126/149 (85%)` | The production request helper can now resolve live canonical map producers from baked `OperationMapBuildingProductionPrefab` rows, deterministically preferring the lowest-placement player building before neutral fallback and rejecting prefab, destroyed/dead, missing-prefab, and source-key-mismatch candidates. The resolver is not yet connected to the popup and cannot spend resources or queue/spawn a soldier, so the reopened authored-building production/interaction row stays unchecked. The next bounded step must add an ECS-owned queue/spawn path without recreating managed `RuntimeBuildingEntity` map ownership. Android interaction confirmation, blue-Autobahn material isolation, and the missing bridge remain separate open gates. No candidate database, production/protected asset, Jenkins/Unity path, or `productionCutover=0` changed. |
| 2026-08-02 | Canonical ECS building-production queue contract | Child independent correctness gate; initial checked wrapper `%TEMP%\warline-operation-map-producer-queue.log` 3/3; legacy request regression `%TEMP%\warline-operation-map-production-request-regression.log` 31/31; rejected final wrapper `%TEMP%\warline-operation-map-producer-queue-final.log` timed out after compiler error `CS0177`; corrected final wrapper `%TEMP%\warline-operation-map-producer-queue-final2.log` 3/3; `git diff --check` | Canonical queue ownership passed; parent progress remains `126/149 (85%)` | A canonical building with one or more baked production prefabs now owns an ECS queue state and inline-capacity pending-request buffer. Enqueue records the exact baked prefab/source/index identity, monotonic per-building request id, and finite queue time while enforcing the existing combined legacy/canonical global pending limit and rejecting absent ownership or id overflow. The rejected final run exposed unassigned output values on the new non-finite-time early return; explicit fail-closed defaults corrected the compile and the exact rerun passed. No UI/economy path invokes the queue and no consumer/spawn path exists yet, so the authored-building interaction row remains open and resources cannot be charged for an unconsumable request. Existing managed production behavior is unchanged and its 31-test request regression passes. Android interaction confirmation, blue-Autobahn material isolation, and the missing bridge remain separate. No candidate database, production/protected asset, Jenkins/Unity path, or `productionCutover=0` changed. |
| 2026-08-02 | Canonical ECS production readiness/FIFO consumption | Child independent correctness gate; checked Windows GUI-licensing wrappers `%TEMP%\warline-operation-map-producer-queue-consumer.log` 3/3 and `%TEMP%\warline-operation-map-producer-queue-timing-regression.log` 3/3; `git diff --check` | Canonical ready-time and exactly-once completion passed; parent progress remains `126/149 (85%)` | Canonical pending requests now retain the existing unit-production duration as a finite ready time. The consumer contract exposes only the lowest-id ready request from a live non-destroyed producer, revalidates exact baked prefab/source/index ownership, rejects missing prefab entities, and removes only that same FIFO request on completion. No runtime system, spawn, UI, or economy integration invokes it yet, so the reopened authored-building interaction row remains unchecked and no soldier-production acceptance is claimed. Android interaction confirmation, blue-Autobahn material isolation, and the missing bridge remain separate. No candidate database, production/protected asset, Jenkins/Unity path, or `productionCutover=0` changed. |
| 2026-08-02 | Canonical ECS soldier spawn transaction | Child independent correctness gate; checked Windows GUI-licensing wrapper `%TEMP%\warline-operation-map-producer-spawn-transaction.log` 3/3; `git diff --check` | Explicit canonical unit materialization passed; parent progress remains `126/149 (85%)` | A ready canonical FIFO request can now instantiate its exact baked unit prefab at a caller-supplied valid cell/position, initialize unit grid/transform/faction/source/runtime state, remove transient path/target/selection state, and consume the request only after successful configuration. Early, invalid-placement, invalid-unit-prefab, and completion-mismatch paths preserve the request and cannot leave a provisional unit. Runtime scheduling and valid spawn-cell ownership are not wired, and UI/economy still cannot invoke the path, so the authored-building production/interaction row remains unchecked and Android acceptance is not claimed. Blue-Autobahn isolation and the missing bridge remain separate. No candidate database, production/protected asset, Jenkins/Unity path, or `productionCutover=0` changed. |
| 2026-08-02 | Canonical ECS occupancy-aware production spawn-cell resolution | Child independent correctness gate; rejected checked wrapper `%TEMP%\warline-operation-map-producer-spawn-cell.log` timed out after test-harness compiler errors `CS1654`/`CS1657`; corrected checked wrappers `%TEMP%\warline-operation-map-producer-spawn-cell-rerun.log` and final `%TEMP%\warline-operation-map-producer-spawn-cell-final.log` 3/3; spawn-transaction regression `%TEMP%\warline-operation-map-producer-spawn-transaction-regression.log` 3/3; `git diff --check` | Deterministic canonical spawn-cell ownership passed; parent progress remains `126/149 (85%)` | A ready canonical producer request can now resolve a unit-footprint placement deterministically just outside its producer footprint through the shared grid utility. The contract requires exact finite grid ownership and exact walkable/blocked/occupied/reserved masks, rejects malformed inputs, and cannot choose blocked, occupied, reserved, unwalkable, out-of-bounds, or producer-overlapping cells; the producer-footprint safeguard preserves pre-existing reservation bits. Explicit disposal corrected the first run's test-only `using`/`ref` compile issue; the focused final gate and prior transaction regression pass. No runtime scheduler supplies live masks or invokes the resolver and spawn transaction, and the popup/economy path is still disconnected, so the authored-building interaction row remains unchecked and Android acceptance is not claimed. Blue-Autobahn isolation and the missing bridge remain separate. No candidate database, production/protected asset, Jenkins/Unity path, or `productionCutover=0` changed. |
| 2026-08-02 | Canonical ECS production runtime scheduler | Child independent correctness gate; rejected checked wrapper `%TEMP%\warline-operation-map-production-runtime-scheduler.log` on compile-only `CS1654`; corrected checked wrapper `%TEMP%\warline-operation-map-production-runtime-scheduler-rerun.log` 3/3; `git diff --check` | Runtime ready-request scheduling passed; parent progress remains `126/149 (85%)` | The building runtime processor now drains ready canonical production queues from exactly one live grid owner in deterministic placement/entity/request order, resolves a shared reservation set before structural changes, caps work at 16 spawns per update, and invokes the accepted spawn transaction. Focused coverage proves one ready request materializes exactly once outside its producer, early requests remain queued, and ambiguous grid ownership fails closed without consuming the request. Explicit native-list disposal corrected the first run's compile-only deterministic-sort declaration. The popup/economy contract still cannot enqueue this ECS path, so the authored-building interaction row remains unchecked and Android soldier-production acceptance is not claimed. Blue-Autobahn isolation and the missing bridge remain separate. No candidate database, production/protected asset, Jenkins/Unity path, or `productionCutover=0` changed. |
| 2026-08-02 | Build-drawer canonical Tent production bridge | Child independent correctness gate; checked Windows wrapper `%TEMP%\warline-operation-map-camp-production-bridge.log` 3/3; managed production regression `%TEMP%\warline-operation-map-camp-production-regression.log` 31/31; `git diff --check` | Canonical producer preflight and economy-safe enqueue passed; parent progress remains `126/149 (85%)` | The camp/build drawer now treats a live friendly canonical building with the exact baked production prefab/source as a valid producer when no managed producer exists. That fallback spends once, writes one ECS request, refunds a rejected enqueue, records only successful orders, and includes canonical plus legacy pending rows under the existing global queue limit. Focused tests cover preflight, spend-once enqueue, and limit-before-spend; all 31 existing managed request tests pass. Together with the accepted runtime scheduler this closes the Windows/Editor request-to-spawn code path, but the authored-building interaction row remains unchecked until a rebuilt Android package proves the popup recognizes the visible Tent and the produced soldier appears. Blue-Autobahn isolation and the missing bridge remain separate. No candidate database, production/protected asset, Jenkins/Unity path, or `productionCutover=0` changed. |
| 2026-08-02 | Canonical Tent Android interaction rerun | Child independent correctness gate; rejected long-path package on Gradle/Ninja path length; rejected first `W:` short-path wrapper after one ARM64 compile exceeded its timeout; accepted exact short-path retry `%TEMP%\warline-canonical-tent-android-profiler-apk-shortpath-retry.log`; APK SHA-256 `93e8c063...c52dcc`; streamed install and exact-activity cold launch on paired Xiaomi `24090RA29G`; main-menu Deploy; Build/Soldiers/Recruit; 55-second observation | Canonical Tent prerequisite and economy-safe request passed; visible completion rejected; parent progress remains `126/149 (85%)` | The fresh 696,278,899-byte profiler APK passed the checked package marker, installed successfully, and cold-launched in `1,088 ms`. The main-menu Deploy route opened the playable base. The visible canonical Tent now satisfies unit preflight: Recruit stayed enabled, materials changed exactly `120 -> 95`, and the drawer reported the Advanced Rifleman request added. The same drawer showed `NO PRODUCTION QUEUED`, and after 55 seconds the five-card visible roster was unchanged, so the produced soldier did not receive device-visible acceptance. The reopened authored-building production/interaction row remains unchecked; no spawn or selection claim is made. Instantaneous on-screen diagnostics were `36 FPS` initially and `46 FPS` after waiting, below target but not a formal Phase 9 profile. Blue-Autobahn isolation and the missing bridge remain separate. No candidate database, production/protected asset, Jenkins/Unity path, or `productionCutover=0` changed. |

| 2026-08-02 | Canonical production authored-grid selection correction | Child independent correctness gate; initial checked batch/GUI runs rejected on the Hub `1.17.4` / Editor `1.18.1` IPC mismatch; verified wrapper-only Editor ownership, stale licensing/Hub recovery, and accepted host-access checked GUI wrapper `%TEMP%\warline-operation-map-production-grid-selection-escalated.log` 4/4; `git diff --check` | Windows/Editor scheduler ownership passed; parent progress remains `126/149 (85%)` | The Android rejection proved canonical Tent preflight, spend, and enqueue while visible completion failed. The runtime scheduler had required exactly one complete grid entity, so an authored grid coexisting with its temporary `RuntimeGridBootstrapGridTag` fallback suppressed all ready canonical requests. The scheduler now prefers the sole complete non-bootstrap grid, uses exactly one complete fallback only when no authored grid exists, and continues to reject multiple authored grid owners without consuming the request. Focused tests cover ready spawn, early preservation, authored-over-bootstrap selection, and ambiguous-authored rejection. The queue drawer remains legacy-only and Android has not been rebuilt for this correction, so the authored-building production/interaction row stays unchecked. Oversized building selection marker, vehicle selection, blue Autobahn, canal confirmation, and missing bridge remain separate. No candidate database, protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |
| 2026-08-02 | Candidate runtime-binding legacy component identity and fresh Android package | Child independent correctness gate; rejected binding reload/package diagnostics; accepted checked dense layout `%TEMP%\warline-dense-candidate-runtime-binding-rebind-corrected.log`; accepted binding/no-op/rollback suite `%TEMP%\warline-candidate-runtime-binding-reuse-corrected.log` 23/23; accepted checked profiler package `%TEMP%\warline-canonical-tent-authored-grid-profiler-apk-repaired.log`; APK SHA-256 `0b578c22...e269`; `git diff --check` | Windows/Editor binding and package passed; parent progress remains `126/149 (85%)` | The serialized thin binding retained a decoration-baker file ID, but Unity loaded the typed reference as null because `CombinedMeshBaker` named the wrong legacy source assembly in `MovedFrom`. Correcting that identity to the original global `Assembly-CSharp` owner restores the component; candidate regeneration additionally requires exactly one baker on the map-owned decoration root and explicitly rebinds it before the unchanged reload validator. The fail-closed diagnostic now reports each missing reference. Both candidate bindings pass the complete focused suite, and a fresh 696,278,899-byte package containing the authored-grid scheduler fix passes the isolated candidate package gate. The discovered Xiaomi endpoint `192.168.2.171:39049` refused ADB, so device install/Deploy/Tent spawn acceptance remains pending and the authored-building production/interaction row stays unchecked. Marker scale, vehicle selection, blue Autobahn/canal isolation, and the missing bridge remain separate work. No candidate database, protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |
| 2026-08-02 | Repaired Tent APK Android completion rerun | Child independent correctness gate; exact checked 696,278,899-byte profiler APK SHA-256 `0b578c22...e269`; bundled-SDK ADB on paired Xiaomi `24090RA29G`; streamed install; exact-activity cold launch; main-menu Deploy; `%TEMP%\warline-fresh-tent-recruit-queued.png`; `%TEMP%\warline-fresh-tent-closed.png`; Android `FrameRateDiag` | Recruit acknowledgement passed; visible/canonical completion rejected; parent progress remains `126/149 (85%)` | The repaired package cold-launched in `835 ms` (`842 ms` wait), reached complete candidate readiness through the required Deploy route, and the canonical Contractor Tent enabled the Soldiers/Recruit flow. Heavy Gunner Male I produced the success acknowledgement, but the displayed materials stayed `120/60`, the legacy queue panel stayed empty, and after more than the advertised `00:45` the canonical runtime count remained exactly `5005` units/source keys with no visible new soldier. The authored-grid scheduler and binding corrections therefore still do not receive Android end-to-end acceptance; the authored-building production/interaction row remains unchecked. One selected Tent frame showed a footprint-bounded marker, which is insufficient to close the reported oversized marker on other buildings; vehicle selection, blue Autobahn/canal isolation, and the missing bridge remain independently open. Observed performance was roughly `39-50 FPS` with GPU samples around `18-27 ms`, below target and not a formal Phase 9 route. No candidate database, protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |
| 2026-08-02 | Canonical production scheduler stall diagnostics | Child independent correctness gate; checked Windows GUI-licensing wrapper `%TEMP%\warline-operation-map-production-scheduler-diagnostics-rerun.log` 5/5; `git diff --check` | Bounded diagnostic contract passed; Android root cause remains pending; parent progress remains `126/149 (85%)` | The canonical scheduler no longer reduces every zero-spawn path to an unobservable return value. It reports a closed outcome plus producer, pending, ready, authored/complete-grid, resolved, and spawned counts, avoids grid queries when no canonical request is pending, preserves normal not-ready requests silently, and rate-limits only genuinely ready stalls to one warning per five seconds per stable signature. The focused gate retains all four prior scheduler cases and proves exact ambiguous-grid diagnostics and throttling. A fresh Android build/run is still required to classify the repaired Tent failure before changing spawn policy, so the authored-building production/interaction row stays unchecked. The newly reported incorrect Tent selection-panel portrait is tracked separately from production, marker scale, vehicle selection, Autobahn/canal isolation, shadows, and the missing bridge. No candidate database, protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |
| 2026-08-02 | Candidate binding normalization and canonical Tent Android transport rejection | Child independent correctness gate; checked short-path profiler package `%TEMP%\warline-canonical-tent-scheduler-diagnostics-profiler-apk-shortpath.log`; `[DenseCityCandidateAndroidPackage] result=Passed`; exact 696,278,899-byte APK SHA-256 `cb24795d...e012d`; bundled-SDK ADB to paired Xiaomi `24090RA29G`; streamed install; exact-activity cold launch; main-menu Deploy; Build/Soldiers/Recruit; retained screenshots under `Build/AndroidEvidence/TentSchedulerBinding`; Android `FrameRateDiag`; `git diff --check` | Candidate binding and direct canonical completion passed; helicopter/drop presentation rejected; parent progress remains `126/149 (85%)` | The candidate-only layout builder now requires exactly one recognized serialized `CombinedMeshBaker` identity and restores the legacy global `Assembly-CSharp` identity before import, allowing a cold checked package build to bind the retained component without mutating the production scene. On Android, the canonical Contractor Tent became selectable and enabled Recruit; one Advanced Rifleman request consumed materials `120 -> 95` and raised stable canonical units/source keys `5005 -> 5006`, rendered models `22 -> 23`, and impostors `4 -> 5`. The required helicopter never arrived and no rope/drop delivery was visible: the canonical ECS scheduler directly materializes a ready request instead of entering the legacy managed transport presenter. The authored-building production/interaction row therefore remains unchecked and the next bounded step must provide transport/drop presentation without recreating managed map ownership. The same run separately reproduces the wrong Tent portrait and an oversized cyan selection footprint; focused/base performance ranged roughly `19-46 FPS`, which is retained for the later VRP-072 gate. No candidate database, protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |
| 2026-08-02 | Canonical Tent transport-presentation bridge | Child independent correctness substep; user-directed normal Unity GUI validation; `[CanonicalTentTransportPresentationValidation] result=Passed transportVisible=1 ropeVisible=1 dropVisualVisible=1 ownership=canonical`; visible `Build/EditorEvidence/CanonicalTentTransportDrop.png`; GUI scheduler regression `[OperationMapProductionRuntimeSchedulerValidation] result=Passed tests=6`; `git diff --check` | Canonical presentation component and scheduler deferral passed; parent progress remains `126/149 (85%)` | The ready canonical Tent request now enters a pooled helicopter delivery session rather than materializing immediately. The presenter visibly arrives, exposes a rope and descending unit visual, reports in-progress without consuming the canonical request, then releases the existing ECS spawn transaction only after drop completion and departs. Scheduler diagnostics now represent this expected wait as `DeliveryInProgress` instead of a false rejection warning. Focused Editor evidence and a six-test scheduler regression pass, but the complete map UI route from selecting the authored Tent through Recruit and visible completion has not yet been exercised in Editor. Therefore the unit-production acceptance row remains unchecked, Android stays deferred, and selection portrait, marker scale, vehicle selection, spawned-unit interaction, and building placement remain separate. No candidate database, package, protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |
| 2026-08-02 | Full in-map canonical Tent delivery acceptance | Child independent correctness gate; rejected checked GUI-wrapper attempts before project code on the generic licensing-channel timeout; accepted user-directed Hub-opened Unity GUI route `Tools/Validation/Canonical Tent Full In-Map Recruit`; global Editor log `[CanonicalTentProductionPlayModeValidation] result=Passed`; visible `Build/EditorEvidence/CanonicalTentFullInMapRecruitDrop.png`; `git diff --check` | Editor-first unit-production row accepted; parent progress remains `126/149 (85%)` | The validation exercises the actual Menu Deploy control and the actual in-map Build, Soldiers, and Recruit controls after the authored map and canonical unit population stabilize. It observes the canonical pooled helicopter, requires the live rope to extend at least `3 m` with the drop visual active before evidence capture, and accepts only after `UnitSourcePrefabKey` count increases exactly `460 -> 461`. The helicopter/rope/drop/completion row is now checked. Spawned-unit visibility and normal selection remain a distinct unchecked row, as do portrait mapping, marker footprint, vehicle selection, and building placement; Android packaging remains deferred until all six pass. No candidate database, package, protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |
| 2026-08-02 | Spawned canonical soldier visibility and normal selection acceptance | Child independent correctness gate; user-directed Hub-opened Unity GUI route `Tools/Validation/Canonical Spawned Soldier Interaction`; global Editor log `[CanonicalSpawnedSoldierInteractionPlayModeValidation] result=Passed`; visible `Build/EditorEvidence/CanonicalSpawnedSoldierSelected.png`; `git diff --check` | Editor-first spawned-unit visibility/interaction row accepted; parent progress remains `126/149 (85%)` | The real Deploy and Build/Soldiers/Recruit path starts from a stable `460`-unit baseline and identifies the newly completed canonical soldier by entity identity. Acceptance requires canonical ownership to reach `461`, an enabled visible ECS render tree, a camera-visible centered world position, and actual `RIFLE SQUAD` card interaction that gives the exact new entity `SelectedUnitTag`, focused-unit read-model ownership, and a visible selection panel. The spawned-unit visibility/interaction row is now checked. Portrait mapping, marker footprint, vehicle selection, and building placement remain distinct unchecked rows, so Android packaging is still deferred. No runtime code/content, candidate database, package, protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |

| 2026-08-02 | Contractor Tent selection-panel portrait acceptance | Child independent correctness gate; user-directed Hub-opened Unity GUI route `Tools/Validation/Canonical Contractor Tent Portrait`; global Editor log `[CanonicalContractorTentPortraitPlayModeValidation] result=Passed`; visible `Build/EditorEvidence/CanonicalContractorTentSelectionPortrait.png`; selected managed owner `id=9`, prefab `Tent_Contractor`, title `Contractor Tent (911,371)`, exact configured action portrait `Portrait_Building_Tent_Contractor_Action_512`; `git diff --check` | Editor-first selection-panel image row accepted; parent progress remains `126/149 (85%)` | The gate follows the actual Menu Deploy and normal managed building-click path, explicitly avoiding the invalid assumption that current visible runtime buildings expose `OperationMapBuildingComponent`. The selected owner, displayed title, source prefab, and exact selection-summary sprite all reconcile to Contractor Tent, so the previously reported wrong Tent portrait is not reproduced in the fresh Editor route. The captured cyan marker is still visibly oversized and remains a separate unchecked row; vehicle selection and building placement are also still pending, and Android packaging remains deferred until those Editor gates pass. No runtime code/content, candidate database, package, protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |
| 2026-08-02 | Canonical building selection-marker footprint acceptance | Child independent correctness gate; rejected checked batch wrapper `%TEMP%\warline-building-selection-marker-focused.log` on the known generic licensing timeout and closed only its exact wrapper-owned Editor PID after 600 seconds; accepted Hub-opened GUI focused route `Tools/Validation/Building Selection Marker Focused` `[BuildingSelectionMarkerFocusedValidation] result=Passed tests=8`; accepted real Deploy/click route `Tools/Validation/Canonical Building Selection Marker` `[CanonicalBuildingSelectionMarkerPlayModeValidation] result=Passed`; visible `Build/EditorEvidence/CanonicalBuildingSelectionMarkerFootprint.png`; `git diff --check` | Editor-first marker-footprint row accepted; parent progress remains `126/149 (85%)` | Map-authored selection no longer derives planar center or size from aggregate renderer bounds. The cyan ground frame is owned by canonical origin, footprint, grid origin, and cell size; renderer bounds remain limited to vertical placement/frame height and the object outline. A deliberately broad `30 x 18` renderer regression stays bounded to its `4 x 3` gameplay footprint. The full Menu Deploy route selects Contractor Tent owner `9`, reconciles origin `(911,371)` and footprint `11 x 13`, and proves marker center `(916.5,377.5)` plus configured-prefab scale `(10.72,1,12.67)` before capturing evidence. Vehicle world/roster selection and building placement remain separate unchecked rows; Android packaging remains deferred. No candidate database, package, protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |

| 2026-08-02 | Canonical vehicle world and roster selection acceptance | Child independent correctness gate; user-directed Hub-opened Unity GUI route `Tools/Validation/Canonical Vehicle World And Roster Selection`; global Editor log `[CanonicalVehicleSelectionPlayModeValidation] result=Passed`; visible `Build/EditorEvidence/CanonicalVehicleWorldSelection.png` and `Build/EditorEvidence/CanonicalVehicleRosterSelection.png`; exact target `Entity(674:3)` / `Unit_Veh_Tank_USA`; `git diff --check` | Editor-first vehicle world/roster row accepted; parent progress remains `126/149 (85%)` | The real Menu Deploy route stabilizes seven selectable player combat vehicles and centers on a named player battle tank. Its projected world point traverses the normal runtime focus-command queue and must resolve exactly one selected entity, the same focused-unit read model, the visible Battle Tank panel, and a visible cyan outline. The validator next invokes the actual `ARMOR` roster card and requires two selected canonical player ground-combat vehicles, including the same tank, plus the visible `2 VEHICLES` panel and both outlines. The reported Editor selection failure is not reproduced in this fresh canonical route; no gameplay policy is changed. Building placement is the only remaining Editor-first row, so Android packaging remains deferred. No runtime code/content, candidate database, package, protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |
| 2026-08-02 | Canonical building placement/spawn acceptance | Child independent correctness gate; user-directed Hub-opened Unity GUI route `Tools/Validation/Canonical Building Placement And Spawn`; global Editor log `[CanonicalBuildingPlacementSpawnPlayModeValidation] result=Passed`; visible `Build/EditorEvidence/CanonicalBuildingPlacementSpawn.png`; selected owner `Barracks (883,376)`, runtime id `434`, footprint `8 x 14`; `git diff --check` | Final Editor-first functional row accepted; parent progress remains `126/149 (85%)` | The real Menu Deploy and Build/Buildings/Place/Confirm route now registers the player's placed building with canonical player ownership through a placement-specific handoff. The gate identifies the exact Barracks catalog result while unrelated runtime additions continue, then requires owner faction `1`, the canonical `8 x 14` footprint, active presentation, `RuntimeBuildingEntityLink`, normal world-click selection, exact active-owner identity, and a visible populated Barracks panel. Visual evidence confirms the placed Barracks and selection presentation. All six Editor-first functional rows now pass, so Android final functional confirmation and performance iteration are dependency-ready but were not run here. No candidate database, package, protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |
| 2026-08-03 | Fresh Editor-accepted Android profiler package | Child independent correctness gate; Hub-opened Unity GUI route `Tools/Validation/Build Dense City Candidate Android Profiler APK`; rejected first GUI build on Windows `MAX_PATH`; bounded workspace-local `GRADLE_USER_HOME=.g`; rejected cached `.utmp` CMake path and stale generated `compressed_assets`; exact generated-output cleanup after worker/process verification; `[DenseCityCandidateAndroidPackage] result=Passed`; exact APK file inspection; bundled-SDK ADB and mDNS probe; `git diff --check` | Fresh current-state package passed; Android final functional confirmation pending; parent progress remains `126/149 (85%)` | The new GUI-only package entry preserves the existing candidate package gate while shortening only Gradle's generated cache root and restoring the prior environment afterward. The accepted build produced `Build/AndroidDenseCandidateProfiler/WarlineCapture-DenseCityCandidate-Profiler.apk`, exactly `696,278,899` bytes with SHA-256 `0ce4dfd1c3a773b8f2aa216505fccd06c4cd954e8160d0af4e2d91a7cfddcdc1`; regenerated Addressables layout/runtime-content evidence reconciles the fresh payload. No ADB device or mDNS service was visible, so install, main-menu Deploy, and final confirmation of the six Editor-accepted behaviors did not run and are not claimed. Blue Autobahn/canal isolation, the missing bridge, and VRP-072 performance policy remain separate. No candidate database, protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |
| 2026-08-03 | Samsung Android final-functional rejection | Child independent correctness gate; exact accepted 696,278,899-byte APK SHA-256 `0ce4dfd1...ddcdc1`; bundled-SDK wireless ADB to Samsung `SM-S918B` / `R5CTC1J02VB`; streamed install; exact-activity cold launch; first-run English/Skip; main-menu Deploy; visible screenshots under `Build/AndroidDenseCandidateProfiler/Evidence`; Android `FrameRateDiag` and selection-hitch diagnostics | Rejected; Editor acceptance remains valid, Android functional closure remains open, parent progress remains `126/149 (85%)` | The exact package installed and cold-launched `com.warlinecapture.game/com.unity3d.player.UnityPlayerGameActivity` in 499 ms, then reached the dense tactical map through the visible Deploy control. Soldier Tent and Contractor Tent/build previews exposed owner-specific labels and images, but Android did not reproduce the complete six-row contract. The Armor roster card and a visible world-vehicle hit produced no visible selected state. Selecting the central Barracks produced a cyan marker/selection region far larger than its visual footprint; that oversized region later won hit tests over the newly placed Contractor Tent. Contractor Tent placement itself accepted a valid `13x11` preview, spent materials exactly `120/60 -> 100/60`, displayed the expected construction image, and increased runtime diagnostic `units/sourceKeys` from the stable `5005` baseline to `5006`. After more than the declared 30-second duration, however, the selected structure still had no health/completed state or Recruit control; after deselection its visible model could not be reselected because the Barracks region was focused instead. Helicopter/rope/drop production and spawned-soldier interaction therefore could not be exercised and are not accepted. Samsung performance ranged about 38-55 FPS, with GPU samples around 12-17 ms and a 585.6 ms selection/update hitch, so 60 FPS is also not accepted. The six Editor rows remain checked as Editor evidence only; VRP-072 remains unstarted until this functional rejection is fixed or explicitly deferred. Blue Autobahn/canal isolation and the missing bridge remain separate. No runtime code/content, candidate database, protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |

| 2026-08-03 | Android-rejected building marker, portrait, and click-ownership Editor repair | Child independent correctness gate; Hub-opened Unity GUI `Tools/Validation/Canonical Building Selection Marker`; `[CanonicalBuildingSelectionMarkerPlayModeValidation] result=Passed`; visible `Build/EditorEvidence/CanonicalBarracksSelectionPresentation.png` and `Build/EditorEvidence/CanonicalContractorTentClickOwnership.png`; focused GUI regression `[BuildingSelectionMarkerFocusedValidation] result=Passed tests=8`; independent GUI portrait gate `[CanonicalContractorTentPortraitPlayModeValidation] result=Passed`; visible `Build/EditorEvidence/CanonicalContractorTentSelectionPortrait.png`; `git diff --check` | Editor repair accepted; fresh checked APK and affected Android rerun required; parent progress remains `126/149 (85%)` | The former Editor acceptance did not cover the Samsung failure: its marker test asserted gameplay footprints and its click path did not prove that a smaller visible building wins over a nearby broad selection area. Runtime building definitions now carry exact configured selection/card sprites, and selection UI resolves the chosen definition's action sprite before late prefab/instance fallbacks. Map-authored markers use transformed presentation bounds for center and planar size, while direct visual-hit ownership is resolved first and independently from gameplay-cell fallback. The real Deploy gate selects Barracks `18`, proves its exact `Portrait_Building_Barrack_Action_512` and bounded marker, then deliberately supplies Barracks origin `(869,396)` while clicking visible Contractor Tent `9`; the Tent owns the resulting marker/panel at `(911,371)` and independently passes exact `Portrait_Building_Tent_Contractor_Action_512`. The rejected Samsung APK was not rerun and is stale after this code change. Vehicle world/roster selection, Contractor completion/Recruit, helicopter/drop, and spawned-unit interaction remain separate Android gates. No candidate database, protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |
| 2026-08-03 | Fresh post-repair Samsung marker/portrait/hit rejection | Child independent correctness gate; checked Windows GUI-licensing package wrapper `%TEMP%\warline-selection-presentation-profiler-apk.log`; `[DenseCityCandidateAndroidPackage] result=Passed`; exact 696,278,899-byte APK SHA-256 `21c1201c40a43b4aa8cc60f2f242a2aff0998eea614eaef35834b48cc588ee44`; bundled-SDK wireless ADB to Samsung `SM-S918B` / `R5CTC1J02VB`; streamed install; device-side size/hash reconciliation; exact-activity 430 ms cold launch; correct main-menu Deploy route; `Build/AndroidDenseCandidateProfiler/Evidence/2026-08-03-selection-fix/main-menu-deploy.png`, `barracks-selected.png`, and `nearby-click.png` | Rejected; packaged static-reuse path remains outside the prior Editor acceptance; parent progress remains `126/149 (85%)` | The exact fresh package installed and reached live gameplay through the correct Deploy route, but the affected gate still failed. Barracks selection retained a cyan frame far larger than the building, a tap on the nearby visible tent retained Barracks ownership, and the periodically refreshed panel showed the generic building portrait instead of `Portrait_Building_Barrack_Action_512`. The package uses a map-authored managed owner with no child renderer while static ECS presentation remains visible; the prior Editor gate instead exercised a managed child-renderer hierarchy. Aggregate `BuildingDefinition.LocalBounds` therefore still enlarged both marker and hit presentation, while periodic HUD refresh bypassed the selected runtime definition's exact sprite. No vehicle, Contractor completion/Recruit, helicopter/drop, or spawned-unit gate was rerun after this rejection. Samsung samples were about 20 FPS during initial load and 54-55 FPS after stabilization; VRP-072 remains unstarted. No candidate database, protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |
| 2026-08-03 | Static-reuse building selection presentation Editor acceptance | Child independent correctness gate; checked GUI-licensing wrappers `%TEMP%\warline-static-reuse-shared-renderer-marker.log` `[BuildingSelectionMarkerFocusedValidation] result=Passed tests=10` and `%TEMP%\warline-static-reuse-nearest-center-runtime-rerun.log` `[RuntimeBuildingSystemFocusedValidation] result=Passed tests=7`; Hub-opened D3D11 `Tools/Validation/Canonical Building Selection Marker`; `[CanonicalBuildingSelectionMarkerPlayModeValidation] result=Passed`; freshly inspected `Build/EditorEvidence/CanonicalBarracksSelectionPresentation.png` and `Build/EditorEvidence/CanonicalContractorTentClickOwnership.png`; `git diff --check` | Editor repair accepted; one fresh checked APK and affected Samsung gate rerun required; parent progress remains `126/149 (85%)` | Map-authored owners now retain their baked presentation center and always size the cyan frame from the canonical gameplay footprint; aggregate local bounds and packed faction renderers contribute no planar hit ownership. Direct-hit rectangles for static-reuse owners likewise use the baked center plus canonical footprint even when several owners reference one packed renderer. Overlapping visible candidates prefer the baked screen center nearest the tap before deterministic area/depth/id ties. The strengthened regression shares one broad renderer across Barracks, Contractor Tent, and a smaller overlapping decoy; the exact Tent-center click still selects the Tent. The full Deploy route visibly proves Barracks `18` with `Portrait_Building_Barrack_Action_512`, then deliberately submits Barracks cell `(869,396)` while clicking Contractor Tent `9`; the Tent owns the bounded marker at its baked center and shows its distinct action portrait. Periodic HUD refresh now keeps the selected runtime definition's exact sprite. Android has not been rerun for this revision; vehicle selection, Tent completion/Recruit, helicopter/drop, and spawned-unit interaction remain separate. VRP-072 remains unstarted. No candidate database, protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |
| 2026-08-03 | Static-reuse Samsung affected-gate rejection | Child independent correctness gate; Hub-opened D3D11 GUI package route `Tools/Validation/Build Dense City Candidate Android Profiler APK`; `[DenseCityCandidateAndroidPackage] result=Passed`; exact 696,278,899-byte APK SHA-256 `e6644876bd68ece1a40d50b7ff28de63a3723dd0884afa372ffb1fb1bda41ff5`; bundled-SDK wireless ADB to Samsung `SM-S918B` / `R5CTC1J02VB` at `192.168.2.33:44569`; streamed install; exact-activity 515 ms cold launch; correct main-menu Deploy; `Build/AndroidDenseCandidateProfiler/Samsung-fresh-barracks.png` and `Samsung-fresh-nearby.png` | Rejected; packaged marker and visual-hit ownership remain open; parent progress remains `126/149 (85%)` | The exact fresh post-`2af246d14` package reached the dense tactical map, but central Barracks selection still drew a cyan frame across a large road-and-building region. Tapping the neighboring beige Tent area kept Barracks selected and enlarged the frame, proving the packaged static-reuse path still captures the nearby hit despite the strengthened Editor regressions. The visible Barracks portrait remained populated and consistent; marker presentation and click ownership nevertheless reject this bounded gate. No other Android functional row was rerun. VRP-072 remains unstarted; blue Autobahn/canal isolation and the missing bridge remain separate. No candidate database, protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |
| 2026-08-03 | Exact placement-prefab static-reuse selection Editor acceptance | Child independent correctness gate; rejected checked GUI-licensing wrapper attempts `%TEMP%\warline-placement-prefab-marker.log`, `%TEMP%\warline-placement-prefab-geometry.log`, and `%TEMP%\warline-static-reuse-selection-geometry.log` on licensing or shutdown fail-closed behavior despite later pass markers; user-directed Hub-opened DX11 GUI `Tools/Validation/Static Reuse Selection Geometry Focused`; global Editor log `[StaticReuseSelectionGeometryValidation] result=Passed placementTests=5 markerTests=10 runtimeTests=7`; real Deploy `[CanonicalBuildingSelectionMarkerPlayModeValidation] result=Passed`; freshly inspected `Build/EditorEvidence/CanonicalBarracksSelectionPresentation.png` and `Build/EditorEvidence/CanonicalContractorTentClickOwnership.png`; `git diff --check` | Editor repair accepted; one fresh checked APK and only the affected Samsung gate rerun required; parent progress remains `126/149 (85%)` | The previous Editor fixture and packaged implementation both used broad canonical gameplay footprints for static-reuse presentation. Map-authored owners now derive exact visual planar size and center from their referenced placement prefab renderer-local bounds, retain placement yaw, and do so without cloning the prefab. Marker rendering and direct screen-hit ownership use this exact oriented presentation geometry; canonical cells remain authoritative for gameplay occupancy and as a fail-closed geometry fallback. Focused coverage proves no-child static reuse retains exact geometry, the Barracks marker uses `10.72 x 12.67` at `90 degrees`, and a nearby Tent wins despite a deliberately huge Barracks gameplay footprint and shared renderer. The real Deploy evidence retains exact Barracks/Tent portraits and independent bounded markers. Android has not been rerun for this revision; vehicle selection, Tent completion/Recruit, helicopter/drop, and spawned-unit interaction remain separate. VRP-072 remains unstarted; blue Autobahn/canal isolation and the missing bridge remain separate. The generated runtime-content report and recovery scene remain untouched, and no protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |
| 2026-08-03 | Exact placement-prefab Samsung affected-gate rejection | Child independent correctness gate; user-directed Hub-opened DX11 GUI `Tools/Validation/Build Dense City Candidate Android Profiler APK`; `Build Finished, Result: Success`; `[DenseCityCandidateAndroidPackage] result=Passed`; exact 758,603,643-byte APK SHA-256 `002eeb0f41f361ce6ba92ccc48172db41b51a2dfc1a4bed40c77857dd86e0cec`; bundled-SDK wireless ADB to Samsung `SM-S918B` / `R5CTC1J02VB` at `192.168.2.33:44569`; streamed install; device-side size/hash equality; exact-activity 408 ms bounded retry cold launch; correctly scaled main-page Deploy; `Build/AndroidDenseCandidateProfiler/Evidence/2026-08-03-exact-geometry-fix/barracks-test.png` and `nearby-tent-test.png`; Android frame/hitch diagnostics | Rejected; packaged marker and nearby visual-hit ownership remain open; parent progress remains `126/149 (85%)` | The fresh post-`4d28bca0e` package installed byte-identically and reached the live dense map through the main-page Deploy control. The first unscaled tap entered Skirmish Setup and a later Launch Mission remained in pregame with `initializedSpawnConfigs=0`; one ordinary cold-launch retry used the verified physical-to-render coordinate scale and the main-page Deploy control entered live gameplay directly. Selecting the central Barracks still drew a cyan frame across the adjacent road and multiple structures rather than tightly matching the visible Barracks. A direct tap on the beige Tent inside that region left the Barracks portrait, panel, and oversized marker unchanged, so exact placement-prefab bounds as currently calculated do not fix packaged static reuse. Selection samples exposed 256.5 ms and 322.8 ms update hitches; stable samples were about 53.5-55 FPS with GPU around 16.5-17.4 ms. No other Android functional row was rerun or accepted. VRP-072 remains unstarted; blue Autobahn/canal isolation and the missing bridge remain separate. The generated runtime-content report and recovery scene remain unstaged, and no protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |
| 2026-08-03 | Owned-renderer fallback Editor acceptance and Samsung rejection | Child independent correctness gate; Hub-opened GUI aggregate `[StaticReuseSelectionGeometryValidation] result=Passed placementTests=5 markerTests=10 runtimeTests=7`; GUI-only `Tools/Validation/Static Reuse Owned Renderer Fallback Focused` `[StaticReuseOwnedRendererFallbackValidation] result=Passed tests=2`; implementation commit `c98b6f1ca`; Hub-opened GUI `Tools/Validation/Build Dense City Candidate Android Profiler APK`; `[DenseCityCandidateAndroidPackage] result=Passed`; exact 758,603,643-byte APK SHA-256 `7f6623da57ba4cc27060d9421204fd30af11f7da9d90bba1109c5b34ea0628c2`; wireless install to Samsung `SM-S918B` / `R5CTC1J02VB`; exact-activity 601 ms cold launch; correctly scaled main-page Deploy; evidence under `Build/AndroidDenseCandidateProfiler/Evidence/2026-08-03-owned-renderer-fallback` | Editor fallback accepted but affected Android gate rejected; parent progress remains `126/149 (85%)` | The repair aggregates only active renderers owned beneath the selected map-authored building, rejects implausibly broad/shared aggregates, and excludes selection-outline renderers; marker presentation and direct-hit ownership share that plausible fallback before gameplay-footprint fallback. The fresh package installed successfully and entered live gameplay through the main-page Deploy control after an initial unscaled tap merely opened Skirmish Setup and the visible Back control returned to the main page. Barracks selection still produced an oversized cyan frame across the road and adjacent presentation. The subsequent nearby beige Tent tap left the Barracks portrait, panel, and frame unchanged, so packaged runtime geometry still bypasses or rejects the new owned-renderer source. The next bounded item is one-shot runtime diagnostics for exact selected owner, presentation flags, renderer count/aggregate, rejection reason, gameplay footprint, and chosen marker/hit source before any further policy mutation or APK rebuild. No other functional row was rerun or accepted. VRP-072 remains unstarted; blue Autobahn/canal isolation and the missing bridge remain separate. The generated runtime-content report and recovery scene remain unstaged, and no protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |

| 2026-08-03 | Packaged building-selection diagnostic rerun | Child independent correctness gate; Hub-opened GUI `[StaticReuseOwnedRendererFallbackValidation] result=Passed tests=4`; diagnostic commits `a6730af1b` and `bd1067f65`; Hub-opened GUI package route after each code change; final `[DenseCityCandidateAndroidPackage] result=Passed`; exact final 758,603,643-byte APK SHA-256 `bc81c510e5485fc1a1698ef616829ff7f530a259c587bf8c5b72b2df7bd7a2e1`; wireless install to Samsung `SM-S918B` / `R5CTC1J02VB`; exact-activity 507 ms cold launch; main-page Deploy; retained evidence under `Build/AndroidDenseCandidateProfiler/Evidence/2026-08-03-geometry-diagnostics` and `2026-08-03-unconditional-geometry-diagnostics` | Diagnostic code and package passed; affected Android marker gate remains rejected; parent progress remains `126/149 (85%)` | The bounded diagnostic records exact marker/direct-hit source, presentation flags, owned-renderer metrics and rejection reason, gameplay footprint, and chosen geometry. Editor emitted the expected record; the first Samsung package retained the oversized frame, while a freshly placed Contractor Tent spent `120/60 -> 100/60`, selected with the correct portrait, and still exposed no health or Recruit control after more than 30 seconds. Because that Development APK emitted no selection records, the second revision removed the `Debug.isDebugBuild` guard and rebuilt once. The final exact APK again selected Barracks with a frame extending off-screen and across the road, but complete logcat still contained neither unconditional marker nor winning-hit record even though generated IL2CPP metadata contains the diagnostic string and normal Unity performance logs remain present. Another speculative fallback change inside the currently instrumented helper is therefore rejected. The next bounded item is direct one-shot instrumentation at `PremiumWorldSelectionFrameView.Configure` and the outer packaged hit-arbitration owner, followed by a measured presentation/hit fix. Contractor completion, vehicles, helicopter/drop, and spawned-unit interaction remain separate. VRP-072 remains unstarted; blue Autobahn/canal isolation and the missing bridge remain separate. The unrelated runtime-content report and recovery scene remain unstaged, and no protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |

| 2026-08-03 | Shared packed-renderer selection-outline suppression | Child independent correctness gate; direct packaged one-shot diagnostics at the outer hit owner and rendered-frame boundary; implementation commits `9d2dfeca5` and `07b1e498a1`; user-directed Hub-opened DX11 GUI `Tools/Validation/Canonical Building Selection Marker`; `[CanonicalBuildingSelectionMarkerPlayModeValidation] result=Passed barracksMarker=placementPrefabGeometry presentationSize=(7.43, 5.14, 13.55) presentationYaw=90.00 barracksPortrait=Portrait_Building_Barrack_Action_512 contractorTentClickOwner=9`; freshly inspected `Build/EditorEvidence/CanonicalBarracksSelectionPresentation.png` and `Build/EditorEvidence/CanonicalContractorTentClickOwnership.png`; `git diff --check` | Measured Editor fix accepted; fresh checked APK and affected Samsung rerun required; parent progress remains `126/149 (85%)` | Direct Samsung evidence proved the normal outer building-hit route reported `handled=0`, while the massive cyan presentation came from the separate object-outline overlay copied from a shared packed static-reuse renderer. It was therefore not evidence of incorrect bounded-frame dimensions. Map-authored static-reuse owners now suppress that unsafe object outline and retain their bounded placement-prefab frame; runtime-spawned buildings retain the existing outline path. The real Deploy validation visibly selects Barracks `18` with its exact action portrait and bounded frame, then resolves the nearby Contractor Tent as independent click owner `9` with its distinct portrait and bounded frame. The gate now fails if an active `SelectionObjectOutline` renderer exists beneath the selected map-authored owner. A post-pass Addressables invalid-handle exception occurred during teardown after the pass marker and evidence writes and is not counted as another acceptance signal. No fresh APK was built, so the Samsung marker/hit gate remains open. Vehicle selection, Tent completion/Recruit, helicopter/drop, and spawned-unit interaction remain separate. VRP-072 remains unstarted; blue Autobahn/canal isolation and the missing bridge remain separate. The unrelated runtime-content report and recovery scene remain unstaged, and no protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |

| 2026-08-03 | Shared-outline fresh Samsung rejection | Child independent correctness gate; checked Windows GUI-licensing package wrapper `%TEMP%\warline-shared-outline-profiler-apk.log`; `Build Finished, Result: Success`; `[DenseCityCandidateAndroidPackage] result=Passed`; exact 696,278,899-byte APK SHA-256 `64ca360f09f2704ed2f9a561b561cf6e9a13139a4bb976dc2ac54a3b982fe49a`; device-side size/hash equality; wireless Samsung `SM-S918B` / `R5CTC1J02VB`; exact-activity 542 ms cold launch; visible main-page Deploy; evidence under `Build/AndroidDenseCandidateProfiler/Evidence/2026-08-03-shared-outline-fix`; Android selection and frame-rate diagnostics | Package passed but affected Android marker gate rejected; parent progress remains `126/149 (85%)` | The exact fresh post-`5871ad36c` package reached live gameplay through the required main-page Deploy route. Selecting Barracks retained its intended panel image, but the cyan presentation still outlined the upper shared building and continued as a long road segment, so suppressing the helper-owned map-authored object outline did not remove the actual packaged overlay. The outer route again reported `handled=0`; the successful Barracks click projected to `(874,397)` and incurred a 290.9 ms update hitch. Stable samples were roughly 42-48 FPS with GPU near 22 ms. Nearby Tent ownership was not accepted because the marker sub-gate already failed. Vehicle selection, Tent completion/Recruit, helicopter/drop, and spawned-unit interaction remain separate Android gates. VRP-072 remains unstarted; blue Autobahn/canal isolation and the missing bridge remain separate. The unrelated runtime-content report and recovery scene remain unstaged, and no protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |

| 2026-08-03 | Generic ECS building-outline suppression and fresh Samsung rejection | Child independent correctness gate; implementation commit `48baeb813`; checked Windows GUI-licensing wrappers `%TEMP%\warline-operation-map-building-outline-playmode-accepted.log` `[OperationMapBuildingSelectionOutlineValidation] result=Passed tests=6 owner=OperationMapBuildingComponent footprintMarker=preserved sharedRendererOutline=suppressed repeatFrames=stable` and `%TEMP%\warline-canonical-building-selection-marker-post-ecs-outline.log` `[CanonicalBuildingSelectionMarkerPlayModeValidation] result=Passed`; visible Editor evidence `Build/EditorEvidence/CanonicalBarracksSelectionPresentation.png` and `CanonicalContractorTentClickOwnership.png`; rejected canonical-path Android build on Windows MAX_PATH; accepted short-path checked wrapper `%TEMP%\warline-operation-map-building-outline-profiler-apk-shortpath.log`; `Build Finished, Result: Success`; `[DenseCityCandidateAndroidPackage] result=Passed`; exact 696,278,899-byte APK SHA-256 `3e3a7d6cfcdb1a2e04ae0abc5074502391c82dacfb00481af55849b6db615f58`; device-side size/hash equality; wireless Samsung `SM-S918B` / `R5CTC1J02VB` at `192.168.2.33:44569`; exact-activity 417 ms cold launch; visible main-page Deploy; evidence under `Build/AndroidDenseCandidateProfiler/Evidence/2026-08-03-generic-ecs-outline-fix`; Android selection/frame diagnostics | Editor contract and package passed, but affected Android marker gate rejected; parent progress remains `126/149 (85%)` | The generic ECS presentation system now destroys copied object-outline instances for selected `OperationMapBuildingComponent` entities while preserving their dedicated footprint marker. Synthetic live-group coverage proves a stale outline entity is removed and stays absent across repeated frames; the real Editor Deploy route still shows distinct bounded Barracks/Tent markers and portraits. The exact fresh package installed byte-identically and reached live gameplay through the required main-page Deploy control, but selecting Barracks still showed the intended portrait alongside a cyan outline on the upper shared presentation that continued as a long road segment. The packaged overlay therefore is not owned by either the earlier helper path or the newly suppressed generic selected-building ECS path. The outer route again reported `handled=0` at projected cell `(874,397)`; the selection frame incurred a 220.4 ms update hitch, followed by a 51.0 FPS sample with 14.3 ms GPU. Nearby Tent ownership was not rerun because the marker sub-gate already failed. The next bounded step must trace the exact live cyan renderer/outline instance and owner at the packaged presentation boundary before another mutation; no speculative geometry or outline policy change is accepted. Vehicle selection, Tent completion/Recruit, helicopter/drop, and spawned-unit interaction remain separate. VRP-072 remains unstarted; blue Autobahn/canal isolation and the missing bridge remain separate. The two generated runtime-content/layout reports and recovery scene remain unstaged, and no candidate database, protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |

| 2026-08-04 | Cold candidate binding migration and Samsung marker/hit acceptance | Child independent correctness gate; implementation commits `58d242c13` and `ba29ba817`; checked GUI-licensing focused `%TEMP%\warline-candidate-binding-migration-final-focused.log` `[OperationMapEntitySceneCandidateBakeAllValidation] result=Passed tests=24`; checked GUI-licensing package `%TEMP%\warline-marker-hitbox-short-profiler-apk-final.log`; `[DenseCityCandidateAndroidPackage] result=Passed`; exact 758,604,112-byte APK SHA-256 `d5c676c4bc6b73de84de010b7450104c776c62051aaf9afe31e02b5705cd2ab5`; streamed wireless install to Samsung `SM-S918B` / `R5CTC1J02VB`; exact-activity 544 ms cold launch; correctly scaled main-page Deploy; evidence under `Build/AndroidDenseCandidateProfiler/Evidence/2026-08-04-marker-hitbox-migration`; Android marker/hit and frame diagnostics | Accepted for the bounded Barracks marker and nearby Tent/Contractor click-ownership gate; parent progress remains `126/149 (85%)` | Cold candidate binding now durably migrates the retained `CombinedMeshBaker` and `MapSurfaceAuthoring` identities before package validation, allowing the fresh package to exercise the intended marker implementation. On Samsung, central Barracks selection no longer produced the road-spanning shared cyan overlay; its retained ECS marker reported `source=baked-building-hitbox`, `footprint=(20,10)`, and bounded scale `(6.292,12.500)`. A direct nearby beige-tent tap changed the visible owner from Barracks to `Soldier Tent` with a separate bounded marker. For the stricter Contractor case, a valid `13x11` Contractor Tent placement at `(874,374)` spent materials `120/60 -> 100/60`; after selecting Barracks, direct Tent hits emitted `kind=hit-selected id=7 instance=Contractor Tent_7`, `handled=1`, and moved the cyan marker to the Tent's own `13x11` gameplay footprint. The Contractor action image and footprint marker changed, while the panel title remained stale as `Barracks`; that label-refresh defect and Contractor completion/Recruit remain separate and are not accepted here. Vehicle selection, helicopter/drop, and spawned-unit interaction were not rerun. Selection hitches of about `195-358 ms` and roughly `50-51.5 FPS` samples remain performance evidence only; VRP-072 remains unstarted. Blue Autobahn/canal isolation and road-over-water-without-bridge remain separate. Generated reports and `_Recovery` scenes remain unstaged; no candidate database, protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |

| 2026-08-04 | Fresh Contractor panel refresh Editor repair | Child independent correctness gate; checked GUI-licensing focused `%TEMP%\warline-contractor-panel-rts-selection-after-preflight.log` `[RtsSelectionInputSystemValidation] result=Passed tests=62`; checked GUI-licensing real-HUD transition `%TEMP%\warline-fresh-contractor-panel-self-contained-visible-title.log` `[FreshContractorSelectionPanelEditorValidation] result=Passed`; freshly inspected `Build/EditorEvidence/FreshContractorSelectionPanelTransition.png`; `git diff --check` | Editor repair accepted; one fresh checked APK and only the affected Samsung panel gate are required; parent progress remains `126/149 (85%)` | The stale Barracks title/description/health after a valid fresh Contractor hit was an input-arbitration defect, not a portrait or marker defect: the handled building click set `SuppressNextWorldClick`, but its release later fell through to ECS focus and let an overlapping Barracks entity replace the Contractor model. The release is now consumed unconditionally while queued movement and pointer state are cleared. The focused regression proves no unit-focus fallthrough. A self-contained Editor gate instantiates the real HUD prefab and selection helper, transitions Barracks to a fresh Contractor model, and proves the title/origin, exact action portrait, subtitle, order, and health refresh atomically; the evidence PNG visibly shows the full Contractor state. The full canonical match route was attempted but rejected before gameplay by the existing Addressables catalog typing defect (`StaticMapPresentationManifest` GUID resolves as `System.Object`), so no full-match Editor pass is claimed. No APK was rebuilt and Samsung was not rerun. The accepted marker/hit gate remains closed; Contractor completion/Recruit, vehicle selection, helicopter/drop, and spawned-unit interaction remain separate. VRP-072 remains unstarted; blue Autobahn/canal isolation and the missing bridge remain separate. The generated runtime-content report and all `_Recovery` scenes remain unstaged; no protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |

| 2026-08-04 | First Contractor panel APK rejection and explicit-building panel priority repair | Child independent correctness gate; rejected checked 758,604,112-byte profiler APK SHA-256 `7508272b78a2ef411e0aae370dc7b9e04ad1a19568b34d882b9d3d7ee9ad1b27`; wireless Samsung `SM-S918B` / `R5CTC1J02VB`; exact-activity 568 ms cold launch; main-page Deploy; evidence under `Build/AndroidDenseCandidateProfiler/Evidence/2026-08-04-contractor-panel-refresh`; checked GUI-licensing `%TEMP%\warline-selection-summary-building-priority-ipc.log` `[SelectionSummaryFocusedValidation] result=Passed tests=21`; checked visible GUI gate `%TEMP%\warline-fresh-contractor-panel-stale-focus.log` `[FreshContractorSelectionPanelEditorValidation] result=Passed`; freshly inspected `Build/EditorEvidence/FreshContractorSelectionPanelTransition.png`; `git diff --check` | First APK rejected; stronger Editor repair accepted and one new checked APK plus only the affected Samsung panel gate are required; parent progress remains `126/149 (85%)` | The first input-only package kept the accepted Contractor hit and marker but periodic HUD refresh still preferred a stale selected/focused Barracks entity, yielding a Contractor action portrait with the stale `Barracks` title and `1200/1200` health. Explicit managed building selection now owns the HUD panel before focused or selected ECS summaries, making title, portrait, subtitle, order, health, storage, and actions atomic while ECS cleanup converges. Focused coverage retains the stale Barracks entity and proves `Contractor Tent (113,100)`, its exact action portrait, `Base Structure`, `Structure selected`, and health `-` win together. The real-HUD visible validator reproduces the same mixed state and visibly renders the correct Contractor panel. GUI licensing recovery removed only verified wrapper-owned failed Editors, restarted the sole Hub/licensing environment signed in, and then passed the unchanged checked wrappers with permitted local IPC access. The rejected APK will not be reused. The accepted marker/hit gate remains closed; Contractor completion/Recruit, vehicle selection, helicopter/drop, and spawned-unit interaction remain separate. VRP-072 remains unstarted; blue Autobahn/canal isolation and road-over-water-without-bridge remain separate. The generated runtime-content report and all `_Recovery` scenes remain unstaged; no protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |

| 2026-08-04 | Second Contractor panel APK rejection and cache round-trip repair | Child independent correctness gate; checked GUI-licensing package `%TEMP%\warline-contractor-panel-priority-profiler-apk-retry.log` `[DenseCityCandidateAndroidPackage] result=Passed`; exact 758,604,112-byte APK SHA-256 `a0be94e7ccedefa8352d6d7aea982897a0462bb2512ef8af61213c3c778f5ade`; streamed wireless install to Samsung `SM-S918B` / `R5CTC1J02VB` at `192.168.2.33:44569`; exact-activity 537 ms cold launch; main-page Deploy; direct Contractor `kind=hit-selected id=7` / `handled=1`; rejected frame `Build/AndroidDenseCandidateProfiler/Evidence/2026-08-04-contractor-panel-priority/10-contractor-reselected-after-barracks.png`; corrected checked GUI-licensing `%TEMP%\warline-selection-summary-cache-transition-rerun.log` `[SelectionSummaryFocusedValidation] result=Passed tests=21`; checked visible GUI gate `%TEMP%\warline-fresh-contractor-panel-cache-transition.log` `[FreshContractorSelectionPanelEditorValidation] result=Passed`; freshly inspected `Build/EditorEvidence/FreshContractorSelectionPanelTransition.png`; `git diff --check` | Second APK rejected; exact Editor cache round-trip repair accepted and one new checked APK plus only the affected Samsung panel gate are required; parent progress remains `126/149 (85%)` | The second package reached the correct Contractor owner but exposed a distinct cache lifecycle defect. Initial Contractor placement cached the Contractor label; a later focused Barracks model replaced the visible panel without clearing that cache; reselecting the same Contractor therefore skipped the full building model and changed only the portrait, leaving `Barracks`, `1200/1200`, and `Idle` after five seconds. Focused and summary panel transitions now invalidate selected-building cache ownership explicitly. The focused regression and real HUD validator both perform the exact `Contractor -> focused Barracks -> same Contractor` sequence and prove the final Contractor title, portrait, subtitle, order, and health return atomically. An initial wrapper run using a nonexistent namespaced test entry point was rejected; only the corrected global-entry pass is accepted. The rejected APK will not be reused. The accepted marker/hit gate remains closed; Contractor completion/Recruit, vehicle selection, helicopter/drop, and spawned-unit interaction remain separate. VRP-072 remains unstarted; blue Autobahn/canal isolation and the missing bridge remain separate. The generated runtime-content report and all `_Recovery` scenes remain unstaged; no protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |

| 2026-08-04 | Contractor panel cache round-trip Samsung acceptance | Child independent correctness gate; implementation commit `22c2772a7`; checked GUI-licensing focused `%TEMP%\warline-selection-summary-cache-transition-rerun.log` `[SelectionSummaryFocusedValidation] result=Passed tests=21`; checked visible GUI `%TEMP%\warline-fresh-contractor-panel-cache-transition.log` `[FreshContractorSelectionPanelEditorValidation] result=Passed`; checked GUI-licensing package `%TEMP%\warline-contractor-panel-cache-roundtrip-profiler-apk.log` `[DenseCityCandidateAndroidPackage] result=Passed`; exact 758,604,112-byte APK SHA-256 `db9240ba8577b27967729186ac114340ec2d0f1ca7d44152f0c5987b33dbabbc`; streamed wireless install to Samsung `SM-S918B` / `R5CTC1J02VB`; exact-activity 551 ms cold launch; main-page Deploy; evidence under `Build/AndroidDenseCandidateProfiler/Evidence/2026-08-04-contractor-panel-cache-roundtrip`; Android direct-hit and frame diagnostics | Accepted for the bounded freshly spawned Contractor selection-panel round trip; parent progress remains `126/149 (85%)` | The exact post-fix package repeated `Contractor -> Barracks -> same Contractor`. Placement accepted the valid `13x11` preview and spent materials `120/60 -> 100/60`; Barracks then displayed `1200/1200` and `Idle`; the direct Contractor hit reported `id=7` and `handled=1`. After seven seconds, the real Samsung panel visibly returned the full Contractor title, configured portrait, `Base Structure`, health `-`, and `Structure selected`, closing this independent panel gate. The already accepted marker/hit gate was not reopened, and prior rejected APKs are not reused. A 239.9 ms selection/update hitch and roughly 51-52 FPS stable samples remain performance evidence only. Contractor completion/Recruit is the next independent functional gate; vehicle selection, helicopter/drop, and spawned-unit interaction remain separate. VRP-072 remains unstarted; blue Autobahn/canal isolation and the missing bridge remain separate. The generated report hash was preserved, all `_Recovery` scenes remain unstaged, and no protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |

| 2026-08-04 | Selected-building health panel Editor repair | Child independent correctness gate; host-access checked Windows GUI-licensing `Build/Logs/contractor-health-selection-summary-host.log` `[SelectionSummaryFocusedValidation] result=Passed tests=21`; checked visible GUI `Build/Logs/fresh-contractor-health-visible-host.log` `[FreshContractorSelectionPanelEditorValidation] result=Passed title=Contractor Tent origin=(113,100) portrait=Portrait_Building_Tent_Contractor_Action_512 subtitle=Base Structure order=Structure selected health=350/350`; freshly inspected `Build/EditorEvidence/FreshContractorSelectionPanelTransition.png`; `git diff --check` | Editor repair accepted; one fresh checked APK and only the affected Samsung health/Recruit gate are required; parent progress remains `126/149 (85%)` | Explicit managed building selection previously hardcoded health `-` and fill `0` despite canonical runtime `UnitHealth`. The HUD now consumes the existing selected-building health query and includes current/max health in its cache key, so the same selected building refreshes after damage. Focused coverage proves `350/350 -> 225/350`, while the real HUD evidence visibly shows the Contractor title, configured portrait, full health bar, `350/350`, and `Structure selected`. Sandboxed GUI licensing failed before compilation; the same checked wrappers passed with host IPC access after verified sole Hub/licensing recovery. No APK was rebuilt and no Android acceptance is claimed. The accepted marker/hit and title/portrait round-trip gates remain closed; Contractor Recruit/completion is the affected device gate, while vehicle selection, helicopter/drop, and spawned-unit interaction remain separate. VRP-072 remains unstarted; blue Autobahn/canal isolation and road-over-water-without-bridge remain separate. The unrelated runtime-content report and all `_Recovery` scenes remain unstaged; no protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |

| 2026-08-04 | Consolidated Editor-first production and selection batch | Child independent correctness gate; host-access checked Windows GUI-licensing `Build/Logs/editor-first-functional-batch-host-sync.log`; wrapper exit code `0`; aggregate `[EditorFirstProductionFunctionalBatchValidation] result=Passed suites=8 tests=92`; constituent production bridge/consumer/scheduler/spawn, canonical transport, camera-focus, RTS selection, and squad-tray markers all passed; freshly inspected `Build/EditorEvidence/CanonicalTentTransportDrop.png`; `git diff --check` | All remaining Editor-first functional rows accepted; one fresh profiler APK and consolidated Samsung pass are next; parent progress remains `126/149 (85%)` | Added a single Editor-only fail-closed runner around the existing 92 focused checks. The batch proves Contractor preflight/spend/queue and completion, live-grid configured-unit spawning, canonical helicopter/rope/drop ownership, spawned-unit focus, world selection, and roster selection; the rendered evidence visibly contains the helicopter, rope, and drop unit. This child correctness evidence does not complete a parent checklist row and does not start VRP-072. No APK was built, and the accepted marker/hit and Contractor panel gates were not retested. Blue Autobahn/canal isolation and road-over-water-without-bridge remain separate. Generated reports, performance settings artifacts, and all `_Recovery` scenes remain unstaged; no protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |

| 2026-08-04 | Consolidated Samsung functional rejection | Child independent correctness gate; implementation commit `7ae86f350`; checked GUI-licensing package `Build/Logs/editor-first-consolidated-profiler-apk-retry.log` `[DenseCityCandidateAndroidPackage] result=Passed`; exact 696,278,899-byte APK SHA-256 `4059228900347253b3208feb355b4a1eff2ae29a6715bfc8a6482e07b9d2047e`; wireless Samsung `SM-S918B` / `R5CTC1J02VB` at `192.168.2.33:44569`; exact-activity 493 ms cold launch; correctly scaled main-page Deploy; screenshots and timed sequence under `Build/AndroidDenseCandidateProfiler/Evidence/2026-08-04-editor-first-functional-batch`; Android scheduler and frame diagnostics | Contractor completion/health and Recruit command accepted; vehicle selection and production completion rejected; parent progress remains `126/149 (85%)` | The fresh package reached the live map through the required Deploy control. A valid `13x11` Contractor placement spent materials `120/60 -> 100/60`; after 38 seconds the selected panel showed the correct portrait and `350/350`. Recruit accepted `Advanced Rifleman Male II` and spent materials `100 -> 75`, but repeated scheduler diagnostics reported `result=Stalled outcome=SpawnRejected producers=20 pending=1 ready=1 authoredGrids=1 completeGrids=1 resolved=1 spawned=0`; `units=5006`, `sourceKeys=5006`, and `models=22` remained stable for more than 56 seconds. No helicopter, rope, drop, or soldier appeared, leaving spawned-unit interaction unreachable. Separately, the Armor roster card and a visible world-vehicle tap showed no selected state or panel change. The accepted marker/hit and Contractor title/portrait cache-roundtrip gates were not retested. Functional-run performance was roughly `34-54 FPS`, retained only as later VRP-072 evidence; VRP-072 remains unstarted. Blue Autobahn/canal isolation and road-over-water-without-bridge remain separate. Generated reports and all `_Recovery` scenes remain unstaged; no protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |

| 2026-08-04 | Canonical production delivery-key normalization | Child independent correctness gate; Samsung `SpawnRejected` evidence above; checked Windows GUI-licensing `Build/Logs/operation-map-production-normalized-delivery.log` `[OperationMapProductionRuntimeSchedulerValidation] result=Passed tests=6`; checked aggregate `Build/Logs/editor-first-functional-batch-normalized-delivery.log` `[EditorFirstProductionFunctionalBatchValidation] result=Passed suites=8 tests=92`; `git diff --check` | Exact production delivery rejection repaired and Editor-accepted; consolidated Android confirmation remains pending; parent progress remains `126/149 (85%)` | The queued operation-map request stored the configured unit prefab's mixed-case name, but the runtime unit lookup is keyed by the canonical trimmed lowercase form. The delivery bridge used the raw request text and therefore rejected the valid Samsung request before the helicopter session could begin. It now normalizes through the shared spawnable-key contract before dictionary lookup. The common Editor test context also uses the production-realistic normalized lookup table, turning the existing delivery-in-progress scheduler test into a regression for this exact packaged mismatch. The focused scheduler suite and full 92-test Editor-first batch pass. No APK was built under the user-directed batching strategy; vehicle selection remains separate, while helicopter/drop/spawn device acceptance waits for the final consolidated package. VRP-072 remains unstarted. Generated reports and all `_Recovery` scenes remain unstaged; no protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |
| 2026-08-04 | Consolidated profiler selection diagnostics | Child independent correctness prerequisite; checked Windows GUI-licensing `Build/Logs/editor-first-functional-batch-profiler-diagnostics.log`; `[EditorFirstProductionFunctionalBatchValidation] result=Passed suites=8 tests=92`; `git diff --check` | Development-player diagnostic prerequisite accepted; consolidated APK/device confirmation remains pending; parent progress remains `126/149 (85%)` | Selection-click diagnostics now activate only in non-Editor development players, including the candidate profiler APK, while Editor and release-player behavior remains unchanged. The next single Samsung batch will therefore expose the real roster-card callback, UI release suppression, world hit lookup, and focus result without another blind package iteration. The full Editor-first batch remains green at 92/92. No APK was built by this bounded prerequisite. VRP-072 remains unstarted; generated reports and all `_Recovery` scenes stay unstaged, and no protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |
| 2026-08-04 | Normalized-production consolidated Samsung partial acceptance | Child independent correctness gate; implementation commits `bfce6be37` and `a58918105`; checked Windows GUI-licensing `Build/Logs/editor-first-functional-batch-profiler-diagnostics.log` `[EditorFirstProductionFunctionalBatchValidation] result=Passed suites=8 tests=92`; checked package `Build/Logs/consolidated-production-selection-profiler-apk-short-gradle-regenerated.log` `[DenseCityCandidateAndroidPackage] result=Passed`; exact 696,278,899-byte APK SHA-256 `62576f58c1d1207a5977d8b565bb324680d4813c40336564dfcb8f3177c69ee8`; wireless Samsung `SM-S918B` / `R5CTC1J02VB` at `192.168.2.33:44569`; exact-activity 363 ms first cold launch; correctly scaled main-page Deploy; evidence under `Build/AndroidDenseCandidateProfiler/Evidence/2026-08-04-consolidated-functional`; Android selection and frame diagnostics | Helicopter delivery/selection and Contractor identity/health accepted; vehicle selection, Contractor Recruit, and visible rope/drop confirmation rejected; parent progress remains `126/149 (85%)` | The exact fresh package reached the live map through the required Deploy control. A valid `13x11` Contractor placement spent materials `120/60 -> 100/60` and, after 35 seconds, visibly showed the correct Contractor title, action portrait, bounded marker, and `350/350`; however, its completed panel still exposed only Return, Destroy, and Camera, with no Recruit control. In an independent fresh gameplay session, Light Attack Helicopter production spent `104` materials (`120 -> 16`) and emitted the success toast while the production pane incorrectly remained `NO PRODUCTION QUEUED`. After more than 50 seconds, the Gunship roster selected a live `Light Attack Helico` at `520/520`, and `[SelectionClick] result=SquadTraySelect slot=AttackHelicopter selected=1` proves normalized delivery, spawned existence, and normal roster interaction. The transient rope/drop sequence was hidden by the stale queue pane and was not visibly accepted. Independently, Armor emitted `result=SquadTraySelectSkipped slot=CombatVehicles reason=NoCandidates`; a visible missile-launcher world hit resolved successfully but then emitted `result=RejectFocus ... reason=NonPlayer`, so neither vehicle route is accepted. Stable samples were roughly `49-54 FPS`; this is retained for later VRP-072 evidence only and authorizes no render-policy mutation. VRP-072 remains unstarted; blue Autobahn/canal isolation and road-over-water-without-bridge remain separate. The unrelated generated report and all `_Recovery` scenes remain unstaged; no protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |
| 2026-08-04 | Authored vehicle ownership adoption | Child independent correctness gate; checked Windows GUI-licensing `Build/Logs/map-vehicle-authored-ownership-adoption-final.log`; `[MapVehiclePlacementStartupCompletionValidation] result=Passed tests=3`; `git diff --check` | Authored neutral vehicle ownership is repaired and Editor-accepted; consolidated Android confirmation remains pending; parent progress remains `126/149 (85%)` | Startup now adopts an exact-position neutral authored vehicle into its canonical configured placement instead of leaving a neutral live duplicate and instantiating a second player copy. The bounded adoption accepts only non-prefab, non-disabled, non-static, non-passenger vehicle-motion entities with no respawn owner, reserves each adopted entity once, and applies faction, grid, transform, source key, respawn, cooldown, and reset state atomically. Focused coverage proves the real entity becomes player-owned and rejects already-owned or distant candidates. No APK was built or installed; this remains part of the user-directed Editor-first batch. VRP-072 remains unstarted, and unrelated generated reports and all `_Recovery` scenes remain unstaged. |
| 2026-08-04 | Building placement live-occupancy exclusion | Child independent correctness gate; checked Windows GUI-licensing `Build/Logs/building-placement-live-occupancy-editor-rerun.log`; `[BuildingPlacementLiveOccupancyValidation] result=Passed tests=2`; checked aggregate `Build/Logs/editor-first-functional-batch-vehicle-occupancy.log`; `[EditorFirstProductionFunctionalBatchValidation] result=Passed suites=8 tests=92`; `git diff --check` | Building/building, building/unit, and building/vehicle overlap rejection is Editor-accepted; consolidated Android confirmation remains pending; parent progress remains `126/149 (85%)` | Placement already rejected canonical runtime-building rectangles but deliberately excluded dynamic occupancy from its cached blocker prefix and never performed a live unit-footprint check, allowing a Tent to be confirmed over another owner. Preview and confirmation now share one placement-occupant boundary: runtime buildings retain their canonical rectangle owner, while live infantry and full multi-cell vehicle footprints are queried directly; runtime-building ECS proxies and static blockers remain excluded from that dynamic layer to avoid double ownership. The first broader test entry point was rejected by an unrelated pre-existing harness setup null; the isolated fail-closed occupancy entry point passed, and the full Editor-first batch remained green. No APK was built or installed under the Editor-first batching strategy. VRP-072 remains unstarted, and unrelated generated reports and all `_Recovery` scenes remain unstaged. |
| 2026-08-04 | Operation-map production queue drawer read model | Child independent correctness gate; checked Windows GUI-licensing `Build/Logs/building-ui-query-operation-map-queue.log` `[BuildingUiQueryValidation] result=Passed tests=11`; checked real-prefab view `Build/Logs/build-drawer-operation-map-queue-view.log` `[BuildDrawerProductionQueueSnapshotValidation] result=Passed tests=1`; rejected aggregate drawer run `Build/Logs/build-drawer-operation-map-queue-regression.log` on an unrelated order-dependent legacy placement-price assertion after 13 passing steps; rejected malformed one-test Test Framework invocation `Build/Logs/build-drawer-queue-snapshot-isolated.log` at its wrapper timeout; `git diff --check` | Stale `NO PRODUCTION QUEUED` read-model cause repaired and Editor-accepted; consolidated Android confirmation remains pending; parent progress remains `126/149 (85%)` | The build drawer queried only managed `RuntimeBuildingEntity.PendingProductions`, while canonical Contractor/operation-map producers enqueue into `OperationMapBuildingUnitProductionRequest`; a successful Recruit or helicopter request could therefore remain invisible in the queue pane. The friendly production query now joins live player-owned, healthy, non-destroyed operation-map producer buffers, resolves their configured preview prefabs, and projects bounded progress/timing without enabling unsupported cancellation. Focused coverage proves a freshly queued Contractor-style request appears at 25% with the correct producer and prefab; the real drawer prefab independently proves active/queued/empty presentation. The unrelated aggregate failure and malformed test invocation are recorded rather than accepted. No APK was built or installed under the Editor-first batching strategy. VRP-072 remains unstarted, and unrelated generated reports and all `_Recovery` scenes remain unstaged. |
| 2026-08-04 | Current-head Editor batch and consolidated Samsung partial acceptance | Child independent correctness gate; checked Windows GUI-licensing `Build/Logs/editor-first-functional-batch-current-head-rerun.log` `[EditorFirstProductionFunctionalBatchValidation] result=Passed suites=8 tests=92`; checked package `Build/Logs/editor-first-consolidated-current-head-profiler-apk.log` `[DenseCityCandidateAndroidPackage] result=Passed`; exact 696,278,899-byte APK SHA-256 `8dd5d7581d5afc26fec9679877959f6f25a911bfc9d233d12d98d02d6e2ada70`; wireless Samsung `SM-S918B` / `R5CTC1J02VB`; exact-activity cold launches `524`, `604`, and `498 ms`; correctly scaled main-page Deploy; evidence under `Build/AndroidDenseCandidateProfiler/Evidence/2026-08-04-editor-batch-final`; Android selection, production, occupancy, and frame diagnostics | Contractor completion/Recruit and attack-helicopter production/selection accepted; placement overlap, Armor selection, Transport/drop visibility, and spawned-soldier interaction rejected; parent progress remains `126/149 (85%)` | The one user-directed Editor-first batch remained green at 92/92 before exactly one fresh profiler build. On Samsung, a clear-ground Contractor completed with the correct portrait and `350/350`; Advanced Rifle recruitment visibly progressed, cleared, and increased runtime ownership `units/sourceKeys 5006 -> 5007` and rendered models `22 -> 23`. Light Attack Helicopter production likewise progressed and cleared, increased runtime ownership, selected through the Gunship roster at `520/520`, and then accepted direct world focus on `Unit_Veh_Helicopter_Attack_Small`. Four independent rejections remain: the initial `13x11` Contractor preview over an existing Tent incorrectly reported valid placement; Armor reported `SquadTraySelectSkipped ... NoCandidates`; Transport reported the same and no rope/drop control became visible; and likely spawned-soldier taps were consumed as `SuppressNextWorldClick` without focused ownership. Functional samples were roughly `34-55 FPS`, with one `122.4 ms` placement/update hitch; these are retained only for later VRP-072 measurement and authorize no render-policy mutation. VRP-072 remains unstarted. Blue Autobahn/canal isolation and road-over-water-without-bridge remain separate. The unrelated generated report and all `_Recovery` scenes remain unstaged; no protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |
| 2026-08-04 | Operation-map building placement occupancy ownership | Child independent correctness gate; checked Windows GUI-licensing `Build/Logs/building-placement-operation-map-occupancy-editor.log`; `[BuildingPlacementLiveOccupancyValidation] result=Passed tests=2`; `git diff --check` | Packaged overlap cause repaired and Editor-accepted; consolidated Android confirmation remains pending; parent progress remains `126/149 (85%)` | The prior live-occupancy repair excluded every ECS entity carrying `RuntimeBuildingCombatTag`, assuming the managed runtime-building dictionary owned all such footprints. Map-authored operation-map buildings carry that tag but are not managed runtime-building instances, so an existing Tent could be invisible to preview and confirmation. The occupancy boundary now excludes only non-operation-map runtime-building proxies; canonical `OperationMapBuildingComponent` entities block through their `UnitGrid` and full `UnitFootprint`. Focused coverage distinguishes a managed proxy, static blocker, live infantry, full vehicle footprint, and map-authored building. No APK was built or installed under the Editor-first batching strategy. Armor selection, Transport/drop visibility, and spawned-soldier interaction remain independent Editor-first defects; VRP-072 remains unstarted. The unrelated generated report and all `_Recovery` scenes remain unstaged; no protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |
| 2026-08-04 | Stable-identity authored vehicle adoption and Armor selection | Child independent correctness gate; checked Windows GUI-licensing `Build/Logs/map-vehicle-stable-identity-selection-editor.log`; `[MapVehiclePlacementStartupCompletionValidation] result=Passed tests=4`; checked real candidate `Build/Logs/operation-map-vehicle-stable-identity-readiness-editor.log`; `[OperationMapEntityPresentationReadiness] result=Passed buildings=432 vehicles=22 renderOnly=9090`; checked aggregate `Build/Logs/editor-first-functional-batch-stable-vehicle-identity.log`; `[EditorFirstProductionFunctionalBatchValidation] result=Passed suites=8 tests=92`; rejected visible full-match Editor attempt in `Logs/Editor.log` at `[CanonicalVehicleSelectionPlayModeValidation] result=Failed timed out deploy=1 matchReady=0` after the pre-existing typed Addressables manifest preload failure; `git diff --check` | Migrated authored-vehicle ownership and Armor roster selection are repaired and Editor-accepted; consolidated Android confirmation remains pending; parent progress remains `126/149 (85%)` | The prior adoption repair still selected candidates only by a one-metre transform heuristic. Entity-presentation migration may preserve visible bounds while changing gameplay/render pivots, so the exact canonical vehicle could remain neutral and the packaged Armor card then reported `NoCandidates`. Startup now resolves migrated vehicles first through `OperationMapAuthoredVehiclePresentation -> UnitDetailedVisualReference.Root -> OperationMapEntityPresentationIdentity.PlacementIndex`; only legacy vehicles without the migrated marker may use proximity fallback, and a mismatched migrated identity is rejected fail-closed. Focused coverage places the migrated vehicle six metres from its compatibility transform, adopts the exact stable identity, applies player ownership, and selects it through the real Armor tray helper. The real candidate readiness gate independently confirms all 22 gameplay vehicles and legacy-placement parity, while the complete 92-test production/selection batch remains green. The full Menu-to-Deploy Editor route did not reach gameplay because GUID `2d7b...` again resolved as `System.Object` instead of `StaticMapPresentationManifest`; no full-match pass is claimed from that rejected route. No APK was built or installed under the Editor-first batching strategy. Transport/drop visibility and spawned-soldier interaction remain separate; VRP-072 remains unstarted. The unrelated generated report and all `_Recovery` scenes remain unstaged; no protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |
| 2026-08-04 | Transport identity selection and post-UI spawned-unit focus | Child independent correctness gate; rejected restricted-host GUI-wrapper attempts `Build/Logs/rts-selection-captured-ui-release-editor.log` and `-rerun.log` before project load on licensing IPC denial; checked host-access GUI wrappers `Build/Logs/rts-selection-captured-ui-release-editor-host.log` `[RtsSelectionInputSystemValidation] result=Passed tests=63`, `Build/Logs/map-vehicle-stable-identity-transport-selection-editor.log` `[MapVehiclePlacementStartupCompletionValidation] result=Passed tests=5`, and `Build/Logs/editor-first-functional-batch-ui-release-transport-rerun.log` `[EditorFirstProductionFunctionalBatchValidation] result=Passed suites=8 tests=92`; rejected aggregate invocation `Build/Logs/editor-first-functional-batch-ui-release-transport.log` because its shorthand class did not exist; `git diff --check` | Remaining Transport roster and spawned-unit input causes are repaired and Editor-accepted; one consolidated Android confirmation remains pending; parent progress remains `126/149 (85%)` | Gameplay UI click capture cleared its input-side release flags but left `RuntimeGameplayState.SuppressNextWorldClick` set, so the first legitimate world tap after Recruit was consumed before unit focus. Captured release completion now clears all release-scoped input flags and runtime suppression atomically; the regression proves the immediately following world-unit click invokes focus exactly once. Independently, stable-identity adoption coverage now includes a migrated transport helicopter eight metres from its compatibility transform, applies player ownership through placement identity `18`, and selects it through the real Transport roster slot. The complete production/selection batch remains green at 92/92, including the existing visible helicopter/rope/drop/completion contract. No APK was built or installed under the Editor-first batching strategy. The next Android work is one consolidated functional pass only; VRP-072 remains unstarted. The unrelated generated report and all `_Recovery` scenes remain unstaged; no render policy, protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |
| 2026-08-04 | Post-UI/Transport consolidated Samsung pass | Child independent correctness gate; implementation commit `b5133b9f7`; checked Windows GUI-licensing `Build/Logs/rts-selection-captured-ui-release-editor-host.log` `[RtsSelectionInputSystemValidation] result=Passed tests=63`, `Build/Logs/map-vehicle-stable-identity-transport-selection-editor.log` `[MapVehiclePlacementStartupCompletionValidation] result=Passed tests=5`, and `Build/Logs/editor-first-functional-batch-ui-release-transport-rerun.log` `[EditorFirstProductionFunctionalBatchValidation] result=Passed suites=8 tests=92`; checked package `Build/Logs/editor-batch-b5133b9f7-profiler-apk.log` `[DenseCityCandidateAndroidPackage] result=Passed`; exact 696,278,899-byte APK SHA-256 `3209bf05532f2a4adc463b03d63f5b9409c6e0b7f13eac7d46125dd150f66090`; wireless Samsung `SM-S918B` / `R5CTC1J02VB` at the currently discovered `192.168.2.33:44569`; exact activity cold launch `361 ms`; correctly scaled main-page Deploy; evidence under `Build/AndroidDenseCandidateProfiler/Evidence/2026-08-04-post-ui-transport-batch`; Android screenshots, selection diagnostics, and frame diagnostics | Contractor completion/Recruit and spawned-soldier existence/roster/direct-world interaction accepted; placement overlap and Armor/Transport roster selection rejected; visible helicopter/rope/drop remains unconfirmed; parent progress remains `126/149 (85%)` | The single fresh post-Editor-batch APK installed successfully and reached the live map through the required main-page Deploy control. A clear-ground `13x11` Contractor spent materials `120/60 -> 100/60`, completed, and visibly showed the correct `Contractor Tent (7)` title, action portrait, bounded marker, and `350/350`. The Soldiers tab exposed Recruit; two Advanced Rifleman requests spent `25` materials each and raised `units/sourceKeys 5006 -> 5007 -> 5008` and rendered models `22 -> 23 -> 24`. The first spawned rifleman selected through the Rifle Squad card with the correct portrait, `135/135`, visible world model, and footprint-scaled cyan marker. Its subsequent direct world tap independently traversed the repaired input path with `suppress=False`, resolved `unitLookup result=True` to `Unit_Chr_Soldier_Male_01_Alt_02`, and emitted both `focusAttempt result=True` and `clickFocus result=True`. The consolidated gate nevertheless remains rejected: the initial preview directly over an existing beige Tent still reported `VALID PLACEMENT (874,374) 13X11`; Armor and Transport each emitted `SquadTraySelectSkipped ... reason=NoCandidates`; and no helicopter/rope/drop sequence was visibly captured during the second Recruit. Stable samples were roughly `44-54 FPS`, with GPU samples about `9.7-20.6 ms`; these remain evidence only and authorize no VRP-072 render-policy mutation. VRP-072 remains unstarted. Blue Autobahn/canal isolation and road-over-water-without-bridge remain separate. The unrelated generated report and all `_Recovery` scenes remain unstaged; no protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |

| 2026-08-04 | Baked operation-map static-blocker occupancy correction | Child independent correctness gate; checked Windows GUI-licensing `Build/Logs/building-placement-baked-operation-map-occupancy-editor.log`; `[BuildingPlacementLiveOccupancyValidation] result=Passed tests=2`; checked aggregate `Build/Logs/editor-first-functional-batch-operation-map-occupancy.log`; `[EditorFirstProductionFunctionalBatchValidation] result=Passed suites=8 tests=92`; `git diff --check` | Exact packaged-Tent archetype is repaired and Editor-accepted; consolidated Android confirmation remains pending; parent progress remains `126/149 (85%)` | The earlier operation-map occupancy test omitted the `StaticGridBlocker` that the real baker adds alongside `RuntimeBuildingCombatTag` and `OperationMapBuildingComponent`. That let production discard every baked map building before its operation-map ownership exception, explaining why Samsung still accepted a `13x11` Contractor preview directly over the beige Tent. The predicate now keeps the exact baked operation-map archetype as an occupied footprint while continuing to exclude ordinary static blockers, prefabs, and non-operation-map runtime proxies. The focused gate now reproduces the baked component combination and the full 92-test Editor batch remains green. No APK was built or installed under the user-directed Editor-first batching strategy. Armor/Transport Android selection and visible rope/drop confirmation remain independent consolidated gates; VRP-072 remains unstarted. The unrelated generated report and all `_Recovery` scenes remain unstaged; no render policy, protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |

| 2026-08-04 | Late-packed authored-vehicle readiness and ownership reconciliation | Child independent correctness gate; checked host-access Windows wrapper `Build/Logs/map-vehicle-late-packed-readiness-editor-host.log`; `[MapVehiclePlacementStartupCompletionValidation] result=Passed tests=6`; checked aggregate `Build/Logs/editor-first-functional-batch-late-packed-readiness-host.log`; `[EditorFirstProductionFunctionalBatchValidation] result=Passed suites=8 tests=92`; `git diff --check`; bundled-SDK ADB/mDNS discovery | Packaged Armor/Transport `NoCandidates` startup cause repaired and Editor-accepted; consolidated Android confirmation remains pending; parent progress remains `126/149 (85%)` | Packed operation-map vehicles can arrive after the managed match shell's one-shot placement cursor. Startup now waits for the baked readiness contract's expected authored-vehicle count before consuming that cursor, preserves normal neutral-vehicle adoption, and only then reconciles stable-placement faction and canonical source identity. This prevents late presentation from remaining neutral/source-stale without causing a duplicate prefab spawn. The strengthened regression proves readiness is false before the expected authored marker, true afterward, and that the reconciled player tank selects through the real Armor roster path; the complete production/selection batch remains green. The first sandboxed rerun exceeded its 600-second licensing timeout and was rejected; an initial aggregate invocation used a nonexistent class and was also rejected. The recorded host-access wrapper runs passed unchanged code. No APK was built or installed under the Editor-first batching strategy. Samsung wireless service `adb-R5CTC1J02VB-lJmfCF` was discoverable at `192.168.2.33:44569` but not connected. VRP-072 remains unstarted; blue Autobahn/canal isolation and road-over-water-without-bridge remain separate. Both generated reports and all `_Recovery` scenes remain unstaged; no render policy, protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |

| 2026-08-04 | Consolidated post-Editor-batch Android package preparation | Child package-readiness gate; implementation commit `4fe833c18`; checked GUI-licensing wrapper `Build/Logs/editor-batch-4fe833c18-profiler-apk-shortpath.log`; `[DenseCityCandidateAndroidPackage] result=Passed`; exact 696,278,899-byte APK SHA-256 `4bab342b42eebfbaa1f9e0e31ea1014a2e39daa18ddf68858cc0bedf22e66f05`; temporary verified `W:\` project mapping removed after success | One fresh consolidated profiler package is accepted and ready for the Samsung functional pass; parent progress remains `126/149 (85%)` | The package was built only after the late-packed vehicle readiness gate passed 6/6 and the complete Editor-first functional batch passed 92/92. An initial invocation using the non-namespaced build class was rejected before building; the first real build was rejected at Gradle on Windows MAX_PATH; the unchanged checked build then passed through the previously accepted short project mapping, including the package ownership gate, and the mapping was removed. No APK installation or device acceptance is claimed here. This exact package is the only authorized input for the next consolidated Samsung pass. VRP-072 remains unstarted, and no render policy, protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. Both generated reports and all `_Recovery` scenes remain unstaged. |

| 2026-08-05 | Consolidated post-Editor-batch Samsung functional rejection | Child independent correctness gate; implementation/package head `4fe833c18`; exact checked 696,278,899-byte APK SHA-256 `4bab342b42eebfbaa1f9e0e31ea1014a2e39daa18ddf68858cc0bedf22e66f05`; streamed wireless install only to Samsung `SM-S918B` / `R5CTC1J02VB` at user-provided `192.168.2.33:45281`; exact activity cold launch `421 ms`; correctly scaled main-page Deploy; evidence under `Build/AndroidDenseCandidateProfiler/Evidence/2026-08-05-consolidated-functional`; Android screenshots, selection diagnostics, ownership counters, and frame diagnostics | Light Attack Helicopter production/completion and Gunship roster selection accepted; baked-building overlap rejection and Armor/Transport roster ownership rejected; Recruit completion accepted but the helicopter/rope/drop presentation remains visually rejected; parent progress remains `126/149 (85%)` | The authorized current-head package installed and reached the live operation map through the required main-page Deploy control. A Light Attack Helicopter entered the production queue, completed, raised runtime ownership, and selected through Gunship with the correct portrait and `520/520`. In an independent fresh session, a clear-ground `13x11` Contractor completed with the correct title, portrait, bounded marker, and `350/350`; Recruit spent `25` materials and raised `units/sourceKeys 5006 -> 5007` plus rendered models `22 -> 23`. The consolidated gate nevertheless remains rejected for three independent reasons. First, a `13x11` Contractor preview visibly intersecting an existing beige Tent still reported `VALID PLACEMENT`. Second, correctly scaled Armor and Transport roster taps emitted `SquadTraySelectSkipped ... reason=NoCandidates`. Third, twelve approximately 1.3-second post-Recruit frames showed no helicopter, rope, or drop presentation even though the unit completed. Previously accepted Contractor and spawned-soldier interaction evidence is retained rather than rerun. Functional samples were roughly `43-55 FPS`; this is evidence only and authorizes no VRP-072 render-policy mutation. VRP-072 remains unstarted. Blue Autobahn/canal isolation and road-over-water-without-bridge remain separate. Both generated reports and all `_Recovery` scenes remain unstaged; no protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |

| 2026-08-05 | Canonical operation-map combat-origin placement occupancy | Child independent correctness gate; checked GUI-licensing focused `Build/Logs/building-placement-canonical-combat-origin-editor.log` `[BuildingPlacementLiveOccupancyValidation] result=Passed tests=2`; rejected aggregate invocation `Build/Logs/editor-first-functional-batch-canonical-combat-origin.log` because the shorthand class did not exist; checked corrected aggregate `Build/Logs/editor-first-functional-batch-canonical-combat-origin-rerun.log` `[EditorFirstProductionFunctionalBatchValidation] result=Passed suites=8 tests=92`; `git diff --check` | The baked-building overlap cause exposed by Samsung is repaired and Editor-accepted; Armor/Transport ownership and visible rope/drop remain Editor-first; parent progress remains `126/149 (85%)` | The prior occupancy repair still derived an operation-map building rectangle from compatibility `UnitGrid` and `UnitFootprint`. Those fields can be stale or center-encoded differently after packed presentation adoption, while the canonical placement owner is `RuntimeBuildingCombatInfo.OriginCell/FootprintCells`. Placement occupancy now uses that canonical combat origin for operation-map buildings and retains the existing unit-grid path for infantry, vehicles, and legacy entities. The focused regression deliberately gives the baked building a stale compatibility center and proves its canonical rectangle blocks placement; the complete 92-test Editor-first functional batch remains green. No APK was built or installed under the user's batching strategy. VRP-072 remains unstarted. Both generated reports and all `_Recovery` scenes remain unstaged; no render policy, protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |

| 2026-08-05 | Headless packed-vehicle readiness-contract race | Child independent correctness gate; checked GUI-licensing focused `Build/Logs/map-vehicle-packed-contract-race-editor.log` `[MapVehiclePlacementStartupCompletionValidation] result=Passed tests=6`; checked aggregate `Build/Logs/editor-first-functional-batch-packed-vehicle-contract-race.log` `[EditorFirstProductionFunctionalBatchValidation] result=Passed suites=8 tests=92`; `git diff --check` | The packaged Armor/Transport `NoCandidates` startup race is repaired and Editor-accepted; visible helicopter/rope/drop remains the final Editor-first defect; parent progress remains `126/149 (85%)` | The earlier readiness repair waited for the expected authored-vehicle count only after a readiness contract already existed. In a headless packed build, the managed startup tick can run before the gameplay-buildings EntityScene supplies that contract; the empty contract query was treated as ready, permanently consuming the one-shot vehicle placement cursor while all vehicles were absent. Headless startup with configured placements now requires the packed readiness contract before progressing, while a live authoring root retains legacy compatibility. The strengthened regression proves missing-contract headless startup is fail-closed, then proves the existing contract/count, stable Armor identity, Transport identity, ownership reconciliation, and both roster selection paths. The complete 92-test Editor-first batch remains green. No APK was built or installed under the user's batching strategy. VRP-072 remains unstarted. Both generated reports and all `_Recovery` scenes remain unstaged; no render policy, protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |

| 2026-08-05 | Late-packed production-transport registry cache repair | Child independent correctness gate; rejected asynchronous full Menu-to-Deploy reproduction `Build/Logs/canonical-tent-full-in-map-repro-20260805-noquit2.log` at the pre-existing typed Addressables manifest mismatch (`2d7b...` resolved as `System.Object`) before gameplay readiness; checked GUI-licensing focused `Build/Logs/canonical-tent-late-packed-transport-editor.log` `[CanonicalTentTransportPresentationValidation] result=Passed transportVisible=1 ropeVisible=1 dropVisualVisible=1 ownership=canonical latePackedTransport=1`; refreshed visible `Build/EditorEvidence/CanonicalTentTransportDrop.png`; checked aggregate `Build/Logs/editor-first-functional-batch-late-packed-transport.log` `[EditorFirstProductionFunctionalBatchValidation] result=Passed suites=8 tests=93`; `git diff --check` | The packaged Recruit direct-spawn cause is repaired and Editor-accepted; one fresh consolidated APK and Samsung pass are dependency-ready; parent progress remains `126/149 (85%)` | The production transport cache previously keyed validity only by the registry list/dictionary object identities. Packed startup mutates those same collection instances after the first empty lookup, so a cached null helicopter survived after `Unit_Veh_Helicopter_Transport` arrived and Recruit bypassed presentation as `NotRequired`. Cache validity now also tracks collection counts and the resolved canonical helicopter/plane objects; late-packed content clears per-prefab settings and re-resolves the delivery transport. The regression proves the same empty list/dictionary instances first resolve no transport, then resolve the newly packed canonical helicopter. The focused presentation gate visibly retains the request during helicopter arrival and rope/drop and releases spawn only after delivery; the complete production/selection batch remains green at 93 tests. No APK was built or installed before all Editor-first fixes completed. VRP-072 remains unstarted. Both generated reports and all `_Recovery` scenes remain unstaged; no render policy, protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |

| 2026-08-05 | Zero-count bootstrap readiness-contract race | Child independent correctness gate; fresh checked package head `8ae22f48f`; exact 696,278,899-byte APK SHA-256 `1c6563a495998654eb9f1807c71e34aead1c79ce4e55788af3306dd4f978e395`; streamed only to Samsung `SM-S918B` / `R5CTC1J02VB`; main-page Deploy succeeded; Armor and Transport each emitted `SquadTraySelectSkipped ... reason=NoCandidates`; checked host-access GUI-licensing focused `Build/Logs/map-vehicle-zero-contract-readiness-editor.log` `[MapVehiclePlacementStartupCompletionValidation] result=Passed tests=6`; checked corrected aggregate `Build/Logs/editor-first-functional-batch-zero-contract-readiness.log` `[EditorFirstProductionFunctionalBatchValidation] result=Passed suites=8 tests=93`; `git diff --check` | The fresh package disproved functional closure; the newly exposed zero-count bootstrap-contract race is repaired and Editor-accepted; one replacement checked package and consolidated Samsung pass remain pending; parent progress remains `126/149 (85%)` | The current package installed and reached the candidate map, but visible Armor and Transport cards still had no player-owned world candidates. The earlier fail-closed repair covered an absent contract; it did not cover an earlier base-stream contract whose expected vehicle count was zero. With configured placements present, that placeholder was incorrectly treated as ready and allowed the one-shot placement cursor to finish before the candidate stream's positive 22-vehicle contract arrived. Packed EntityScene startup now requires a positive expected count before progressing, while legacy authoring-root startup remains compatible. The regression inserts the zero-count bootstrap contract before the positive map contract and proves startup remains closed. Two sandboxed GUI attempts failed licensing IPC and timed out; precise wrapper-owned cleanup, a verified stale-client restart, and the documented host-access checked-wrapper workaround produced the accepted focused and aggregate results. An aggregate shorthand class invocation was rejected before the corrected 93/93 pass. No replacement APK or Android acceptance is claimed yet. VRP-072 remains unstarted; blue Autobahn/canal isolation and road-over-water-without-bridge remain separate. Both generated reports and all `_Recovery` scenes remain unstaged; no protected/production asset, Jenkins/Unity path, or `productionCutover=0` changed. |

| 2026-08-05 | Candidate runtime-binding MonoScript serialization | Child independent correctness gate; rejected checked package head `ddb904691`; exact 696,278,808-byte APK SHA-256 `fd8bb448ed2c036c90dce297d2e47933660ebf89491009b579ea0f978fe407c7`; streamed only to Samsung `SM-S918B` / `R5CTC1J02VB`; main-page Deploy stalled at 90% with `OperationMapSourceScene code=MetadataBindFailed` and missing `decorationCombinedMeshBaker`; package log warned that `CombinedMeshBaker` was missing from the candidate scene; checked GUI-licensing diagnostic `Build/Logs/combined-mesh-baker-monoscript-without-movedfrom.log` `[CombinedMeshBakerMonoScriptValidation] result=Passed`; repair `Build/Logs/candidate-runtime-binding-resolvable-monoscript-repair.log` `[CandidateRuntimeBindingColdRepairValidation] result=Passed scenes=2`; cold focused `Build/Logs/candidate-runtime-binding-resolvable-monoscript-final-focused.log` `[OperationMapEntitySceneCandidateBakeAllValidation] result=Passed tests=25`; aggregate `Build/Logs/editor-first-functional-batch-resolvable-monoscript-final.log` `[EditorFirstProductionFunctionalBatchValidation] result=Passed suites=8 tests=93`; `git diff --check` | The replacement package disproved functional closure before gameplay; candidate binding serialization is repaired and Editor-accepted; one fresh checked package and consolidated Samsung pass remain pending; parent progress remains `126/149 (85%)` | The candidate builder previously deleted the valid copied `CombinedMeshBaker`, recreated it through a transient embedded `MonoScript`, externalized that row, and serialized a migration-only identity. Android Addressables stripped the component, so Deploy could not bind the operation-map view. Direct Unity diagnostics proved the script GUID existed but `MonoScript.GetClass()` returned null while the live component carried `MovedFrom`; removing that migration restored exact resolution to `Game.Runtime.CombinedMeshBaker`. The builder now preserves the typed component from the production `SaveScene` copy, both candidate bindings retain the concrete current identity, and focused coverage fails closed on GUID/class drift. Rejected identity-only attempts and one compile-diagnostic timeout are not counted as validation. No fresh APK or Android functional acceptance is claimed yet. VRP-072 remains unstarted; blue Autobahn/canal isolation and road-over-water-without-bridge remain separate. Both generated reports and all `_Recovery` scenes remain unstaged; no render policy, Jenkins/Unity path, or `productionCutover=0` changed. |

| 2026-08-05 | Resolvable-MonoScript fresh package consolidated Samsung rejection | Child independent correctness gate; implementation/package/documentation head `96be8078a`; checked GUI-licensing package `Build/Logs/editor-batch-96be8078a-profiler-apk-shortpath.log` `[DenseCityCandidateAndroidPackage] result=Passed`; exact 696,278,899-byte APK SHA-256 `4ed33553c81aa8b816337e6b7bb336e598b7ce17235c3945cac21f5d05826522`; streamed only to Samsung `SM-S918B` / `R5CTC1J02VB` at `192.168.2.33:33675`; main-page Deploy; screenshots and Android diagnostics under `Build/AndroidDenseCandidateProfiler/Evidence/2026-08-05-monoscript-consolidated` | Candidate metadata binding and baked-building overlap rejection accepted; Armor/Transport roster/world selection and visible helicopter/rope/drop rejected; parent progress remains `126/149 (85%)` | The fresh package contains the accepted candidate-binding repair, emits no missing-`CombinedMeshBaker` package warning, and reaches live gameplay through the required main-page Deploy route. A Contractor preview directly overlapping an existing beige Tent now visibly reports `BLOCKED BY ROAD OR BLOCKER`, accepting the overlap repair. Armor and Transport roster taps still emit `SquadTraySelectSkipped ... reason=NoCandidates`, so neither roster nor world selection can be accepted. A clear-ground Contractor completed and selected with the expected portrait/title and `350/350`; two Recruit actions spent `25` materials each and increased stable runtime unit/model ownership, retaining the earlier accepted spawned-soldier interaction evidence. Nevertheless, 16 initial frames plus 45 wide-camera frames covering the full production window showed no helicopter, rope, or drop, so the Android presentation remains rejected despite the accepted Editor gate. Functional samples were approximately `39-51 FPS`; this authorizes no VRP-072 mutation. VRP-072 remains unstarted. Blue Autobahn/canal isolation and road-over-water-without-bridge remain separate. Both generated reports and all `_Recovery` scenes remain unstaged; no protected/production asset, Jenkins/Unity path, render policy, or `productionCutover=0` changed. |

| 2026-08-05 | Completed-bootstrap late-packed vehicle ownership reconciliation | Child independent correctness gate; checked GUI-licensing focused `Build/Logs/map-vehicle-completed-bootstrap-reconcile-editor.log` `[MapVehiclePlacementStartupCompletionValidation] result=Passed tests=7`; rejected aggregate shorthand `Build/Logs/editor-first-functional-batch-completed-bootstrap-reconcile.log` before tests because `BuildingProductionSystemTests` was not the actual class; checked corrected aggregate `Build/Logs/editor-first-functional-batch-completed-bootstrap-reconcile-rerun.log` `[EditorFirstProductionFunctionalBatchValidation] result=Passed suites=8 tests=93`; `git diff --check` | The packaged Armor/Transport `NoCandidates` cause is repaired and Editor-accepted; visible helicopter/rope/drop remains open and one replacement package is pending; parent progress remains `126/149 (85%)` | The fresh package showed 22 authored vehicle presentations but no selectable player vehicle. A prior bootstrap stream can persist the map-vehicle cursor as queued and authoring-hidden before the candidate EntityScene arrives. When the positive readiness contract later became ready, the helper synchronized that persisted state, marked itself complete, and returned before its existing reconciliation branch could assign faction and canonical source identity. Packed startup now performs that reconciliation once before honoring a completed cursor. The new regression reproduces the completed cursor plus late-packed ready vehicle through the real update path and proves player ownership and canonical source identity; aggregate behavior remains green at 93/93. No package or Android acceptance is claimed yet. VRP-072 remains unstarted. Blue Autobahn/canal isolation and road-over-water-without-bridge remain separate. Both generated reports and all `_Recovery` scenes remain unstaged; no protected/production asset, Jenkins/Unity path, render policy, or `productionCutover=0` changed. |
| 2026-08-05 | Packed-infantry canonical transport fallback | Child independent correctness gate; checked GUI-licensing focused `Build/Logs/canonical-tent-packed-infantry-fallback-focused.log` `[CanonicalTentTransportPresentationValidation] result=Passed transportVisible=1 ropeVisible=1 dropVisualVisible=1 ownership=canonical latePackedTransport=1 packedInfantryFallback=1`; refreshed visible `Build/EditorEvidence/CanonicalTentTransportDrop.png`; checked aggregate `Build/Logs/editor-first-functional-batch-packed-infantry-fallback.log` `[EditorFirstProductionFunctionalBatchValidation] result=Passed suites=8 tests=93`; `git diff --check` | The packaged Recruit direct-spawn cause is repaired and Editor-accepted; one fresh checked package and consolidated Samsung pass remain pending; parent progress remains `126/149 (85%)` | The rejected Samsung package proved Recruit could remain spawnable while canonical delivery returned `NotRequired`. Transport lookup already resolved the explicitly configured canonical helicopter, but the default path discarded it when the packed produced prefab did not expose authoring-only production metadata. The fallback now assigns that configured helicopter only to metadata-less non-vehicle, non-air prefabs; likely ground and air vehicles remain fail-closed. The regression reproduces a packed infantry prefab without `UnitGridAuthoring` metadata and proves the canonical helicopter is retained, while the visible focused gate still proves helicopter, rope, drop, deferred spawn, and canonical ownership. The first restricted-host wrapper invocation was rejected before Unity start by duplicate `Path`/`PATH`; the checked host-access wrapper then passed. Its command host returned early after launching Unity, so only the exact wrapper-owned headless process that had already logged complete shutdown was terminated before the aggregate passed normally. No package or Android acceptance is claimed yet. VRP-072 remains unstarted. Blue Autobahn/canal isolation and road-over-water-without-bridge remain separate. Both generated reports and all `_Recovery` scenes remain unstaged; no protected/production asset, Jenkins/Unity path, render policy, or `productionCutover=0` changed. |
| 2026-08-05 | Packed-infantry fallback fresh profiler package | Implementation/package head `a01ecbf6f`; checked GUI-licensing short-path build `Build/Logs/editor-batch-a01ecbf6f-profiler-apk-shortpath.log` `[DenseCityCandidateAndroidPackage] result=Passed`; exact `Build/AndroidDenseCandidateProfiler/WarlineCapture-DenseCityCandidate-Profiler.apk`, 696,278,899 bytes, SHA-256 `a91eb453d577024d1c0b4ec26227cf860a4fb0f197817639ae603557e890ea4d` | One fresh checked package is accepted for the consolidated Samsung functional pass; no Android acceptance is claimed; parent progress remains `126/149 (85%)` | The APK was built only after the focused canonical delivery gate and complete 93/93 Editor-first functional batch passed. The checked build used the previously accepted temporary `W:` mapping to avoid Windows Gradle/MAX_PATH failure; the mapping was verified before use and removed immediately after the package passed. This exact hash is the only authorized input for the next Samsung pass. VRP-072 remains unstarted. Blue Autobahn/canal isolation and road-over-water-without-bridge remain separate. Both generated reports and all `_Recovery` scenes remain unstaged; no protected/production asset, Jenkins/Unity path, render policy, or `productionCutover=0` changed. |

| 2026-08-05 | Packed vehicle ownership-completion fence | Child independent correctness gate; rejected real Editor Menu-to-Deploy reproduction `%TEMP%\canonical-vehicle-packed-runtime-diagnostic-nonquit.log` before Match readiness on the pre-existing typed Addressables manifest mismatch (`2d7b...` resolved as `System.Object`); checked diagnostic profiler package `Build/Logs/packed-vehicle-runtime-diagnostic-profiler-apk.log` `[DenseCityCandidateAndroidPackage] result=Passed`; exact diagnostic APK 738,752,587 bytes, SHA-256 `4e9b1134df8bb9e82c6c8b1186e41d8097877c72eaa93434f7d863cae867c631`; Samsung `SM-S918B` / `R5CTC1J02VB` main-page Deploy and Armor/Transport diagnostics; checked GUI-licensing focused `Build/Logs/map-vehicle-packed-ownership-completion-focused.log` `[MapVehiclePlacementStartupCompletionValidation] result=Passed tests=8`; checked aggregate `Build/Logs/editor-first-functional-batch-packed-vehicle-ownership-completion.log` `[EditorFirstProductionFunctionalBatchValidation] result=Passed suites=8 tests=93`; `git diff --check` | Exact Android cause is proven and the ownership-completion fix is Editor-accepted; one clean replacement package and consolidated Samsung pass remain open; parent progress remains `126/149 (85%)` | Device diagnostics inspected all 22 actual packed vehicle entities. Every entity was non-prefab/non-disabled and carried the expected `UnitGrid`, `UnitMove`, `LocalToWorld`, motion/transport classification, `UnitDetailedVisualReference`, role-2 `OperationMapEntityPresentationIdentity`, correct operation-map id, placement index, and canonical source key; every one remained `Faction.Id=0`, so Armor/Transport correctly found no player candidates. The prior completed-cursor reconciliation existed, but the outer building-startup owner stopped invoking the vehicle startup tick as soon as queued/hidden progress reported complete. Packed-mode startup completion now additionally requires every readiness-contract vehicle to match its placement faction and canonical source identity, keeping the existing startup tick alive until reconciliation succeeds. The regression proves neutral packed ownership blocks completion, reconciliation assigns player ownership, and completion then opens. The diagnostic package is evidence only and is not final acceptance. VRP-072 remains unstarted. Blue Autobahn/canal isolation and road-over-water-without-bridge remain separate. Both generated reports and all `_Recovery` scenes remain unstaged; no protected/production asset, Jenkins/Unity path, render policy, or `productionCutover=0` changed. |
| 2026-08-05 | Ownership-completion fence fresh Samsung rejection | Child independent correctness gate; implementation/package head `cd1eed5fd`; recovered cold binding checks `Build/Logs/combined-mesh-baker-cd1eed5fd-restored-resolution.log` `[CombinedMeshBakerMonoScriptValidation] result=Passed` and `Build/Logs/candidate-runtime-binding-cd1eed5fd-reimport-repair.log` `[CandidateRuntimeBindingColdRepairValidation] result=Passed scenes=2`; checked GUI-licensing short-path package `Build/Logs/editor-batch-cd1eed5fd-profiler-apk-shortpath-rerun.log` `[DenseCityCandidateAndroidPackage] result=Passed`; exact 738,748,891-byte APK SHA-256 `0825b8a3a3506ad0e9682093d8c1d4f4bdcd8a5f6f8a4e606c11e0f07b4fee5f`; streamed only to Samsung `SM-S918B` / `R5CTC1J02VB`; corrected display-scaled main-page Deploy; evidence under `Build/AndroidDenseCandidateProfiler/Evidence/2026-08-05-packed-ownership-completion` | Fresh Android package disproves the Editor-only ownership fence; Armor roster selection remains rejected and parent progress remains `126/149 (85%)` | The valid main-page Deploy route passed operation-map readiness and logged `[LoadingGate] ready ... playRequested=1 simulationActive=1`; the loaded map retained 22 vehicle models. Armor still emitted `SquadTraySelectSkipped slot=CombatVehicles reason=NoCandidates`, and the exact live diagnostic again showed every packed vehicle as `faction=0 player=0`. The completion callback therefore did not keep the live startup branch open. Code audit identifies the remaining branch mismatch: the packed-mode requirement is captured once from the managed compatibility `sceneView` during initialization, while the compatibility vehicle authoring root remains assigned; the later positive packed readiness contract does not promote that frozen flag, and simulation ticks no longer invoke map-vehicle reconciliation. Transport/world selection and helicopter/rope/drop were not rerun after the Armor gate rejected; earlier accepted overlap, Contractor, and spawned-soldier evidence remains retained. The APK will not be reinstalled. The next bounded correction must make the live positive readiness contract promote reconciliation independently of the stale managed-mode snapshot and must cover the post-startup late-contract route. VRP-072 remains unstarted. Blue Autobahn/canal isolation and road-over-water-without-bridge remain separate. Both generated reports and all `_Recovery` scenes remain unstaged; no protected/production asset, Jenkins/Unity path, render policy, or `productionCutover=0` changed. |
| 2026-08-05 | Live packed-contract ownership promotion | Child independent correctness gate; checked GUI-licensing focused `Build/Logs/map-vehicle-live-contract-promotion-focused.log` `[MapVehiclePlacementStartupCompletionValidation] result=Passed tests=9`; rejected aggregate shorthand `Build/Logs/editor-first-functional-batch-live-packed-contract-promotion.log` before tests because `BuildingProductionSystemTests` was not the actual class; checked corrected aggregate `Build/Logs/editor-first-functional-batch-live-packed-contract-promotion-rerun.log` `[EditorFirstProductionFunctionalBatchValidation] result=Passed suites=8 tests=93`; `git diff --check` | The proven stale-mode/post-startup ownership branch is repaired and Editor-accepted; one fresh checked package and consolidated Samsung pass remain required; parent progress remains `126/149 (85%)` | A positive live `OperationMapEntityPresentationReadinessContract` now promotes packed vehicle handling even when the managed startup snapshot was compatibility mode and a compatibility authoring root remains assigned. If that contract arrives after startup has already opened, the simulation path invokes only the existing vehicle placement/reconciliation action while canonical ownership is incomplete, then stops once all packed vehicles match placement faction and source identity. The regression starts from a completed compatibility cursor with a positive late contract and a neutral/source-stale packed vehicle, proving the real update path promotes packed mode and assigns player ownership/canonical source. The first focused wrapper command host timed out after launching Unity; the exact wrapper-started Editor passed 9/9 and logged complete shutdown before its lingering process was terminated. No APK or Android acceptance is claimed yet. VRP-072 remains unstarted. Blue Autobahn/canal isolation and road-over-water-without-bridge remain separate. Both generated reports and all `_Recovery` scenes remain unstaged; no protected/production asset, Jenkins/Unity path, render policy, or `productionCutover=0` changed. |
| 2026-08-05 | Live packed-contract fresh Samsung rejection | Child independent correctness gate; implementation/package head `6158e1388`; first checked short-path package `Build/Logs/editor-batch-6158e1388-profiler-apk-shortpath.log` rejected before Gradle on the known cold `CombinedMeshBaker` importer-cache condition; reversible source-touch recovery `Build/Logs/combined-mesh-baker-6158e1388-reimport-touch.log` passed, exact restored source `Build/Logs/combined-mesh-baker-6158e1388-restored-resolution.log` passed, and candidate repair `Build/Logs/candidate-runtime-binding-6158e1388-reimport-repair.log` passed `scenes=2`; checked package retry `Build/Logs/editor-batch-6158e1388-profiler-apk-shortpath-rerun.log` `[DenseCityCandidateAndroidPackage] result=Passed`; exact 738,752,365-byte APK SHA-256 `c1a2d3ee49bdf1c0e147e2505052438658d1e1faa6f21639228a81975461215b`; streamed only to Samsung `SM-S918B` / `R5CTC1J02VB`; evidence under `Build/AndroidDenseCandidateProfiler/Evidence/2026-08-05-live-packed-contract-promotion` | Fresh Android package disproves the live-contract Editor fixture; Armor selection remains rejected and parent progress remains `126/149 (85%)` | The required main-page Deploy route reached `[LoadingGate] ready ... playRequested=1 simulationActive=1` with 22 rendered vehicle models. Armor still emitted `SquadTraySelectSkipped slot=CombatVehicles reason=NoCandidates`; exact live diagnostics still show every packed vehicle neutral, beginning with `placement=11 ... faction=0 player=0`. Therefore the new simulation reconciliation branch is not reached in the actual packaged composition, despite the focused 9/9 and aggregate 93/93 Editor gates. Transport/world selection and helicopter/rope/drop were not rerun after Armor rejected; earlier accepted overlap, Contractor, and spawned-soldier evidence remains retained. This APK will not be reinstalled. The next step must instrument the actual packaged building-runtime composition/callback invocation and live config/contract state before another ownership change; synthetic helper fixtures are insufficient. VRP-072 remains unstarted. Blue Autobahn/canal isolation and road-over-water-without-bridge remain separate. Both generated reports and all `_Recovery` scenes remain unstaged; no protected/production asset, Jenkins/Unity path, render policy, or `productionCutover=0` changed. |
| 2026-08-05 | Late operation-map vehicle config resolution | Child independent correctness gate; diagnostic implementation head `d2a9dfe75`; checked diagnostic package `Build/Logs/editor-batch-d2a9dfe75-packaged-composition-diagnostic-rerun.log` `[DenseCityCandidateAndroidPackage] result=Passed`; exact 738,751,031-byte APK SHA-256 `d007025695afcf08faf63a942b1bce0cef9d2277a6ba8dbc13e957834cae172f`; streamed only to Samsung `SM-S918B` / `R5CTC1J02VB`; packaged markers `[MapVehicleOwnershipRuntime] phase=CompositionInit configAssigned=0 placements=-1 authoringRootAssigned=1 configuredPacked=1`, `phase=RuntimeContextReady ... positiveContract=1`, and `phase=LiveContractObserved ... authoredVehicles=22`; checked host-access GUI-licensing focused `Build/Logs/map-vehicle-live-config-provider-focused-host-final.log` `[MapVehiclePlacementStartupCompletionValidation] result=Passed tests=10`; checked aggregate `Build/Logs/editor-first-functional-batch-live-config-provider-host.log` `[EditorFirstProductionFunctionalBatchValidation] result=Passed suites=8 tests=93`; `git diff --check` | The actual packaged composition cause is proven and the live-config correction is Editor-accepted; one fresh checked package and consolidated Samsung pass remain required; parent progress remains `126/149 (85%)` | The diagnostic APK proved that all 22 baked vehicle presentations and the positive readiness contract were live, while managed building composition had captured `MapVehiclePlacementConfig=null`; consequently the existing ownership callback had no placement/faction/source contract to reconcile and every rendered vehicle remained neutral. Match bootstrap now supplies bounded live providers for the operation-map vehicle config and authoring root; every building-runtime vehicle update, ownership-completion check, and startup-completion check resolves the current values instead of the stale initialization snapshot. The new integration regression initializes composition with a null snapshot, publishes the config later, invokes the real simulation update, and proves the neutral packed tank becomes player-owned with canonical source identity. Restricted GUI attempts were rejected on licensing IPC; after precise wrapper-owned cleanup, verified stale-client restart, official Unity-log review, and the documented checked host-access recovery, the focused and aggregate wrappers returned success. No corrected APK or Android acceptance is claimed yet. The diagnostic APK will not be reinstalled. VRP-072 remains unstarted. Blue Autobahn/canal isolation and road-over-water-without-bridge remain separate. Both generated reports and all `_Recovery` scenes remain unstaged; no protected/production asset, Jenkins/Unity path, render policy, or `productionCutover=0` changed. |
| 2026-08-05 | Baked vehicle faction ownership | Child independent correctness gate; checked GUI-licensing focused `Build/Logs/vehicle-baked-faction-dedicated-authoring-focused.log` passed 11/11; transactional candidate migration `Build/Logs/vehicle-baked-faction-dedicated-candidate-migration.log` passed 22; final real candidate bake `Build/Logs/vehicle-baked-faction-dedicated-real-candidate-bake.log` passed 432 buildings/22 vehicles; dense ownership propagation `Build/Logs/vehicle-baked-faction-dedicated-dense-propagation.log` passed 22; focused actual dense bake `Build/Logs/vehicle-baked-faction-dedicated-real-dense-focused.log` passed `placementFactionParity=22/22`; aggregate `Build/Logs/editor-first-functional-batch-vehicle-baked-faction-dedicated.log` passed 93/93 | The exact packaged neutral-faction cause is repaired in baked authoring and Editor-accepted; one fresh checked package and consolidated Samsung pass remain required; parent progress remains `126/149 (85%)` | The candidate runtime intentionally omits `MapVehiclePlacementConfig`, so ownership can no longer be recovered from a managed provider. A dedicated authoring component exists only on the 22 gameplay-vehicle models; its authoritative per-placement faction bakes atomically into both `Faction` and a diagnostic `OperationMapAuthoredVehiclePresentation` placement/faction contract. Rollback-backed transactions updated only the two candidate EntityScenes; accepted and production sources remained hash-guarded. The final protected candidate bake and the real dense EntityScene Android consumes passed all 22 exact placement/faction pairs, including placement 9 remaining neutral. The broad dense validator is honestly rejected on a separate pre-existing building-authoring count mismatch (`4977/5403`) before its vehicle phase, so only the focused actual-dense vehicle bake is accepted. The final dedicated-component aggregate passed 93/93. No APK or Android acceptance is claimed yet. VRP-072 remains unstarted. Blue Autobahn/canal isolation and road-over-water-without-bridge remain separate. Both generated reports and all `_Recovery` scenes remain unstaged; accepted/production sources, Jenkins/Unity paths, render policy, and `productionCutover=0` remain unchanged. |
| 2026-08-05 | Placement confirmation parent-canvas ownership | Child independent correctness gate; Samsung pointer/InputDispatcher evidence under `Build/AndroidDenseCandidateProfiler/Evidence/2026-08-05-baked-vehicle-ownership`; checked GUI-licensing focused `Build/Logs/build-placement-parent-canvas-narrow-focused-live.log` `[BuildPlacementParentCanvasValidation] result=Passed`; checked aggregate `Build/Logs/editor-first-functional-batch-placement-parent-canvas-rerun.log` `[EditorFirstProductionFunctionalBatchValidation] result=Passed suites=8 tests=93`; `git diff --check` | The proven Android confirmation-bar click-through defect is repaired and Editor-accepted; one fresh checked package and remaining consolidated Samsung gates are required; parent progress remains `126/149 (85%)` | The visible Confirm-center tap reached Unity at the correct coordinate but bypassed UI raycasts and selected the world behind the bar. The transient bar's nested local Canvas/GraphicRaycaster, introduced by HUD batching, allowed rendered and interactive geometry to diverge on Android. The bar now remains on the parent shell Canvas/raycaster; all other HUD batching is unchanged. The focused live-component regression exercises `Awake`/layout caching and proves no local Canvas or GraphicRaycaster is created. Two earlier broad/cold fixtures were rejected before the bounded assertion on unrelated menu-router/prefab-import state and are not counted as passes. No replacement APK or Android acceptance is claimed yet. VRP-072 remains unstarted. Blue Autobahn/canal isolation and road-over-water-without-bridge remain separate. Both generated reports and all `_Recovery` scenes remain unstaged; no protected/production asset, Jenkins/Unity path, render policy, or `productionCutover=0` changed. |
| 2026-08-06 | Consolidated Samsung functional acceptance | Child independent functional gate; implementation/package head `8a2643b4e`; corrected full reimport; APH-808 `Build/Logs/aph808-after-bounded-source-touch.log` passed; focused parent-canvas `Build/Logs/build-placement-parent-canvas-after-import-recovery.log` passed; aggregate `Build/Logs/editor-first-functional-batch-after-import-recovery.log` passed 93/93; checked package `Build/Logs/editor-batch-8a2643b4e-profiler-apk-after-import-recovery.log` passed with zero missing-script warnings; exact 777,444,713-byte APK SHA-256 `376018de87667295184fe968de027751c5aeac3b3227dae0bba22b2916cf95ec`; device evidence under `Build/AndroidDenseCandidateProfiler/Evidence/2026-08-06-placement-parent-canvas-import-recovery` | The complete Editor-first functional queue is Android-accepted; parent progress remains `126/149 (85%)` because this independent gate closes no parent checklist row | The package was installed exactly once only to authenticated Samsung `SM-S918B` / `R5CTC1J02VB`. Following one explicitly user-authorized cold app restart, the correctly scaled main-page Deploy route reached live gameplay with `playRequested=1` and `simulationActive=1`. Normal visible UI accepted Armor roster selection (`2`), Transport roster selection (`1`), and direct world selection of the intended APC. A `13x11` Contractor preview visibly rejected occupied terrain as `BLOCKED BY ROAD OR BLOCKER`, and the repaired transient bar consumed Cancel without selecting the world behind it. The canonical Contractor Tent selected at `350/350`; Recruit delivery then visibly showed helicopter, vertical rope, suspended drop, and completed ground spawn, with materials and stable unit/model/source counts advancing consistently. Earlier accepted Contractor and spawned-soldier interaction evidence remains retained. The initial wrong unscaled Skirmish route is rejected evidence; the subsequent wait was solely the automation's explicit cold-restart authorization fence and not a product, package, or licensing blocker. No reinstall or second restart occurred. VRP-072 remains unstarted. Blue Autobahn/canal isolation and road-over-water-without-bridge remain separate. Both generated reports and all `_Recovery` scenes remain unstaged; no protected/production asset, Jenkins/Unity path, render policy, or `productionCutover=0` changed. |

| 2026-08-07 | Child VRP-072 Android-evidence shadow/small-detail policy Editor checkpoint | Child focused render virtualization passed `48/48`; Android visual-quality passed `17/17`; exact inventory/database determinism and two-pass direct bake passed; complete Editor-first functional aggregate passed `93/93` | Child implementation is Editor-accepted; parent progress remains `126/149 (85%)` until the child's fresh package and matched Android visual/GPU gate pass | The child retained LOD0 because the baked candidate contains no alternate LOD recipes. Exactly `12,185` eligible logical part rows now stop casting/static shadows while continuing to receive shadows: all virtualized Vegetation/Prop plus `882` rows at the existing `1 m` small-detail threshold. Large structures, resident exceptions, gameplay ownership, production cutover, and protected/production assets remain unchanged. The regenerated database has four fixed buckets and `7,784` slots with exact measured-envelope headroom; no VRP-074 capacity tuning is included. One pushed implementation revision, one fresh checked APK, and matched Android visual/GPU evidence remain required before the child advances to `57/80`; no parent checklist row closes at this checkpoint. Blue Autobahn/canal isolation and road-over-water-without-bridge remain separate. |
| 2026-08-07 | Child VRP-072 fresh checked profiler package | Pushed child implementation head `2c43c1f75`; checked package log `Build/Logs/vrp072-2c43c1f75-profiler-apk-qualified.log`; exact `777,444,713`-byte APK SHA-256 `e07e68af43a80fe6c540ac40ea6466285c35802711b04491fb3fc014119876f2` | Child package is accepted but not installed; parent progress remains `126/149 (85%)` and child remains `56/80` pending matched Android visual/GPU acceptance | The fully qualified checked package method produced the candidate-only profiler APK from the pushed implementation revision with production EntityScene inclusion `0`. Exact Samsung `SM-S918B` / `R5CTC1J02VB` was not reachable at its prior endpoint, and the newly started bundled ADB daemon did not answer its local protocol; only those verified task-started ADB processes were stopped. No install, screenshot, frame sample, GPU sample, or 57/80 closure is claimed. |
| 2026-08-07 | Child VRP-072 matched Samsung visual/GPU acceptance | Official Google-signed Platform Tools ADB `37.0.1-15733141`; one explicitly authorized `install --no-streaming -r` to authenticated Samsung `SM-S918B` / `R5CTC1J02VB` at `192.168.2.33:43479`; exact `777,444,713`-byte APK SHA-256 `e07e68af43a80fe6c540ac40ea6466285c35802711b04491fb3fc014119876f2`; evidence under `Build/AndroidDenseCandidateProfiler/Evidence/2026-08-07-vrp072-shadow-policy` | Child `VRP-072` is Android-accepted and advances to `57/80`; parent progress remains `126/149 (85%)` because no parent checklist row closes | The fresh package reached normal Deploy gameplay. Fixed-crop analysis reduced historical pitch-black urban pixels from `10.527%` to `4.323-4.932%` across three matched daylight captures while preserving complete geometry without gross holes, popping, shadow/material leakage, or gameplay regression. Base evidence showed `52-56 FPS`, daylight urban evidence `45-56 FPS`, and the metrics-enabled camera traversal held `54-55 FPS` after rebuilds. Runtime command versions `1-4` all reported `overflow=0 deficit=0`; the densest sampled command used `1,469/7,784` slots across 64 cells and 941 active placements. This closes only the child's shadow/small-detail Android visual/GPU regression gate. Phase 9's sustained-60-FPS acceptance remains open; production cutover, protected assets, blue Autobahn/canal isolation, and road-over-water-without-bridge remain unchanged/separate. The retained consolidated Samsung functional evidence was not needlessly rerun. |
| 2026-08-07 | Child VRP final-performance duration/threshold amendment | Explicit user authorization; child contract-only update | Child Phase 9 now accepts average `>=54 FPS`, 10th-percentile `>=50 FPS`, average frame time `<=18.6 ms`, and a final two-minute thermal route; parent progress remains `126/149 (85%)` | This intentionally supersedes the child's forward-looking 60-FPS-class and ten-minute-thermal requirements while retaining 120-second representative routes, p95 `<20 ms`, p99 `<25 ms`, CPU/GPU, allocation, overflow, lifecycle, visual, and gameplay-equivalence gates. The amendment itself closes no child or parent checklist row; the exact final candidate must still produce fresh diagnostics-disabled device evidence. Historical rows remain unchanged. |
| 2026-08-08 | Child `VRP-073` exact representation/package metrics | Checked host-access GUI-licensing inventory `Build/Logs/vrp073-exact-metrics-inventory-host.log`; database determinism `Build/Logs/vrp073-exact-metrics-database-host.log`; real two-pass candidate bake `Build/Logs/vrp073-exact-metrics-direct-bake-host.log`; deterministic report hashes; `git diff --check` | Child advances to `58/80`; parent progress remains `126/149 (85%)` because no parent checklist row closes | The current candidate deterministically reconciles `76,517` authored source rows, `61,925` eligible logical rows, `61,783` selected packed physical renderers, `14,017` packed resident physical rows, `40,460` placements, `9,107` prototypes, `12,293` parts, `1,934` occupied cells, four fixed policy buckets, and `7,784` slots. All four buckets retain exact measured-envelope 20% ceiling headroom over `14,592` canonical samples; packed output contains zero eligible source rows. Database content/order hashes remain `bfb350f0...96e` / `a43040ee...318`, and two real bakes reproduce fingerprint `0e6e4746...caa`. This is metric regeneration only: VRP-074 owns representative-route capacity tuning and VRP-075 owns the packed `MaterialMeshInfo` limit. Production/protected assets, `productionCutover=0`, the blue Autobahn/canal defect, and road-over-water-without-bridge remain unchanged/separate. The two modified generated package reports and all `_Recovery` scenes remain preserved and unstaged. |

| 2026-08-08 | Child `VRP-074` bounded tactical-follow transition Editor checkpoint | User-authorized child scope amendment; exact Samsung before-route evidence; focused `Build/Logs/vrp074-tactical-follow-bounded-transition-focused.log` passed `33/33`; aggregate `Build/Logs/vrp074-editor-first-functional-batch-corrected.log` passed `93/93`; `git diff --check` | Child remains `58/80` pending fresh package and matched Android chained-route acceptance; parent remains `126/149 (85%)` because no parent checklist row closes | The representative route proved the fixed `7,784` slots cover steady max zoom and isolated follow, while a far-view-to-follow transition created a transient `4,853` deficit from incoherent camera position/look-target movement. Capacity inflation would exceed the next child's `8,000` proxy limit, so the user authorized a narrow camera interpolation repair. Position and a virtual look point now share damping, preserving a bounded downward frustum without changing capacities, gameplay selection/follow ownership, production/protected assets, or `productionCutover=0`. Fresh checked package and exact Samsung after-route evidence remain required before child closure. The generated reports and `_Recovery` scenes remain preserved and unstaged; blue Autobahn/canal and road-over-water-without-bridge remain separate. |
| 2026-08-08 | Child `VRP-074` fresh-package matched Samsung chained-route acceptance | Pushed implementation `a8cbe99c0`; checked profiler package log; exact APK SHA-256 `c63ca8a3...88e6`; one no-streaming install through official ADB 37 to verified Samsung `SM-S918B` / `R5CTC1J02VB`; evidence under `Build/AndroidDenseCandidateProfiler/Evidence/2026-08-08-vrp074-bounded-transition-after` | Child advances to `59/80`; parent remains `126/149 (85%)` because no parent checklist row closes | Cold main-page Deploy and normal visible controls reproduced the exact max-zoom/remote-pan/Armor Camera chain. Metrics command versions `1-5` all retained `overflow=0 deficit=0`; the maximum sampled enabled count was `1,193/7,784`, and repaired far follow settled at `489/7,784`. Early/mid/final captures retain a bounded downward view with intact presentation and no holes, popping, stale bindings, or material leakage. Fixed capacity remains `7,784` with the accepted measured-envelope headroom; no production/protected asset, gameplay ownership, or `productionCutover=0` value changed. Generated reports and `_Recovery` scenes remain preserved and unstaged; blue Autobahn/canal and road-over-water-without-bridge remain separate. |
| 2026-08-08 | Child `VRP-075` exact packed-render limits | Accepted checked host-access GUI-licensing two-pass bake `Build/Logs/vrp075-packed-mmi-two-pass-accepted.log`; schema-4 direct report `Design/AgentReports/2026-07-30_dense_city_render_virtualization_pilot_enabled.json`; two rejected diagnostic count assumptions retained in their logs; `git diff --check` | Child advances to `60/80` and completes Phase 7; parent remains `126/149 (85%)` because no parent checklist row closes | The final packed query excludes Unity `BakingOnlyEntity` conversion rows and measures exactly `14,592/24,000` shipped map entities carrying `MaterialMeshInfo`. The separately validated fixed proxy pool is `7,784/8,000`; every enabled subset is therefore statically bounded below the limit, while the matched Samsung route independently peaked at `1,193` with zero overflow/deficit. Two failed assumptions were rejected rather than normalized: logical resident rows are not one-to-one render entities, and proxy ownership does not co-locate with `MaterialMeshInfo` on one converted entity. Two final real bakes reproduce fingerprint `d08de8ef11048a8bbafe34f7f429daf03de89a7ff9625db6fa3db9a530980bac`. Restricted-host process access and wrapper detachment were recovered through the authorized checked GUI-licensing host route, so no licensing or launcher condition remained a passive blocker. Production/protected assets, `productionCutover=0`, both unrelated modified package reports, all `_Recovery` scenes, the blue Autobahn/canal defect, and road-over-water-without-bridge remain unchanged/separate. |
| 2026-08-08 | Child `VRP-080` direct logical/resident parity | Checked source gate `Build/Logs/vrp080-current-source-logical-parity.log`; checked packed gate `Build/Logs/vrp080-current-packed-resident-parity.log`; exact deterministic content/order hashes and two packed fingerprints; `git diff --check` | Child advances to `61/80`; parent remains `126/149 (85%)` because no parent checklist row closes | All `76,517` material/submesh source rows resolve to exactly `61,925` immutable logical rows or `14,592` named resident exceptions. Source-derived persisted fields match exactly for mesh/material/submesh, identities, matrices, bounds, color, state/semantic ownership, policy, LOD, shadows, and spatial membership; two database builds are byte-identical. The packed ownership layer separately reconciles `75,800` renderer owners into `61,783` stripped virtualized renderers and `14,017` unique retained renderer-owner/path mappings, reproducing fingerprint `d08de8ef...0bac` twice. The `717` row/renderer difference is the explicit multi-material/submesh expansion, not missing ownership. No implementation or generated report byte changed. Production/protected assets, `productionCutover=0`, preserved worktree files, blue Autobahn/canal isolation, and road-over-water-without-bridge remain unchanged/separate. |
| 2026-08-08 | Child `VRP-081` exhaustive materialized parity | Checked compile gate `Build/Logs/vrp081-materialized-parity-compile-gate-rerun.log`; checked host-access GUI-licensing production-job gate `Build/Logs/vrp081-canonical-envelope-materialized-parity-final.log`; generated `Design/AgentReports/2026-08-08_dense_city_render_materialized_parity.json`; `git diff --check` | Child advances to `62/80`; parent remains `126/149 (85%)` because no parent checklist row closes | The child now executes the real selection, state-aware gather, deterministic assignment, and parallel slot-apply jobs across all `14,592` accepted `12 x 9` / `9 x 12` materialized envelopes in intact and destroyed state (`29,184` samples). Exact reciprocal binding, enable/disable, release reset, matrix, bounds, material, mesh, submesh, bucket, placement/part, and linear-color parity passes with zero overflow or extra active slots. Peak enabled counts are `4,773` intact and `4,396` destroyed inside the unchanged `7,784` pool; fingerprint is `7674a2c0...0b18`. Historical whole-map and sparse-pilot five-camera fixtures are explicitly rejected as capacity inputs for the expanded current database; five-camera screenshots remain owned by child VRP-086. No runtime algorithm or capacity changed, production/protected assets and `productionCutover=0` remain unchanged, preserved worktree files remain unstaged, and the two independent map defects remain separate. |

### Editor-First Functional Acceptance Queue

Android packaging and device iteration were blocked until every row below passed through checked Unity wrappers and visible Editor evidence. All six rows now pass; Android final functional confirmation is dependency-ready. Each row remains a separate bounded correctness item, and evidence for one row cannot close another.

- [x] Correct selection-panel image mapping for the selected owner, including Contractor Tent.
- [x] Selection marker matches the selected owner's rendered presentation bounds, while direct-click ownership remains independent from broad gameplay footprints.
- [x] Vehicle world and roster selection resolve the intended canonical vehicle.
- [x] Unit production visibly runs the helicopter arrival, rope/drop, and completion sequence.
- [x] The spawned unit visibly exists after delivery and is interactable through the normal selection path.
- [x] Building placement/spawn visibly completes with canonical ownership, footprint, interaction, and presentation intact.

| 2026-08-08 | Child `VRP-082` current packed lifecycle closure and non-passive Windows recovery | Child readiness sync `Build/Logs/vrp082-current-readiness-contract-role-sync-recovered-space.log`; runtime-content rebuild `Build/Logs/vrp082-current-overlap-runtime-content-role-sync-retry.log`; packed lifecycle XML `Build/TestResults/vrp082-current-packed-lifecycle-overlap-accepted.xml` `2/2`; markers for two cycles and failure/reset/retry with `staleEntities=0`; child focused `48/48`; aggregate `93/93`; `git diff --check` | Child `VRP-082` accepted at `63/80`; parent progress remains `126/149 (85%)` because no parent checklist row changes | Current packed readiness now reconciles retained/virtualized overlap instead of double-counting it and synchronizes the actual candidate to `9,544` accepted identities, `36,304` generated identities, `4,977` buildings, `22` vehicles, and `40,849` render-only identities. Two complete packed load/unload cycles and one intentional readiness failure followed by cleanup/reset/retry/unload leave no stale scene, section, database, or proxy-slot entities. Bounded Win32-1224 mapped-file recovery, checked-wrapper live-log/fatal-marker handling, the authorized version-matched licensing fallback, and exact rebuildable-cache disk recovery prevent infrastructure failures from becoming passive blockers without bypassing wrappers or pass markers. Rejected zero-test, wrong-mode, wrong-schema, editor-Relay-noise, stale namespaced execute-method, licensing IPC, and disk-full attempts remain recorded as rejected. Production/protected ownership and `productionCutover=0` are unchanged; unrelated reports and all `_Recovery` scenes remain preserved; blue Autobahn/canal and road-over-water-without-bridge remain separate. |
| 2026-08-08 | Child `VRP-083` zero camera-travel streaming operations | Checked current dense production PlayMode route `Build/Logs/vrp083-dense-packed-camera-zero-operations-production-camera.log` and XML `1/1`; `[PackedCameraTravelOperations] result=Passed mapSceneOperations=0 addressablesOperations=0 staticStreamerOperations=0`; child focused `48/48`; aggregate `93/93`; `git diff --check` | Child advances to `64/80`; parent progress remains `126/149 (85%)` because no parent checklist row changes | Monotonic diagnostic counters at the existing loader/streamer boundaries prove normal production-camera travel after packed readiness starts no source-scene load/unload, presentation-manifest/Addressables load, packed EntityScene load/unload, or static-streamer operation. The same route retains stable section/database/residency ownership. Rejected synthetic top-down projection and incorrect aggregate-class attempts remain rejected; acceptance uses the real production camera and the corrected 93-test entry point. No production/protected ownership, `productionCutover=0`, parent checklist state, unrelated modified report, or `_Recovery` scene changed; blue Autobahn/canal isolation and road-over-water-without-bridge remain separate. |
| 2026-08-08 | Child `VRP-084` camera-travel simulation-state independence | Checked current dense production PlayMode route `Build/Logs/vrp084-dense-packed-camera-authoritative-state.log` and XML `1/1`; exact marker `simulationEntities=4999 buildings=4977 vehicles=22 healthOwners=4999 canonicalStates=4530`; child focused `48/48`; aggregate `93/93`; `git diff --check` | Child advances to `65/80`; parent progress remains `126/149 (85%)` because no parent checklist row changes | A controlled quiescent interval after packed readiness isolates camera causality and proves exact authoritative grid, faction, health, building/vehicle ownership, destroyed/presentation, production-cursor, canonical building-state/version, and runtime gameplay-flag parity before/after normal production-camera travel. The original simulation-active flag is restored. Continuously normalized presentation-facing vehicle transforms observed in rejected timed diagnostics are not misclassified as authoritative gameplay state. Counts and canonical values are exact; no production/protected ownership, `productionCutover=0`, parent checklist row, preserved worktree file, or independent map defect changed. |
| 2026-08-08 | Child `VRP-085` two-run full virtualization Bake All determinism | Checked GUI-licensing full gate `Build/Logs/vrp085-two-full-bake-all-determinism.log`; two complete current passes; exact nine-output manifest `131,560,240` bytes / SHA-256 `efe4a13c...8c21`; content/order hashes `bfb350f0...96e` / `a43040ee...318`; direct packed fingerprint `23bebb45...55a`; child focused `48/48`; aggregate `93/93`; `git diff --check` | Child advances to `66/80`; parent progress remains `126/149 (85%)` because no parent checklist row changes | The current child Bake All gate composes the exact inventory/source-row, prototype/part, logical-placement, spatial-cell, measured-capacity, transactional database, and real direct packed-bake stages twice. Every persisted logical byte, database count/capacity/hash/order field, packed ownership count, and packed fingerprint agrees, while protected production remains unchanged. The historical pre-expansion candidate no-op runner is not used as a substitute because it retains obsolete candidate-count assumptions and owns an unrelated preserved report. The child dependency hash regeneration is stable across both current passes. Production/protected ownership, `productionCutover=0`, parent checklist state, preserved Android report and `_Recovery` scenes, and both independent map defects remain unchanged/separate. |
| 2026-08-08 | Child `VRP-086` five-camera and camera-edge visual continuity | Checked recovered full production PlayMode `Build/Logs/vrp086-production-five-camera-edge-accepted.log`; XML `1/1`; `17` accepted frames across five fixed views before/after a seven-frame edge traversal; exact marker records max repeated mean delta `0.0005080157`, interior changed ratio `0.0001595052`, `2,612` physical slot reassignments, and zero holes/stale logical bindings/overflow/deficit; generated child report; visual inspection; child focused `48/48`; aggregate `93/93`; `git diff --check` | Child Phase 8 completes at `67/80`; parent progress remains `126/149 (85%)` because no parent checklist row changes | The child now exercises the real packed Menu -> Match production camera instead of the obsolete sparse pilot. Exact sorted placement/part/bucket logical sets reproduce after physical pool recycling, all active ownership is unique and bounded, and repeated pixels remain within the established parity thresholds. Rejected bare-terrain, inactive historical-overview, exact-PNG, and exact-physical-slot diagnostics are retained rather than counted. The authorized non-passive licensing ladder recovered a detached GUI wrapper and stale generic licensing client before the checked batchmode route passed. Parent ownership, protected/production assets, `productionCutover=0`, the preserved Android report and `_Recovery` scenes, and both independent map defects remain unchanged/separate. |
| 2026-08-08 | Child `VRP-090` exact clean-cache candidate profiler APK | Accepted checked wrapper `Build/Logs/vrp090-3ca09f86c-clean-profiler-apk-short-gradle-recovered.log`; exact pushed head `3ca09f86c9a99fb3dee48e4548bf4a5204b2ada1`; package marker passed with candidate EntityScene ownership and `productionEntitySceneIncluded=0`; `567,845,052`-byte APK SHA-256 `7e8efc51b32090ae39cd73a537dae71abad18b9350b5a5a49b2daddc21154cbc`; zero missing-script matches; `git diff --check` | Child advances to `68/80` and VRP-091 exact-Samsung route capture is dependency-ready; parent progress remains `126/149 (85%)` because no parent checklist row changes | The first clean wrapper failed closed on Windows Gradle/Ninja MAX_PATH. The existing process-scoped `.g` recovery initially exposed a generated `.utmp` CMake cache that still embedded the old long cache path; after exact process verification, Gradle's own `--stop` stopped only the two wrapper-created daemons and only the ignored workspace-local `.utmp` cache was removed. The final identical clean build regenerated from `.g`, exited successfully, and produced one fresh uninstalled APK. Provenance `dirty=True` is limited to the user-preserved Android report and seven `_Recovery` scene/meta pairs while local/origin heads match exactly. No tracked implementation, protected/production asset, Jenkins/Unity path, or `productionCutover=0` value changed; the two independent map defects remain separate. |
| 2026-08-08 | Child `VRP-091` Match HUD lifecycle-route enabling repair | Rejected exact-package Samsung route evidence after seven diagnostic 120-second routes because Pause/Settings taps passed through; checked prefab repair passed; focused settings/pause `8/8`; corrected header-routing focused `14/14`; aggregate `93/93`; APH-808 passed; `git diff --check` | Child remains `68/80` pending one fresh package and complete same-package VRP-091 routes; parent remains `126/149 (85%)` because no parent checklist row changes | The Match HUD batching header has no parent GraphicRaycaster by design, while Pause/Settings lacked the scoped parent-canvas route already used by ResourceStrip. Their taps therefore reached underlying world/roster controls. The bounded repair adds scoped raycasters only to those buttons, wires shell Pause show/hide and the existing pause-menu Resume/Settings/Exit actions, serializes the prefab assignment, and makes destroyed assistant-popup cleanup Unity-null-safe when popup-layer replacement occurs. All accepted checks used repository wrappers with explicit logs/timeouts and pass markers after the authorized non-passive licensing recovery. The installed VRP-090 package is rejected for lifecycle and will not be reused. No production/protected ownership, capacity, render policy, `productionCutover=0`, parent checklist state, preserved worktree file, or independent map defect changed. |
| 2026-08-08 | Child `VRP-091` fresh lifecycle-repair package/install checkpoint | Pushed child head `8c138f362`; checked clean package passed with zero missing-script matches; exact `569,056,801`-byte APK SHA-256 `ff5b46aba9623ba6ff57b15e727cabbdc92275971192ef22f6b4943e7e19c328`; official ADB 37 verified and installed once with `--no-streaming -r` only to Samsung `SM-S918B` / `R5CTC1J02VB`; cold main-page Deploy reached active gameplay | Child remains `68/80` while same-package routes are captured; parent remains `126/149 (85%)` because no parent checklist row changes | The fresh package was created only after all lifecycle repair Editor gates passed and was installed exactly once to the authorized device. Fixed-pose start/end evidence exists, but the complete route set and performance metrics are not yet accepted. The earlier lifecycle-rejected package is not reused. Production/protected ownership, `productionCutover=0`, parent checklist state, preserved report/recovery files, and both independent map defects remain unchanged. |
| 2026-08-08 | Child `VRP-091` pause-modal exit sequencing correction | Fresh package completed fixed, slow/fast pan, zoom, tactical-follow, selected-building destruction, and minimap/full-map captures; exact lifecycle frames/log under `Build/AndroidDenseCandidateProfiler/Evidence/2026-08-08-vrp091-representative-routes-8c138f362` reject Exit to Main Menu because the loading layer remained over live Match simulation with no operation-map unload request; checked focused sequencing `8/8`; header routing `14/14`; aggregate `93/93`; APH-808 passed; `git diff --check` | Child remains `68/80` pending one replacement package and complete two-cycle lifecycle; parent remains `126/149 (85%)` because no parent checklist row changes | The Pause modal remained active when `MatchMenu` queued the route, allowing the packaged shell to start loading before closing its modal presentation sequence. The bounded repair queues Pause hide first and retains the main-menu route; focused ECS coverage proves the route remains queued through HidePopup and starts ShowLoading plus ExitMatchHud only after the matching transition completion. The installed package is lifecycle-rejected and will not be reinstalled; its seven completed routes remain retained diagnostic evidence. No parent ownership, protected/production asset, `productionCutover=0`, preserved report/recovery file, or independent map defect changed. |
| 2026-08-08 | Child `VRP-091` loading-only return-to-menu sequencing correction | Replacement head `b48bda68d`; checked clean package `569,812,925` bytes / SHA-256 `b6fdc7c7bf3bbafec71f39eff32b8f09d751a55fde60ef4f8182b7025f278c23`; installed once only to verified Samsung; rejected lifecycle evidence `Build/AndroidDenseCandidateProfiler/Evidence/2026-08-08-vrp091-pause-exit-b48bda68d`; focused `8/8`; header routing `14/14`; corrected aggregate `93/93`; APH-808 passed; `git diff --check` | Child remains `68/80` pending one new package and complete two-cycle lifecycle; parent remains `126/149 (85%)` because no parent checklist row changes | The replacement proves Pause closes before routing, but the next `ShowLoading + ExitMatchHud` presentation sequence remained opaque over live simulation and emitted no operation-map unload request. Return-to-menu now publishes only the zero-duration opaque loading command, allowing the authoritative unload owner to start; the existing post-unload `ExitLoading + EnterMenu` sequence replaces the covered HUD regions. The installed package is rejected and will not be reinstalled. No parent ownership, protected/production asset, `productionCutover=0`, preserved report/recovery file, or independent map defect changed. |
| 2026-08-08 | Child `VRP-091` immediate opaque-loading completion correction | Rejected pushed/package head `2d6437c68`; checked exact `569,812,926`-byte APK SHA-256 `96985c07073582f4ff6045f9d7560a763f45bcf5541e3186eb793c034606e2e2`; installed once only to verified Samsung; rejected evidence `Build/AndroidDenseCandidateProfiler/Evidence/2026-08-08-vrp091-loading-only-2d6437c68`; isolated loading-completion gate `1/1`; authoritative aggregate `93/93`; APH-808 passed; `git diff --check` | Child remains `68/80` pending one replacement package and complete two-cycle lifecycle; parent remains `126/149 (85%)` because no parent checklist row changes | The loading-only package still remained opaque over live Match and emitted no source-scene unload request, proving the zero-duration loading presentation itself never acknowledged completion to ECS. Single-command opaque loading now establishes the visible curtain and completes synchronously after cancelling prior motion; development-package diagnostics log command start, completion, and completion-buffer flush. Two broad shell attempts rejected on a pre-existing test-7 default-world fixture before reaching this regression and are not counted; the exact isolated gate, 93-test aggregate, and APH-808 pass. The authorized licensing ladder recovered stale/generic/version-matched failures through the same-integrity checked GUI wrapper without bypassing wrappers or markers. No parent ownership, protected/production asset, `productionCutover=0`, preserved report/recovery file, or independent map defect changed. |
| 2026-08-08 | Child `VRP-091` Match-route legacy diagnostics suppression | Rejected installed/package head `73a9eb5e6`; live device frame `Build/AndroidDenseCandidateProfiler/Evidence/2026-08-08-vrp091-immediate-loading-73a9eb5e6/05-live-white-diagnostic.png`; focused settings/diagnostics `9/9`; aggregate `93/93`; APH-808 passed; `git diff --check` | Child remains `68/80` pending one fresh package and complete two-cycle lifecycle; parent remains `126/149 (85%)` because no parent checklist row changes | Live evidence corrects the prior visual diagnosis: the shell transition completed and Match continued at about 54 FPS with `playRequested=1`, `simulationActive=1`, and `5,005` units, while the legacy `MenuDiagnosticsView` full-screen log panel produced the opaque white surface. The bounded correction suppresses only that panel on the Match route while retaining the small FPS counter and menu-route diagnostics. The installed package is rejected and will not be reinstalled. Initial compile-import and wrong aggregate-entry attempts remain rejected. No parent ownership, protected/production asset, `productionCutover=0`, preserved report/recovery file, or independent map defect changed. |
| 2026-08-08 | Child `VRP-091` fresh diagnostics-suppression package and two-cycle Android acceptance | Pushed head `24afba97b`; checked clean package `573,342,961` bytes / SHA-256 `60a3659ae433defc76787421ce38a47a73d9e956d93207292717d42f04920d0b`; zero missing-script matches; installed once with official ADB 37 only to verified Samsung; two visible Match -> Pause -> Menu cycles accepted under `Build/AndroidDenseCandidateProfiler/Evidence/2026-08-08-vrp091-match-diagnostics-24afba97b` | Child remains `68/80` pending the complete same-package diagnostics-disabled 120-second route set; parent remains `126/149 (85%)` because no parent checklist row changes | The white-screen defect is closed: both Match frames show the intended HUD at 54-56 FPS with no full-screen diagnostics panel, both cycles emit source-scene unload requests, and both return to the real main page. The combined log has zero `MenuDiagnostics`, Unity null/missing-reference exception, or failure-marker matches. The development FPS overlay intercepted one center Exit probe, so accepted cycles tap the same normal visible Exit bar away from that overlay; no internal command or rejected-package reinstall was used. Remaining representative routes and their metric gates are not claimed. No parent ownership, protected/production asset, `productionCutover=0`, preserved report/recovery file, or independent map defect changed. |
| 2026-08-08 | Child `VRP-091` complete release-equivalent representative Android route set | Pushed head `f79fa9662`; checked release build passed with zero missing-script matches; exact `543,539,773`-byte APK SHA-256 `63a51ed4dbd55e7a14b1f007b176bd434d0ec5570a17fc22b70f9dcde3f88986`; installed once with official ADB 37 only to verified Samsung; complete same-package evidence under `Build/AndroidDenseCandidate/Evidence/2026-08-08-vrp091-release-routes-f79fa9662` | Child advances to `69/80`; parent remains `126/149 (85%)` because no parent checklist row changes; VRP-092 remains open | The release-equivalent package completed fixed/steady/thermal, slow-pan, fast-pan, zoom, tactical-follow, full-screen-map, selected-building destruction, and two-cycle lifecycle routes with opt-in profiler markers/top-system diagnostics disabled. Ordinary low-FPS logging remained active, so every timing number remains diagnostic rather than VRP-092 acceptance: full-screen-map (`51.656 FPS`, `19.390 ms`), destruction (`52.520 FPS`, `19.040 ms`), and intermittent CPU spikes explicitly fail. Route scans show zero overflow/deficit/exception failure matches. Parent ownership, production/protected assets, `productionCutover=0`, preserved report/recovery files, and both independent map defects remain unchanged/separate. |
| 2026-08-08 | Child `VRP-092` GC-fallback fixed-route exact-device attempt | User explicitly permitted the AC-powered run without changing any metric or correctness gate; exact replacement package installed once with official ADB 37 only to verified Samsung; `6,627` samples / `120.013 s` after `15.001 s` warmup; rejected evidence `Build/AndroidDenseCandidate/Evidence/2026-08-08-vrp092-gc-fallback-f0d70aff2` | Child remains `69/80`; parent remains `126/149 (85%)`; package rejected and will not be reinstalled/reused | The release allocation fallback passed with source `main-thread-allocated-bytes` and zero allocated bytes; ordinary timing diagnostics and fatal matches remain zero. Average `55.219 FPS` and GPU `12.552/12.943 ms` pass, but p10 `40.021 FPS`, frame p95/p99 `25.008/41.677 ms`, and CPU `18.112/32.813 ms` fail. The dominant `>=25 ms` spike gap is `12` frames (`327` occurrences), isolating the remaining work to a periodic CPU-side owner rather than GPU, logging, startup, white-screen, allocation, or battery-drain behavior. No parent item, production/protected asset, `productionCutover=0`, preserved file, or independent map defect changes. |
| 2026-08-08 | Child `VRP-092` GC-fallback replacement release package | Pushed head `f0d70aff2`; checked clean release package passed with zero missing-script matches; exact APK `543,548,337` bytes / SHA-256 `1bb34e92cf01d01ac734b6cb7b721e0b170924132b315a9265e7940b1a6d0f73`; `git diff --check` | Child remains `69/80`; parent remains `126/149 (85%)`; fresh package has not been installed | The exact Samsung cooled to thermal status `0` / skin `34.7 C` but remains AC-powered. The single future install and fixed-route retry wait for physical power disconnection under the prior cooled/unplugged precondition. The prior package remains rejected and no metric or parent item changes. |
| 2026-08-08 | Child `VRP-092` release GC-allocation fallback after exact-device counter rejection | Checked GUI-licensing focused passed `26/26`; aggregate passed `93/93`; APH-808 passed; `git diff --check` | Child remains `69/80`; parent remains `126/149 (85%)`; no replacement package is claimed | APH-804 remains profiler-counter-only. Only VRP-092 uses allocation-free `GC.GetAllocatedBytesForCurrentThread` deltas when the clean release player lacks `GC Allocated In Frame`, names that source in JSON, and retains a mandatory sample count. The rejected fixed-route timing remains rejected. Replacement packaging waits for an unplugged/cooled exact Samsung because the prior AC-powered route ended at thermal status `1`; no gate changes. |
| 2026-08-08 | Child `VRP-092` first diagnostics-disabled fixed-route package attempt | Exact fresh package installed once with official ADB 37 only to verified Samsung; `6,506` frames / `120.013 s` after `15.010 s` warmup; rejected evidence `Build/AndroidDenseCandidate/Evidence/2026-08-08-vrp092-diagnostics-disabled-5355f5fb4` | Child remains `69/80`; parent remains `126/149 (85%)`; the package is rejected and will not be reinstalled/reused | Ordinary periodic performance logging was absent, but the release player exposed neither the GC-allocation profiler counter nor the already-known batch counter. Raw fixed-route timing independently fails p10/p95/p99/CPU gates despite passing average and GPU: `54.211 FPS`, `18.447 ms` average, `40.007` p10 FPS, `33.335/49.995 ms` p95/p99, CPU `18.447/34.480 ms`, GPU `13.955/14.633 ms`. Post-route thermal status was `1`, skin `41.1 C`, and AC power was connected; a cooled/unplugged replacement run is required without weakening any gate. |
| 2026-08-08 | Child `VRP-092` diagnostics-disabled 120-second release-recorder contract and fresh checked package | Final checked GUI-licensing recorder validation passed `25/25`; final Editor-first aggregate passed `93/93`; final APH-808 passed; pushed implementation head `5355f5fb4`; checked clean release package passed with zero missing-script matches; exact APK `543,548,429` bytes / SHA-256 `5175c2f3aa5a80e76c4c896aa42b117fb6d6cde4ea6b89da2aa45bce6a2a251e`; rejected earlier focused/aggregate and licensing-failed APH-808 attempts remain non-acceptance evidence; `git diff --check` | Child remains `69/80` and VRP-092 remains open pending one exact-device install and matched routes; parent remains `126/149 (85%)` because no parent checklist row changes | The bounded release recorder adds exact route-labelled 15-second-warmup plus 120-second captures for every Phase-9 gameplay/performance route and suppresses ordinary frame-rate/freeze/slow-frame logging only while VRP-092 owns measurement. It preserves clean release CPU/GPU, GC, memory, battery, and raw-frame evidence without periodic diagnostic query/string/log contamination. APH-804 remains unchanged at 60+600 seconds with its full counter contract; only VRP-092 accepts Android's unavailable batch counter. The package records exact pushed source `5355f5fb4`; its dirty flag is explained solely by the explicitly preserved generated report and recovery scenes, which remain unstaged and were not packaged as implementation. The authorized recovery ladder precisely removed one orphaned failed wrapper tree, restarted only the verified stale licensing client after confirming no Editor, and then passed the same checked wrappers without bypassing their markers. No metric pass, parent row, protected/production asset, render policy, capacity, `productionCutover=0`, preserved file, or independent map defect changed. |

| 2026-08-08 | Child `VRP-092` optimized Android frame-pacing correction | Rejected exact-device fixed route shows a 12-frame periodic CPU cadence; child focused Android visual-quality gate passed `18/18`; aggregate passed `93/93`; APH-808 passed; `git diff --check` | Child remains `69/80`; parent remains `126/149 (85%)`; fresh package and Android acceptance remain open | The exact Samsung runs its active display at 120 Hz while the release player targets 60 FPS. Because gameplay Update and GPU timing are below the failing CPU cadence, the bounded correction enables Unity optimized Android frame pacing (`androidUseSwappy=1`) and adds a fail-closed Editor regression. It does not relax the authorized 54-FPS/two-minute amendment or any p10, p95/p99, CPU/GPU, GC, memory, thermal, correctness, capacity, protected/production, or parent gate. The initial aggregate licensing-mutex attempt remains rejected; the same checked GUI-licensing wrapper passed. AC-powered testing remains user-authorized, preserved report/recovery files remain unstaged, and both independent map defects remain separate. |

| 2026-08-08 | Child `VRP-092` frame-pacing package rejection and periodic-barrier correction | Exact fresh `543,628,944`-byte APK SHA-256 `7df43ff0dcafc6e246d49349942a27be97d23b493afb4481be6557ab022a7f02`; one exact-Samsung install; completed `6,701`-sample fixed route; corrected recorder `28/28`, minimap `4/4`, aggregate `93/93`, APH-808 passed; `git diff --check` | Child remains `69/80`; parent remains `126/149 (85%)`; rejected APK will not be reused and a new package/device result remains open | Pacing passes average/p10/GPU but rejects p95/p99 at about `33.33 ms`. Evidence correlates `118/120` synchronous one-second PSS reads with the immediately following slow frame and the remaining 11-12-frame cadence with the minimap producer's exact 0.2-second synchronous dependency barrier. VRP-092 now keeps PSS outside timed frames, retains VRP-094 as the unchanged peak-memory owner, publishes deterministic minimap markers through jobs without a main-thread completion, and measures Unity's documented active `cpuMainThreadFrameTime` for the CPU-main gate instead of total cadence including presentation wait. No threshold, parent row, protected/production asset, capacity, `productionCutover=0`, preserved file, or independent map defect changed. |

| 2026-08-08 | Child `VRP-092` residual 5 Hz threat-scan candidate prefilter | Rejected exact `543,618,152`-byte package SHA-256 `3beb35a01c3de3eec2ea67ea34fe184367388e2be5fbccfe69932f4e78ac136a`; one exact-Samsung install; fixed-route evidence `Build/AndroidDenseCandidate/Evidence/2026-08-08-vrp092-periodic-barrier-4595370a8`; focused threat-warning pass; aggregate `93/93`; APH-808 passed; `git diff --check` | Child remains `69/80`; parent remains `126/149 (85%)`; prior package is rejected and a fresh package/device result remains open | The corrected route passes average/p10/frame-p95/GPU/allocation/battery/correctness but rejects p99 and CPU-main: `57.278 FPS`, `16.804 ms` p95, `33.340 ms` p99, and CPU-main `13.158/30.213 ms`. Every remaining >=25 ms frame is >=33 ms at the exact `0.2 s` threat-warning cadence. The Burst scan now prefilters hostile live aircraft/vehicles once instead of re-traversing all roughly 5,000 runtime units per detector/close-contact sensor; all warning semantics remain unchanged and a source regression protects the bounded algorithm. The first GUI attempt hit the known stale licensing mutex; after no-Editor verification only that exact stale client was restarted, and the same checked wrappers passed. A malformed shorthand aggregate invocation remains rejected; the correct method passed. No threshold, parent row, protected/production asset, capacity, `productionCutover=0`, preserved file, or independent map defect changed. |

| 2026-08-09 | Child `VRP-092` threat-prefilter near-pass and close-contact sensor cache | Pushed/package head `3328bc9c0`; checked clean `543,650,072`-byte APK SHA-256 `fa2b1e0f78b15fad1ad24941cc7ab73c4a4e27ef60b05c62da7f22d43a299dc6`; one exact-Samsung install; rejected evidence `Build/AndroidDenseCandidate/Evidence/2026-08-09-vrp092-threat-prefilter-3328bc9c0`; focused threat pass; aggregate `93/93`; APH-808 passed | Child remains `69/80`; parent remains `126/149 (85%)`; prior package is rejected and a fresh package/device result remains open | The prefilter eliminates the 5 Hz spikes and passes every fixed-route gate except CPU-main average: `59.961 FPS`, p10 `59.978 FPS`, frame p95/p99 `16.676/16.752 ms`, GPU `14.324/14.538 ms`, zero allocation, and CPU-main `12.010514 ms` against the unchanged `12.000 ms` limit. The bounded follow-up collects unchanged live player close-contact sensors during the existing target pass and reuses that compact list, removing the redundant second roughly 5,000-entity traversal without changing warning semantics. Host-access checked GUI licensing passed focused, aggregate, and corrected exact-marker APH-808 validation. No metric is rounded, no threshold or parent row changes, the rejected APK will not be reused, preserved files remain unstaged, and both independent map defects remain separate. |

| 2026-08-09 | Child `VRP-092` close-contact cache package rejection and candidate-first exact scan | Pushed/package head `756d8df75`; checked short-path/version-matched build passed; exact `543,985,027`-byte APK SHA-256 `c1d73343a4563926815502a6929d8fe6e642bca45a895c73600f69dd9e867974`; one exact-Samsung install; rejected evidence `Build/AndroidDenseCandidate/Evidence/2026-08-09-vrp092-close-sensor-cache-756d8df75`; focused threat pass; aggregate `93/93`; recovered APH-808 passed | Child remains `69/80`; parent remains `126/149 (85%)`; rejected APK will not be reused and a stronger Editor-accepted replacement remains open | The fixed route passes every unchanged gate except CPU-main average: `59.961 FPS`, p10 `59.974 FPS`, frame p95/p99 `16.678/16.770 ms`, GPU `14.335/14.541 ms`, zero allocation, CPU p95 `13.705 ms`, and CPU average `12.021921 ms` versus the exact `12.000 ms` limit. The stronger candidate-first scan deduplicates friendly sensor cells, early-outs already-known close threats, and preserves the minimum new-threat ETA before a single registration. The Windows full-path build rejected on the 260-character native-header limit; a proven checked `W:\.` version-matched route passed and removed its temporary mapping afterward. One cold-null APH script association recovered by timestamp-only exact-script reimport with zero content change. No parent row, metric, threshold, protected/production asset, `productionCutover=0`, preserved file, or independent map defect changed. |

| 2026-08-09 | Child `VRP-092` candidate-first package rejection and measurement-overhead correction | Pushed/package head `4db303dfb`; checked exact `543,996,087`-byte APK SHA-256 `34612b24b899b3183ac002f2ad3295aeec3d347fa1e7d1f020d4619211aae3fc`; one exact-Samsung install; rejected evidence `Build/AndroidDenseCandidate/Evidence/2026-08-09-vrp092-candidate-first-close-4db303dfb`; recorder focused `30/30`; aggregate `93/93`; recovered APH-808 passed | Child remains `69/80`; parent remains `126/149 (85%)`; rejected APK will not be reused and the bounded recorder correction is Editor-accepted | The route passes all gates except unchanged CPU average: `59.961 FPS`, p10 `59.978 FPS`, frame p95/p99 `16.676/16.744 ms`, CPU `12.031435/13.655 ms`, GPU `14.349/14.543 ms`, zero allocation, zero correctness/log failures, and thermal status `0`. This stable floor persists after the stronger threat optimization. VRP-092 retains per-frame CPU/GPU and allocation measurement but removes four known-empty render-counter reads and moves VRP-094-owned managed-memory probes to boundaries plus 1 Hz; APH-804 remains unchanged. No parent row, threshold, protected/production asset, `productionCutover=0`, preserved file, or independent map defect changed. |

| 2026-08-09 | Child `VRP-092` low-overhead-recorder package rejection and baked-helicopter dependency correction | Pushed/package head `cbf0297c9`; checked exact `543,998,003`-byte APK SHA-256 `67a207e09b0e6df333816a17a0146a81dc0300a96897a1c08261e974c5ec68ad`; one exact-Samsung install; rejected evidence `Build/AndroidDenseCandidate/Evidence/2026-08-09-vrp092-low-overhead-cbf0297c9`; checked focused `Build/Logs/vrp092-async-baked-helicopter-blades-focused-shortpath-host-versioned-rerun.log` passed `9/9`; corrected aggregate `Build/Logs/vrp092-async-baked-helicopter-blades-aggregate-shortpath-host-versioned-rerun.log` passed `93/93`; checked GUI-licensing APH-808 `Build/Logs/vrp092-async-baked-helicopter-blades-aph808-shortpath-host-gui-final.log` passed | Child remains `69/80`; parent remains `126/149 (85%)`; rejected APK will not be reused and the bounded correction is Editor-accepted | The fixed route remains visibly correct and passes FPS, p10, frame p95/p99, CPU p95, GPU, allocation, capacity, ordinary-log, and thermal gates: `7,197` samples, `59.969 FPS`, p10 `59.979 FPS`, frame `16.676/16.761 ms`, CPU p95 `13.658 ms`, GPU `14.373/14.625 ms`, zero allocation, and thermal status `0`. CPU average `12.043045 ms` still fails the exact `12.000 ms` limit, so the package was stopped. The accepted correction keeps canonical baked helicopter blade rotation asynchronous in the ECS dependency chain and excludes baked/empty-buffer aircraft from legacy descendant scanning; only genuine no-buffer legacy helicopters retain the synchronous fallback safety boundary. The first aggregate shorthand was rejected before tests, and two validator-passing APH-808 batchmode runs remained fail-closed on missing shutdown status before the same unchanged gate passed through checked GUI licensing. No parent checklist row, threshold, protected/production asset, `productionCutover=0`, preserved file, or independent map defect changed. |

| 2026-08-09 | Child `VRP-092` async-helicopter package route acceptance, clean zoom rejection, and mobile overview cap | Pushed/package head `927f2b544`; checked exact `543,998,979`-byte APK SHA-256 `378a6b335af8ac2e85c550471ae2b523e315da34fb6e0eb382825a014fbb0bbf`; installed once with official ADB 37 only to verified Samsung; evidence `Build/AndroidDenseCandidate/Evidence/2026-08-09-vrp092-async-helicopter-927f2b544`; fixed/slow-pan/fast-pan accepted; clean zoom rejected; focused camera validation `34/34`; corrected aggregate `93/93`; recovered APH-808 passed; `git diff --check` | Child remains `69/80`; parent remains `126/149 (85%)`; prior package will not be reinstalled/reused and the bounded mobile correction is Editor-accepted | The exact package passes fixed, representative slow-pan, and fast-pan routes at about `59.96 FPS` with passing frame/CPU/GPU/allocation/correctness gates. The first zoom result is invalid because it began at thermal status `1`; the clean status-`0` rerun completes `6,687` samples / `120.012 s` and passes average `55.719 FPS`, p10 `59.940 FPS`, CPU `11.875/14.642 ms`, GPU average `14.087 ms`, zero allocation, and thermal status, but rejects frame p95/p99 `33.333/33.356 ms` and GPU p95 `20.261 ms`. Only the final zoomed-out state degrades, averaging `21.129 ms` with `380` frames at or above `25 ms`. The existing Android/mobile camera-normalization owner now caps maximum overview height at `40`, the already configured fullscreen overview height, rather than the failing `45`-height footprint; desktop behavior and all metric/visual gates remain unchanged. The single APH cold-null audio association recovered by timestamp-only exact-script refresh with no content change. A fresh package and remaining child routes are still required; no parent checklist item, protected/production asset, `productionCutover=0`, preserved file, or independent map defect changed. |

| 2026-08-09 | Child `VRP-092` mobile-overview route continuation and fullscreen single-owner correction | Pushed/package predecessor `c48ecb3c7`; exact checked `543,998,951`-byte APK SHA-256 `40085348fb49dd60956156b1e40a0b431a3d7120b6b06a10730a6952c1d320cc`; one official-ADB-37 install only to verified Samsung; evidence `Build/AndroidDenseCandidate/Evidence/2026-08-09-vrp092-mobile-zoom-cap-c48ecb3c`; pushed replacement implementation `327af8eb7`; focused `19/19`; corrected aggregate `93/93`; recovered APH-808 passed | Child remains `69/80`; parent remains `126/149 (85%)`; zoom/tactical-follow/destruction are accepted, fullscreen is rejected, and a fresh replacement package/device result remains open | Zoom, tactical-follow, and the canonical selected-building Destroy interaction pass every unchanged FPS/frame/CPU/GPU/allocation/correctness gate. Fullscreen passes FPS/frame/GPU/allocation but rejects CPU average twice (`12.331 ms` hot and `12.193 ms` after a clean status-`0` start, limit `12.000 ms`) and ends at thermal status `1`. The bounded correction stops refreshing the hidden compact minimap while the visible fullscreen map owns presentation and naturally refreshes compact immediately after close. It changes no visible/fullscreen input, marker, cadence, threshold, parent row, protected/production asset, capacity, render policy, or `productionCutover=0` contract. The authorized GUI-licensing ladder recovered one failed wrapper tree and only the verified stale licensing client; one wrong aggregate shorthand was rejected before tests; APH-808 recovered its exact `46` cold-null scoped scripts through timestamp-only refresh with zero content diff. Preserved reports/recovery scenes remain unstaged and both independent map defects remain separate. |

| 2026-08-09 | Child `VRP-092` complete representative-route acceptance | Pushed source/documentation head `05a969892`; checked exact `543,998,651`-byte APK SHA-256 `e4e239486de3ecb8900e07ea1fdc117292d9ef7ca5a6570243360ca99fdcfb1e`; one official-ADB-37 install only to verified Samsung; current evidence `Build/AndroidDenseCandidate/Evidence/2026-08-09-vrp092-fullscreen-single-owner-05a969892`; retained accepted predecessor evidence for the unaffected routes | Child advances to `70/80`; parent remains `126/149 (85%)`; child VRP-093 is next | Current-package fullscreen passes at `59.852 FPS`, CPU `11.561/13.308 ms`, GPU `14.297/14.517 ms`, zero allocation, zero failures, and thermal status `0 -> 0`; steady passes at `59.961 FPS`, CPU `11.277/12.868 ms`, GPU `13.873/14.032 ms`, zero allocation, zero failures, and thermal status `0 -> 0`. Visible start/end captures retain complete fullscreen-map and dense gameplay presentation without white screen, holes, leakage, popping, or UI loss. The popup-scoped correction cannot affect closed-popup fixed/pan/zoom/tactical/destruction routes, whose accepted results are retained to complete the representative set. No parent checklist row, threshold, duration, protected/production asset, capacity, `productionCutover=0`, preserved file, or independent map defect changed. |

| 2026-08-09 | Child `VRP-093` zero-allocation/capacity/rebuild/no-sync acceptance | Accepted exact-device route evidence; checked initialization `9/9`, apply `8/8`, state-sync `8/8`, aggregate `93/93`, and APH-808; pushed pure-gate test seam `b7fc940e1`; source completion audit; `git diff --check` | Child advances to `71/80`; parent remains `126/149 (85%)`; child VRP-094 is next | Accepted routes report `0 B/frame` and zero overflow/deficit/fatal matches. Focused gates prove bounded first/map-generation/camera/visual-state rebuilds, stable-envelope and stable-command no-ops, asynchronous dependency-ordered apply, deterministic state recycling, and fail-before-mutation validation. The four production `.Complete()` calls are confined to guarded initialization replacement or removal/teardown disposal; no recurring virtualization job is synchronously completed. The only implementation change exposes the unchanged pure state-change validator to its execute-method test because ECS logs system exceptions instead of rethrowing them. Rejected direct-runtime/`LogAssert` assertions and the unrelated broad 13-snapshot architecture ratchet are retained honestly and were not used to weaken this bounded gate. No parent checklist row, production behavior, threshold, protected/production asset, capacity, `productionCutover=0`, preserved file, or independent map defect changed. |

| 2026-08-09 | Child `VRP-094` exact Phase 0/current-candidate memory comparison | Frozen Phase 0 report; current machine-readable comparison report; accepted installed package; exact Samsung corrected 120-second evidence; readiness/retained `dumpsys meminfo`; fresh recorder/logcat/screenshot | Child advances to `72/80`; parent remains `126/149 (85%)`; child VRP-095 is next | Current retained PSS/RSS/graphics are `2,140,028/2,232,260/901,816 KiB`, respectively `14.132%/15.025%/17.757%` below the exact frozen Phase 0 artifacts; native heap is `65,584 KiB`, `3.347%` higher. Peak recorder allocated/Mono/RSS are `904.107/26.523/2,090.092 MiB`. The route completed at `16.670 ms` average with `0 B/frame`, and its retained screen preserves the dense city and UI without a white screen or gross hole. The malformed first activity launch is rejected; only the corrected rerun is accepted. Because baseline and current devices differ, the exact comparison is not presented as identical-device causality. No reinstall, unplugged test, parent row, production/protected asset, `productionCutover=0`, preserved file, or independent map defect changed. |

| 2026-08-09 | Child `VRP-095` exact Android state-scenario diagnostics checkpoint | Checked GUI focused `32/32`, aggregate `93/93`, APH-808 passed, and `git diff --check` | Child remains `72/80`; parent remains `126/149 (85%)`; a fresh pushed package/exact-device result is required | The bounded opt-in diagnostic drives the real camera and canonical destruction system across three distant compatible virtualized buildings. It requires visible intact-to-destroyed replacement, exact intact-slot reuse by a second owner, off-camera canonical destruction with zero proxy materialization and no disturbance to the visible owner, destroyed-only materialization on arrival, deterministic destroyed-slot return, and zero overflow/deficit. Ordinary runtime behavior and all acceptance thresholds are unchanged. No parent row, protected/production asset, capacity, render policy, `productionCutover=0`, preserved file, or independent map defect changed. |

| 2026-08-09 | Child `VRP-095` fresh checked Android package | Pushed package head `28d2595b2`; rejected stale-Bee full-path build; accepted checked short-path GUI build; exact `543,993,843`-byte APK SHA-256 `246d19ee83d5619f8bc270818128d4c771bca395ada5bf873a55a2f1d886f2a7` | Child remains `72/80`; parent remains `126/149 (85%)`; exact-Samsung installation/device evidence remain open | The verified project-only `W:` mapping resolved the retained Bee graph and was removed immediately after the package gate passed. The APK remains uninstalled because the previously authenticated Samsung currently advertises no reachable wireless-debugging service; bounded official-ADB-37 discovery cleaned up only each newly task-started verified server. No other device, reinstall, parent row, production/protected asset, `productionCutover=0`, preserved file, or independent map defect changed. |

| 2026-08-09 | Child `VRP-095` rejected first Android scenario and recycled-owner correction | Exact checked APK installed once with official ADB 37 and `--no-streaming -r` only to verified Samsung `SM-S918B` / `R5CTC1J02VB`; rejected evidence `Build/AndroidDenseCandidate/Evidence/2026-08-09-vrp095-state-scenario-28d2595b2-rerun`; corrected focused `32/32`, aggregate `93/93`, and recovered APH-808 pass | Child remains `72/80`; parent remains `126/149 (85%)`; the installed package is rejected and a fresh checked replacement/exact-device result remain required | Device evidence passes visible intact-to-destroyed replacement, then rejects the diagnostic's assumption that a preselected travel anchor must receive the released physical IDs. The corrected opt-in diagnostic instead deterministically selects the active intact canonical building that actually owns the largest exact overlap, retaining the exact-reuse, no-stale-state, off-camera destruction, deterministic return, zero-overflow/deficit, and bounded-wait requirements. Sandboxed GUI/version-matched licensing attempts and the first stale-import APH-808 run remain rejected; checked host-access GUI validation passed after the authorized recovery, and timestamp-only refresh of the single correctly serialized `AudioPlaybackPresentationRuntimeView.cs` association changed no source/meta bytes. Official Unity licensing troubleshooting and 6000.5.0f1 release notes were reviewed and recorded in the child tracker. No parent checklist row, ordinary production behavior, gate, protected/production asset, `productionCutover=0`, preserved file, or independent map defect changed. |

| 2026-08-09 | Child `VRP-095` replacement-package rejection and stable recycled-owner follow-up | Checked `0aa70a8d7` package, exact `544,008,871` bytes / SHA-256 `733abd36adcca889322de1d05ad4f206caec7def0eabb106af5f758523987acc`, installed once only to verified Samsung; rejected evidence `Build/AndroidDenseCandidate/Evidence/2026-08-09-vrp095-recycled-owner-0aa70a8d7-rerun2` | Child remains `72/80`; parent remains `126/149 (85%)`; rejected APK will not be reinstalled and the stable-owner follow-up remains Editor/device-open | Exact physical reuse passes when owner `3383` receives two released intact slots, but that owner is a one-frame transient and disappears before the off-camera destruction check. The bounded follow-up waits for the same actual recycled owner and exact slot set to remain intact for `60` frames before advancing; a disappearing transient returns to deterministic owner resolution. The two earlier malformed controller launches never started the app and remain rejected as controller evidence. No production behavior, threshold, parent row, protected/production asset, `productionCutover=0`, preserved file, or independent map defect changed. |

| 2026-08-09 | Child `VRP-095` stable recycled-owner Editor acceptance | Rejected compile/timeout log; accepted focused `32/32`, aggregate `93/93`, and APH-808 logs recorded in child tracker | Child remains `72/80`; parent remains `126/149 (85%)`; a fresh checked replacement package and exact-device pass remain required | The checked wrapper rejected one local-name collision and cleaned only its own Unity tree at the explicit timeout. The compile-only rename then passed every unchanged Editor gate. The opt-in diagnostic requires `60` stable frames on the actual exact-slot recipient before advancing, and re-resolves transient recipients without weakening physical-reuse or canonical-state assertions. No production behavior, threshold, parent row, protected/production asset, `productionCutover=0`, preserved file, or independent map defect changed. |

| 2026-08-09 | Child `VRP-095` stable-owner package rejection and bounded recipient wait | Checked `29ad0dcaf` package, exact `544,009,963` bytes / SHA-256 `a4bd9d53abf6e1352df147bac785267b174a261acec862323b5506cacf46510e`, installed once only to verified Samsung; rejected evidence recorded in child tracker | Child remains `72/80`; parent remains `126/149 (85%)`; rejected APK will not be reinstalled and the bounded timing correction remains open | Exact visible replacement passes, but the diagnostic rejects during a short no-recipient interval before slot reassignment. The follow-up uses the existing `240`-frame fail-closed wait for a positive exact-overlap recipient, followed by the unchanged `60`-frame stability requirement. No exact-reuse, canonical-state, capacity, performance, or correctness gate is weakened. No production behavior, parent row, protected/production asset, `productionCutover=0`, preserved file, or independent map defect changed. |

| 2026-08-09 | Child `VRP-095` bounded recipient-wait Editor acceptance | Focused `32/32`, aggregate `93/93`, recovered APH-808 pass, and `git diff --check`; exact logs recorded in child tracker | Child remains `72/80`; parent remains `126/149 (85%)`; fresh checked package/device pass remain required | The bounded timing correction is Editor-accepted without weakening positive exact-overlap or the subsequent `60`-frame stability requirement. One known cold-null script association recovered through timestamp-only exact-script refresh with unchanged source/meta hashes. No production behavior, threshold, parent row, protected/production asset, `productionCutover=0`, preserved file, or independent map defect changed. |

| 2026-08-09 | Child `VRP-095` bounded-wait package rejection and anchor-independent resolution | Checked `b004bb717` package, exact `544,014,699` bytes / SHA-256 `ae0831cb8d86a285da9eeba26bdc08c1de553034d6c8686b598532b193f9cfe1`, installed once only to verified Samsung; rejected evidence recorded in child tracker | Child remains `72/80`; parent remains `126/149 (85%)`; rejected APK will not be reinstalled and anchor-independent resolution remains open | The camera travel anchor never materializes, but it is not the owner under test. The follow-up deterministically waits for any active intact owner that actually receives positive exact overlap, retaining both the `240`-frame fail-closed bound and `60`-frame stable exact-slot requirement. No production behavior, threshold, parent row, protected/production asset, `productionCutover=0`, preserved file, or independent map defect changed. |

| 2026-08-09 | Child `VRP-095` anchor-independent actual-owner Editor acceptance | Focused `32/32`, aggregate `93/93`, recovered APH-808, and `git diff --check`; exact logs in child tracker | Child remains `72/80`; parent remains `126/149 (85%)`; fresh package/device pass remain required | Anchor materialization is no longer a prerequisite for selecting the active intact owner with positive exact released-slot overlap; the bounded wait and `60`-frame exact-slot stability gate remain unchanged. One cold-null association recovered with no source/meta content change. No production behavior, threshold, parent row, protected/production asset, `productionCutover=0`, preserved file, or independent map defect changed. |

| 2026-08-09 | Child `VRP-095` anchor-independent package rejection and destroyed-slot semantics correction | Checked `7d2c3f5fd` package, exact `544,012,763` bytes / SHA-256 `e9d11bc46d7040a48d4c78f4a65d38016f206f61db06713a4be52a1647da667b`, installed once only to verified Samsung; rejected evidence in child tracker | Child remains `72/80`; parent remains `126/149 (85%)`; rejected APK will not be reinstalled and the bounded diagnostic correction remains open | Every state/recycling gate passes through off-camera destroyed-only arrival except an invented requirement that two different destroyed owners share one physical ID. The correction aligns with accepted fixed-pool semantics: exact intact cross-owner reuse and exact same-owner destroyed return remain required, while unrelated cross-owner destroyed IDs may differ. No production behavior, threshold, parent row, protected/production asset, `productionCutover=0`, preserved file, or independent map defect changed. |

| 2026-08-09 | Child `VRP-095` destroyed-slot semantics Editor acceptance and GUI-licensing race recovery | Focused `32/32`, aggregate `93/93`, recovered APH-808, unchanged timestamp-refreshed source/meta hashes, and `git diff --check`; exact logs and process evidence in child tracker | Child remains `72/80`; parent remains `126/149 (85%)`; fresh package/device pass remain required | The correction removes only unrelated destroyed-owner physical-ID equality; destroyed-only arrival and exact same-owner return remain required. Hub `3.20.0` generic licensing client `1.17.4` repeatedly reclaimed the pipe before Editor `6000.5.2f1` could start its required `1.18.1` client. The accepted bounded recovery verified no Editor, stopped only the exact generic client, and atomically launched the checked GUI wrapper; no Hub, unrelated process, Unity/Jenkins path, production behavior, threshold, parent row, protected/production asset, `productionCutover=0`, preserved file, or independent map defect changed. |

| 2026-08-09 | Child `VRP-095` `d749e8675` package rejection and intact cross-owner slot-semantics correction | Checked `544,013,547`-byte package SHA-256 `08e07807a1b34ee5b930f94a0b27b670f35cbc20b346a3205e26919415e6637a`, installed once only to verified Samsung; rejected evidence recorded in child tracker | Child remains `72/80`; parent remains `126/149 (85%)`; rejected APK will not be reinstalled and corrected diagnostic remains Editor/package/device-open | The exact device passes visible intact-to-destroyed replacement but rejects an invented requirement that an unrelated intact owner must share the first owner's numeric physical slot IDs. The correction accepts positive compatible intact materialization at the travel anchor, retains exact-overlap owner resolution only as an absent-anchor fallback, and still requires `60` stable frames for the selected same owner and exact slot set. Every destroyed-state, off-camera, deterministic-return, bounded-wait, overflow, and deficit assertion remains unchanged. No production behavior, threshold, parent row, protected/production asset, `productionCutover=0`, preserved file, or independent map defect changed. |

| 2026-08-09 | Child `VRP-095` intact cross-owner slot-semantics Editor acceptance | Focused `32/32`, aggregate `93/93`, recovered APH-808, unchanged timestamp-refreshed source/meta hashes, and `git diff --check`; exact logs in child tracker | Child remains `72/80`; parent remains `126/149 (85%)`; fresh checked package/device pass remain required | The corrected diagnostic now accepts positive intact travel-owner materialization without cross-owner numeric slot equality, while retaining exact same-owner `60`-frame slot stability and all destruction, off-camera, deterministic-return, bounded-wait, overflow, and deficit gates. No production behavior, threshold, parent row, protected/production asset, `productionCutover=0`, preserved file, or independent map defect changed. |

| 2026-08-09 | Child `VRP-095` `4498791a3` package rejection and same-owner logical-return correction | Checked `544,012,567`-byte package SHA-256 `54e2ca1a00aa5a7c263244ad27c99f58a5ed43f83fbbfe949b348cd2a2bd354f`, installed once only to verified Samsung; rejected evidence recorded in child tracker | Child remains `72/80`; parent remains `126/149 (85%)`; rejected APK will not be reinstalled and logical-return correction remains open | The device passes every state/recycling phase until the first destroyed owner returns on a different free physical slot ID. The correction keeps the actual visual contract: the same canonical owner must return positively with destroyed-only parts and its complete original destroyed-recipe count. It removes only equality of disposable physical slot numbers after intervening allocations; bounded waits and zero overflow/deficit remain unchanged. No production behavior, threshold, parent row, protected/production asset, `productionCutover=0`, preserved file, or independent map defect changed. |

| 2026-08-09 | Child `VRP-095` same-owner logical-return Editor acceptance | Focused `32/32`, aggregate `93/93`, accepted GUI APH-808, and `git diff --check`; rejected GUI licensing and shutdown-detached attempts recorded in child tracker | Child remains `72/80`; parent remains `126/149 (85%)`; fresh checked package/device pass remain required | The corrected final-return gate requires the same canonical owner, positive destroyed-only materialization, and complete original destroyed-recipe part count while allowing legitimate physical slot reassignment. The recovery ladder moved from a GUI pipe-race rejection to the checked version-matched focused/aggregate route; after two APH pass-marker runs detached during shutdown, the checked GUI route supplied an acceptable wrapper exit. No production behavior, threshold, parent row, protected/production asset, `productionCutover=0`, preserved file, or independent map defect changed. |

| 2026-08-09 | Child `VRP-095` exact Samsung destruction/recycling acceptance | Pushed/package head `f6accf53e`; exact checked `544,011,811`-byte APK SHA-256 `65a4a57c8014ff99a06151a6adb2b4046a7304bfb1f1b817ad49c764128b452d`; one official-ADB-37 install only to verified Samsung; full phase screenshots/logcat in child evidence | Child advances to `73/80`; parent remains `126/149 (85%)`; child VRP-096 is next | The state route passes visible intact-to-destroyed replacement, `60` stable frames on a compatible intact owner, off-camera canonical destruction with zero materialization/no disturbance, destroyed-only materialization on arrival, complete destroyed-only return, and terminal `overflow=0` / `deficit=0`. The retained live Match frames preserve city geometry and UI without a white screen, gross holes, or stale proxy state; residual dark shadow patches are unchanged and not reclassified by this gate. No reinstall, performance rerun, threshold, parent checklist row, protected/production asset, `productionCutover=0`, preserved file, or independent map defect changed. |
| 2026-08-09 | Child `VRP-096` exact Samsung two-cycle lifecycle | Accepted installed/package head `f6accf53e`; official ADB 37 exact-device route; evidence `Build/AndroidDenseCandidate/Evidence/2026-08-09-vrp096-lifecycle-f6accf53e` | Child advances to `74/80`; parent remains `126/149 (85%)`; child VRP-097 is next | Menu -> Match -> Menu -> Match -> Menu completes visibly on Samsung `SM-S918B` / `R5CTC1J02VB`. Each entry independently resolves the packaged dense-city definition, streams both support and candidate EntityScenes, and reaches `playRequested=1` / `simulationActive=1`; each exit removes the Match HUD and restores the complete menu. The two post-unload menu frames are byte-identical, and the accepted log contains exactly two ready markers and two streams of each EntityScene with no Unity fatal, failed-result, positive-overflow, or positive-deficit match. No package reinstall, unplugged requirement, parent checklist row, protected/production asset, `productionCutover=0`, preserved file, or independent map defect changed. |
| 2026-08-09 | Child `VRP-097` final cooled Samsung thermal acceptance | Accepted installed/package head `f6accf53e`; official ADB 37 exact-device route; evidence `Build/AndroidDenseCandidate/Evidence/2026-08-09-vrp097-final-thermal-f6accf53e-cool-rerun2`; structured recorder, start/end frames, battery/thermal snapshots, and logcat | Child advances to `75/80` and completes Phase 9; parent remains `126/149 (85%)`; child VRP-100 is next | The accepted `120.007 s` cooled route sustains `59.980` average FPS / `59.997` p10 FPS, `16.672/16.670/16.740 ms` average/p95/p99 frame time, CPU `11.568/13.218 ms` average/p95, GPU `13.849/14.001 ms` average/p95, `0 B/frame` allocation, and `0%` battery drain. Thermal status is `0` before and after, retained frames show complete live Match presentation without a white screen or gross visual loss, and logcat contains no failed result, positive overflow/deficit, fatal exception, SIGSEGV, or OOM match. The earlier warm run remains rejected at CPU average `12.025 ms` versus the unchanged `12.000 ms` limit, and two pre-measurement launcher failures remain rejected. No reinstall, unplugged requirement, parent checklist row, gate weakening, protected/production asset, `productionCutover=0`, preserved file, or independent map defect changed. |
| 2026-08-09 | Child `VRP-100` architecture/workflow/output-ownership closeout | Five shared architecture/workflow/ownership/rollback documents updated with exact accepted types, hashes, counts, transaction owners, invalidation rules, and rollback ledger; `git diff --check` | Child advances to `76/80`; parent remains `126/149 (85%)`; child VRP-101 is next | Shared documentation now distinguishes candidate-only `EntityScene + VirtualizedProxyPool` acceptance from the unchanged `StaticSceneChunks + ResidentEntities` production path. It names the generated config/authoring/database owners, runtime blob/components/systems, exact source/logical/resident/packed/capacity identities, and required complete regeneration on any mismatch. Candidate failure rollback, protected-production revalidation, contiguous production-cutover revert range/path ledger, and virtualized production rollback verification are explicit. No parent checklist row, implementation, generated evidence byte, protected/production asset, Addressables setting, package, threshold, `productionCutover=0`, preserved file, or independent map defect changed. |
| 2026-08-09 | Child `VRP-101` accepted parent parity/residency semantics | Parent renderer ownership, parity, residency, building/attachment, render-only, performance, and Phase 6/9 summary clauses updated; `git diff --check` | Child advances to `77/80`; parent remains `126/149 (85%)`; child VRP-102 remains gated by open parent production prerequisites | The parent now treats the accepted immutable logical database, named resident exceptions, and fixed pre-baked slot set as the map-lifetime virtualized representation; only slot assignment/enable state changes with the camera. Direct resident parity and logical-plus-materialized virtualized parity are separate exact contracts. The earlier future spatial-section loader assumption is closed and camera travel must continue starting zero map scene/Addressables/static-streamer operations. Current database/packed/device values replace stale Phase 6/9 summaries without closing unrelated parent checklist rows or authorizing production. No implementation, generated byte, protected/production asset, Addressables setting, package, threshold, `productionCutover=0`, preserved file, or independent map defect changed. |
| 2026-08-09 | Phase 0A authored-building ECS production/interaction closure | Retained checked package/source head `8a2643b4e`; exact `777,444,713`-byte APK SHA-256 `376018de87667295184fe968de027751c5aeac3b3227dae0bba22b2916cf95ec`; accepted evidence `Build/AndroidDenseCandidateProfiler/Evidence/2026-08-06-placement-parent-canvas-import-recovery`; checked APH-808, focused placement, and aggregate `93/93` logs recorded in the child tracker | Parent advances to `127/149 (85%)`; Phase 0A completes; child remains `77/80` because VRP-102 is still gated by other parent production prerequisites | Retained exact-Samsung evidence closes the previously reopened consumer gap: canonical `Tent_Contractor` ownership and `350/350` health resolved, occupied placement was blocked through the visible interaction path, each Recruit invocation spent exactly 25 materials and increased stable unit/model/source ownership, and captures show the helicopter/rope/suspended-drop/completed-ground-spawn sequence. The unchanged accepted Editor evidence retains health, faction, footprint, targeting, selection, and intact/destroyed ownership coverage. No contradictory evidence required a rerun. Production stays `StaticSceneChunks + ResidentEntities`, `productionCutover=0`; no package reinstall, implementation change, protected/production mutation, preserved file, or independent map defect changed. |
| 2026-08-09 | Phase 9 Android acceptance umbrella closure | Final pushed/package head `f6accf53e`; exact `544,011,811`-byte APK SHA-256 `65a4a57c8014ff99a06151a6adb2b4046a7304bfb1f1b817ad49c764128b452d`; complete child VRP-090 through VRP-097 package, representative-route, memory, state, lifecycle, and final thermal evidence; packed asset-sharing report | Parent advances to `128/149 (86%)`; child remains `77/80`; production cleanup/cutover prerequisites remain open | The exact Samsung accepted the offline package and current revision. The representative set passes the final 54-FPS contract and unchanged p95/p99/CPU/GPU/allocation/correctness gates; retained PSS/RSS/graphics improve over frozen Phase 0; state recycling ends with zero overflow/deficit; packed sharing has one shared render array/EntityScene archive and zero duplicated dependency bytes; visual, navigation/interaction, destruction, delivery, authored-vehicle, and two-cycle full-unload coverage is retained. The final cooled route passes at `59.980` average FPS, `59.997` p10 FPS, CPU `11.568/13.218 ms`, GPU `13.849/14.001 ms`, and `0 B/frame`. No rerun, reinstall, gate change, production authorization, protected/production mutation, `productionCutover=0` change, preserved-file change, or independent defect reclassification occurred. |
| 2026-08-09 | Phase 6 conditional LOD and mobile visual-policy closure | VRP-072 exact inventory/database/direct-bake/focused/Android evidence; retained representative and final thermal routes; `git diff --check` | Parent advances to `131/149 (88%)`; child remains `77/80`; no LOD/impostor output is introduced | Complete inventory proves all `12,293` parts are LOD0-only with no alternate recipe, so both conditional LOD rows close without synthetic geometry; current database/packed/materialized hashes remain the deterministic source/output contract. The accepted shadow/detail policy changes exactly `12,185` eligible logical rows while preserving receive-shadows and all large/resident exceptions. Exact-Samsung captures reduce pitch-black coverage by more than half with no gross visual defect, and later GPU/frame/thermal gates pass. Any future alternate recipe reopens hash/transition validation. No implementation, production setting, protected/production asset, package, reinstall, `productionCutover=0`, preserved file, or independent defect changed. |
| 2026-08-09 | Phase 9 memory/GC/thermal/visual-travel/baseline closure | VRP-083, VRP-086, and VRP-092 through VRP-097 accepted reports, exact-device evidence, packed Build Layout, and `git diff --check` | Parent advances to `135/149 (91%)`; child remains `77/80`; production prerequisites remain open | Exact current memory and authoring exclusion, zero steady/transition allocation, cooled sustained timing/thermal behavior, LOD0 visual continuity, and zero camera-driven map-content operations are all accepted. Current retained PSS/RSS/graphics improve over the frozen Phase 0 artifacts, every final performance budget passes, and all historical failing attempts remain rejected. No rerun, reinstall, implementation, gate, production authorization, protected/production mutation, `productionCutover=0` change, preserved-file change, or independent defect reclassification occurred. |
| 2026-08-09 | Phase 9 Entities Graphics/culling/integration/destruction-cost closure | Packed sharing/parity reports; VRP-080 through VRP-086; accepted VRP-092/VRP-095/VRP-096 exact-Samsung evidence; lifecycle stream-latency log; `git diff --check` | Parent advances to `136/149 (91%)`; child remains `77/80` | One shared render array/archive, complete gameplay/resident/logical/render counts, fixed-slot culling with zero simulation mutation/content streaming, LOD0 continuity, two-cycle EntityScene latency, and destruction-route/state-transition cost/correctness are all recorded. No new run, implementation, package, production authorization, protected/production mutation, `productionCutover=0` change, preserved-file change, or independent defect reclassification occurred. |
| 2026-08-09 | Phase 9 navigation/aircraft/anchor closure and cross-platform definition-gate repair | Retained exact-Samsung Phase 0A/Phase 9 interaction and representative routes; `Build/Logs/parent-navigation-current-map-anchors-canonical-hash.log` `4/4`; `Build/Logs/parent-navigation-aircraft-scenario-focused.log` `3/3`; `Build/Logs/parent-navigation-helipad-pathing-atomic-version-matched.log` `2/2`; `git diff --check` | Parent advances to `137/149 (92%)`; child remains `77/80` | Retained device evidence covers targeting/selection, placement, helicopter delivery, camera travel, and minimap. Fresh focused gates cover production-definition identity, deployment cells/anchors, runway and helipad runtime consumers/teardown, scenario requirements, and helipad pathing. The July 19 runtime cutover's stale test/rebuild-helper SubScene reference now resolves the established production operation-map SubScene; deterministic text hashing canonicalizes CRLF to LF so Windows and macOS validate identical content. A narrow `3/3` executeMethod owner was added for the existing aircraft/scenario tests. No production definition/gameplay asset, package, threshold, `productionCutover=0`, preserved file, or independent defect changed. The unrelated stale automatic-base fixture and licensing-start failures remain rejected. |
| 2026-08-09 | Final Android package exact-plan gate and installed-artifact reconciliation | Official ADB `37.0.1` verified `SM-S918B` / `R5CTC1J02VB` at `192.168.2.33:42611`; installed/local APK identity `544,011,811` bytes / SHA-256 `65a4a57c8014ff99a06151a6adb2b4046a7304bfb1f1b817ad49c764128b452d`; focused `Build/Logs/parent-android-package-exact-dense-plan-focused-host-gui.log` `13/13`; expected real-APK rejection `Build/Logs/parent-android-package-stale-bundle-real-apk-rejection.log`; `git diff --check` | Parent remains `137/149 (92%)`; child remains `77/80`; final package-size/ownership row stays open and this APK will not be reinstalled | The installed base APK is byte-for-byte identical to the accepted local artifact, but exact ZIP inventory exposed a final-package ownership defect: the current report/build output owns four dense bundles totaling `915,827` bytes, whereas the APK contains six totaling `1,070,729` bytes. `059ad5323b1112826e7d3560ced80254.bundle` (`152,869` bytes) and `c2059c47c5fbfa12fb10b95f9419f34c.bundle` (`2,033` bytes) are unreachable leftovers from incremental player packaging. The strengthened fail-closed gate requires exact equality between the package's dense catalog/hash/bundle paths and the current deterministic deployment plan; its focused suite passes `13/13`, and the real artifact now rejects on the first stale entry. Existing behavioral/performance evidence remains accepted because the active catalog never references those files, but final package size, generated-artifact ownership, and production cutover require a fresh clean checked artifact. The first GUI wrapper failed before compilation; only its exact task-started orphan was cleaned, and the identical host-access GUI wrapper passed without changing Unity/Hub paths. No install, launch, parent checkbox, production/protected asset, `productionCutover=0`, preserved file, or independent defect changed. |
| 2026-08-09 | Dense-candidate clean-cache package root-cause correction | Checked GUI-licensing focused `Build/Logs/parent-android-package-clean-cache-focused-host-gui.log` `[DenseCityCandidateAndroidPackageValidation] result=Passed tests=15`; `git diff --check` | Parent remains `137/149 (92%)`; child remains `77/80`; fresh post-cutover package evidence remains required | Dense-candidate release and profiler player builds now unconditionally include `BuildOptions.CleanBuildCache`. Because current deterministic Addressables content hashes rename dense bundle files, the former optional/incremental build path could preserve unreachable old bundles in the APK even after the active catalog changed. Focused coverage proves both build variants force a clean cache while retaining release detailed-report or profiler development semantics. The rejected installed/local APK is unchanged and will not be reinstalled; no fresh package, device run, parent checkbox, production/protected mutation, `productionCutover=0` change, preserved-file change, or independent defect reclassification occurred. |
| 2026-08-09 | Authorized staged production EntityScene cutover | Checked GUI-licensing wrapper `Build/Logs/vrp102-production-cutover-host-gui-r2.log`; `[OperationMapDenseCityProductionCutover] result=Passed`; rollback checkpoint `Design/AgentReports/2026-08-09_dense_city_production_cutover_rollback_checkpoint.json`; `git diff --check` | Parent advances to `138/149 (93%)`; child advances to `78/80`; fresh production package and rollback restore are next | The transaction captured pre-cutover definition, runtime binding, complete Addressables settings, and frozen-static hashes before mutation. Production now uses `EntityScene + VirtualizedProxyPool`, its thin binding has the accepted dense SubScene as canonical sole presentation owner, and operation-map Addressables have catalog/core/shared/presentation entry counts `2/4/0/0`. The rollback record binds source revision `b9383b6a12e7b4ef6b9494a05a8a8788db90b859` and exact static ownership of `545` files, `269` scenes, and `42,765,094` bytes. The static output remains tracked and untouched until the fresh clean package and rollback gate pass; preserved generated reports and `_Recovery` scenes remain unstaged. |
| 2026-08-09 | Production clean-package checked entry point | Checked GUI-licensing package contract `Build/Logs/vrp103-production-build-entry-package-focused-gui.log` `15/15`; checked report/provenance contract `Build/Logs/vrp103-production-build-entry-report-focused-gui-r2.log` `14/14`; `git diff --check` | Parent remains `138/149 (93%)`; child remains `78/80`; fresh production artifact is next | The production acceptance build now has one focused execute method that hard-requires clean cache and publishes an explicit package pass marker only after Addressables build-layout validation, player build, EntityScene ZIP validation, and Android report publication. A stale source-text assertion exposed by the first report-suite run was corrected to validate the existing option helper plus the new forced-clean entry point; that first run remains rejected and the rerun passes. No package, install, device claim, static deletion, preserved-file mutation, or independent defect reclassification occurred. |
| 2026-08-09 | First clean production APK rejection and current-definition EntityScene registration | Rejected checked build `Build/Logs/vrp103-production-9299cc962-clean-apk-gui.log`; accepted focused `Build/Logs/vrp103-production-entityscene-build-addition-versioned.log` `[OperationMapEntitySceneBuildAdditionsValidation] result=Passed tests=2`; production Addressables report | Parent remains `138/149 (93%)`; child remains `78/80`; replacement clean package remains required | The player build itself succeeded, but the post-build gate correctly rejected the APK because built-in EntityScenes still registered old rollback GUID `d50925a...` and omitted production cutover GUID `c00140f...`. The build-additions hook now follows the current production definition in EntityScene mode while preserving static rollback and candidate override behavior. The fresh Build Layout proves six operation-map addresses, five bundles totaling `135,568,624` bytes, and zero duplicated dependencies. One GUI focused recovery timed out after entitlement before project load; its wrapper stopped only its own process tree, and the identical checked version-matched fallback passed `2/2`. The rejected APK is not authorized for installation. No parent checkbox, static deletion, preserved-file staging, or independent defect changed. |
| 2026-08-09 | Independent blue-Autobahn/canal material isolation correction | Checked focused `Build/Logs/autobahn-canal-material-isolation-focused-version-matched-host.log` `2/2`; checked full-scene `Build/Logs/autobahn-original-road-material-scene-validation-host.log`; exact `1,108` Autobahn tiles / `5,540` material slots; `git diff --check` | Parent remains `138/149 (93%)`; child remains `78/80`; final Android visual confirmation remains part of VRP-103 | Blue coloring now requires both the canonical water material and canonical canal-surface prefab instead of material path alone. The protected Autobahn validator independently requires every renderer slot to retain the original approved opaque road-prefab material and rejects water, transparency, slot-count drift, or material substitution. The complete scene passes without mutating either source scene. User-authorized recovery stopped only verified wrapper-created Unity trees that failed before project load; the verified stale licensing client alone was restarted, Hub remained open, and the identical checked host-access wrappers passed. The canal remains blue. Road-over-water-without-bridge remains separate and open. No production/protected asset, frozen static output, preserved report, or `_Recovery` scene changed. |
| 2026-08-09 | Regenerated production candidate and exact Editor-to-bake material/transform parity | Rejected installed production APK SHA-256 `d6417690...e66f`; transactional regeneration and protected Autobahn logs; rejected legacy count-gated full validator; accepted `Build/Logs/production-dense-city-material-transform-parity.log`; generated parity report; `git diff --check` | Parent remains `138/149 (93%)`; child remains `78/80`; replacement production package, Android visual proof, and rollback restore remain open | The first cutover package launched normal Match gameplay, but visible blue roads/dark ground rejected it. The corrected source converter had not been propagated into the serialized production candidate before that package. Transactional regeneration now updates both candidate scenes, and the protected source audit passes all `1,108` tiles / `5,540` opaque road slots. The pre-cutover full validator is retained as rejected because its obsolete `5,403`-building assertion stops before parity against the accepted current `4,977`; no threshold was changed. The new post-cutover gate verifies exact production definition/EntityScene/source-scene binding and compares the in-memory runtime bake against the Editor candidate for mesh, material, base color, world matrix/scale, and renderer bounds. It passes `36,304` matched identities and `62,455` matched renderer entries, zero unresolved assets, zero mesh/material/base-color mismatches, identical identity-set hashes, and zero rejected rows. The rejected APK will not be reused; canal and road-over-water defects remain separately classified. Preserved reports and `_Recovery` scenes remain unstaged. |
| 2026-08-10 | Open-ground Android material correction and clean Editor-to-runtime parity | Rejected APK SHA-256 `c15cf22bc84393a84f9334459a28ccc80d4d7ad41b0df2227fe865fcec3addd7` and exact Samsung urban-pan evidence; `Build/Logs/open-ground-source-material-integration-host-gui.log`; `Build/Logs/vrp103-open-ground-source-material-regenerate.log`; `Build/Logs/vrp103-open-ground-source-material-autobahn.log`; rejected first parity `Build/Logs/vrp103-open-ground-source-material-production-parity.log`; `Build/Logs/vrp103-open-ground-source-material-database-rebuild.log`; accepted `Build/Logs/vrp103-open-ground-source-material-production-parity-r2.log` | Parent remains `138/149 (93%)`; child remains `78/80`; replacement package/device visual proof and rollback restore remain open | Controlled Android world panning reproduced the black ground on the fresh production artifact, which is rejected and will not be reused. The affected open-ground round family alone was forcing the custom ground-variation shader instead of retaining its prefab's opaque source material; the bounded fix changes only that material selection and leaves every placement, world matrix, scale, renderer bound, surface record, road, canal, and protected source unchanged. Full generation and protected Autobahn gates pass. The first parity run is rejected for its explicit stale virtualized-database exception even though it later printed a pass marker. The checked database rebuild passes source parity, and the clean rerun has no exception and proves all `36,304` identities / `62,455` renderer entries with zero mesh, material, base-color, transform/scale, bounds, missing, unexpected, or rejected mismatch. This establishes Editor-to-baked-runtime data parity; Android visual acceptance still requires the next fresh package. Autobahn/canal isolation and road-over-water-without-bridge remain separate defects. |
| 2026-08-10 | Fresh production loading-curtain diagnosis and match-start deadlock correction | Rejected exact APK `557,964,913` bytes / SHA-256 `9efa6a3612fa07fa96568303d222adbe8b969d0a640b662c34a1cacc3f9efec1`; exact Samsung evidence `Build/AndroidDenseCandidate/Evidence/2026-08-10-vrp103-production-5edc44c7f-open-ground-parity`; `Build/Logs/vrp103-match-runtime-support-entityscene-android-final-r2.log`; `Build/Logs/vrp103-match-start-no-registry-deadlock-focused.log` `6/6`; rejected `Build/Logs/vrp103-match-start-editor-shell-smoke.log`; `git diff --check` | Parent remains `138/149 (93%)`; child remains `78/80`; replacement package/device visual proof and rollback restore remain open | The exact newly installed package loaded the support and dense EntityScenes but reproducibly remained behind the opaque loading curtain with `playRequested=0` and `simulationActive=0`, so it is rejected and will not be reinstalled. Its embedded `b57de3f...` support payload is byte-identical to the checked artifact (`843,528` bytes, SHA-256 `ae950e...f4c5`), and a new Android-target streaming gate proves one packed unit registry with `51` valid prefab entities. The defect was an obsolete pre-`BeginGameplay` registry predicate at the match-start boundary; match start now invokes the existing gameplay startup owner without trying to pre-project its runtime content. Focused coverage passes `6/6`. The asynchronous Editor shell route is honestly rejected because checked-wrapper `-quit` ended before its PlayMode continuation produced a marker. No Android visual, rollback, or retirement claim is made. The accepted Editor-to-baked-runtime material, size, position, matrix, scale, and bounds parity remains a hard gate; Autobahn/canal isolation and road-over-water-without-bridge remain separate. |
| 2026-08-10 | Replacement-package rejection and circular start-gate correction | Rejected exact clean APK `557,966,917` bytes / SHA-256 `565d15ba5686cbe2f274f71762f19d2b2bb409f28c2b15485ac6d860d41b6c3f`; build head `4e9731b8f91a9bd0b891c032ec9edf07fd6d94a8`; exact Samsung evidence `Build/AndroidDenseCandidate/Evidence/2026-08-10-vrp103-production-4e9731b8f-startup-fix`; accepted `Build/Logs/vrp103-match-start-view-publication-focused-r2.log` `7/7` | Parent remains `138/149 (93%)`; child remains `78/80`; replacement device visual proof and rollback restore remain open | The fresh package passed its checked build and content-provenance gates, installed once, and is rejected because its loading curtain stayed byte-identical at `20 s`, `65 s`, and later with gameplay never requested. The remaining circular gate required a published `streamedMatchView` before advancing the start boundary that itself safely waits for that publication. Match routes now advance while the view is null; once present, ordinary static-presentation routes remain preload-gated and EntityScene routes retain their intended static-streamer bypass. Direct focused coverage passes `7/7`. No Android visual parity, rollback, retirement, or performance acceptance is claimed; exact Editor-to-runtime material, size, and position remain mandatory. |
| 2026-08-10 | Second replacement rejection and persistent-Editor startup/runtime dependency correction | Rejected exact clean APK `557,966,561` bytes / SHA-256 `670d093121102f7f84c765ef04ae041de2ae13fd4199f74ea4805d37ca989957`; build head `08e67c23774559926f833185264c4dd6e03dcafb`; exact Samsung evidence `Build/AndroidDenseCandidate/Evidence/2026-08-10-vrp103-production-08e67c237-view-publication-fix`; rejected persistent GUI diagnostics through `Build/Logs/vrp103-persistent-gui-runtime-smoke-r10.log`; focused `Build/Logs/vrp103-custom-game-packed-registry-focused.log` `5/5` and `Build/Logs/vrp103-unit-render-budget-dependency-focused.log` `38/38`; `git diff --check` | Parent remains `138/149 (93%)`; child remains `78/80`; startup and air/render dependency fixes pass focused gates while exact-device visual proof and rollback restore remain open | The exact package passed checked build/provenance, installed once on the verified Samsung, and is rejected/stopped because its curtain remained byte-identical with `playRequested=0`. Persistent checked GUI diagnosis proved the EntityScene reaches `mapReady=1` and isolated the next hold to `0.38 / Preparing unit prefabs`: the support registry and startup config share one packed entity, but the legacy projection skipped that exact registry before clearing/rebuilding it and then repeatedly scanned all prefab names. The initializer now captures the still-tagged baked buffer first, while any already-projected runtime entity remains excluded because its tag was removed. The next GUI run reaches `playRequested=1`, active simulation, complete Match HUD/curtain, and EntityScene readiness. A separate repeated safety failure in the aircraft/render path was also corrected by carrying the system dependency into the render-budget snapshot job that reads `LocalTransform` after helicopter blade spin writes it. Focused gates pass; the full GUI smoke is retained as rejected because the Editor SubScene worker alone emits its dense-scene debug-name-capacity error, which is not hidden. No replacement APK, Android visual acceptance, rollback restore, static retirement, or performance claim is made. Existing zero-mismatch Editor-to-baked-runtime material/base-color/size/position/matrix/scale/bounds evidence remains the hard parity gate. |
| 2026-08-10 | Current-head Editor/runtime parity and protected Autobahn reconfirmation | Pushed implementation head `25e86e5bebc98493e1fc4516922b8dcf1c64d233`; checked GUI/D3D11 `Build/Logs/vrp103-current-head-production-parity.log`; exact zero-mismatch production parity marker; checked GUI/D3D11 `Build/Logs/vrp103-current-head-protected-autobahn.log`; protected Autobahn marker; wrapper exits `0` | Parent remains `138/149 (93%)`; child remains `78/80`; current head is eligible for one fresh package but exact-device visual/performance and rollback evidence remain open | The current pushed head rebakes all accepted production content and proves `36,304/36,304` identities plus `62,455/62,455` renderer entries with zero material, base-color, size, position, matrix, scale, bounds, mesh, missing, unexpected, or rejected mismatch. The separate protected audit proves all `1,108` Autobahn tiles / `5,540` road slots retain the approved opaque prefab materials, independently of blue canal water. The parity bake emits the same Unity Entities Editor debug-name-capacity diagnostic already present in the earlier accepted exhaustive parity run; the full runtime-shell smoke stays honestly rejected for that diagnostic, while this bounded data comparator completes and passes through the checked wrapper. No APK, Android visual acceptance, performance, rollback restore, or static retirement claim is made. |
| 2026-08-10 | Exact current-head clean production package readiness | Rejected unqualified no-op build invocation; accepted checked GUI/D3D11 `Build/Logs/vrp103-production-0c3e7543b-clean-apk-r2.log`; exact pushed head `0c3e7543b38b65c05f3a290072fbe4ee3a91a8fb`; clean production package/report markers; `Build/AndroidAPK/WarlineCapture.apk` `557,991,885` bytes / SHA-256 `45c741573b331117841c716ec21e718899970d748555e06e4e4d3a3f0bb8200a`; direct embedded EntityScene hashes | Parent remains `138/149 (93%)`; child remains `78/80`; exact package is eligible for one Samsung install after the phone refreshes wireless debugging | The package is built from the exact pushed parity/startup head with clean cache and zero static presentation partitions or duplicated dependencies. Its support and `37,667,924`-byte dense production EntityScene entries are byte-identical to the checked build inputs. The first invocation is rejected because its unqualified class did not execute and left an old APK untouched. For the accepted artifact, the authorized Samsung serial remains discoverable and the unchanged host ADB key completes TLS, but the phone never finishes the ADB transport on either advertised wireless endpoint; the final USB check found no attached device. No install, Android visual/performance, rollback restore, or static retirement claim is made until an on-device wireless-debugging refresh supplies a working primary connect port. |
| 2026-08-10 | Current-head exact Samsung startup rejection | Exact clean APK `557,991,885` bytes / SHA-256 `45c741573b331117841c716ec21e718899970d748555e06e4e4d3a3f0bb8200a`; installed/local APK byte equality; exact `SM-S918B` / `R5CTC1J02VB`; evidence `Build/AndroidAPK/Evidence/2026-08-10-vrp103-production-0c3e7543b`; byte-identical `20 s` / `65 s` frames SHA-256 `db4de69c...0656`; device log | Parent remains `138/149 (93%)`; child remains `78/80`; package rejected/stopped and Android startup diagnosis remains open | The exact phone recovered without a new key or pairing credential, was reverified before installation, and received the fresh package once. Its pulled installed base APK is byte-identical to the accepted local artifact. Menu and setup render normally, then Launch Mission reproduces the prior opaque transition frame with `playRequested=0` / `simulationActive=0`; there is no crash or AndroidRuntime exception. The package is rejected, stopped, and prohibited from reinstallation. Editor/baked material, size, position, matrix, scale, bounds, and protected Autobahn parity remain accepted data gates, but no Android visual/performance, rollback restore, or static retirement checkbox closes. |
| 2026-08-10 | Child `VRP-103` authoritative shell-route launch correction | Rejected exact-package Samsung startup evidence; checked `Build/Logs/vrp103-match-launch-route-atomic-stale-client-recovery.log`; focused `[MatchLaunchRouteValidation] result=Passed tests=5`; explicit successful termination marker; wrapper exit `0`; `git diff --check` | Parent remains `138/149 (93%)`; child remains `78/80`; fresh pushed package/device evidence is still required | The menu adapter previously bypassed the authoritative shell route by directly requesting scene load and match start, allowing both packaged EntityScenes to load while the shell remained on a menu route with `playRequested=0`. It now submits one idempotent `EnterMatch` request to the unique shell boundary and leaves transition/startup ownership with that existing system. Missing/ambiguous boundaries fail closed, duplicate/active Match requests are suppressed, and focused validation proves the adapter creates no Match-start boundary. Hub `3.20.0` still ships generic licensing client `1.17.4` against Editor `1.18.1`; current official Unity release and activation guidance documents no fix for that pipe skew. The authorized recovery verified no Editor, stopped only the exact stale generic client, and atomically launched the identical checked GUI wrapper, which connected to the versioned channel and passed. No parent row, APK/device acceptance, rollback, static retirement, production/protected asset, preserved file, or independent map defect changes. |
| 2026-08-10 | Child `VRP-103` authoritative-route clean replacement package | Pushed source `f753e6c68c46a17d0bd47fe3e3ba1cb077dadf01`; checked `Build/Logs/vrp103-production-f753e6c68-clean-apk.log`; production Addressables/build/package markers; exact `557,961,973`-byte APK SHA-256 `4f1f99b959556e883cd7e360e0af0a72f75efde061512f80a9ff5125294abe43` | Parent remains `138/149 (93%)`; child remains `78/80`; package is eligible for one exact-Samsung install | The clean package reports exact pushed provenance, one map, six addresses, zero static presentation partitions, and zero duplicate dependency bytes. The checked wrapper passed after the authorized atomic version-matched licensing recovery and completed with exit `0`. The rejected predecessor APK remains prohibited from reinstall; only this exact artifact may proceed to startup, Android material/Autobahn parity, amended performance, rollback, and retirement evidence. Preserved reports and `_Recovery` scenes remain unstaged, and no parent checkbox or independent defect changes. |
| 2026-08-10 | Child `VRP-103` authoritative-route Samsung startup pass and black-ground visual rejection | Exact pushed APK `557,961,973` bytes / SHA-256 `4f1f99b959556e883cd7e360e0af0a72f75efde061512f80a9ff5125294abe43`; exact `SM-S918B` / `R5CTC1J02VB`; installed/local byte equality; evidence `Build/AndroidAPK/Evidence/2026-08-10-vrp103-production-f753e6c68-authoritative-route`; successful cold launch and live-Match captures; eastward visual route; full logcat/battery evidence | Parent remains `138/149 (93%)`; child remains `78/80`; startup is repaired but the package is rejected/stopped on black ground | The normal visible Deploy route now leaves the loading curtain and reaches an active Match in about six seconds; runtime markers prove the shell submitted both deferred scene and gameplay-start requests and the loading gate completed with gameplay requested and simulation active. The same one-time install then failed the mandatory visual gate: the third and fourth eastward views show widespread irregular pitch-black ground patches. Performance routes, rollback restore, and static retirement were not run or claimed. The package is stopped and prohibited from reinstall. The visible gray roads do not constitute a protected Autobahn route, so Autobahn/canal material isolation remains independent; road-over-water-without-bridge also remains separate. Accepted Editor/bake data parity is retained, while the Android visual rejection requires source/Editor diagnosis before any replacement build. |

| 2026-08-10 | Remaining natural-ground black-patch source correction and cutover-aware Editor visual refresh | Rejected first capture `Build/Logs/vrp103-all-natural-ground-source-material-editor-capture.log`; accepted material/generation `Build/Logs/vrp103-natural-ground-source-material-integration-r2.log`; regeneration, database, Autobahn, production-parity, and accepted fixed-camera logs under `Build/Logs/vrp103-all-natural-ground-source-material-*`; refreshed five-view report/images | Parent remains `138/149 (93%)`; child remains `78/80`; Android visual/performance, rollback, and retirement gates remain open | The remaining black terrain was not Android-only: road underlays and building ground patches were additional `SM_Env_Ground_Round_01` callers that forced the custom ground-variation shader. They now retain the prefab's opaque source material without changing placement, scale, bounds, roads, canals, or protected sources. Full generation proves `12,626` ground-round renderers, `12,020` source-material slots, and zero custom slots; the rebuilt inventory proves `7,622` source-material rows and zero custom rows. The deterministic render database passes `40,460` placements / `7,784` slots, exhaustive production parity passes all `36,304` identities / `62,455` renderers with zero mismatches, and the independent Autobahn gate retains all `5,540` approved gray road slots. The stale pre-cutover fixed-camera count failed closed; its corrected `productionCutover=1` rerun captures five current Editor views / `76,052` renderers and direct review finds no former pitch-black ground patches. Canal blue and road-over-water-without-bridge remain separately classified. The rejected APK is not reused, and no Android, rollback, static-retirement, or completion checkbox closes here. |

| 2026-08-10 | Child `VRP-103` live-Match protected Autobahn blue-reflection correction | Pre/post actual Match captures under `Build/AndroidAPK/Evidence/2026-08-10-vrp103-production-daf769091-natural-ground/windows-runtime-proof*`; checked runtime proof `Build/Logs/vrp103-protected-autobahn-windows-runtime-proof-fixed-recovery.log`; checked source gate `Build/Logs/vrp103-matte-autobahn-source-validator-recovery.log`; checked exhaustive parity `Build/Logs/vrp103-matte-autobahn-production-parity-atomic-r3.log`; wrapper exits `0`; `git diff --check` | Parent remains `138/149 (93%)`; child remains `78/80`; fresh Android visual/performance, rollback, and retirement evidence remain open | The exact protected corridor reproduced blue in the real Windows Match runtime, proving the defect preceded Android packaging. The dedicated road-center material was smooth and reflected the blue environment; it is now matte with reflections disabled while retaining its material identity and all geometry/transform data. The same actual Match route now shows dark-gray Autobahn and tan ground. A permanent fail-closed gate requires the matte material on every protected road owner and passes `1,108` matte slots / `1,108` tiles plus all `5,540` approved opaque road slots. Exhaustive cutover parity remains zero-mismatch across `36,304` identities and `62,455` renderers, including material/base color, size, position, matrix, scale, and bounds. Blue canal geometry and road-over-water-without-bridge remain separate defects. No parent checkbox, Android acceptance, performance, rollback restore, static retirement, or completion claim is made. |

| 2026-08-10 | Child `VRP-103` authoritative-route presentation-lifetime race correction | Reproduced exact Samsung black route in `Build/AndroidAPK/Evidence/2026-08-10-vrp103-production-490e71e22-matte-autobahn`; full device log `logcat-full-r2-20s.txt`; checked focused `Build/Logs/vrp103-shell-lifetime-focused-gui.log`; `[MatchLaunchRouteValidation] result=Passed tests=6`; wrapper exit `0`; rejected asynchronous attempts `Build/Logs/vrp103-shell-lifetime-deploy-playmode-gui.log` and `Build/Logs/vrp103-shell-lifetime-deploy-playmode-general-gui.log`; `git diff --check` | Parent remains `138/149 (93%)`; child remains `78/80`; fresh pushed package/device evidence remains open | The package repeatedly queued one authoritative Match route, then stayed black without a crash. All shell presentation owners share the router's GameObject, so the adapter's manual deactivation stopped transition completion before the ECS shell could consume a route submitted during the setup transition. The launch command now only queues the request and leaves shell visibility/lifetime to the authoritative command sequence. The focused regression invokes the public launch path during an in-progress transition, proves the shell remains active and exactly one request is retained, and preserves prior fail-closed/idempotence coverage. The asynchronous wrapper attempts remain rejected: one was ended by focused `-quit`, while the general wrapper emitted a real `Loading/Match` marker but exited nonzero on Unity shutdown. No APK, Android visual/performance, rollback restore, static retirement, parent row, or completion checkbox closes. |

| 2026-08-10 | Child `VRP-103` shell-lifetime clean replacement package | Exact pushed implementation `5600688756a144dc7bafeb6d44c2b92d72de2196`; checked `Build/Logs/vrp103-production-560068875-clean-apk.log`; clean Addressables/build/package markers; wrapper exit `0`; exact APK `557,495,989` bytes / SHA-256 `cba0d21627ff5dd274f6fc91f26237a6a853dac51072fe736082f63deab45f58` | Parent remains `138/149 (93%)`; child remains `78/80`; package is eligible for one exact-Samsung install | Exact provenance, one map, six addresses, five bundles / `135,333,888` bytes, zero static partitions, and zero duplicated dependencies pass through the checked clean Android wrapper. Preserved report and `_Recovery` dirt explains the report flag and remains unstaged. The rejected installed predecessor is not eligible for reuse; only this fresh package may proceed to startup, natural-ground/protected-Autobahn visual parity, amended performance/thermal, rollback restore, and static retirement. No checkbox closes from build evidence alone. |

| 2026-08-10 | Child `VRP-103` Match-HUD stale setup-overlay correction | Exact Samsung evidence `Build/AndroidAPK/Evidence/2026-08-10-vrp103-production-560068875-shell-lifetime`; checked focused `Build/Logs/vrp103-match-popup-lifecycle-focused-gui.log`; `[MatchLaunchRouteValidation] result=Passed tests=7`; wrapper exit `0`; `git diff --check` | Parent remains `138/149 (93%)`; child remains `78/80`; fresh pushed package/device visual proof and rollback remain open | The shell-lifetime package reaches live Match but is rejected because its Skirmish Setup popup remains above the HUD. The setup route owns `PopupLayer`; authoritative Match-HUD installation did not clear that prior menu layer. Match-HUD preparation now clears it without disabling the persistent shell. A focused behavioral regression seeds the stale popup and proves `EnterMatchHud` leaves the shell active with an empty popup layer. The rejected APK will not be reinstalled. No Android visual/performance, rollback, static retirement, parent checkbox, or completion claim is made. |

| 2026-08-10 | Child `VRP-103` popup-clear clean replacement package | Exact pushed implementation `b397a26cbe27a9479d49249979366e511fc96b52`; checked `Build/Logs/vrp103-production-b397a26cb-popup-clear-clean-apk.log`; clean Addressables/build/package markers; wrapper exit `0`; exact APK `557,492,325` bytes / SHA-256 `65e3221ce32b6de1d20a94e3c50869faab33a28f02ab083a4877ce6bffcb9a29` | Parent remains `138/149 (93%)`; child remains `78/80`; package is eligible for one exact-Samsung install | Exact pushed provenance, one map, six addresses, five bundles / `135,333,888` bytes, zero static partitions, and zero duplicated dependencies pass through the checked clean wrapper. The rejected predecessor is not eligible for reuse. No device acceptance, performance, rollback, static retirement, parent checkbox, or completion claim is made. |

| 2026-08-10 | Child `VRP-103` transactional rollback-restore acceptance | Checked GUI wrapper `Build/Logs/vrp103-production-rollback-restore-gui-r5.log`; exact pass marker; generated rollback validation report | Parent remains `138/149 (93%)`; child remains `78/80`; final production Android route acceptance remains open | The checked transaction restores and semantically validates the exact pre-cutover static definition/runtime binding plus `54` source-owned Addressables files from `b9383b6a...`, verifies frozen rollback ownership of `545` files / `269` scenes / `42,765,094` bytes at `8cb615...3764`, then reapplies current EntityScene production and proves the full current transaction hash is byte-identical before/after. Five ignored Addressables build-state files were included by the historical aggregate checkpoint but never source-archived; they are now explicitly separated, preserved, and restored byte-exactly while the reproducible revision-owned tree hashes to `a738ec...23ca`. Production fixed/slow-pan/fast-pan pass on the exact Samsung package; rejected zoom stress and battery/thermal-invalid reruns do not close Android acceptance. Static retirement remains prohibited until the remaining VRP-103 production route gate passes. Preserved reports and `_Recovery` scenes remain unstaged; blue canal and road-over-water-without-bridge remain separate. |

| 2026-08-10 | Child `VRP-103` exact production Android and rollback acceptance | Exact `557,492,325`-byte APK SHA-256 `65e3221ce32b6de1d20a94e3c50869faab33a28f02ab083a4877ce6bffcb9a29`; installed-base byte equality; exact Samsung `SM-S918B` / `R5CTC1J02VB`; production visual/performance evidence roots; `Design/AgentReports/2026-08-10_dense_city_production_android_acceptance.json`; accepted rollback report/log | Parent remains `138/149 (93%)`; child advances to `79/80`; child static retirement and parent closeout rows remain open | The fresh package reaches live Match, retains Editor-matched tan ground/neutral matte Autobahn geometry and material output, and keeps the blue canal independently classified. Nine diagnostics-disabled `120 s` routes pass the amended average/p10/average-frame contract and unchanged p95/p99/CPU/GPU/allocation gates. The final thermal route records `59.968` average FPS / `59.979` p10 FPS, CPU `11.768/13.386 ms`, GPU `14.250/14.422 ms`, thermal status `0 -> 0`, temperature `36.0 -> 37.5 C`, battery `100 -> 100`, and `0 B/frame`. The app is stopped and device display/power settings are restored. Transactional rollback restore remains accepted. Rejected packages and invalid route samples are excluded; blue canal and road-over-water-without-bridge remain separate. This closes only child `VRP-103`; no parent checkbox or static file is changed in this step. |

| 2026-08-10 | Child `VRP-104` verified static-package retirement | Explicit authorization; checked preflight/postflight logs; `Design/AgentReports/2026-08-10_dense_city_static_retirement_validation.json`; exact rollback revision/hash; `git diff --check` | Parent advances to `139/149 (93%)`; child completes at `80/80`; ten parent closeout rows remain | The separately reviewable cleanup removes exactly `543` tracked legacy files / `269` scenes / `42,628,450` bytes and retains only the unchanged minimap pair. Preflight bound the full `545`-file frozen package to revision `b9383b6a...` and SHA-256 `8cb615...3764`; postflight proves current EntityScene production, zero static manifest/chunk entries, prior rollback/Android acceptance, and complete Git recovery. No protected map authoring, production EntityScene content, blue canal, or road-over-water-without-bridge evidence changed. |

| 2026-08-10 | Parent production dependency-closure acceptance | Checked repair `Build/Logs/parent-physics-free-prefab-dependency-repair-gui-r9.log`; checked closure `Build/Logs/parent-production-dependency-closure-gui-r10.log`; `Design/AgentReports/2026-08-10_dense_city_production_dependency_closure.json`; explicit successful wrapper exits | Parent advances to `140/149 (94%)`; child remains complete at `80/80`; nine parent closeout rows remain | Future dense-city prefab realization resolves collider-bearing visual sources to deterministic physics-free generated mirrors, while three collider-bearing building definitions use dedicated fully unpacked physics-free definitions. The current candidate redirects `501,925` serialized references across `78` unique prefab mappings without mutating shared source prefabs. The fail-closed six-root closure traverses `1,665` assets, opens both production scenes, validates `898` directly owned/generated map GameObject assets / `905` roots, and reports zero prohibited physics, retired authoring dependencies, or static entries. It separately classifies `220` transitive gameplay-unit assets reached through valid production configuration as non-map-owned, preserving their gameplay physics contract. Hub entitlement was verified active; the checked GUI wrapper passed after approved stale-client recovery and 66.34 GiB of only rebuildable `Library/Bee`, `Library/BuildCache`, and `Library/GradleCache` was removed after exact audits/no-Editor checks. Preserved reports and `_Recovery` scenes remain unstaged. |

| 2026-08-10 | Parent final-package report publication isolation | Rejected checked build `Build/Logs/parent-final-production-apk-c76bd84e8.log`; checked redirected report build `Build/Logs/parent-final-addressables-report-override-validation.log`; `Design/AgentReports/2026-08-10_dense_city_final_operation_map_addressables_build_report.json`; `git diff --check` | Enabling repair accepted; parent remains `140/149 (94%)`; nine Android/final-evidence rows remain open | The first fresh production package attempt failed closed before player packaging because the report publisher could not replace the explicitly preserved modified canonical Addressables report. The editor-only builder now accepts one fail-closed process-scoped output override, restricted to `.json` files under `Design/AgentReports`; the identical checked Addressables path publishes the fresh five-bundle / `135,348,884`-byte / zero-duplicate result to a new dated evidence file. The preserved canonical report remains byte-for-byte at its pre-run user state, no rejected APK is installed, and runtime/build content policy is unchanged. |

| 2026-08-11 | Parent packed/static byte-accounting closeout | Accepted packed EntityScene/runtime-content evidence; `Design/AgentReports/2026-08-10_dense_city_final_operation_map_addressables_build_report.json`; `Design/AgentReports/2026-08-10_dense_city_production_dependency_closure.json`; `Design/AgentReports/2026-08-10_dense_city_static_retirement_validation.json`; JSON parse; `git diff --check` | Parent advances to `141/149 (95%)`; child remains complete at `80/80`; eight parent closeout rows remain | The accepted packed archive remains `137,862,756` bytes with zero shared/duplicated dependency GUIDs or bytes. Frozen rollback is separately retained at a `10,097,753`-byte manifest plus `269` scene chunks / `32,381,589` bytes. The final production report records five Android bundles / `135,348,884` bytes and no duplicate dependencies; independent closure and retirement reports both prove zero live static-manifest and static-chunk entries. No device result, material/transform claim, preserved worktree file, protected asset, blue canal, or road-over-water classification changes. |

| 2026-08-11 | Parent final APK/install/content accounting | `Design/AgentReports/2026-08-11_dense_city_final_android_package_evidence.json`; checked package log; exact host/device base length and SHA-256; Android package storage stats; final Addressables, dependency-closure, and retirement reports; `Build/Logs/parent-final-android-package-evidence-validation.log`; JSON parse; `git diff --check` | Parent advances to `142/149 (95%)`; child remains complete at `80/80`; seven parent closeout rows remain | The exact `557,478,145`-byte APK and Samsung-installed base are byte-identical at SHA-256 `25e8dae...1ff9`. Package storage reports `741,172,224` code bytes and `758,293,504` code+data+cache bytes; the storage service's restricted follow-up query is disclosed while independent base `stat`/`sha256sum` pass. The package contains five map bundles / `135,348,884` bytes, zero duplicated dependencies, zero static entries, and zero protected authoring-scene dependencies. The existing one-time install is retained; no reinstall, route acceptance, material/transform claim, or independent-defect classification changes. |

| 2026-08-11 | Parent final-package two-cycle lifecycle acceptance | `Design/AgentReports/2026-08-11_dense_city_final_android_lifecycle_evidence.json`; exact installed package identity; cold-launch `am start -W`; two complete Android log/capture cycles under `Build/AndroidAPK/Evidence/2026-08-10-parent-final-6b4d268b8/lifecycle`; JSON parse; `git diff --check` | Parent advances to `143/149 (96%)`; child remains complete at `80/80`; six parent closeout rows remain | The exact installed final package cold-launches in `230/238 ms` total/wait and completes two visible Menu -> Match -> Menu cycles without reinstall. Deferred Match request to fail-closed gameplay readiness is `3,039 ms` then `2,851 ms`; the second cycle visibly records `Loading map presentation` at `90%` / `LOADING REQUIRED DATA`. Both unloads visibly return to the complete Menu. Because release logging emits no teardown-duration marker, the report records honest screenshot-polling upper bounds of `5,605 ms` and `8,297 ms` and combines them with the already accepted exact support/candidate EntityScene decomposition and teardown contract. No fatal/ANR, package mutation, protected asset change, or independent-defect reclassification occurred. |

| 2026-08-11 | Parent final-package representative-route and thermal acceptance | `Design/AgentReports/2026-08-11_dense_city_final_android_performance_acceptance.json`; nine exact-package recorder JSON files and bounded route logs/screenshots under `Build/AndroidAPK/Evidence/2026-08-10-parent-final-6b4d268b8`; explicit recorder completion markers; thermal/battery pre/post snapshots; fail-closed non-Unity validation; `git diff --check` | Parent advances to `144/149 (97%)`; child remains complete at `80/80`; five parent closeout rows remain | All nine diagnostics-disabled routes run `120 s` on exact Samsung `SM-S918B` / `R5CTC1J02VB` without reinstall and pass the amended average/p10/average-frame plus unchanged p95/p99/CPU/GPU/allocation gates. The final thermal route begins and ends at status `0`, runs `120.009 s`, and records `59.979` average FPS / `59.997` p10, CPU `11.861/13.516 ms`, GPU `14.243/14.420 ms`, and `0 B/frame`. The first fast-pan and destruction samples remain rejected; cooled `_r2` samples replace them for performance. The fresh destruction taps did not select a building, so only the earlier same-package Barracks selected/destroyed frames are used for correctness, not its rejected performance. Release render counters are explicitly unavailable and are reconciled with accepted diagnostic, source-inventory, and current packed-representation counts rather than claimed as zero workload. Blue canal and road-over-water-without-bridge remain separate. |

| 2026-08-11 | Parent final Android transform/bounds parity reconciliation | `Design/AgentReports/2026-08-11_dense_city_final_android_transform_bounds_parity.json`; exact final package/content reports; zero-mismatch Editor-to-baked manifest; current logical database parity; accepted exhaustive two-state materialization; two-cycle exact EntityScene stream log; nine-route Android visual report; fail-closed non-Unity validation; `git diff --check` | Parent advances to `145/149 (97%)`; child remains complete at `80/80`; four parent closeout rows remain | The exact Android-target package streams dense EntityScene `c00140...cbd0` twice and is reconciled with `36,304/36,304` identities, `62,455/62,455` renderers, current `76,517 = 61,925 + 14,592` source disposition, `40,460` placements, and `7,784` fixed slots. All accepted manifests report zero mesh, material, base-color, matrix, scale, bounds, missing, unexpected, duplicate, or rejected mismatch. The report explicitly qualifies that the diagnostics-disabled release player does not emit a new full device-row dump; immutable package provenance, exact runtime load identity, exhaustive checked manifests, no Android-specific transform owner, and noncontradictory final device visuals supply the comparison. The accepted older materialized fingerprint predates bounded material substitutions and is used structurally only; the current database has a new hash and passes logical source parity while the final exhaustive manifest covers substituted data. Independent blue-canal and road-over-water defects remain separate. |

| 2026-08-11 | Parent final destruction-family acceptance | `Design/AgentReports/2026-08-11_dense_city_final_destruction_family_acceptance.json`; accepted VRP-067 House/Shop image pairs; fresh exact-package Contractor Tent intact/destroyed pair; retained same-package Barracks intact/destroyed pair; image SHA-256 verification; filtered Android fatal/ANR scan; fail-closed non-Unity validation; `git diff --check` | Parent advances to `146/149 (98%)`; child remains complete at `80/80`; three parent closeout rows remain | House and Shop retain their accepted complete generated-family state transitions. The exact final Samsung package separately proves an authored canonical Contractor Tent loses its complete canvas/interior target and becomes rubble, and a selected Barracks loses its complete intact target and becomes a ruined presentation. Paired visual review reconciled with the canonical complete-recipe/state-owner contract reports zero target-owned intact shell, roof prop, interior, sign, awning, or tent-content leakage. Independently owned neighboring tents, containers, vehicles, and the second barracks correctly remain and are not conflated with target leakage. The app was force-stopped after capture; no reinstall, performance-sample promotion, package mutation, protected asset change, blue-canal classification, or road-over-water classification occurred. |

| 2026-08-11 | Parent final deterministic evidence index | `Design/AgentReports/2026-08-11_dense_city_final_evidence_index.json`; direct read-only APK ZIP hashing; all referenced report/log/screenshot byte and SHA-256 checks; final performance threshold aggregation; tracker count; JSON parse; `git diff --check` | Parent advances to `147/149 (99%)`; child remains complete at `80/80`; two parent ownership/completion rows remain | The final evidence index records the exact source/package/device identity, final map-bundle and ContentArchive entry/aggregate fingerprints, deterministic database hashes/counts, transform/bounds counts, Android package/storage sizes, rollback bytes, all nine accepted route distributions, and exact report/log/screenshot hashes. Package-derived aggregation uses sorted `path|length|sha256` rows plus LF and independently reproduces five map bundles / `135,348,884` bytes with zero duplicate dependency and three ContentArchives / `118,765,607` bytes. Rejected samples are not promoted, protected/production assets are unchanged, and blue-canal versus road-over-water evidence remains separate. |

| 2026-08-11 | Parent final generated-artifact ownership closeout | `Tools/CI/dense_city_generated_artifact_ownership_audit.py --final-closeout`; seven focused Python tests; `Design/AgentReports/2026-08-10_dense_city_final_generated_artifact_ownership_audit.json`; exact tracked/ignored inventory; JSON parse; `git diff --check` | Parent advances to `148/149 (99%)`; child remains complete at `80/80`; only the final completion row remains | The opt-in final mode preserves foundation behavior while requiring eleven committed final tracker/report paths and five Android acceptance paths. The repository audit passes with both final booleans true, `4,803` tracked persistent-output files, `111` tracked dense evidence files including the staged final audit itself, zero missing ownership/final/Android paths, zero tracked transient or forbidden transaction artifacts, and zero failures across eight ignore probes. Build/log/cache/device artifacts remain excluded. The five preserved modified generated reports and fourteen untracked `_Recovery` scene/meta files remain untouched and outside this stable step. |

| 2026-08-11 | Parent and child final completion | Parent/child zero-open-checkbox audit; parent status/progress/phase reconciliation; exact package/performance/lifecycle/parity/destruction/evidence/ownership reports; device force-stop and restored-display record; `git diff --check` | Passed; parent complete at `149/149 (100%)`; child complete at `80/80`; no checklist row remains open | The complete dependency-ordered implementation and acceptance chain is closed without promoting rejected packages or route samples. Exact final Android package/device evidence passes the amended performance and two-minute thermal contract plus unchanged correctness gates. Final artifact ownership is committed and transient build/device evidence remains ignored. The preserved five modified generated reports and fourteen `_Recovery` scene/meta files remain outside the commit. Independent blue-canal/Autobahn isolation and road-over-water evidence remain separately classified and were not merged into this closeout. |

| 2026-08-21 | Dense candidate ECS debug-name capacity hardening | Transactional backfill `/private/tmp/warline-dense-debug-name-migration-path-safe.log`; direct virtualized bake `/private/tmp/warline-dense-debug-name-direct-bake-accepted.log`; production parity `/private/tmp/warline-dense-debug-name-production-parity-final.log`; readiness `31/31`; building/render-only replay `2/2` each; presentation budget `24/24`; assembly boundary `31/31`; zero-error Editor/test assembly builds | Passed regression hardening; parent remains complete at `149/149 (100%)`; no checklist row reopens | The user-reported first-Mission SubScene bake exhausted Unity Entities' global unique debug-name table. Generated stable owner roots now reuse bounded prefab/category debug names, while nested building attachments retain their accepted names because those names participate in renderer-relative virtualization keys. The accepted candidate records `36,304` normalized owners, `6,463` distinct direct names, and SHA-256 `52b30f...5b4f`. The direct bake emits no name-capacity or logical/source-stripping error, reconciles `447` resident plus `4,530` virtualized building presentations, and writes `76,517` baked render rows plus `7,784` fixed proxy slots (`84,301` manifest rows). Current production parity remains exact at `36,304/36,304` identities and `62,455/62,455` renderers with zero mesh, material, base-color, transform, bounds, or rejected-row mismatch. The retired pre-cutover two-pass pilot runner still rejects the completed production mode by design, and the broad ECS debt gate still reports its unrelated repository baseline of `26` array snapshots against a stale ceiling of `0`; this editor-only change adds no runtime system, snapshot, structural hot-loop change, managed steady-state allocation, production cutover, or Android claim. |

| 2026-08-23 | Mission 1 exact Demo material and grading parity | Source/database parity `/private/tmp/warline-m01-demo-source-parity.log`; exact material/volume assertion `/private/tmp/warline-m01-demo-material-validation.log`; mobile visual-quality contract `/private/tmp/warline-demo-visual-quality-contract-rerun.log` `22/22`; focused M01 reuse `/private/tmp/warline-m01-demo-reuse-contract.log` `8/8`; checked rendered production-route proof `/private/tmp/warline-demo-live-capture-accepted.log` and `/private/tmp/warline-demo-capture-accepted/current_gameplay_zoom.png` SHA-256 `05ecfddc...ea038f`; deterministic database report `Design/AgentReports/2026-07-28_dense_city_render_virtualization_database.json` | Passed regression hardening, checked-wrapper capture exit, and visual inspection; parent remains complete at `149/149 (100%)`; no checklist row reopens | Removed the rejected custom brown dirt-road substitution. All ten source road prefabs and ten generated physics-free definitions now use exact Demo material `PolygonMilitary_Mat_01_A`; all `18` packed `Dirt[...]` ECS parts carry that same material identity and white linear base color. Match baseline plus High/Ultra profiles use exact Demo `Military_Demo` volume ownership, and full Demo shadow strength is retained. The production route renders the authored bright sand/yellow dirt response with no gray or dark-brown multiplier. Environment-driven GUI captures now exit their wrapper-owned Editor after leaving Play Mode, while menu-triggered captures continue to leave the user's Editor open. Source disposition remains `76,517 = 61,925 + 14,592` with zero removed rows and two byte-identical database builds at content hash `ec96a4...93dac` and ordering hash `7df0db...a378`. Accepted source scene, SubScene, production Addressables ownership, runtime ownership, and Android package evidence remain unchanged. |

| 2026-08-23 | Mission 1 runtime lighting-parity correction | Focused virtualization `/private/tmp/warline-proxy-lighting-validation.log` `48/48`; runtime ownership `/private/tmp/warline-m01-operation-map-bootstrap-final.log` `15/15`; visual-quality contract `/private/tmp/warline-visual-quality-final-validation-2.log` `23/23`; checked M01 bake/capture `/private/tmp/warline-m01-probe-lighting-proof.log`; Mission 1 proof `/private/tmp/warline-m01-probe-lighting-proof/current_gameplay_zoom.png` SHA-256 `31d4b787...efd44`; exact Demo road reference `/private/tmp/warline-demo-road-reference/demo_dirt_road.png` SHA-256 `38bddf26...2849`; runtime road-component evidence `/private/tmp/warline-m01-probe-lighting-proof/current_gameplay_zoom_dirt_roads.txt`; scoped `git diff --check` | Corrects the earlier incomplete rendered-parity claim; parent remains complete at `149/149 (100%)`; no checklist row reopens | The reported gray Mission 1 road persisted for two independent reasons that source/material identity checks did not cover. The additive runtime left `Menu` active, so Menu RenderSettings overrode the loaded Match scene; Match now takes active-scene environment authority once its operation-map source is ready. Fixed virtualized slots also omitted Entities Graphics `WorldToLocal_Tag` and `BlendProbeTag`; the baker now emits both and has a bumped baking version. Fresh runtime evidence proves `SM_Env_DirtRoad_Straight_01` part `4383` uses exact material `PolygonMilitary_Mat_01_A`, white ECS base color, positive transform determinant, world-up normal, and both lighting tags. The checked Mission 1 render now shows the same pale sand road response as the exact Demo reference, with authored Demo fog, ambient colors, spherical-harmonic probe, directional lights, and `Military_Demo` volume restored. The broad M01 aggregate was rejected only by its existing Windows-style `FileShare` locking assertion on macOS (`InterruptedAtomicReplacePreservesPriorProfile` expected `IOException`); all mission suites before that unrelated portability assertion passed, and the directly affected ownership/rendering suites pass. No source scene, production Addressables definition, runtime structural hot loop, or Android package is changed. |

| 2026-08-23 | Mission 1 virtualized road-surface grounding parity | Bounds-filtered runtime-binding repair `/private/tmp/warline-road-overlay-repair-v10.log` (`3,434` overlays in each accepted/dense binding); final binding/bounds parity `/private/tmp/warline-runtime-binding-road-overlays-v10.log` `14/14`; surface bootstrap/revision publication `/private/tmp/warline-surface-bootstrap-overlay-v10.log` `9/9`; living/dead grounding, same-count refresh, and in-flight-job rebuild safety `/private/tmp/warline-surface-layered-grounding-v10.log` `22/22`; ECS contract `/private/tmp/warline-operation-map-ecs-contract-grounding-v10.log` `7/7`; checked Mission 1 capture `/private/tmp/warline-m01-grounding-proof-v10.log`; proof image `/private/tmp/warline-m01-grounding-proof-v10/current_gameplay_zoom.png` SHA-256 `e4cc3efc...9d237`; exact grounding diagnostics `/private/tmp/warline-m01-grounding-proof-v10/current_gameplay_zoom_grounding.txt` SHA-256 `fdc0b859...8a525` | Passed regression hardening and checked-wrapper live Mission 1 proof; parent remains complete at `149/149 (100%)`; no checklist row reopens and no blocker remains | The runtime surface blob described terrain below the raised virtualized road meshes, so living actors and retained corpses grounded beneath the visible road. Runtime binding generation now serializes deterministic road overlays derived from the exact packed render-database placement/part matrices and transformed renderer bounds, alongside the accepted authored bridge/ramp overlays. Mission 1's straight dirt-road renderer and overlay both resolve to top `y=0.418999`; all three on-road friendlies resolve to `y=0.518999` with their `0.1` infantry offset, and all three hostiles resolve to `y=0.418999`. The zero-health grounding regression proves retained bodies use the same overlay plus offset. Generation rejects authored and virtualized overlays wholly outside the active baked surface, and runtime binning independently rejects legacy out-of-bounds records. The Burst tracking system indexes the immutable overlays once in persistent 32-cell bins and queries only the local bin per unit, with an explicit unmanaged publication revision preventing stale same-count cache reuse; rebuilds complete any active tracking dependency before replacing persistent cache storage. This adds no managed steady-state hierarchy search, structural hot-loop change, or full-overlay per-unit scan. Normal runtime-binding rebuilds include the same virtualized overlays and fingerprint the render database in ledger schema `4`; future rebuilds cannot silently remove this correction. Accepted source scenes, accepted SubScene, production Addressables ownership, rollback ownership, and Android package evidence remain unchanged. |

| 2026-08-23 | Mission 1 reciprocal tutorial counterfire | Focused finale policy `/private/tmp/warline-m01-enemy-counterfire-finale-v2.log` `9/9`; production-equivalent force spawn and real finale-stage transition `/private/tmp/warline-m01-enemy-counterfire-spawn-v3.log` `6/6`; focused Burst/AOT architecture `/private/tmp/warline-m01-enemy-counterfire-burst-aot.log` `3/3`; scoped `git diff --check`; broad aggregate diagnostics `/private/tmp/warline-m01-enemy-counterfire-architecture.log`, `/private/tmp/warline-m01-enemy-counterfire-source-growth.log`, and `/private/tmp/warline-m01-enemy-counterfire-ecs-architecture.log` | Passed directly affected behavior, spawn, deferred-structural-change, and Burst/AOT gates; parent remains complete at `149/149 (100%)`; no checklist row reopens | When the tutorial finale camera reaches the four-versus-three firing line, the existing command buffer now removes combat suppression and assigns every live stationary hostile a deterministic commanded target from the live player squad. Mission 1 spawn policy raises only hostile attack range to a `60`-world-unit floor, sufficient for the authored formation; shared prefabs, player units, movement, damage, and other missions are unchanged. The three enemies retain `CampaignMissionStationaryUnitTag`, aim/fire in place, and enter normal auto-engagement on the next update. Existing finale protection still restores only player-faction health during stages `2-3`, so hostile fire is visible but cannot kill the tutorial squad; enemies remain mortal. The broad M01 aggregate again stops on the unrelated macOS `FileShare` portability assertion. Source-growth and ECS hot-path aggregates stop on existing unrelated repository baselines (eleven oversized/unreviewed production files and `26` array snapshots against stale ceiling `0`); none of this slice's production files appears in either violation inventory. No map content, accepted scene/SubScene, Addressables ownership, runtime map ownership, steady-state managed allocation, or Android package changed. |

| 2026-08-23 | Mission 1 High-tier shadow stability and exact Demo fog | Visual-quality contract `/private/tmp/warline-m01-shadow-fog-quality.log` `23/23`; operation-map runtime ownership `/private/tmp/warline-m01-shadow-fog-bootstrap.log` `15/15`; virtualization contract `/private/tmp/warline-m01-shadow-fog-virtualization-rerun.log` `48/48`; checked production-route capture `/private/tmp/warline-m01-shadow-fog-capture.log`; gameplay proof `/private/tmp/warline-m01-shadow-fog-proof/current_gameplay_zoom.png` SHA-256 `d34cdde2...bb40`; max-zoom proof `/private/tmp/warline-m01-shadow-fog-proof/current_max_zoom_out.png` SHA-256 `aaaf59b5...a49b`; exact live environment diagnostics `/private/tmp/warline-m01-shadow-fog-proof/current_gameplay_zoom_environment.txt`; scoped `git diff --check` | Editor regression step accepted; parent remains complete at `149/149 (100%)`; no checklist row reopens; user-triggered Android High-tier GPU/thermal recheck remains required before this post-completion quality tuning is release-accepted | The production High mobile pipeline no longer spreads one `512` hard-shadow cascade across the full `48 m` tactical range. It now uses a bounded `1024` atlas, two stable cascades split at `0.35`, medium soft-shadow filtering, and the accepted `0.05/0.35` depth/normal biases; Low and Medium remain unchanged. Match RenderSettings now copy the Demo scene's exact fog enablement, exponential mode, density, color, linear fallback range, ambient colors, and subtractive shadow color. The checked Mission 1 route confirms `fog=True`, density `0.002`, exact Demo fog/ambient values, both authored directional lights, and the exact global `Military_Demo` profile after additive loading and ECS presentation warmup. No day/night owner, runtime update loop, map source, SubScene, render database, Addressables owner, or Android package changes. The increased High-tier shadow GPU cost is deliberately not promoted as Android performance acceptance without the required user-triggered same-device frame/thermal evidence. |

| 2026-08-23 | Mission 1 High-tier overhead shadow-distance correction | Visual-quality contract `/private/tmp/warline-m01-shadow-distance-quality-rerun.log` `23/23`; checked production-route capture `/private/tmp/warline-m01-shadow-distance-capture.log`; gameplay proof `/private/tmp/warline-m01-shadow-distance-proof/current_gameplay_zoom.png` SHA-256 `208d123e...25be`; max-zoom proof `/private/tmp/warline-m01-shadow-distance-proof/current_max_zoom_out.png` SHA-256 `1a2380ab...3041`; exact live environment diagnostics `/private/tmp/warline-m01-shadow-distance-proof/current_max_zoom_out_environment.txt` SHA-256 `17a236d9...a78`; scoped `git diff --check` | Editor regression step accepted; parent remains complete at `149/149 (100%)`; no checklist row reopens; user-triggered Android High-tier GPU/thermal recheck remains required before this post-completion quality tuning is release-accepted | The production High mobile pipeline shadow distance increases from `48 m` to `90 m`, retaining directional shadows throughout the accepted overhead tactical framing instead of dropping them as the camera zooms out. The checked Mission 1 max-zoom frame visibly retains building, prop, and unit shadows across the rendered area while preserving the accepted `1024` atlas, two cascades at `0.35`, medium soft-shadow filtering, and exact Demo fog/environment authority. Low and Medium remain unchanged, and no runtime system, camera owner, map source, SubScene, render database, Addressables owner, or Android package changes. The increased High-tier shadow range is deliberately not promoted as Android performance acceptance without the required user-triggered same-device frame/thermal evidence. |

## 25. Completion Rule

This tracker is complete only when:

- all current existing map visual owners have accepted stable-identity migration dispositions with no mixed/unresolved row;
- existing military, handmade, building, vehicle, road, bridge, terrain, mountain, vegetation, prop, infrastructure, and horizon visuals bake to the protected map SubScene;
- the city regenerates under one disposable ownership set with exactly one accepted root in each required source scene;
- authored military, handmade, infrastructure, placement, and override behavior/transform identity remains semantically and visually preserved through migration;
- every permanent map visual's candidate authoring, baked ECS, and Addressables-loaded runtime matrix and transformed renderer bounds match the accepted Editor source manifest within the Section 12.0A tolerances, with no runtime placement system applying a second offset;
- production map content has zero colliders/Rigidbodies;
- ECS surface/blocker data is generated from simplified deterministic records/proxies;
- every damageable existing/generated building exists at runtime only as a canonical ECS simulation entity plus either resident intact/destroyed entity visuals or an accepted immutable virtualized recipe materialized through fixed ECS render slots;
- every building-attached roof/interior/shop/tent visual has one explicit building/state owner and transitions atomically with that building, leaving no intact or orphan prop after destruction;
- every other existing/generated visual exists at runtime only as either a resident Entities Graphics render entity or an accepted immutable virtualized logical row sharing source art through fixed Entities Graphics render slots;
- current-map production Addressables/build/runtime ownership contains zero static manifest/chunk scene entries unless a measured named exception was approved in this tracker;
- the 514-scene static package is frozen for rollback during migration and retired/deleted through the accepted cleanup only after parity acceptance;
- operation-map and SubScene source GameObject hierarchies are absent from the runtime package;
- Bake All is deterministic, transactional, no-op on a second run, and fail-closed;
- Addressables load/readiness/unload behavior passes twice consecutively with no map-visual camera streaming or static handles;
- Editor acceptance precedes Android acceptance;
- Android size, memory, frame, GC, visual, navigation, and lifecycle gates pass;
- final evidence and rollback instructions are recorded.

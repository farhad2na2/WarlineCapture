# Shared Brand Logo Lockup Prompt V01

Use the active `VisualLockLayered V15 3D Green-Screen Workflow`.

Purpose: create the single approved WarlineCapture brand logo layer for all 3D-direction screens.

Output asset:

- `shared_brand_logo_lockup_warline_capture_green.png`

Prompt:

Create a clean isolated WarlineCapture brand logo lockup on a flat pure `#00ff00` green background for chroma-key extraction. The lockup must match the approved target mockups, not the old SCN-02 logo layer.

Required logo content:

- Silver angular military shield/emblem at left.
- Exact readable title text: `WARLINE`.
- Exact readable subtitle text: `CAPTURE`.
- Gold tactical underline/chevron accent after `CAPTURE`.
- No extra words, no misspellings, no watermark, no panel, no frame, no background texture.

Style:

- AAA mobile military command UI.
- Metallic silver title with subtle dark bevel.
- Gold subtitle and accent.
- Sharp, compact, horizontal logo composition suitable for the top-left header area.

Workflow rules:

- Do not crop this logo from any target mockup.
- Do not reuse old logo layers from another screen.
- Do not include this logo in generic icon sheets.
- Approve this isolated source before adding it to any screen layer manifest.

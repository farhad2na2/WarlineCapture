# S002 library launch repair — 2026-09-23

S002 remains the fourth playable skirmish library entry. The launch now reads packaged S002 Resources content, including its authored definition and setup matrix, instead of a repository-only `Design` CSV. Its Desert Base deployment and shared S003/S004 layout anchors are within the loaded map's world bounds.

Attempt-owned combatants now clone the baked ECS unit registry and use the shared render, selection, animation, movement, fuel, and engagement systems. Spawn placement checks the walkable grid and occupancy; attack orders use shared path following and combat. The expanded stock ledger supplies vehicle fuel and the HUD. The S002 HUD shows the 18-minute deadline, 48-infantry cap, and the scenario's materials, oil, and fuel. Opening camera focus retries until the base visual and camera request are ready.

The command rail keeps Attack and Hold colored and clickable with no selection. Hold retains its existing rejection feedback, and Attack now reports “Select units before attacking.”

Validation through `Tools/CI/invoke_unity_macos.sh` on disposable project copies:

- `[SkirmishExpandedDefinitionTests] result=Passed` — `/private/tmp/warline-s002-definitions-final.log`
- `[SkirmishS002AriaHarnessTests] result=Passed` — `/private/tmp/warline-s002-harness-final2.log`
- `[SkirmishS002GameView] result=Passed` — `/private/tmp/warline-s002-capture-final-20260923.log`; the captured playable frame is `_Evidence/s002-regular-standard-104731-playing-repaired.png`.
- ARIA, Regular/Standard, seed 130365, English: Victory by `MainBaseDestroyed` at 132.155 seconds. The harness recorded `appended=1 countedWin=1`; see `runs.csv` and `_Evidence/s002-aria-rs-130365-en-3.jsonl`.

The Unity Editor playthrough and focused validation establish a working S002 mission and an ARIA win. A standalone player build without the `Design` directory was not run in this repair.
